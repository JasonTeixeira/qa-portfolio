import { ShowcaseIndex } from './showcase-index'

export const metadata = {
  title: 'Interactive Business Prototype Warehouse | Sage Ideas',
  description: 'Open working AI systems, quote engines, intake flows, support dashboards, and conversion prototypes before you book a build call.',
}

export default function ShowcasePage() {
  return <ShowcaseIndex />
}
