import { Proto } from './Proto'

export const metadata = {
  title: 'Sage Ideas — design prototype',
  robots: { index: false, follow: false },
}

export default function ProtoPage() {
  return <Proto />
}
