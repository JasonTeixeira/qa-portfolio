{
  "backend-request-response": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint (or a realistic one) and write the request/response contract: the typed request shape, the validation and auth boundary it must pass, the exactly-once persistence rule, and the response envelope for success AND failure. Build it as a reviewable artifact, not a note.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the request schema, the validation + auth boundary, an idempotency key + transaction boundary, the success/error response envelope, and one named failure (duplicate submit) with its repair. A reviewer can replay the duplicate request and see the second call return the first result, not a second write.",
      "unlock": "Your contract validates input before it touches state, authorizes before it acts, persists exactly once under retry, and returns a typed envelope for both success and failure.",
      "doNotClaim": "Do not claim you 'handle the request' if your handler writes to the database before validation and auth pass, or if a retried POST creates a second row. A handler that is correct only on the happy path is a demo, not a contract. The contract is the behavior under bad input, wrong caller, and retry."
    },
    {
      "type": "mission",
      "text": "A customer taps 'Place order' on a flaky connection, the spinner hangs, they tap again — and the warehouse ships two. The handler did exactly what the code said on each call; nobody wrote down what it must do when the same request arrives twice."
    },
    {
      "type": "context",
      "text": "Every backend endpoint is a promise: given this request, under this caller, I will change state exactly this much and return exactly this shape — even when the input is wrong, the caller is not allowed, or the network retries. This lesson turns that promise into the first artifact every later module (queues, jobs, observability, compatibility) builds on."
    },
    {
      "type": "pretest",
      "prompt": "Your POST /orders handler works perfectly in every test. A client sends the same request twice because the first response timed out. What does your endpoint do — and is that a bug in the client or in you? Answer before reading on.",
      "reveal": "It is your bug. A timeout means the client never learned whether the write happened, so a retry is correct behavior — the contract has to absorb it. A handler that creates a second order on the second call is missing idempotency: a key that lets the server recognize 'I already did this' and return the FIRST result. The request/response contract is not 'parse and write'; it is what happens on the second, wrong, or unauthorized copy of the same request."
    },
    {
      "type": "concept",
      "title": "A contract is validate → authorize → act-once → respond — a fixed order, never just 'parse and write'",
      "text": "Validate the request shape before it touches state, authorize the caller before it acts, persist exactly once under retry (idempotency key + one transaction), and return a typed envelope for success AND failure. The invariant: state changes only after the gates pass, and only once."
    },
    {
      "type": "diagram",
      "title": "The request/response contract as a gated flow",
      "subtitle": "A POST travels client → gateway → handler. Each gate can reject early; only after validation, auth, and the idempotency check does it open one transaction. The failure path (duplicate submit) returns the first result instead of writing twice.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "may retry on timeout",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "TLS, routing, rate limit",
          "kind": "service"
        },
        {
          "id": "validate",
          "label": "Validate",
          "description": "schema check → 400 on bad shape",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "authorize",
          "label": "Authorize",
          "description": "auth boundary → 401/403",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "idem",
          "label": "Idempotency check",
          "description": "seen this key before?",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "txn",
          "label": "One transaction",
          "description": "write order + key, commit or rollback",
          "kind": "process",
          "tone": "success"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "source of truth",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "response",
          "label": "Response envelope",
          "description": "typed success OR error",
          "kind": "process"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "POST /orders",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "validate",
          "label": "route",
          "kind": "sync"
        },
        {
          "from": "validate",
          "to": "authorize",
          "label": "shape ok",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "authorize",
          "to": "idem",
          "label": "caller ok",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "idem",
          "to": "txn",
          "label": "new key → act",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "idem",
          "to": "response",
          "label": "seen key → replay first result",
          "kind": "data",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "txn",
          "to": "db",
          "label": "commit (or rollback)",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "txn",
          "to": "response",
          "label": "201 + body",
          "kind": "data",
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "gates that reject before any write"
        },
        {
          "tone": "warning",
          "label": "idempotency — the retry path (no second write)"
        },
        {
          "tone": "success",
          "label": "the single transaction + source of truth"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Writing the handler, one boundary at a time",
      "subtitle": "POST /orders in the order the contract demands: validate, authorize, act-once, respond — the same arc as the diagram, in real code.",
      "filename": "create_order.ts",
      "language": "ts",
      "code": "// POST /orders — state-changing handler with an exactly-once contract\nexport async function createOrder(req: Request): Promise<Response> {\n  const parsed = OrderSchema.safeParse(await req.json())\n  if (!parsed.success) {\n    return json(400, { ok: false, error: 'INVALID_BODY', detail: parsed.error.issues })\n  }\n\n  const caller = await authorize(req)               // verify token + ownership\n  if (!caller) return json(401, { ok: false, error: 'UNAUTHENTICATED' })\n  if (caller.id !== parsed.data.customerId) {\n    return json(403, { ok: false, error: 'FORBIDDEN' })\n  }\n\n  const key = req.headers.get('Idempotency-Key')\n  if (!key) return json(400, { ok: false, error: 'IDEMPOTENCY_KEY_REQUIRED' })\n\n  return db.transaction(async (tx) => {\n    const prior = await tx.idempotency.find(key)\n    if (prior) return json(200, prior.response)     // replay the FIRST result\n    const order = await tx.orders.insert(parsed.data)\n    const body = { ok: true, data: { orderId: order.id } }\n    await tx.idempotency.save(key, body)            // same txn → commit together\n    return json(201, body)\n  })                                                 // any throw → full rollback\n}",
      "steps": [
        {
          "lines": [
            3,
            4,
            5,
            6
          ],
          "label": "Validate before state",
          "note": "Parse against a schema first. A bad shape returns 400 and never reaches the database — the request never gets to touch state until it is well-formed."
        },
        {
          "lines": [
            8,
            9,
            10,
            11,
            12
          ],
          "label": "Authorize the caller",
          "note": "Authentication (who are you) then authorization (may you act on THIS resource). 401 vs 403 is the auth boundary: missing identity vs forbidden action. This is OWASP API1 — broken object-level authorization — caught at the door."
        },
        {
          "lines": [
            14,
            15
          ],
          "label": "Require the idempotency key",
          "note": "A state-changing request with no idempotency key cannot be made safe under retry. Demand it; reject with 400 if absent."
        },
        {
          "lines": [
            17,
            18,
            19
          ],
          "label": "The exactly-once check",
          "note": "Inside the transaction, first look up the key. If it was already processed, replay the FIRST response — no second write. This is the line that absorbs the double-tap."
        },
        {
          "lines": [
            20,
            21,
            22
          ],
          "label": "One transaction, write + key together",
          "note": "Insert the order AND save the idempotency record in the SAME transaction so they commit or roll back as a unit. Saving the key outside the txn would let a crash double-write."
        },
        {
          "lines": [
            23,
            24
          ],
          "label": "Typed envelope, success and rollback",
          "note": "201 with a typed body on success; any throw rolls the whole transaction back, leaving no partial order. The response shape is the contract clients code against."
        }
      ],
      "caption": "Replay the proof: send the same body twice with one Idempotency-Key — the first returns 201 and writes one row, the second returns 200 with the identical body and writes nothing."
    },
    {
      "type": "compare",
      "title": "Two handlers, same endpoint",
      "subtitle": "POST /orders. Both compile and pass the happy-path test. Only one survives bad input, a wrong caller, and a retry.",
      "left": {
        "label": "Parse-and-write",
        "tone": "warning",
        "lines": [
          "Reads body, inserts the order immediately",
          "No schema validation — bad fields reach the DB",
          "Trusts the customerId in the body, no auth check",
          "No idempotency key — retry = a second order",
          "Returns 500 with a raw stack trace on any error"
        ],
        "verdict": "Ships duplicate orders and leaks internals"
      },
      "right": {
        "label": "Validate → authorize → act-once → respond",
        "tone": "success",
        "lines": [
          "Schema-validates before touching state (400 on bad shape)",
          "Authenticates, then authorizes object ownership (401/403)",
          "Idempotency key + single transaction = exactly once",
          "Retry replays the first result; no second write",
          "Typed envelope: { ok, data } or { ok, error } — never a stack trace"
        ],
        "verdict": "Correct under bad input, wrong caller, and retry"
      },
      "caption": "The gold version is not 'more code' — it is the same work in a fixed order, with the write fenced behind validation, auth, and an idempotency check."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "Grounded in OWASP API Security Top 10: the #1 risk is broken object-level authorization — a handler that authenticates ('you have a valid token') but never authorizes ('this token may act on THIS order'). The senior tell is the line `caller.id !== body.customerId → 403`: identity is not permission, and the contract checks both before it writes anything."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks for the exact evidence that your endpoint is exactly-once, not just happy-path. What do you point to?",
      "options": [
        "The unit test showing one valid request creates one order.",
        "A replay test: the same request with the same Idempotency-Key sent twice — the second returns the first result with zero new rows — plus the rollback test showing a mid-transaction failure leaves no partial order.",
        "The fact that the handler compiles and the schema type-checks.",
        "A comment in the code that says // idempotent."
      ],
      "answer": 1,
      "explanation": "Exactly-once is a behavior under retry and failure, so the proof has to exercise retry and failure: a duplicate-submit replay that writes nothing the second time, and a rollback test proving a crash mid-transaction leaves no partial state. The happy-path test (1), a clean compile (3), and a comment (4) all only confirm the path that was never in question."
    },
    {
      "type": "verification",
      "intro": "Prove your contract holds — replay it, don't trust it:",
      "items": [
        "Send a malformed body. Confirm it returns 400 and that NOTHING was written — the validation gate fired before any state change.",
        "Send a valid body with a token for a different customer. Confirm 403 (forbidden), not 200 — authentication passed but authorization correctly refused.",
        "Send the same valid request twice with one Idempotency-Key. Confirm the first returns 201 + one row, the second returns the identical body + zero new rows.",
        "Force a failure between the order insert and the key save. Confirm the transaction rolled back completely — no order, no key, no partial state.",
        "Read your error responses. Confirm every failure returns the typed envelope { ok: false, error } with a stable code — and never a raw stack trace or internal field name."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why must validation and authorization run BEFORE the write, not after — what does a single out-of-order write cost you?",
        "Explain idempotency to a junior using your own endpoint: what is the key, where is it checked, and why must it live in the same transaction as the write?",
        "Walk the failure path of your contract out loud: a duplicate submit arrives — name every gate it passes and the exact response it gets, with no second order created."
      ]
    },
    {
      "type": "transfer",
      "text": "This is not a /orders move — it is THE backend move. A Stripe webhook receiver replays events and must process each one exactly once (same idempotency key, same transaction boundary). A billing endpoint validates, authorizes the account owner, and charges once under retry. A queue worker dedupes by message ID; an internal API authorizes the calling service, not just a user. What stays invariant is the order — validate → authorize → act-once → respond. What changes is the key source (header, event ID, message ID) and the blast radius of getting it wrong: a double-charge hurts more than a double-read. The api_contract_and_failure_repair_packet.md you just built is your first portfolio evidence — not 'I understand backend requests' but 'here is an endpoint I made correct under bad input, a wrong caller, and a retry — replay it yourself.' Carry it into the capstone unchanged in structure."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "resource-modeling": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint (real or realistic — e.g. POST /orders) and model the resource: name the resource, its fields and invariants, the request/response contract, and the validation + authorization boundary every request must cross before it touches the database.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "Section 1 of api_contract_and_failure_repair_packet.md: a named resource with typed fields, a request schema, a response schema, and the explicit validation + auth gate. A reviewer reads it and can send a malformed request and predict the exact rejection — status code, error shape, and which boundary caught it — without running your code.",
      "unlock": "Your model names the resource, its invariants, the typed request/response contract, and the validation + auth boundary that guards the write path.",
      "doNotClaim": "Do not claim you have 'modeled the resource' if your model is just a database table. A table is storage; a resource is the contract the outside world sees plus the invariants you defend. If the only thing you wrote down is columns, you skipped the boundary that actually protects the system."
    },
    {
      "type": "mission",
      "text": "A POST /orders endpoint ships with no model — just a handler that trusts its input and inserts. Friday, a client double-clicks 'Pay', two identical requests race, and the database now holds two orders for one cart. Nobody modeled the resource, so nobody owned the invariant that should have made that impossible."
    },
    {
      "type": "context",
      "text": "Every backend endpoint is a promise: this resource, these fields, these rules, enforced here. Resource modeling is where that promise is written down — Section 1 of the contract packet you carry through validation, authorization, persistence, and idempotency."
    },
    {
      "type": "pretest",
      "prompt": "Your handler does `db.orders.insert(request.body)` — it takes the JSON the client sent and writes it straight to the table. Before you optimize anything, what is wrong with that as a resource model? Answer before reading on.",
      "reveal": "There is no model — there is a passthrough. The client, not you, decides the shape, and the client is untrusted external input. Nothing checks that `total` matches the line items, nothing rejects an extra `isAdmin: true` field, nothing confirms this user may create an order at all. A resource model is the layer that turns 'whatever the client sent' into 'a valid Order, owned by this caller, that satisfies my invariants' — and its absence is invisible until production sends you the request you never imagined."
    },
    {
      "type": "concept",
      "title": "A resource is a contract + invariants + a boundary — not a table",
      "text": "Name the resource and its typed fields, state the invariants that must always hold, define the request/response schemas the outside sees, and place the validation + authorization boundary the write must cross. The invariant: untrusted input becomes a valid, owned resource before it touches the store."
    },
    {
      "type": "diagram",
      "title": "The request crossing the resource boundary",
      "subtitle": "Every state-changing request runs the same gauntlet: parse → validate the contract → check authorization → enforce invariants → persist. The validation and auth gates are the suspect path; an unvalidated write is the failure mode.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "untrusted JSON",
          "kind": "client",
          "tone": "muted"
        },
        {
          "id": "handler",
          "label": "API Handler",
          "description": "POST /orders",
          "kind": "service"
        },
        {
          "id": "validate",
          "label": "Validate schema",
          "description": "shape · types · field whitelist",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "authz",
          "label": "Authorize",
          "description": "may this caller create this?",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "invariant",
          "label": "Enforce invariants",
          "description": "total == sum(items); status valid",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "source of truth",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "reject",
          "label": "Reject",
          "description": "422 / 403 — typed error",
          "kind": "external",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "handler",
          "label": "request",
          "kind": "sync"
        },
        {
          "from": "handler",
          "to": "validate",
          "label": "parse body",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "validate",
          "to": "authz",
          "label": "on valid",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "authz",
          "to": "invariant",
          "label": "on allowed",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "invariant",
          "to": "db",
          "label": "write owned resource",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "validate",
          "to": "reject",
          "label": "bad shape",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "authz",
          "to": "reject",
          "label": "not allowed",
          "kind": "async",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the resource boundary — validation + auth gates"
        },
        {
          "tone": "warning",
          "label": "invariant enforcement + typed rejection"
        },
        {
          "tone": "success",
          "label": "source of truth — reached only by a valid, owned write"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Modeling the Order resource, one declaration at a time",
      "subtitle": "The contract as code — schema, invariant, ownership, response. Each line moves a rule out of the handler's head and onto the boundary.",
      "filename": "order_resource.ts",
      "language": "ts",
      "code": "import { z } from 'zod'\n\n// Request contract: the ONLY shape the outside world may send.\n// Zod 4: z.strictObject() is the idiomatic whitelist (.strict() still works but is deprecated).\nexport const CreateOrderRequest = z.strictObject({\n  cartId: z.string().uuid(),\n  items: z.array(z.object({\n    sku: z.string().min(1),\n    qty: z.number().int().positive(),\n    unitPrice: z.number().int().nonnegative(), // cents\n  })).min(1),\n  total: z.number().int().nonnegative(),       // cents, client-asserted — reject unknown fields, no isAdmin smuggling\n})\n\n// Invariant the resource must satisfy before it persists.\nexport function assertOrderInvariant(req: CreateOrderRequest): void {\n  const sum = req.items.reduce((n, i) => n + i.qty * i.unitPrice, 0)\n  if (sum !== req.total) throw new ContractError('total != sum(items)')\n}\n\n// Response contract: what the resource looks like coming back.\nexport const OrderResponse = z.object({\n  id: z.string().uuid(),\n  status: z.enum(['pending', 'paid', 'cancelled']),\n  total: z.number().int().nonnegative(),\n  ownerId: z.string().uuid(),\n  createdAt: z.iso.datetime(), // Zod 4: ISO datetime lives under z.iso\n})",
      "steps": [
        {
          "lines": [
            5,
            13
          ],
          "label": "The request schema",
          "note": "The resource's inbound shape, typed. The client may send these fields and only these — this is the contract, not a suggestion."
        },
        {
          "lines": [
            5
          ],
          "label": "z.strictObject() — the field whitelist",
          "note": "Reject unknown fields. Without this, a client can append isAdmin: true or ownerId: <someone-else> and you'd silently persist it. In Zod 4 the idiomatic form is z.strictObject({...}); the older z.object({...}).strict() still works but is deprecated. Either way, this closes a mass-assignment hole (OWASP API3)."
        },
        {
          "lines": [
            16,
            19
          ],
          "label": "The invariant",
          "note": "A resource is more than its types: total must equal the sum of line items. Types can't express this; the invariant function can. A test that can only confirm the happy path never catches a tampered total — this can."
        },
        {
          "lines": [
            22,
            28
          ],
          "label": "The response contract",
          "note": "What the resource looks like coming back — including ownerId, set by the server, never by the client. The outbound shape is part of the model, not an afterthought. (Zod 4: the ISO timestamp uses z.iso.datetime().)"
        }
      ],
      "caption": "A reviewer should read these declarations and send a request with a mismatched total or an extra field — and predict the exact rejection before running anything."
    },
    {
      "type": "compare",
      "title": "Same endpoint, two resource models",
      "subtitle": "Both accept POST /orders and write to the same table. Only one survives a malicious or careless client.",
      "left": {
        "label": "Table-as-resource (passthrough)",
        "tone": "warning",
        "lines": [
          "db.orders.insert(request.body)",
          "Client decides the shape — untrusted input trusted",
          "No field whitelist: isAdmin / ownerId smuggle in",
          "total taken on faith; no invariant",
          "No auth check: any caller writes any order",
          "Response leaks raw row, internal columns and all"
        ],
        "verdict": "Ships a mass-assignment + IDOR hole"
      },
      "right": {
        "label": "Modeled resource (contract + boundary)",
        "tone": "success",
        "lines": [
          "Parse → strict schema (z.strictObject) rejects unknown fields",
          "assertOrderInvariant: total == sum(items)",
          "Authorize: caller may create for THIS cart",
          "ownerId set by server from the session, never the body",
          "Persist only a valid, owned resource",
          "Response is a typed projection — no internal leakage"
        ],
        "verdict": "A malformed request fails predictably at the boundary"
      },
      "caption": "The model is the difference between 'whatever the client sent' and 'a valid Order owned by this caller'. The boundary is the whole skill — the table was never the resource."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell is that the server owns identity and ownership, never the client. ownerId, id, status transitions, and timestamps are set server-side from the authenticated session — never read from the request body. The moment a field that decides 'who owns this' or 'what may this become' comes from untrusted input, you have an IDOR or a broken-object-authorization bug (OWASP API1/API3) waiting for the request you didn't imagine."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: point to the exact evidence that proves your resource model is safe, not just a happy-path answer. Which response actually proves it?",
      "options": [
        "\"The endpoint returns 201 and the order appears in the database when I send a valid request.\"",
        "\"Here is a contract test rejecting an unknown field, an integrity test where a tampered total returns 422, and an authorization test where caller B is denied creating an order on caller A's cart with 403.\"",
        "\"The schema is written in TypeScript so the types guarantee correctness.\"",
        "\"I added logging to the handler so we can see requests in production.\""
      ],
      "answer": 1,
      "explanation": "Proof of a resource model is failing input handled correctly, not passing input passing. Option 2 shows the boundary working under attack: an unknown field rejected (mass-assignment closed), a tampered invariant caught (422), and a cross-owner write denied (403). The happy-path 201 (1) proves nothing about the failure modes; types (3) can't express the total invariant or ownership; logging (4) observes the breach, it doesn't prevent it."
    },
    {
      "type": "verification",
      "intro": "Prove your resource model holds — under bad input, not good:",
      "items": [
        "Contract test: send a request with an extra field the schema doesn't list. It must be rejected (422), not silently dropped or persisted — that proves the strict schema (z.strictObject) closed mass-assignment.",
        "Integrity test: send items whose quantities×prices do not equal total. The invariant must reject it before any write reaches the store.",
        "Authorization test: as caller B, attempt to create a resource owned by caller A. It must return 403, and no row may be written — proves the auth boundary, not just the schema.",
        "Idempotency replay: send the same create request twice with the same idempotency key. The second must return the first order, not create a duplicate — proves the exactly-once invariant the mission's double-click broke.",
        "Response audit: confirm the response is a typed projection (OrderResponse), exposing no internal columns and no other caller's data — the outbound contract is enforced too."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why is a database table NOT a resource model, and what does the model add that the table cannot?",
        "Explain why ownerId and status must be set by the server from the session, never read from the request body — using your own endpoint as the example.",
        "Name one invariant on your resource that the type system cannot express, and the exact test that proves you enforce it."
      ]
    },
    {
      "type": "transfer",
      "text": "The same model — typed contract, server-owned identity, an enforced invariant, a guarded boundary — is how every state-changing surface stays safe. A webhook validates the signature and rejects unknown event types before acting; a billing endpoint sets the customer from the authenticated key, never the body, and enforces 'amount > 0'; a queue worker treats its message as untrusted input and replays idempotently; an AI service backend caps and validates the prompt payload before spending tokens. What stays invariant: untrusted input becomes a valid, owned resource before it touches the store. What changes is the field set, the failure you fear, and the cost of getting it wrong. This is Section 1 of api_contract_and_failure_repair_packet.md — the capstone reuses this exact model and its proofs to break the weak version and ship the repair."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "api-contracts-schemas": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint and write its API contract as an enforced schema: the request shape, the response shape, the validation that rejects bad input at the boundary, and the exact error envelope a caller gets back. Then prove the contract rejects what it should and an idempotency replay does not double-apply.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "Section 1 of api_contract_and_failure_repair_packet.md: a named request/response schema (Zod or equivalent), a contract test that PASSES on valid input and FAILS-with-422 on malformed input, and an idempotency-replay test showing the same key applied twice yields one state change. A reviewer reads the schema and can call your endpoint correctly without reading the handler.",
      "unlock": "Your contract is a schema validated at the boundary — not prose — and a test proves it rejects a malformed request with a typed error, not a 500.",
      "doNotClaim": "Do not claim you have an API contract if validation lives inside business logic three calls deep. A contract that is only enforced after the handler has already touched the database is not a boundary — it is a hope. The schema must reject at the door, before any side effect."
    },
    {
      "type": "mission",
      "text": "A partner integration goes live, and within an hour the orders table fills with rows where quantity is the string \"two\" and currency is null — because the endpoint trusted whatever JSON arrived. The contract was a paragraph in a wiki, not a schema at the door, so every bad request became a corrupted row instead of a clean 422."
    },
    {
      "type": "context",
      "text": "An API contract is the promise a service makes about what it accepts and what it returns; the schema is that promise made executable, checked on every request. It sits at the foundation of Backend Engineering because every later concern — authorization, persistence, queues, observability, and not breaking callers on the next deploy — assumes the request was already validated and the response shape is stable."
    },
    {
      "type": "pretest",
      "prompt": "Your handler starts with `const { amount, currency } = req.body` and goes straight to charging the card. A request arrives with amount as the string \"100\" and no currency. What actually happens, and where? Answer before reading on.",
      "reveal": "It does not fail at the door — it fails deep, as garbage. \"100\" * 1 coerces to 100 in some paths and NaN in others; missing currency becomes undefined, which your payment client may serialize as the literal \"undefined\" or default to USD. The bad request became a real charge in the wrong shape, discovered later in a reconciliation report. A schema at the boundary turns all of that into one deterministic 422 BEFORE any side effect — the request never reaches the card. Validation that lives in the handler validates too late to matter."
    },
    {
      "type": "concept",
      "title": "A contract is a schema enforced at the boundary, returning a typed error envelope",
      "text": "Define request and response shapes as a schema; parse every request against it at the edge, before any side effect. Invalid input returns a typed 4xx envelope, never a 500. The invariant: no handler logic runs on unvalidated data."
    },
    {
      "type": "diagram",
      "title": "Request lifecycle: where the contract stands guard",
      "subtitle": "A POST /payments request meets the schema gate FIRST. Invalid input fails fast as a 422 envelope; only validated, idempotency-checked requests reach the handler and the database. The failure path and the repair path are both drawn.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "sends POST /payments JSON",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "edge routing + authn",
          "kind": "service"
        },
        {
          "id": "schema",
          "label": "Schema gate",
          "description": "parse request vs contract",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "reject",
          "label": "422 envelope",
          "description": "typed field errors, no side effect",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "idem",
          "label": "Idempotency check",
          "description": "seen this key before?",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "handler",
          "label": "Handler",
          "description": "business logic on clean data",
          "kind": "service",
          "tone": "success"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "one charge, in a transaction",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "response",
          "label": "Response schema",
          "description": "stable output shape",
          "kind": "process",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "request",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "schema",
          "label": "raw body",
          "kind": "sync"
        },
        {
          "from": "schema",
          "to": "reject",
          "label": "on invalid",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "reject",
          "to": "client",
          "label": "422 (repair: fix shape, retry)",
          "kind": "sync",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "schema",
          "to": "idem",
          "label": "on valid",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "idem",
          "to": "handler",
          "label": "new key",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "idem",
          "to": "response",
          "label": "replay: return stored result",
          "kind": "data",
          "dashed": true,
          "tone": "muted"
        },
        {
          "from": "handler",
          "to": "db",
          "label": "writes (txn)",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "handler",
          "to": "response",
          "label": "maps to output",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "response",
          "to": "client",
          "label": "200 typed body",
          "kind": "sync",
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the contract gates: schema parse + idempotency"
        },
        {
          "tone": "warning",
          "label": "failure path: typed 422, zero side effect"
        },
        {
          "tone": "success",
          "label": "validated data only — handler, txn, response"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Writing the contract, one line at a time",
      "subtitle": "A schema-first payments endpoint: parse at the boundary, reject as a typed envelope, apply exactly once.",
      "filename": "create_payment.ts",
      "language": "ts",
      "code": "import { z } from 'zod'\n\nexport const CreatePaymentRequest = z.object({\n  amountCents: z.number().int().positive(),\n  currency: z.enum(['USD', 'EUR', 'GBP']),\n  idempotencyKey: z.string().uuid(),\n})\n\nexport const PaymentResponse = z.object({\n  id: z.string().uuid(),\n  status: z.enum(['succeeded', 'pending']),\n  amountCents: z.number().int(),\n})\n\nexport async function createPayment(rawBody: unknown, db: Db) {\n  const parsed = CreatePaymentRequest.safeParse(rawBody)\n  if (!parsed.success) {\n    return { status: 422, body: { error: 'invalid_request', fields: parsed.error.flatten().fieldErrors } }\n  }\n  const req = parsed.data\n\n  const existing = await db.findPaymentByKey(req.idempotencyKey)\n  if (existing) return { status: 200, body: PaymentResponse.parse(existing) }\n\n  const payment = await db.tx(async (t) => {\n    return t.insertPayment({ ...req, status: 'succeeded' })\n  })\n  return { status: 201, body: PaymentResponse.parse(payment) }\n}",
      "steps": [
        {
          "lines": [
            3,
            4,
            5,
            6,
            7
          ],
          "label": "The request contract",
          "note": "amountCents is a positive integer — not a string, not a float of dollars. currency is a closed enum, so \"usd\" or null is rejected by the schema, not by a downstream surprise. This object IS the promise to callers."
        },
        {
          "lines": [
            9,
            10,
            11,
            12,
            13
          ],
          "label": "The response contract",
          "note": "The output shape is named too. Parsing the response on the way out means you cannot accidentally leak an internal field or change the shape and break callers silently."
        },
        {
          "lines": [
            16,
            17,
            18,
            19
          ],
          "label": "Parse at the boundary, FIRST",
          "note": "safeParse runs before any database call or side effect. On failure you return a typed 422 envelope with per-field errors — never a 500, never a half-applied write. This is the gate the whole lesson is about."
        },
        {
          "lines": [
            22,
            23
          ],
          "label": "Idempotency check",
          "note": "Look up the request's idempotency key before doing work. If this exact request was already applied, return the stored result. This is what makes a retry safe — the network can replay, the charge cannot double."
        },
        {
          "lines": [
            25,
            26,
            27
          ],
          "label": "Apply exactly once, in a transaction",
          "note": "Only validated, not-yet-seen requests reach the write — and the write is wrapped in a transaction so a mid-flight crash rolls back cleanly rather than leaving a partial charge. Validated data, one effect, atomic."
        }
      ],
      "caption": "A reviewer reads CreatePaymentRequest and knows exactly how to call you — and a malformed body never reaches line 25."
    },
    {
      "type": "compare",
      "title": "Trusting the body vs enforcing the contract",
      "subtitle": "Same endpoint: POST /payments. Both run. Only one is a contract.",
      "left": {
        "label": "Trust-the-body handler",
        "tone": "warning",
        "lines": [
          "const { amountCents, currency } = req.body",
          "No schema — shape is whatever arrived",
          "\"100\" coerces; missing currency = undefined",
          "Bad input reaches the DB as a corrupt row",
          "Errors surface as 500s with stack traces",
          "Retries double-charge — no idempotency key"
        ],
        "verdict": "Every bad request becomes a bad write"
      },
      "right": {
        "label": "Schema-enforced contract",
        "tone": "success",
        "lines": [
          "CreatePaymentRequest.safeParse(rawBody) FIRST",
          "amountCents: positive int · currency: enum",
          "Invalid input → typed 422, zero side effect",
          "Only validated data reaches the handler",
          "Response shape parsed on the way out too",
          "idempotencyKey → same request applies once"
        ],
        "verdict": "Bad input dies at the door as a 422"
      },
      "caption": "The contract is not the prose in the docs — it is the safeParse on line one. If validation runs after the first side effect, you have a hope, not a boundary."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell is the error envelope, not the happy path. Anyone can return 200 on good input; a contract you can trust returns a typed, machine-readable 422 with per-field errors on bad input — every time, in the same shape. This is exactly what OWASP API Security flags as the difference between a hardened endpoint and one waiting to be exploited: unvalidated input and leaked 500 stack traces are top-10 API risks, not style nits."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: \"Show me the exact evidence that this is a real contract and not just happy-path code.\" Which artifact proves it?",
      "options": [
        "A passing test that POSTs a valid body and asserts a 201.",
        "A contract test that asserts a malformed body returns 422 with typed field errors (no 500), plus an idempotency-replay test showing the same key applied twice yields one row.",
        "A README section describing the request and response fields in prose.",
        "Manual confirmation in Postman that the endpoint returned 200 once."
      ],
      "answer": 1,
      "explanation": "A real contract is proven by what it REJECTS and how it behaves under replay, not by a single happy path. Option 2 shows the boundary rejecting bad input as a typed 422 (not a leaked 500) and the idempotency key preventing a double-apply on retry — the two failure-path proofs the reviewer challenge demands. The happy-path test (1), prose docs (3), and one manual 200 (4) never exercise the failure path the contract exists to govern."
    },
    {
      "type": "verification",
      "intro": "Prove the contract holds — no vibes:",
      "items": [
        "Run the contract test on a malformed request (wrong type, missing field, out-of-enum currency). It must return a typed 4xx envelope with per-field errors — never a 500, never a partial write.",
        "Confirm the schema parse runs BEFORE any side effect: trace the handler and verify no database call or external request precedes safeParse.",
        "Run the idempotency-replay test: send the same idempotency key twice and assert exactly one row was written and both responses match.",
        "Verify the response is parsed against its own schema on the way out, so a shape change cannot silently break callers.",
        "Confirm a rollback case: simulate a crash mid-write and assert the transaction left no partial state behind."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does validation that lives inside the handler validate too late to protect the database?",
        "Explain the difference between a contract (the prose promise) and a schema (the enforced promise), using your own endpoint as the example.",
        "Walk through what happens on a retry with the same idempotency key — and why that makes a state-changing endpoint safe to call twice."
      ]
    },
    {
      "type": "transfer",
      "text": "The same move governs every boundary where untrusted data crosses in. A webhook receiver schema-validates the provider's payload before trusting it (and verifies the signature first). A queue worker validates each message off the topic before processing — a poison message becomes a dead-letter, not a crash loop. An internal service-to-service call still enforces its contract, because \"internal\" is not \"trusted.\" What stays invariant: parse at the boundary, reject as a typed envelope, apply exactly once. What changes with context is the cost of getting it wrong — a billing endpoint's feared failure is a double charge; an AI service backend's is an unbounded prompt running up token cost. This contract-and-failure packet is the first reusable piece of your backend service proof packet — the capstone reuses this exact schema, contract test, and idempotency replay as evidence."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "validation-error-model": [
    {
      "type": "sprint-contract",
      "outcome": "Design the Validation Error Model for one state-changing endpoint: a single structured error contract (status code + machine-readable body with per-field detail) that every rejection on that path obeys — wrong shape, failed auth, conflicting state, and a replayed request all answered the same way.",
      "intensity": "standard",
      "time": "25-30 min",
      "proof": "api_contract_and_failure_repair_packet.md with the error envelope schema, the status-code map (400 vs 401/403 vs 409 vs 422), one worked field-level rejection, and proof it holds: a contract test asserting the body shape plus an idempotency-replay returning the SAME error, not a second mutation. A peer hits the endpoint with bad input and parses the error programmatically without reading your code.",
      "unlock": "Your error model gives one stable, parseable shape across every rejection class on the path, maps each class to the correct status code, and survives a replayed request without double-applying.",
      "doNotClaim": "Do not claim a 'validation error model' if your endpoint returns a bare 500 with a stringified stack trace, or a 200 with {\"ok\": false} in the body. A leaked stack trace is an information-disclosure bug, and a 200-on-failure breaks every client that trusts the status line. A model is a contract clients can code against, not a place to dump whatever the exception said."
    },
    {
      "type": "mission",
      "text": "A partner's checkout integration silently drops 8% of orders for a month. The cause: your endpoint answered invalid carts with HTTP 200 and a human sentence in the body, so their retry logic treated every rejection as success and never resubmitted. One ambiguous error model, thousands of lost orders, and not a single alert fired."
    },
    {
      "type": "context",
      "text": "Every endpoint that changes state will be hit with input it must refuse — and a client on the other side has to tell 'you sent garbage, fix it' apart from 'try again later'. The Validation Error Model is the contract that makes refusal legible: it sits between your request/response contract and your persistence layer, and it is Section 2 of the proof packet you carry through this module."
    },
    {
      "type": "pretest",
      "prompt": "A POST /orders gets a negative quantity. You reject it. Which HTTP status — 400, 422, 500, or 200-with-an-error-flag? Decide before reading on.",
      "reveal": "The instinct is 400, and 400 is defensible — but the sharper answer is 422 Unprocessable Content: the JSON parsed fine (so it is not a 400 malformed-syntax case), it was the meaning that failed validation. The real 'oh': the status code is not decoration, it is the first field of your error model. 500 says 'my fault, retry' and invites a retry storm against a request that will never succeed; 200 says 'success' and corrupts every client that trusts the status line. Picking the code IS designing the contract — clients branch on it before they ever read your body."
    },
    {
      "type": "concept",
      "title": "One stable error shape, the right status, machine-readable per-field detail",
      "text": "A validation error model is a single response contract every rejection obeys: a correct status code (the class of failure), a stable parseable body, and field-level detail naming what failed and why. The invariant: the SHAPE never changes — only the contents. Clients code against the shape, not the prose."
    },
    {
      "type": "diagram",
      "title": "Where the error model lives on a state-changing request",
      "subtitle": "POST /orders flows through four gates — parse, authorize, validate, persist. Each gate can REJECT, and every rejection exits through the one shared error model (status + structured body). The mutation only commits past all four.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "partner integration",
          "kind": "client"
        },
        {
          "id": "parse",
          "label": "Parse / shape",
          "description": "malformed JSON → 400",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "authz",
          "label": "Auth boundary",
          "description": "no token → 401 · wrong scope → 403",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "validate",
          "label": "Validate (semantic)",
          "description": "qty < 0, bad SKU → 422",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "idem",
          "label": "Idempotency check",
          "description": "replayed key → prior result",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "tx",
          "label": "DB transaction",
          "description": "conflict → 409 · else commit",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "errmodel",
          "label": "Validation Error Model",
          "description": "one shape: status + type + errors[]",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "client2",
          "label": "Client parses",
          "description": "branches on status + field",
          "kind": "client"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "parse",
          "label": "POST body",
          "kind": "sync"
        },
        {
          "from": "parse",
          "to": "authz",
          "label": "well-formed",
          "kind": "sync"
        },
        {
          "from": "authz",
          "to": "validate",
          "label": "authorized",
          "kind": "sync"
        },
        {
          "from": "validate",
          "to": "idem",
          "label": "valid",
          "kind": "sync"
        },
        {
          "from": "idem",
          "to": "tx",
          "label": "first time",
          "kind": "sync"
        },
        {
          "from": "tx",
          "to": "client2",
          "label": "201 Created",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "parse",
          "to": "errmodel",
          "label": "400",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "authz",
          "to": "errmodel",
          "label": "401 / 403",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "validate",
          "to": "errmodel",
          "label": "422 + field detail",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "tx",
          "to": "errmodel",
          "label": "409 conflict",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "idem",
          "to": "errmodel",
          "label": "replay → same result",
          "kind": "async",
          "tone": "accent",
          "dashed": true
        },
        {
          "from": "errmodel",
          "to": "client2",
          "label": "one parseable shape",
          "kind": "data",
          "tone": "accent"
        }
      ],
      "legend": [
        {
          "tone": "warning",
          "label": "rejection gate (never reaches the DB)"
        },
        {
          "tone": "accent",
          "label": "the error model + the semantic-validation path"
        },
        {
          "tone": "success",
          "label": "the mutation actually commits"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The contract, then the handler that obeys it",
      "subtitle": "RFC 9457 Problem Details as the envelope, one Zod gate, one error mapper — every rejection exits the same door.",
      "filename": "orders.handler.ts",
      "language": "ts",
      "code": "// The ONE error shape (RFC 9457 Problem Details + a field-level errors[]).\ninterface FieldError { field: string; code: string; message: string }\ninterface ProblemDetails {\n  type: string        // stable URI naming the error class\n  title: string       // short, human, NOT the stack trace\n  status: number      // mirrors the HTTP status line\n  detail?: string\n  errors?: FieldError[]\n}\n\nconst CreateOrder = z.object({\n  sku: z.string().min(1),\n  quantity: z.number().int().positive(),\n})\n\nasync function createOrder(req: Request): Promise<Response> {\n  // GATE 1 — semantic validation. ONE place, structured failure.\n  const parsed = CreateOrder.safeParse(await req.json())\n  if (!parsed.success) {\n    return problem(422, 'about:blank#invalid-order', 'Order failed validation',\n      parsed.error.issues.map((i) => ({\n        field: i.path.join('.'), code: i.code, message: i.message,\n      })))\n  }\n  // GATE 2 — idempotency: a replayed key returns the FIRST result, no 2nd write.\n  const key = req.headers.get('Idempotency-Key')\n  const prior = key ? await idem.lookup(key) : null\n  if (prior) return prior.response\n\n  // GATE 3 — mutate inside a transaction; a conflict is a 409, not a 500.\n  try {\n    const order = await db.tx((t) => t.orders.insert(parsed.data))\n    const res = json(201, order)\n    if (key) await idem.store(key, res)   // record BEFORE returning\n    return res\n  } catch (e) {\n    if (e instanceof ConflictError)\n      return problem(409, 'about:blank#stock-conflict', 'Insufficient stock')\n    throw e   // unknown → 500 by the framework, body still a ProblemDetails\n  }\n}",
      "steps": [
        {
          "lines": [
            2,
            3,
            4,
            5,
            6,
            7,
            8,
            9
          ],
          "label": "Declare the shape ONCE",
          "note": "Every rejection on this path returns a ProblemDetails. type is a stable URI clients can switch on; title is human but is NEVER the exception message — that is the information-disclosure leak the do-not-claim warns about."
        },
        {
          "lines": [
            11,
            12,
            13,
            14
          ],
          "label": "The validation gate is a schema, not scattered ifs",
          "note": "One Zod object is the source of truth. Hand-rolled ifs drift: the schema cannot disagree with itself, and it generates the field-level errors for free."
        },
        {
          "lines": [
            18,
            19,
            20,
            21,
            22,
            23,
            24
          ],
          "label": "Semantic failure → 422 + per-field errors[]",
          "note": "Map each Zod issue to {field, code, message}. The client highlights the exact bad field without parsing English. 422 (not 400) because the JSON parsed — it was the meaning that failed."
        },
        {
          "lines": [
            26,
            27,
            28
          ],
          "label": "Idempotency BEFORE the write",
          "note": "This is the premium scenario's spine: a replayed Idempotency-Key returns the first stored response, so a retry never creates a second order. Exactly-once survives a flaky network."
        },
        {
          "lines": [
            31,
            32,
            33,
            34,
            35,
            36,
            37
          ],
          "label": "Conflicts are 409, unknowns stay structured",
          "note": "A stock conflict is a CLIENT-meaningful 409, not a 500 — the difference between 'change your request' and 'retry later'. Even the re-thrown unknown must serialize as ProblemDetails, never a raw trace."
        }
      ],
      "caption": "Four exits, one shape. A partner integration parses the body the same way whether it got a 422 or a 409 — that sameness IS the model."
    },
    {
      "type": "compare",
      "title": "Weak error model vs gold error model",
      "subtitle": "Same endpoint, same negative-quantity request. One leaks and lies; one is a contract a partner can code against.",
      "left": {
        "label": "Stringly-typed, status-blind",
        "tone": "warning",
        "lines": [
          "HTTP 200 with {\"error\": \"bad quantity\"}",
          "Status line says success; client retries never fire",
          "Body shape differs per error site (prose, not schema)",
          "Auth failure returns the same 200 as a bad SKU",
          "500 path leaks the raw stack trace to the caller",
          "A replayed POST creates a SECOND order"
        ],
        "verdict": "Unparseable + double-mutates — caps the lesson at 78"
      },
      "right": {
        "label": "Structured, status-correct, idempotent",
        "tone": "success",
        "lines": [
          "422 with RFC 9457 body + errors[]:[{field:'quantity'…}]",
          "Status line is the first branch; clients act before reading prose",
          "ONE shape across 400 / 401 / 403 / 409 / 422",
          "type URI lets clients switch on the error class",
          "title is human; the stack trace never crosses the boundary",
          "Replayed key returns the first result — exactly once"
        ],
        "verdict": "Parseable + exactly-once — the reviewable artifact"
      },
      "caption": "The tell is the replay row: a model that double-mutates on retry is not a model, it is a latent incident. Map the class to the status, then keep the shape stable."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "Grounding, not vibes: OWASP API Security Top 10 lists Broken Object/Property-Level Authorization and excessive data exposure among the top risks — a leaked stack trace in an error body hits both at once. RFC 9457 (Problem Details) exists precisely so you do not invent a bespoke error shape per service. SWEBOK V4 frames this as the contract being part of the design, not an afterthought. The senior tell: their error model is designed with the success path, never bolted on after the demo."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this isn't just a happy-path answer.' Which response actually proves the validation error model holds?",
      "options": [
        "A screenshot of the endpoint returning 201 for a valid order.",
        "A contract test asserting the 422 body shape for bad input, PLUS an idempotency-replay test proving a repeated request returns the same response and does not create a second row.",
        "A paragraph in the README describing the intended error format.",
        "A 500 handler that catches everything and logs it server-side."
      ],
      "answer": 1,
      "explanation": "Proof of an error model is proof of the REJECTION paths, not the success path. The contract test pins the structured body so clients can rely on the shape; the idempotency-replay test pins the exactly-once invariant the premium scenario demands. The 201 screenshot (1) only shows the happy path; a README (3) is intent, not evidence; a catch-all 500 (4) is the failure mode — it erases the very distinctions the model exists to make."
    },
    {
      "type": "verification",
      "intro": "Prove the model holds — concrete items, no vibes:",
      "items": [
        "Send malformed JSON, a failed auth, a negative quantity, and a stock conflict. Confirm each returns the SAME body shape and the CORRECT distinct status (400 / 401-403 / 422 / 409).",
        "Parse a rejection programmatically (no human reading): can a client extract the failing field and an error code without regexing prose? If not, it is not machine-readable yet.",
        "Replay an identical POST with the same Idempotency-Key. Confirm it returns the first response and the DB row count did NOT increase — capture the before/after count.",
        "Grep every error path for the raw exception message or stack trace reaching the response body. Any hit is an information-disclosure bug — cut it.",
        "Run the contract/schema test against the error envelope so a future change to the shape fails CI, not a partner's integration in production."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain, using your own endpoint, why returning 200 with an error flag breaks clients in a way a 422 does not.",
        "Walk the four gates (parse, auth, validate, persist) and name which status each rejection maps to and why — from memory.",
        "Why is the idempotency-replay test part of the VALIDATION error model and not a separate concern? What invariant would break without it?"
      ]
    },
    {
      "type": "transfer",
      "text": "Same model, any state-changing surface. A billing endpoint maps a declined card to a structured 402 with a decline-code field, never a 500 the client retries into a double-charge. A webhook receiver returns a stable error body so the sender's retry-with-backoff can tell 'malformed payload, give up' from 'I'm down, resend'. A queue worker writes the same field-level rejection to a dead-letter record so an operator can triage without reading code. What stays invariant: one shape, the right status class, machine-readable detail, exactly-once under replay. What changes is the status vocabulary (402 vs 409 vs a DLQ reason) and who reads it — a partner, a sender, or an on-call operator. Carry api_contract_and_failure_repair_packet.md into the capstone: it is not 'I understand validation errors,' it is 'here is an endpoint I made refuse correctly, and here is the test that proves it.'"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "http-status-codes": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint (POST /payments, or a realistic one) and write its status-code contract: which code each path returns — success, validation failure, auth failure, conflict, server fault — and what the client is supposed to DO on each. Then prove the codes match the behavior, including a duplicate request that must return the same answer twice.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "The `Status Codes` section of api_contract_and_failure_repair_packet.md: a path-by-code table where every code carries a client action, plus a replay test showing a retried POST returns 200/201 the second time (not a second charge). A reviewer reads the table and can predict the response of any request without running it.",
      "unlock": "Your contract maps each path to a status class that tells the client whether to retry, fix-and-resend, re-auth, or stop — and an idempotent replay returns a safe, identical result.",
      "doNotClaim": "Do not claim a status-code contract if every error is a 500 or every error is a 200 with `{\"error\": ...}` in the body. A code the client cannot branch on is decoration. The status code IS the contract; the body is the detail."
    },
    {
      "type": "mission",
      "text": "A customer's card gets charged twice. The retry on a flaky network hit your endpoint a second time, your handler returned 500 the first time after the charge succeeded, and the client — seeing 500 — retried. Nobody decided what 500 meant. The status code was the contract, and it lied."
    },
    {
      "type": "context",
      "text": "This sits in the foundation phase of Backend Engineering: it turns the HTTP contract from earlier request/response lessons into a precise, machine-readable promise. The status code is the one byte every client, proxy, retry library, and load balancer reads BEFORE the body — get it wrong and correct code downstream does the wrong thing."
    },
    {
      "type": "pretest",
      "prompt": "A POST that creates a payment fails because the amount is negative. Your handler returns `200 OK` with `{\"error\": \"invalid amount\"}`. What breaks downstream — even though your message is perfectly clear? Answer before reading on.",
      "reveal": "Everything that reads the code instead of your body. The client's HTTP library sees 200 and resolves the promise as success; the retry middleware never fires; the dashboard graphs it as a healthy request; the proxy may cache it. Your message is human-readable and machine-invisible. 200-with-an-error-body is the most common way a status-code contract silently fails: the one field every layer agrees to read says 'fine' while the truth is in a field only your frontend parses."
    },
    {
      "type": "concept",
      "title": "The status code is a contract about what the CLIENT does next",
      "text": "2xx: succeeded, stop. 4xx: YOU sent a bad request — fix it, don't blind-retry. 401/403: re-authenticate or you're forbidden. 409: it conflicts with current state. 5xx: WE failed — safe to retry. The invariant: the code must match the action the client should take."
    },
    {
      "type": "diagram",
      "title": "One state-changing request, five exits",
      "subtitle": "A POST /payments handler is a decision tree, and each leaf is a status code that tells the client what to do next. The failure path (charge succeeds, handler 500s) and the repair (idempotency key replays the stored result) are both on the map.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "POST /payments + Idempotency-Key",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "validate",
          "label": "Validate body",
          "description": "amount > 0, currency set?",
          "kind": "decision"
        },
        {
          "id": "authz",
          "label": "Auth boundary",
          "description": "token valid? allowed?",
          "kind": "decision"
        },
        {
          "id": "idem",
          "label": "Idempotency store",
          "description": "seen this key before?",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "charge",
          "label": "Charge + persist",
          "description": "transaction commits",
          "kind": "process"
        },
        {
          "id": "r422",
          "label": "422 Unprocessable",
          "description": "client: fix body, resend",
          "kind": "external",
          "tone": "warning"
        },
        {
          "id": "r401",
          "label": "401 / 403",
          "description": "client: re-auth / stop",
          "kind": "external",
          "tone": "warning"
        },
        {
          "id": "r201",
          "label": "201 Created",
          "description": "client: done, store id",
          "kind": "external",
          "tone": "success"
        },
        {
          "id": "r409",
          "label": "409 Conflict",
          "description": "client: reconcile state",
          "kind": "external",
          "tone": "warning"
        },
        {
          "id": "r500",
          "label": "500 / 503",
          "description": "client: safe to retry",
          "kind": "external",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "validate",
          "label": "request",
          "kind": "sync"
        },
        {
          "from": "validate",
          "to": "r422",
          "label": "bad input",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "validate",
          "to": "authz",
          "label": "valid",
          "kind": "control"
        },
        {
          "from": "authz",
          "to": "r401",
          "label": "no/forbidden",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "authz",
          "to": "idem",
          "label": "allowed",
          "kind": "control"
        },
        {
          "from": "idem",
          "to": "r201",
          "label": "key replayed → stored result",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "idem",
          "to": "charge",
          "label": "new key",
          "kind": "control"
        },
        {
          "from": "charge",
          "to": "r409",
          "label": "duplicate / stale state",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "charge",
          "to": "r201",
          "label": "committed",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "charge",
          "to": "r500",
          "label": "infra fault (DB down)",
          "kind": "control",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the incoming state-changing request"
        },
        {
          "tone": "success",
          "label": "2xx — succeeded, including a safe replay"
        },
        {
          "tone": "warning",
          "label": "4xx/5xx — each tells the client a different next move"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The handler that makes the contract true",
      "subtitle": "FastAPI POST /payments — every exit raises the code that matches the client action, and the idempotency key makes a retry return the SAME result instead of charging twice.",
      "filename": "payments_handler.py",
      "language": "python",
      "code": "@router.post(\"/payments\", status_code=201)\nasync def create_payment(body: PaymentIn, idem_key: str = Header(...), user=Depends(auth)):\n    if body.amount <= 0:\n        raise HTTPException(422, \"amount must be positive\")\n    if not user.can_charge(body.account_id):\n        raise HTTPException(403, \"not allowed to charge this account\")\n\n    prior = await store.get_by_idem_key(idem_key)\n    if prior is not None:\n        return JSONResponse(prior.body, status_code=prior.status)  # replay, no re-charge\n\n    try:\n        async with db.transaction():\n            payment = await charge_and_persist(body, idem_key)\n    except DuplicateChargeError:\n        raise HTTPException(409, \"payment already exists for this order\")\n    except InfraError:\n        raise HTTPException(503, \"payment provider unavailable, retry\")\n\n    return JSONResponse(payment.public(), status_code=201)",
      "steps": [
        {
          "lines": [
            1
          ],
          "label": "Declare the success code up front",
          "note": "201 Created — not 200 — because a POST that creates a resource should announce the creation. The default code is part of the contract, not an afterthought."
        },
        {
          "lines": [
            3,
            4
          ],
          "label": "Client error → 422, never 200",
          "note": "The body is bad, so the CLIENT must fix and resend. A 4xx tells their retry library 'do not blind-retry this — it will fail identically.' Returning 200 here is the pretest trap."
        },
        {
          "lines": [
            5,
            6
          ],
          "label": "Auth boundary → 403",
          "note": "Distinct from 401 (not authenticated) — 403 means authenticated but forbidden. Conflating them costs the client the ability to decide between re-login and stop."
        },
        {
          "lines": [
            8,
            10
          ],
          "label": "Idempotency replay → identical result",
          "note": "A retry with the same Idempotency-Key returns the STORED response, same status, no second charge. This is the line that fixes the double-charge in the mission."
        },
        {
          "lines": [
            15,
            16
          ],
          "label": "Conflict → 409",
          "note": "A duplicate or stale-state collision is the client's to reconcile, not a server fault. 409 says 'your request conflicts with current state', not 'we broke'."
        },
        {
          "lines": [
            17,
            18
          ],
          "label": "Infra fault → 503, safe to retry",
          "note": "5xx is the one class that INVITES retry. 503 (provider down) tells the client to back off and try again — the opposite signal from the 4xx above."
        }
      ],
      "caption": "Replay the worked failure: a flaky-network retry hits this twice with the same key → first call charges + returns 201, second call returns the stored 201. One charge, two identical 201s — the contract held."
    },
    {
      "type": "compare",
      "title": "Two responses to the same five failures",
      "subtitle": "Endpoint: POST /payments. Weak version collapses meaning into one code; gold version makes each path branchable.",
      "left": {
        "label": "Collapsed contract",
        "tone": "warning",
        "lines": [
          "Bad input → 200 {error: 'bad amount'}",
          "Forbidden → 200 {error: 'no access'}",
          "Duplicate → 200 {error: 'dup'}",
          "DB down → 200 {error: 'oops'}",
          "Client must parse the BODY to know if it worked",
          "Retry library never fires; proxy caches the 'error'"
        ],
        "verdict": "One code for five futures — the client is blind"
      },
      "right": {
        "label": "Branchable contract",
        "tone": "success",
        "lines": [
          "Bad input → 422 (fix and resend)",
          "Forbidden → 403 (re-auth or stop)",
          "Duplicate → 409 (reconcile state)",
          "DB down → 503 (safe to retry, back off)",
          "Success → 201 (done, store the id)",
          "Client branches on the CODE, body is just detail"
        ],
        "verdict": "Five codes, five correct client actions"
      },
      "caption": "OWASP API Security flags both extremes: leaking detail in error bodies AND hiding real failures behind 200s. The status class is the safe, machine-readable channel; the body is where you stay sparing."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell, grounded in OWASP API Top 10: the dangerous failures are not the loud 500s — they're the 200s that hide an error and the 5xx that leaks a stack trace. Pick the code by what the CLIENT must do (retry / fix / re-auth / reconcile / stop), then keep the body minimal. A correct status code with a vague body beats a 200 with a perfect error message every time, because every layer between you and the user reads the code and almost none read the body."
    },
    {
      "type": "quiz",
      "question": "Reviewer challenge: a teammate's POST /orders returns 500 when the request body is missing a required field. Why is that the wrong code, and what is the cost in production?",
      "options": [
        "It is fine — any error can be a 500, the body explains the rest.",
        "A missing field is a CLIENT error (4xx, e.g. 422/400), not a server fault (5xx). Returning 500 tells retry libraries to re-send the identical bad request, inflates your error-rate SLO with non-incidents, and pages on-call for a problem only the caller can fix.",
        "It should be a 200 so the client's promise resolves cleanly.",
        "It should be a 404 because the order was not created."
      ],
      "answer": 1,
      "explanation": "5xx means WE failed and a retry might work; 4xx means YOU sent something we can't process and retrying unchanged won't help. A malformed body is squarely 4xx. Mislabeling it 5xx triggers automatic retries of a doomed request, corrupts reliability metrics (DORA change-failure-rate, error budgets), and wakes on-call for a caller bug — the exact 'happy-path answer' the reviewer is hunting for."
    },
    {
      "type": "verification",
      "intro": "Prove the contract holds — contract test, replay, no vibes:",
      "items": [
        "For every path in your table, name the client action the code implies (retry / fix-and-resend / re-auth / reconcile / stop). If two paths share a code but need different actions, split the code.",
        "Send a malformed body: assert a 4xx (not 200, not 500). The error must be machine-detectable from the status line alone.",
        "Send the SAME state-changing request twice with one idempotency key: assert the second response is identical and no second side effect occurred (one charge, one row).",
        "Force an infra fault (kill the DB / mock provider timeout): assert a 5xx, and assert the client's retry policy would treat it as retryable.",
        "Grep the handler for `200` returned alongside an error field. Any hit is a collapsed contract — convert it to the right 4xx/5xx."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain to a junior why 422 and 503 are NOT interchangeable, using the retry behavior each one triggers in the client.",
        "Walk through your own endpoint: name each status code you return and the single client action it promises — from memory, contract closed.",
        "Why does the idempotency key belong in the same lesson as status codes? What does a 'safe replay' return, and which code does it carry?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same move travels to every state-changing surface. A webhook receiver returns 2xx to mean 'I durably accepted this, stop redelivering' and 5xx to mean 'redeliver later' — get it backwards and you either drop events or get hammered by duplicates. A queue worker's ack/nack is the same contract without HTTP. A billing endpoint leans hardest on 409 + idempotency because money makes duplicates unforgivable. What stays invariant: the code names the consumer's next action. What changes with risk and cost: how much you invest in idempotency (a read endpoint barely needs it; a charge endpoint cannot ship without it). This `Status Codes` section now plugs straight into the capstone's api_contract_and_failure_repair_packet.md — the same table, the same replay proof, reused unchanged."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "pagination-filtering-sorting": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real list endpoint (GET /orders or similar) and make it production-shaped: cursor (keyset) pagination with a bounded page size, an allowlisted filter + sort contract, and a query that cannot be injected or knocked over by a deep page. Capture it as api_contract_and_failure_repair_packet.md.",
      "intensity": "deep",
      "time": "30 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the request/response contract (filter params, sort allowlist, limit cap, next_cursor), the rejected approach (OFFSET pagination) and why, the expected failure you broke (drift or injection), the verification method (a contract test + a deep-page query plan), one reviewer objection, and the capstone reuse note. A reviewer can page from first to last with zero dupes and zero gaps.",
      "unlock": "Your endpoint pages by keyset (not OFFSET), caps the limit, allowlists sort columns + direction, and returns an opaque next_cursor — proven by a test that pages to the end with no duplicates.",
      "doNotClaim": "Do not claim 'pagination works' from a screenshot of page 1. Page 1 always looks fine. The skill lives at the seam between pages and at page 5,000 — if you have not paged to the end while rows are being inserted, you have not tested pagination, you have tested a SELECT with a LIMIT."
    },
    {
      "type": "mission",
      "text": "A customer exports their order history, scrolls to page 2, and sees three orders that were already on page 1 — and never sees the four that fell through the crack. Nobody wrote a bug. Someone wrote OFFSET 20 over a table that keeps getting new rows."
    },
    {
      "type": "context",
      "text": "Every list endpoint that ships unbounded is a latent outage: one client asks for all 4M rows, sorts by an un-indexed column, and takes the database down. Pagination, filtering, and sorting are one contract — the invariant is a stable, bounded, injection-proof window over a moving result set."
    },
    {
      "type": "pretest",
      "prompt": "Your endpoint does ORDER BY created_at LIMIT 20 OFFSET 40 to fetch page 3. Rows are being inserted at the top while users page. What goes wrong — and why does page 1 still look perfect in the demo?",
      "reveal": "OFFSET counts rows from the current result set, not from where the user was. Insert two rows at the top and OFFSET 40 now points two rows earlier than last time — the user re-sees rows (duplicates) and skips rows (gaps). The demo passes because in a quiet demo nothing is being inserted between page loads. The bug is invisible until there is concurrent write traffic — i.e. always, in production. Keyset pagination fixes it by remembering WHERE you were (the last row's key), not HOW FAR you scrolled."
    },
    {
      "type": "concept",
      "title": "One contract: bounded window, stable key, allowlisted filter + sort",
      "text": "Page by the last row's sort key (keyset), not by OFFSET. Cap the limit. Allowlist which columns can be filtered and sorted, and the direction. The invariant: stable order under concurrent writes, a hard ceiling on work, and no user string ever reaching SQL identifiers."
    },
    {
      "type": "diagram",
      "title": "The list-endpoint request path — where each control lives",
      "subtitle": "Client query string → validate (allowlist + cap) → keyset WHERE on an indexed (sort_key, id) → bounded scan → opaque next_cursor. The failure path (OFFSET drift / unbounded scan / injected ORDER BY) is the warning branch.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "?limit&sort&filter&cursor",
          "kind": "client"
        },
        {
          "id": "handler",
          "label": "API Handler",
          "description": "parse query string",
          "kind": "service"
        },
        {
          "id": "validate",
          "label": "Validate contract",
          "description": "cap limit · allowlist sort/filter · decode cursor",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "query",
          "label": "Keyset Query",
          "description": "WHERE (created_at,id) < cursor ORDER BY ... LIMIT n+1",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "index",
          "label": "Composite Index",
          "description": "(created_at, id) — covers the sort",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "danger",
          "label": "Unsafe path",
          "description": "OFFSET drift · unbounded LIMIT · ORDER BY $userInput",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "resp",
          "label": "Response",
          "description": "items[] + opaque next_cursor",
          "kind": "external",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "handler",
          "label": "GET /orders",
          "kind": "sync"
        },
        {
          "from": "handler",
          "to": "validate",
          "label": "raw params",
          "kind": "data"
        },
        {
          "from": "validate",
          "to": "query",
          "label": "safe, bounded",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "query",
          "to": "index",
          "label": "seeks, no scan",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "query",
          "to": "resp",
          "label": "n+1 → hasMore",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "validate",
          "to": "danger",
          "label": "if you skip it",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the contract: validate + keyset (the whole skill)"
        },
        {
          "tone": "success",
          "label": "indexed, bounded, opaque — what reviewers trust"
        },
        {
          "tone": "warning",
          "label": "the production failure if validation is skipped"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The gold handler — keyset, capped, allowlisted",
      "subtitle": "GET /orders done right: opaque cursor decode, sort allowlist, limit cap, n+1 trick for hasMore. No OFFSET, no user string in SQL identifiers.",
      "filename": "listOrders.ts",
      "language": "ts",
      "code": "const SORTABLE = { created_at: 'created_at', total: 'total_cents' } as const\nconst MAX_LIMIT = 100\n\nexport async function listOrders(req: Request) {\n  const q = new URL(req.url).searchParams\n  const limit = Math.min(Number(q.get('limit') ?? 20) || 20, MAX_LIMIT)\n  const sortKey = SORTABLE[q.get('sort') as keyof typeof SORTABLE] ?? 'created_at'\n  const status = q.get('status') // filter, bound-param below\n  const cursor = decodeCursor(q.get('cursor')) // -> { key, id } | null\n\n  const rows = await sql`\n    SELECT id, status, total_cents, created_at FROM orders\n    WHERE customer_id = ${req.customerId}\n      ${status ? sql`AND status = ${status}` : sql``}\n      ${cursor ? sql`AND (${sql(sortKey)}, id) < (${cursor.key}, ${cursor.id})` : sql``}\n    ORDER BY ${sql(sortKey)} DESC, id DESC\n    LIMIT ${limit + 1}`\n\n  const hasMore = rows.length > limit\n  const items = hasMore ? rows.slice(0, limit) : rows\n  const last = items[items.length - 1]\n  return Response.json({\n    items,\n    next_cursor: hasMore ? encodeCursor(last[sortKey], last.id) : null,\n  })\n}",
      "steps": [
        {
          "lines": [
            1,
            4
          ],
          "label": "Allowlist + cap as constants",
          "note": "SORTABLE maps a public sort name to a real column; an unknown sort falls back, never reaches SQL. MAX_LIMIT is the ceiling that stops 'give me 4 million rows'."
        },
        {
          "lines": [
            7
          ],
          "label": "Bound the limit",
          "note": "Math.min caps it and the || 20 handles garbage input. This single line is the difference between a slow endpoint and a database outage."
        },
        {
          "lines": [
            8
          ],
          "label": "Resolve sort through the allowlist",
          "note": "q.get('sort') is a user string. It is used only as a KEY into SORTABLE — the value put into SQL is a column you control. This is how ORDER BY stops being an injection vector."
        },
        {
          "lines": [
            10
          ],
          "label": "Decode the opaque cursor",
          "note": "The cursor carries the last row's (sort key, id) — its position, not its page number. Opaque so clients cannot forge offsets or leak internals."
        },
        {
          "lines": [
            16
          ],
          "label": "The keyset WHERE — the whole trick",
          "note": "(sort_key, id) < (cursor.key, cursor.id) on a composite index seeks straight to where you left off. id is the tiebreaker that makes the order total, so no row is ever duplicated or skipped under concurrent inserts."
        },
        {
          "lines": [
            18
          ],
          "label": "LIMIT n+1 → hasMore",
          "note": "Fetch one extra row. If it comes back, there is a next page; you slice it off and mint next_cursor from the genuine last item. No second COUNT(*) query, no OFFSET."
        }
      ],
      "caption": "Page to the end while rows insert: every item appears exactly once. That property — not page 1 rendering — is the proof."
    },
    {
      "type": "compare",
      "title": "OFFSET endpoint vs keyset endpoint",
      "subtitle": "Same GET /orders. Both return JSON. Only one is correct under writes and safe under a hostile sort param.",
      "left": {
        "label": "OFFSET + interpolated sort",
        "tone": "warning",
        "lines": [
          "ORDER BY ${req.sort} LIMIT 20 OFFSET ${page*20}",
          "Drift: inserts shift OFFSET → dupes + gaps",
          "Deep page: OFFSET 100000 scans + discards 100k rows",
          "Injectable: req.sort flows into SQL identifier",
          "No limit cap → one client can request all rows",
          "next page = page+1 (assumes a frozen result set)"
        ],
        "verdict": "Correct only in a demo with no concurrent writes"
      },
      "right": {
        "label": "Keyset + allowlist + cap",
        "tone": "success",
        "lines": [
          "WHERE (created_at,id) < cursor ORDER BY ... LIMIT n+1",
          "Stable: position is the last row's key, not a count",
          "Deep page: index seek, O(limit) regardless of depth",
          "Sort param is a KEY into an allowlist, never SQL",
          "limit = min(requested, MAX_LIMIT) — bounded work",
          "next_cursor = opaque (last key, id) — total order"
        ],
        "verdict": "Correct under concurrent writes, safe, O(limit) deep"
      },
      "caption": "OFFSET asks 'how far did I scroll?' over a list that is moving. Keyset asks 'where exactly was I?' — and the database can seek straight there."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "OWASP API4 (Unrestricted Resource Consumption) names the uncapped limit explicitly: a list endpoint with no maximum page size is a denial-of-service primitive, not a convenience. And the tell of a senior backend engineer is the tiebreaker — they always sort by (business_key, id), never business_key alone, because a non-unique sort column makes the page boundary ambiguous and silently reintroduces the duplicate/gap bug keyset was supposed to kill."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this is not just a happy-path answer.' Which single piece of evidence best proves the pagination contract holds?",
      "options": [
        "A screenshot of page 1 rendering 20 orders correctly.",
        "A test that pages from the first cursor to next_cursor === null while rows are inserted, asserting every id appears exactly once (no dupes, no gaps), plus an EXPLAIN showing an index seek at a deep cursor.",
        "A comment in the code saying '// keyset pagination, no OFFSET'.",
        "A load test showing the endpoint returns 200 OK under traffic."
      ],
      "answer": 1,
      "explanation": "The invariant is 'each row appears exactly once across the full traversal, under concurrent writes.' Only option 2 proves it — it walks the whole result set via cursors (not page numbers) while inserts happen, asserts the no-dupe/no-gap property, and the EXPLAIN proves deep pages stay O(limit) instead of scanning. Page-1 screenshots (1), comments (3), and a 200 status (4) are exactly the happy-path theater the reviewer is asking you to get past."
    },
    {
      "type": "verification",
      "intro": "Prove the contract — no vibes:",
      "items": [
        "Page from the first request to next_cursor === null and collect every id. Assert the set has no duplicates and the count equals a snapshot COUNT(*). If you have not done this WHILE inserting rows, you have not tested the drift case.",
        "Run EXPLAIN on the query with a deep cursor. It must show an index seek on (sort_key, id), not a sequential scan or a large OFFSET discard.",
        "Send sort=created_at;DROP and sort=__proto__. The endpoint must fall back to the default sort, never error with a SQL message and never echo the input into the query.",
        "Send limit=1000000. The response must contain at most MAX_LIMIT items — confirm the cap fires, not just that it is written down.",
        "Confirm the sort column has a unique tiebreaker (id). Sort by a non-unique column alone and watch a duplicate appear at the page boundary — then re-add id and watch it vanish."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain to a teammate why OFFSET produces duplicates and gaps under concurrent inserts, using the word 'position' and the word 'count' — and why keyset does not.",
        "Why is allowlisting the sort column an injection defense AND a performance defense? Name both jobs that one constant does.",
        "What is the n+1 row trick, and what two extra database round-trips does it save you compared to a naive 'COUNT then OFFSET' pager?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same contract is invariant across every list surface; only the key changes. A webhook backfill pages an external API by its cursor token so a retry never re-delivers events. A queue worker drains a backlog by (priority, id) keyset so new high-priority jobs do not shuffle the window. A billing export pages invoices by (issued_at, id) so a month-end report never double-counts a charge. What stays constant: bounded page size, a unique-tiebreaker stable order, an allowlisted filter/sort, an opaque cursor. What changes with scale, risk, and cost: how aggressively you cap, whether the cursor is signed, and whether you expose total counts at all. Carry api_contract_and_failure_repair_packet.md into the capstone — the service you build there reuses this exact list contract and this exact proof."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "idempotency-retry-safety": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real state-changing endpoint (a payment charge, an order create, a webhook handler) and make it safe to retry: add an idempotency key, persist it inside the same database transaction as the effect, and return the first response on every replay. Capture it in api_contract_and_failure_repair_packet.md.",
      "intensity": "standard",
      "time": "25-30 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the HTTP contract (Idempotency-Key header), the selected approach, the rejected approach, the expected failure, the verification method (replay the same key, assert one charge), a reviewer objection, and the capstone connection. A replay test fires the same key twice and proves exactly one side effect.",
      "unlock": "A duplicate request with the same idempotency key produces exactly one effect and returns the original response — proven by a replay test, not by claim.",
      "doNotClaim": "Do not claim idempotency because the request 'looks the same'. Identical payloads are not the invariant — a stored, transaction-bound idempotency key is. If the dedupe check and the write are not in one atomic unit, you have a race, not safety."
    },
    {
      "type": "mission",
      "text": "The customer's card was charged three times. The app didn't double-submit — the network timed out, the mobile client retried twice, and your 'charge' handler ran clean each time because nothing told it this was the same request."
    },
    {
      "type": "context",
      "text": "Retries are not an edge case; they are the default in any distributed system — clients, gateways, and queue workers all retry on timeout. Idempotency retry safety is the contract that lets a request arrive twice and change the world once, which is Section 1 of the packet you carry into the capstone."
    },
    {
      "type": "pretest",
      "prompt": "Your POST /charges returns 200, but the client never got the response — the connection dropped. The client retries the exact same request. What does a naive handler do, and why is 'just check if a charge already exists' not enough? Answer before reading on.",
      "reveal": "The naive handler charges again — the second request is indistinguishable from a real new charge. And 'check if one exists first' is a race: two retries can both pass the check before either writes, so both charge. The fix is not a lookup before the write; it is a unique idempotency key reserved in the SAME transaction as the effect, so the database — not your code's timing — enforces exactly-once."
    },
    {
      "type": "concept",
      "title": "One key, one effect — enforced by the transaction, not by a pre-check",
      "text": "The client sends an Idempotency-Key. The server stores that key with the result inside the same DB transaction as the side effect. A unique constraint makes the second insert fail, so a replay returns the first response. The invariant: dedupe and effect commit atomically, or not at all."
    },
    {
      "type": "diagram",
      "title": "The idempotent charge path — first request vs replay",
      "subtitle": "Client sends Idempotency-Key. The handler reserves the key and writes the charge in ONE transaction; the unique constraint on the keys store is what enforces exactly-once. A replay hits the stored result and skips the gateway entirely. The failure path is the dashed retry; the repair is the keys store.",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "retries on timeout",
          "kind": "client"
        },
        {
          "id": "handler",
          "label": "Charge Handler",
          "description": "API endpoint",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "keys",
          "label": "Idempotency Keys",
          "description": "key → result, UNIQUE",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "charges",
          "label": "Charges Table",
          "description": "the side effect",
          "kind": "store"
        },
        {
          "id": "gateway",
          "label": "Payment Gateway",
          "description": "external, charges once",
          "kind": "external",
          "tone": "warning"
        },
        {
          "id": "replay",
          "label": "Replay arrives",
          "description": "same key, second time",
          "kind": "decision",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "handler",
          "label": "POST + Idempotency-Key",
          "kind": "sync"
        },
        {
          "from": "handler",
          "to": "keys",
          "label": "reserve key (same txn)",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "handler",
          "to": "charges",
          "label": "write charge (same txn)",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "handler",
          "to": "gateway",
          "label": "charge once",
          "kind": "sync",
          "tone": "warning"
        },
        {
          "from": "replay",
          "to": "keys",
          "label": "key exists → return stored result",
          "kind": "control",
          "dashed": true,
          "tone": "muted"
        },
        {
          "from": "keys",
          "to": "client",
          "label": "first response, no re-charge",
          "kind": "data",
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "atomic: key reserve + effect in one transaction"
        },
        {
          "tone": "success",
          "label": "the keys store — enforces exactly-once"
        },
        {
          "tone": "warning",
          "label": "external effect that must run only once"
        },
        {
          "tone": "muted",
          "label": "the replay path — short-circuits to stored result"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Making POST /charges replay-safe, line by line",
      "subtitle": "The Worked Expert Example: key reserved and effect written in ONE transaction; the unique constraint does the enforcing.",
      "filename": "charges.ts",
      "language": "ts",
      "code": "// migration: CREATE UNIQUE INDEX ON idempotency_keys (key);\nasync function createCharge(req: ChargeRequest): Promise<ChargeResult> {\n  const key = req.headers['idempotency-key']\n  if (!key) throw new HttpError(400, 'Idempotency-Key required')\n\n  return db.transaction(async (tx) => {\n    const existing = await tx.idempotencyKeys.find(key)\n    if (existing) return existing.result          // replay → first response\n\n    const charge = await tx.charges.insert({ amount: req.amount })\n    const result = { id: charge.id, status: 'captured' }\n\n    // UNIQUE(key): a concurrent retry that raced past find() fails HERE\n    await tx.idempotencyKeys.insert({ key, result })\n    await gateway.charge(req.amount, { idempotencyKey: key })\n    return result\n  })\n}",
      "steps": [
        {
          "lines": [
            3,
            4
          ],
          "label": "The HTTP contract",
          "note": "The Idempotency-Key header is the request's identity. No key, no safety — reject at the boundary rather than guess."
        },
        {
          "lines": [
            6
          ],
          "label": "One transaction wraps everything",
          "note": "The dedupe read, the effect write, and the key insert all commit together or roll back together. This is the whole invariant."
        },
        {
          "lines": [
            7,
            8
          ],
          "label": "Replay short-circuit",
          "note": "If the key already has a stored result, return it untouched. This is what makes the second, third, Nth request safe."
        },
        {
          "lines": [
            10,
            11
          ],
          "label": "The effect",
          "note": "Write the charge and build the response. Not yet visible to anyone — the transaction hasn't committed."
        },
        {
          "lines": [
            14
          ],
          "label": "The UNIQUE constraint is the enforcer",
          "note": "Two retries can both pass find() at line 7. Only one survives the insert here; the loser's transaction rolls back and retries into the replay branch. The database, not your timing, guarantees exactly-once."
        },
        {
          "lines": [
            15
          ],
          "label": "Pass the key downstream too",
          "note": "Stripe and most gateways accept their own idempotency key. Forward it so even a partial-commit retry can't double-charge externally. But the DB transaction cannot roll back an external charge: if the gateway succeeds and the COMMIT then fails, you have a charge with no record. It is the gateway's OWN idempotency key (not the DB transaction) that makes the external effect exactly-once — and holding DB row locks across a network call is itself a cost. A transactional outbox avoids this."
        }
      ],
      "caption": "Run the replay test: fire the same key twice → assert one row in charges, one gateway call, identical response body both times."
    },
    {
      "type": "compare",
      "title": "Check-then-write vs key-in-transaction",
      "subtitle": "Same endpoint under a client retry storm. Both 'check for duplicates'. Only one survives a race.",
      "left": {
        "label": "Check-then-write",
        "tone": "warning",
        "lines": [
          "if (await findCharge(req)) return; // pre-check",
          "Lookup and write are separate steps",
          "Two retries both pass the check, then both write",
          "No idempotency key — relies on payload equality",
          "Gateway charged twice; no stored response to replay"
        ],
        "verdict": "Races under concurrency — charges twice"
      },
      "right": {
        "label": "Key reserved in the transaction",
        "tone": "success",
        "lines": [
          "Client-supplied Idempotency-Key is the identity",
          "find + insert charge + insert key in ONE txn",
          "UNIQUE(key) makes the racing retry's insert fail",
          "Loser rolls back, retries, returns stored result",
          "Exactly one charge, one gateway call, replay-safe"
        ],
        "verdict": "Atomic — exactly one effect, proven by replay"
      },
      "caption": "The difference is not 'do you check' — both check. It's whether the database enforces uniqueness atomically with the effect, or your code hopes the timing works out."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "Retry-safety is both a correctness property (no duplicate effects) and a security property — a duplicate-submission bug is also an abuse vector (replayed payments, doubled credits). Replayed state-changing requests relate to OWASP API6:2023 (Unrestricted Access to Sensitive Business Flows), where idempotency keys are a commonly recommended MITIGATION — the Top 10 text itself doesn't name retry-safety. The senior tell — scope the key's lifetime and store the response, not just the key. A key with no stored result can't answer a replay; a key that never expires leaks storage forever. Bound it (24h is a common TTL) and store the exact body you returned."
    },
    {
      "type": "quiz",
      "question": "Reviewer challenge: a teammate says 'my handler is idempotent because it checks for an existing charge before inserting.' What is the exact evidence that proves this is NOT safe under retries?",
      "options": [
        "Show that the payloads of the two requests are byte-identical.",
        "Run two concurrent requests with the same idempotency key against the real DB and show two charges committed because the check and the insert are not in one transaction.",
        "Confirm the endpoint returns 200 on the happy path.",
        "Point out that the handler has no logging."
      ],
      "answer": 1,
      "explanation": "Idempotency is an atomicity property, so the proof is a concurrency test, not a payload comparison. Two parallel retries can both pass a separate pre-check before either commits — the only thing that prevents the double-write is a unique constraint enforced inside the same transaction as the effect. Identical payloads (option 1) and a green happy path (option 3) say nothing about the race."
    },
    {
      "type": "verification",
      "intro": "Prove the endpoint is retry-safe — no vibes:",
      "items": [
        "Contract test: a request without an Idempotency-Key is rejected at the boundary (400), not silently processed.",
        "Replay test: fire the same key twice sequentially → assert exactly one row in the effect table and one downstream gateway call.",
        "Concurrency test: fire two requests with the same key in parallel → exactly one effect commits; the other returns the stored result, not an error.",
        "Rollback evidence: force the effect write to fail mid-transaction → confirm the idempotency key was NOT persisted (no orphan key that would block a legitimate retry).",
        "Replay returns the ORIGINAL response body, byte-for-byte — not a fresh 'already processed' message the client didn't expect."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why is 'check if it already exists, then write' a race, and what specifically does putting the key insert inside the transaction fix?",
        "Explain why payload equality is the wrong invariant for idempotency and a stored idempotency key is the right one — using your own endpoint as the example.",
        "Walk through what happens on a replay step by step: where does the second request stop, and what does the client receive?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same key-in-transaction move travels across every state-changing boundary. A webhook handler dedupes on the provider's event ID so a redelivered Stripe event credits an account once. A queue worker stores the message ID before acking so an at-least-once redelivery doesn't re-run the job. An internal API that mutates inventory keys on a client request ID. What stays invariant: dedupe and effect commit atomically, and a replay returns the first result. What changes by context — where the key comes from (client header, event ID, message ID), how long you retain it (a one-off charge vs a high-volume webhook stream), and whether the downstream effect has its own idempotency key to forward. Carry api_contract_and_failure_repair_packet.md into the capstone: it is reused there as the proof that your service path survives retries, not just the happy path."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "auth-authorization-boundaries": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing API endpoint and draw its auth boundary: prove that every request enforces authorization against the authenticated principal AND the specific object, not just a valid token. Capture it as a reviewable packet — the boundary, the failure you fear (BOLA/IDOR), the kill-test, and the repair.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the endpoint contract, the auth boundary (who is the principal, what object, what rule), the expected failure (object-level authorization bypass), a contract/integration test that returns 403 when a valid token requests another tenant's object, and the repair note. A reviewer can replay the cross-tenant request and watch it get denied.",
      "unlock": "Your packet names the principal, the object, the authorization rule enforced server-side, and a test where a fully-authenticated token is still denied the wrong object.",
      "doNotClaim": "Do not claim you have an auth boundary because the route is behind a login. Authentication answers 'who are you'; authorization answers 'may you touch THIS'. A 200 for a valid token on someone else's order is the exact failure this lesson exists to kill — being logged in is not permission."
    },
    {
      "type": "mission",
      "text": "GET /api/orders/1043 returns the order. A curious user edits the URL to /api/orders/1044 and reads a stranger's invoice — name, address, card last four. The token was valid the whole time; nobody ever checked that 1044 was theirs. That single missing line is OWASP API Top 10 #1, and it ships to production constantly."
    },
    {
      "type": "context",
      "text": "This lesson sits in the applied-skill phase of Backend Engineering: you already write request/response contracts and validate input — now you make authorization a boundary the request cannot cross. The auth boundary is Section 1 of the api_contract_and_failure_repair_packet.md you carry through validation, persistence, and the capstone."
    },
    {
      "type": "pretest",
      "prompt": "Your endpoint requires a valid JWT, and the request arrives with one. A logged-in user requests /api/orders/1044 — an order that belongs to a different customer. Is returning it a bug? Decide before reading.",
      "reveal": "Yes — and the most common high-severity one in production APIs. A valid token proves authentication (who you are), not authorization (what you may touch). Trusting the client-supplied object id `1044` without checking it belongs to the authenticated principal is Broken Object-Level Authorization (BOLA / IDOR). The boundary is not the login wall; it is the per-object ownership check on every single request, enforced server-side, every time."
    },
    {
      "type": "concept",
      "title": "Authentication is the door; authorization is the boundary on every object",
      "text": "The invariant: every request re-derives the principal from the trusted token and checks that principal against the specific object before acting. Never trust a client-supplied id or role to grant access. Authenticated is not authorized."
    },
    {
      "type": "diagram",
      "title": "Where the auth boundary lives in the request path",
      "subtitle": "Authn happens once at the gateway; authz must happen again at the handler, against the object the request names. The dashed edge is the failure: skipping the ownership check.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "sends token + object id 1044",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "verifies JWT signature → principal",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "handler",
          "label": "Order Handler",
          "description": "the authz boundary lives HERE",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "authz",
          "label": "Authorization check",
          "description": "order.customer_id == principal.id?",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "source of truth: order.customer_id",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "deny",
          "label": "403 Forbidden",
          "description": "principal ≠ owner",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "leak",
          "label": "200 + stranger's data",
          "description": "BOLA / IDOR breach",
          "kind": "external",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "Bearer token + id",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "handler",
          "label": "authenticated principal",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "handler",
          "to": "db",
          "label": "load object",
          "kind": "data"
        },
        {
          "from": "handler",
          "to": "authz",
          "label": "owns this object?",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "authz",
          "to": "deny",
          "label": "no → deny",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "authz",
          "to": "leak",
          "label": "skip check →",
          "kind": "control",
          "dashed": true,
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "trusted principal flows from authn → handler"
        },
        {
          "tone": "warning",
          "label": "the authz boundary — enforced per object, every request"
        },
        {
          "tone": "muted",
          "label": "the failure path: object check skipped (BOLA)"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Closing the boundary in the handler, line by line",
      "subtitle": "The repaired endpoint: derive the principal from the token, load the object, deny if the principal does not own it — before returning anything.",
      "filename": "orders_handler.ts",
      "language": "ts",
      "code": "export async function getOrder(req: Request, res: Response) {\n  // principal is set by the authn middleware from the verified JWT — never the body\n  const principal = req.principal\n  if (!principal) return res.status(401).json({ error: 'unauthenticated' })\n\n  const orderId = Number(req.params.id)\n  const order = await db.orders.findById(orderId)\n  if (!order) return res.status(404).json({ error: 'not_found' })\n\n  // THE BOUNDARY: this principal must own THIS object\n  if (order.customerId !== principal.customerId) {\n    log.warn('authz_denied', { principal: principal.id, object: orderId })\n    return res.status(403).json({ error: 'forbidden' })\n  }\n\n  return res.status(200).json(toOrderDto(order))\n}",
      "steps": [
        {
          "lines": [
            3,
            4
          ],
          "label": "Principal from the token, never the body",
          "note": "The authn middleware sets req.principal from the verified JWT. If the handler reads customer_id from the request body or a query param, the client picks their own identity — instant privilege escalation."
        },
        {
          "lines": [
            7,
            8
          ],
          "label": "Load the object first, 404 if absent",
          "note": "You cannot authorize against an object you have not loaded. Return 404 (not 403) for missing rows so you do not leak which ids exist."
        },
        {
          "lines": [
            11
          ],
          "label": "The authorization boundary",
          "note": "order.customerId !== principal.customerId is the whole skill in one line: the authenticated principal compared to the specific object's owner. This runs on EVERY request — there is no cache, no skip."
        },
        {
          "lines": [
            12
          ],
          "label": "Log the denial as a security signal",
          "note": "An authz_denied log line with principal + object id is your observability hook: a spike of these is an enumeration attack in progress."
        },
        {
          "lines": [
            13
          ],
          "label": "403, not 404, on ownership failure",
          "note": "The object exists but you may not have it → 403 Forbidden. (Some teams return 404 to hide existence; either is defensible — picking deliberately is the judgment.)"
        },
        {
          "lines": [
            16
          ],
          "label": "Only now return the data",
          "note": "The DTO is emitted strictly after the boundary passes. No early return, no logging the body before the check — the boundary gates the response, not the other way around."
        }
      ],
      "caption": "Replay test: a valid token for customer 12 requests order 1044 (owned by customer 88) → 403. Same token requests their own order 1043 → 200. The token is identical; only the object check changes the answer."
    },
    {
      "type": "compare",
      "title": "Authenticated-only vs an enforced boundary",
      "subtitle": "Same endpoint, same valid token. One trusts the login; one re-checks the object every request.",
      "left": {
        "label": "Weak: trusts the token",
        "tone": "warning",
        "lines": [
          "Route sits behind login middleware — feels 'secured'",
          "Reads order id straight from the URL, returns the row",
          "No check that the order belongs to the caller",
          "customer_id sometimes read from request body (client-controlled)",
          "Cross-tenant request → 200 with a stranger's data"
        ],
        "verdict": "BOLA / IDOR — OWASP API #1, ships silently"
      },
      "right": {
        "label": "Gold: enforced auth boundary",
        "tone": "success",
        "lines": [
          "Principal derived from the verified JWT, never the body",
          "Object loaded, then principal-vs-owner check before acting",
          "Ownership mismatch → 403 + authz_denied log line",
          "Same rule on read, write, and delete — no exceptions",
          "Integration test proves a valid token is denied the wrong object"
        ],
        "verdict": "Authenticated ≠ authorized — the boundary holds per object"
      },
      "caption": "The weak version is indistinguishable from secure on the happy path — your own data always returns 200. The breach only appears when someone else's id is requested, which is exactly the test the weak version never wrote."
    },
    {
      "type": "callout",
      "tone": "note",
      "text": "OWASP grounds this: Broken Object-Level Authorization is API Security Top 10 #1, precisely because the happy-path test always passes and the boundary is one easy-to-omit line. The senior tell is testing authorization with a VALID token for the WRONG object — not an expired or missing one. Anyone tests the locked door; pros test whether a real key opens the wrong lock."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this endpoint isn't just happy-path-safe.' Which artifact actually proves the auth boundary holds?",
      "options": [
        "A test that an expired or missing token returns 401.",
        "A test where a fully valid token for customer A requests customer B's object and receives 403 — plus the server-side ownership check it exercises.",
        "A comment in the handler saying '// auth required'.",
        "Confirmation the route is registered behind the login middleware."
      ],
      "answer": 1,
      "explanation": "Only option 2 tests authorization rather than authentication. 401-on-bad-token (1) and login middleware (4) prove the door is locked — they say nothing about object-level access. The comment (3) is documentation, not enforcement. The boundary is only proven when a genuinely authenticated principal is denied an object it does not own; that 403 is the irreplaceable evidence."
    },
    {
      "type": "verification",
      "intro": "Prove the boundary holds — no vibes:",
      "items": [
        "The principal is read ONLY from the verified token, never from the request body, query string, or a client-supplied header. Grep the handler for customer_id and confirm it never comes from req.body.",
        "Every state-changing path (read, write, delete) runs the same principal-vs-object check — not just the GET. List the verbs and point to the check in each.",
        "An integration test sends a VALID token for one principal against another principal's object and asserts 403 (or 404-by-policy). If you only test 401 on bad tokens, you tested authentication, not authorization — add the cross-tenant case.",
        "The denial emits an observable signal (authz_denied log with principal + object id) so an enumeration attack is visible in metrics, not invisible.",
        "Missing-object and forbidden-object responses are deliberate (404 vs 403) and you can defend the choice — existence-hiding vs honest-forbidden is a decision, not an accident."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why is 'the route is behind login' not the same as 'the endpoint is authorized', and what is the one line that turns one into the other?",
        "Explain why reading the principal's customer_id from the request body instead of the token is a privilege-escalation hole, using your own endpoint as the example.",
        "Walk through the exact request that breaks the weak version and the exact response the boundary returns instead — name the status code and what gets logged."
      ]
    },
    {
      "type": "transfer",
      "text": "The same boundary travels across every backend surface in this course. A webhook must verify the signing secret AND that the event belongs to the account it claims. A billing endpoint must check the authenticated org owns the subscription it is mutating, or one tenant downgrades another. A queue worker that processes a job carrying a user_id must re-derive trust from the job's signed envelope, not believe the field. What stays invariant: re-establish the principal from a trusted source and check it against the specific object, every time. What changes is the principal's shape (user, service account, signed event) and the object (order, subscription, message). Carry api_contract_and_failure_repair_packet.md into the capstone — its 403 cross-tenant test is the reusable proof the capstone demands."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "persistence-repository-boundary": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint (or a realistic one) and put a Persistence Repository Boundary on it: the service talks to a repository interface, never to the ORM/SQL directly. Then write api_contract_and_failure_repair_packet.md proving the boundary holds under one realistic failure.",
      "intensity": "standard",
      "time": "30 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the repository interface, the service that depends on it (not on SQL), a contract test, an idempotency-replay test, and database rollback evidence. A reviewer can swap the storage implementation behind the interface without touching a line of service logic.",
      "unlock": "Your service depends on a repository INTERFACE; persistence details (SQL, ORM, transaction handling) live behind it; and a replayed request neither double-writes nor leaves a partial write.",
      "doNotClaim": "Do not claim you have a repository boundary if your service still imports the ORM, builds queries inline, or knows table names. Wrapping `db.query(...)` in a function called `repository` is not a boundary — it is a rename. The boundary is real only when the service cannot tell which database is underneath."
    },
    {
      "type": "mission",
      "text": "The endpoint worked in every demo. Then a client retried a timed-out POST /charges, your handler re-ran the INSERT, and one customer got billed twice — because the service was wired straight into the database with no boundary to catch the replay."
    },
    {
      "type": "context",
      "text": "A state-changing endpoint must persist exactly once, validate, authorize, and roll back cleanly under failure. The Persistence Repository Boundary is the seam that makes those guarantees the storage layer's job — not something smeared through your service code."
    },
    {
      "type": "pretest",
      "prompt": "Your order service calls `db.insert('orders', row)` directly inside the handler. Before we add anything — what is the real cost of that, beyond \"it's a bit ugly\"? Answer before reading on.",
      "reveal": "It fuses three concerns the database should own — transaction control, idempotency, and rollback — into business code that can't enforce them. With no boundary, every caller re-implements \"insert exactly once\" by hand, so a retry double-writes, a mid-write crash leaves a half-saved order, and you cannot test the service without a live database. The boundary isn't tidiness; it's where exactly-once actually lives."
    },
    {
      "type": "concept",
      "title": "A repository boundary is a storage interface the service depends on, not a query it runs",
      "text": "The service calls a repository INTERFACE (save, findById). Behind it, one implementation owns SQL, the transaction, and idempotency keys. The invariant: persistence details never leak across the boundary — swap the database and the service is unchanged."
    },
    {
      "type": "diagram",
      "title": "The persistence boundary on a state-changing endpoint",
      "subtitle": "Request flows handler → service → OrderRepository (interface) → Postgres. The repository owns the transaction, the idempotency key, and rollback. The ORM lives ONLY behind the boundary; the service never sees it.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "retries on timeout",
          "kind": "client"
        },
        {
          "id": "handler",
          "label": "API Handler",
          "description": "validate · authorize",
          "kind": "service"
        },
        {
          "id": "service",
          "label": "Order Service",
          "description": "business rules only",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "repo",
          "label": "OrderRepository",
          "description": "interface: save / findById",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "pg",
          "label": "Postgres",
          "description": "tx · idempotency key · rollback",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "orm",
          "label": "ORM / SQL",
          "description": "details kept behind boundary",
          "kind": "external",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "handler",
          "label": "POST /orders (retry)",
          "kind": "sync"
        },
        {
          "from": "handler",
          "to": "service",
          "label": "validated cmd",
          "kind": "sync"
        },
        {
          "from": "service",
          "to": "repo",
          "label": "depends on interface",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "repo",
          "to": "pg",
          "label": "save in one tx",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "repo",
          "to": "orm",
          "label": "uses (hidden)",
          "kind": "control",
          "dashed": true,
          "tone": "muted"
        },
        {
          "from": "service",
          "to": "orm",
          "label": "must NOT reach in",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "service ↔ repository interface (the boundary)"
        },
        {
          "tone": "success",
          "label": "where exactly-once + rollback live"
        },
        {
          "tone": "warning",
          "label": "the leak the boundary forbids"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Building the boundary, one piece at a time",
      "subtitle": "The interface the service depends on, the Postgres implementation that owns the transaction and idempotency key.",
      "filename": "order_repository.ts",
      "language": "ts",
      "code": "// 1. The boundary: the service knows ONLY this shape.\nexport interface OrderRepository {\n  findByIdempotencyKey(key: string): Promise<Order | null>\n  save(order: Order, idempotencyKey: string): Promise<Order>\n}\n\n// 2. Service depends on the interface — no SQL, no ORM, no table names.\nexport class OrderService {\n  constructor(private readonly orders: OrderRepository) {}\n\n  async place(cmd: PlaceOrder, idempotencyKey: string): Promise<Order> {\n    const existing = await this.orders.findByIdempotencyKey(idempotencyKey)\n    if (existing) return existing // replay → return prior result, no re-write\n    return this.orders.save(Order.fromCommand(cmd), idempotencyKey)\n  }\n}\n\n// 3. One implementation owns persistence: tx + idempotency + rollback.\nexport class PgOrderRepository implements OrderRepository {\n  constructor(private readonly db: Pool) {}\n\n  async save(order: Order, idempotencyKey: string): Promise<Order> {\n    const tx = await this.db.connect()\n    try {\n      await tx.query('BEGIN')\n      await tx.query(\n        'INSERT INTO orders (id, total, idempotency_key) VALUES ($1,$2,$3)',\n        [order.id, order.total, idempotencyKey],\n      )\n      await tx.query('COMMIT')\n      return order\n    } catch (err) {\n      await tx.query('ROLLBACK') // partial write is impossible past here\n      throw err\n    } finally {\n      tx.release()\n    }\n  }\n  // findByIdempotencyKey omitted for brevity\n}",
      "steps": [
        {
          "lines": [
            2,
            3,
            4,
            5
          ],
          "label": "The boundary itself",
          "note": "A pure interface — no `Pool`, no SQL, no `orders` table name. This is the contract the service is written against; everything else is swappable behind it."
        },
        {
          "lines": [
            8,
            9,
            10
          ],
          "label": "Service depends on the interface",
          "note": "`OrderService` takes an `OrderRepository`, never a database handle. You can hand it a Postgres impl in prod and a fake in tests — same service, zero changes."
        },
        {
          "lines": [
            13,
            14
          ],
          "label": "Idempotency check, not a re-insert",
          "note": "On a replayed request the service returns the prior order. This is the exactly-once guarantee, expressed in business terms, not SQL."
        },
        {
          "lines": [
            22,
            23,
            24,
            25,
            26,
            27,
            28
          ],
          "label": "The transaction lives behind the boundary",
          "note": "BEGIN → INSERT → COMMIT in one connection. The unique constraint on idempotency_key turns a concurrent double-submit into a caught error, not a double charge."
        },
        {
          "lines": [
            29,
            30,
            31
          ],
          "label": "Rollback makes partial writes impossible",
          "note": "Any failure past BEGIN rolls back the whole unit. The service never wrote rollback logic — it can't, and shouldn't. That's the boundary earning its place."
        }
      ],
      "caption": "A reviewer can delete PgOrderRepository, write a DynamoOrderRepository, and OrderService never notices. That is the test of a real boundary."
    },
    {
      "type": "compare",
      "title": "Same endpoint, two persistence styles",
      "subtitle": "Both \"save an order.\" Only one survives a retried POST and a mid-write crash.",
      "left": {
        "label": "ORM smeared through the service",
        "tone": "warning",
        "lines": [
          "Handler calls db.insert('orders', row) directly",
          "Transaction control? None — autocommit per query",
          "Retry re-runs the INSERT → double charge",
          "Crash mid-write → half-saved order, no rollback",
          "Can't unit-test without a live database"
        ],
        "verdict": "Exactly-once is hope, not a guarantee"
      },
      "right": {
        "label": "Repository boundary",
        "tone": "success",
        "lines": [
          "Service depends on OrderRepository interface",
          "save() owns BEGIN / COMMIT / ROLLBACK in one unit",
          "Idempotency key short-circuits the replay",
          "Failure rolls back the whole write — no partial state",
          "Fake repo in tests; swap Postgres → Dynamo untouched"
        ],
        "verdict": "Exactly-once + rollback enforced at the seam"
      },
      "caption": "The boundary doesn't make the code prettier — it moves transaction, idempotency, and rollback to the one place that can actually enforce them."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell isn't \"we use the repository pattern\" — it's where the transaction boundary sits. If a reviewer asks \"what happens when this exact POST arrives twice?\" and your answer points to a unique idempotency-key constraint inside one repository transaction, you have a boundary. If the answer is \"the client shouldn't retry,\" you have a leak with a nice interface bolted on top (OWASP API6: unrestricted access to business flows starts exactly here)."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: \"Prove this is a real persistence boundary, not just a renamed query.\" Which piece of evidence actually proves it?",
      "options": [
        "The class is named OrderRepository and lives in a /repositories folder.",
        "The OrderService is constructed with a fake repository in a unit test and passes with no database running, while a contract test runs the same interface against Postgres.",
        "Every method has a comment saying \"// repository pattern\".",
        "The repository method wraps a single db.query() call so the SQL is in one place."
      ],
      "answer": 1,
      "explanation": "A boundary is real only when the service is provably independent of storage. Constructing OrderService with a fake repo (no DB) AND running the same interface contract against Postgres proves both sides: the service depends on the interface, and the implementation honors it. Naming (1, 3) and a thin wrapper around one query (4) are cosmetics — the ORM and table knowledge can still leak straight through."
    },
    {
      "type": "verification",
      "intro": "Prove the boundary holds — no vibes:",
      "items": [
        "Grep your service file for the ORM import, raw SQL, and any table name. If any appear, persistence leaked across the boundary — push it behind the interface.",
        "Construct the service with a fake repository and run a unit test with NO database. If it can't run without a live DB, the dependency is on storage, not on the interface.",
        "Run the contract test: the same interface (save → findByIdempotencyKey) against real Postgres. The saved order must come back identical.",
        "Replay the exact same request twice (same idempotency key). Assert exactly one row exists and both calls return the same order — the idempotency-replay proof.",
        "Force a failure after BEGIN (throw before COMMIT) and assert zero rows persisted. That is your rollback evidence; record the row count in the packet."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does owning the transaction INSIDE the repository — not in the handler — make exactly-once enforceable instead of hopeful?",
        "Explain to a junior the difference between a repository boundary and a function that just wraps one SQL query. What can the first do that the second can't?",
        "Point at your own artifact: which line proves the service cannot tell whether Postgres or Dynamo is underneath?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same boundary governs every state-changing path, only the failure changes. A billing endpoint reuses the idempotency key so a retried charge bills once. A webhook handler dedupes on the provider event id behind the repository so a redelivered event doesn't double-process. A queue worker wraps consume-and-persist in one repository transaction so an at-least-once delivery becomes effectively-once. What stays invariant: the service depends on an interface, and transaction + idempotency + rollback live behind it. What changes: the idempotency key's source and the cost of a double-write. Your api_contract_and_failure_repair_packet.md carries straight into the capstone — it's the persistence proof the whole service path stands on."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "background-jobs-queue-basics": [
    {
      "type": "sprint-contract",
      "outcome": "Take one slow, state-changing endpoint (a /checkout that charges, emails, and fulfills inline) and move the slow work to a background job queue — enqueuing exactly once, returning fast with a job handle, and making the worker safe to retry. Capture the design in api_contract_and_failure_repair_packet.md.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the request/response contract (202 + job id), the enqueue-once boundary inside the DB transaction, the idempotent worker, the retry + dead-letter policy, plus a replayed-failure transcript (duplicate enqueue → one charge). A reviewer can trace one request from POST to a single side effect.",
      "unlock": "Your handler commits the job and the state row in ONE transaction, returns 202 with a job id, and the worker produces exactly one charge even when the same job runs twice.",
      "doNotClaim": "Do not claim you have a queue if you 'enqueue then commit' (or commit then enqueue) as two separate steps — that is a dual-write, and it drops or double-fires jobs on a crash between them. And do not claim idempotency from a try/catch: a worker that is merely retried is not idempotent unless re-running it changes nothing."
    },
    {
      "type": "mission",
      "text": "Black Friday, 11pm: /checkout charges the card, sends the receipt, and calls the fulfillment API — all inside the request, all 9 seconds. The load balancer times out at 8, the customer hits retry, and now you have charged them twice while the first request was still running."
    },
    {
      "type": "context",
      "text": "A background job queue is how a request says 'accepted' in 80ms and does the slow, failure-prone work later, off the request path. This lesson is the queues-and-jobs rung of Module 3: it sits on top of the request/response contract and the database transaction you already use, and it is the backbone of every webhook, billing flow, and async API you will ship."
    },
    {
      "type": "pretest",
      "prompt": "To make /checkout fast, you move the charge into a worker. Inside the handler you write `queue.enqueue(job)` and then `db.commit()`. The process crashes between those two lines. What just happened — and is the opposite order any safer?",
      "reveal": "You enqueued a job for an order the database never committed: the worker wakes up and charges a card for an order that doesn't exist. Flip the order — commit first, then enqueue — and a crash after commit leaves a committed order with no job: a silent no-charge. Either way you have a dual-write across two systems with no shared transaction. The fix isn't ordering; it's putting the job row in the SAME database transaction as the state change (the transactional outbox), so they commit or fail together — and a separate dispatcher hands committed rows to the queue."
    },
    {
      "type": "concept",
      "title": "Accept fast, do slow work once, make the worker safe to retry",
      "text": "Commit the state change and the job in one DB transaction (outbox), return 202 with a job id, let a worker do the slow work. The invariant: at-least-once delivery means the worker WILL run twice — so it must be idempotent. Retries that exhaust go to a dead-letter queue, never silently vanish."
    },
    {
      "type": "diagram",
      "title": "The checkout job pipeline — accept fast, charge once",
      "subtitle": "The handler commits the order AND the outbox job in one Postgres transaction, then returns 202. A dispatcher moves committed jobs to the queue; the worker charges through an idempotency key; exhausted retries land in the dead-letter queue. Email/search are out of scope.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "POST /checkout (Idempotency-Key)",
          "kind": "client"
        },
        {
          "id": "api",
          "label": "Checkout API",
          "description": "validate · authorize · 202 + job id",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "orders + outbox jobs (one txn)",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "queue",
          "label": "Job Queue",
          "description": "at-least-once delivery",
          "kind": "queue",
          "tone": "accent"
        },
        {
          "id": "worker",
          "label": "Charge Worker",
          "description": "idempotent · retried",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "psp",
          "label": "Payment API",
          "description": "external · idempotency key",
          "kind": "external"
        },
        {
          "id": "dlq",
          "label": "Dead-letter Queue",
          "description": "exhausted retries, never lost",
          "kind": "queue",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "api",
          "label": "request",
          "kind": "sync"
        },
        {
          "from": "api",
          "to": "db",
          "label": "order + job (1 txn)",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "api",
          "to": "client",
          "label": "202 + job id",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "db",
          "to": "queue",
          "label": "dispatch committed jobs",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "queue",
          "to": "worker",
          "label": "deliver (≥1×)",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "worker",
          "to": "psp",
          "label": "charge w/ idem key",
          "kind": "sync"
        },
        {
          "from": "worker",
          "to": "queue",
          "label": "retry on transient fail",
          "kind": "control",
          "dashed": true
        },
        {
          "from": "queue",
          "to": "dlq",
          "label": "after N retries",
          "kind": "async",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the async job path"
        },
        {
          "tone": "success",
          "label": "one transaction = no dual-write"
        },
        {
          "tone": "warning",
          "label": "dead-letter — failures parked, not dropped"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The enqueue-once handler and the idempotent worker",
      "subtitle": "Outbox enqueue inside the order transaction; worker charges through an idempotency key so a second run is a no-op.",
      "filename": "checkout_jobs.py",
      "language": "python",
      "code": "@app.post(\"/checkout\", status_code=202)\ndef checkout(req: CheckoutRequest, user: User = Depends(auth)):\n    validate(req)                       # boundary: reject bad input early\n    with db.begin() as tx:              # ONE transaction for both writes\n        order = tx.insert_order(user.id, req.cart, status=\"pending\")\n        tx.insert_job(                  # transactional outbox row\n            kind=\"charge_order\",\n            order_id=order.id,\n            idem_key=f\"charge:{order.id}\",\n            payload={\"amount\": req.total},\n        )\n    return {\"order_id\": order.id, \"status\": \"accepted\"}\n\ndef handle_charge_order(job):           # worker — runs AT LEAST once\n    order = db.get_order(job.order_id)\n    if order.status == \"charged\":        # idempotency guard: already done\n        return                          # second delivery is a no-op\n    try:\n        psp.charge(                      # external call keyed by idem_key\n            amount=job.payload[\"amount\"],\n            idempotency_key=job.idem_key,\n        )\n        db.set_order_status(order.id, \"charged\")\n    except TransientError:\n        raise Retry(backoff=\"exponential\", max=5)   # re-queued; >5 -> DLQ",
      "steps": [
        {
          "lines": [
            3
          ],
          "label": "Validate at the boundary",
          "note": "Reject malformed/over-limit carts before any write. Never trust the request body — this is the auth + validation rung this lesson stands on."
        },
        {
          "lines": [
            4,
            5,
            6,
            7,
            8,
            9,
            10,
            11
          ],
          "label": "Order + job in ONE transaction",
          "note": "The outbox: the job row lives in the same DB commit as the order. A crash rolls back both, or commits both. No dual-write, no orphan job, no silent no-charge."
        },
        {
          "lines": [
            12
          ],
          "label": "Return 202, not 200",
          "note": "202 Accepted means 'the work is queued, not done.' The client gets an order id to poll — the request path stays under 100ms instead of 9 seconds."
        },
        {
          "lines": [
            16,
            17,
            18
          ],
          "label": "Idempotency guard first",
          "note": "The queue delivers at least once, so this handler WILL run twice. Checking status == 'charged' makes the second run a no-op — this is what 'idempotent' actually buys you."
        },
        {
          "lines": [
            20,
            21,
            22,
            23
          ],
          "label": "Key the external charge too",
          "note": "Even if the guard races, the payment API's own idempotency key collapses two charge calls into one. Defense in depth: never let a retry become a double-charge."
        },
        {
          "lines": [
            25,
            26
          ],
          "label": "Retry, then dead-letter",
          "note": "Transient failures (timeout, 503) re-queue with exponential backoff; after 5 attempts the job goes to the DLQ where an operator can inspect it. Failures are parked for evidence, never swallowed."
        }
      ],
      "caption": "Trace it: two identical deliveries of charge:1042 → first charges, second returns at the guard → exactly one charge. That transcript is the proof the packet must show."
    },
    {
      "type": "compare",
      "title": "Inline handler vs queued, idempotent worker",
      "subtitle": "Same /checkout, same Black-Friday retry storm. Only one survives a timeout without double-charging.",
      "left": {
        "label": "Inline + dual-write (weak)",
        "tone": "warning",
        "lines": [
          "Charge + email + fulfill run inside the request (9s)",
          "Load balancer times out at 8s; client retries",
          "Two in-flight requests both charge the card",
          "queue.enqueue() then db.commit() — crash drops the job",
          "On failure: try/catch logs and returns 200 anyway",
          "No retry, no dead-letter — failed orders vanish silently"
        ],
        "verdict": "Times out, double-charges, loses failures"
      },
      "right": {
        "label": "Outbox + idempotent worker (gold)",
        "tone": "success",
        "lines": [
          "Handler validates, commits order+job in one txn, returns 202 in 80ms",
          "Retry hits the same order id → same idempotency key",
          "Worker guard sees status='charged' → second run is a no-op",
          "Payment API idem key collapses any racing charge to one",
          "Transient fail → exponential backoff, max 5 retries",
          "Exhausted → dead-letter queue, operator-inspectable"
        ],
        "verdict": "Accepts fast, charges once, parks failures"
      },
      "caption": "The weak version's bug is invisible until load: it 'works' in dev and double-charges in production. At-least-once delivery is not a flaw to avoid — it is the contract you design the worker against."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell: they design for at-least-once, never exactly-once. True exactly-once delivery across a network is effectively impossible (Two Generals Problem) — so make every worker idempotent and let the queue redeliver freely. 'I'll just make sure it only fires once' is the sentence that precedes every double-charge incident. A double-charge is a data-integrity / idempotency failure, not something the OWASP API Top 10 names directly."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence that this worker is safe, not just a happy-path answer.' Which artifact actually proves it?",
      "options": [
        "A screenshot of one successful /checkout returning 202.",
        "A replay transcript where the SAME job is delivered twice and the order is charged exactly once, plus a dead-letter entry from an exhausted-retry run.",
        "A comment in the code that says `# this is idempotent`.",
        "A load test showing the endpoint returns in under 100ms."
      ],
      "answer": 1,
      "explanation": "Idempotency and durability are claims about the FAILURE path, so the proof must exercise it: a duplicate-delivery replay that yields one side effect proves the guard works, and a dead-letter entry proves exhausted retries are parked, not dropped. A happy-path 202 (option 0), a comment (2), or a latency number (3) never touch the second delivery or the failure — exactly where the danger lives."
    },
    {
      "type": "verification",
      "intro": "Prove the queue holds — replay the failure, no vibes:",
      "items": [
        "Enqueue + state change commit in ONE transaction: kill the process between the two writes and confirm you never get a job without an order (or an order without a job).",
        "Deliver the same job twice and confirm exactly one charge — capture the transcript; the second run must hit the idempotency guard and no-op.",
        "Confirm the external charge call carries its own idempotency key, so a race past the guard still collapses to one charge.",
        "Force a transient failure 6 times and confirm the job lands in the dead-letter queue — not retried forever, not silently dropped.",
        "Confirm the handler returns 202 with a job/order id (not 200) and the request path stays off the slow work — measure the latency before and after."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why 'enqueue then commit' is a dual-write, and how the transactional outbox removes the crash window — using your own /checkout as the example.",
        "Your queue guarantees at-least-once delivery. In your own words, why does that make idempotency mandatory rather than optional for the worker?",
        "What is in your dead-letter queue, who looks at it, and what would happen to those orders if it didn't exist?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same three moves — accept fast, do slow work once, make the worker retry-safe — are the spine of every async backend surface. A Stripe webhook handler must be idempotent because Stripe redelivers events; an email/SMS job must dedupe on a key so a retry doesn't double-send; a video-transcode or AI-inference endpoint returns 202 + a job id and streams the result later. What stays invariant: one transactional enqueue, an idempotency key, a dead-letter for the exhausted. What changes with context: the backoff curve (a $0.50 email tolerates more retries than a $5,000 charge), whether you need ordering (FIFO) or just throughput, and how long results live. Carry api_contract_and_failure_repair_packet.md into the capstone — the service that ships there reuses this exact worker and its replay proof unchanged."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "worker-state-retries-dlq": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing job your worker processes (charge a card, send an email, fulfill an order) and make it survive retries safely: idempotent on replay, bounded in attempts, and routed to a dead-letter queue when it cannot succeed — with operator evidence for every poisoned message.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "Section of api_contract_and_failure_repair_packet.md covering the worker: the idempotency key, the retry/backoff policy with a max-attempts cap, the DLQ routing rule, and an idempotency-replay test showing the SAME message delivered twice produces ONE side effect. A reviewer can replay a poison message and watch it land in the DLQ instead of looping forever.",
      "unlock": "Your worker is idempotent on redelivery, caps retries, and dead-letters poison messages with a captured failure reason — proven by a replay test, not asserted.",
      "doNotClaim": "Do not claim the worker is 'retry-safe' because the happy path passes. At-least-once delivery means every message can arrive twice; if a duplicate double-charges or a permanent failure retries forever, you have not handled retries — you have only handled success."
    },
    {
      "type": "mission",
      "text": "A queue worker charges a card, then crashes before it can ack the message. The broker, doing exactly its job, redelivers — and the customer is charged twice. By morning there are forty duplicate charges and one message that has retried itself 9,000 times, pinning a worker no one can find."
    },
    {
      "type": "context",
      "text": "Queues hand you at-least-once delivery: every message can be redelivered, so a worker that is not idempotent turns reliability into duplication. This lesson makes a state-changing job safe to replay, bounded in retries, and observable when it finally gives up — the worker section of the failure-repair packet you carry through the module."
    },
    {
      "type": "pretest",
      "prompt": "Your broker promises 'at-least-once' delivery and your worker charges a card. Where exactly does the double-charge come from — the broker, the network, or your code? Answer before reading on.",
      "reveal": "Your code. At-least-once is a guarantee, not a bug: the broker MUST redeliver if it never sees an ack, and acks get lost to crashes and timeouts all the time. The duplicate is the system working as designed. The only place to absorb it is the consumer — an idempotency key that makes the second delivery a no-op. 'Exactly-once processing' is something you build at the worker, not something the queue gives you."
    },
    {
      "type": "concept",
      "title": "Idempotent worker + bounded retries + a dead-letter queue",
      "text": "Make the job safe to run twice (idempotency key), retry transient failures with backoff up to a cap, and route anything still failing to a DLQ with its reason. The invariant: redelivery changes nothing; permanent failure is parked, never looped."
    },
    {
      "type": "diagram",
      "title": "The worker lifecycle: ack, retry, or dead-letter",
      "subtitle": "A message enters, the worker checks the idempotency store, does the work, and acks. Transient failures requeue with backoff; once attempts hit the cap, the message is dead-lettered with its reason — never retried forever.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "queue",
          "label": "Job Queue",
          "description": "at-least-once delivery",
          "kind": "queue",
          "tone": "accent"
        },
        {
          "id": "worker",
          "label": "Worker",
          "description": "consumes one message",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "idem",
          "label": "Idempotency Store",
          "description": "seen this key? → skip side effect",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "work",
          "label": "Do the work",
          "description": "charge card · write DB row",
          "kind": "process"
        },
        {
          "id": "decision",
          "label": "Succeeded?",
          "description": "ack · retry · give up",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "dlq",
          "label": "Dead-Letter Queue",
          "description": "parked + reason + payload",
          "kind": "queue",
          "tone": "warning"
        },
        {
          "id": "alert",
          "label": "Alert / Dashboard",
          "description": "operator evidence",
          "kind": "external",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "queue",
          "to": "worker",
          "label": "deliver (maybe twice)",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "worker",
          "to": "idem",
          "label": "check key",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "idem",
          "to": "work",
          "label": "first time → run",
          "kind": "control"
        },
        {
          "from": "idem",
          "to": "decision",
          "label": "duplicate → ack, skip",
          "kind": "control",
          "dashed": true,
          "tone": "success"
        },
        {
          "from": "work",
          "to": "decision",
          "label": "result",
          "kind": "data"
        },
        {
          "from": "decision",
          "to": "queue",
          "label": "transient → requeue w/ backoff",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "decision",
          "to": "dlq",
          "label": "attempts ≥ max → dead-letter",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "dlq",
          "to": "alert",
          "label": "surface to operator",
          "kind": "control",
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the delivery path (may redeliver)"
        },
        {
          "tone": "success",
          "label": "idempotency — duplicate becomes a no-op"
        },
        {
          "tone": "warning",
          "label": "retry-or-give-up — the DLQ ends the loop"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "An idempotent worker that dead-letters instead of looping",
      "subtitle": "The handler from the failure-repair packet — keyed, capped, and routed. Each step is a forcing function.",
      "filename": "worker.py",
      "language": "python",
      "code": "MAX_ATTEMPTS = 5\n\ndef handle(message):\n    key = message.idempotency_key      # stable per real-world event\n    attempt = message.delivery_count   # broker-tracked, survives redelivery\n\n    if store.already_processed(key):\n        return ack(message)            # duplicate: no second side effect\n\n    try:\n        with db.transaction():\n            charge_card(message.amount, key)\n            store.mark_processed(key)  # commit work + key atomically\n        return ack(message)\n    except TransientError as e:\n        if attempt >= MAX_ATTEMPTS:\n            return dead_letter(message, reason=str(e))\n        return requeue(message, delay=backoff(attempt))\n    except PermanentError as e:\n        return dead_letter(message, reason=str(e))  # never retry a NON-RETRYABLE 4xx (400/401/403/404/422)",
      "steps": [
        {
          "lines": [
            1
          ],
          "label": "Cap the retries",
          "note": "A named max. Without it, a poison message retries forever and quietly burns a worker — the 9,000-attempt failure from the mission."
        },
        {
          "lines": [
            4
          ],
          "label": "A STABLE idempotency key",
          "note": "Derived from the real event (order_id + attempt-of-intent), NOT a random UUID per delivery — or every redelivery looks new and the guard never fires."
        },
        {
          "lines": [
            5
          ],
          "label": "Use the broker's delivery count",
          "note": "delivery_count is tracked by the broker and survives crashes; an in-memory counter resets every restart and never reaches the cap."
        },
        {
          "lines": [
            7,
            8
          ],
          "label": "Check before the side effect",
          "note": "If we've already processed this key, ack and return. The duplicate becomes a no-op — this is 'exactly-once processing' built at the consumer."
        },
        {
          "lines": [
            11,
            12,
            13
          ],
          "label": "Commit work AND key together",
          "note": "Charge and mark-processed in ONE transaction. If they can split, a crash between them either double-charges or forgets the key. Atomicity is the whole game."
        },
        {
          "lines": [
            15,
            16,
            17,
            18
          ],
          "label": "Transient → backoff, then DLQ at the cap",
          "note": "A timeout deserves a retry with growing delay; but past MAX_ATTEMPTS it stops being transient and gets parked with its reason."
        },
        {
          "lines": [
            19,
            20
          ],
          "label": "Permanent → dead-letter immediately",
          "note": "Most 4xx (malformed payload — 400/401/403/404/422) fail identically forever — dead-letter them. But 429 Too Many Requests and 408 Request Timeout are TRANSIENT 4xx: retry them with backoff (honor Retry-After for 429), don't dead-letter on first sight."
        }
      ],
      "caption": "Replay the same message twice: the first charges and marks the key; the second sees the key, acks, and skips. One side effect, two deliveries — the proof the sprint demands."
    },
    {
      "type": "compare",
      "title": "Naive retry loop vs idempotent-and-bounded worker",
      "subtitle": "Same job — 'charge the card and record it' — under a broker that redelivers. Only one survives a duplicate and a poison message.",
      "left": {
        "label": "Retry on a hunch",
        "tone": "warning",
        "lines": [
          "No idempotency key — every delivery runs the charge",
          "Redelivery after a lost ack → customer charged twice",
          "Catches all errors and requeues — no max attempts",
          "Permanent 4xx retries forever, pinning a worker",
          "Failure vanishes: no DLQ, no reason, no alert"
        ],
        "verdict": "At-least-once becomes at-least-twice-charged"
      },
      "right": {
        "label": "Idempotent + bounded + dead-lettered",
        "tone": "success",
        "lines": [
          "Stable key checked before any side effect",
          "Work + key committed in one transaction",
          "Transient errors back off; cap at MAX_ATTEMPTS",
          "Permanent errors dead-letter on first sight",
          "DLQ holds payload + reason → operator can replay"
        ],
        "verdict": "Replay-safe, loop-free, and observable"
      },
      "caption": "The teaching signal: idempotency absorbs the duplicate, the cap ends the loop, and the DLQ turns a silent failure into a reviewable artifact a human can act on."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell from reliability engineering (AWS SQS dead-letter queues, the Enterprise Integration Patterns 'Dead Letter Channel', general SRE practice): a poison message must FAIL LOUDLY, not silently retry. A DLQ with the payload and the failure reason is operator evidence — it converts '3 a.m. mystery' into 'here is the exact message that broke and why.' A worker that swallows permanent failures is not resilient; it is hiding the incident until it is bigger."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: 'Show me the exact evidence this worker is not just a happy-path answer.' Which proof actually demonstrates the skill?",
      "options": [
        "A unit test showing one valid message is charged and acked successfully.",
        "An idempotency-replay test where the SAME message is delivered twice and asserts exactly one charge, plus a poison message landing in the DLQ after the attempt cap with its reason captured.",
        "A log line confirming the worker started and connected to the queue.",
        "A comment in the handler saying '# idempotent, retries handled'."
      ],
      "answer": 1,
      "explanation": "The skill lives in the failure paths. Only option 2 proves the two invariants that matter: redelivery causes one side effect (idempotency under at-least-once), and a permanent failure stops looping and becomes operator-visible (bounded retries + DLQ). The happy-path test (1), a startup log (3), and a comment (4) never exercise the duplicate or the poison message — exactly where naive workers break."
    },
    {
      "type": "verification",
      "intro": "Prove the worker holds — replay it, don't trust it:",
      "items": [
        "Deliver the SAME message twice and assert exactly one side effect (one charge, one DB row). If two fire, your idempotency key is not stable across redeliveries.",
        "Confirm the work and the idempotency-key write commit in ONE transaction. Crash between them in a test; the system must be either fully done or fully not-done — never half.",
        "Send a permanently-failing message (malformed / 4xx) and confirm it dead-letters on the FIRST failure, not after the full retry budget.",
        "Send a transiently-failing message and confirm attempts back off and then stop at MAX_ATTEMPTS — count the deliveries; they must be bounded.",
        "Open the DLQ entry and confirm it carries the original payload AND a human-readable reason. If a reason is missing, an operator cannot triage it."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain to a teammate why 'exactly-once delivery' is a myth but 'exactly-once processing' is achievable — using your own worker's idempotency key as the example.",
        "Why must the side effect and the idempotency-key write share one transaction? Describe the bug that appears if a crash can land between them.",
        "Walk through what happens to a poison message in your design, from first failure to the DLQ, and name the exact evidence an on-call engineer would see."
      ]
    },
    {
      "type": "transfer",
      "text": "The same three moves govern every at-least-once consumer, not just a card charge. A webhook receiver dedupes on the provider's event ID so a retried Stripe webhook fulfills an order once; a billing job keys on the invoice period so a rerun never double-bills; an AI-service backend caps retries on a flaky model call and dead-letters the prompt that always 500s instead of looping the bill. What stays invariant: redelivery is free, permanent failure is parked, and every poison message leaves evidence. What changes is the key source and the cost of a duplicate — higher stakes (money, data loss) demand a stricter transaction boundary. This worker section now plugs straight into the capstone's api_contract_and_failure_repair_packet.md: the same idempotency-replay test and DLQ-routing proof are the evidence the capstone reuses unchanged."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "event-vs-command": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint you own (or a realistic checkout/billing path) and split its messaging into a Command (imperative, one handler, rejectable, idempotency-keyed) and the Event it emits (past-tense fact, zero-to-many subscribers, immutable). Write the contract for both in `api_contract_and_failure_repair_packet.md`: the command schema with its idempotency key and auth boundary, the event schema with its version, and the exactly-once rule that links them.",
      "intensity": "deep",
      "time": "30 min",
      "proof": "The `api_contract_and_failure_repair_packet.md` artifact: a command contract (imperative name, target handler, rejection cases, idempotency key) AND an event contract (past-tense name, immutable payload, schema version, subscriber list), plus the rule stating the command is processed exactly once and the event is the fact it leaves behind. A reviewer can tell, from the names alone, which message can be rejected and which cannot.",
      "unlock": "Your packet names a command (imperative, rejectable, single handler, idempotency-keyed) and the event it emits (past-tense, immutable, fan-out) — and states how a retried command does not double-charge.",
      "doNotClaim": "Do not claim you have separated event from command if your event name is imperative (`SendEmail`, `UpdateInventory`). That is a command wearing an event's clothes — it presumes one obedient handler and smuggles coupling back in. An event is a fact in the past tense (`OrderPlaced`); whoever reacts is none of its business."
    },
    {
      "type": "mission",
      "text": "A retry storm hits at 2 a.m.: the mobile client times out on checkout and fires the same POST four times. One service treated 'charge the card' as a fact to broadcast instead of a command to run once — and four payment captures land on one cart. The on-call is now writing refund scripts because nobody drew the line between asking and announcing."
    },
    {
      "type": "context",
      "text": "This lesson sits in Backend Engineering's production-pressure phase, where request/response contracts meet queues, jobs, and idempotency. Confusing the request to DO something (a command) with the announcement that it HAPPENED (an event) is the root of double-charges, lost auth boundaries, and unfixable fan-out — Section 1 of the `api_contract_and_failure_repair_packet.md` you carry into the capstone."
    },
    {
      "type": "pretest",
      "prompt": "Your service publishes a message named `ProcessRefund` onto a topic that three services subscribe to. Before reading on: what breaks the first time a fourth service subscribes?",
      "reveal": "You get three refunds, then four. `ProcessRefund` is an imperative — a command — and a command has exactly one handler that can accept or reject it. By putting it on a fan-out topic you handed every subscriber a license to act, so each new subscriber is a new side effect you never authorized. The fact you actually meant to broadcast is `RefundIssued` (past tense): it already happened, it cannot be rejected, and ten subscribers reacting to it is fine because reacting is not re-doing. The name was the bug."
    },
    {
      "type": "concept",
      "title": "A command is a request to act; an event is a fact that it acted",
      "text": "Command: imperative name, one handler, can be rejected, must run exactly once (idempotency key). Event: past-tense name, immutable, zero-to-many subscribers, cannot be rejected. The invariant: commands are processed, events are observed. If a 'fact' can be refused or must run once, it is a command."
    },
    {
      "type": "diagram",
      "title": "Command in, event out — the exactly-once seam",
      "subtitle": "The `ChargeCard` command crosses the auth boundary to a single handler, is deduped by idempotency key, writes the charge once, then emits the immutable `PaymentCaptured` fact to whoever cares. Receipts, ledger, and analytics subscribe to the fact — they never re-run the charge.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "checkout, may retry",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "auth boundary",
          "kind": "service",
          "tone": "warning"
        },
        {
          "id": "handler",
          "label": "Payments handler",
          "description": "the ONE command handler",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "idem",
          "label": "Idempotency store",
          "description": "key seen? reject replay",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "charge row, txn",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "bus",
          "label": "Event bus",
          "description": "PaymentCaptured (fact)",
          "kind": "queue",
          "tone": "success"
        },
        {
          "id": "receipts",
          "label": "Receipts",
          "description": "subscriber",
          "kind": "service"
        },
        {
          "id": "ledger",
          "label": "Ledger",
          "description": "subscriber",
          "kind": "service"
        },
        {
          "id": "analytics",
          "label": "Analytics",
          "description": "subscriber",
          "kind": "external",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "ChargeCard (cmd)",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "handler",
          "label": "authorized cmd",
          "kind": "sync",
          "tone": "warning"
        },
        {
          "from": "handler",
          "to": "idem",
          "label": "dedupe by key",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "idem",
          "to": "handler",
          "label": "first time only",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "handler",
          "to": "db",
          "label": "charge once (txn)",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "handler",
          "to": "bus",
          "label": "emit PaymentCaptured",
          "kind": "async",
          "tone": "success"
        },
        {
          "from": "bus",
          "to": "receipts",
          "label": "observe",
          "kind": "async"
        },
        {
          "from": "bus",
          "to": "ledger",
          "label": "observe",
          "kind": "async"
        },
        {
          "from": "bus",
          "to": "analytics",
          "label": "observe",
          "kind": "async",
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "warning",
          "label": "auth boundary — commands cross it, events do not"
        },
        {
          "tone": "accent",
          "label": "single handler + idempotency = exactly once"
        },
        {
          "tone": "success",
          "label": "the immutable fact, fanned out to subscribers"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Writing the contract: one command, one fact",
      "subtitle": "The handler in `api_contract_and_failure_repair_packet.md` — reject the replay, charge in a transaction, emit the past-tense fact.",
      "filename": "payments_handler.ts",
      "language": "ts",
      "code": "// COMMAND: imperative, one handler, rejectable, idempotency-keyed\ntype ChargeCard = {\n  idempotencyKey: string   // dedupe retries of the SAME intent\n  cartId: string\n  amountCents: number\n  actorId: string          // auth boundary: who is allowed to ask\n}\n\n// EVENT: past tense, immutable fact, schema-versioned, fan-out\ntype PaymentCaptured = {\n  schema: 'payment.captured.v1'\n  chargeId: string\n  cartId: string\n  amountCents: number\n  capturedAt: string\n}\n\nasync function handleChargeCard(cmd: ChargeCard): Promise<PaymentCaptured> {\n  const seen = await idem.get(cmd.idempotencyKey)\n  if (seen) return seen.event          // replay: return prior fact, do NOT recharge\n\n  return await db.tx(async (t) => {\n    const charge = await t.charge(cmd.cartId, cmd.amountCents, cmd.actorId)\n    const event: PaymentCaptured = {\n      schema: 'payment.captured.v1',\n      chargeId: charge.id, cartId: cmd.cartId,\n      amountCents: cmd.amountCents, capturedAt: new Date().toISOString(),\n    }\n    await idem.put(cmd.idempotencyKey, { event })  // same txn as the charge\n    await bus.emit(event)               // announce the fact; subscribers observe\n    return event\n  })\n}",
      "steps": [
        {
          "lines": [
            1,
            2,
            3,
            4,
            5,
            6,
            7
          ],
          "label": "The command type",
          "note": "Imperative name `ChargeCard`. It carries an idempotencyKey (so retries collapse), an actorId (the auth boundary — who may ask), and exactly the inputs the ONE handler needs."
        },
        {
          "lines": [
            9,
            10,
            11,
            12,
            13,
            14,
            15,
            16
          ],
          "label": "The event type",
          "note": "Past tense `PaymentCaptured`, a versioned schema string, and only facts that are already true. No instructions, no recipient — whoever subscribes is not the event's concern."
        },
        {
          "lines": [
            19,
            20
          ],
          "label": "Reject the replay",
          "note": "The exactly-once seam. A retried command with a seen key returns the prior fact and does NOT charge again. This single check is what stops the 2 a.m. double-charge."
        },
        {
          "lines": [
            22,
            23,
            24
          ],
          "label": "Charge once, in a transaction",
          "note": "The state change is the command's job and the command's alone — one handler, one row, atomic."
        },
        {
          "lines": [
            29
          ],
          "label": "Persist the key WITH the charge",
          "note": "idem.put and the charge share the transaction, so a crash can never leave 'charged but not deduped' — the failure that recreates the double-charge."
        },
        {
          "lines": [
            30
          ],
          "label": "Emit the fact, then let go",
          "note": "bus.emit announces `PaymentCaptured`. Receipts, ledger, analytics observe it independently. The handler does not know or care who reacts — that decoupling is the whole point of an event."
        }
      ],
      "caption": "Run it twice with the same idempotencyKey: one charge, one `PaymentCaptured`, second call returns the first fact. Found exactly-once with one store and one transaction."
    },
    {
      "type": "compare",
      "title": "Same checkout, two designs",
      "subtitle": "Trigger: 'when a cart is paid, charge the card and notify everyone.' Only one survives a retry and a new subscriber.",
      "left": {
        "label": "Imperative event (fan-out command)",
        "tone": "warning",
        "lines": [
          "Publishes `ChargeCard` onto a fan-out topic",
          "Every subscriber runs the charge — N handlers",
          "Past/present tense ignored: a command is broadcast",
          "No idempotency key: each retry = another charge",
          "Adding a subscriber adds a side effect (and a bill)",
          "Auth boundary lost — anyone on the bus can 'ask'"
        ],
        "verdict": "Double-charges on retry, multiplies on every new subscriber"
      },
      "right": {
        "label": "Command in, event out",
        "tone": "success",
        "lines": [
          "`ChargeCard` is a command to ONE handler past the gateway",
          "Idempotency key collapses retries to one charge",
          "Handler emits `PaymentCaptured` — a past-tense fact",
          "Subscribers observe the fact; they never re-charge",
          "New subscriber = new reaction, zero new side effects",
          "Auth checked once, at the boundary the command crosses"
        ],
        "verdict": "Exactly one charge; fan-out is free and safe"
      },
      "caption": "The tell is the verb tense in the name. `Charge` (imperative) must be a command with one handler; `Captured` (past) is a fact anyone may observe. Reject-able and run-once ⇒ command. Already-true and fan-out ⇒ event.",
      "mono": true
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "Standards grounding: the most common production failure here is a retried write without an idempotency key — a command that turns every client timeout into duplicate state changes (duplicate charge). That is a business-logic idempotency flaw; the retry *volume* itself can separately trip OWASP API4:2023 'Unrestricted Resource Consumption', but the duplicate-charge harm is an idempotency defect, not a resource-consumption one. The senior tell, and a DORA change-failure-rate lever: the auth boundary lives on the COMMAND (one place to check 'who may ask'), never on the event — facts are not authorized, they simply happened."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this isn't just a happy-path answer — what proves a retried checkout doesn't double-charge?' Which design + proof actually answers that?",
      "options": [
        "The event is named `PaymentCaptured` and there are three subscribers, so it scales.",
        "`ChargeCard` is a command with an idempotency key persisted in the SAME transaction as the charge, and an idempotency-replay test shows the second call returns the prior `PaymentCaptured` with no new charge row.",
        "The message is published to a queue, and queues guarantee delivery so retries are safe.",
        "The handler logs every request, so duplicates can be found and refunded later."
      ],
      "answer": 1,
      "explanation": "Exactly-once is a property of the command path, proven by a replay test, not by the event's name or the queue's delivery guarantee. Option 1 describes fan-out (an event virtue, irrelevant to double-charge). Option 3 confuses at-least-once delivery with exactly-once processing — the queue will happily redeliver. Option 4 detects the failure after money moved. Only option 2 binds the key to the charge transactionally and shows the replay returning the prior fact with zero new state."
    },
    {
      "type": "verification",
      "intro": "Prove the command/event split in your packet holds — no vibes:",
      "items": [
        "Read each message name aloud. Imperative (`Charge`, `Send`, `Process`) ⇒ it must be a command with exactly one handler. Past tense (`Captured`, `Sent`, `Processed`) ⇒ it must be an immutable event. A mismatch is the bug.",
        "Point to the idempotency key on every command that changes state, and confirm it is persisted in the same transaction as the change — a contract test or schema check, not a comment.",
        "Run the idempotency replay: fire the command twice with one key; assert one state row and that the second call returns the first event. Capture the transcript as evidence.",
        "Confirm the event payload contains only facts already true (no instructions, no recipient) and carries a schema version, so a new subscriber is a no-risk addition.",
        "Confirm the auth boundary is checked on the command's entry, not on the event — and that no subscriber can trigger the state change by reacting."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words, name the four properties that make a message a command and the four that make it an event — and which single property (rejectable? run-once?) decides a borderline case like `ProcessRefund`.",
        "Walk a junior through your `api_contract_and_failure_repair_packet.md`: where does the idempotency key live, why is it in the charge transaction, and what exact failure happens if you move it outside?",
        "Explain why putting the auth boundary on the event instead of the command is a security hole, using your own contract as the example."
      ]
    },
    {
      "type": "transfer",
      "text": "The same split governs every state-changing path. A webhook: the inbound `SubscriptionCreated` from Stripe is a FACT you observe (verify signature, dedupe by event id, never reject the past); your own `ProvisionAccount` is a command you run once. A queue worker: the job message is a command (one consumer, idempotency-keyed, can dead-letter on rejection); what it emits when done is an event. An AI service backend: `GenerateSummary` is a command (rejectable, rate-limited, run-once per request id); `SummaryGenerated` is the fact your UI and billing observe. What stays invariant: commands are authorized, run exactly once, and can be refused; events are immutable facts that fan out freely. What changes with scale, cost, and latency: where you dedupe (gateway vs handler vs store) and whether the event bus is in-process or a real broker. Carry this packet into the capstone — its command contract, idempotency proof, and event schema are reused verbatim when the production service path is assembled."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "observability-request-ids-logs-metrics": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real (or realistic) state-changing API endpoint and make it observable: stamp every request with a request ID that flows through structured logs, emit the three metrics that matter (rate, errors, duration), and prove you can answer \"what happened to request X?\" from logs alone. Capture it in api_contract_and_failure_repair_packet.md.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "The Observability section of api_contract_and_failure_repair_packet.md: a request-ID middleware that generates/propagates an X-Request-ID, log lines that are structured JSON carrying that ID, and three named metrics (request rate, error rate, p50/p99 latency). An on-call peer is handed a request ID and reconstructs that request's full path through your service — from your logs, without you narrating.",
      "unlock": "Every request carries a propagated request ID, your logs are structured and queryable by that ID, and you emit the RED metrics (Rate, Errors, Duration) — not just a count of 200s.",
      "doNotClaim": "Do not claim a service is \"observable\" because it has print statements. Unstructured logs you cannot filter, a request ID that dies at your service boundary, or a dashboard that only shows total request count are not observability — they are noise that looks like diligence. Observability means you can answer a question you did not pre-plan, about one specific request, after the incident."
    },
    {
      "type": "mission",
      "text": "3 a.m. page: \"checkout is failing for some users.\" One engineer greps a million lines of \"Error: something went wrong\" and finds nothing. The other pastes the customer's request ID into the log search, watches the request flow gateway → orders → payment → 504 from a downstream timeout, and ships the fix before the first finishes scrolling."
    },
    {
      "type": "context",
      "text": "In production pressure, the question is never \"is the service up?\" — it's \"what happened to THIS request, and how bad is it across all of them?\" Request IDs make a single request reconstructable; structured logs make logs queryable; the RED metrics turn \"some users\" into a number you can alert on."
    },
    {
      "type": "pretest",
      "prompt": "Your API handler runs `print(\"order failed\")` when something breaks, and your dashboard shows total requests per minute. A customer reports their order failed two hours ago. Can you find what happened to their specific request? Answer before reading on.",
      "reveal": "Almost certainly not. `print(\"order failed\")` has no request ID, no timestamp you trust, no user, no error class — under load it's one indistinguishable line among thousands of identical ones, with no way to isolate ONE request. And a total-requests graph hides the failures inside the success volume: 0.5% errors are invisible next to 99.5% of 200s. You have logging and a metric, but zero observability — because you cannot answer an unplanned question about one request after the fact."
    },
    {
      "type": "concept",
      "title": "Observability = a request ID through structured logs + RED metrics",
      "text": "Stamp each request with an ID, propagate it downstream, and log structured JSON carrying it — so one request is reconstructable. Then emit RED: Rate, Errors, Duration. The invariant: every log line and every error is attributable to a specific request."
    },
    {
      "type": "diagram",
      "title": "How a request ID flows — and the trace/log view it unlocks",
      "subtitle": "A request enters at the gateway, gets (or keeps) an X-Request-ID, and that ID is bound to the log context and forwarded on every downstream call. The failure path: a downstream 504 (gateway timeout) is logged WITH the id; the metrics pipeline counts it as an error and records its duration.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "may send X-Request-ID",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "generate id if absent",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "handler",
          "label": "Orders Handler",
          "description": "binds id to log context",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "payment",
          "label": "Payment Svc",
          "description": "id forwarded in header",
          "kind": "external",
          "tone": "warning"
        },
        {
          "id": "logs",
          "label": "Structured Logs",
          "description": "JSON, queryable by request_id",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "metrics",
          "label": "Metrics (RED)",
          "description": "rate · errors · duration",
          "kind": "store",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "request",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "handler",
          "label": "X-Request-ID",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "handler",
          "to": "payment",
          "label": "forwards id",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "payment",
          "to": "handler",
          "label": "504 timeout",
          "kind": "sync",
          "tone": "warning"
        },
        {
          "from": "handler",
          "to": "logs",
          "label": "log WITH id",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "handler",
          "to": "metrics",
          "label": "count + duration",
          "kind": "data",
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "request ID generated + propagated"
        },
        {
          "tone": "warning",
          "label": "failure path (downstream 504)"
        },
        {
          "tone": "success",
          "label": "evidence: queryable logs + RED metrics"
        }
      ],
      "caption": "Drop the id at any hop and the chain breaks: a payment-service log line with no request_id cannot be tied back to the customer's order. Propagation is the whole skill."
    },
    {
      "type": "code-walkthrough",
      "title": "Instrumenting one FastAPI endpoint — request ID, structured logs, RED metrics",
      "subtitle": "The Worked Expert Example, filled. Middleware stamps the id; the handler logs structured JSON; a metrics middleware records rate, errors, and duration.",
      "filename": "app/observability.py",
      "language": "python",
      "code": "import time, uuid, logging, json\nfrom contextvars import ContextVar\nfrom fastapi import FastAPI, Request\nfrom prometheus_client import Counter, Histogram\n\nrequest_id_ctx: ContextVar[str] = ContextVar(\"request_id\", default=\"-\")\n\nREQUESTS = Counter(\"http_requests_total\", \"Rate + Errors\", [\"method\", \"path\", \"status\"])\nLATENCY = Histogram(\"http_request_duration_seconds\", \"Duration\", [\"method\", \"path\"])\n\nclass JsonLogFormatter(logging.Formatter):\n    def format(self, record: logging.LogRecord) -> str:\n        return json.dumps({\n            \"ts\": self.formatTime(record),\n            \"level\": record.levelname,\n            \"request_id\": request_id_ctx.get(),\n            \"msg\": record.getMessage(),\n        })\n\napp = FastAPI()\nlog = logging.getLogger(\"orders\")\n\n@app.middleware(\"http\")\nasync def observe(request: Request, call_next):\n    rid = request.headers.get(\"X-Request-ID\") or str(uuid.uuid4())\n    request_id_ctx.set(rid)\n    start = time.perf_counter()\n    response = await call_next(request)\n    elapsed = time.perf_counter() - start\n    path = request.url.path\n    REQUESTS.labels(request.method, path, response.status_code).inc()\n    LATENCY.labels(request.method, path).observe(elapsed)\n    response.headers[\"X-Request-ID\"] = rid\n    log.info(f\"{request.method} {path} -> {response.status_code} in {elapsed*1000:.0f}ms\")\n    return response",
      "steps": [
        {
          "lines": [
            7
          ],
          "label": "Bind the id to a context var",
          "note": "A ContextVar carries the request ID through the whole call stack without threading it as an argument — so any log call deep in the handler can read it."
        },
        {
          "lines": [
            9,
            10
          ],
          "label": "The RED metrics, declared once",
          "note": "One Counter labeled by status gives you Rate AND Errors (filter status>=500); one Histogram gives you Duration (p50/p99). This IS RED — not a bare request count."
        },
        {
          "lines": [
            13,
            14,
            15,
            16,
            17,
            18,
            19
          ],
          "label": "Structured JSON logs carry the id",
          "note": "Every line is parseable JSON with request_id baked in — so the log store can index it and you can query `request_id = \"...\"` and get exactly one request's trail."
        },
        {
          "lines": [
            25,
            26
          ],
          "label": "Generate OR propagate",
          "note": "Reuse an inbound X-Request-ID if the caller sent one (so the id spans services); otherwise mint a uuid4. This is the propagation invariant from the diagram."
        },
        {
          "lines": [
            28,
            32,
            33
          ],
          "label": "Record duration around the real work",
          "note": "perf_counter brackets call_next, so LATENCY measures the actual handler — the Duration in RED, the number your p99 alert watches."
        },
        {
          "lines": [
            34
          ],
          "label": "Echo the id back to the client",
          "note": "Returning X-Request-ID lets a user (or a support ticket) hand you the exact id to search — closing the loop from incident report to log query."
        }
      ],
      "caption": "Hand an on-call this endpoint and a request ID from a failed call: they query `request_id = \"abc\"`, see method/path/status/latency, and confirm the error in the metrics — no narration needed."
    },
    {
      "type": "compare",
      "title": "Weak vs gold observability — same incident, one is answerable",
      "subtitle": "\"Some checkouts are failing.\" Only one setup lets you find the specific request and prove the blast radius.",
      "left": {
        "label": "Weak: print + a request counter",
        "tone": "warning",
        "lines": [
          "print(\"order failed\") — no id, no level, no fields",
          "Logs are free text: cannot filter by request",
          "Request ID dies at the service boundary (not propagated)",
          "One metric: total requests/min — errors hidden in the volume",
          "No duration metric: a slow p99 is invisible until users scream",
          "Question \"what happened to request abc?\" → ungrepable"
        ],
        "verdict": "Logging without observability — looks diligent, answers nothing"
      },
      "right": {
        "label": "Gold: request ID + structured logs + RED",
        "tone": "success",
        "lines": [
          "Structured JSON: {request_id, level, msg, status, ms}",
          "Query request_id = \"abc\" → exactly that request's trail",
          "X-Request-ID generated, forwarded downstream, echoed to client",
          "Rate + Errors from one labeled counter (filter status>=500)",
          "Duration histogram → real p50/p99 latency to alert on",
          "Question \"what happened to request abc?\" → answered in 1 query"
        ],
        "verdict": "Answers an unplanned question about ONE request, after the fact"
      },
      "caption": "The line that separates them: can you reconstruct a single request you never anticipated? Print-and-count cannot; an id through structured logs plus RED metrics can."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell, grounded in DORA and the RED method: don't alert on \"a 500 happened\" — alert on the error RATE and the latency p99 crossing your SLO, both sliced by request so a spike points you straight at the affected requests. A single error is noise; a rate change against an objective is a signal. And never log a token, password, or full card number into your structured fields — logging secrets/PII is a security-misconfiguration / sensitive-data-in-logs risk (OWASP API8:2023 Security Misconfiguration; general OWASP A09:2021 Security Logging and Monitoring Failures), and request IDs exist precisely so you can correlate without logging the sensitive payload."
    },
    {
      "type": "quiz",
      "question": "An on-call hands you a request ID from a failed checkout and asks you to prove this isn't just a happy-path demo. What is the exact evidence that your service is genuinely observable?",
      "options": [
        "The dashboard shows total requests per minute is high, so traffic is flowing.",
        "Querying that request_id returns one request's structured trail (method, path, status, latency), and the RED metrics show the error rate and p99 for that path crossing the SLO — reconstructed from logs/metrics, not from re-running it.",
        "You added print statements to the handler and can re-run the request locally to see what happens.",
        "Every endpoint returns HTTP 200 in the integration tests."
      ],
      "answer": 1,
      "explanation": "Observability is reconstructing an unplanned, specific past request from evidence. Only option 2 does that: the request ID isolates one request's structured trail AND the RED metrics quantify the blast radius against an objective. Total-request count (1) hides errors in volume; re-running locally (3) is not the production request; green tests (4) prove the happy path, which is exactly what the reviewer challenge says to look past."
    },
    {
      "type": "verification",
      "intro": "Prove the endpoint is observable — no vibes:",
      "items": [
        "Fire a request, grab the returned X-Request-ID, and query your logs for it — you get exactly that request's lines, in structured (parseable) form, and nothing else.",
        "Make a downstream call inside the handler and confirm the SAME request_id appears in the downstream service's logs — the id propagated, it did not die at your boundary.",
        "Hit the metrics endpoint and confirm three things exist: a request rate, an error count you can filter by status>=500, and a latency histogram with a real p99 — RED, not a bare counter.",
        "Force a failure (e.g. downstream timeout → 504) and confirm the error log line carries the request_id AND the error counter increments — the failure is attributable.",
        "Grep your logs for a token, password, or card number and confirm none leak — you correlate by request_id, not by logging the sensitive payload."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain to a teammate why `print(\"order failed\")` is logging but not observability — using the words request ID, structured, and queryable.",
        "Walk through how a single request ID travels from the client through your handler to a downstream service, and what breaks at exactly the hop where the id is not propagated.",
        "Name the three RED metrics, and explain why a total-request-count graph hides the failure that the error RATE would reveal."
      ]
    },
    {
      "type": "transfer",
      "text": "The same instrument applies to any backend path that can fail unobserved. A webhook receiver: stamp the inbound event with an id, log it structured, count delivery successes/failures and processing duration — so a \"we never got the event\" dispute is answerable. A queue worker: propagate the producing request's id onto the message, so a job that fails three hops later traces back to the user action that created it. A billing endpoint: the request ID becomes your audit thread tying a charge to the exact API call, and the RED metrics become your fraud/anomaly alarm. What stays invariant: every unit of work is attributable to a specific request, and you emit rate/errors/duration. What changes with context: retention and PII rules tighten for billing (privacy), sampling kicks in at scale (cost), and an async worker must carry the id ON the message because there is no live HTTP request to read it from. Carry api_contract_and_failure_repair_packet.md forward — its Observability section is the proof you bring to the capstone: not \"I understand logging\" but \"here is a request I reconstructed from logs alone, and the metric that caught it.\""
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "backend-testing-contracts": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint (POST /payments or POST /orders) and write its testing contract: a contract test that pins the request/response shape and status codes, an integration test that exercises validation + auth + persistence, an idempotency replay that proves a retried request charges once, and a rollback test that proves a mid-transaction failure leaves no partial write.",
      "intensity": "deep",
      "time": "30 min",
      "proof": "api_contract_and_failure_repair_packet.md in the evidence ledger, plus a passing test transcript: contract test (schema + 400/401/409 codes), integration test (happy path + one rejected path), an idempotency replay showing the second call returns the first result with zero new side effects, and rollback evidence showing the row count is unchanged after an injected failure.",
      "unlock": "Your suite has a contract test, an integration test, an idempotency replay, AND rollback evidence — and each can FAIL on a real regression, not just pass on the happy path.",
      "doNotClaim": "Do not claim the endpoint is 'tested' if every test only asserts 200 on valid input. A suite that cannot return red when validation, auth, idempotency, or rollback breaks is theater. The contract is the set of promises a caller relies on — prove you keep them when things go wrong, not only when they go right."
    },
    {
      "type": "mission",
      "text": "A retry storm hits checkout at 2 a.m.: the client times out, fires the same POST /payments three times, and your green test suite never noticed — because it only ever sent one clean request. The customer is charged three times, and the only test that would have caught it was the one nobody wrote."
    },
    {
      "type": "context",
      "text": "An endpoint's real interface is not its handler code — it is the contract callers depend on: the request shape it accepts, the status codes it returns, who is allowed in, and what it promises about exactly-once and rollback. This lesson, in the production-pressure phase, makes that contract executable so a regression turns a test red instead of a customer angry."
    },
    {
      "type": "pretest",
      "prompt": "Your test sends a valid order, asserts 200, and the body has an id. It passes. What promise to the caller is still completely untested? Answer before reading on.",
      "reveal": "Almost all of them. You proved the happy path returns 200 — but not that a malformed body returns 400, that a missing token returns 401, that a duplicate request returns the SAME result instead of a second charge (409 or replayed 200), and that a failure halfway through writes nothing. The contract is mostly the unhappy paths; the happy path is the one promise that rarely breaks in production."
    },
    {
      "type": "concept",
      "title": "A testing contract pins the promises a caller relies on — not just the happy path",
      "text": "Test the four boundaries the caller depends on: shape (request/response schema + status codes), authorization (who is let in), idempotency (a retry produces one effect), and rollback (a mid-transaction failure leaves no partial write). The invariant: every promise has a test that can return red."
    },
    {
      "type": "diagram",
      "title": "What the contract tests guard along the request path",
      "subtitle": "One POST /payments crosses four trust boundaries. Each gets a test that can fail: schema (400), auth (401), idempotency replay (409/replay), and rollback on a failed transaction. The processor is the irreversible external side effect the whole contract exists to protect.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client (retries)",
          "description": "may send the same request twice",
          "kind": "client",
          "tone": "warning"
        },
        {
          "id": "gateway",
          "label": "API Handler",
          "description": "POST /payments",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "validate",
          "label": "Validate",
          "description": "schema → 400 on bad body",
          "kind": "decision"
        },
        {
          "id": "authz",
          "label": "Auth boundary",
          "description": "token → 401 on missing/expired",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "idem",
          "label": "Idempotency store",
          "description": "key seen? → replay, do not re-charge",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "db",
          "label": "Postgres (txn)",
          "description": "all-or-nothing write",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "processor",
          "label": "Payment processor",
          "description": "external, NON-reversible charge",
          "kind": "external",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "request (+ Idempotency-Key)",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "validate",
          "label": "contract test: shape",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "validate",
          "to": "authz",
          "label": "on valid",
          "kind": "control"
        },
        {
          "from": "authz",
          "to": "idem",
          "label": "on authorized",
          "kind": "control"
        },
        {
          "from": "idem",
          "to": "db",
          "label": "first time → write in txn",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "idem",
          "to": "gateway",
          "label": "duplicate → replay stored result",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "db",
          "to": "processor",
          "label": "charge once; rollback if txn fails",
          "kind": "sync",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "contract + idempotency promises under test"
        },
        {
          "tone": "warning",
          "label": "auth + rollback — the costly failure paths"
        },
        {
          "tone": "success",
          "label": "the all-or-nothing transaction"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The four contract tests, one promise at a time",
      "subtitle": "pytest + httpx against POST /payments — each test pins one promise and can return red.",
      "filename": "test_payments_contract.py",
      "language": "python",
      "code": "def test_rejects_malformed_body(client):\n    r = client.post(\"/payments\", json={\"cents\": -1})\n    assert r.status_code == 400\n    assert r.json()[\"error\"][\"field\"] == \"cents\"\n\ndef test_requires_auth(client):\n    r = client.post(\"/payments\", json=VALID, headers={})\n    assert r.status_code == 401\n\ndef test_idempotent_replay_charges_once(client, processor_spy):\n    headers = {**AUTH, \"Idempotency-Key\": \"abc-123\"}\n    first = client.post(\"/payments\", json=VALID, headers=headers)\n    second = client.post(\"/payments\", json=VALID, headers=headers)\n    assert first.status_code == 201\n    assert second.json()[\"id\"] == first.json()[\"id\"]\n    assert processor_spy.charge_count == 1\n\ndef test_rollback_leaves_no_partial_write(client, db, fail_processor):\n    before = db.count(\"payments\")\n    r = client.post(\"/payments\", json=VALID, headers=AUTH)\n    assert r.status_code == 502\n    assert db.count(\"payments\") == before",
      "steps": [
        {
          "lines": [
            1,
            2,
            3,
            4
          ],
          "label": "Shape contract: reject the bad body",
          "note": "Pin BOTH the 400 and the error envelope shape. Callers parse error.field — that field name is part of the contract, so an integration test asserts it, not just the status."
        },
        {
          "lines": [
            6,
            7,
            8
          ],
          "label": "Auth boundary returns 401, not 500",
          "note": "A missing token must be a clean 401. If it 500s, you have leaked an internal error and failed the auth promise — this test catches that regression."
        },
        {
          "lines": [
            10,
            14,
            15
          ],
          "label": "Idempotency replay: same key, one charge",
          "note": "The retry uses the SAME Idempotency-Key. The second call must return the first call's id and the processor spy must show charge_count == 1. This is the 2 a.m. retry-storm bug, made into a red/green check."
        },
        {
          "lines": [
            17,
            18,
            19,
            20,
            21
          ],
          "label": "Rollback: a failed processor leaves zero rows",
          "note": "Inject a failing processor mid-transaction, then assert the row count is unchanged. Proving the absence of a partial write is the test most suites skip — and the one that prevents orphaned, un-charged orders."
        }
      ],
      "caption": "Run it: a regression that drops validation, breaks auth, double-charges on retry, or leaves a partial row turns exactly one of these red. The suite is the contract, executable."
    },
    {
      "type": "compare",
      "title": "Happy-path smoke test vs a real contract suite",
      "subtitle": "Same endpoint, POST /payments. Both are green. Only one keeps the promises a caller actually depends on.",
      "left": {
        "label": "Happy-path only",
        "tone": "warning",
        "lines": [
          "Sends one valid request, asserts 200",
          "No malformed-body case → 400 untested",
          "No missing-token case → 401 untested",
          "Sends each request once → retry double-charge invisible",
          "Never injects a failure → rollback unproven"
        ],
        "verdict": "Green while the contract silently breaks"
      },
      "right": {
        "label": "Contract suite",
        "tone": "success",
        "lines": [
          "Pins response schema + 400/401/409 status codes",
          "Asserts the error envelope shape callers parse",
          "Replays the SAME idempotency key → charge_count == 1",
          "Injects a mid-txn failure → row count unchanged",
          "Every promise has a test that can return red"
        ],
        "verdict": "Red the moment a real promise is broken"
      },
      "caption": "Coverage of lines is not coverage of promises. A suite that only asserts 200 tests the one path that rarely fails in production."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell, straight out of OWASP API Top 10: most production incidents live on the unhappy paths — broken object-level auth, missing input validation, unsafe retries — not the happy path. So pros write the failing-path tests FIRST and treat the 200 case as the easy one. If your suite can only go green, it is documentation, not a contract."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks for the exact evidence that POST /payments is safe under retries. Which artifact actually proves it?",
      "options": [
        "A test that sends one valid request and asserts the response is 201 with an id.",
        "A test that sends the same request twice with one Idempotency-Key and asserts the second returns the first's id while the processor spy shows charge_count == 1.",
        "Manual confirmation that the endpoint 'worked when I tried it in Postman'.",
        "A code comment stating the handler is idempotent."
      ],
      "answer": 1,
      "explanation": "Only option 2 is a replay that can return red: it reissues the SAME idempotency key and asserts a single downstream charge plus a replayed result. The single-request test (1) never exercises a retry; Postman (3) and a comment (4) are claims, not executable proof. Idempotency is a promise about the SECOND identical request — so the test must send two."
    },
    {
      "type": "verification",
      "intro": "Prove the suite is a contract, not a happy-path demo — no vibes:",
      "items": [
        "Break validation on purpose (accept a negative amount) and confirm the malformed-body test goes RED. If it stays green, you are testing the path, not the promise.",
        "Comment out the auth check and confirm the 401 test fails. A passing auth test must depend on the auth code actually running.",
        "Make the second identical request re-charge (disable the idempotency lookup) and confirm the replay test fails with charge_count == 2.",
        "Inject a failure after the row insert but before commit and confirm the rollback test shows an unchanged row count — proving all-or-nothing, not partial.",
        "Confirm api_contract_and_failure_repair_packet.md records the selected approach, the rejected approach, the expected failure, and a reviewer objection — not just the passing output."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why testing only the 200 response leaves most of the endpoint's contract unproven — name the four promises a contract suite must cover.",
        "In your own words: why must an idempotency test send the SAME request twice, and what exactly does the processor spy prove that a status-code assertion cannot?",
        "Describe how you would prove a rollback works without ever seeing a partial write — what do you assert, and why is asserting an absence the hard part?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same four promises — shape, auth, idempotency, rollback — are the contract for any state-changing backend surface. A webhook receiver must replay-protect duplicate deliveries (the provider WILL retry). A billing endpoint must roll back if the invoice write succeeds but the ledger entry fails. A queue worker must process a redelivered message exactly once. An internal API must still enforce object-level auth even though the caller is 'trusted'. What stays invariant is the test-the-promises discipline; what changes is the cost of breaking each — a double-charge on payments, a duplicate email on a webhook, a corrupted ledger on billing. The packet you built carries straight into the capstone: it IS the proof that your service path is safe, reusable unchanged in structure on the next endpoint."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "rate-limits-backpressure": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real write endpoint (or a realistic one) and make it survive overload: add a token-bucket rate limit at the auth boundary AND backpressure on the queue behind it, so the service sheds load with a clean 429 + Retry-After instead of melting. Package it as a reviewable artifact, not a note.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "`api_contract_and_failure_repair_packet.md`: the limiter config (bucket size, refill rate, key), the 429 contract (status + Retry-After + RateLimit headers), the queue's bounded depth + reject policy, and a load-test transcript showing the service stays up while excess requests get 429'd — not 500'd or hung.",
      "unlock": "Your endpoint returns 429 with Retry-After under flood, the queue is bounded (never unbounded), and a load test proves p99 latency holds for admitted traffic while overflow is shed.",
      "doNotClaim": "Do not claim 'rate limiting' if your limiter only logs and still admits every request — that is a counter, not a limit. Do not claim 'backpressure' if your queue is unbounded: an unbounded queue does not push back, it just hides the overload until the heap dies."
    },
    {
      "type": "mission",
      "text": "Black Friday, 11:58pm: a retry storm from one broken mobile client hits the orders endpoint at 40k req/s. The service with no limiter queues every request in memory, OOM-kills, and takes checkout down for everyone. The one next to it returns 429 Retry-After to the abusive client and keeps serving the other 9,000 customers."
    },
    {
      "type": "context",
      "text": "Rate limits and backpressure are how a service says 'no' on purpose before the kernel says it for you with an OOM. This sits in the production-pressure phase: it's the difference between a service that degrades gracefully and one that fails as a cascade through every upstream that called it."
    },
    {
      "type": "pretest",
      "prompt": "Your queue worker is slower than the incoming request rate. Someone suggests 'just make the queue bigger so we don't drop anything.' What's wrong with that? Answer before reading on.",
      "reveal": "A bigger queue doesn't fix a rate mismatch — it delays the failure and makes it worse. If arrivals > service rate, an unbounded queue grows without limit: latency climbs toward infinity (every request waits behind a longer line), memory fills, and you OOM instead of returning errors. The queue depth IS your latency. Backpressure means BOUNDING the queue and rejecting overflow fast (429), so admitted work stays fast and the caller learns to slow down. 'Drop nothing' is how you drop everything."
    },
    {
      "type": "concept",
      "title": "Limit at the edge, bound the queue, push back fast",
      "text": "A token bucket admits requests at a sustainable rate and rejects bursts with 429 + Retry-After. Behind it, a bounded queue rejects when full. The invariant: arrivals you can't serve are refused fast and cheap — never silently buffered until the service dies."
    },
    {
      "type": "diagram",
      "title": "Rate limit + backpressure: the failure path and the repair path",
      "subtitle": "Top path = no control (retry storm → unbounded queue → OOM cascade). Bottom path = token bucket sheds at the auth boundary, bounded queue rejects overflow, worker stays at safe concurrency. The decision node is where the request lives or dies.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Clients + retry storm",
          "description": "bursty, abusive included",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "auth boundary",
          "kind": "service"
        },
        {
          "id": "limiter",
          "label": "Token bucket",
          "description": "per-key: capacity + refill rate",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "queue",
          "label": "Bounded queue",
          "description": "max depth, reject-when-full",
          "kind": "queue",
          "tone": "accent"
        },
        {
          "id": "worker",
          "label": "Worker pool",
          "description": "fixed concurrency = service rate",
          "kind": "service"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "protected downstream",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "reject",
          "label": "429 + Retry-After",
          "description": "shed fast + cheap",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "oom",
          "label": "Unbounded buffer → OOM",
          "description": "failure path: hides overload",
          "kind": "store",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "flood",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "limiter",
          "label": "check token",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "limiter",
          "to": "reject",
          "label": "no token → shed",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "limiter",
          "to": "queue",
          "label": "token → admit",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "queue",
          "to": "oom",
          "label": "if UNBOUNDED (the bug)",
          "kind": "data",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "queue",
          "to": "reject",
          "label": "full → reject (the repair)",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "queue",
          "to": "worker",
          "label": "pull at safe rate",
          "kind": "async"
        },
        {
          "from": "worker",
          "to": "db",
          "label": "bounded load",
          "kind": "sync",
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the control plane: bucket + bounded queue"
        },
        {
          "tone": "warning",
          "label": "shed path (429) and the failure it prevents (OOM)"
        },
        {
          "tone": "success",
          "label": "downstream protected from the storm"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The artifact: a token-bucket limiter + bounded queue, filled in",
      "subtitle": "Express handler from `api_contract_and_failure_repair_packet.md`. Atomic Redis bucket at the boundary; the worker side bounds the queue so admitted work stays fast.",
      "filename": "orders.limiter.ts",
      "language": "ts",
      "code": "// Atomic token bucket in Redis (one round trip, no race between read & write)\nconst BUCKET = `\n  local key, cap, refill, now = KEYS[1], tonumber(ARGV[1]), tonumber(ARGV[2]), tonumber(ARGV[3])\n  local b = redis.call('HMGET', key, 'tokens', 'ts')\n  local tokens = tonumber(b[1]) or cap\n  local ts = tonumber(b[2]) or now\n  tokens = math.min(cap, tokens + (now - ts) * refill)   -- refill since last hit\n  if tokens < 1 then return redis.call('PTTL', key) end   -- empty: caller must wait\n  redis.call('HMSET', key, 'tokens', tokens - 1, 'ts', now)\n  redis.call('PEXPIRE', key, math.ceil(cap / refill * 1000))\n  return -1                                                -- admitted\n`\n\nasync function ordersHandler(req: Request, res: Response) {\n  const key = `rl:orders:${req.apiKeyId}`               // limit per API key, not global\n  const waitMs = await redis.eval(BUCKET, 1, key, 100, 0.05, Date.now())\n  if (waitMs >= 0) {\n    res.setHeader('Retry-After', Math.ceil(waitMs / 1000))\n    res.setHeader('RateLimit-Limit', 100)\n    return res.status(429).json({ error: 'rate_limited' }) // shed fast, cheap\n  }\n  if (queue.size >= QUEUE_MAX) {                          // bounded queue = backpressure\n    res.setHeader('Retry-After', 1)\n    return res.status(429).json({ error: 'overloaded' })  // reject, never buffer unbounded\n  }\n  await queue.add(() => processOrder(req.body))          // worker pool drains at safe rate\n  return res.status(202).json({ status: 'accepted' })\n}",
      "steps": [
        {
          "lines": [
            2,
            3,
            4,
            5,
            6
          ],
          "label": "Atomic bucket, one round trip",
          "note": "Refill + decrement happen inside the Redis script. Doing read-then-write in app code races under load — two requests both read 1 token and both pass. Atomicity is the invariant of a distributed limiter."
        },
        {
          "lines": [
            7,
            8,
            9
          ],
          "label": "Empty bucket returns the wait",
          "note": "When tokens < 1 we return PTTL — exactly how long until a token refills. That number becomes Retry-After, so the client backs off the right amount instead of hammering."
        },
        {
          "lines": [
            15
          ],
          "label": "Key per API key, not global",
          "note": "`rl:orders:${apiKeyId}` isolates tenants: one abusive client gets throttled without starving everyone else. A single global counter would let one caller consume the whole budget."
        },
        {
          "lines": [
            17,
            18,
            19,
            20,
            21
          ],
          "label": "The 429 contract",
          "note": "Status 429 + Retry-After + RateLimit-Limit. This is the reviewable HTTP contract — the client and any proxy can act on it. A bare 500 teaches the caller nothing and invites a retry storm."
        },
        {
          "lines": [
            23,
            24,
            25,
            26
          ],
          "label": "Bounded queue = real backpressure",
          "note": "Even past the limiter, if the in-flight queue is full we reject. This is the line that turns 'unbounded buffer → OOM' into 'shed overflow.' QUEUE_MAX is sized to keep p99 under SLO."
        },
        {
          "lines": [
            27,
            28
          ],
          "label": "202 + fixed-concurrency drain",
          "note": "Admitted work returns 202 Accepted and is drained by a fixed worker pool — concurrency = your real service rate, so Postgres never sees more load than it can take."
        }
      ],
      "caption": "Run the load test: at 40k req/s the bucket admits ~100/s per key, returns 429+Retry-After to the rest, queue never exceeds QUEUE_MAX, and p99 for admitted traffic holds under SLO. That transcript is the proof."
    },
    {
      "type": "compare",
      "title": "Weak 'rate limit' vs gold load-shedding",
      "subtitle": "Same endpoint under a 40k req/s retry storm. Both have the word 'limit' in the code. Only one survives.",
      "left": {
        "label": "Weak: counter that never says no",
        "tone": "warning",
        "lines": [
          "In-app counter: read count, if < N increment (races under load)",
          "On limit: returns 500 (or just logs and admits anyway)",
          "No Retry-After — client retries instantly, amplifying the storm",
          "Global counter: one abusive key starves all tenants",
          "Queue is unbounded — 'we don't drop requests'",
          "Result: latency → ∞, heap fills, OOM, full cascade"
        ],
        "verdict": "Hides overload until the service dies — not a limit"
      },
      "right": {
        "label": "Gold: shed fast, bound the queue",
        "tone": "success",
        "lines": [
          "Atomic Redis token bucket — no read/write race",
          "On limit: 429 + Retry-After + RateLimit headers",
          "Retry-After = real refill time → client backs off correctly",
          "Per-API-key bucket — abusive caller isolated",
          "Bounded queue rejects overflow before it buffers",
          "Result: admitted p99 holds under SLO, overflow shed cleanly"
        ],
        "verdict": "Pushes back early — degrades gracefully, no cascade"
      },
      "caption": "The tell: the weak version's failure mode is invisible until production (the queue grows quietly); the gold version's failure mode is a visible, contract-compliant 429 you can dashboard and the client can obey.",
      "mono": false
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "OWASP API4:2023 (Unrestricted Resource Consumption) makes the limiter a security control, not just a perf knob — an endpoint with no rate limit is a documented API vulnerability. And the senior tell: rate-limit per principal (API key / user / tenant), never one global bucket. A global limit means any single abusive caller can lock out every other tenant — you've built a shared fuse where you needed isolated ones."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: 'Prove this isn't just happy-path. What's the single strongest evidence that your endpoint actually pushes back under overload instead of hiding the overload?'",
      "options": [
        "The code contains a variable named rateLimit and a config value of 100.",
        "A load-test transcript at 40k req/s showing the queue never exceeds QUEUE_MAX, excess requests get 429 + Retry-After, and admitted-traffic p99 stays under SLO — while memory stays flat.",
        "The endpoint returns 200 quickly when you curl it once by hand.",
        "A comment in the PR saying 'added rate limiting for safety.'"
      ],
      "answer": 1,
      "explanation": "Backpressure is a behavior under load, so only a load-test transcript proves it. The key signals: bounded queue depth (it never grows without limit), 429+Retry-After on overflow (contract-compliant shedding, not 500s or hangs), p99 holding for admitted traffic (the limit protects service quality), and flat memory (no unbounded buffering → no OOM). A named variable (1), a single manual 200 (3), or a comment (4) prove nothing about overload behavior."
    },
    {
      "type": "verification",
      "intro": "Prove the limiter and backpressure are real — under load, not by inspection:",
      "items": [
        "Flood the endpoint past the bucket rate and confirm excess requests get HTTP 429 with a Retry-After header — not 500, not a hang, not a silent 200.",
        "Verify Retry-After equals the real time-to-refill (the client that obeys it stops getting 429'd) — a constant or missing Retry-After is theatre.",
        "Watch queue depth during the flood: it must plateau at QUEUE_MAX and reject beyond it. If it grows unbounded, you have a buffer, not backpressure.",
        "Watch process memory during the flood: flat means you're shedding; a climbing heap means you're hiding the overload until OOM.",
        "Confirm the bucket is per-principal: throttle one API key to zero and check a second key still gets served. A global bucket fails this — one caller starves all.",
        "Confirm p99 latency for ADMITTED (202/200) traffic stays under SLO while overflow is shed. Protecting service quality for admitted work is the whole point."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why an unbounded queue is the SAME bug as having no rate limit at all — what does 'queue depth is your latency' mean in your own words?",
        "Walk through what a well-behaved client does when it receives your 429 + Retry-After, and why that turns a retry storm into a calm backoff.",
        "Defend one design choice from your packet: why per-API-key bucket over global, OR why you rejected overflow with 429 instead of buffering it 'just in case'."
      ]
    },
    {
      "type": "transfer",
      "text": "The same shape protects any saturating boundary. A webhook receiver bounds its delivery queue and 429s the sender's burst so the sender's own retry policy backs off. A billing endpoint rate-limits per-customer so one client's runaway script can't exhaust the shared Stripe call budget. A queue worker calling a downstream API applies the limit OUTBOUND — a semaphore capping concurrency so you don't DDoS your own dependency. What stays invariant: bound the buffer, shed overflow fast with a contract the caller can obey, isolate per-principal. What changes: where the limit lives (inbound at auth boundary vs outbound at the dependency call) and the cost of a dropped request (a 429'd webhook must be safely retriable; a 429'd login just waits). This is the artifact your capstone reuses — the same `api_contract_and_failure_repair_packet.md` becomes the load-shedding section of the production service you'll defend in review."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "versioning-compatibility": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real state-changing endpoint and evolve its contract WITHOUT breaking existing callers: ship an additive v2 field, keep v1 clients green, and prove both versions pass against the same handler. Capture the decision and the failure in api_contract_and_failure_repair_packet.md.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the contract change, the assumption (who still sends v1), the selected approach (additive + version negotiation), the rejected approach (rename/remove in place), the expected failure (old client 400s on the new required field), a contract test that runs BOTH the v1 and v2 request shapes against one handler and passes, and a reviewer objection. A reviewer agrees no deployed client breaks the moment you ship.",
      "unlock": "Your change is additive (or version-negotiated), both old and new request shapes pass the same contract test, and the packet names the exact client that would have broken under a naive edit.",
      "doNotClaim": "Do not claim 'backward compatible' because the new code compiles. Compatibility is about deployed CALLERS, not your repo. If you renamed a field, made an optional field required, or tightened validation, you broke someone — even if every test you wrote still passes, because you only wrote tests for the new shape."
    },
    {
      "type": "mission",
      "text": "The deploy goes out at 2 p.m. By 2:04 the mobile fleet — three app versions still in the wild, none auto-updating — is throwing 400s on checkout, because someone made one optional field required. The API was 'fixed'; the contract was broken."
    },
    {
      "type": "context",
      "text": "An API is a promise to callers you no longer control: old mobile builds, partner integrations, queued messages written last week. Versioning compatibility is the discipline of changing that promise without breaking anyone who already trusted it — the invariant behind every safe deploy."
    },
    {
      "type": "pretest",
      "prompt": "You add a new required field shippingTier to POST /orders so every order is classified. Tests are green. What breaks in production? Answer before reading on.",
      "reveal": "Every client that doesn't know shippingTier exists yet — which is all of them until they redeploy. Your tests pass because you only sent the new shape; the deployed apps still send the old shape and now fail validation. The invariant you broke: a new field added to a REQUEST contract must be OPTIONAL with a server-side default, never required. Required-on-input is a breaking change wearing an 'I just added a field' disguise."
    },
    {
      "type": "concept",
      "title": "Additive-and-tolerant, or version it — never break in place",
      "text": "Backward compatibility means old callers keep working unchanged. New request fields must be optional with defaults; never remove or rename fields, narrow types, or tighten validation in place. When a change can't be additive, negotiate a version. The invariant: the deployed caller, not the new code, defines 'compatible'."
    },
    {
      "type": "diagram",
      "title": "One handler, two contract versions, zero broken callers",
      "subtitle": "Old mobile (v1) and new web (v2) hit the same endpoint. The gateway reads the requested version; the handler upgrades a v1 body to the internal shape with a default, so a single code path serves both. The contract test exercises BOTH shapes. Breaking the upgrade step is the blast radius.",
      "nodes": [
        {
          "id": "v1client",
          "label": "v1 Client",
          "description": "old mobile build, can't redeploy",
          "kind": "client",
          "tone": "warning"
        },
        {
          "id": "v2client",
          "label": "v2 Client",
          "description": "new web app",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "reads Accept-Version header",
          "kind": "service"
        },
        {
          "id": "negotiate",
          "label": "Version negotiate",
          "description": "default v1 if header absent",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "upgrade",
          "label": "Upgrade to internal shape",
          "description": "fill defaults for missing v2 fields",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "handler",
          "label": "Order Handler",
          "description": "one validation + persistence path",
          "kind": "service",
          "tone": "success"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "source of truth",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "ctest",
          "label": "Contract test",
          "description": "asserts v1 AND v2 bodies pass",
          "kind": "external",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "v1client",
          "to": "gateway",
          "label": "POST /orders (no shippingTier)",
          "kind": "sync",
          "tone": "warning"
        },
        {
          "from": "v2client",
          "to": "gateway",
          "label": "POST /orders (with shippingTier)",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "negotiate",
          "label": "route",
          "kind": "control"
        },
        {
          "from": "negotiate",
          "to": "upgrade",
          "label": "v1 → upgrade",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "negotiate",
          "to": "handler",
          "label": "v2 → pass through",
          "kind": "control"
        },
        {
          "from": "upgrade",
          "to": "handler",
          "label": "internal shape",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "handler",
          "to": "db",
          "label": "write",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "ctest",
          "to": "gateway",
          "label": "replays both shapes",
          "kind": "async",
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "warning",
          "label": "the caller you can't redeploy (defines compatible)"
        },
        {
          "tone": "accent",
          "label": "the upgrade seam — break here and v1 dies"
        },
        {
          "tone": "success",
          "label": "single internal path both versions share"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "An additive, version-tolerant order handler",
      "subtitle": "Zod tolerant-reader on input, a v1→internal upgrader with a default, one validation path. This is the artifact's selected approach.",
      "filename": "orders_handler.ts",
      "language": "ts",
      "code": "import { z } from 'zod'\n\n// Tolerant reader: v2 ADDS shippingTier, but it is OPTIONAL on the wire.\n// Old v1 callers omit it entirely and must still succeed.\nconst OrderRequestV2 = z.object({\n  itemId: z.string(),\n  qty: z.number().int().positive(),\n  shippingTier: z.enum(['standard', 'express']).optional(),\n})\n\n// Internal shape the handler actually uses — shippingTier is always present.\ntype InternalOrder = { itemId: string; qty: number; shippingTier: 'standard' | 'express' }\n\n// The upgrade seam: fill the default for callers that never sent the field.\nfunction upgrade(body: z.infer<typeof OrderRequestV2>): InternalOrder {\n  return { ...body, shippingTier: body.shippingTier ?? 'standard' }\n}\n\nexport async function handleOrder(raw: unknown): Promise<{ id: string }> {\n  const parsed = OrderRequestV2.parse(raw) // rejects garbage, tolerates missing shippingTier\n  const order = upgrade(parsed)            // v1 body becomes a complete internal order\n  return persist(order)                    // ONE persistence path for both versions\n}\n\ndeclare function persist(o: InternalOrder): Promise<{ id: string }>",
      "steps": [
        {
          "lines": [
            6,
            7,
            8,
            9
          ],
          "label": "Add the field as OPTIONAL",
          "note": "shippingTier is new in v2, but .optional() means a v1 body with no shippingTier still validates. Mark it required here and every deployed v1 caller 400s on deploy — the exact pretest failure."
        },
        {
          "lines": [
            12
          ],
          "label": "Separate wire shape from internal shape",
          "note": "The internal type has shippingTier as a non-optional. The handler never reasons about 'maybe missing' — the upgrade step guarantees it."
        },
        {
          "lines": [
            15,
            16,
            17
          ],
          "label": "The upgrade seam",
          "note": "?? 'standard' fills the default for old callers. This one line is what keeps v1 alive. It is also the single point that, if removed, breaks every v1 request — mark it in the diagram."
        },
        {
          "lines": [
            21
          ],
          "label": "Tolerant parse, not a v2-only schema",
          "note": "parse rejects genuinely malformed input but accepts the absence of a new optional field. Tolerant reader: ignore what you don't recognize, require only what was always required."
        },
        {
          "lines": [
            22,
            23
          ],
          "label": "One internal path",
          "note": "After upgrade, both versions are an InternalOrder. Validation, authz, and persistence run once — you do not fork business logic per version, only the input seam."
        }
      ],
      "caption": "The contract test replays a v1 body { itemId, qty } AND a v2 body with shippingTier against handleOrder — both must return an id. Run it before and after the change; the v1 case is the one that proves you didn't break the fleet."
    },
    {
      "type": "compare",
      "title": "Same feature, two ways to ship it",
      "subtitle": "Goal: classify every order by shipping tier. One change breaks the deployed fleet; one doesn't.",
      "left": {
        "label": "Break in place",
        "tone": "warning",
        "lines": [
          "Add shippingTier as a REQUIRED field on the request",
          "Rename qty → quantity 'while we're in here'",
          "Tighten itemId to a stricter regex",
          "Tests pass — they only send the new shape",
          "v1 fleet (no redeploy) 400s on the next request"
        ],
        "verdict": "Compiles, tests green, production down for old clients"
      },
      "right": {
        "label": "Additive + tolerant",
        "tone": "success",
        "lines": [
          "shippingTier OPTIONAL, defaulted server-side to 'standard'",
          "Keep qty; never rename a field callers send",
          "Validation unchanged for fields v1 already sent",
          "Contract test replays BOTH v1 and v2 bodies",
          "v1 fleet keeps working; v2 gets the new field"
        ],
        "verdict": "No deployed caller breaks the moment you ship"
      },
      "caption": "The breaking move FEELS like the same amount of work. The only difference is whether you asked 'who is still sending the old shape?' — and wrote a test that proves they still pass."
    },
    {
      "type": "tradeoff",
      "question": "When an additive change isn't enough and you must truly version, where does the version live?",
      "optionA": {
        "label": "URL path (/v2/orders)",
        "text": "Explicit, cacheable, trivially routable at the gateway, obvious in logs. But it forks the route surface and tempts you to copy whole handlers per version — version sprawl."
      },
      "optionB": {
        "label": "Header (Accept-Version: 2)",
        "text": "Keeps one clean resource URL and negotiates per request, so the internal path stays shared. But it's invisible in a browser address bar, easy to forget in clients, and harder to cache by version."
      },
      "guidance": "Default to additive + tolerant reader so you rarely need a hard version at all. When you must: header negotiation for internal/partner APIs where you control clients and want one resource; URL versioning for wide public APIs where discoverability and caching win. Either way, never let it become per-version copies of business logic — version the SEAM, share the path."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell, straight from the OWASP API and SWEBOK V4 evolution guidance: backward compatibility is owned by the CONSUMER, not the producer. A change is breaking if any deployed caller would behave differently — which means the real review question is never 'is my new code correct?' but 'name the oldest client still in the wild, and show me it still passes.' If you can't name that client, you haven't assessed compatibility, you've assumed it."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this is backward compatible, not just a happy-path answer.' Which one actually proves it?",
      "options": [
        "All existing unit tests are green after the change.",
        "A contract test that sends the OLD (v1) request body — with no new fields — against the new handler and asserts it still succeeds, run alongside the v2 body.",
        "The new field has a sensible name and a docstring.",
        "The OpenAPI spec was regenerated and is valid."
      ],
      "answer": 1,
      "explanation": "Only option 2 exercises the deployed caller's actual shape. Existing unit tests (1) were written for shapes you already support and won't catch a newly-required field; a good name (3) and a valid spec (4) say nothing about whether an old client's request still validates. Compatibility is proven by replaying the OLD contract against the NEW code and watching it pass — that v1 case is the evidence the reviewer is asking for."
    },
    {
      "type": "verification",
      "intro": "Prove the change is compatible — no vibes:",
      "items": [
        "Name the oldest deployed caller that hits this endpoint. If you can't name one, you haven't assessed compatibility — go find it before claiming it.",
        "Every new request field is optional with a server-side default. Find one that's required-on-input and you have a breaking change, not an additive one.",
        "No field a caller sends was renamed, removed, or type-narrowed in place. Grep the diff for renames; each one is a break.",
        "A contract test replays a v1 body (old shape, missing the new field) AND a v2 body against the same handler — both pass. Run it; watch the v1 case go green.",
        "Validation for fields v1 already sent is unchanged or only loosened. Tightening an existing field (stricter regex, lower max) silently 400s old callers."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why 'all my tests pass' is not evidence of backward compatibility, using your own endpoint as the example.",
        "Walk through the upgrade seam in your handler: what does the default line do, and which deployed client dies if you delete it?",
        "State the additive invariant in one sentence, then name one change to your contract that violates it and one that respects it."
      ]
    },
    {
      "type": "transfer",
      "text": "The same invariant governs every contract you can't atomically redeploy on both sides. A queue worker must tolerate messages enqueued by last week's producer — so consumers ignore unknown fields and never require a field producers don't yet emit. A webhook payload to a partner is a public API: add fields, never remove them. A database migration that drops a column is the breaking-in-place move — expand-and-contract (add new, backfill, dual-write, then remove) is the additive version. A billing endpoint, an internal RPC, an AI service request schema: same rule — old callers define compatible, version the seam, prove it with the old shape replayed against the new code. Carry api_contract_and_failure_repair_packet.md into the capstone; the contract test that replays both shapes is the proof the capstone reuses unchanged."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "security-privacy-boundaries": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real state-changing endpoint (or a realistic one) and draw its security & privacy boundaries: where untrusted input is validated, where authorization is enforced, what PII may cross each edge, and what the handler must REFUSE to do. Capture it in a reviewable packet, not a note.",
      "intensity": "deep",
      "time": "30 min",
      "proof": "api_contract_and_failure_repair_packet.md with: the request/response contract, the auth boundary (who may call, on whose data), the validation rule for every field, the PII classification per field, the expected failure (a broken-auth or over-exposure case), and the verification method (contract test + an authz-deny test). A reviewer can point to the exact line that stops a tenant from reading another tenant's row.",
      "unlock": "Your packet names the auth boundary, validates every field at the edge, classifies PII per field, and shows one authorization-deny test that returns 403/404 — not 200.",
      "doNotClaim": "Do not claim you 'secured the endpoint' because you added a login check. Authentication is who you are; authorization is what THIS request may touch. An endpoint that authenticates the caller but trusts the id in the body to pick the row is an IDOR waiting to happen — the #1 item on the OWASP API Top 10."
    },
    {
      "type": "mission",
      "text": "POST /api/accounts/{id}/transfer ships behind a login. Six weeks later a user changes {id} in the URL to a number that isn't theirs — and moves someone else's money. The auth check passed. The boundary was never drawn."
    },
    {
      "type": "context",
      "text": "Security and privacy boundaries are the invisible lines a request must not cross: untrusted input stops at validation, a caller stops at the data they own, and PII stops at the systems cleared to hold it. This is the synthesis lesson — it sits on top of your request/response contracts, validation, and authorization, and it is what a reviewer actually inspects before a state-changing path ships."
    },
    {
      "type": "pretest",
      "prompt": "Your endpoint requires a valid JWT, so only logged-in users reach the handler. Is the request now safe to act on? Decide before reading.",
      "reveal": "No — you only proved WHO is calling, not WHAT they may touch. The JWT says 'user 42 is logged in'; it says nothing about whether user 42 owns account {id} in the path. Authentication is the doorman; authorization is the lock on each room. Skipping the per-object authorization check is Broken Object Level Authorization (BOLA/IDOR) — OWASP API #1 — and it passes every happy-path test because the attacker is a real, logged-in user."
    },
    {
      "type": "concept",
      "title": "A boundary validates input, authorizes the object, and contains PII — at the edge",
      "text": "Three lines every state-changing handler must draw: untrusted input is validated before use, the caller is authorized against THIS object (not just logged in), and PII is classified so it never leaks past a system cleared to hold it. The invariant: trust nothing the client sent."
    },
    {
      "type": "diagram",
      "title": "Where the boundaries sit on a state-changing request",
      "subtitle": "POST /accounts/{id}/transfer. Three gates the request must pass — validate input, authorize the object, contain PII — before the DB transaction commits. The dashed edge is the attack: an id from the body bypassing the authz gate.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "untrusted — may be hostile",
          "kind": "client",
          "tone": "warning"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "TLS, rate limit, JWT verify",
          "kind": "service"
        },
        {
          "id": "validate",
          "label": "Validate input",
          "description": "schema: types, ranges, allow-list",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "authz",
          "label": "Authorize object",
          "description": "does caller OWN {id}?",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "handler",
          "label": "Handler",
          "description": "business logic in a txn",
          "kind": "service"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "row-scoped by tenant",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "log",
          "label": "Logs / traces",
          "description": "PII must be redacted here",
          "kind": "external",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "request + JWT",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "validate",
          "label": "authenticated",
          "kind": "sync"
        },
        {
          "from": "validate",
          "to": "authz",
          "label": "shape OK",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "authz",
          "to": "handler",
          "label": "owns object",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "handler",
          "to": "db",
          "label": "commit (WHERE tenant_id=...)",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "handler",
          "to": "log",
          "label": "observe (redacted)",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "client",
          "to": "handler",
          "label": "id from body bypasses authz (IDOR)",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the two gates: validate then authorize"
        },
        {
          "tone": "warning",
          "label": "untrusted edges + PII leak surface"
        },
        {
          "tone": "success",
          "label": "row-scoped store — the contained side"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The transfer handler, boundary by boundary",
      "subtitle": "FastAPI. The repaired version: validate at the edge, authorize the object from the token, scope the query, redact the log.",
      "filename": "transfer.py",
      "language": "python",
      "code": "from fastapi import APIRouter, Depends, HTTPException\nfrom pydantic import BaseModel, condecimal\n\nrouter = APIRouter()\n\nclass TransferIn(BaseModel):\n    to_account: str            # validated: non-empty, owned-bank format\n    amount: condecimal(gt=0, max_digits=12, decimal_places=2)\n\n@router.post(\"/accounts/{account_id}/transfer\")\ndef transfer(account_id: str, body: TransferIn, caller=Depends(current_user)):\n    # authorize the OBJECT, not just the session\n    acct = db.fetch_one(\n        \"SELECT id, owner_id FROM accounts WHERE id = %s AND tenant_id = %s\",\n        [account_id, caller.tenant_id],\n    )\n    if acct is None or acct.owner_id != caller.id:\n        raise HTTPException(status_code=404)        # 404, not 403: don't confirm existence\n    with db.transaction():\n        debit(acct.id, body.amount)\n        credit(body.to_account, body.amount)\n    log.info(\"transfer ok\", account=acct.id, amount=str(body.amount))  # no PII, no full body\n    return {\"status\": \"ok\"}",
      "steps": [
        {
          "lines": [
            6,
            7,
            8
          ],
          "label": "Validate input at the edge",
          "note": "Pydantic rejects malformed bodies before any logic runs. condecimal(gt=0) blocks negative/zero amounts — a classic state-corruption input. Never trust the client's types or ranges."
        },
        {
          "lines": [
            12,
            13,
            14,
            15
          ],
          "label": "Authorize the OBJECT, not the session",
          "note": "The query is scoped by tenant_id from the trusted token AND checks owner_id. The client cannot widen this — the id in the path is only used to look up, never to grant access."
        },
        {
          "lines": [
            16
          ],
          "label": "404, not 403, on a foreign object",
          "note": "Returning 403 confirms the row exists. 404 leaks nothing about other tenants' data — a privacy boundary, not just an access one."
        },
        {
          "lines": [
            17,
            18,
            19
          ],
          "label": "Mutate inside a transaction",
          "note": "Debit and credit commit together or roll back together. A boundary that authorizes correctly but leaves a half-applied transfer still fails the user."
        },
        {
          "lines": [
            20
          ],
          "label": "Redact PII at the log boundary",
          "note": "Log the account id and amount, never the raw body or the counterparty's name/email. Logs and traces are an external system — PII that crosses into them is a leak you can't recall."
        }
      ],
      "caption": "Two trusted facts come from the token (caller.id, caller.tenant_id); everything from the client is validated then checked against them. That is the whole boundary."
    },
    {
      "type": "compare",
      "title": "Same endpoint, boundary drawn vs not",
      "subtitle": "POST /accounts/{id}/transfer. Both authenticate the caller. Only one authorizes the object.",
      "left": {
        "label": "Authenticated but unbounded",
        "tone": "warning",
        "lines": [
          "Login required → feels secure",
          "account_id from the path picks the row directly",
          "No owner_id / tenant_id check on the query",
          "Amount taken as-is (negatives, huge values pass)",
          "log.info(request.body) — PII into logs forever",
          "403 on foreign object → confirms it exists"
        ],
        "verdict": "Passes every happy-path test; ships an IDOR + PII leak"
      },
      "right": {
        "label": "Boundary drawn",
        "tone": "success",
        "lines": [
          "Login required AND object authorized to caller",
          "Query scoped by tenant_id + owner_id from the token",
          "Pydantic validates every field at the edge",
          "condecimal(gt=0) rejects negative/zero amounts",
          "Logs redacted: id + amount, never raw body",
          "404 on foreign object → leaks nothing"
        ],
        "verdict": "An authz-deny test returns 404; a reviewer can sign it"
      },
      "caption": "The difference is one query clause and one validation schema — and it is the difference between a contract and a breach. Measure the boundary by what it REFUSES, not what it allows."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "OWASP API Top 10 ranks Broken Object Level Authorization (BOLA/IDOR) #1 for a reason: it is invisible to the test suite. The attacker is a legitimate, logged-in user — every auth check passes. The only test that catches it is the one you have to write on purpose: log in as user A, request user B's object, assert 404. If that test doesn't exist, the boundary doesn't either."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks for the exact evidence that this endpoint enforces its boundary — not just understands the idea. What do you point to?",
      "options": [
        "The line that verifies the JWT signature at the gateway.",
        "A passing happy-path test where the owner transfers their own funds.",
        "An authorization-deny test: logged in as user A, requesting user B's account returns 404, plus the input-validation test that rejects a negative amount.",
        "A comment in the handler that says '# authorized'."
      ],
      "answer": 2,
      "explanation": "JWT verification (1) proves authentication, not object authorization. The happy path (2) and a comment (4) never exercise the boundary — they confirm the door opens for the owner, not that it stays shut for everyone else. Only the cross-user deny test plus the rejected-input test prove the boundary holds against the realistic attack, which is the reviewer's whole question."
    },
    {
      "type": "verification",
      "intro": "Prove the boundary holds — no vibes:",
      "items": [
        "Object authorization: log in as user A, request user B's object. If it returns 200 (or 403 that confirms existence), you have an IDOR — fix it to 404 scoped by the token.",
        "Input validation: send a negative amount, a missing field, and a wrong type. Each must be rejected at the edge before any business logic runs.",
        "PII containment: grep your logs and traces for the request body, email, or name. If PII appears, redact at the boundary — logs are an external system you cannot recall.",
        "Transaction integrity: kill the process mid-transfer. The debit and credit must both roll back — a boundary that half-applies a mutation still breaks the user.",
        "Contract test: the response schema is asserted, so a future change that leaks an extra field (another tenant's id, an internal flag) fails CI."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain the difference between authentication and object-level authorization using your own endpoint, and name which one IDOR exploits.",
        "Why does returning 404 instead of 403 on a foreign object protect privacy, not just access? Use your packet as the example.",
        "Point to the one line in your handler that stops a tenant from reading another tenant's row — and the one test that proves it."
      ]
    },
    {
      "type": "transfer",
      "text": "The same three boundaries govern every state-changing path you will ever ship. A webhook validates the signature (untrusted input) before trusting the payload. A billing endpoint authorizes the subscription against the caller, not the id in the body. A queue worker re-checks the object on dequeue because the message is as untrusted as a request. An internal API still scopes by tenant — 'internal' is not an authorization. What stays invariant: validate input, authorize the object, contain PII. What changes is the surface — signature vs JWT, synchronous vs queued — never the boundary. Carry api_contract_and_failure_repair_packet.md into the capstone: it is the artifact the capstone reuses to prove the path is safe, and the deny-test inside it is the line that turns 'I understand security boundaries' into 'here is the request another user could not make.'"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "backend-debugging-incident-loop": [
    {
      "type": "sprint-contract",
      "outcome": "Take one production-shaped, state-changing API path (a POST that writes through validation, an auth boundary, and a database transaction) and run the Backend Debugging Incident Loop on a real failure: observe the symptom, reproduce it deterministically, isolate the layer, repair it, and prove the repair — then package it as a reviewable artifact, not a note.",
      "intensity": "deep",
      "time": "30 min",
      "proof": "api_contract_and_failure_repair_packet.md with: context + assumptions, the reproduced symptom, the isolated layer, selected vs rejected approach, the expected failure, and the verification method — backed by a contract test, an integration test, an idempotency replay (same key twice → one row), and database rollback evidence. A reviewer can replay your failure from the packet alone.",
      "unlock": "You ran observe → reproduce → isolate → repair → prove on a real state-changing path, and the packet shows the broken case, the fix, and proof for both.",
      "doNotClaim": "Do not claim you 'fixed the bug' from a green happy-path test. A fix proven only by the path that already worked is theater. The proof is the replay of the FAILURE returning the correct behavior — same idempotency key twice still writes one row, and a mid-transaction error leaves zero rows, not half."
    },
    {
      "type": "mission",
      "text": "The page fires at 2 a.m.: a customer was charged twice for one order. The retry-on-timeout that looked harmless in review is now double-writing through an endpoint with no idempotency key — and the on-call who can find which layer lies between the HTTP contract and the database transaction goes back to bed first."
    },
    {
      "type": "context",
      "text": "This is the synthesis lesson: it threads request/response contracts, validation, the auth boundary, persistence, queues, and observability into one loop you run under pressure. The Incident Loop is what turns 'something is wrong in the backend' into a layer, a repair, and operator evidence."
    },
    {
      "type": "pretest",
      "prompt": "A duplicate charge is in production. Your instinct is to read the payment-handler code you wrote last week. Before you open that file — what should you do first? Answer, then read on.",
      "reveal": "Reproduce it deterministically before you read a single line. A bug you cannot reproduce on demand, you cannot prove you fixed — every later step is a guess. The loop's order is load-bearing: observe the exact symptom (two rows, same order_id), reproduce it (replay the same request twice), THEN isolate the layer. Jumping to your own code is searching where the light is good; the duplicate may live in a retry above your handler, not inside it."
    },
    {
      "type": "concept",
      "title": "The Incident Loop: observe → reproduce → isolate → repair → prove → prevent",
      "text": "Capture the exact symptom, reproduce it on demand, isolate which layer lies (contract, validation, auth, persistence, queue), repair the smallest unit, prove the failure path now behaves, and prevent the class. The invariant: no repair claim without a reproduced failure AND its proof."
    },
    {
      "type": "diagram",
      "title": "Where the lie lives: the state-changing request path",
      "subtitle": "A POST /orders threads six layers. The Incident Loop isolates WHICH edge double-writes. The suspect edge (handler → transaction, with no idempotency guard) is marked; the blast radius is duplicate persisted state. Queue and analytics are downstream readers, deliberately scoped as context.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "retries on timeout",
          "kind": "client",
          "tone": "warning"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "HTTP contract + routing",
          "kind": "service"
        },
        {
          "id": "auth",
          "label": "Auth Boundary",
          "description": "who may write",
          "kind": "decision"
        },
        {
          "id": "handler",
          "label": "Order Handler",
          "description": "validate + idempotency check",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "db",
          "label": "Postgres",
          "description": "transaction · source of truth",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "queue",
          "label": "Event Queue",
          "description": "downstream consumers",
          "kind": "queue",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "POST /orders (x2 on retry)",
          "kind": "sync",
          "tone": "warning"
        },
        {
          "from": "gateway",
          "to": "auth",
          "label": "contract checked",
          "kind": "sync"
        },
        {
          "from": "auth",
          "to": "handler",
          "label": "authorized",
          "kind": "sync"
        },
        {
          "from": "handler",
          "to": "db",
          "label": "INSERT (NO idempotency guard)",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "db",
          "to": "queue",
          "label": "emits order.created x2",
          "kind": "async",
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "warning",
          "label": "where the retry originates"
        },
        {
          "tone": "accent",
          "label": "suspect edge — the unguarded write"
        },
        {
          "tone": "muted",
          "label": "downstream blast radius (double events)"
        }
      ],
      "caption": "Isolate by walking the path: did the contract accept two requests? Did auth pass both? Did the handler write twice? The diagram is the search space the loop shrinks."
    },
    {
      "type": "code-walkthrough",
      "title": "The repair: an idempotent, transactional handler",
      "subtitle": "The before was a bare INSERT inside a retry-prone path. The after guards on an idempotency key inside one transaction — replay-safe, rollback-safe.",
      "filename": "order_handler.py",
      "language": "python",
      "code": "async def create_order(req: OrderRequest, user: User, db: Connection) -> OrderResult:\n    # 1. validation boundary — reject malformed input before any write\n    payload = OrderRequest.model_validate(req)        # raises 422 on bad input\n    # 2. auth boundary — this user may write THIS order\n    if not user.can_create_order(payload.account_id):\n        raise Forbidden(\"not your account\")\n    # 3. idempotency guard — same key twice returns the first result, no new row\n    async with db.transaction():                      # all-or-nothing\n        existing = await db.fetchrow(\n            \"SELECT id FROM orders WHERE idempotency_key = $1\", payload.key)\n        if existing:\n            return OrderResult(id=existing[\"id\"], replayed=True)\n        row = await db.fetchrow(\n            \"INSERT INTO orders (account_id, idempotency_key, total) \"\n            \"VALUES ($1, $2, $3) RETURNING id\",\n            payload.account_id, payload.key, payload.total)\n        await db.execute(\"INSERT INTO ledger (order_id, amount) VALUES ($1, $2)\",\n            row[\"id\"], payload.total)               # if THIS fails, order rolls back too\n    return OrderResult(id=row[\"id\"], replayed=False)",
      "steps": [
        {
          "lines": [
            3
          ],
          "label": "Validation boundary first",
          "note": "Malformed input is rejected with a 422 before a single byte is written. The contract is enforced at the door, not deep inside."
        },
        {
          "lines": [
            5,
            6
          ],
          "label": "Auth boundary second",
          "note": "Authorization is checked AFTER shape validation but BEFORE any persistence. A valid-shaped request from the wrong user must still write nothing."
        },
        {
          "lines": [
            8
          ],
          "label": "One transaction wraps the whole write",
          "note": "db.transaction() makes the order INSERT and the ledger INSERT all-or-nothing. This is the rollback evidence the proof demands."
        },
        {
          "lines": [
            9,
            10,
            11,
            12
          ],
          "label": "Idempotency guard — the actual repair",
          "note": "Same idempotency_key replayed returns the first row with replayed=True. The double-charge symptom from the diagram dies here: two requests, one row."
        },
        {
          "lines": [
            16,
            17
          ],
          "label": "The rollback trap",
          "note": "If the ledger INSERT throws, the order INSERT is rolled back with it — zero rows, never a half-written order. This is the failure path you must prove, not the happy path."
        }
      ],
      "caption": "Reproduce by sending the same key twice and by injecting a ledger failure: the first proves idempotency (one row), the second proves rollback (zero rows)."
    },
    {
      "type": "compare",
      "title": "Two debug write-ups, same incident",
      "subtitle": "Symptom: 'customer charged twice for one order.' One is a note that closes the ticket; one is the packet that proves the fix and prevents the class.",
      "left": {
        "label": "Weak: vague closure",
        "tone": "warning",
        "lines": [
          "\"Added an idempotency check, should be fine now\"",
          "No reproduction — the double-write was never replayed",
          "Proof = the happy-path test that already passed",
          "Rollback behavior on partial failure untested",
          "No prevention: next retry-prone endpoint repeats it"
        ],
        "verdict": "Closes the ticket, not the failure class"
      },
      "right": {
        "label": "Gold: api_contract_and_failure_repair_packet.md",
        "tone": "success",
        "lines": [
          "Symptom captured: 2 ledger rows, same order_id, t+0.4s apart",
          "Reproduced: replay same idempotency_key → was 2 rows, now 1",
          "Isolated: handler INSERT, no guard (auth + contract were fine)",
          "Proven: contract test + integration test + idempotency replay + rollback (ledger-fail → 0 rows)",
          "Prevented: idempotency key required by contract for all writes"
        ],
        "verdict": "Replayable failure + proof + a prevention rule"
      },
      "caption": "The deliverable is the reproduced failure returning correct behavior — not a green test for the path that never broke."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "Standards grounding (OWASP API Top 10 + DORA): the senior tell is proving the FAILURE path, not the happy path. Anyone can show the request that works. A reviewer trusts the engineer who shows 'same key twice → one row' and 'mid-transaction error → zero rows' — because those are the exact behaviors that, left unproven, become the next 2 a.m. page. High-performing teams (DORA) make mean-time-to-restore low precisely because the repair ships with the replay."
    },
    {
      "type": "quiz",
      "question": "Reviewer challenge: \"Show me the exact evidence this is not just a happy-path answer.\" Which response actually proves the Incident Loop ran?",
      "options": [
        "\"The unit tests for create_order all pass green.\"",
        "\"I reproduced the double-write by replaying the same idempotency key (was 2 rows, now 1), and I injected a ledger-INSERT failure to prove the order rolls back to 0 rows — both are in the packet.\"",
        "\"I added an idempotency check and the code reviewer approved the diff.\"",
        "\"I read the handler carefully and I'm confident the logic is correct now.\""
      ],
      "answer": 1,
      "explanation": "Only option 2 points to the reproduced failure AND its proof on both axes the bug touches: idempotency replay (same key → one row) and transactional rollback (forced mid-write error → zero rows). Green happy-path tests (1), an approved diff (3), and confidence from reading (4) all skip the one thing the loop exists to produce — evidence that the FAILURE path now behaves."
    },
    {
      "type": "verification",
      "intro": "Prove the loop ran — no vibes:",
      "items": [
        "You can REPRODUCE the symptom on demand: replaying the same request produces the exact bad state (e.g. two ledger rows) before your fix. If you cannot reproduce it, you cannot prove you fixed it.",
        "You ISOLATED the layer with evidence, not a hunch — show the contract accepted, auth passed, and the write was the unguarded step (rule out the layers you do not own).",
        "Idempotency replay: the same idempotency key sent twice now yields exactly one persisted row, with the second call returning replayed=True.",
        "Rollback evidence: a forced error mid-transaction (e.g. the ledger INSERT throws) leaves ZERO rows, never a half-written order.",
        "The packet api_contract_and_failure_repair_packet.md contains context, assumptions, selected vs rejected approach, expected failure, verification method, reviewer objection, and the capstone connection — and a peer can replay your failure from it alone."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Walk the six steps of the Incident Loop — observe, reproduce, isolate, repair, prove, prevent — naming what each produced for YOUR duplicate-write incident.",
        "Explain why reproducing the failure must come before reading your own code, using the auth/validation/persistence layers as the search space.",
        "Defend your proof to a reviewer: which two behaviors did you replay (idempotency + rollback), and why does a green happy-path test prove neither of them?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same loop runs on any state-changing backend path. A webhook receiver must be idempotent on the provider's event id (same delivery retried → one effect). A billing endpoint proves a failed charge rolls back the subscription change. A queue worker proves a redelivered message does not double-apply. What stays invariant: reproduce the failure, isolate the layer, prove the failure path. What changes with scale, cost, and risk: how expensive the replay is and how much rollback evidence a reviewer demands. The packet you built here is the exact artifact your capstone reuses — the backend service proof packet with tests, failure drills, and reviewable contracts. You don't just describe the bug now. You can reproduce it, repair it, and prove it."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "backend-integration-mini-project": [
    {
      "type": "sprint-contract",
      "outcome": "Assemble one production-shaped service path that processes a state-changing POST exactly once: request/response contract, validation, an auth boundary, a single database transaction, idempotency replay protection, a durable queue hand-off, and an observability trace — then package the proof in api_contract_and_failure_repair_packet.md.",
      "intensity": "capstone",
      "time": "35–45 min",
      "proof": "api_contract_and_failure_repair_packet.md attached to the evidence ledger, containing: context + assumptions, the selected approach, the rejected approach, the expected failure, a verification method (contract test, integration test, idempotency replay, DB rollback evidence), a reviewer objection, repair notes, and the capstone connection. A reviewer replays your retry and sees ONE charge, not two.",
      "unlock": "Your service path enforces exactly-once on a state-changing request, the idempotency replay returns the first result without re-executing, and the failed transaction leaves no partial write — all shown as proof output, not described.",
      "doNotClaim": "Do not claim 'exactly-once' from a happy-path 200 alone. Exactly-once is only proven by the DUPLICATE request: fire the same Idempotency-Key twice and show one row, one charge, one event. A handler that 'usually works' under no retries has proven nothing this lesson cares about."
    },
    {
      "type": "mission",
      "text": "A payment retries after a network blip and the customer is charged twice. The fix was never 'add a cache' — it was one boundary the first handler skipped: the same request, arriving twice, must change the world exactly once."
    },
    {
      "type": "context",
      "text": "This is the synthesis lesson: every earlier backend skill — contract, status codes, validation, auth, persistence, idempotency, queues, observability — has to cooperate on ONE request path or the system leaks money. The packet you produce is the artifact your capstone reuses unchanged.",
      "intensity": "capstone"
    },
    {
      "type": "pretest",
      "prompt": "A client sends POST /charges, the request succeeds on the server, but the 200 never reaches the client (timeout). The client retries the identical request. What does a naive handler do — and why is that the most expensive bug on this list?",
      "reveal": "It charges twice. The server already committed the transaction; the retry is indistinguishable from a fresh request, so it commits a second one. The 'oh!' — at-least-once delivery is the default everywhere (clients retry, load balancers retry, queues redeliver), so exactly-once is never free. You buy it with an idempotency key the client sends and the server remembers: same key → return the stored first result, never re-run the side effect."
    },
    {
      "type": "concept",
      "title": "Exactly-once is at-least-once delivery plus a remembered idempotency key",
      "text": "The network guarantees only at-least-once: requests get retried. Make the side effect safe to repeat by binding it to a client-supplied Idempotency-Key, persisting the first outcome, and returning that stored outcome on any replay — inside one transaction so a crash leaves no half-write."
    },
    {
      "type": "diagram",
      "title": "The exactly-once service path — assembled",
      "subtitle": "One state-changing POST threading every backend skill: contract → auth → validate → idempotency check → ONE transaction (charge + record key + enqueue) → 202. The replay short-circuits at the idempotency store; the queue carries the after-effect; the tracer watches the whole path. Auth failures and validation failures exit before any write.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "sends Idempotency-Key header",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "authn + rate limit",
          "kind": "service"
        },
        {
          "id": "handler",
          "label": "Charge Handler",
          "description": "contract + Zod validation",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "idem",
          "label": "Idempotency Store",
          "description": "key → stored first result",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "txn",
          "label": "DB Transaction",
          "description": "charge + key insert, atomic",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "queue",
          "label": "Queue",
          "description": "receipt email, async",
          "kind": "queue"
        },
        {
          "id": "dlq",
          "label": "DLQ",
          "description": "poison messages",
          "kind": "queue",
          "tone": "warning"
        },
        {
          "id": "otel",
          "label": "Tracer",
          "description": "span per request",
          "kind": "external",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "POST /charges",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "handler",
          "label": "authed request",
          "kind": "sync"
        },
        {
          "from": "handler",
          "to": "idem",
          "label": "key seen before?",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "idem",
          "to": "client",
          "label": "replay: return stored 202",
          "kind": "sync",
          "dashed": true,
          "tone": "accent"
        },
        {
          "from": "handler",
          "to": "txn",
          "label": "first time: BEGIN",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "txn",
          "to": "queue",
          "label": "enqueue receipt",
          "kind": "async"
        },
        {
          "from": "queue",
          "to": "dlq",
          "label": "after max retries",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "handler",
          "to": "otel",
          "label": "emit span",
          "kind": "data",
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "idempotency boundary — the replay short-circuit"
        },
        {
          "tone": "success",
          "label": "the one atomic transaction (no partial write)"
        },
        {
          "tone": "warning",
          "label": "failure containment (DLQ)"
        },
        {
          "tone": "muted",
          "label": "observability — the trace that proves it"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The handler, one boundary at a time",
      "subtitle": "An end-to-end slice: validate → auth → idempotency replay → ONE transaction (charge + key + enqueue) → 202, traced. This is the artifact's selected approach.",
      "filename": "charges.handler.ts",
      "language": "ts",
      "code": "const Body = z.object({ amountCents: z.number().int().positive(), currency: z.literal('usd') })\n\nexport async function postCharge(req: Request): Promise<Response> {\n  return tracer.startActiveSpan('POST /charges', async (span) => {\n    const key = req.headers.get('Idempotency-Key')\n    if (!key) return json(400, { error: 'Idempotency-Key header required' })\n\n    const actor = await authorize(req)              // auth boundary — 401/403 before any write\n    if (!actor) return json(401, { error: 'unauthenticated' })\n\n    const parsed = Body.safeParse(await req.json())  // validate at the boundary\n    if (!parsed.success) return json(422, { error: 'invalid body', detail: parsed.error.issues })\n\n    const replay = await idem.get(actor.id, key)     // exactly-once check\n    if (replay) { span.setAttribute('idem.replay', true); return json(202, replay.result) }\n\n    const result = await db.transaction(async (tx) => {     // ONE atomic unit\n      const charge = await tx.charges.insert({ actorId: actor.id, ...parsed.data })\n      await tx.idempotencyKeys.insert({ actorId: actor.id, key, result: { id: charge.id } }) // unique(actorId,key)\n      await tx.outbox.insert({ topic: 'receipt.email', chargeId: charge.id })  // enqueue in-txn → no lost event\n      return { id: charge.id }\n    })\n    return json(202, result)                          // 202: accepted; receipt email is async\n  })\n}",
      "steps": [
        {
          "lines": [
            1,
            9,
            10,
            11
          ],
          "label": "Request/response contract + validation",
          "note": "The contract is the boundary: missing Idempotency-Key is 400, a malformed body is 422 — distinct status codes, never a generic 500. Untrusted input is parsed with Zod before anything touches the DB."
        },
        {
          "lines": [
            6,
            7
          ],
          "label": "Auth boundary",
          "note": "Authorize BEFORE any side effect. A 401/403 must never leave a half-written charge behind. This is the OWASP API broken-authorization control on the write path."
        },
        {
          "lines": [
            13,
            14
          ],
          "label": "Idempotency replay — the exactly-once short-circuit",
          "note": "Same actor + same key already seen? Return the STORED first result with no re-execution. This single line is the difference between one charge and two."
        },
        {
          "lines": [
            16,
            17,
            18,
            19,
            20
          ],
          "label": "One transaction: charge + key + outbox, atomic",
          "note": "The unique(actorId,key) constraint makes a concurrent duplicate fail at the DB, not in app logic. The outbox row is inserted INSIDE the transaction — the queue event can never be lost or fire without the charge (transactional outbox pattern)."
        },
        {
          "lines": [
            21
          ],
          "label": "202 + async after-effect",
          "note": "The receipt email is enqueued, not awaited — 202 Accepted communicates 'durably recorded, side-effect in flight'. A queue worker drains the outbox; poison messages land in the DLQ."
        },
        {
          "lines": [
            3,
            15,
            21
          ],
          "label": "Observability wraps the whole path",
          "note": "One span per request, tagged idem.replay on the short-circuit. When this fails in prod, the trace shows exactly which boundary the request died at — the operator evidence the packet requires."
        }
      ],
      "caption": "Fire this twice with one Idempotency-Key: the first commits, the second hits line 14 and returns the stored result. One charge. That replay IS the proof."
    },
    {
      "type": "compare",
      "title": "Hollow handler vs the assembled path",
      "subtitle": "Same endpoint, same retry. One leaks money and loses events; one is exactly-once and durable.",
      "left": {
        "label": "Hollow (happy-path only)",
        "tone": "warning",
        "lines": [
          "No Idempotency-Key → retry = second charge",
          "Body trusted, no validation → 500 on bad input",
          "Charge committed, THEN email sent separately",
          "Crash between commit and enqueue → lost receipt",
          "No span → 'it failed somewhere' in prod",
          "Proof = a single 200 under zero retries"
        ],
        "verdict": "Charges twice; loses events; unprovable"
      },
      "right": {
        "label": "Assembled (exactly-once)",
        "tone": "success",
        "lines": [
          "Idempotency-Key + unique(actorId,key) → replay returns first result",
          "Zod at the boundary → 422 with field detail",
          "Charge + key + outbox in ONE transaction",
          "Crash rolls back all three — no partial write",
          "Span per request, idem.replay tagged",
          "Proof = duplicate request shows ONE row, ONE charge, ONE event"
        ],
        "verdict": "Safe to retry; durable; reviewable"
      },
      "caption": "The contract test, integration test, idempotency replay, and rollback evidence on the right are exactly the four proofs api_contract_and_failure_repair_packet.md demands."
    },
    {
      "type": "callout",
      "tone": "note",
      "text": "Standards grounding: OWASP API Top 10 puts broken object/function authorization at the top — so the auth boundary fires before any write, never after. SWEBOK V4 frames this as construction-for-verification: the unique(actorId,key) constraint and the transactional outbox move correctness from 'the app remembers to check' to 'the database makes the wrong state impossible'. The senior tell is pushing the invariant down to the constraint, where a careless caller can't bypass it."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence that this is exactly-once, not just a happy-path answer.' Which artifact line actually proves it?",
      "options": [
        "A passing 200 response from a single POST /charges with no retries.",
        "A replay test: the same Idempotency-Key sent twice returns the stored first result, and the DB shows one charge row and one outbox event.",
        "A unit test asserting the Zod schema rejects a negative amount.",
        "A screenshot of the endpoint documented in the OpenAPI spec."
      ],
      "answer": 1,
      "explanation": "Exactly-once is a property of the DUPLICATE request, so only the replay test exercises it — same key twice, one row, one event, one charge. The single 200 (1) never retries, so it can't distinguish a safe handler from a double-charging one. Validation (3) and docs (4) are real controls but answer a different question; they don't prove the retry is safe."
    },
    {
      "type": "verification",
      "intro": "Prove the path is exactly-once — no vibes:",
      "items": [
        "Contract test: missing Idempotency-Key returns 400, malformed body returns 422 (not 500), success returns 202. Distinct codes, asserted.",
        "Integration test: a valid POST creates exactly one charge row, one idempotency-key row, and one outbox event — count them, don't eyeball.",
        "Idempotency replay: fire the identical request twice with the same key; the second returns the stored result and the row/charge/event counts do NOT increase.",
        "DB rollback evidence: force the outbox insert to throw mid-transaction; confirm the charge row was rolled back too — no partial write survives.",
        "Trace check: the replay request's span carries idem.replay=true, so an operator can tell a deduped retry from a fresh charge in the logs."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Roll-call the path from memory: contract → validation → auth → idempotency check → transaction (charge + key + outbox) → 202 → queue/DLQ → trace. Name what each boundary is FOR.",
        "Explain why the outbox row is inserted INSIDE the transaction instead of enqueuing after commit — what failure does that prevent?",
        "Your reviewer claims the handler 'works'. Point to the one test that would change their mind if it failed, and say what it proves that a 200 cannot."
      ]
    },
    {
      "type": "transfer",
      "text": "The exact same packet ports to any state-changing endpoint where a retry must not double-act: a webhook receiver (dedupe on the provider's event id), a billing/subscription endpoint, an internal API that debits inventory, or a queue worker draining its own redeliveries. What stays invariant: at-least-once delivery is the default, so the side effect is bound to a remembered key, committed in one transaction, and proven by a duplicate request — never by a happy-path 200. What changes with context: where the key comes from (client header vs provider event id vs message id), how long you retain it, and how much latency the synchronous check can cost. Carry api_contract_and_failure_repair_packet.md into the capstone unchanged in structure — it is your portfolio claim: not 'I understand backend integration' but 'here is a service path I framed, built, broke, repaired, and proved exactly-once — replay it yourself.'"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "backend-capstone": [
    {
      "type": "sprint-contract",
      "outcome": "Take one state-changing endpoint and assemble the whole course into a single reviewable artifact: api_contract_and_failure_repair_packet.md. It must carry the request/response contract, the auth boundary, the persistence + idempotency story, the queue/DLQ path, the observability hooks, and one realistic production failure you broke and repaired — proven, not asserted.",
      "intensity": "capstone",
      "time": "45–60 min",
      "proof": "api_contract_and_failure_repair_packet.md with: context · assumptions · selected approach · rejected approach · expected failure · verification method (contract test, integration test, idempotency replay, rollback evidence) · reviewer objection · repair notes · capstone connection. A reviewer reads it and can name the exact evidence that proves the path survives a duplicate request, not just a happy one.",
      "unlock": "Your packet shows the full slice — contract, auth, persistence+idempotency, queue/DLQ, observability — with a broken case that changed behavior and a proof output that catches it.",
      "doNotClaim": "Do not claim a capstone because you described the system fluently. Vocabulary is not evidence. If your packet has no idempotency replay and no rollback proof, you proved the happy path and nothing else — that caps at 78. The capstone is the artifact a reviewer can re-run, not the paragraph they can nod at."
    },
    {
      "type": "mission",
      "text": "The retry fires at 2 a.m.: a checkout client never got your 200, so it sends the exact same charge again. Every skill in this course — the contract, the auth check, the transaction, the idempotency key, the queue, the DLQ, the trace — either fuses into one path that charges once, or the customer gets billed twice and the incident has your name on it."
    },
    {
      "type": "context",
      "text": "This is the synthesis phase: the course taught request/response contracts, status codes, pagination, auth, persistence, idempotency, queues, DLQs, events, observability, testing, rate limits, and versioning as separate moves. The capstone is the proof they compose into one production-shaped slice — and that you can break it, repair it, and show the evidence."
    },
    {
      "type": "pretest",
      "prompt": "Your POST /charges endpoint validates input, checks auth, writes a row, and returns 200. A network blip makes the client retry the identical request. What breaks — and which earlier lesson was supposed to prevent it? Answer before reading on.",
      "reveal": "Two charges, one intent. The endpoint is correct on every axis except the one that matters at scale: it has no idempotency boundary, so a retry — the most ordinary event on the internet — duplicates a side effect. The fix isn't 'validate harder'; it's an idempotency key persisted inside the same transaction as the charge, so the second request returns the first result instead of repeating it. A capstone fails here precisely because each skill looked done in isolation."
    },
    {
      "type": "concept",
      "title": "A capstone is one slice where every contract holds at once",
      "text": "Assemble the moves into a single state-changing path: validated contract → auth boundary → transaction that persists the idempotency key with the write → async work via queue with a DLQ → traces and structured logs. The invariant: the path is exactly-once-effecting and reviewable, never just demonstrable."
    },
    {
      "type": "diagram",
      "title": "The assembled charge path — the whole course in one slice",
      "subtitle": "POST /charges runs the full stack: contract → rate limit → auth → idempotency-guarded transaction → async fulfillment with a DLQ, with traces and logs on every hop. The duplicate-request path (dashed) is the failure this capstone exists to survive.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Client",
          "description": "retries on timeout",
          "kind": "client"
        },
        {
          "id": "gateway",
          "label": "API Gateway",
          "description": "rate limit · TLS · versioned route",
          "kind": "service"
        },
        {
          "id": "handler",
          "label": "Charge Handler",
          "description": "validate contract · auth boundary",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "idem",
          "label": "Idempotency check",
          "description": "key seen before?",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "db",
          "label": "Postgres (txn)",
          "description": "charge + idem key, one commit",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "queue",
          "label": "Fulfillment Queue",
          "description": "async receipt + ledger",
          "kind": "queue",
          "tone": "accent"
        },
        {
          "id": "dlq",
          "label": "Dead-letter Queue",
          "description": "poison messages park here",
          "kind": "queue",
          "tone": "warning"
        },
        {
          "id": "obs",
          "label": "Traces + Logs",
          "description": "request id on every hop",
          "kind": "external",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "gateway",
          "label": "POST /v1/charges",
          "kind": "sync"
        },
        {
          "from": "gateway",
          "to": "handler",
          "label": "200 / 4xx / 429",
          "kind": "sync"
        },
        {
          "from": "handler",
          "to": "idem",
          "label": "Idempotency-Key",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "idem",
          "to": "db",
          "label": "new → write in txn",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "idem",
          "to": "client",
          "label": "seen → replay stored 200",
          "kind": "sync",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "db",
          "to": "queue",
          "label": "enqueue after commit",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "queue",
          "to": "dlq",
          "label": "on repeated failure",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "handler",
          "to": "obs",
          "label": "span + structured log",
          "kind": "data",
          "dashed": true,
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the synthesized happy path"
        },
        {
          "tone": "warning",
          "label": "idempotency + DLQ — the failure-survival edges"
        },
        {
          "tone": "success",
          "label": "the one transaction that makes it exactly-once"
        },
        {
          "tone": "muted",
          "label": "observability woven through, not bolted on"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The end-to-end slice — contract, auth, idempotent transaction, queue, trace",
      "subtitle": "One handler that folds the course together. The idempotency key and the charge commit in the SAME transaction — that single fact is what makes a retry safe.",
      "filename": "charge_handler.ts",
      "language": "ts",
      "code": "// POST /v1/charges — the capstone slice\nexport async function createCharge(req: Request, res: Response) {\n  const span = tracer.startSpan('createCharge', { requestId: req.id })\n  // 1. Contract: validate the request body against the schema (fail fast, 422)\n  const body = ChargeSchema.safeParse(req.body)\n  if (!body.success) return res.status(422).json({ error: body.error.message })\n  // 2. Auth boundary: the caller must own the account it is charging\n  if (!can(req.actor, 'charge:create', body.data.accountId))\n    return res.status(403).json({ error: 'forbidden' })\n  const key = req.header('Idempotency-Key')\n  if (!key) return res.status(400).json({ error: 'Idempotency-Key required' })\n  try {\n    // 3. Persistence + idempotency in ONE transaction\n    const charge = await db.tx(async (t) => {\n      const prior = await t.idempotency.find(key)\n      if (prior) return prior.response          // replay: same key, same answer\n      const c = await t.charges.insert(body.data) // the side effect\n      await t.idempotency.insert(key, c)          // committed atomically with it\n      return c\n    })\n    // 4. Async fulfillment AFTER commit (queue owns receipt + ledger; DLQ catches poison)\n    await queue.publish('charge.created', { chargeId: charge.id, requestId: req.id })\n    log.info({ requestId: req.id, chargeId: charge.id, replay: charge.replayed ?? false })\n    return res.status(charge.replayed ? 200 : 201).json(charge)\n  } catch (err) {\n    log.error({ requestId: req.id, err: serialize(err) }) // transaction already rolled back\n    return res.status(500).json({ error: 'charge_failed', requestId: req.id })\n  } finally {\n    span.end()\n  }\n}",
      "steps": [
        {
          "lines": [
            5,
            6
          ],
          "label": "The request/response contract",
          "note": "Schema validation at the boundary: a malformed body fails fast with 422, never reaching the database. This is the contract lesson, enforced."
        },
        {
          "lines": [
            8,
            9
          ],
          "label": "The auth boundary",
          "note": "Authentication answers 'who'; authorization answers 'may they touch THIS account'. The check is per-resource, not a blanket login gate."
        },
        {
          "lines": [
            10,
            11
          ],
          "label": "Idempotency key is mandatory",
          "note": "A state-changing endpoint without a required key cannot be safely retried. Reject the request that omits it — 400, not silent acceptance."
        },
        {
          "lines": [
            14,
            15,
            16,
            17,
            18,
            19
          ],
          "label": "The atomic core",
          "note": "The charge and the idempotency key insert in ONE transaction. A retry finds the prior key and replays the stored response — exactly-once effecting. This is the line that makes 2 a.m. safe."
        },
        {
          "lines": [
            22
          ],
          "label": "Queue AFTER commit",
          "note": "Publish only after the transaction commits, or you enqueue work for a charge that rolled back. The DLQ catches a fulfillment message that keeps failing instead of blocking the queue."
        },
        {
          "lines": [
            3,
            23,
            26,
            30
          ],
          "label": "Observability woven through",
          "note": "A span around the call and a structured log carrying requestId on success AND failure. When this pages someone, the trace already exists — it is not added during the incident."
        }
      ],
      "caption": "Run the idempotency replay test: send the identical request twice, assert one row in charges and two 2xx responses with the same chargeId. That single assertion proves the whole slice."
    },
    {
      "type": "callout",
      "tone": "note",
      "text": "OWASP API Security Top 10 puts Broken Object Level Authorization at #1 for a reason: the most common capstone failure is an endpoint that authenticates the caller but never checks they own the specific resource. 'Logged in' is not 'allowed to charge THAT account'. A reviewer's first probe is always a valid token against someone else's id — your packet should already show that test returning 403."
    },
    {
      "type": "compare",
      "title": "Hollow capstone vs proven capstone",
      "subtitle": "Same endpoint described two ways. One is a fluent paragraph; one is a re-runnable artifact a reviewer trusts.",
      "left": {
        "label": "Hollow (vocabulary, no evidence)",
        "tone": "warning",
        "lines": [
          "\"It's idempotent, validated, and observable\" — asserted, not shown",
          "Idempotency key written AFTER the charge, separate write",
          "No replay test: duplicate request was never sent",
          "Queue publish before commit — orphan jobs on rollback",
          "Logs added only when the incident already started",
          "No rejected option, no broken case, no rollback proof"
        ],
        "verdict": "Caps at 78 — happy path only"
      },
      "right": {
        "label": "Proven (artifact + failure + proof)",
        "tone": "success",
        "lines": [
          "Charge + idem key commit in one transaction",
          "Idempotency replay test: 2 requests → 1 row, 2 matching 2xx",
          "Integration test for the BOLA case → 403 on cross-account",
          "Queue publishes after commit; DLQ catches poison messages",
          "requestId on every span and log, success and failure",
          "Packet names the rejected option + the repaired broken case"
        ],
        "verdict": "Reviewable, re-runnable, exactly-once"
      },
      "caption": "The difference is not eloquence — it is the replay test and the rollback evidence. A capstone is the path you can break in front of a reviewer and watch survive."
    },
    {
      "type": "debug",
      "symptom": "Load test shows duplicate charges under retries, and the queue occasionally references a chargeId that does not exist in Postgres.",
      "brokenCode": "const charge = await db.charges.insert(body.data)\nawait queue.publish('charge.created', { chargeId: charge.id })\nawait db.idempotency.insert(key, charge) // separate write, after the publish",
      "language": "ts",
      "task": "Two production bugs hide here. Name both and reorder the operations so a retry is safe and the queue never sees a phantom charge.",
      "fix": "Bug 1: the idempotency key is written in a separate statement, so a retry that lands between the charge insert and the key insert creates a second charge — the key must commit in the SAME transaction as the charge. Bug 2: the queue publish happens before the charge is durably committed, so a rollback leaves the queue holding a chargeId that never existed. Fix: wrap the charge insert and the idempotency insert in one db.tx(...) and publish to the queue only AFTER that transaction commits."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Point to the exact evidence that proves this endpoint is usable in production, not just understood.' Which response actually does that?",
      "options": [
        "\"It uses idempotency keys, a transaction, a queue with a DLQ, and structured logging.\"",
        "\"Here is the idempotency replay test (two identical requests → one charge row, two matching 2xx), the cross-account integration test returning 403, and the rollback evidence showing no orphaned queue job — all in the packet, all re-runnable.\"",
        "\"The code passed code review and the team approved the design.\"",
        "\"I followed OWASP and the request/response contract from the earlier lessons.\""
      ],
      "answer": 1,
      "explanation": "The reviewer challenge demands evidence, not description. Only option 2 points to concrete, re-runnable proof — a replay test, an authorization test, and rollback evidence — that catches the specific production failures. Options 1 and 4 list vocabulary; option 3 cites approval, not behavior. The capstone passes on artifacts a reviewer can execute, not claims they can read."
    },
    {
      "type": "verification",
      "intro": "Prove the slice — no vibes. Each item is a line in api_contract_and_failure_repair_packet.md:",
      "items": [
        "Contract test: a malformed body returns 422 and never reaches the database; a valid body returns 201 with the documented shape.",
        "Idempotency replay: send the identical request twice and assert exactly one row in charges and two 2xx responses carrying the same chargeId.",
        "Authorization (BOLA) test: a valid token for account A charging account B returns 403 — run it, paste the output.",
        "Rollback evidence: force the transaction to fail mid-write and confirm no charge row AND no queue message survive.",
        "DLQ proof: a fulfillment message that always throws lands in the dead-letter queue instead of blocking the queue, and is observable there.",
        "Observability: a single requestId threads the span and the structured log on both the success and the 500 path — show both log lines."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Roll-call the course: name each skill this endpoint uses — contract, status codes, auth boundary, persistence, idempotency, queue, DLQ, events, observability, testing, rate limits, versioning — and where in the slice each one lives.",
        "Explain why the idempotency key MUST commit in the same transaction as the charge, using your own replay test as the evidence.",
        "Defend the reviewer objection: point to the exact proof in your packet that the endpoint survives a duplicate request and a cross-account attempt — not the paragraph that says it does."
      ]
    },
    {
      "type": "transfer",
      "text": "You came in able to name twelve backend skills; you leave able to fuse them into one path that charges exactly once at 2 a.m. and to hand a reviewer the evidence that proves it. The same packet structure transfers move-for-move: a webhook receiver (idempotency on event id, DLQ on poison payloads), a billing endpoint (the slice you just built), an internal API (auth boundary becomes service-to-service mTLS), a queue worker (the idempotency boundary moves to the consumer). What stays invariant is the discipline — a contract, an auth boundary, an atomic side effect, an async path with a dead-letter, traces on every hop, and a broken case you proved you survive. What changes is the cost of being wrong: a double-charge bills a customer, a double-fulfilled webhook double-ships an order. api_contract_and_failure_repair_packet.md is now portfolio evidence — not 'I understand backend engineering' but 'here is a production path I framed, built, broke, repaired, and proved. Re-run it.' That is the capstone. Carry it forward."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ]
}