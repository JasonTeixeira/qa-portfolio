{
  "product-workflow-states": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real user-facing workflow (a checkout step, a form submit, a feed load) and build component_state_matrix_and_accessibility_packet.md: every UI state the component can hold — loading, empty, error, disabled, success — each with its trigger, its visible+accessible render, and the keyboard/screen-reader behavior. Then prove one failing state path with a checklist.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a state matrix where every row is a reachable UI state with trigger, render, and a11y note, plus a verification section (state diagram, keyboard checklist, viewport-failure note). A reviewer reaches every state from the diagram and confirms none is a dead end or a silent failure.",
      "unlock": "Your matrix enumerates loading, empty, error, disabled, and success as first-class states — each with a trigger, an accessible render, and a recovery path — and no state is reachable-but-unrendered.",
      "doNotClaim": "Do not claim you 'handle the states' if your matrix only lists the happy path (loading → success). The skill is the OTHER states: empty, error, disabled, and the transitions between them. A component that renders nothing on error is not 'handling error' — it is hiding a failure from the user."
    },
    {
      "type": "mission",
      "text": "A user taps Submit on a slow connection, sees nothing change, taps again — and books the appointment twice. The button had a success path and a failure path, but no loading state and no disabled state, so the gap between click and response became a bug with the customer's name on it."
    },
    {
      "type": "context",
      "text": "Every interactive surface is a small state machine: it is loading, empty, erroring, disabled, or done — never just 'working'. Naming those states up front is what turns a demo that looks finished into a product that survives a slow API, an empty result, and a keyboard-only user."
    },
    {
      "type": "pretest",
      "prompt": "A reviewer asks: \"What does this list show before the data arrives, and what does it show when the data arrives empty?\" You built it and it looked fine. Why is that the wrong evidence? Answer before reading on.",
      "reveal": "Because \"it looked fine\" means you only saw the success state on a fast connection with seeded data. Loading was invisible (data was instant), empty never happened (the fixture had rows), error never fired (localhost doesn't 500). The states that break in production are exactly the ones a happy local run never renders. The matrix exists to force you to render each state on purpose — not wait for a user to find the one you skipped."
    },
    {
      "type": "concept",
      "title": "A component holds states, not a state — enumerate them before you style one",
      "text": "Every async UI is a set of mutually exclusive states: loading, empty, error, disabled, success. The invariant: every state a component can REACH must have a defined render and an accessible cue. An unrendered reachable state is a silent failure."
    },
    {
      "type": "diagram",
      "title": "The five states of an async workflow component",
      "subtitle": "A submit/fetch component is a state machine. idle → loading branches to success, empty, or error; disabled gates re-entry. The failure path (error) and its repair path (retry → loading) are first-class, not afterthoughts.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "idle",
          "label": "idle",
          "description": "ready, awaiting input",
          "kind": "process",
          "tone": "default"
        },
        {
          "id": "disabled",
          "label": "disabled",
          "description": "guarded: invalid form / in-flight",
          "kind": "decision",
          "tone": "muted"
        },
        {
          "id": "loading",
          "label": "loading",
          "description": "request in flight · aria-busy",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "success",
          "label": "success",
          "description": "data rendered · focus moved",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "empty",
          "label": "empty",
          "description": "200 but zero rows",
          "kind": "store",
          "tone": "default"
        },
        {
          "id": "error",
          "label": "error",
          "description": "4xx/5xx/timeout · role=alert",
          "kind": "store",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "idle",
          "to": "disabled",
          "label": "invalid input",
          "kind": "control",
          "tone": "muted"
        },
        {
          "from": "idle",
          "to": "loading",
          "label": "submit (then disable)",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "loading",
          "to": "success",
          "label": "200 + rows",
          "kind": "async",
          "tone": "success"
        },
        {
          "from": "loading",
          "to": "empty",
          "label": "200 + zero rows",
          "kind": "async"
        },
        {
          "from": "loading",
          "to": "error",
          "label": "4xx / 5xx / timeout",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "error",
          "to": "loading",
          "label": "retry (repair path)",
          "kind": "control",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "in-flight (loading) — must disable the trigger"
        },
        {
          "tone": "warning",
          "label": "error + its retry repair path"
        },
        {
          "tone": "success",
          "label": "success — the only state demos usually show"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Rendering all five states, not just success",
      "subtitle": "A typed status union forces every branch — TypeScript won't compile if you forget one. This is the artifact's render contract.",
      "filename": "OrderList.tsx",
      "language": "ts",
      "code": "type Status =\n  | { kind: 'loading' }\n  | { kind: 'empty' }\n  | { kind: 'error'; message: string }\n  | { kind: 'success'; orders: Order[] }\n\nfunction OrderList({ status, onRetry }: Props) {\n  switch (status.kind) {\n    case 'loading':\n      return <Spinner aria-busy=\"true\" aria-label=\"Loading orders\" />\n    case 'empty':\n      return <Empty title=\"No orders yet\" hint=\"They'll appear here after checkout.\" />\n    case 'error':\n      return (\n        <div role=\"alert\">\n          <p>{status.message}</p>\n          <button onClick={onRetry}>Retry</button>\n        </div>\n      )\n    case 'success':\n      return <ul>{status.orders.map((o) => <OrderRow key={o.id} order={o} />)}</ul>\n  }\n}",
      "steps": [
        {
          "lines": [
            1,
            2,
            3,
            4,
            5
          ],
          "label": "The status union IS the contract",
          "note": "A discriminated union enumerates every reachable state in one place. 'success' carries the data; 'error' carries the message. You cannot render the component without naming a kind."
        },
        {
          "lines": [
            8
          ],
          "label": "Exhaustive switch",
          "note": "The switch over status.kind makes TypeScript your reviewer: add a sixth state and forget a case, and the build fails. The state matrix becomes compiler-enforced."
        },
        {
          "lines": [
            9,
            10
          ],
          "label": "loading announces itself",
          "note": "aria-busy + aria-label means a screen reader says 'Loading orders', not silence. The visual spinner alone is invisible to assistive tech — WCAG 2.2 Status Messages (4.1.3)."
        },
        {
          "lines": [
            11,
            12
          ],
          "label": "empty is a designed state, not a blank",
          "note": "200-with-zero-rows is the most-skipped state. A blank screen reads as 'broken'; a titled empty state with a hint reads as 'working, nothing here yet'."
        },
        {
          "lines": [
            13,
            14,
            15,
            16,
            17
          ],
          "label": "error carries a message AND a repair path",
          "note": "role=\"alert\" makes the screen reader announce the failure immediately. The Retry button is the repair edge from the state diagram — error must lead somewhere, never dead-end."
        },
        {
          "lines": [
            18,
            19
          ],
          "label": "success is one branch of five",
          "note": "The happy path is the last case, not the only one. If this were the whole component, the other four states would render as undefined — silent failures."
        }
      ],
      "caption": "Add a state to the union and the switch stops compiling until you render it. The matrix is no longer documentation — it's a type the component must satisfy."
    },
    {
      "type": "compare",
      "title": "Happy-path component vs a state-complete component",
      "subtitle": "Same OrderList. One renders on the demo; one survives a slow API, an empty result, and a keyboard user.",
      "left": {
        "label": "Happy-path only",
        "tone": "warning",
        "lines": [
          "Renders data when it arrives; nothing before",
          "Empty API result → blank screen, reads as broken",
          "Fetch 500 → undefined render, no message, no retry",
          "Submit stays clickable in-flight → double submit",
          "Spinner is visual only → screen reader hears silence"
        ],
        "verdict": "Looks done on localhost; fails the first real user"
      },
      "right": {
        "label": "State-complete (the matrix)",
        "tone": "success",
        "lines": [
          "loading: spinner + aria-busy before data",
          "empty: titled state with a hint, not a blank",
          "error: role=alert message + a Retry repair path",
          "disabled: trigger gated while in-flight + on invalid input",
          "success: data rendered, focus moved to the result"
        ],
        "verdict": "Every reachable state has a render and an accessible cue"
      },
      "caption": "The gap between these two is never the success state — it's the four states a fast local run never makes you render. Measure the component by its error and empty states, not its demo."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell isn't more states — it's the accessible cue on each. WCAG 2.2 Status Messages (4.1.3) says a state change a sighted user sees (spinner, error banner, 'saved') must also reach assistive tech via role=\"alert\" or aria-live — without moving focus. A visually perfect error banner that a screen reader never announces is a silent failure for the users who most need the signal."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: \"Show me the exact evidence this component isn't just a happy-path answer.\" Which artifact proves it?",
      "options": [
        "A screenshot of the component rendering the loaded list with data.",
        "A state matrix where loading, empty, error, and disabled each have a trigger, an accessible render, and (for error) a retry path — plus a keyboard checklist and a note on the viewport where it breaks.",
        "A statement that the component \"handles all the states correctly.\"",
        "Passing tests for the success case and clean TypeScript types."
      ],
      "answer": 1,
      "explanation": "Only option 2 is evidence of the non-happy-path states — the ones a demo skips. A loaded screenshot (1) and success-only tests (4) prove the easy state; the bare claim (3) is the vibe the reviewer is challenging. The matrix forces each reachable state to have a render AND an accessible cue, and the keyboard + viewport notes prove it was exercised, not assumed."
    },
    {
      "type": "verification",
      "intro": "Prove your state matrix is complete — no vibes:",
      "items": [
        "List every state your component can REACH (loading, empty, error, disabled, success). For each, point to the exact render. Any reachable state with no render is a silent failure — fix it.",
        "Force the error state on purpose (block the network, return a 500). Confirm a message appears AND a screen reader announces it via role=\"alert\" or aria-live — not just a visual banner.",
        "Force the empty state (a query with zero results). Confirm it renders a designed empty state with a hint, not a blank screen that reads as broken.",
        "Tab to the trigger with the keyboard only, activate it, and confirm it disables in-flight (no double submit) and that focus lands somewhere sensible on success — not lost at the top of the page.",
        "Shrink the viewport to 320px and confirm no state overflows or clips the error message or retry button. Note the smallest width each state survives."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why is the empty state (200 with zero rows) the one most teams skip, and what does a user conclude when it renders as a blank screen?",
        "Explain the difference between a state that LOOKS handled (a visible error banner) and one that IS handled (announced to assistive tech) — using WCAG 4.1.3 and your own component.",
        "Walk the error → retry edge of your state diagram: what triggers error, what the user sees, and how they get back to loading without reloading the page."
      ]
    },
    {
      "type": "transfer",
      "text": "Same matrix, any async surface. A payment form has the identical five states (validating, processing, declined, succeeded, disabled-while-pending) — only the labels and the stakes change. A search box has loading, no-results, error, and results. What stays invariant: every reachable state needs a render and an accessible cue, and error must carry a repair path. What changes with context: a payment's error needs more recovery guidance than a search's, and a feed's empty state is reassuring where a checkout's empty cart is a conversion problem. This artifact — component_state_matrix_and_accessibility_packet.md — is your first portfolio evidence for this course: not \"I know about UI states\" but \"here is a workflow I enumerated, made accessible, and proved across loading, empty, error, disabled, and success.\" The capstone reuses it: the fullstack product dashboard with browser proof carries this exact matrix into a real, verified screen."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "semantic-html-structure": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real product surface (a content page, an article, or a dashboard panel) and rebuild its skeleton in semantic HTML: one main landmark, a heading outline with no level skips, and every interactive control a real button or link. Then record it as the structure section of your component_state_matrix_and_accessibility_packet.md.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "The 'Semantic structure' section of component_state_matrix_and_accessibility_packet.md: a landmark map (header / nav / main / footer), a heading outline (h1 → h2 → h3, no skips), and a control inventory marking every clickable thing as button vs link. Proof item: run the page through the browser's accessibility tree and a screen-reader landmark list — both must match your map with zero div-soup controls.",
      "unlock": "Your page exposes exactly one main landmark, a gap-free heading outline, and zero clickable divs — confirmed in the browser accessibility tree, not just the source.",
      "doNotClaim": "Do not claim a page is 'semantic' because it validates as HTML. <div class=\"button\"> validates and renders identically — but it is invisible to the accessibility tree, unfocusable by keyboard, and silent to a screen reader. Validation checks syntax; semantics is about whether the meaning survives into the accessibility tree."
    },
    {
      "type": "mission",
      "text": "A keyboard-only user lands on your checkout, hits Tab to reach 'Place order', and the focus ring skips clean past it — because the team shipped <div onClick> instead of <button>. The visual demo passed; the accessibility tree had no order button at all."
    },
    {
      "type": "context",
      "text": "Semantic HTML is the foundation every later frontend skill stands on: CSS targets it, component state decorates it, and accessibility, screen readers, and even your test selectors read it. Get the skeleton wrong here and you pay for it in every lesson that follows — forms, state, accessibility, and the capstone all assume the structure underneath is real."
    },
    {
      "type": "pretest",
      "prompt": "These two render pixel-identical: <div class=\"btn\" onClick={submit}>Save</div> and <button onClick={submit}>Save</button>. If they look the same and both fire on click, what actually breaks with the first one? Answer before reading on.",
      "reveal": "The div is missing everything the browser gives <button> for free: it is not in the tab order (no Tab focus), Enter and Space do not fire it, it is announced as plain text instead of 'Save, button', and it has no disabled/aria-pressed semantics for component state. You would have to re-add tabindex, role, keydown handlers, and focus styles by hand — and most teams forget at least two. The element IS the behavior; choosing <div> means re-implementing a button badly."
    },
    {
      "type": "concept",
      "title": "The element IS the meaning — structure feeds the accessibility tree",
      "text": "Choose the tag for what the content MEANS (navigation, main, heading, button), not how it looks. The invariant: the browser builds an accessibility tree from your tags. Right tag means free focus, roles, and announcements; a styled div means none of it."
    },
    {
      "type": "diagram",
      "title": "How HTML becomes the accessibility tree (and where div-soup dies)",
      "subtitle": "Two source paths into the same renderer. The semantic path produces a real accessibility tree with landmarks and named controls; the div path renders pixels but feeds the screen reader nothing. The failure edge is dashed; the repair edge swaps the tag.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "semantic",
          "label": "Semantic source",
          "description": "main / nav / h1 / button",
          "kind": "process",
          "tone": "success"
        },
        {
          "id": "divsoup",
          "label": "Div-soup source",
          "description": "div onClick + class=\"btn\"",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "dom",
          "label": "DOM",
          "description": "parsed element tree",
          "kind": "service"
        },
        {
          "id": "render",
          "label": "Render tree",
          "description": "pixels on screen",
          "kind": "store",
          "tone": "muted"
        },
        {
          "id": "a11ytree",
          "label": "Accessibility tree",
          "description": "roles · names · states",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "sr",
          "label": "Screen reader / keyboard",
          "description": "landmarks · Tab · announcements",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "repair",
          "label": "Repair: swap tag",
          "description": "div → button / nav / main",
          "kind": "decision",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "semantic",
          "to": "dom",
          "label": "parses",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "divsoup",
          "to": "dom",
          "label": "parses",
          "kind": "data",
          "tone": "warning"
        },
        {
          "from": "dom",
          "to": "render",
          "label": "both render identically",
          "kind": "data",
          "tone": "muted"
        },
        {
          "from": "semantic",
          "to": "a11ytree",
          "label": "names + roles survive",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "divsoup",
          "to": "a11ytree",
          "label": "control vanishes",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "a11ytree",
          "to": "sr",
          "label": "exposes meaning",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "repair",
          "to": "semantic",
          "label": "restores the path",
          "kind": "control",
          "dashed": true,
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "accessibility tree — the meaning consumers read"
        },
        {
          "tone": "warning",
          "label": "div-soup: renders but drops out of the a11y tree"
        },
        {
          "tone": "success",
          "label": "semantic tags + the tag-swap repair"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The same panel, rebuilt as a real semantic skeleton",
      "subtitle": "A dashboard 'Recent orders' card — from one landmark down to the named control. This is the artifact you paste into the packet.",
      "filename": "recent-orders-panel.html",
      "language": "ts",
      "code": "<main id=\"content\">\n  <header>\n    <nav aria-label=\"Account\">\n      <a href=\"/orders\" aria-current=\"page\">Orders</a>\n      <a href=\"/billing\">Billing</a>\n    </nav>\n  </header>\n\n  <section aria-labelledby=\"recent-heading\">\n    <h1 id=\"recent-heading\">Recent orders</h1>\n    <article>\n      <h2>Order #4821</h2>\n      <p>Shipped to Chicago, IL</p>\n      <button type=\"button\" aria-pressed=\"false\">Track</button>\n    </article>\n  </section>\n\n  <footer>\n    <a href=\"/help\">Need help?</a>\n  </footer>\n</main>",
      "steps": [
        {
          "lines": [
            1
          ],
          "label": "One main landmark",
          "note": "Exactly one <main> per page. It is the 'skip to content' target and the screen-reader anchor. Two mains, or none, is the most common landmark bug."
        },
        {
          "lines": [
            3
          ],
          "label": "Named navigation",
          "note": "<nav> with aria-label distinguishes this from any other nav on the page. A landmark list reads 'Account navigation' instead of an anonymous region."
        },
        {
          "lines": [
            4
          ],
          "label": "Links navigate, aria-current marks place",
          "note": "An <a href> is a link because it goes somewhere. aria-current=\"page\" is the component-state hook the screen reader announces as 'current page'."
        },
        {
          "lines": [
            11
          ],
          "label": "Heading outline, no skips",
          "note": "h1 then h2 — never h1 then h3. The outline IS the document's table of contents; a skipped level is a hole a screen-reader user falls through."
        },
        {
          "lines": [
            13,
            14
          ],
          "label": "section labelled by its heading",
          "note": "aria-labelledby ties the region to its visible h1, so the landmark is named by text already on screen — no duplicate hidden label to drift out of sync."
        },
        {
          "lines": [
            15
          ],
          "label": "A real button + its state",
          "note": "Track is a <button type=\"button\"> — focusable, Enter/Space-activatable, announced as a button. aria-pressed carries the loading/active component state the next lesson decorates."
        }
      ],
      "caption": "Run this through the browser accessibility tree: you should see one main, one named nav, a clean h1→h2 outline, and a button named 'Track' with a pressed state — all from tags alone, zero ARIA roles invented."
    },
    {
      "type": "compare",
      "title": "Div-soup vs semantic skeleton — same pixels, different reality",
      "subtitle": "Both render an identical card. Only one survives into the accessibility tree, the keyboard, and your test selectors.",
      "left": {
        "label": "Div-soup (weak)",
        "tone": "warning",
        "lines": [
          "<div class=\"card\"> wraps everything, no landmarks",
          "<div class=\"title\"> instead of a heading — no outline",
          "<div class=\"btn\" onclick> — not focusable, not announced",
          "Accessibility tree: a flat bag of 'group' / text",
          "Keyboard user can never Tab to 'Track'",
          "Tests must select by brittle .btn class, not role"
        ],
        "verdict": "Renders fine, fails every non-visual consumer"
      },
      "right": {
        "label": "Semantic skeleton (gold)",
        "tone": "success",
        "lines": [
          "<main> + named <nav> = real landmarks to skip between",
          "<h1>/<h2> = a gap-free heading outline / TOC",
          "<button> = free focus, Enter/Space, announced role",
          "Accessibility tree mirrors the visible structure",
          "Keyboard reaches every control in source order",
          "Tests select by role+name ('button', 'Track') — stable"
        ],
        "verdict": "Same render, intact meaning for every consumer"
      },
      "mono": true,
      "caption": "The visual diff is zero. The accessibility-tree diff is total. 'Looks the same' is exactly the trap — the meaning lives where the eye cannot check it."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 grounds the senior tell here: 1.3.1 Info and Relationships requires that structure conveyed visually is also available programmatically, and 4.1.2 Name, Role, Value requires every control to expose exactly that. A styled div violates both by construction — no role, no name, no keyboard. Pros check the accessibility tree, not the screenshot, because that tree is the contract those success criteria actually test."
    },
    {
      "type": "quiz",
      "question": "A reviewer says 'this page is accessible — it validates and looks identical with CSS off.' Which response actually proves semantic structure?",
      "options": [
        "'It passes the HTML validator, so the structure is correct.'",
        "'Here is the browser accessibility tree and the screen-reader landmark list — one main, a gap-free h1→h2 outline, and every control announced with a role and name; no clickable divs.'",
        "'I added role=\"button\" and tabindex=\"0\" to the divs, so they behave like buttons now.'",
        "'It looks the same as the design, and the onClick handlers all fire.'"
      ],
      "answer": 1,
      "explanation": "Validation (1) checks syntax, not meaning — div-soup validates fine. Visual sameness and firing handlers (4) are exactly the trap from the pretest. Bolting role+tabindex onto divs (3) is the weak version: it re-implements a button by hand, usually missing Enter/Space and disabled state, and is what WCAG 4.1.2 exists to catch. Only (2) points at the accessibility tree and landmark list — the actual evidence that structure survived where non-visual consumers read it."
    },
    {
      "type": "verification",
      "intro": "Prove the structure is real — inspect the accessibility tree, not the screenshot:",
      "items": [
        "Open the browser's accessibility-tree inspector: confirm exactly ONE main landmark, and that every <nav> has a distinct accessible name.",
        "Walk the heading outline top to bottom — h1, then h2, then h3 — and confirm zero skipped levels. A jump from h1 to h3 is a hole; fix it.",
        "Tab through the page with the mouse untouched: every interactive control must receive focus in source order, and Enter/Space must activate buttons. Anything you can click but not Tab to is a div pretending to be a control.",
        "For each control, read its line in the accessibility tree: it must have a role (button/link) AND an accessible name. 'clickable, no name' fails WCAG 4.1.2.",
        "Turn the screen reader's landmark list on: it should let you jump header → nav → main → footer. If main is missing or doubled, the skip-to-content path is broken."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why do <div onClick> and <button> render identically yet differ completely in the accessibility tree? Name three things the button gets for free.",
        "Explain the heading-outline invariant to a teammate: what does a skipped level (h1 → h3) actually do to a screen-reader user navigating by headings?",
        "Your reviewer says 'it validates, ship it.' Reproduce the exact evidence you would show to prove the page is semantic — and name what validation cannot tell you."
      ]
    },
    {
      "type": "transfer",
      "text": "The invariant — 'tag for meaning, verify in the accessibility tree' — travels straight into the next lessons. CSS in Lesson 3 styles these landmarks instead of fighting div-soup; component state (loading, error, disabled) in the forms lesson hangs off real <button> and aria states you already placed; and the accessibility lesson audits the very tree you built here. What stays invariant is the skeleton; what changes is the device (a 360px viewport reflows the same landmarks) and the consumer (a screen reader vs. a keyboard vs. your Playwright getByRole selectors). The capstone's component_state_matrix_and_accessibility_packet.md reuses this exact structure section as its foundation — every state you matrix later decorates a real semantic element, not a div you have to retrofit."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "responsive-layout-grid": [
    {
      "type": "sprint-contract",
      "outcome": "Build one real responsive layout with CSS Grid that reflows across viewport sizes without a single media-query hack — then prove every breakpoint stays usable by recording it in component_state_matrix_and_accessibility_packet.md.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "A working grid (code + the artifact) where the SAME markup reflows from 1 column on mobile to a multi-column layout on desktop with NO horizontal scroll at 320px, NO content clipping, and a documented row in the state matrix for mobile/tablet/desktop. A reviewer resizes the viewport and the layout never overflows or collapses into unreadable columns.",
      "unlock": "Your layout reflows by intrinsic sizing (the grid decides column count), passes the 320px no-overflow check, and the state matrix records the rendered result at each viewport.",
      "doNotClaim": "Do not claim a layout is 'responsive' because it has a media query. A fixed grid that only swaps at three magic pixel widths still breaks at every width in between. Responsive means the grid adapts continuously to the space it is given — the media query is the patch, not the skill."
    },
    {
      "type": "mission",
      "text": "A demo on the big screen, the founder hands their phone to an investor, and the pricing page has a third column hanging off the right edge with a horizontal scrollbar nobody can find. The layout was 'responsive' — on the developer's 27-inch monitor."
    },
    {
      "type": "context",
      "text": "This sits in the foundation phase of frontend engineering: you can write semantic HTML and CSS, now you make it survive every viewport. Layout grid is the skeleton every later skill — component state, forms, accessibility — hangs on; if the skeleton overflows, nothing above it can be usable."
    },
    {
      "type": "pretest",
      "prompt": "You want three cards side-by-side on desktop and stacked on mobile. Your instinct is a media query at 768px that flips display from flex-column to flex-row. What breaks between 320px and 768px — and at 769px on a narrow tablet? Answer before reading.",
      "reveal": "Everything in between is a guess. A single 768px breakpoint says nothing about 400px, 600px, or a 769px tablet held in portrait — the layout is correct at exactly two widths and unverified at every other. The grid way: `grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))` lets the BROWSER compute how many 220px-minimum columns fit the actual width. One line, zero magic numbers, correct at every viewport — because the grid responds to space, not to a pixel you hardcoded."
    },
    {
      "type": "concept",
      "title": "A responsive grid sizes to the space, not to a breakpoint",
      "text": "Declare a minimum column width and let the grid compute the column count for whatever viewport it gets. The invariant: the layout reflows continuously by intrinsic sizing. A media query is a deliberate exception, never the mechanism."
    },
    {
      "type": "diagram",
      "title": "How one grid reflows across three viewports",
      "subtitle": "The SAME markup and SAME CSS. The viewport width feeds auto-fit + minmax; the browser computes the column count. The 320px overflow path (the failure this lesson kills) is marked.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "markup",
          "label": "Semantic HTML",
          "description": "one card list, no per-size variants",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "grid",
          "label": "Grid container",
          "description": "auto-fit · minmax(220px, 1fr)",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "viewport",
          "label": "Viewport width",
          "description": "the input the grid responds to",
          "kind": "external",
          "tone": "muted"
        },
        {
          "id": "mobile",
          "label": "320px → 1 column",
          "description": "stacks, no horizontal scroll",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "tablet",
          "label": "768px → 2–3 columns",
          "description": "browser fits what it can",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "desktop",
          "label": "1440px → 4+ columns",
          "description": "fills the space, even gaps",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "overflow",
          "label": "Fixed-px columns",
          "description": "min-width: 320px → overflows at 320px",
          "kind": "store",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "markup",
          "to": "grid",
          "label": "renders into",
          "kind": "data"
        },
        {
          "from": "viewport",
          "to": "grid",
          "label": "feeds width",
          "kind": "data",
          "tone": "muted"
        },
        {
          "from": "grid",
          "to": "mobile",
          "label": "narrow",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "grid",
          "to": "tablet",
          "label": "medium",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "grid",
          "to": "desktop",
          "label": "wide",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "grid",
          "to": "overflow",
          "label": "if px not fr/minmax",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "one grid, declared once"
        },
        {
          "tone": "success",
          "label": "reflows correctly — no overflow"
        },
        {
          "tone": "warning",
          "label": "the 320px overflow failure path"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The responsive grid, one declaration at a time",
      "subtitle": "The whole skill is six lines. minmax + auto-fit do the work a stack of media queries used to.",
      "filename": "card-grid.css",
      "language": "ts",
      "code": ".card-grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));\n  gap: clamp(0.75rem, 2vw, 1.5rem);\n}\n\n.card {\n  min-width: 0; /* lets cards shrink instead of overflowing */\n}\n\n@media (max-width: 480px) {\n  .card-grid { gap: 0.75rem; } /* the ONE deliberate exception */\n}",
      "steps": [
        {
          "lines": [
            2
          ],
          "label": "Declare the grid",
          "note": "display: grid turns the container into a grid formatting context. Children become grid items — no float, no flex hacks."
        },
        {
          "lines": [
            3
          ],
          "label": "auto-fit + minmax — the whole trick",
          "note": "minmax(220px, 1fr): each column is AT LEAST 220px, growing to share leftover space (1fr). auto-fit asks the browser to fit as many such columns as the width allows. The column COUNT is computed, never hardcoded."
        },
        {
          "lines": [
            4
          ],
          "label": "Intrinsic gap",
          "note": "clamp(min, preferred, max) scales the gutter with the viewport — tight on mobile, roomy on desktop — with no breakpoint. Spacing responds to space too."
        },
        {
          "lines": [
            8
          ],
          "label": "min-width: 0 on items",
          "note": "The non-obvious one. Grid items default to min-width: auto, so a long unbreakable string (a URL, a token) forces the column wider than 220px and overflows at 320px. min-width: 0 lets the item shrink. THIS is the line that kills the horizontal-scroll bug."
        },
        {
          "lines": [
            11,
            12
          ],
          "label": "The one honest media query",
          "note": "Not for layout — for a spacing tweak below 480px. A media query as a deliberate exception is fine; a media query AS the layout mechanism is the anti-pattern."
        }
      ],
      "caption": "Resize the browser from 320px to 1920px: columns appear and disappear on their own, gutters breathe, nothing overflows. Six lines, zero magic widths."
    },
    {
      "type": "compare",
      "title": "Breakpoint-driven vs intrinsic grid",
      "subtitle": "Same three-card layout. One is correct at three widths; one is correct at every width.",
      "left": {
        "label": "Breakpoint hack",
        "tone": "warning",
        "lines": [
          "display: flex; flex-wrap: wrap",
          ".card { width: 320px }  /* fixed px */",
          "@media (max-width:768px){ width: 100% }",
          "@media (min-width:769px){ width: 33.33% }",
          "Correct at 767px and 769px — untested between",
          "320px viewport + 320px card = horizontal scroll"
        ],
        "verdict": "Overflows at 320px · breaks on the investor's phone"
      },
      "right": {
        "label": "Intrinsic grid",
        "tone": "success",
        "lines": [
          "display: grid",
          "repeat(auto-fit, minmax(220px, 1fr))",
          "gap: clamp(0.75rem, 2vw, 1.5rem)",
          ".card { min-width: 0 }",
          "Browser computes column count per width",
          "No horizontal scroll at 320px — proven, not hoped"
        ],
        "verdict": "Reflows continuously · passes the 320px no-overflow check"
      },
      "caption": "minmax(220px, 1fr) caps the minimum AND lets columns grow; min-width:0 lets a card shrink. The fixed-320px card cannot shrink, so it overflows the 320px viewport — the exact failure the artifact must record and repair."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell, and the WCAG 2.2 receipt: Success Criterion 1.4.10 (Reflow) requires content to render at 320 CSS px wide with no two-dimensional scrolling. An intrinsic grid passes 1.4.10 by construction; a fixed-px grid fails it the moment the column is wider than the viewport. 'Responsive' is not an aesthetic — it is an accessibility conformance requirement (WCAG 2.2 · SWEBOK V4 software-design quality · ABET problem-solving outcomes), and your state matrix is the evidence that you met it."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Prove this layout is responsive, not just that it looks right on your screen.' Which response is the actual proof?",
      "options": [
        "\"It has a media query at 768px, so it's responsive.\"",
        "\"Here is the state matrix: at 320px it renders 1 column with no horizontal scroll, at 768px 2 columns, at 1440px 4 — all verified by resizing, and min-width:0 prevents the long-string overflow.\"",
        "\"It uses Flexbox, which is a modern layout system.\"",
        "\"It looks fine on my monitor and on the iPhone in dev tools.\""
      ],
      "answer": 1,
      "explanation": "Responsiveness is proven by behavior across viewports, not by the presence of a feature. Option 2 names the rendered result at each breakpoint, the WCAG 1.4.10 no-overflow check at 320px, and the specific line (min-width:0) that defends against the realistic failure — that is the evidence a reviewer can re-run. 'It has a media query' (1) and 'it uses Flexbox' (3) name tools, not outcomes; 'looks fine on my screen' (4) is exactly the happy-path claim the reviewer is challenging."
    },
    {
      "type": "verification",
      "intro": "Prove the layout is responsive — no vibes, no 'looks fine':",
      "items": [
        "Set the viewport to 320px wide. Confirm there is NO horizontal scrollbar and no content clipped off the right edge. This is WCAG 2.2 SC 1.4.10 — if it fails, the layout fails, full stop.",
        "Drag the viewport slowly from 320px to 1920px. The column count should change on its own with no sudden breaks or empty gaps you did not intend — reflow is continuous, not stepped at three pixels.",
        "Drop a long unbreakable string (a 60-character URL with no spaces) into one card. If it forces a horizontal scrollbar, you are missing min-width: 0 on the grid items — add it and re-check.",
        "Record the rendered result for mobile (≈320px), tablet (≈768px), and desktop (≈1440px) as rows in component_state_matrix_and_accessibility_packet.md: viewport, column count, overflow yes/no.",
        "Tab through the cards with the keyboard. Confirm the focus order still follows the visual order after reflow — a grid that reorders content can break the reading and tab sequence."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain to a teammate why repeat(auto-fit, minmax(220px, 1fr)) is more responsive than three media queries — using the word 'intrinsic' and naming what the browser computes.",
        "What does min-width: 0 actually do, and what specific bug appears at 320px if you forget it?",
        "Point at your state matrix: which row is the proof that this layout meets WCAG 2.2 Reflow, and why is 'it looks fine on my screen' not that proof?"
      ]
    },
    {
      "type": "transfer",
      "text": "The move is the same anywhere layout meets unknown space. A dashboard's widget grid uses the identical auto-fit/minmax to pack KPI cards from 1-up on a phone to 6-up on a wall display. An email template (where media queries are unreliable) leans on intrinsic sizing for the same reason. What stays invariant: let the container compute the count from the space, and prove the smallest viewport (320px) never overflows. What changes with context: the minmax floor (a data table needs a wider minimum than a photo tile), and on a constrained device you may cap the maximum column count so cards never get absurdly wide. You now have the skeleton — next, you give each card real component state (loading, empty, error) on top of a grid you have proven will hold them at every size."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "types-and-ui-contracts": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real component that fetches data (a profile card, an orders table, a search box) and write its UI contract: a discriminated-union state type that makes every reachable state — loading, empty, error, disabled, success — explicit and impossible to confuse, then a state matrix that proves each one renders an accessible, distinct surface.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a TypeScript state union where the compiler rejects illegal combinations (data present while loading, error and success at once), a 5-row state matrix (loading/empty/error/disabled/success → what renders + the a11y signal), and a note tying each error/empty state to a WCAG 2.2 success criterion. A reviewer reads the type and can name every state the UI can be in.",
      "unlock": "Your state type is a discriminated union the compiler enforces, and your matrix maps all five states to a distinct, accessible rendering — no boolean soup.",
      "doNotClaim": "Do not claim you have a UI contract if your state is three loose booleans (isLoading, hasError, isEmpty). That permits illegal states like loading-and-error at once. A contract makes the illegal state unrepresentable — the type, not your discipline, forbids it."
    },
    {
      "type": "mission",
      "text": "A blank white screen ships to production: the fetch failed, but the component only knew isLoading and data, so error fell through the cracks and rendered nothing. No spinner, no message, no retry — just a void a screen reader announces as an empty page. One missing state in the type, one user stranded."
    },
    {
      "type": "context",
      "text": "A component's type is a promise about what it can be — and an unhandled state is a broken promise that surfaces as a blank screen, a frozen spinner, or a button users tab into but cannot use. Typing UI state as one explicit contract is the move that turns 'I think I handled the error case' into 'the compiler will not let me forget it.'"
    },
    {
      "type": "pretest",
      "prompt": "A component holds `isLoading: boolean`, `data: User | null`, and `error: string | null`. How many distinct states can those three fields combine into — and how many are ones your UI should actually be in? Answer before reading on.",
      "reveal": "Two booleans-plus-nullable give 2 × 2 × 2 = 8 raw combinations, but only 4 are legal (loading, success, empty, error) — the other 4 are nonsense the type still permits: loading-with-data, error-with-data, loading-and-error together, all-null-forever. Every illegal combination is a bug waiting for production. A discriminated union collapses those 8 into exactly the states you allow, so the impossible ones cannot even be typed."
    },
    {
      "type": "concept",
      "title": "A UI contract is a discriminated union that makes illegal states unrepresentable",
      "text": "Model component state as one tagged union — `{ status: 'loading' } | { status: 'empty' } | { status: 'error'; message } | { status: 'success'; data }` — not loose booleans. The invariant: each state carries exactly the data it needs and nothing it does not, so the compiler forbids contradictions."
    },
    {
      "type": "diagram",
      "title": "The five UI states as one state machine",
      "subtitle": "Every fetch-backed component travels this graph. The contract names every node and every edge; the failure mode is an edge with no node to land on — a blank render.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "idle",
          "label": "loading",
          "description": "request in flight · skeleton + aria-busy",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "success",
          "label": "success",
          "description": "data present · render the real surface",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "empty",
          "label": "empty",
          "description": "valid response, zero rows · guidance, not blank",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "error",
          "label": "error",
          "description": "fetch failed · message + retry · role=alert",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "disabled",
          "label": "disabled",
          "description": "action not allowed yet · aria-disabled + reason",
          "kind": "decision",
          "tone": "muted"
        },
        {
          "id": "blank",
          "label": "BLANK SCREEN",
          "description": "unhandled state · the bug",
          "kind": "external",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "idle",
          "to": "success",
          "label": "data arrives",
          "kind": "async",
          "tone": "success"
        },
        {
          "from": "idle",
          "to": "empty",
          "label": "200, zero rows",
          "kind": "async",
          "tone": "muted"
        },
        {
          "from": "idle",
          "to": "error",
          "label": "fetch rejects",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "success",
          "to": "disabled",
          "label": "guard fails",
          "kind": "control"
        },
        {
          "from": "idle",
          "to": "blank",
          "label": "state not in the type",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "transient (loading)"
        },
        {
          "tone": "success",
          "label": "the happy path"
        },
        {
          "tone": "warning",
          "label": "error / the blank-screen bug"
        },
        {
          "tone": "muted",
          "label": "easy-to-forget states (empty, disabled)"
        }
      ],
      "caption": "Empty and disabled are the two states beginners drop. The dashed edge is what happens when a state is reachable in the app but absent from the type: the component renders nothing."
    },
    {
      "type": "code-walkthrough",
      "title": "The UI contract, typed line by line",
      "subtitle": "A discriminated union plus an exhaustive render switch — the compiler now guards every state.",
      "filename": "UserCard.tsx",
      "language": "ts",
      "code": "// The contract: one tagged union, illegal states unrepresentable.\ntype CardState =\n  | { status: 'loading' }\n  | { status: 'empty' }\n  | { status: 'error'; message: string }\n  | { status: 'success'; user: User }\n\nfunction UserCard({ state }: { state: CardState }) {\n  switch (state.status) {\n    case 'loading':\n      return <Skeleton aria-busy=\"true\" aria-label=\"Loading profile\" />\n    case 'empty':\n      return <Empty>No profile yet — invite this teammate.</Empty>\n    case 'error':\n      return <Alert role=\"alert\">{state.message} <button>Retry</button></Alert>\n    case 'success':\n      return <Profile user={state.user} /> // state.user is typed here\n    default:\n      return assertNever(state) // compile error if a state is unhandled\n  }\n}",
      "steps": [
        {
          "lines": [
            2,
            3,
            4,
            5,
            6
          ],
          "label": "The discriminated union",
          "note": "The shared `status` literal is the discriminant. Because `user` lives ONLY on the success arm, `state.user` is a compile error in any other branch — illegal access is unrepresentable, not just discouraged."
        },
        {
          "lines": [
            10,
            11
          ],
          "label": "loading → aria-busy",
          "note": "A skeleton with aria-busy='true' tells assistive tech 'content is coming', satisfying WCAG 2.2 status-message expectations. A bare spinner with no label is invisible to a screen reader."
        },
        {
          "lines": [
            12,
            13
          ],
          "label": "empty ≠ blank",
          "note": "Zero rows is a valid answer, not a failure. Empty renders guidance — the state beginners forget, which is why their lists render as a void."
        },
        {
          "lines": [
            14,
            15
          ],
          "label": "error → role=alert + retry",
          "note": "role='alert' makes the failure announced, not silent; the Retry button gives a recovery path instead of a dead end."
        },
        {
          "lines": [
            16,
            17
          ],
          "label": "success narrows the type",
          "note": "Inside this case, TypeScript knows `state.user` exists and is non-null — no optional-chaining, no `user?.name`. The union did the narrowing."
        },
        {
          "lines": [
            18,
            19
          ],
          "label": "assertNever — the exhaustiveness guard",
          "note": "If someone adds a 'disabled' arm to the union and forgets to render it, `assertNever` fails to compile. The contract refuses to let a new state ship unhandled — this is the line that prevents the blank screen."
        }
      ],
      "caption": "Add a state to the union and the `default` branch breaks the build until you render it. The type is the checklist that can never be skipped."
    },
    {
      "type": "compare",
      "title": "Boolean soup vs a UI contract",
      "subtitle": "Same component, same five states. Only one makes the blank-screen bug impossible to write.",
      "mono": true,
      "left": {
        "label": "Boolean soup",
        "tone": "warning",
        "lines": [
          "isLoading: boolean",
          "data: User | null",
          "error: string | null",
          "Render: if (isLoading) ... else if (data) ...",
          "// no else → error & empty render NOTHING",
          "8 combos typeable, 4 of them illegal",
          "data?.name everywhere — nullable leaks downstream"
        ],
        "verdict": "Permits loading-and-error at once; blank screen is one missed `if` away"
      },
      "right": {
        "label": "Discriminated-union contract",
        "tone": "success",
        "lines": [
          "type CardState =",
          "  | { status: 'loading' }",
          "  | { status: 'empty' }",
          "  | { status: 'error'; message }",
          "  | { status: 'success'; user }",
          "switch + assertNever → exhaustive by compile",
          "state.user non-null inside success — no ?. needed"
        ],
        "verdict": "Illegal states unrepresentable; a new state can't ship unhandled"
      },
      "caption": "Booleans multiply states you didn't mean; a union enumerates only the states you allow. The contract moves 'did I handle every case?' from your memory into the compiler."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell: they design the state type BEFORE the JSX. Beginners write the happy-path markup, then bolt on isLoading and error as afterthoughts — which is exactly how empty and disabled get dropped. Grounded in WCAG 2.2, every one of those five states is also an accessibility surface: loading needs aria-busy, error needs role='alert', disabled needs aria-disabled plus a reason. The type isn't just for the compiler — it's the contract your screen-reader users depend on."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this handles more than the happy path.' Which artifact actually proves it?",
      "options": [
        "A paragraph explaining that the component supports loading, error, and success states.",
        "A discriminated-union state type plus a 5-row state matrix mapping each state to its rendering and its WCAG a11y signal, with an assertNever exhaustiveness check that fails the build if a state is unhandled.",
        "A screenshot of the component on the happy path with real data loaded.",
        "Three booleans (isLoading, hasError, isEmpty) and a comment saying 'all cases covered'."
      ],
      "answer": 1,
      "explanation": "Only option 2 is reviewable proof: the union enumerates the legal states, the matrix shows each renders a distinct accessible surface, and assertNever makes 'unhandled state' a compile error rather than a promise. The prose (1) and happy-path screenshot (3) prove nothing about the failure states; booleans-plus-a-comment (4) is exactly the soup that permits the illegal combinations a contract exists to forbid."
    },
    {
      "type": "verification",
      "intro": "Prove your UI contract holds — no vibes:",
      "items": [
        "Read your state type. If it is booleans (isLoading, hasError) instead of a tagged union, you can type an illegal combination — rewrite it as a discriminated union with a `status` discriminant.",
        "Confirm data lives ONLY on the success arm: accessing `state.user` in the loading branch should be a compile error, not a runtime null.",
        "Delete the success case from your render switch. The build must fail at `assertNever`. If it compiles, your exhaustiveness guard is missing.",
        "Walk your state matrix: every one of loading / empty / error / disabled / success renders a DISTINCT surface — empty is not a blank div, disabled is not just greyed pixels.",
        "Tie each non-happy state to a WCAG 2.2 signal: loading→aria-busy, error→role='alert', disabled→aria-disabled + visible reason. A state with no a11y signal is a state a screen-reader user cannot perceive."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does modeling state as two booleans permit bugs that a discriminated union makes impossible to even write?",
        "Explain 'make illegal states unrepresentable' using your own component — name one illegal combination booleans allowed and how the union forbids it.",
        "Which of your five states was easiest to forget (empty? disabled?), and what does the user experience when a component skips it?"
      ]
    },
    {
      "type": "transfer",
      "text": "The contract pattern travels anywhere data crosses a boundary. A form field is the same union — pristine / editing / validating / invalid / submitted — and typing it stops the 'submit button enabled while validation is still running' bug. An API response envelope is a discriminated union too: `{ ok: true; data } | { ok: false; error }` forces every caller to handle failure before touching data. What stays invariant is the rule — enumerate the legal states, carry each state's data on its own arm, make the impossible ones untypeable. What changes is the domain: states multiply with risk, latency, and how many users a blank screen would strand. The next lesson takes this exact contract into forms and validation, where the cost of a missed state is a user who can't tell why their submission silently failed."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "component-composition": [
    {
      "type": "sprint-contract",
      "outcome": "Take one user-facing surface (a checkout panel, a search box, a profile form) and compose it from small, single-job components — then write component_state_matrix_and_accessibility_packet.md that maps every state (loading, empty, error, disabled, success) to which composed piece owns it.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a state matrix (rows = states, columns = the composed components), the suspect state where the design leaked, a keyboard/ARIA note per interactive piece, and a reviewer objection. A peer reads it and can name which component renders the error state without you pointing.",
      "unlock": "Your surface is built from composable parts where each owns one job, and your packet maps all five states to the part that renders each.",
      "doNotClaim": "Do not claim you have 'composed' a component if you built one 400-line component that takes 18 props and renders every state with nested ternaries. That is configuration, not composition. Composition means small parts assembled — not one part with a switchboard of flags."
    },
    {
      "type": "mission",
      "text": "The design review stalls on one screen: a single ProductCard component with 22 props, where `isLoading && !isError && hasData && !isDisabled` decides what renders, and nobody can answer 'what shows when the API is slow AND the user has no items?' The staff engineer deletes it and assembles five small pieces in its place — and suddenly every state has an obvious home."
    },
    {
      "type": "context",
      "text": "Composition is how you keep a growing UI honest: instead of one component absorbing every variation through props and conditionals, you assemble small parts that each render one thing. It is the foundation the rest of this module builds on — form validation, error boundaries, and responsive layout all assume your UI is composed, not monolithic."
    },
    {
      "type": "pretest",
      "prompt": "You need a button that can be a primary CTA, a destructive delete, a loading spinner, and a disabled placeholder. Do you add four boolean props to one Button, or something else? Decide before reading.",
      "reveal": "Four booleans is the trap — they create 2^4 = 16 combinations, most of which are nonsense (loading AND disabled AND destructive?). The composition move: a single Button that accepts a `variant` and *children*, and lets the caller compose the spinner IN (`<Button><Spinner/> Saving…</Button>`). The button owns press behavior and focus ring; the caller owns content. Props configure; children compose. Reaching for another boolean is the smell that you should have opened a slot instead."
    },
    {
      "type": "concept",
      "title": "Composition is small parts assembled — props configure, children compose",
      "text": "Build a surface from components that each render one thing and own one job. Pass behavior down through props; pass *content* through children/slots. The invariant: no single component renders every state through conditionals — each state has a part that owns it."
    },
    {
      "type": "diagram",
      "title": "The five UI states, and which composed part owns each",
      "subtitle": "A CheckoutPanel composed from small parts. The state machine routes loading → empty → error → success; the FAILURE PATH (error with no error boundary) and the REPAIR PATH (boundary catches, renders ErrorState) are both shown.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "panel",
          "label": "CheckoutPanel",
          "description": "the composition root",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "loading",
          "label": "<LoadingState>",
          "description": "skeleton + aria-busy",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "empty",
          "label": "<EmptyState>",
          "description": "cart is empty",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "list",
          "label": "<CartList>",
          "description": "success: renders items",
          "kind": "process",
          "tone": "success"
        },
        {
          "id": "error",
          "label": "<ErrorState>",
          "description": "retry CTA, role=alert",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "boundary",
          "label": "ErrorBoundary",
          "description": "catches render throws",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "crash",
          "label": "White screen",
          "description": "no boundary → app blanks",
          "kind": "external",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "panel",
          "to": "loading",
          "label": "fetch pending",
          "kind": "control",
          "tone": "muted"
        },
        {
          "from": "panel",
          "to": "empty",
          "label": "data = []",
          "kind": "control"
        },
        {
          "from": "panel",
          "to": "list",
          "label": "data.length > 0",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "panel",
          "to": "crash",
          "label": "throw, NO boundary (FAILURE)",
          "kind": "async",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "panel",
          "to": "boundary",
          "label": "throw, WITH boundary (REPAIR)",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "boundary",
          "to": "error",
          "label": "renders fallback",
          "kind": "data",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "composition root (assembles the parts)"
        },
        {
          "tone": "success",
          "label": "the success state"
        },
        {
          "tone": "warning",
          "label": "failure path + its repair (error boundary)"
        },
        {
          "tone": "muted",
          "label": "non-success states, each owned by one part"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Composing the panel — assembling parts, not stacking flags",
      "subtitle": "The CheckoutPanel routes to small state components; the ErrorBoundary wraps the render so a throw becomes <ErrorState>, not a white screen.",
      "filename": "CheckoutPanel.tsx",
      "language": "ts",
      "code": "function CheckoutPanel({ cartId }: { cartId: string }) {\n  const { data, isLoading, error } = useCart(cartId)\n\n  return (\n    <ErrorBoundary fallback={<ErrorState onRetry={() => location.reload()} />}>\n      <section aria-labelledby=\"cart-h\" aria-busy={isLoading}>\n        <h2 id=\"cart-h\">Your cart</h2>\n        {isLoading && <LoadingState />}\n        {error && <ErrorState onRetry={() => location.reload()} />}\n        {data?.length === 0 && <EmptyState />}\n        {data && data.length > 0 && <CartList items={data} />}\n      </section>\n    </ErrorBoundary>\n  )\n}",
      "steps": [
        {
          "lines": [
            2
          ],
          "label": "One data hook, three signals",
          "note": "isLoading / error / data are the only inputs the panel routes on. The panel decides WHICH part renders; it never renders the states itself."
        },
        {
          "lines": [
            5,
            6
          ],
          "label": "The error boundary is the repair path",
          "note": "Wrapping the render means a throw from any child becomes <ErrorState> — not the white screen from the diagram's failure path. The boundary catches render-time errors; the `error` check handles async fetch errors."
        },
        {
          "lines": [
            7
          ],
          "label": "Semantic container + aria-busy",
          "note": "A real <section> with aria-labelledby, and aria-busy so screen readers announce the loading state. The container is composed once, around whichever state renders."
        },
        {
          "lines": [
            9
          ],
          "label": "Loading owned by one part",
          "note": "<LoadingState> renders a skeleton — the panel doesn't know what loading looks like. Swap the skeleton later and the panel never changes."
        },
        {
          "lines": [
            10
          ],
          "label": "Async error vs render error",
          "note": "This handles a failed fetch (data layer). The boundary above handles a thrown exception (render layer). Two different failures, two different owners — that separation is the composition."
        },
        {
          "lines": [
            11,
            12
          ],
          "label": "Empty and success are siblings, not nested",
          "note": "data === [] → <EmptyState>; data with items → <CartList>. Flat sibling branches, each a named part. No 22-prop monolith, no nested ternary deciding all five states."
        }
      ],
      "caption": "Run the failure: throw inside <CartList>. WITHOUT the boundary the app blanks; WITH it, <ErrorState> renders and the retry CTA stays keyboard-reachable — the diagram's repair path, in code."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell, straight from WCAG 2.2: every non-success state must be a real, announced thing, not a vanished UI. A loading state needs aria-busy, an error state needs role=\"alert\", an empty state needs readable text — a screen-reader user who hits the error path should hear it, not silence. Composition makes this cheap: each state is its own component, so its accessibility lives in one place instead of being smeared across one monolith's conditionals."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: 'Show me the exact evidence this composition handles the error path, not just the happy path.' Which answer actually proves it?",
      "options": [
        "'The component has an isError prop and a comment that says it handles errors.'",
        "'Here is my state matrix: the error row maps to <ErrorState>; here is the ErrorBoundary that catches a thrown render; and here is the keyboard note that the retry CTA is focusable with role=alert announced.'",
        "'It renders fine in the browser when the API is up.'",
        "'It is one component with all the logic in it, so nothing can be missed.'"
      ],
      "answer": 1,
      "explanation": "Only option 2 points at evidence: the state matrix row, the boundary that turns a throw into a rendered fallback, and the accessibility proof (focusable retry + role=alert). Option 1 is a claim with no proof, option 3 only exercises the happy path, and option 4 describes the monolith this lesson exists to break — 'nothing missed' is exactly how the error state gets silently dropped."
    },
    {
      "type": "verification",
      "intro": "Prove your composition holds — no vibes:",
      "items": [
        "Open your packet's state matrix: every one of the five states (loading, empty, error, disabled, success) maps to a named component that owns it. A blank cell is a missing state.",
        "Force the error path: throw inside a child. The ErrorBoundary must render <ErrorState> — if the app blanks, your repair path is missing, not optional.",
        "Tab through the rendered surface. Every interactive part (retry, submit, links) is reachable in a sane order and shows a visible focus ring — composition does not excuse you from keyboard access.",
        "Confirm no single component renders more than one state through conditionals. If one part has isLoading && isError && hasData nesting, split it — that is the monolith, not composition.",
        "Shrink the viewport to 320px. Each composed part should reflow independently; if one part breaking layout drags the others, they are too coupled to be called composed."
      ]
    },
    {
      "type": "compare",
      "title": "One monolith vs a composed surface",
      "subtitle": "Same screen: a cart panel that must handle loading, empty, error, disabled, and success.",
      "left": {
        "label": "Prop-driven monolith",
        "tone": "warning",
        "lines": [
          "One <CartPanel> with 22 props",
          "isLoading && !error && data?.length ternary tree",
          "Error state is one forgotten `: null` branch",
          "No error boundary — a throw blanks the app",
          "aria-busy / role=alert smeared across conditionals"
        ],
        "verdict": "Configuration, not composition — states leak"
      },
      "right": {
        "label": "Composed parts",
        "tone": "success",
        "lines": [
          "<CheckoutPanel> assembles 4 small state parts",
          "Each state owned by one named component",
          "<ErrorBoundary> turns a throw into <ErrorState>",
          "Each part's a11y lives in one place",
          "State matrix in the packet maps every cell"
        ],
        "verdict": "Each state has a home — and a reviewer can find it"
      },
      "caption": "The monolith feels faster to write and is impossible to review; the composed version is five small files a peer can trace in seconds. Small parts beat one switchboard."
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does adding a fourth boolean prop to a component signal you should have opened a slot (children) instead?",
        "Explain the difference between the async error path (a failed fetch) and the render error path (a thrown exception), using your own panel — which part owns each?",
        "Point at one cell in your state matrix and name the exact component that renders it, plus how a screen-reader user would know that state is active."
      ]
    },
    {
      "type": "transfer",
      "text": "The move travels past React: any system where one surface must show many states wants composition over configuration. A native mobile screen composes loading/empty/error views the same way; a CLI composes a spinner, a table, and an error block rather than one mega-renderer with flags. What stays invariant is the rule — every state owned by one part, no monolith deciding all of them through conditionals. What changes is the slot mechanism (children, render props, named outlets). Next lesson keeps this exact composed panel and adds real form validation inside the <CartList>'s quantity inputs — because validated input states (pristine, invalid, submitting) are just more states that each need a part to own them, and your state matrix already has the columns ready."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "client-state-machine": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real user-facing component you own (a checkout step, a search box, a form submit) and model its UI as an explicit Client State Machine: name every status it can be in — idle, loading, empty, error, disabled, success — the events that move between them, and the single status it renders from. No loose boolean flags. Capture it in component_state_matrix_and_accessibility_packet.md with an accessibility row per state.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a state matrix (status × what renders × what is announced/focused), a state diagram with the failure path AND the repair path, the suspect transition named, and one impossible-state combination you have made unrepresentable. A peer reads it and can trace a request from idle to success — and from idle to error and back — without you narrating.",
      "unlock": "Your component renders from ONE status value, every state has an accessible announcement, and at least one previously-possible broken combination (e.g. loading + error at once) is now impossible by construction.",
      "doNotClaim": "Do not claim you have a state machine if you still have isLoading, hasError, and isEmpty as separate booleans and a pile of && in the JSX. Three booleans is eight combinations, most of them nonsense. A state machine has ONE status with named transitions — the count of legal states is something you can list on one hand."
    },
    {
      "type": "mission",
      "text": "The user taps Submit, the spinner spins, the network drops — and the screen now shows a spinner AND a red error AND the old success toast, because three booleans each flipped on their own clock. A screen reader announces nothing. That is not a bug in one place; it is the absence of a state machine."
    },
    {
      "type": "context",
      "text": "Every interactive surface — a form, a search, a checkout — is a tiny finite state machine whether you admit it or not. Modeling it explicitly is the difference between a component that survives loading, empty, error, disabled, and success without losing context, and one that paints contradictory states because each flag drifts independently."
    },
    {
      "type": "pretest",
      "prompt": "A submit button tracks `isLoading`, `isError`, and `isSuccess` as three booleans. How many distinct combinations can those three flags produce — and how many of them are states you actually want to render? Answer before reading on.",
      "reveal": "Three booleans = 2³ = eight combinations, and only three or four are real. loading+error, loading+success, error+success — those are impossible-state combinations the type system happily lets you reach, and the network WILL reach them under a slow retry. A single `status: 'idle' | 'loading' | 'error' | 'success'` makes the illegal seven unrepresentable. You did not 'add a state machine' — you deleted the states that should never have existed."
    },
    {
      "type": "concept",
      "title": "One status value + named transitions — illegal states made unrepresentable",
      "text": "A Client State Machine replaces independent boolean flags with one `status` enum and an explicit set of events that move between states. The invariant: the component renders from that single value, so two contradictory states cannot coexist. Model the transitions, not the flags."
    },
    {
      "type": "diagram",
      "title": "The submit component as a state machine — failure path and repair path",
      "subtitle": "Six states on the idle → loading → success path. The failure edge (loading → error on REJECT) and the repair edge (error → loading on RETRY) are marked. EMPTY and DISABLED are real states, not afterthoughts.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "idle",
          "label": "idle",
          "description": "form ready, button enabled",
          "kind": "process",
          "tone": "default"
        },
        {
          "id": "disabled",
          "label": "disabled",
          "description": "invalid input — submit blocked",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "loading",
          "label": "loading",
          "description": "request in flight, spinner + aria-busy",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "empty",
          "label": "empty",
          "description": "request ok, zero results",
          "kind": "store",
          "tone": "default"
        },
        {
          "id": "error",
          "label": "error",
          "description": "request rejected, role=alert announced",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "success",
          "label": "success",
          "description": "result rendered, focus moved",
          "kind": "store",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "idle",
          "to": "disabled",
          "label": "INVALIDATE",
          "kind": "control",
          "tone": "muted"
        },
        {
          "from": "disabled",
          "to": "idle",
          "label": "VALIDATE",
          "kind": "control",
          "tone": "muted"
        },
        {
          "from": "idle",
          "to": "loading",
          "label": "SUBMIT",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "loading",
          "to": "success",
          "label": "RESOLVE (n>0)",
          "kind": "async",
          "tone": "success"
        },
        {
          "from": "loading",
          "to": "empty",
          "label": "RESOLVE (n=0)",
          "kind": "async"
        },
        {
          "from": "loading",
          "to": "error",
          "label": "REJECT — failure path",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "error",
          "to": "loading",
          "label": "RETRY — repair path",
          "kind": "sync",
          "tone": "accent"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "active transition (request in flight / retry)"
        },
        {
          "tone": "warning",
          "label": "failure path — REJECT lands here"
        },
        {
          "tone": "success",
          "label": "terminal success — focus moves here"
        },
        {
          "tone": "muted",
          "label": "disabled — a real modeled state, not a flag"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The reducer that makes illegal states unrepresentable",
      "subtitle": "One status union, one transition function. The network can only move the component along edges that exist.",
      "filename": "useSubmitMachine.ts",
      "language": "ts",
      "code": "type Status = 'idle' | 'disabled' | 'loading' | 'empty' | 'error' | 'success'\ntype Event =\n  | { type: 'VALIDATE' } | { type: 'INVALIDATE' }\n  | { type: 'SUBMIT' } | { type: 'RESOLVE'; count: number }\n  | { type: 'REJECT'; message: string } | { type: 'RETRY' }\n\ninterface State { status: Status; error?: string }\n\nfunction reducer(state: State, event: Event): State {\n  switch (state.status) {\n    case 'idle':\n      if (event.type === 'INVALIDATE') return { status: 'disabled' }\n      if (event.type === 'SUBMIT')     return { status: 'loading' }\n      return state\n    case 'disabled':\n      return event.type === 'VALIDATE' ? { status: 'idle' } : state\n    case 'loading':\n      if (event.type === 'RESOLVE') return { status: event.count > 0 ? 'success' : 'empty' }\n      if (event.type === 'REJECT')  return { status: 'error', error: event.message }\n      return state\n    case 'error':\n      return event.type === 'RETRY' ? { status: 'loading' } : state\n    default:\n      return state\n  }\n}",
      "steps": [
        {
          "lines": [
            1
          ],
          "label": "One status, six legal values",
          "note": "This single union replaces isLoading + hasError + isEmpty. There is no value that means 'loading and error at once' — the impossible states have no name, so they cannot exist."
        },
        {
          "lines": [
            2,
            3,
            4,
            5
          ],
          "label": "Events, not setters",
          "note": "The UI dispatches intent (SUBMIT, REJECT, RETRY), never `setStatus('error')`. Only the reducer decides what a state machine event means — that is where the rules live."
        },
        {
          "lines": [
            11,
            12,
            13
          ],
          "label": "Transitions are guarded by current state",
          "note": "A REJECT only matters while loading. A stray REJECT in 'idle' falls through to `return state` — a late network callback can no longer corrupt a state it has no edge to."
        },
        {
          "lines": [
            16,
            17
          ],
          "label": "The fork that beginners flatten",
          "note": "RESOLVE branches on count: zero rows is 'empty', a distinct first-class state — not 'success with nothing in it'. Empty state is the one teams forget until a customer sees a blank screen with no message."
        },
        {
          "lines": [
            18
          ],
          "label": "The failure path",
          "note": "REJECT carries the message into state.error so the view can render role=alert. The error text lives in the machine, not scattered in catch blocks."
        },
        {
          "lines": [
            21
          ],
          "label": "The repair path",
          "note": "From 'error', only RETRY is legal, and it returns to 'loading' — the exact edge the diagram marks as repair. Retry is a modeled transition, not a re-mounted component."
        }
      ],
      "caption": "Trace it: idle —SUBMIT→ loading —REJECT→ error —RETRY→ loading —RESOLVE(0)→ empty. Every hop is an edge in the diagram; nothing else is reachable."
    },
    {
      "type": "compare",
      "title": "Three booleans vs one status",
      "subtitle": "Same submit button, same slow-network retry. Only one can never paint two states at once.",
      "mono": true,
      "left": {
        "label": "Boolean soup",
        "tone": "warning",
        "lines": [
          "isLoading / hasError / isEmpty as 3 useState",
          "2³ = 8 combinations, ~4 of them nonsense",
          "JSX: {isLoading && <Spin/>}{hasError && <Err/>}",
          "Slow retry flips error THEN loading → both show",
          "No aria-busy, no role=alert, no focus move",
          "Empty state forgotten — blank screen, no message"
        ],
        "verdict": "Network reaches a state you never designed"
      },
      "right": {
        "label": "One status machine",
        "tone": "success",
        "lines": [
          "status: 6 named values, render switch(status)",
          "Illegal combos have no name — unrepresentable",
          "View dispatches SUBMIT/REJECT/RETRY, reads error",
          "REJECT in idle falls through — stale callback inert",
          "Each state owns its aria-busy / role=alert / focus",
          "empty is a first-class state with real copy"
        ],
        "verdict": "Only diagram edges are reachable — by construction"
      },
      "caption": "The win is subtraction: you did not add handling for the bad states, you deleted their ability to exist. Fewer states, not more code."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 grounds this: each state is a perceivable announcement, not just a pixel. loading sets aria-busy=\"true\"; error renders role=\"alert\" so a screen reader announces it without a focus jump; success moves focus to the result heading so keyboard and AT users are not stranded on a dead button. The senior tell: a state machine whose transitions each name what is ANNOUNCED and where FOCUS goes, not just what is drawn. SWEBOK calls this making the model explicit; a reviewer calls it the row in your matrix that proves you thought about the user who cannot see the spinner."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this is a real state machine and not booleans in a trench coat.' Which artifact detail actually proves it?",
      "options": [
        "The component uses useReducer instead of useState.",
        "A state diagram whose edges enumerate every legal transition, plus a named impossible-state combination (e.g. loading+error) that the single status union makes unrepresentable — and an accessibility row per state.",
        "The JSX is shorter than the old version.",
        "There is a loading spinner and an error message somewhere on screen."
      ],
      "answer": 1,
      "explanation": "useReducer (1) is a mechanism, not proof — you can write boolean soup inside a reducer. Brevity (3) and the mere presence of a spinner (4) say nothing about reachability. The proof is structural: a transition graph that shows which moves are legal, a named combination that is now impossible by construction, and an accessibility commitment per state. That is the difference between modeling the states and decorating them."
    },
    {
      "type": "verification",
      "intro": "Prove your Client State Machine holds — no vibes:",
      "items": [
        "Count your states: you can name every status on one hand, and you can name at least one boolean combination (loading+error, error+success) that is now impossible to construct.",
        "Render reads ONE value: the view is a switch (or map) on status, not a stack of && conditions across independent flags.",
        "Run the Playwright flow: submit, force a network reject, retry, force an empty result — the screen never shows two states at once, and the URL/context is preserved across the failure.",
        "Keyboard + screen-reader checklist passes: loading sets aria-busy, error is announced via role=alert, success moves focus to the result — each verified, not assumed.",
        "Viewport check: at 320px the error and empty states still fit without overflow, and you have a screenshot note proving the failure state renders, not just the happy path.",
        "Stale-callback test: dispatch a REJECT after the machine left loading — confirm it falls through and corrupts nothing."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does 'three booleans' quietly equal 'eight states', and how does one status union delete the seven you never wanted?",
        "Walk the failure path and the repair path of YOUR component out loud — name the event on each edge (SUBMIT, REJECT, RETRY) and what is announced when you land.",
        "Which state did you almost forget — empty or disabled — and what would a customer have seen if you had left it as 'success with nothing in it'?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same machine governs any surface with in-flight, contested states. A multi-step checkout is a machine of steps where each step is itself idle/loading/error. A media player is buffering/playing/paused/ended. A long-running export is queued/running/failed/done with a retry edge. What stays invariant: one status value, transitions you can draw, illegal combinations made unrepresentable, and an accessibility commitment per state. What changes with scale and risk: a payment step's error path may be a one-way door demanding a confirmation before RETRY, where a search box can retry freely. Carry component_state_matrix_and_accessibility_packet.md into the capstone — the fullstack product dashboard reuses this exact matrix and Playwright proof as its evidence that every surface survives loading, empty, error, disabled, and success without losing the user's context."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "forms-validation": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real user-facing form (a sign-up, checkout, or settings form) and build its validation so every field announces its own error accessibly, the submit button reflects loading and disabled state, and a screen-reader user can complete and recover from a mistake without losing context. Capture the result in component_state_matrix_and_accessibility_packet.md.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a state matrix (idle · validating · invalid · submitting · error · success) for one form, plus the validation wiring (aria-invalid, aria-describedby, role=alert), a keyboard-only pass, and a viewport-failure note. A screen-reader user reaches success — or hears exactly what to fix — without seeing the screen.",
      "unlock": "Each invalid field programmatically points to its own error (aria-describedby), the form blocks submit on invalid, and a keyboard + screen-reader user can recover from an error to success.",
      "doNotClaim": "Do not claim a form is 'validated' because it turns a border red. Color-only and an alert() popup are not validation — they fail a screen-reader user and a colorblind user. Validation that no assistive tech can perceive is decoration, not validation."
    },
    {
      "type": "mission",
      "text": "A user fills out your eight-field checkout, hits Submit, and the page silently scrolls — one field went red somewhere above the fold, but their screen reader said nothing and their cart is now gone. That red border is the entire difference between a sale and a support ticket."
    },
    {
      "type": "context",
      "text": "Forms are where users hand you their money, their data, and their trust — and where the most accessibility lawsuits land. This lesson turns 'the field is wrong' into a perceivable, recoverable, keyboard-reachable state, the contract every real product form must meet."
    },
    {
      "type": "pretest",
      "prompt": "Your email field is invalid, so you set its border to red and show red text below it. Is that field now accessibly validated? Decide before reading on.",
      "reveal": "No — on two counts. A screen reader announcing 'email, edit text' never mentions the error, because the red text is just nearby pixels, not programmatically tied to the input. And a colorblind or low-vision user may not perceive 'red' at all. Accessible validation needs aria-invalid='true' to mark the field broken, aria-describedby pointing at the error's id so the input OWNS the message, and a non-color signal (text + icon). The red border is the last 10% — the wiring is the skill."
    },
    {
      "type": "concept",
      "title": "A validated field is a field that ANNOUNCES its own error",
      "text": "The invariant: every error is bound to its field, not just placed near it. aria-invalid flags the input, aria-describedby links it to the message, role='alert' speaks the change. Validation no assistive tech can perceive is not validation."
    },
    {
      "type": "diagram",
      "title": "The form validation state machine",
      "subtitle": "Six component states one form moves through. The accessibility wiring (aria-invalid, aria-describedby, role=alert) lives on the 'invalid' and 'error' transitions — the failure path AND the repair path are both drawn.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "idle",
          "label": "Idle",
          "description": "pristine, untouched",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "validating",
          "label": "Validating",
          "description": "on blur / on submit",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "invalid",
          "label": "Invalid field",
          "description": "aria-invalid + describedby + role=alert",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "submitting",
          "label": "Submitting",
          "description": "button disabled + aria-busy",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "error",
          "label": "Server error",
          "description": "error boundary · summary refocus",
          "kind": "store",
          "tone": "warning"
        },
        {
          "id": "success",
          "label": "Success",
          "description": "confirmed, focus moved",
          "kind": "store",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "idle",
          "to": "validating",
          "label": "user blurs / submits",
          "kind": "sync"
        },
        {
          "from": "validating",
          "to": "invalid",
          "label": "fails rule",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "invalid",
          "to": "validating",
          "label": "user fixes (repair path)",
          "kind": "async",
          "tone": "success"
        },
        {
          "from": "validating",
          "to": "submitting",
          "label": "all rules pass",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "submitting",
          "to": "error",
          "label": "server rejects",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "submitting",
          "to": "success",
          "label": "server accepts",
          "kind": "async",
          "tone": "success"
        },
        {
          "from": "error",
          "to": "validating",
          "label": "retry",
          "kind": "control"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "in-flight: validating / submitting"
        },
        {
          "tone": "warning",
          "label": "failure path (where a11y wiring lives)"
        },
        {
          "tone": "success",
          "label": "repair + success path"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Wiring one accessible field, line by line",
      "subtitle": "The real artifact: a controlled email field that owns its error. This is the Worked Expert Example, filled — not pseudocode.",
      "filename": "EmailField.tsx",
      "language": "ts",
      "code": "function EmailField({ value, onChange }: EmailFieldProps) {\n  const [touched, setTouched] = useState(false)\n  const error =\n    touched && !/^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(value)\n      ? 'Enter a valid email, e.g. you@work.com'\n      : null\n  const errorId = 'email-error'\n\n  return (\n    <div className=\"field\">\n      <label htmlFor=\"email\">Work email</label>\n      <input\n        id=\"email\"\n        type=\"email\"\n        value={value}\n        onChange={(e) => onChange(e.target.value)}\n        onBlur={() => setTouched(true)}\n        aria-invalid={error ? true : undefined}\n        aria-describedby={error ? errorId : undefined}\n      />\n      {error && (\n        <p id={errorId} role=\"alert\" className=\"field-error\">\n          {error}\n        </p>\n      )}\n    </div>\n  )\n}",
      "steps": [
        {
          "lines": [
            2
          ],
          "label": "Track 'touched' as component state",
          "note": "Do not shout an error before the user has typed. Validation fires on blur (or submit), not on the first keystroke — that is the difference between guidance and harassment."
        },
        {
          "lines": [
            3,
            4,
            5,
            6
          ],
          "label": "Derive the error, never store it",
          "note": "The error is computed from value + touched. Storing it in a second state field invites drift — the immutable, derived version cannot disagree with the input."
        },
        {
          "lines": [
            11
          ],
          "label": "label htmlFor binds the name",
          "note": "Without this, a screen reader reads 'edit text' with no name. The label-for-id pair is the floor of an accessible field."
        },
        {
          "lines": [
            17
          ],
          "label": "aria-invalid marks the field broken",
          "note": "This is the programmatic 'this is wrong' — set to true only when there is an error, undefined otherwise so a pristine field is not flagged."
        },
        {
          "lines": [
            18
          ],
          "label": "aria-describedby links field → message",
          "note": "The input now OWNS the error text by id. A screen reader on the input reads the label, the value, AND the error in one breath. This single attribute is the whole lesson."
        },
        {
          "lines": [
            21,
            22,
            23
          ],
          "label": "role=alert speaks the change",
          "note": "When the error appears, role='alert' makes assistive tech announce it immediately — the blind user hears the problem the instant it renders, not on next focus."
        }
      ],
      "caption": "Tab to the field, type 'bob', tab away: a screen reader announces 'Work email, edit text, invalid, Enter a valid email'. That sentence is the proof."
    },
    {
      "type": "compare",
      "title": "Two versions of 'the email is wrong'",
      "subtitle": "Both render a red message. Only one is perceivable to the user who cannot see red.",
      "left": {
        "label": "Color-only / orphaned error",
        "tone": "warning",
        "lines": [
          "Sets input border to red, adds red <span> below",
          "No aria-invalid — field reads as valid to AT",
          "No aria-describedby — message is unlinked pixels",
          "Error stored in separate state (drifts from value)",
          "Submit still fires; server is the only gate",
          "Colorblind user perceives no error at all"
        ],
        "verdict": "Looks validated; fails every non-sighted user"
      },
      "right": {
        "label": "Bound, perceivable, recoverable",
        "tone": "success",
        "lines": [
          "aria-invalid='true' flags the field programmatically",
          "aria-describedby ties input to the error id",
          "role='alert' announces it the moment it appears",
          "Error DERIVED from value — cannot disagree",
          "Text + icon, not color alone (WCAG 1.4.1)",
          "Submit blocked until valid; focus moves to first error"
        ],
        "verdict": "Perceivable, recoverable, keyboard + SR complete"
      },
      "caption": "WCAG 2.2 SC 1.4.1 (Use of Color) and 3.3.1 (Error Identification): an error a user must SEE in red to know about is not identified for everyone."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell: after a failed submit, move focus to the error summary at the top, not just paint fields red. WCAG 2.2 SC 3.3.1 wants the error identified — but a keyboard user who submitted from the bottom of the form has no idea anything changed 600px above them. 'Set the red borders' is junior; 'refocus the user onto what they must fix' is the move that turns an abandoned form into a completed one."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this form works for a screen-reader user, not just a happy-path mouse user.' Which artifact answers the challenge?",
      "options": [
        "A screenshot of the form with all fields filled in green.",
        "A keyboard-only pass plus a screen-reader transcript showing the field announced as 'invalid' with its error text via aria-describedby, and focus landing on the error after submit.",
        "A note that 'we added red borders and validation messages'.",
        "The Zod schema that defines the validation rules."
      ],
      "answer": 1,
      "explanation": "The challenge is specifically about perceivability and recovery for a non-sighted user, so only the keyboard pass + screen-reader transcript proves it: it shows aria-invalid/aria-describedby actually announcing, and focus management after submit. A green screenshot (1) and red borders (3) only prove the sighted happy path; the schema (4) proves the rules exist but not that any human can perceive or recover from a violation."
    },
    {
      "type": "verification",
      "intro": "Prove the form holds for everyone — no vibes:",
      "items": [
        "Tab through the entire form with no mouse. Every field is reachable, every error is read aloud by a screen reader (or VoiceOver/NVDA), and you can reach Submit.",
        "Trigger one invalid field and inspect it: aria-invalid='true' is present AND aria-describedby points to the id of the visible error text. If either is missing, the error is orphaned.",
        "Submit the invalid form: it does NOT post, and focus moves to an error summary or the first invalid field — not silently nowhere.",
        "Remove color from the equation (grayscale the screen): the error is still identifiable by text and/or icon, satisfying WCAG 1.4.1.",
        "Resize to a 320px viewport: labels, inputs, and error messages still fit and stack without truncation or horizontal scroll — record the viewport-failure note either way."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why aria-describedby is the line that matters most, using your own field — what does a screen-reader user hear with it vs. without it?",
        "Walk the form through its six states (idle → validating → invalid → submitting → error/success) and name what the user perceives at each, including the keyboard and screen-reader user.",
        "Why is deriving the error from the input value safer than storing it in its own state variable? Give the failure that storing it invites."
      ]
    },
    {
      "type": "transfer",
      "text": "The same pattern travels to every input surface. A search box announces 'no results' via role='status'; a multi-step wizard refocuses to the first error of the failed step; a mobile date-picker keeps its error in the 320px viewport instead of off-screen. What stays invariant: the field owns its error (aria-describedby), the failure is perceivable without color, and the user can recover with a keyboard alone. What changes is the trigger (blur vs. submit vs. async), the latency (a slow server needs a submitting/aria-busy state), and the stakes (a checkout error costs a sale; a settings error costs a retry). Carry component_state_matrix_and_accessibility_packet.md into the capstone — its state matrix and screen-reader transcript are reused there as the accessibility evidence for the whole product surface."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "loading-empty-error-success": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real async UI surface (a search results list, a dashboard panel, a submitted form) and design all four of its states — Loading, Empty, Error, Success — as one explicit contract. Then capture them in the named artifact: component_state_matrix_and_accessibility_packet.md.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a 4-row state matrix (Loading · Empty · Error · Success) where each row names the trigger, what the user SEES, the accessibility behavior (aria-live / aria-busy / focus), and the recovery action. Plus a keyboard checklist and a viewport-failure note. A reviewer opens it and can name every state your component can be in without running the app.",
      "unlock": "Your matrix has all four states, each with a distinct visible UI, an accessibility behavior, and — for Empty and Error — a recovery action. Empty is treated as a designed state, not a blank screen.",
      "doNotClaim": "Do not claim you 'handled the states' if Empty renders the same blank container as Loading, or if Error is a raw thrown exception with no recovery. A spinner that never resolves and a white screen on zero results are both unhandled states wearing a finished component's clothes."
    },
    {
      "type": "mission",
      "text": "The demo worked because the API was fast and the account had data. In the live review the network stalls for three seconds on a blank screen, a new user with zero results sees the same nothing as a crash, and one 500 paints a red stack trace across the page — three states the happy path never showed anyone."
    },
    {
      "type": "context",
      "text": "Every component that fetches or submits is really four components in a trench coat: it is loading, empty, errored, or successful. This lesson turns 'it works on my machine' into an explicit state contract — Section 1 of the component_state_matrix_and_accessibility_packet.md you carry into the capstone."
    },
    {
      "type": "pretest",
      "prompt": "Your results list fetches data and maps it to rows. The query returns []. What does the user see — and is that a bug? Answer before reading on.",
      "reveal": "They see nothing: an empty container, no message, often indistinguishable from a component that is still loading or one that crashed. It is a bug — a designed-away state. Empty is not the absence of UI; it is a UI. 'No results for \"xyz\" — try a broader term' is a state you build on purpose. A blank screen forces the user to guess whether the app is thinking, broken, or genuinely has nothing."
    },
    {
      "type": "concept",
      "title": "Every async surface has four states; render each on purpose",
      "text": "Loading, Empty, Error, Success are mutually exclusive states of one component. Pick exactly one per render, give each its own visible UI plus accessibility behavior, and give Empty and Error a recovery path. The invariant: no async surface may have an unrendered state."
    },
    {
      "type": "diagram",
      "title": "The four-state machine of an async component",
      "subtitle": "Fetch fires from Idle. The response routes to exactly ONE terminal state — and Empty is a first-class destination, not a side door off Success.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "idle",
          "label": "Idle",
          "description": "before fetch",
          "kind": "client",
          "tone": "muted"
        },
        {
          "id": "loading",
          "label": "Loading",
          "description": "skeleton · aria-busy",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "fetch",
          "label": "Response",
          "description": "resolve or reject",
          "kind": "decision"
        },
        {
          "id": "error",
          "label": "Error",
          "description": "message + Retry · alert",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "empty",
          "label": "Empty",
          "description": "explanation + next action",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "success",
          "label": "Success",
          "description": "the data, rendered",
          "kind": "store",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "idle",
          "to": "loading",
          "label": "fetch()",
          "kind": "control"
        },
        {
          "from": "loading",
          "to": "fetch",
          "label": "await",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "fetch",
          "to": "error",
          "label": "reject / 4xx·5xx",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "fetch",
          "to": "empty",
          "label": "resolve · data.length === 0",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "fetch",
          "to": "success",
          "label": "resolve · data.length > 0",
          "kind": "control",
          "tone": "success"
        },
        {
          "from": "error",
          "to": "loading",
          "label": "Retry refires",
          "kind": "control",
          "dashed": true
        },
        {
          "from": "empty",
          "to": "loading",
          "label": "broaden query refires",
          "kind": "control",
          "dashed": true
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "in-flight (aria-busy)"
        },
        {
          "tone": "warning",
          "label": "non-happy states with a recovery path"
        },
        {
          "tone": "success",
          "label": "data present, rendered"
        }
      ],
      "caption": "The bug class lives on the resolve edge: most code splits reject vs resolve but forgets resolve splits AGAIN into empty vs success. Two edges out of Response is the amateur mistake; three is the contract."
    },
    {
      "type": "code-walkthrough",
      "title": "One component, four explicit branches",
      "subtitle": "A search results list in React. Each state is its own return — never one blank fall-through. This is the artifact's reference implementation.",
      "filename": "SearchResults.tsx",
      "language": "ts",
      "code": "function SearchResults({ query }: { query: string }) {\n  const { data, error, isPending } = useQuery({\n    queryKey: ['search', query],\n    queryFn: () => searchApi(query),\n  })\n\n  // 1. LOADING — skeleton, not a blank box; signal busy to AT\n  if (isPending) {\n    return <ResultsSkeleton aria-busy=\"true\" aria-label=\"Loading results\" />\n  }\n\n  // 2. ERROR — explain + recover; announce assertively\n  if (error) {\n    return (\n      <div role=\"alert\">\n        <p>Couldn’t load results. {getErrorMessage(error)}</p>\n        <button onClick={() => refetch()}>Retry</button>\n      </div>\n    )\n  }\n\n  // 3. EMPTY — a designed state, not the absence of one\n  if (data.length === 0) {\n    return (\n      <EmptyState\n        title={`No results for “${query}”`}\n        action=\"Try a broader term or check spelling\"\n      />\n    )\n  }\n\n  // 4. SUCCESS — the data, with a live region for the count\n  return (\n    <ul aria-live=\"polite\">\n      <p className=\"sr-only\">{data.length} results</p>\n      {data.map((r) => <ResultRow key={r.id} result={r} />)}\n    </ul>\n  )\n}",
      "steps": [
        {
          "lines": [
            2,
            3,
            4,
            5
          ],
          "label": "Three signals, not one",
          "note": "isPending, error, and data are separate. A boolean `loading` flag alone cannot tell empty from errored from successful — you need all three to branch correctly."
        },
        {
          "lines": [
            8,
            9,
            10
          ],
          "label": "Loading is a skeleton + aria-busy",
          "note": "A skeleton previews layout (less shift on swap); aria-busy=\"true\" tells a screen reader the region is updating instead of reading a half-built tree."
        },
        {
          "lines": [
            13,
            18,
            19
          ],
          "label": "Error explains AND recovers",
          "note": "role=\"alert\" makes assistive tech announce it immediately. getErrorMessage() narrows unknown to a user-safe string — never leak a raw stack. The Retry button refires the query: a state with no exit is a dead end."
        },
        {
          "lines": [
            23,
            24,
            25,
            26,
            27,
            28
          ],
          "label": "Empty is its own return",
          "note": "The whole lesson in one branch: data.length === 0 gets a title + a next action, not the blank fall-through that makes empty look identical to loading or broken."
        },
        {
          "lines": [
            32,
            33,
            34,
            35
          ],
          "label": "Success announces the count",
          "note": "aria-live=\"polite\" + an sr-only count means a non-sighted user hears '12 results' when the list swaps in — sighted users see it, everyone gets the state change."
        }
      ],
      "caption": "Four returns, one per state. Read top to bottom it IS the state matrix — and it’s the code rows of component_state_matrix_and_accessibility_packet.md."
    },
    {
      "type": "compare",
      "title": "Two-state thinking vs the four-state contract",
      "subtitle": "Same search component. Both 'work' in the demo. Only one survives a slow network, a new user, and a 500.",
      "left": {
        "label": "Happy-path only",
        "tone": "warning",
        "lines": [
          "if (loading) return <Spinner/> — one boolean for everything",
          "On []: falls through to map() → blank container",
          "On reject: error bubbles → red stack trace on screen",
          "Empty looks identical to loading and to crashed",
          "No aria-busy, no role=alert, no recovery button"
        ],
        "verdict": "Three of four states are unhandled — demo-only"
      },
      "right": {
        "label": "Four-state contract",
        "tone": "success",
        "lines": [
          "isPending → skeleton + aria-busy=\"true\"",
          "data.length === 0 → designed EmptyState + next action",
          "error → role=alert message + Retry that refires",
          "data.length > 0 → list + aria-live count announcement",
          "Each state visibly distinct; empty ≠ loading ≠ error"
        ],
        "verdict": "Every state rendered on purpose — ships"
      },
      "mono": true,
      "caption": "The tell is the resolve branch: the weak version forgets that a successful response with zero rows is its own state. 'It worked in the demo' means 'the data was non-empty and the network was fast' — two conditions production won’t guarantee."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 makes this non-optional, not nice-to-have: a sighted user sees the spinner stop, but a screen-reader user gets silence unless you announce the change. Loading needs aria-busy, errors need role=\"alert\" (assertive), and a successful swap needs an aria-live region — otherwise the state changed for everyone except the people who most need to be told. Status by color alone (red error text, no icon or label) also fails contrast and color-only signaling."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this isn’t just a happy-path component.' Which response actually proves the four-state contract holds?",
      "options": [
        "'It renders the results correctly and the spinner shows while loading.'",
        "'Here’s the state matrix: each of Loading/Empty/Error/Error has a distinct rendered UI, plus a keyboard checklist, a forced-error screenshot, and a zero-results screenshot proving empty ≠ loading.'",
        "'I used a loading boolean and a try/catch, so all cases are covered.'",
        "'The component passed code review and the types compile.'"
      ],
      "answer": 1,
      "explanation": "The reviewer is asking for evidence of the non-happy states specifically. Only option 2 points to the artifact and the forced-failure proof: a matrix that distinguishes empty from loading, and screenshots that exercise the states the happy path hides. A loading boolean + try/catch (3) still can’t tell empty from success; 'renders correctly' (1) and 'types compile' (4) only attest to the success branch."
    },
    {
      "type": "verification",
      "intro": "Prove the four states hold — no vibes:",
      "items": [
        "Force each state and confirm a DISTINCT visible UI: throttle the network (Loading), search a nonsense string (Empty), kill the API or mock a 500 (Error), search a real term (Success). Empty must not look like Loading.",
        "Run the keyboard checklist: Tab to the Retry button, activate it with Enter/Space, confirm focus is visible and the action refires the request.",
        "Turn on a screen reader (VoiceOver / NVDA): Loading announces busy, Error announces immediately via role=alert, and the result count is announced on success.",
        "Capture the viewport-failure note: at 320px, does the Empty/Error copy wrap without overflow, and does the Retry button stay tappable (≥44px)?",
        "Confirm every Error branch shows a user-safe message (getErrorMessage), never a raw stack trace or leaked internal detail — and offers a recovery action."
      ]
    },
    {
      "type": "teachback",
      "prompt": "Teach it back in order — problem, contract, proof:",
      "prompts": [
        "Why does a successful response still split into two states, and what bug appears when you forget the second one — using your own component as the example?",
        "Explain what each accessibility hook is FOR: aria-busy on Loading, role=alert on Error, aria-live on Success. What does a screen-reader user lose if you drop them?",
        "Point at your artifact: which row proves Empty is a designed state and not just 'no results rendered nothing'?"
      ]
    },
    {
      "type": "transfer",
      "text": "The same four-state contract governs every async surface, not just search. A submitted form: Loading (button disabled + spinner), Empty (no validation errors yet), Error (field-level messages + focus moved to the first invalid field), Success (confirmation + cleared form). A dashboard tile, an infinite-scroll feed, a file upload — all four states, every time. What stays invariant: exactly one state per render, each one designed, Empty and Error given a way out. What changes with context: a one-way action (payment) demands a louder Error and a confirmed Success; a low-stakes widget can lean on a quiet skeleton. Carry component_state_matrix_and_accessibility_packet.md into the capstone — its matrix + accessibility proof is reused verbatim as the per-component contract for the fullstack product dashboard."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "api-client-contracts": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real read-endpoint your UI calls and write its Api Client Contract: a runtime-validated response schema, a typed client function that never returns `any`, and the full state matrix (loading · empty · error · success) the UI must render — then attach it all to component_state_matrix_and_accessibility_packet.md.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "component_state_matrix_and_accessibility_packet.md containing: the response schema (Zod or equivalent), the typed client function, a 4-row state matrix mapping each API outcome to a named UI state with its accessibility behavior, and one reviewer objection answered. A peer can name what the screen reader announces in every state without you narrating.",
      "unlock": "Your client parses the response at the boundary (no unchecked `any`), the inferred type flows into the component, and every API outcome maps to a designed UI state with an accessibility note.",
      "doNotClaim": "Do not claim you have an Api Client Contract if your fetch returns `res.json()` straight into the component as `any`. An unvalidated payload is a promise, not a contract — the first malformed field becomes a white-screen error boundary in production, not a typed failure you handle."
    },
    {
      "type": "mission",
      "text": "The demo works on your machine, then the backend ships a field rename and the customer's dashboard goes blank — `Cannot read properties of undefined` — because nothing checked the shape between the API and the screen. A contract is the guardrail that turns that white screen into a handled error state."
    },
    {
      "type": "context",
      "text": "This is the applied-skill lesson that connects component state, form validation, accessibility, and API integration into one boundary. Lock it and every later screen — lists, forms, dashboards — inherits typed data and designed failure instead of hope."
    },
    {
      "type": "pretest",
      "prompt": "`const data = await res.json()` — TypeScript happily lets you write `data.user.name`. So what is wrong with trusting that line? Answer before reading on.",
      "reveal": "`res.json()` is typed `any` (or `Promise<any>`) — TypeScript stops checking the moment the network boundary is crossed. `data.user.name` compiles even when the server sent `{ error: 'rate limited' }`. The type was a comforting fiction; at runtime the field is `undefined` and the render throws. A contract replaces the fiction with a parse: validate the bytes, THEN you have a real type."
    },
    {
      "type": "concept",
      "title": "A contract validates the shape at the boundary, then pins every UI state to an outcome",
      "text": "Parse the response against a schema at the network edge — invalid data fails loudly there, not deep in render. The invariant: typed data in, typed failure out, and loading · empty · error · success each get a designed, accessible state."
    },
    {
      "type": "diagram",
      "title": "The Api Client Contract boundary",
      "subtitle": "The component never touches raw `any`. The client parses at the edge; valid data flows to the success state, a parse failure routes to the error state, and an error boundary catches anything that still slips through. Auth and analytics are out of scope.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "component",
          "label": "Component",
          "description": "renders one of 4 states",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "client",
          "label": "Typed API Client",
          "description": "fetch + schema.parse()",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "schema",
          "label": "Response Schema",
          "description": "the contract (Zod)",
          "kind": "decision",
          "tone": "success"
        },
        {
          "id": "api",
          "label": "Backend API",
          "description": "untrusted JSON",
          "kind": "external",
          "tone": "muted"
        },
        {
          "id": "errstate",
          "label": "Error State",
          "description": "announced, retryable",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "boundary",
          "label": "Error Boundary",
          "description": "last-resort catch",
          "kind": "process",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "component",
          "to": "client",
          "label": "calls",
          "kind": "sync"
        },
        {
          "from": "client",
          "to": "api",
          "label": "fetch",
          "kind": "sync"
        },
        {
          "from": "api",
          "to": "schema",
          "label": "raw JSON",
          "kind": "data",
          "tone": "muted"
        },
        {
          "from": "schema",
          "to": "component",
          "label": "valid → typed data",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "schema",
          "to": "errstate",
          "label": "invalid → throw",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "errstate",
          "to": "component",
          "label": "render error UI",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "component",
          "to": "boundary",
          "label": "uncaught render throw",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "your typed code (no `any` crosses here)"
        },
        {
          "tone": "success",
          "label": "the contract — validates then types"
        },
        {
          "tone": "warning",
          "label": "failure paths, both designed"
        },
        {
          "tone": "muted",
          "label": "untrusted / out of scope"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Writing the contract, one layer at a time",
      "subtitle": "Schema → inferred type → a client that parses → a component that renders all four states. No `any` survives the boundary.",
      "filename": "lib/api/orders.ts",
      "language": "ts",
      "code": "import { z } from 'zod'\n\n// 1. The contract: the exact shape the UI is allowed to depend on.\nconst OrderSchema = z.object({\n  id: z.string(),\n  status: z.enum(['pending', 'shipped', 'delivered']),\n  total: z.number().nonnegative(),\n})\nconst OrdersResponse = z.object({ orders: z.array(OrderSchema) })\n\n// 2. The type is INFERRED from the contract — one source of truth.\nexport type Order = z.infer<typeof OrderSchema>\n\n// 3. The typed client: validates at the boundary, never returns `any`.\nexport async function fetchOrders(): Promise<Order[]> {\n  const res = await fetch('/api/orders')\n  if (!res.ok) throw new ApiError(res.status, 'orders request failed')\n  const json: unknown = await res.json()        // untrusted\n  const parsed = OrdersResponse.parse(json)      // contract enforced here\n  return parsed.orders\n}\n\nexport class ApiError extends Error {\n  constructor(public status: number, message: string) { super(message) }\n}",
      "steps": [
        {
          "lines": [
            4,
            5,
            6,
            7,
            8
          ],
          "label": "Declare the contract",
          "note": "The schema is the single statement of what the UI may depend on. `status` is an enum, so an unknown status from the backend fails the parse instead of silently rendering a broken badge."
        },
        {
          "lines": [
            11
          ],
          "label": "Infer the type — never hand-write it",
          "note": "`z.infer` derives `Order` from the schema. Type and runtime check can never drift, because they are the same declaration."
        },
        {
          "lines": [
            17
          ],
          "label": "Type the raw payload as `unknown`, not `any`",
          "note": "This is the whole discipline in one line: the boundary is untrusted, so the compiler forces you to validate before you read a field."
        },
        {
          "lines": [
            18
          ],
          "label": "Enforce the contract",
          "note": "`.parse()` either returns fully-typed data or throws a precise error naming the bad field. A backend rename now fails HERE, loudly, not three components deep."
        },
        {
          "lines": [
            15
          ],
          "label": "Distinguish transport failure from shape failure",
          "note": "A non-2xx (`!res.ok`) is a different outcome than malformed-but-200 JSON. The state matrix needs both to drive the right UI."
        },
        {
          "lines": [
            23
          ],
          "label": "A typed error carries status into the UI",
          "note": "`ApiError.status` lets the error state say '429 — too many requests, retrying' instead of a generic 'something went wrong'."
        }
      ],
      "caption": "Run it against a renamed field and `.parse()` throws `Invalid enum value at orders[0].status` — a named failure you can route to the error state, not a `undefined` crash in render."
    },
    {
      "type": "compare",
      "title": "Same endpoint, two clients",
      "subtitle": "Both fetch orders. Only one has a contract — and only one survives a backend that changes under it.",
      "left": {
        "label": "Trusting fetch (no contract)",
        "tone": "warning",
        "lines": [
          "const data = await res.json()  // typed any",
          "Component reads data.orders[0].total directly",
          "No runtime check — type is a fiction",
          "Only state: success (loading/empty/error unhandled)",
          "Backend renames a field → undefined → error boundary",
          "Screen reader announces nothing; user sees a blank page"
        ],
        "verdict": "Compiles green, white-screens in prod"
      },
      "right": {
        "label": "Api Client Contract",
        "tone": "success",
        "lines": [
          "json: unknown → OrdersResponse.parse(json)",
          "Type inferred from the schema, one source of truth",
          "Invalid shape throws a named error at the boundary",
          "Four states designed: loading · empty · error · success",
          "Backend rename → handled error state + retry",
          "Error state has role=alert; empty state is announced"
        ],
        "verdict": "Fails loud, stays accessible, recovers"
      },
      "caption": "The contract does not stop the backend from changing — it converts an invisible runtime crash into a typed, announced, recoverable UI state."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "Standards grounding (WCAG 2.2 + SWEBOK V4): the error state is not done when it renders red — it is done when it is announced. Put `role=\"alert\"` (or an aria-live region) on the error and empty states so a screen-reader user learns the fetch failed without watching the viewport. The contract owns the data shape; the state matrix owns the accessible behavior of each outcome. A reviewer checks both."
    },
    {
      "type": "quiz",
      "question": "Reviewer challenge: 'Show me this isn't just a happy-path client.' Which evidence actually proves the contract holds?",
      "options": [
        "The TypeScript build passes with zero errors and the success state renders.",
        "A test feeds the client a malformed payload (wrong enum / missing field) and asserts it throws a named parse error, plus the state matrix shows each outcome's UI and what the screen reader announces.",
        "The component has a loading spinner and a try/catch around the fetch.",
        "The schema file exists and the types look correct in the editor."
      ],
      "answer": 1,
      "explanation": "A green build (1, 4) only proves the happy path compiles — TypeScript never sees the runtime payload. A spinner and bare try/catch (3) don't prove the bad shape is caught or that failure is accessible. The contract is proven only when a deliberately malformed response is rejected at the boundary AND every outcome maps to a designed, announced state — that is what survives review and the capstone."
    },
    {
      "type": "verification",
      "intro": "Prove the contract — no vibes:",
      "items": [
        "Grep the client for `: any` and for `res.json()` used directly in a component. If either reads a field without a parse in between, the boundary is unguarded — fix it.",
        "Feed the client a payload with a wrong enum value or a missing required field and confirm it throws a named error AT the client, not a crash inside render.",
        "Confirm the type is `z.infer`'d from the schema, not hand-written — so the runtime check and the type can never drift.",
        "Walk the state matrix: loading, empty, error, and success each have a defined UI, and the error and empty states are announced (role=alert / aria-live), not silent.",
        "Confirm a non-2xx response (`!res.ok`) and a malformed-200 response route to DIFFERENT, correct states — transport failure and shape failure are distinct outcomes."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why `res.json()` being typed `any` is the exact spot the contract has to live — in your own words, using your endpoint.",
        "Walk your state matrix out loud: name what renders AND what the screen reader announces for loading, empty, error, and success.",
        "A teammate says 'the build is green, the client is fine.' Explain why a green build proves nothing about the runtime payload, and what evidence you'd show instead."
      ]
    },
    {
      "type": "transfer",
      "text": "The same contract pattern transfers to the inbound direction: a form's submit is just an Api Client Contract in reverse — validate the user's input against a schema BEFORE it leaves the browser, mirror that schema on the server, and map each outcome (invalid · pending · rejected · accepted) to an accessible state. What stays invariant: untrusted data gets parsed at the boundary and every outcome gets a designed, announced UI. What changes with risk: a payments mutation demands a stricter schema, idempotency, and an optimistic-then-rollback state, where a read tolerated a simple retry. Your component_state_matrix_and_accessibility_packet.md carries straight into the capstone dashboard — that screen reuses this exact client, schema, and state matrix as its data layer, so the proof you built here is the proof it ships on."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "tables-filters-pagination": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real list surface (a data table with a filter and pagination) and build its component_state_matrix_and_accessibility_packet.md: every cell of the loading × empty × error × success grid, what the URL holds, and the keyboard + screen-reader behavior for each state — not prose, a reviewable artifact a teammate could implement from.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a 5-row state matrix (loading, empty, error, success, filtering-in-flight) crossed with table/filter/pager, the URL contract for filter+page, and a keyboard/aria checklist per state. A peer changes the filter, hits page 3, refreshes, and lands on the exact same view — because the state lives in the URL, not React.",
      "unlock": "Your matrix covers all five states across table/filter/pager, the URL is the source of truth for filter+page, and every state declares its keyboard + aria-live behavior.",
      "doNotClaim": "Do not claim you 'handle pagination' if your page number lives only in useState. The moment a user shares the link, hits back, or refreshes, the view resets — that is a broken table wearing a working one's clothes. State that survives a refresh is the bar; component memory is not."
    },
    {
      "type": "mission",
      "text": "A customer filters 40,000 orders down to \"refunded, last 7 days,\" clicks page 4, finds the one they need — then refreshes to grab the link for support, and the table snaps back to page 1, unfiltered. They lost it. The filter worked; the state didn't survive a single refresh."
    },
    {
      "type": "context",
      "text": "A table with filters and pagination is the most common product surface there is — and the one where state, async, accessibility, and the URL all collide at once. Get the state model right here and you have the pattern for every list view, search page, and admin grid you will ever build."
    },
    {
      "type": "pretest",
      "prompt": "Where should the active filter and current page number actually live — React component state (useState), or somewhere else? Decide before reading on.",
      "reveal": "Not component state — the URL. Filter and page are shareable, bookmarkable, back-button-able view state, so the query string (?status=refunded&page=4) is their real home. Put them in useState and you silently break refresh, deep links, the back button, and open-in-new-tab — four things users do constantly. The rule: if a teammate would want to paste the link in Slack and have it show the same view, it belongs in the URL, not the component."
    },
    {
      "type": "concept",
      "title": "The list surface is a state matrix, and the URL is its memory",
      "text": "A table is never one view — it is loading × empty × error × success, each with its own keyboard and screen-reader behavior. Filter and page are URL state, not component state. The invariant: the URL fully determines what renders, so a refresh reproduces it exactly."
    },
    {
      "type": "diagram",
      "title": "The list-surface request loop — URL in, state out",
      "subtitle": "The query string drives the fetch; the response selects one cell of the state matrix. aria-live announces the transition so a screen-reader user is never stranded. Sorting and column config are deliberately out of scope.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "url",
          "label": "URL / query string",
          "description": "?status=refunded&page=4",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "filter",
          "label": "Filter + Pager UI",
          "description": "writes to URL, never local state",
          "kind": "client"
        },
        {
          "id": "fetch",
          "label": "Data fetch",
          "description": "keyed by the query string",
          "kind": "service"
        },
        {
          "id": "api",
          "label": "List API",
          "description": "/orders?status&page&limit",
          "kind": "external"
        },
        {
          "id": "state",
          "label": "Render state",
          "description": "loading · empty · error · success",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "table",
          "label": "Table + aria-live",
          "description": "announces row count / errors",
          "kind": "process",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "filter",
          "to": "url",
          "label": "pushes params",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "url",
          "to": "fetch",
          "label": "is the cache key",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "fetch",
          "to": "api",
          "label": "GET",
          "kind": "sync"
        },
        {
          "from": "api",
          "to": "state",
          "label": "data | [] | error",
          "kind": "async"
        },
        {
          "from": "state",
          "to": "table",
          "label": "selects one cell",
          "kind": "data",
          "tone": "warning"
        },
        {
          "from": "table",
          "to": "filter",
          "label": "user refines",
          "kind": "control",
          "dashed": true,
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "URL is the source of truth (survives refresh)"
        },
        {
          "tone": "warning",
          "label": "the four render states — each needs aria + keyboard"
        },
        {
          "tone": "success",
          "label": "aria-live announces the transition"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Reading filter + page FROM the URL, not state",
      "subtitle": "The whole pattern: URL is the source of truth, the fetch is keyed by it, and the empty/error states are real branches — not afterthoughts.",
      "filename": "OrdersTable.tsx",
      "language": "ts",
      "code": "function OrdersTable() {\n  const [params, setParams] = useSearchParams()\n  const status = params.get('status') ?? 'all'\n  const page = Number(params.get('page') ?? '1')\n\n  // The query string IS the cache key — refresh reproduces this view exactly.\n  const { data, error, isLoading } = useOrders({ status, page, limit: 25 })\n\n  function setFilter(next: string) {\n    setParams({ status: next, page: '1' }) // new filter resets to page 1\n  }\n\n  if (isLoading) return <TableSkeleton rows={25} aria-busy=\"true\" />\n  if (error) return <ErrorState onRetry={() => setParams(params)} />\n  if (data.rows.length === 0) return <EmptyState status={status} onClear={() => setFilter('all')} />\n\n  return (\n    <>\n      <FilterBar status={status} onChange={setFilter} />\n      <table aria-live=\"polite\">{/* rows */}</table>\n      <Pager page={page} total={data.total} onPage={(p) => setParams({ status, page: String(p) })} />\n    </>\n  )\n}",
      "steps": [
        {
          "lines": [
            2,
            3,
            4
          ],
          "label": "URL is the source of truth",
          "note": "status and page are READ from the query string, never from useState. Refresh, back button, and shared links all reproduce the exact view for free."
        },
        {
          "lines": [
            7
          ],
          "label": "Fetch is keyed by the URL",
          "note": "useOrders({status, page}) — the params are the cache key. Change the URL, the data refetches; nothing else can drift out of sync."
        },
        {
          "lines": [
            10,
            11
          ],
          "label": "New filter resets the page",
          "note": "The classic bug: filter to 3 results while on page 7 and you render an empty page that exists. Always reset page to 1 when the filter changes."
        },
        {
          "lines": [
            14
          ],
          "label": "Loading is a real state",
          "note": "A skeleton with aria-busy, sized to the incoming rows (25) so the layout doesn't shift — no CLS, and assistive tech knows a fetch is in flight."
        },
        {
          "lines": [
            15
          ],
          "label": "Error is a branch, not a crash",
          "note": "A retry affordance that re-runs the SAME params. The user keeps their filter and page through the failure."
        },
        {
          "lines": [
            16
          ],
          "label": "Empty ≠ error",
          "note": "Zero rows for a valid filter is success-with-no-results, with a way OUT (clear the filter) — not an error screen and not a blank table."
        },
        {
          "lines": [
            21
          ],
          "label": "aria-live announces the change",
          "note": "polite announces \"showing 25 of 412\" when results swap, so a screen-reader user isn't left wondering if anything happened after they filtered."
        }
      ],
      "caption": "Copy this view's URL, paste it in a new tab — same filter, same page, same rows. That is the test the whole lesson is built around."
    },
    {
      "type": "compare",
      "title": "Two implementations of the same orders table",
      "subtitle": "Both render rows. Only one survives a refresh, a shared link, and a screen reader.",
      "left": {
        "label": "State-in-component, happy-path-only",
        "tone": "warning",
        "lines": [
          "filter + page in useState — gone on refresh",
          "Shared link / back button reset to page 1",
          "Empty result renders a blank table (looks broken)",
          "Fetch error throws — white screen, filter lost",
          "Filter changes but page stays at 7 → empty page",
          "No aria-live: screen reader hears nothing change"
        ],
        "verdict": "Demos fine, breaks the first time a real user refreshes"
      },
      "right": {
        "label": "URL-driven state matrix",
        "tone": "success",
        "lines": [
          "filter + page in the query string — survive refresh",
          "Link is shareable, back button works, deep-linkable",
          "Empty state names the filter + offers a way out",
          "Error state retries the SAME params, keeps context",
          "New filter resets page to 1 — no phantom empty page",
          "aria-live announces \"showing N of M\" on every change"
        ],
        "verdict": "Survives refresh, sharing, failure, and a screen reader"
      },
      "caption": "The difference isn't the rows — it's the five states and where the page number lives. \"It renders\" is not \"it works.\""
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 is why empty and loading are first-class states, not afterthoughts. A blank table after filtering is a 'status message' failure (SC 4.1.3) — the screen-reader user gets silence and assumes the app froze. aria-live=\"polite\" on the results region and aria-busy on the skeleton are the whole fix. Senior engineers design the empty and error cells of the matrix before the happy path, because that's where products actually break in production."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: \"Prove this isn't just a happy-path table.\" Which answer is real evidence?",
      "options": [
        "\"The rows render correctly and the filter dropdown works when I click it.\"",
        "\"Here's the state matrix: loading shows an aria-busy skeleton, empty names the filter with a clear-action, error retries the same params, and filter+page live in the URL — so refresh, this shared link, and the back button all reproduce the view, and aria-live announces each result change.\"",
        "\"I used a well-known table library, so accessibility and pagination are handled for me.\"",
        "\"It looks good on my screen and the pagination buttons increment the page number.\""
      ],
      "answer": 1,
      "explanation": "Only option 2 points at the artifact: the full state matrix (loading/empty/error/success), the URL contract that survives refresh and sharing, page-reset-on-filter, and the aria-live announcement. The others are happy-path vibes — 'it renders,' 'the library handles it,' 'looks good' — none of which a reviewer can verify against a refresh or a screen reader."
    },
    {
      "type": "verification",
      "intro": "Prove the table holds — no vibes:",
      "items": [
        "Copy the URL after filtering + paging, paste in a new tab. Same filter, same page, same rows? If it resets, your state is in the component — move it to the query string.",
        "Filter down to a status with zero matches. You should see an empty state that names the filter and offers a clear-action — not a blank table and not an error screen.",
        "Kill the network (devtools offline) and load the table. You should get an error state with a retry that re-runs the SAME params, keeping the filter — not a white screen.",
        "Filter while on page 7 of the old result set. The page must reset to 1; if you land on an empty page that 'exists,' you skipped the reset.",
        "Tab through the filter and pager with no mouse: every control reachable, focus visible, and a screen reader announces the result count change (aria-live). If filtering is silent to assistive tech, it fails WCAG 4.1.3."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain to a teammate why filter and page belong in the URL and not useState — name two specific user actions that break when they don't.",
        "Walk through your state matrix: what does the table, the filter, and the pager each show in the loading, empty, and error states?",
        "Why is 'empty' a different state from 'error,' and what does each one owe the user that a blank table doesn't?"
      ]
    },
    {
      "type": "transfer",
      "text": "This exact pattern — URL as the source of truth, a state matrix with real loading/empty/error cells, and aria-live on the results — is every list surface you will build: a search page, an admin dashboard, an infinite-scroll feed (where 'page' becomes a cursor), a faceted product grid (where one filter becomes five). What stays invariant: shareable view-state lives in the URL, and every async surface owes the user a loading, empty, and error state with accessible announcements. What changes is scale (cursor vs. offset pagination), risk (debounce a text filter so you don't hammer the API on every keystroke), and device (the filter bar collapses into a sheet on mobile). Carry component_state_matrix_and_accessibility_packet.md into the capstone dashboard — it is the proof that you build surfaces that survive refresh, sharing, failure, and a screen reader, not ones that only look right in a demo."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "dashboard-metrics-charts": [
    {
      "type": "sprint-contract",
      "outcome": "Build one real metrics dashboard panel that renders a chart from a live API and survives ALL five UI states — loading, empty, error, disabled, success — then capture the proof in component_state_matrix_and_accessibility_packet.md. Not a happy-path screenshot: a state matrix, a keyboard pass, and a viewport-failure note.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "component_state_matrix_and_accessibility_packet.md attached to the evidence ledger: a 5-row state matrix (loading/empty/error/disabled/success), a Playwright flow that drives the panel through them, a keyboard + screen-reader checklist, and a viewport-failure screenshot note (e.g. chart overflows at 320px). A reviewer reads it and can reproduce every state without you narrating.",
      "unlock": "Your panel renders all five states from real API behavior, the chart is keyboard-reachable with a text alternative, and the matrix proves each state was hit — not assumed.",
      "doNotClaim": "Do not claim a dashboard is 'done' because the chart renders when the data loads. A dashboard that only has a success state is a demo. The skill is the states where there is NO data, BAD data, or a SLOW network — that is where real dashboards break in production."
    },
    {
      "type": "mission",
      "text": "The exec opens the revenue dashboard on launch morning; the metrics API is rate-limited, the chart silently renders an empty canvas with no axes, and the room concludes revenue is zero. The number was fine — the missing error state lied for it."
    },
    {
      "type": "context",
      "text": "A metrics chart is the load-bearing surface of every product dashboard: it is where async API data, component state, accessibility, and responsive layout all meet at once. Get the states right and the dashboard is trustworthy; skip one and a healthy system looks broken — or a broken one looks healthy."
    },
    {
      "type": "pretest",
      "prompt": "Your chart component fetches /api/metrics and renders bars from the response. You ship it. What is the most likely way it lies to a user in production — before any bug in your charting code? Answer before reading on.",
      "reveal": "It conflates 'no data yet' with 'data is zero'. An empty array, a 200-with-null, a 500, and a still-loading request can ALL render as an empty or flat chart — and an empty chart reads as 'the metric is zero', not 'we don't know'. The fix is not better charting; it is distinct, honest states: a skeleton while loading, an explicit empty message, a visible error with retry, and the chart only when real data exists. The chart is the easy part; the states around it are the skill."
    },
    {
      "type": "concept",
      "title": "A metrics chart is a state machine first, a chart second",
      "text": "Every async panel has the same five states: loading, empty, error, disabled, success. The invariant: each state renders a DISTINCT, honest surface — never reuse the empty chart for loading or error. The chart is one branch; the dashboard is the whole machine."
    },
    {
      "type": "diagram",
      "title": "The metrics panel as a state machine",
      "subtitle": "One fetch, five distinct surfaces. The dashed path is the failure most teams skip — error and empty both collapsing into the same blank chart, which is the production lie.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "mount",
          "label": "Panel mounts",
          "description": "fetch /api/metrics",
          "kind": "client",
          "tone": "muted"
        },
        {
          "id": "loading",
          "label": "Loading",
          "description": "skeleton + aria-busy",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "api",
          "label": "Metrics API",
          "description": "200 · 200-empty · 4xx/5xx",
          "kind": "external"
        },
        {
          "id": "success",
          "label": "Success",
          "description": "chart + text alternative",
          "kind": "process",
          "tone": "success"
        },
        {
          "id": "empty",
          "label": "Empty",
          "description": "'no data for range'",
          "kind": "process"
        },
        {
          "id": "error",
          "label": "Error",
          "description": "message + Retry",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "blank",
          "label": "Blank chart",
          "description": "the silent lie",
          "kind": "store",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "mount",
          "to": "loading",
          "label": "request starts",
          "kind": "control"
        },
        {
          "from": "loading",
          "to": "api",
          "label": "awaits",
          "kind": "async",
          "tone": "accent"
        },
        {
          "from": "api",
          "to": "success",
          "label": "rows > 0",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "api",
          "to": "empty",
          "label": "rows = 0",
          "kind": "data"
        },
        {
          "from": "api",
          "to": "error",
          "label": "4xx / 5xx / timeout",
          "kind": "data",
          "tone": "warning"
        },
        {
          "from": "error",
          "to": "loading",
          "label": "Retry",
          "kind": "control"
        },
        {
          "from": "empty",
          "to": "blank",
          "label": "if collapsed into chart",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "error",
          "to": "blank",
          "label": "if swallowed",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "loading — skeleton, never a blank chart"
        },
        {
          "tone": "success",
          "label": "success — chart WITH a text alternative"
        },
        {
          "tone": "warning",
          "label": "error/empty collapsing into a blank chart — the lie"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Building the panel, one branch at a time",
      "subtitle": "A real MetricsChart: fetch → narrow the response → render a DISTINCT surface per state. Recharts for the bars, but the branching is the lesson.",
      "filename": "MetricsChart.tsx",
      "language": "ts",
      "code": "type Point = { label: string; value: number }\ntype State =\n  | { kind: 'loading' }\n  | { kind: 'empty' }\n  | { kind: 'error'; message: string }\n  | { kind: 'success'; data: Point[] }\n\nexport function MetricsChart({ range }: { range: string }) {\n  const [state, setState] = useState<State>({ kind: 'loading' })\n\n  useEffect(() => {\n    let live = true\n    setState({ kind: 'loading' })\n    fetch(`/api/metrics?range=${range}`)\n      .then(async (res) => {\n        if (!res.ok) throw new Error(`Metrics unavailable (${res.status})`)\n        const json = (await res.json()) as { points: Point[] }\n        if (!live) return\n        setState(\n          json.points.length === 0\n            ? { kind: 'empty' }\n            : { kind: 'success', data: json.points },\n        )\n      })\n      .catch((e) => live && setState({ kind: 'error', message: e.message }))\n    return () => { live = false }\n  }, [range])\n\n  if (state.kind === 'loading')\n    return <Skeleton aria-busy=\"true\" label=\"Loading revenue\" />\n  if (state.kind === 'empty')\n    return <Empty>No revenue in this range.</Empty>\n  if (state.kind === 'error')\n    return <ErrorState message={state.message} onRetry={() => setRange(range)} />\n\n  return (\n    <figure>\n      <BarChart data={state.data} aria-hidden=\"true\">{/* bars */}</BarChart>\n      <figcaption className=\"sr-only\">\n        Revenue by day: {state.data.map((p) => `${p.label} ${p.value}`).join(', ')}\n      </figcaption>\n    </figure>\n  )\n}",
      "steps": [
        {
          "lines": [
            2,
            3,
            4,
            5,
            6
          ],
          "label": "Model the states as a union",
          "note": "A discriminated union ('kind') makes the five surfaces exhaustive and impossible to confuse. This IS the dashboard — the chart is one arm of it."
        },
        {
          "lines": [
            13,
            14
          ],
          "label": "An ok-check, not just a .json()",
          "note": "A 500 still returns a body. Without res.ok, you parse an error page as data and render garbage. This single line is where most 'silent blank chart' bugs are born."
        },
        {
          "lines": [
            18,
            19,
            20,
            21
          ],
          "label": "Empty is a decision, not an accident",
          "note": "rows === 0 routes to a distinct 'empty' state. Never let an empty array fall through to the chart — that is the zero-vs-unknown lie from the pretest."
        },
        {
          "lines": [
            23
          ],
          "label": "Catch maps to a VISIBLE error",
          "note": "live && setState guards against a state update after unmount (the classic React leak). The error surfaces a message + a Retry, not a swallowed console.error."
        },
        {
          "lines": [
            26,
            27,
            28
          ],
          "label": "Each state is its own surface",
          "note": "Loading → skeleton with aria-busy; empty → text; error → message+retry. No branch reuses another's UI. A reviewer can hit each one independently."
        },
        {
          "lines": [
            32,
            33,
            34
          ],
          "label": "The chart needs a text alternative",
          "note": "aria-hidden on the visual + an sr-only figcaption listing the values. A screen reader reads the numbers; canvas/SVG bars alone are invisible to assistive tech. This is the WCAG line."
        }
      ],
      "caption": "Drive it: throttle the network → skeleton; return points:[] → empty; return 500 → error+retry; return rows → chart + readable caption. Four states proven from one component."
    },
    {
      "type": "compare",
      "title": "Two metrics panels, same API",
      "subtitle": "Both render a chart on the happy path. Only one is safe to put in front of an executive.",
      "left": {
        "label": "Happy-path-only chart",
        "tone": "warning",
        "lines": [
          "fetch(...).then(r => r.json()) — no res.ok check",
          "data ?? [] passed straight to <BarChart>",
          "Empty array → blank chart, reads as 'zero'",
          "500 error → parsed as data → garbage or crash",
          "Loading shows the same empty canvas",
          "Chart is canvas-only — invisible to screen readers",
          "Overflows its container at 320px"
        ],
        "verdict": "Lies on no-data, bad-data, and slow networks"
      },
      "right": {
        "label": "State-complete panel",
        "tone": "success",
        "lines": [
          "res.ok gates parsing; non-200 throws",
          "Discriminated union: loading|empty|error|success",
          "Empty → explicit 'No revenue in this range'",
          "Error → visible message + Retry button",
          "Loading → skeleton with aria-busy",
          "figcaption sr-only lists values for assistive tech",
          "Responsive container, no overflow at 320px"
        ],
        "verdict": "Honest in every state — empty ≠ zero ≠ error ≠ loading"
      },
      "caption": "The difference is not charting skill. It is refusing to let four different realities (slow, none, broken, real) render as the same blank rectangle."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell on dashboards: they design the EMPTY and ERROR states first, before the chart. WCAG 2.2 (1.1.1, 4.1.3) makes it non-negotiable — a chart with no text alternative and a status change with no aria-live is inaccessible by definition. Pros also know empty ≠ zero: a flat line at 0 and 'no data reported' mean opposite things to the business, and only one of them is a problem."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this dashboard isn't just a happy-path answer.' Which response actually proves it?",
      "options": [
        "A screenshot of the chart rendering correctly with last week's data.",
        "The state matrix in component_state_matrix_and_accessibility_packet.md plus a Playwright flow that mocks 200-empty / 500 / slow-network and asserts a distinct, accessible surface for each.",
        "A note saying 'I tested it locally and it worked.'",
        "The component code, showing it compiles with no type errors."
      ],
      "answer": 1,
      "explanation": "Only option 2 points at the four pieces the reviewer challenge demands: the artifact (state matrix), the broken cases (mocked empty/500/slow), the proof (Playwright asserting a distinct accessible surface per state), and reproducibility. A success screenshot (1) and 'it worked' (3) are exactly the happy-path claims the challenge exists to reject; compiling (4) proves types, not behavior across states."
    },
    {
      "type": "verification",
      "intro": "Prove the panel across every state — no vibes, no happy-path screenshot:",
      "items": [
        "Force each state and confirm it renders a DISTINCT surface: throttle the network (skeleton), return points:[] (explicit empty message), return a 500 (visible error + Retry), return real rows (chart). If any two look the same, the machine is incomplete.",
        "Tab to the chart with the keyboard only: the panel is reachable, the Retry button is focusable, and focus is visible. No mouse-only controls.",
        "Run a screen reader (or read the accessibility tree): the success chart exposes a text alternative (figcaption / aria-label) with the actual values, and the loading/error states announce via aria-busy / aria-live — the canvas is not silent.",
        "Resize to 320px: the chart stays inside its container with no horizontal overflow, and record the failure-and-fix as a viewport note in the packet.",
        "Confirm res.ok gates parsing: a non-200 response routes to the error state, never to the chart. Empty array routes to empty, never to a blank success chart."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain, using your own panel, why an empty chart is a lie and what each of your five states says to the user instead.",
        "Walk a junior through why you check res.ok before .json() — what specifically breaks on a 500 if you skip it?",
        "Your chart is canvas/SVG. Explain what a screen-reader user experiences without your figcaption, and what your text alternative gives them."
      ]
    },
    {
      "type": "transfer",
      "text": "The state machine is the pattern, not the chart. A data table, a map, a KPI tile, an infinite-scroll feed — every async product surface has the same loading / empty / error / disabled / success states, and every one of them lies the same way when you skip empty-vs-error. What stays invariant: distinct honest surfaces per state, a text alternative for any visual, and a responsive container. What changes by context: a 320px phone needs the empty state to be the whole screen, a high-stakes financial view needs the error state to block rather than degrade, and a slow upstream API needs the loading state to set expectations (skeleton, then a 'still working' message). Carry component_state_matrix_and_accessibility_packet.md into the capstone — the fullstack dashboard reuses this exact matrix and proof as its accessibility evidence. You don't just render a chart now. You render the truth in every state."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "detail-routes-drawers": [
    {
      "type": "sprint-contract",
      "outcome": "Take one list-to-detail surface (an order, a user, a ticket) and decide it correctly: a routed detail page when the detail is shareable/deep-linkable, a drawer when it's a quick aside over the list. Then build the smallest version that survives loading, empty, error, and success — with the URL as the source of truth.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "Section of component_state_matrix_and_accessibility_packet.md: a state matrix for the detail surface (loading / empty / error / success), the route-vs-drawer decision with its reversal condition, and a keyboard/focus checklist. A peer opens your detail URL in a fresh tab and lands on the right item — no list context required.",
      "unlock": "Your detail surface is reachable by URL, covers all four states, and traps focus correctly when it's a drawer.",
      "doNotClaim": "Do not claim you 'built the detail view' if it only renders the success state for data already in memory. A detail surface that breaks on refresh, on an empty result, or on a 404 is a happy-path demo, not a reviewable artifact."
    },
    {
      "type": "mission",
      "text": "A customer pastes the order link a teammate sent them — and the page shows an empty drawer over yesterday's list, because the detail lived in component state, not the URL. The link was never real; it only worked for the person who clicked the row."
    },
    {
      "type": "context",
      "text": "This lesson sits in the production-pressure phase: every list eventually needs a detail view, and the first real decision is route vs drawer. Get the URL-as-source-of-truth invariant right here and deep links, refresh, and back-button all work for free downstream."
    },
    {
      "type": "pretest",
      "prompt": "You open a detail drawer by clicking a row, then hit refresh. Should the drawer still be open afterward — and what determines whether it is? Decide before reading.",
      "reveal": "It should — but only if the open/selected state lives in the URL (e.g. /orders?selected=8842 or /orders/8842), not in a useState boolean. After refresh, React state is gone; the URL is the only thing that survives the round-trip to the server. The 'oh': a drawer is just a detail route with a different presentation. If the identity of what's open isn't in the URL, your detail surface is un-shareable and un-refreshable by construction."
    },
    {
      "type": "concept",
      "title": "The URL owns the selected item; the component only renders states",
      "text": "Put the detail's identity in the URL (route param or search param). The component reads it, fetches, and renders one of loading / empty / error / success. Route when the detail is a shareable destination; drawer when it's a quick aside over the list."
    },
    {
      "type": "diagram",
      "title": "List to detail: how the URL drives the surface and its four states",
      "subtitle": "Clicking a row writes the id to the URL. The detail surface reads it, fetches, and renders exactly one state. The dashed path is the failure that proves the surface is real: a deep link / refresh re-enters from the URL, not from in-memory list state.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "list",
          "label": "List view",
          "description": "rows; click selects",
          "kind": "client"
        },
        {
          "id": "url",
          "label": "URL",
          "description": "/orders/:id or ?selected=id",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "surface",
          "label": "Detail surface",
          "description": "route page OR drawer",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "fetch",
          "label": "Fetch by id",
          "description": "load the detail",
          "kind": "external"
        },
        {
          "id": "loading",
          "label": "Loading",
          "description": "skeleton, not blank",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "empty",
          "label": "Empty / 404",
          "description": "id has no record",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "error",
          "label": "Error",
          "description": "fetch failed; retry",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "success",
          "label": "Success",
          "description": "the detail renders",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "deeplink",
          "label": "Deep link / refresh",
          "description": "enters from URL alone",
          "kind": "client",
          "tone": "muted"
        }
      ],
      "edges": [
        {
          "from": "list",
          "to": "url",
          "label": "click writes id",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "url",
          "to": "surface",
          "label": "reads id",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "surface",
          "to": "fetch",
          "label": "fetch(id)",
          "kind": "async"
        },
        {
          "from": "fetch",
          "to": "loading",
          "label": "pending",
          "kind": "control",
          "tone": "muted"
        },
        {
          "from": "fetch",
          "to": "empty",
          "label": "404 / null",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "fetch",
          "to": "error",
          "label": "rejected",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "fetch",
          "to": "success",
          "label": "resolved",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "deeplink",
          "to": "url",
          "label": "re-enters here",
          "kind": "control",
          "dashed": true,
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "URL-as-source-of-truth (the invariant)"
        },
        {
          "tone": "warning",
          "label": "the states beginners skip (empty + error)"
        },
        {
          "tone": "success",
          "label": "the only state most demos show"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "A deep-linkable detail surface that renders all four states",
      "subtitle": "Next.js App Router page for /orders/[id]. The id comes from the URL — so refresh and deep links work — and every branch (loading, empty, error, success) is a real render path.",
      "filename": "app/orders/[id]/OrderDetail.tsx",
      "language": "ts",
      "code": "'use client'\nimport { useQuery } from '@tanstack/react-query'\nimport { useRouter } from 'next/navigation'\n\nexport function OrderDetail({ id }: { id: string }) {\n  const router = useRouter()\n  const { data, status, refetch } = useQuery({\n    queryKey: ['order', id],\n    queryFn: () => fetchOrder(id), // resolves Order | null\n  })\n\n  if (status === 'pending') return <DetailSkeleton />        // loading\n  if (status === 'error')\n    return <ErrorState onRetry={() => refetch()} />          // error\n  if (data === null)\n    return <EmptyState message={`Order ${id} not found`} />  // empty / 404\n\n  return (\n    <article aria-labelledby=\"order-title\">                 {/* success */}\n      <h1 id=\"order-title\">Order {data.number}</h1>\n      <OrderSummary order={data} />\n      <button onClick={() => router.push('/orders')}>Back to orders</button>\n    </article>\n  )\n}",
      "steps": [
        {
          "lines": [
            9
          ],
          "label": "Identity comes from the URL",
          "note": "id is a route param passed in from app/orders/[id]/page.tsx — not a useState set by a row click. This single choice is what makes the surface deep-linkable and refresh-proof."
        },
        {
          "lines": [
            10,
            11,
            12,
            13
          ],
          "label": "Fetch keyed by id",
          "note": "The query key includes id, so navigating to a different order refetches and caches per-order. queryFn resolves Order | null so 'not found' is data, not an exception."
        },
        {
          "lines": [
            15
          ],
          "label": "Loading is a state, not a blank",
          "note": "Render a skeleton while pending. A blank screen on slow networks reads as broken; a skeleton reads as loading."
        },
        {
          "lines": [
            16,
            17
          ],
          "label": "Error is recoverable",
          "note": "A failed fetch gets its own branch with a retry, not a thrown error that blows up the whole route. refetch() lets the user recover without losing the URL."
        },
        {
          "lines": [
            18,
            19
          ],
          "label": "Empty is distinct from error",
          "note": "data === null means the id is valid but has no record (a 404). Showing the error UI here would lie about what happened — empty and error are different states."
        },
        {
          "lines": [
            21,
            22,
            26
          ],
          "label": "Success names itself for assistive tech",
          "note": "aria-labelledby ties the region to its heading, and 'Back to orders' router.push keeps navigation in the URL so the back button behaves."
        }
      ],
      "caption": "Swap <article> for a focus-trapping <Dialog> and the same component is a drawer — the four states and the URL-driven id are identical. Route vs drawer is presentation; the state matrix is the contract."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 grounding the pros internalize: a drawer is a modal, so it must trap focus, restore focus to the triggering row on close, close on Escape, and carry aria-modal / role=dialog. A routed detail page doesn't trap focus — it moves focus to the new <h1>. Picking route vs drawer silently picks which accessibility contract you owe."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: 'Prove this detail surface isn't just a happy-path answer.' Which evidence actually proves it?",
      "options": [
        "A screenshot of the success state with a fully rendered order.",
        "Opening the detail URL in a fresh tab (deep link), then forcing loading, a 404 id, and a network failure — showing all four states render and that refresh keeps you on the same item.",
        "A note saying 'I used a drawer component from the design system.'",
        "A passing TypeScript build with no type errors on the component."
      ],
      "answer": 1,
      "explanation": "The reviewer challenge is specifically about evidence beyond the happy path. Only option 2 exercises the broken cases the lesson exists to cover — empty (404 id), error (network failure), loading — and proves the URL-as-source-of-truth invariant by deep-linking in a fresh tab and surviving refresh. A success screenshot (1), a component choice (3), and a clean compile (4) all say nothing about empty, error, or shareability."
    },
    {
      "type": "verification",
      "intro": "Prove your detail surface holds — no vibes:",
      "items": [
        "Open the detail URL in a brand-new tab. If you land on the right item with no list context, the identity is in the URL; if you land on a blank or the wrong item, it isn't — move it to the route or a search param.",
        "Force each state: a slow network (loading skeleton, not blank), an id that doesn't exist (empty/404, not error), and a failing request (error with a working retry). All four must be reachable.",
        "If it's a drawer: Tab through it — focus stays inside, Escape closes it, and on close focus returns to the row that opened it.",
        "Hit the browser back button from the detail. It should return to the list and the previous scroll/selection, not dump you out of the app.",
        "Confirm the route-vs-drawer choice is written down with its reversal condition (e.g. 'drawer now; promote to a route if users start sharing links to it')."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does putting the selected item's id in the URL (instead of useState) make refresh and deep links work for free?",
        "Explain the difference between the empty state and the error state of a detail view, using your own surface as the example. Why does collapsing them into one screen lie to the user?",
        "When would you choose a drawer over a routed detail page, and which accessibility obligations does that choice add?"
      ]
    },
    {
      "type": "compare",
      "title": "Two order-detail surfaces, same data",
      "subtitle": "Both render the order. Only one survives a teammate pasting the link.",
      "left": {
        "label": "In-memory drawer",
        "tone": "warning",
        "lines": [
          "open + selectedId held in useState",
          "Refresh closes it; deep link shows blank",
          "Only the success state exists",
          "404 id throws; network error white-screens",
          "Drawer doesn't trap focus or restore it",
          "Back button exits the app, not the drawer"
        ],
        "verdict": "A demo — works only for the person who clicked"
      },
      "right": {
        "label": "URL-driven detail surface",
        "tone": "success",
        "lines": [
          "id comes from the route / search param",
          "Refresh + fresh-tab deep link land on the item",
          "loading / empty / error / success all render",
          "404 id → empty state; failure → error + retry",
          "Drawer traps focus, Escape closes, focus restored",
          "Back returns to the list with its scroll/selection"
        ],
        "verdict": "Shareable, refresh-proof, reviewable"
      },
      "caption": "The whole gap is one decision: is the identity of what's open in the URL, or in a component variable that dies on refresh?"
    },
    {
      "type": "transfer",
      "text": "Same pattern, any list-to-detail surface: a user profile drawer over a directory, a ticket modal over a Kanban board, a property page over a map of search results. What stays invariant is the URL owning the selected id and the surface covering all four states; what changes is presentation (drawer vs route) and which accessibility contract that presentation owes. Next this exact component plugs into the capstone's fullstack product dashboard — its state matrix and keyboard checklist become the browser-proof + accessibility evidence the dashboard ships with, so build the matrix to be handed off, not thrown away."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "action-safety-optimistic-ui": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real user-facing mutation (like, follow, add-to-cart, delete) and make it feel instant AND safe: apply the change optimistically, snapshot the previous state, send the request, and roll back with a visible, accessible error if it fails. Write the artifact that proves you covered every state.",
      "intensity": "standard",
      "time": "25–30 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a state matrix (idle · pending · success · error · disabled · empty) for one mutation, the optimistic-apply + snapshot + rollback path, an idempotency key on the request, and an accessibility line per state (aria-busy, aria-live announcement, focus retention). A reviewer can point to the exact line where a failed request restores the prior UI.",
      "unlock": "Your mutation applies optimistically, snapshots prior state, rolls back on failure with an announced error, and the request is idempotent — all six states are named in the matrix.",
      "doNotClaim": "Do not claim 'optimistic UI' if your code only updates the screen and never rolls back. An optimistic update with no snapshot and no rollback is not optimistic — it is a lie the UI tells the user until the next refresh contradicts it."
    },
    {
      "type": "mission",
      "text": "A user taps Follow, the button flips instantly, they scroll on — and the request silently 500s. They think they followed. They didn't. Three weeks later they wonder why they never see that creator's posts. The button felt fast and was wrong, which is worse than slow and honest."
    },
    {
      "type": "context",
      "text": "This lesson sits in the production-pressure phase of frontend engineering: the network is real, slow, and fails. Optimistic UI buys perceived speed by updating before the server confirms — but that speed is only safe if you can take it back, and only humane if every transition (pending, error, rollback) is visible to a screen reader and to the eye."
    },
    {
      "type": "pretest",
      "prompt": "You apply an optimistic update, fire the request, and it fails. You have the new state on screen. What is the ONE thing you needed to capture BEFORE you mutated, and why is it almost always forgotten? Answer before reading on.",
      "reveal": "The previous state — a snapshot taken before the optimistic write. It's forgotten because the happy path never needs it: when the request succeeds you just keep the new value. The snapshot only earns its keep on failure, so it's the first thing cut under deadline. Without it, rollback has nothing to roll back TO, and you're left guessing the old value (or worse, refetching and flashing the user). The invariant: snapshot before you mutate, restore on reject."
    },
    {
      "type": "concept",
      "title": "Optimistic UI = snapshot → apply → request → reconcile (commit or roll back), announced at every state",
      "text": "Before mutating, snapshot the current state. Apply the change immediately. Send an idempotent request. On success, commit. On failure, restore the snapshot and announce the error via aria-live. Speed without a rollback path is not optimism — it is unverified hope."
    },
    {
      "type": "diagram",
      "title": "The optimistic mutation lifecycle — happy path and rollback path",
      "subtitle": "One user action. The snapshot is taken first; success commits; failure restores the snapshot and announces the error. The dashed edge is the path beginners skip — and the one that makes it SAFE.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "click",
          "label": "User action",
          "description": "tap Follow / Like / Delete",
          "kind": "client"
        },
        {
          "id": "snapshot",
          "label": "Snapshot prior state",
          "description": "capture old value BEFORE mutating",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "apply",
          "label": "Apply optimistically",
          "description": "UI flips now · aria-busy=true",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "request",
          "label": "Send request",
          "description": "POST + Idempotency-Key",
          "kind": "external"
        },
        {
          "id": "commit",
          "label": "Commit",
          "description": "2xx · keep new state · clear busy",
          "kind": "process",
          "tone": "success"
        },
        {
          "id": "rollback",
          "label": "Roll back",
          "description": "restore snapshot · announce error",
          "kind": "decision",
          "tone": "warning"
        },
        {
          "id": "announce",
          "label": "Announce",
          "description": "aria-live region · keep focus",
          "kind": "process",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "click",
          "to": "snapshot",
          "label": "first",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "snapshot",
          "to": "apply",
          "label": "then mutate",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "apply",
          "to": "request",
          "label": "fire",
          "kind": "async"
        },
        {
          "from": "request",
          "to": "commit",
          "label": "2xx",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "request",
          "to": "rollback",
          "label": "4xx / 5xx / timeout",
          "kind": "data",
          "tone": "warning",
          "dashed": true
        },
        {
          "from": "rollback",
          "to": "announce",
          "label": "restore + tell user",
          "kind": "control",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "snapshot + optimistic apply (the speed)"
        },
        {
          "tone": "success",
          "label": "commit — server agreed"
        },
        {
          "tone": "warning",
          "label": "rollback path — the safety beginners skip"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Building the safe optimistic mutation, line by line",
      "subtitle": "A useMutation 'Follow' handler — snapshot, apply, idempotent request, rollback, announce. This is the artifact's reference implementation.",
      "filename": "useToggleFollow.ts",
      "language": "ts",
      "code": "export function useToggleFollow(userId: string) {\n  const qc = useQueryClient()\n  const announce = useLiveAnnounce() // writes into an aria-live region\n\n  return useMutation({\n    mutationFn: (next: boolean) =>\n      api.post(`/users/${userId}/follow`, { following: next }, {\n        headers: { 'Idempotency-Key': `follow:${userId}:${next}` },\n      }),\n\n    onMutate: async (next) => {\n      await qc.cancelQueries({ queryKey: ['user', userId] })\n      const prev = qc.getQueryData<User>(['user', userId])       // 1. snapshot\n      qc.setQueryData<User>(['user', userId], (u) =>             // 2. apply now\n        u ? { ...u, isFollowing: next } : u)\n      return { prev }                                            // 3. hand snapshot to onError\n    },\n\n    onError: (_err, _next, ctx) => {\n      if (ctx?.prev) qc.setQueryData(['user', userId], ctx.prev) // 4. roll back\n      announce('Could not update follow. Reverted — try again.') // 5. tell the user\n    },\n\n    onSettled: () => qc.invalidateQueries({ queryKey: ['user', userId] }), // 6. reconcile w/ truth\n  })\n}",
      "steps": [
        {
          "lines": [
            6,
            7,
            8,
            9
          ],
          "label": "Idempotent request",
          "note": "The Idempotency-Key means a retried or double-fired request applies once. Without it, a flaky network turns one tap into two follows."
        },
        {
          "lines": [
            12
          ],
          "label": "Cancel in-flight reads",
          "note": "Stop any running refetch first, or a stale server response could clobber your optimistic value mid-flight."
        },
        {
          "lines": [
            13
          ],
          "label": "Snapshot BEFORE mutating",
          "note": "prev is the whole reason rollback can exist. Capture it before line 14 changes anything."
        },
        {
          "lines": [
            14,
            15
          ],
          "label": "Apply optimistically",
          "note": "Immutable update — new object, never mutate the cached user. The button flips this frame; aria-busy goes true in the component."
        },
        {
          "lines": [
            16
          ],
          "label": "Hand the snapshot forward",
          "note": "Returned context becomes ctx in onError. This is the thread that connects 'before' to 'undo'."
        },
        {
          "lines": [
            19,
            20
          ],
          "label": "Roll back on failure",
          "note": "Restore the exact prior value — no guessing, no flash. The one line a reviewer should be able to point to."
        },
        {
          "lines": [
            21
          ],
          "label": "Announce, accessibly",
          "note": "A silent revert is a second bug. aria-live tells screen-reader users the state changed back; focus stays on the button."
        },
        {
          "lines": [
            24
          ],
          "label": "Reconcile with the server",
          "note": "Win or lose, refetch so the UI ends on confirmed truth, not on optimism."
        }
      ],
      "caption": "Snapshot (13) → apply (14) → idempotent send (6–9) → rollback (20) → announce (21) → reconcile (24). Every state the matrix names has a home here."
    },
    {
      "type": "compare",
      "title": "Fast-and-wrong vs fast-and-safe",
      "subtitle": "Same Follow button. Both flip instantly. Only one survives a failed request.",
      "left": {
        "label": "Optimism without a net",
        "tone": "warning",
        "lines": [
          "setIsFollowing(true) — UI flips, done",
          "No snapshot of the prior value",
          "Request fails → UI still says 'Following'",
          "No aria-busy, no aria-live announcement",
          "User believes a thing that did not happen"
        ],
        "verdict": "Feels instant, lies on failure"
      },
      "right": {
        "label": "Optimistic AND safe",
        "tone": "success",
        "lines": [
          "Snapshot prev, then apply optimistically",
          "Idempotency-Key so retries apply once",
          "On failure: restore snapshot exactly",
          "aria-busy during, aria-live on revert",
          "Reconcile via invalidate on settle"
        ],
        "verdict": "Feels instant, stays truthful"
      },
      "caption": "The teaching contrast is the rollback path: a state matrix that names 'error' but has no restore line is a matrix with a hole exactly where production fails."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 grounding: a state change the user can SEE must also be perceivable without sight (success criterion 4.1.3, Status Messages). A failed mutation that silently reverts passes your eye test and fails a screen-reader user — wrap revert/error text in a role=\"status\" or aria-live=\"polite\" region. The senior tell: optimistic UI is judged on its error path, not its happy path."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: 'Prove this optimistic update is safe, not just fast.' Which evidence actually proves it?",
      "options": [
        "A screen recording of the button flipping instantly on a fast network.",
        "A test that forces the request to fail and asserts the UI restores the exact prior state AND announces the revert via an aria-live region.",
        "The fact that it uses a popular data-fetching library with optimistic support.",
        "A comment in the code that says // optimistic update."
      ],
      "answer": 1,
      "explanation": "Safety lives on the failure path, so the proof must exercise failure: force a reject, assert the snapshot is restored (no guessing, no flash) and that the revert is announced accessibly. The happy-path recording (1) shows speed, not safety; using a library (3) or a comment (4) proves intent, not behavior. The reviewer challenge is answered only by demonstrating rollback + announcement under a forced failure."
    },
    {
      "type": "verification",
      "intro": "Prove the mutation is fast AND safe — force the failure, do not assume it:",
      "items": [
        "Throttle the network (or mock a 500) and confirm the optimistic change applies immediately, before the request resolves.",
        "Force the request to reject and confirm the UI restores the EXACT prior value — not a refetch flash, not a guess.",
        "Confirm aria-busy is true during the pending state and an aria-live region announces the error on rollback.",
        "Double-fire the action (rapid taps) and confirm the Idempotency-Key makes the server apply it once.",
        "Tab to the control, trigger it by keyboard, and confirm focus stays on the control through pending → error → rollback (no focus loss)."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why is an optimistic update without a snapshot not actually optimistic — what can it not do?",
        "Walk a junior through your error path: what gets restored, and how does a screen-reader user learn the action was reverted?",
        "Why does the request need an idempotency key the moment you add retries or let users tap fast?"
      ]
    },
    {
      "type": "transfer",
      "text": "The pattern is mutation-shaped, not Follow-shaped. A drag-to-reorder list snapshots the order, reorders on drop, and snaps back if the save fails. A collaborative doc applies your edit locally and reconciles against the server's version, rolling back on conflict. An offline-first app queues the optimistic write and replays it idempotently when connectivity returns. What stays invariant: snapshot before apply, idempotent request, restore-and-announce on failure, reconcile on settle. What changes with context: how loud the rollback is (a silent retry for a like, a blocking dialog for a payment), and how long you tolerate the optimistic state before forcing reconciliation. Carry component_state_matrix_and_accessibility_packet.md into the capstone — the dashboard reuses this exact matrix and rollback proof for its action surfaces."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "auth-protected-routes": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real route that must require sign-in (e.g. /dashboard) and gate it: a server-side session check that redirects unauthenticated users, plus the four auth UI states wired (loading, unauthenticated, error, authorized). Capture it in component_state_matrix_and_accessibility_packet.md.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "component_state_matrix_and_accessibility_packet.md with: the state matrix (loading / unauthenticated / error / authorized → what renders + redirect), the server-side gate code, a Playwright flow that hits the route logged-out and asserts the redirect, and a keyboard/screen-reader note for the loading and redirect states. A reviewer logs out, hits the URL, and lands on /login — not a flash of private data.",
      "unlock": "Your gate enforces auth on the SERVER before private data renders, redirects unauthenticated users, and the matrix covers all four states with accessibility evidence.",
      "doNotClaim": "Do not claim a route is 'protected' if the guard is client-side only (a useEffect redirect). The private data already shipped to the browser and flashed on screen before the redirect ran — that is a leak, not a guard. Protection happens server-side, before render."
    },
    {
      "type": "mission",
      "text": "A teammate ships /dashboard behind a slick client-side redirect. It demos perfectly — until someone opens the URL logged out, sees another user's revenue numbers flash for 200ms before the redirect fires, and screenshots it. The data was never protected; it was hidden a beat too late."
    },
    {
      "type": "context",
      "text": "Auth protected routes sit in the production-pressure phase of frontend/fullstack work: the moment a UI stops being public and starts guarding real user data. Get the gate's LAYER wrong and every accessibility, state, and API lesson before it leaks through the hole."
    },
    {
      "type": "pretest",
      "prompt": "Your dashboard redirects unauthenticated users with a useEffect that runs on mount. Before you call it protected — what's wrong with guarding the route this way? Answer before reading on.",
      "reveal": "The redirect runs AFTER the component renders. By the time useEffect fires, the server already sent the private HTML and the browser painted it — the unauthorized user saw the data and could screenshot or curl it. A client redirect is UX, not security. Real protection refuses to render private content on the server until the session is verified, so unauthorized requests get a redirect, never the data."
    },
    {
      "type": "concept",
      "title": "Gate on the server, before render — the client redirect is only polish",
      "text": "Verify the session server-side (middleware or a Server Component) and redirect unauthenticated requests before any private data is sent. The invariant: private bytes never reach an unauthorized browser. Client-side guards improve UX; they never replace the server gate."
    },
    {
      "type": "diagram",
      "title": "Two gates for one request: middleware, then the page",
      "subtitle": "A request to /dashboard hits middleware first (no session → redirect at the edge), then the Server Component re-checks before fetching private data. Private bytes only flow on the authorized path; login and public routes are out of scope.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Browser",
          "description": "GET /dashboard",
          "kind": "client"
        },
        {
          "id": "mw",
          "label": "Middleware",
          "description": "read session cookie",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "login",
          "label": "/login",
          "description": "redirect target",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "page",
          "label": "Server Component",
          "description": "re-verify session",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "db",
          "label": "Private data",
          "description": "user-scoped query",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "html",
          "label": "Rendered page",
          "description": "authorized HTML only",
          "kind": "process",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "mw",
          "label": "request",
          "kind": "sync"
        },
        {
          "from": "mw",
          "to": "login",
          "label": "no session → 307",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "mw",
          "to": "page",
          "label": "session present",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "page",
          "to": "login",
          "label": "stale/invalid → redirect",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "page",
          "to": "db",
          "label": "fetch (user-scoped)",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "db",
          "to": "html",
          "label": "render",
          "kind": "data",
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "session checks (edge + page)"
        },
        {
          "tone": "warning",
          "label": "redirect path — no private bytes"
        },
        {
          "tone": "success",
          "label": "authorized path — private data flows here only"
        }
      ],
      "caption": "Defense in depth: middleware redirects cheaply at the edge; the page re-verifies because a cookie's presence is not proof it is valid. The browser never receives private HTML on the redirect path."
    },
    {
      "type": "code-walkthrough",
      "title": "Gating /dashboard in Next.js App Router",
      "subtitle": "Middleware redirects unauthenticated requests; the Server Component re-checks and renders private data only when authorized.",
      "filename": "app/dashboard/page.tsx + middleware.ts",
      "language": "ts",
      "code": "// middleware.ts — runs at the edge, before the route renders\nimport { NextResponse, type NextRequest } from 'next/server'\nimport { getSession } from '@/lib/auth'\n\nexport async function middleware(req: NextRequest) {\n  const session = await getSession(req)\n  if (!session) {\n    const url = new URL('/login', req.url)\n    url.searchParams.set('next', req.nextUrl.pathname)\n    return NextResponse.redirect(url) // 307 — private page never renders\n  }\n  return NextResponse.next()\n}\n\nexport const config = { matcher: ['/dashboard/:path*'] }\n\n// app/dashboard/page.tsx — Server Component, re-verifies before any fetch\nimport { redirect } from 'next/navigation'\nimport { getSession } from '@/lib/auth'\nimport { getUserDashboard } from '@/lib/data'\n\nexport default async function DashboardPage() {\n  const session = await getSession()\n  if (!session) redirect('/login') // belt-and-suspenders: cookie present != valid\n\n  const data = await getUserDashboard(session.userId) // scoped to THIS user\n  return <Dashboard data={data} />\n}",
      "steps": [
        {
          "lines": [
            5,
            6
          ],
          "label": "Read the session at the edge",
          "note": "Middleware runs before the route module loads. getSession verifies the cookie/JWT — presence alone is not trust."
        },
        {
          "lines": [
            7,
            8,
            9,
            10,
            11
          ],
          "label": "No session → redirect, carry intent",
          "note": "307 redirect to /login with ?next=/dashboard so the user lands back where they wanted after sign-in. The private page never renders."
        },
        {
          "lines": [
            15
          ],
          "label": "Scope the matcher",
          "note": "The gate applies to /dashboard and its children only — public routes stay public. An over-broad matcher locks out /login itself."
        },
        {
          "lines": [
            23,
            24
          ],
          "label": "Re-verify inside the page",
          "note": "Defense in depth: a request can reach the Server Component with a stale or forged cookie. Re-check before touching data."
        },
        {
          "lines": [
            26
          ],
          "label": "Scope the query to the user",
          "note": "getUserDashboard(session.userId) — authorization, not just authentication. A valid session for user A must never return user B's data (IDOR)."
        }
      ],
      "caption": "Two server-side checks, then a user-scoped query. The unauthenticated request gets a redirect at line 9 and never sees the data fetched at line 26."
    },
    {
      "type": "compare",
      "title": "Client guard vs server gate",
      "subtitle": "Same /dashboard route. Both 'redirect' logged-out users. Only one actually protects the data.",
      "left": {
        "label": "Client-side useEffect redirect",
        "tone": "warning",
        "lines": [
          "'use client' page renders immediately",
          "Server already sent private HTML to the browser",
          "useEffect(() => { if (!user) router.push('/login') })",
          "Private data flashes 100-300ms before redirect",
          "curl with no cookie still returns the full payload"
        ],
        "verdict": "Hides data late — a leak, not a guard"
      },
      "right": {
        "label": "Server gate (middleware + RSC)",
        "tone": "success",
        "lines": [
          "Middleware checks session before the route renders",
          "No session → 307 redirect, zero private bytes sent",
          "Server Component re-verifies before any data fetch",
          "Query scoped to session.userId (no IDOR)",
          "curl with no cookie gets the redirect, never the data"
        ],
        "verdict": "Refuses to render — the data never leaves"
      },
      "caption": "The test that separates them: hit the route with curl and no cookie. The client guard hands you the private JSON; the server gate hands you a redirect. A loading spinner over private HTML is still a leak underneath."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell: they verify the session in TWO places — the edge (cheap redirect) and the page (re-check before the data fetch) — because a cookie reaching your Server Component proves it exists, not that it's valid. And they scope every query to session.userId: authentication asks 'who are you?', authorization asks 'is this YOUR row?'. Skipping the second is the IDOR bug that leaks one user's data to another even when both are logged in. WCAG 2.2 closes the loop: a route transition (the redirect, and the protected page mounting) must move focus and announce, or a screen-reader user is silently teleported."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Prove this route is actually protected, not just happy-path.' Which evidence settles it?",
      "options": [
        "A screenshot of the dashboard loading correctly while signed in.",
        "A Playwright test that, with NO session cookie, requests /dashboard and asserts the response redirects to /login and the private data never appears in the DOM — plus a curl showing the same.",
        "The useEffect redirect runs and the URL changes to /login after a moment.",
        "The login form validates the password before submitting."
      ],
      "answer": 1,
      "explanation": "Protection is proven by the unauthorized path, server-side. Only option 2 exercises the route with no session and asserts the private data is never delivered (DOM + curl) — the falsifiable check. The happy-path screenshot (1) never tests the gate; the useEffect (3) is exactly the late-redirect leak; password validation (4) is a different concern entirely."
    },
    {
      "type": "verification",
      "intro": "Prove the route is protected — no vibes:",
      "items": [
        "Run `curl -i https://app/dashboard` with NO cookie. You must get a 307/302 to /login, and the body must NOT contain any private data. If the payload comes back, the gate is client-side — fix it.",
        "Open the route logged out in a real browser: there must be ZERO flash of private content before the redirect. A flash means render happened before the check.",
        "Confirm the data query is scoped to session.userId — try a session for user A and verify it cannot return user B's rows (the IDOR check).",
        "Tab through the loading and redirect: focus moves to a sensible landmark and the state change is announced (aria-live or focus management), not a silent teleport.",
        "Your state matrix names all four states — loading, unauthenticated (→ redirect), error (session fetch fails), authorized — and says exactly what renders in each."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does a client-side useEffect redirect leak the data even though the user ends up on /login?",
        "Explain the difference between authentication and authorization using your own /dashboard query as the example — what does scoping to session.userId prevent?",
        "Walk the four auth states in your matrix (loading, unauthenticated, error, authorized) and name what the screen-reader user experiences in each."
      ]
    },
    {
      "type": "transfer",
      "text": "The same gate-on-the-server pattern travels: an API route guards before reading the body, not after; a GraphQL resolver checks the viewer before resolving a field; an admin-only page layers a ROLE check on top of the session check (authenticated AND role === 'admin'). What stays invariant is the rule — never render or return private data before verifying the requester, and always scope the query to them. What changes is the surface (page, API, resolver), the granularity (route vs field vs row), and the redirect-vs-403 response. Carry component_state_matrix_and_accessibility_packet.md into the capstone: its state matrix and Playwright redirect flow are the reusable proof that your protected surface holds under the unauthorized path, not just the demo."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "accessibility-keyboard": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real interactive surface you built — a custom dropdown, modal, tab set, or icon button — and make it fully keyboard-operable: reachable by Tab, operable by Enter/Space/Escape/Arrows, with a visible focus ring and no trap. Then write it up in component_state_matrix_and_accessibility_packet.md.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "Section in component_state_matrix_and_accessibility_packet.md: a keyboard checklist (Tab order, focus visibility, key bindings, trap behavior) plus a Playwright flow that drives the component with the keyboard only — no mouse — and asserts focus lands where it should. A reviewer unplugs the mouse and completes the flow.",
      "unlock": "Your component is reachable, operable, and escapable by keyboard alone, with a visible focus indicator and a passing keyboard-only Playwright flow.",
      "doNotClaim": "Do not claim a component is 'accessible' because a screen reader announces it. Keyboard operability is a separate axis: a div with role and aria-label that you cannot Tab to or activate with Enter is still broken. Announce-able is not operable."
    },
    {
      "type": "mission",
      "text": "The demo is flawless — until a reviewer pushes the mouse aside and tries to Tab through it. Your custom dropdown swallows the focus, Escape does nothing, and the keyboard user is trapped on a screen they can see but cannot leave."
    },
    {
      "type": "context",
      "text": "A user-facing surface that only works with a mouse fails everyone who navigates by keyboard — power users, motor-impaired users, screen-reader users — and silently fails a WCAG audit. Keyboard access is the section of your accessibility packet that proves the component is operable, not just announce-able."
    },
    {
      "type": "pretest",
      "prompt": "You swap a <button> for a styled <div onClick={...}> to get pixel-perfect control. Visually identical, click works. What did you just break — before you read on?",
      "reveal": "Three things, invisibly. A <div> is not in the tab order (no Tab focus), it has no implicit role (a screen reader calls it 'group', not 'button'), and it ignores Enter/Space (those keys only fire native button semantics). You traded one CSS win for a control no keyboard user can reach or operate. The native <button> gave you focusability, role, and keyboard activation for free — you paid all three back to delete a border-radius."
    },
    {
      "type": "concept",
      "title": "Keyboard access = focusable + operable + escapable",
      "text": "Every interactive control must be reachable in a logical Tab order, operable by its expected keys (Enter/Space to activate, Arrows within a widget, Escape to dismiss), and never a trap. Prefer native elements; they grant all three by default."
    },
    {
      "type": "diagram",
      "title": "The keyboard interaction model of a modal dialog",
      "subtitle": "Tab opens the dialog, focus moves IN and is contained, Escape returns focus to the trigger. The failure path (no focus move, leaked Tab, dead Escape) is marked in warning; the repair path in success.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "trigger",
          "label": "Trigger button",
          "description": "native <button>, in tab order",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "open",
          "label": "Dialog opens",
          "description": "focus moves to first control INSIDE",
          "kind": "process",
          "tone": "success"
        },
        {
          "id": "trap",
          "label": "Focus trap",
          "description": "Tab cycles within dialog only",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "escape",
          "label": "Escape / Close",
          "description": "dismiss + RESTORE focus to trigger",
          "kind": "decision",
          "tone": "success"
        },
        {
          "id": "leak",
          "label": "Focus leaks to page",
          "description": "Tab escapes to content behind",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "lost",
          "label": "Focus lost to <body>",
          "description": "Escape closes, focus vanishes",
          "kind": "store",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "trigger",
          "to": "open",
          "label": "Enter / Space",
          "kind": "sync",
          "tone": "success"
        },
        {
          "from": "open",
          "to": "trap",
          "label": "contain focus",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "trap",
          "to": "escape",
          "label": "Escape pressed",
          "kind": "control"
        },
        {
          "from": "escape",
          "to": "trigger",
          "label": "restore focus",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "trap",
          "to": "leak",
          "label": "no trap → Tab leaks",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "escape",
          "to": "lost",
          "label": "no restore → focus to body",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "native control + contained focus"
        },
        {
          "tone": "success",
          "label": "correct path (move in, restore out)"
        },
        {
          "tone": "warning",
          "label": "failure path (leak / lost focus)"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Making a custom dropdown keyboard-operable",
      "subtitle": "From a click-only <div> to a control that handles Enter, Space, Arrows, and Escape — the artifact from the Build.",
      "filename": "AccessibleDropdown.tsx",
      "language": "ts",
      "code": "function AccessibleDropdown({ options, onSelect }: DropdownProps) {\n  const [open, setOpen] = useState(false)\n  const [active, setActive] = useState(0)\n  const triggerRef = useRef<HTMLButtonElement>(null)\n\n  function onKeyDown(e: React.KeyboardEvent) {\n    if (e.key === 'Enter' || e.key === ' ') {\n      e.preventDefault()\n      open ? onSelect(options[active]) : setOpen(true)\n    } else if (e.key === 'ArrowDown') {\n      e.preventDefault()\n      setActive((i) => Math.min(i + 1, options.length - 1))\n    } else if (e.key === 'ArrowUp') {\n      e.preventDefault()\n      setActive((i) => Math.max(i - 1, 0))\n    } else if (e.key === 'Escape') {\n      setOpen(false)\n      triggerRef.current?.focus()\n    }\n  }\n\n  return (\n    <button\n      ref={triggerRef}\n      aria-haspopup=\"listbox\"\n      aria-expanded={open}\n      onKeyDown={onKeyDown}\n      onClick={() => setOpen((o) => !o)}\n      className=\"dropdown-trigger\"\n    >\n      {options[active]?.label ?? 'Select…'}\n    </button>\n  )\n}",
      "steps": [
        {
          "lines": [
            19,
            20,
            21
          ],
          "label": "Start from a native <button>",
          "note": "Not a <div>. This buys you tab-order placement, the button role, and a visible focus ring for free — the three things the pretest broke."
        },
        {
          "lines": [
            8,
            9,
            10,
            11
          ],
          "label": "Enter and Space activate",
          "note": "Native buttons fire onClick for these keys, but once you add a custom listbox you must handle them explicitly. preventDefault stops Space from scrolling the page."
        },
        {
          "lines": [
            12,
            13,
            14,
            15,
            16,
            17,
            18
          ],
          "label": "Arrows move WITHIN the widget",
          "note": "Arrow keys navigate options without leaving the control. clamping with Math.min/Math.max keeps the active index in range — no wrap-around surprises."
        },
        {
          "lines": [
            19,
            20,
            21
          ],
          "label": "Escape dismisses AND restores focus",
          "note": "The critical line: triggerRef.current?.focus() returns focus to the trigger. Without it, Escape closes the menu and dumps focus on <body> — the keyboard user is stranded."
        },
        {
          "lines": [
            22,
            23
          ],
          "label": "aria-expanded reflects state",
          "note": "aria-haspopup + aria-expanded tell assistive tech the button opens a listbox and whether it is open. Operability and announcement, wired together."
        }
      ],
      "caption": "Unplug the mouse: Tab to it, Space to open, Arrows to choose, Enter to select, Escape to leave — and focus lands back on the trigger every time."
    },
    {
      "type": "compare",
      "title": "Same dropdown, two implementations",
      "subtitle": "Both render identically. Only one survives a keyboard-only pass.",
      "left": {
        "label": "Click-only div",
        "tone": "warning",
        "lines": [
          "<div onClick> — not in the tab order",
          "Cannot Tab to it; mouse is the only way in",
          "Enter and Space do nothing",
          "No Escape handler — open menu is a trap",
          "Screen reader announces 'group', not 'button'",
          "No visible focus ring (divs get none)"
        ],
        "verdict": "Fails keyboard, role, and focus — three WCAG criteria"
      },
      "right": {
        "label": "Native button + key handlers",
        "tone": "success",
        "lines": [
          "<button> — focusable, in logical tab order",
          "Enter / Space activate; Arrows navigate options",
          "Escape closes and restores focus to the trigger",
          "aria-haspopup + aria-expanded announce state",
          "Visible focus ring inherited from native control",
          "Verified by a mouse-free Playwright flow"
        ],
        "verdict": "Reachable, operable, escapable — and proven"
      },
      "caption": "The fix is rarely more code — it is the right base element plus the keys a real user expects. Native first; ARIA only to fill the gap."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 names this directly: 2.1.1 Keyboard (every function reachable by keyboard), 2.1.2 No Keyboard Trap (you can always Tab back out), and 2.4.7 Focus Visible (you can always see where focus is). The senior tell is checking 2.1.2 — close your menu with Escape and confirm focus went somewhere sane, not to <body>. A trap passes the happy-path demo and fails the audit."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this dropdown is keyboard-accessible, not just a happy-path mouse demo.' Which response actually proves it?",
      "options": [
        "'A screen reader announces the options correctly when I hover them.'",
        "'A Playwright flow drives it with Tab, Arrow, Enter, and Escape — no mouse — and asserts focus returns to the trigger after Escape.'",
        "'It looks identical to a native dropdown and the click handler works.'",
        "'I added role=\"button\" and aria-label to the div, so assistive tech sees it.'"
      ],
      "answer": 1,
      "explanation": "Only option 2 produces inspectable proof of operability: a keyboard-only flow that exercises every binding and asserts the focus-restore (WCAG 2.1.2). Option 1 tests announcement, not operability. Option 3 tests the mouse path. Option 4 adds a role but a div is still not focusable or Enter/Space-operable — it announces as a button it cannot behave as."
    },
    {
      "type": "verification",
      "intro": "Prove the component is keyboard-operable — mouse unplugged:",
      "items": [
        "Tab to the control from the page top. If you cannot reach it without a mouse, it is not in the tab order — start from a native element.",
        "Activate it with Enter and with Space. Both must work for anything that presents as a button.",
        "Inside a composite widget (menu, tabs, listbox), Arrow keys move between items without leaving the widget.",
        "Press Escape on any open overlay: it must close AND return focus to the trigger, not drop focus on <body>.",
        "Confirm a visible focus indicator on every focusable element (WCAG 2.4.7) — Tab through and watch the ring move.",
        "Run the Playwright keyboard flow: it drives Tab/Arrow/Enter/Escape only and asserts focus position. Green = proven."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does swapping <button> for <div onClick> break keyboard access in three separate ways at once?",
        "Explain the difference between a component being announce-able (screen reader reads it) and operable (keyboard can use it) — using your own dropdown.",
        "Walk through what Escape must do on your overlay, and what specifically goes wrong if you close the overlay but forget to restore focus."
      ],
      "intro": ""
    },
    {
      "type": "transfer",
      "text": "The same three-part test — focusable, operable, escapable — governs every interactive surface, not just dropdowns. A modal must trap focus while open and release it on close; a tab set uses Arrow keys with a single tab-stop (roving tabindex); a date picker, a combobox, a carousel each have a documented keyboard contract in the WAI-ARIA Authoring Practices. What stays invariant: reach it by Tab, operate it by its expected keys, never trap the user. What changes by widget is only which keys and where focus moves. Carry the keyboard checklist and the mouse-free Playwright flow into component_state_matrix_and_accessibility_packet.md — they are the same proof your capstone dashboard reuses to certify every state (loading, empty, error, disabled, success) is operable without a mouse."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "responsive-visual-proof": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real component that has multiple states (loading, empty, error, disabled, success) and produce Responsive Visual Proof: a state × viewport matrix, a Playwright flow that walks every cell, a keyboard checklist, and named viewport-failure screenshots — packaged as component_state_matrix_and_accessibility_packet.md.",
      "intensity": "deep",
      "time": "30 min",
      "proof": "component_state_matrix_and_accessibility_packet.md attached to the evidence ledger: a state × viewport matrix (5 states × 4 breakpoints), a runnable Playwright flow that visits every cell, a keyboard-only checklist, and at least one annotated viewport-failure screenshot note with its repair. A reviewer can open the packet and SEE the component survive 320px and an error state without your narration.",
      "unlock": "Your packet shows every state at every breakpoint with browser-captured evidence — not a claim that 'it's responsive', but a matrix of proof a reviewer can re-run.",
      "doNotClaim": "Do not claim 'responsive' from one desktop screenshot of the happy path. A success state at 1440px is one cell of a 20-cell matrix. Until loading, empty, error, and disabled survive 320px AND keyboard, you have a demo, not proof."
    },
    {
      "type": "mission",
      "text": "The PR says 'responsive — see screenshot'. The reviewer opens it at 320px, tabs to the submit button, triggers the error state, and the error toast overflows the viewport while focus vanishes into a hidden node. One desktop screenshot hid four failures."
    },
    {
      "type": "context",
      "text": "A component is not one thing — it is a grid of states crossed with viewports, and bugs hide in the cells nobody screenshotted. Responsive Visual Proof turns 'I think it works' into a matrix of browser-captured evidence a reviewer can re-run, the artifact you carry into the capstone dashboard."
    },
    {
      "type": "pretest",
      "prompt": "You're asked to prove a checkout form is responsive. You attach a clean screenshot of the filled-in form on desktop. What did that screenshot NOT prove? Answer before reading on.",
      "reveal": "Almost everything that breaks in production. It proved one state (success) at one viewport (desktop) with a mouse. It says nothing about the empty state at 320px, the error boundary when the API 500s, the disabled state during submit, focus order under keyboard, or whether the error message overflows on a phone. 'Responsive' is a claim about a matrix; a single happy-path desktop shot proves one cell of it."
    },
    {
      "type": "concept",
      "title": "Proof is a state × viewport matrix walked by the browser",
      "text": "Enumerate the component's states (loading, empty, error, disabled, success) and breakpoints (320 · 768 · 1024 · 1440). Every cell is a claim. Responsive Visual Proof drives the real browser through each cell and captures evidence. The invariant: no cell unproven, no claim without a screenshot."
    },
    {
      "type": "diagram",
      "title": "The proof matrix and its two failure paths",
      "subtitle": "States down, viewports across. The happy-path cell (success @ 1440) is what gets screenshotted; the failure path lives in error @ 320 — overflow + lost focus — and the repair path drives every cell through the browser.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "component",
          "label": "Component",
          "description": "5 states × 4 viewports",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "states",
          "label": "State axis",
          "description": "loading · empty · error · disabled · success",
          "kind": "process"
        },
        {
          "id": "viewports",
          "label": "Viewport axis",
          "description": "320 · 768 · 1024 · 1440",
          "kind": "process"
        },
        {
          "id": "happy",
          "label": "success @ 1440",
          "description": "the cell most people screenshot",
          "kind": "store",
          "tone": "muted"
        },
        {
          "id": "failcell",
          "label": "error @ 320",
          "description": "toast overflows · focus lost",
          "kind": "store",
          "tone": "warning"
        },
        {
          "id": "runner",
          "label": "Playwright flow",
          "description": "drives every cell, captures evidence",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "packet",
          "label": "Proof packet",
          "description": "matrix · screenshots · keyboard checklist",
          "kind": "store",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "states",
          "to": "component",
          "label": "rows",
          "kind": "data"
        },
        {
          "from": "viewports",
          "to": "component",
          "label": "columns",
          "kind": "data"
        },
        {
          "from": "component",
          "to": "happy",
          "label": "easy to prove",
          "kind": "control",
          "tone": "muted"
        },
        {
          "from": "component",
          "to": "failcell",
          "label": "failure path",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "component",
          "to": "runner",
          "label": "walk all cells",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "runner",
          "to": "packet",
          "label": "captures evidence",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "failcell",
          "to": "packet",
          "label": "repair path: re-prove the cell",
          "kind": "async",
          "dashed": true,
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the component + the runner that proves it"
        },
        {
          "tone": "warning",
          "label": "failure path — the unproven cell that breaks"
        },
        {
          "tone": "success",
          "label": "the proof packet — re-runnable evidence"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Writing the proof: walk every cell, capture evidence",
      "subtitle": "A Playwright flow that drives the component through each state at each viewport and screenshots the matrix — the artifact's executable core.",
      "filename": "state-matrix.spec.ts",
      "language": "ts",
      "code": "import { test, expect } from '@playwright/test'\n\nconst VIEWPORTS = [320, 768, 1024, 1440]\nconst STATES = ['loading', 'empty', 'error', 'disabled', 'success'] as const\n\nfor (const width of VIEWPORTS) {\n  for (const state of STATES) {\n    test(`checkout · ${state} @ ${width}px`, async ({ page }) => {\n      await page.setViewportSize({ width, height: 900 })\n      await page.goto(`/checkout?state=${state}`)\n\n      // 1. State actually rendered (no silent fallthrough to success)\n      await expect(page.getByTestId(`state-${state}`)).toBeVisible()\n\n      // 2. No horizontal overflow — the 320px killer\n      const overflow = await page.evaluate(\n        () => document.documentElement.scrollWidth - document.documentElement.clientWidth,\n      )\n      expect(overflow, 'horizontal overflow').toBeLessThanOrEqual(0)\n\n      // 3. Keyboard reaches the primary control and focus stays visible\n      await page.keyboard.press('Tab')\n      await expect(page.getByRole('button', { name: /pay|retry/i })).toBeFocused()\n\n      // 4. Evidence: one screenshot per cell of the matrix\n      await page.screenshot({ path: `proof/checkout-${state}-${width}.png` })\n    })\n  }\n}",
      "steps": [
        {
          "lines": [
            3,
            4
          ],
          "label": "Declare both axes",
          "note": "Four breakpoints, five component states. The double loop generates all 20 matrix cells — no cell can be quietly skipped."
        },
        {
          "lines": [
            9
          ],
          "label": "Force the exact viewport",
          "note": "320px is the cell where most layouts break: text wraps, toasts overflow, touch targets collapse. Test it first, not last."
        },
        {
          "lines": [
            13
          ],
          "label": "Prove the state actually rendered",
          "note": "Guards against the silent fallthrough — a broken ?state=error that falls back to success and screenshots green. The state must be VISIBLE, not assumed."
        },
        {
          "lines": [
            16,
            19
          ],
          "label": "The horizontal-overflow assertion",
          "note": "scrollWidth > clientWidth means content spills past the viewport — the #1 responsive failure. This is the assertion a single screenshot can never make."
        },
        {
          "lines": [
            23,
            24
          ],
          "label": "Keyboard + focus check (WCAG 2.2)",
          "note": "Tab must land on the primary action and focus must be visible (WCAG 2.2 SC 2.4.7 / 2.4.11). Mouse-only proof hides every keyboard failure."
        },
        {
          "lines": [
            27
          ],
          "label": "Capture the cell as evidence",
          "note": "One screenshot per (state, viewport) — the visual proof. Named by cell so error-320 vs success-1440 are distinguishable in the packet."
        }
      ],
      "caption": "Run it: 20 cells, 20 screenshots, every state-at-viewport claim now has browser-captured evidence a reviewer can re-run."
    },
    {
      "type": "compare",
      "title": "A claim vs Responsive Visual Proof",
      "subtitle": "Same checkout component. One is a vibe; one is a matrix a reviewer can re-run.",
      "left": {
        "label": "\"It's responsive — see screenshot\"",
        "tone": "warning",
        "lines": [
          "One screenshot: success state, 1440px, mouse",
          "Loading / empty / error / disabled never shown",
          "320px never opened — overflow invisible",
          "No keyboard pass — focus order unproven",
          "No error boundary — 500 response untested",
          "Reviewer must trust the author's word"
        ],
        "verdict": "One cell of a 20-cell matrix — a demo"
      },
      "right": {
        "label": "component_state_matrix_and_accessibility_packet.md",
        "tone": "success",
        "lines": [
          "5 states × 4 viewports = 20 cells, each screenshotted",
          "Playwright flow walks every cell, re-runnable in CI",
          "Horizontal-overflow assertion at 320px (the killer)",
          "Keyboard checklist: Tab reaches + focus visible (WCAG 2.2)",
          "Error boundary triggered + captured, not assumed",
          "Annotated failure shot + the repair that fixed it"
        ],
        "verdict": "Every claim has evidence — proof, not trust"
      },
      "caption": "The gold version's value is the cells nobody wanted to look at: error @ 320 and disabled-under-keyboard are where production breaks."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 (SC 1.4.10 Reflow) requires content to work at 320px CSS width with no horizontal scrolling — that's not a nice-to-have, it's the line the law draws. The senior tell: they prove the error and empty states at the smallest viewport FIRST, because that's the cell that fails. Anyone can screenshot success at 1440."
    },
    {
      "type": "quiz",
      "question": "A reviewer asks: 'Show me the exact evidence that proves this component is responsive — not just the happy path.' Which response actually answers?",
      "options": [
        "A clean screenshot of the success state on a desktop monitor.",
        "The state × viewport matrix with a per-cell screenshot, a re-runnable Playwright flow that asserts no overflow at 320px, and a keyboard checklist — including the error and empty cells.",
        "A statement that the CSS uses flexbox and media queries, so it must be responsive.",
        "A Lighthouse score of 100 on the desktop page."
      ],
      "answer": 1,
      "explanation": "Only option 2 produces evidence for every cell of the claim: each state at each viewport, browser-captured, with the overflow and keyboard assertions a screenshot can't make — and crucially it includes the error and empty states, not just success. A desktop screenshot (1), a description of the CSS (3), and a single desktop Lighthouse run (4) all prove at most one happy-path cell."
    },
    {
      "type": "verification",
      "intro": "Prove your packet is real proof — no vibes:",
      "items": [
        "Every state (loading, empty, error, disabled, success) appears at every breakpoint (320, 768, 1024, 1440) — count the cells, not the screenshots you happened to take.",
        "The 320px cell of the error state has a screenshot AND a passing no-horizontal-overflow assertion. If the error toast overflows, that's a real failure to log, not a cell to skip.",
        "A keyboard-only pass reaches the primary action and focus is visible at each cell (WCAG 2.2 SC 2.4.7 / 2.4.11) — recorded as a checklist, not a memory.",
        "The error state was triggered by a real failure path (a mocked 500 or thrown error boundary), not faked by a ?state=error that silently renders success.",
        "The Playwright flow re-runs green from a clean checkout — a reviewer can reproduce your evidence without you, or it is your screenshot, not proof."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why is one desktop screenshot of the success state proof of almost nothing? Frame it as a matrix.",
        "Explain the horizontal-overflow assertion at 320px to a teammate — what does scrollWidth > clientWidth mean, and why can a screenshot never make that assertion for you?",
        "Walk one failure cell from your packet: which (state, viewport) broke, what the evidence showed, and what the repair was."
      ]
    },
    {
      "type": "transfer",
      "text": "The matrix-of-proof move travels to any surface where a single happy-path shot hides the failures: a data table proved across empty / one-row / overflowing / error-loading at each viewport; a chart proved with zero / sparse / dense / NaN data; a form proved across every validation state under keyboard. What stays invariant is the discipline — enumerate the claim as a grid, drive the real browser through every cell, capture evidence a reviewer can re-run. What changes is the axes (states, data shapes, locales, devices) and the cost of the cell nobody wanted to open. Carry component_state_matrix_and_accessibility_packet.md into the capstone: the fullstack dashboard ships with this exact packet attached, so 'the dashboard is responsive and accessible' stops being a claim and becomes a re-runnable matrix of browser proof."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "frontend-testing-strategy": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real component that has more than a happy path (a form, a data list, an async panel) and write its Frontend Testing Strategy: a state matrix covering loading, empty, error, disabled, and success, each row mapped to the cheapest test layer that proves it, plus a keyboard + viewport accessibility check that a happy-path test would never catch.",
      "intensity": "standard",
      "time": "25 min",
      "proof": "component_state_matrix_and_accessibility_packet.md: a 5-state matrix (loading/empty/error/disabled/success), each state assigned a test layer (unit · component · e2e) with the assertion it makes, one Playwright flow for the user journey, and a keyboard + viewport checklist. A reviewer can point to the exact test that fails if any one state regresses.",
      "unlock": "Your strategy names a test for every non-happy state, assigns each to the cheapest layer that proves it, and includes a keyboard + viewport check no happy-path test covers.",
      "doNotClaim": "Do not claim the component is tested because the success path renders. A test suite that only proves the success state is a happy-path answer wearing a green checkmark. The strategy exists to prove the loading, empty, error, and disabled states — the ones users hit on a bad network at 2 a.m."
    },
    {
      "type": "mission",
      "text": "The demo is flawless: type a query, results appear, ship it. In production the API is slow, then 500s, then returns zero rows — and the component renders a blank white box with a spinner that never stops, because nobody tested the four states that are not success."
    },
    {
      "type": "context",
      "text": "This lesson sits in the synthesis phase of Frontend And Fullstack Product Engineering: you can already build component state, form validation, and accessible UI — now you prove they hold. The Frontend Testing Strategy turns 'it works on my machine' into a reviewable matrix that pins every UI state to a test at the cheapest layer that can fail honestly."
    },
    {
      "type": "pretest",
      "prompt": "Your component has five states: loading, empty, error, disabled, success. You write one test that types input and asserts results appear. How many of the five states did you just cover? Answer before reading on.",
      "reveal": "One — success. The single happy-path test renders success and silently skips loading (does the spinner show and clear?), empty (zero-results message vs blank box?), error (does the error boundary catch the 500, or does React unmount the tree?), and disabled (is the submit button truly non-interactive, or just gray and still clickable?). Four of five states — the four users actually complain about — are untested. 'It works' meant 'one of five paths works.'"
    },
    {
      "type": "concept",
      "title": "A testing strategy is a state matrix mapped to the cheapest honest layer",
      "text": "List every UI state — loading, empty, error, disabled, success — and assign each the cheapest test that can FAIL when it regresses: unit for pure logic, component for rendered states, e2e for the user journey. The invariant: every state has a test that can return red."
    },
    {
      "type": "diagram",
      "title": "The testing trophy for a frontend component",
      "subtitle": "Four layers, widest where it is cheapest. The state matrix routes each UI state to the lowest layer that can prove it; the suspect gap — untested error and empty states — is where production breaks. Visual regression and a11y wrap the component states.",
      "rankdir": "TB",
      "nodes": [
        {
          "id": "static",
          "label": "Static (types + lint)",
          "description": "TypeScript + ESLint catch prop typos free",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "unit",
          "label": "Unit",
          "description": "pure logic: validators, reducers, formatters",
          "kind": "process",
          "tone": "default"
        },
        {
          "id": "component",
          "label": "Component",
          "description": "render each state: loading · empty · error · disabled · success",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "e2e",
          "label": "E2E (Playwright)",
          "description": "the real user journey through the browser",
          "kind": "process",
          "tone": "default"
        },
        {
          "id": "matrix",
          "label": "State matrix",
          "description": "5 states → cheapest layer each",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "gap",
          "label": "Untested error + empty",
          "description": "blank box, infinite spinner",
          "kind": "store",
          "tone": "warning"
        },
        {
          "id": "a11y",
          "label": "Keyboard + viewport",
          "description": "tab order, focus, 320px overflow",
          "kind": "external",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "static",
          "to": "unit",
          "label": "cheapest first",
          "kind": "control",
          "tone": "muted"
        },
        {
          "from": "unit",
          "to": "component",
          "label": "then rendered states",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "component",
          "to": "e2e",
          "label": "then the journey",
          "kind": "control"
        },
        {
          "from": "matrix",
          "to": "component",
          "label": "routes 4 of 5 states here",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "matrix",
          "to": "gap",
          "label": "skipped → prod break",
          "kind": "data",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "a11y",
          "to": "component",
          "label": "wraps every state",
          "kind": "control",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the state matrix + component layer (where most states live)"
        },
        {
          "tone": "warning",
          "label": "the states a happy-path suite skips"
        },
        {
          "tone": "muted",
          "label": "free layers — run them before writing one assertion"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Testing the five states, one assertion at a time",
      "subtitle": "The component-level suite that turns the state matrix into red-or-green — error and empty get tested, not just success.",
      "filename": "SearchPanel.test.tsx",
      "language": "ts",
      "code": "import { render, screen } from '@testing-library/react'\nimport userEvent from '@testing-library/user-event'\nimport { SearchPanel } from './SearchPanel'\n\ntest('loading: spinner shows while fetching, role=status', () => {\n  render(<SearchPanel state=\"loading\" />)\n  expect(screen.getByRole('status')).toHaveTextContent(/searching/i)\n})\n\ntest('empty: zero results renders a message, not a blank box', () => {\n  render(<SearchPanel state=\"success\" results={[]} />)\n  expect(screen.getByText(/no results/i)).toBeVisible()\n})\n\ntest('error: 500 is caught and shows a retry, tree stays mounted', () => {\n  render(<SearchPanel state=\"error\" />)\n  expect(screen.getByRole('alert')).toHaveTextContent(/try again/i)\n  expect(screen.getByRole('button', { name: /retry/i })).toBeEnabled()\n})\n\ntest('disabled: empty query keeps submit truly non-interactive', async () => {\n  render(<SearchPanel state=\"idle\" query=\"\" />)\n  const submit = screen.getByRole('button', { name: /search/i })\n  expect(submit).toBeDisabled()\n  await userEvent.click(submit)\n  expect(screen.queryByRole('status')).not.toBeInTheDocument()\n})",
      "steps": [
        {
          "lines": [
            5,
            6,
            7,
            8
          ],
          "label": "Loading state",
          "note": "Assert the spinner exists AND carries role=status, so screen readers announce it. A spinner with no role is a happy-path spinner — sighted-only."
        },
        {
          "lines": [
            10,
            11,
            12,
            13
          ],
          "label": "Empty state",
          "note": "Success with zero rows is its OWN state. Without this test, an empty array renders a blank box and the user assumes the app is broken."
        },
        {
          "lines": [
            15,
            16,
            17,
            18,
            19
          ],
          "label": "Error state",
          "note": "Prove the error boundary catches the 500, renders an alert, and keeps the tree mounted with a retry. A 500 that unmounts React is the white screen of death."
        },
        {
          "lines": [
            21,
            22,
            23,
            24,
            25,
            26
          ],
          "label": "Disabled state",
          "note": "Disabled must be REAL: assert toBeDisabled, then click it and assert nothing fired. A button that only looks gray but still submits is the classic disabled-in-name-only bug."
        }
      ],
      "caption": "Four tests, four non-success states — each can return red on its own. The success path is the one you do not have to fight for; these are."
    },
    {
      "type": "compare",
      "title": "Two test suites for the same component",
      "subtitle": "Same SearchPanel, same green CI badge. Only one survives a slow, failing API.",
      "left": {
        "label": "Happy-path suite",
        "tone": "warning",
        "lines": [
          "One test: type query → assert results appear",
          "Mocks the API to always return rows",
          "No loading, empty, error, or disabled test",
          "No keyboard nav or 320px viewport check",
          "Green CI = success state renders, nothing more"
        ],
        "verdict": "Ships the white screen of death to prod"
      },
      "right": {
        "label": "State-matrix strategy",
        "tone": "success",
        "lines": [
          "Five states, each its own failing-capable test",
          "Empty array → 'no results', not a blank box",
          "500 → error boundary catches, retry stays enabled",
          "Disabled button clicked → asserts nothing fired",
          "Playwright journey + tab order + 320px overflow"
        ],
        "verdict": "Each regression turns exactly one test red"
      },
      "caption": "Green CI is not proof — it is proof of whatever you asserted. A suite that only asserts success proves only success. Test the states users hit on a bad day."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 lives in the layer the happy path never reaches. The senior tell: their component suite tabs through the form by keyboard (focus order, visible focus ring, Enter submits), renders at a 320px viewport to catch overflow, and asserts role/aria on every state — because the user on a screen reader at 320px is exactly the user a happy-path test pretends does not exist. Grounded in WCAG 2.2 (w3.org/TR/WCAG22) and SWEBOK V4 verification."
    },
    {
      "type": "quiz",
      "question": "A reviewer says 'show me the exact evidence this isn't just a happy-path suite.' Which answer proves a real Frontend Testing Strategy?",
      "options": [
        "'CI is green and code coverage is 92%.'",
        "'Here is the empty-state test asserting a no-results message, the error test asserting the boundary catches a 500 and keeps retry enabled, the disabled test that clicks and asserts nothing fired, plus a Playwright keyboard flow at 320px.'",
        "'I tested the main user flow end to end in Playwright.'",
        "'Every component renders without throwing when I open the app.'"
      ],
      "answer": 1,
      "explanation": "Only option 2 points to tests that can FAIL on the non-success states — empty, error, disabled — plus the keyboard + viewport check WCAG demands. Coverage % (1) can be 92% and still skip every error branch; one e2e flow (3) is still one path; render-without-throwing (4) is the weakest possible assertion. The proof is a red-capable test per state, not a green badge."
    },
    {
      "type": "verification",
      "intro": "Prove your strategy tests more than success — no green-badge vibes:",
      "items": [
        "Point to the test that fails if the empty state renders a blank box instead of a 'no results' message. If there is none, your success test is masking the empty state.",
        "Point to the test that fails if a 500 unmounts the tree instead of hitting the error boundary. Force the mock to reject and watch it go red.",
        "Click your disabled control inside a test and assert nothing fired — 'looks gray' is not 'is disabled'.",
        "Run one Playwright flow with keyboard only: tab to the field, type, Enter to submit, and assert focus lands somewhere sane. WCAG 2.2 keyboard-operable is non-negotiable.",
        "Render the component at a 320px viewport and assert no horizontal overflow. Note the screenshot. The narrowest real phone is the cheapest a11y regression to catch."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does a single happy-path test give a false sense of safety, and which four states does it silently skip?",
        "Explain 'cheapest honest layer' using one row of your own state matrix — why does that state belong at unit, component, or e2e rather than the others?",
        "What is the difference between a button that looks disabled and one that is disabled, and which assertion in your suite proves it?"
      ]
    },
    {
      "type": "transfer",
      "text": "The state matrix is the move, not a React trick. A checkout flow has the same five states across payment-pending, card-declined, cart-empty, button-locked, and order-confirmed — and the same rule: test the four that are not 'confirmed'. A mobile app at a different viewport, a form behind a flaky API, a dashboard with no data on day one — each is a new matrix where loading, empty, and error change but the invariant holds: every state a user can hit gets a test that can fail. The component_state_matrix_and_accessibility_packet.md you wrote here is the same artifact your capstone reuses — swap the component, keep the matrix. That is portfolio evidence: not 'I write tests' but 'here is the five-state matrix, the Playwright flow, and the 320px keyboard proof for a component that survives a bad network.'"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "fullstack-integration-workflow": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real user-facing workflow (a form that posts to an API, a list that fetches) and build its component_state_matrix_and_accessibility_packet.md: every UI state the workflow can occupy — loading, empty, error, disabled, success — wired to a verified API behavior, plus the accessibility evidence (keyboard path + screen-reader announcement) that each state actually reaches the user.",
      "intensity": "deep",
      "time": "30-35 min",
      "proof": "component_state_matrix_and_accessibility_packet.md attached to the evidence ledger: a state matrix covering loading/empty/error/disabled/success, the API trigger and component-state transition for each, a Playwright flow that drives one full happy path, a keyboard checklist, and a viewport failure note. A reviewer can replay the error path and watch the UI announce it.",
      "unlock": "Your packet enumerates every reachable state, ties each to a real API condition, and proves the error and empty states are reachable by keyboard and announced to assistive tech — not just the success path.",
      "doNotClaim": "Do not claim the workflow is integrated because the happy path renders. A workflow that only handles the 200 response is a demo, not an integration. If you cannot point to what the UI does on a 500, a slow response, and an empty result, the matrix has holes and the packet is not done."
    },
    {
      "type": "mission",
      "text": "The demo nailed it on stage: type, submit, green checkmark. In production the API times out, the spinner spins forever, the submit button is still live so the user double-posts, and a screen-reader user never hears that anything happened. The happy path was never the workflow — the other four states were."
    },
    {
      "type": "context",
      "text": "Fullstack integration is the synthesis move of this course: it fuses component state, form validation, API behavior, and accessibility into one workflow the user can actually complete. The matrix is where those threads meet — Section 1 of the packet you carry into the capstone."
    },
    {
      "type": "pretest",
      "prompt": "A form posts to /api/subscribe. You wire onSubmit to call the API and show a success toast on resolve. What is wrong before you have written a single bug?",
      "reveal": "You modeled one of five states. A POST can be in-flight (loading — and the button must disable to stop double-submits), rejected (error — the user needs the message and focus moved to it), or return nothing useful (empty). 'Resolve and toast' silently assumes the network is fast, never fails, and always returns data. The integration is the state matrix, not the call."
    },
    {
      "type": "concept",
      "title": "A workflow is a state machine over API outcomes — every state designed, reachable, and announced",
      "text": "Enumerate the states a user-facing workflow can occupy (loading, empty, error, disabled, success), bind each to the API condition that triggers it, and prove each is reachable by keyboard and announced to assistive tech. The invariant: no state is unhandled."
    },
    {
      "type": "diagram",
      "title": "The integration workflow as a state machine",
      "subtitle": "One form-to-API workflow. The POST fans out into five states; the error and empty paths (often skipped) are first-class. aria-live carries each transition to assistive tech.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "idle",
          "label": "Idle / valid",
          "description": "form filled, submit enabled",
          "kind": "client",
          "tone": "default"
        },
        {
          "id": "submit",
          "label": "POST /api/subscribe",
          "description": "API call fires",
          "kind": "external",
          "tone": "accent"
        },
        {
          "id": "loading",
          "label": "Loading",
          "description": "submit disabled, aria-busy",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "success",
          "label": "Success",
          "description": "201 — confirm + reset",
          "kind": "process",
          "tone": "success"
        },
        {
          "id": "empty",
          "label": "Empty",
          "description": "200 but no match",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "error",
          "label": "Error",
          "description": "4xx/5xx/timeout",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "live",
          "label": "aria-live region",
          "description": "announces every transition",
          "kind": "store",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "idle",
          "to": "submit",
          "label": "valid submit",
          "kind": "sync"
        },
        {
          "from": "submit",
          "to": "loading",
          "label": "in-flight: disable button",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "loading",
          "to": "success",
          "label": "201",
          "kind": "async",
          "tone": "success"
        },
        {
          "from": "loading",
          "to": "empty",
          "label": "200, no result",
          "kind": "async",
          "tone": "muted"
        },
        {
          "from": "loading",
          "to": "error",
          "label": "5xx / timeout",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "error",
          "to": "idle",
          "label": "re-enable, focus message",
          "kind": "control"
        },
        {
          "from": "success",
          "to": "live",
          "label": "polite announce",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "error",
          "to": "live",
          "label": "assertive announce",
          "kind": "data",
          "tone": "warning"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "in-flight — the state that disables submit"
        },
        {
          "tone": "warning",
          "label": "error / failure path (assertive announce)"
        },
        {
          "tone": "muted",
          "label": "empty — 200 with nothing to show"
        },
        {
          "tone": "success",
          "label": "success — the only path most demos handle"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "Wiring the five states into one workflow",
      "subtitle": "A typed status union drives both the render and the accessibility announcement — no boolean soup, no unhandled state.",
      "filename": "SubscribeForm.tsx",
      "language": "ts",
      "code": "type Status =\n  | { kind: 'idle' }\n  | { kind: 'loading' }\n  | { kind: 'success'; email: string }\n  | { kind: 'empty' }\n  | { kind: 'error'; message: string }\n\nasync function onSubmit(email: string, set: (s: Status) => void) {\n  set({ kind: 'loading' })\n  try {\n    const res = await fetch('/api/subscribe', {\n      method: 'POST',\n      headers: { 'content-type': 'application/json' },\n      body: JSON.stringify({ email }),\n    })\n    if (!res.ok) throw new Error(`Subscribe failed (${res.status})`)\n    const data = await res.json()\n    set(data.created ? { kind: 'success', email } : { kind: 'empty' })\n  } catch (err) {\n    set({ kind: 'error', message: getErrorMessage(err) })\n  }\n}\n\n// render: status drives BOTH the UI and the announcement\n<button type=\"submit\" disabled={status.kind === 'loading'} aria-busy={status.kind === 'loading'}>\n  {status.kind === 'loading' ? 'Subscribing…' : 'Subscribe'}\n</button>\n<p role=\"status\" aria-live={status.kind === 'error' ? 'assertive' : 'polite'}>\n  {status.kind === 'error' ? status.message : status.kind === 'success' ? 'You are subscribed.' : ''}\n</p>",
      "steps": [
        {
          "lines": [
            1,
            2,
            3,
            4,
            5,
            6
          ],
          "label": "The state machine, in the type",
          "note": "A discriminated union makes the five states exhaustive: the compiler will not let you forget 'empty' or 'error'. This is the matrix expressed as code."
        },
        {
          "lines": [
            8,
            9
          ],
          "label": "Enter loading on every submit",
          "note": "Set loading BEFORE the await. This is what disables the button — the gap between click and response is exactly where double-submits happen."
        },
        {
          "lines": [
            14
          ],
          "label": "A non-2xx is not success",
          "note": "fetch only rejects on network failure, so res.ok must be checked explicitly. Forgetting this is the single most common integration bug: a 500 sails into the success branch."
        },
        {
          "lines": [
            16
          ],
          "label": "200 is not always content",
          "note": "A 200 with no created record is the EMPTY state, not success. Distinguishing empty from success is what separates an integration from a happy-path demo."
        },
        {
          "lines": [
            18
          ],
          "label": "Catch maps failure to a user message",
          "note": "The error boundary of the workflow: timeouts, 5xx, and thrown errors all land here as a message the user can read — never a silent swallow."
        },
        {
          "lines": [
            24,
            25,
            26
          ],
          "label": "State drives the announcement",
          "note": "aria-live='assertive' interrupts for errors; 'polite' for success. aria-busy + disabled stop the double-submit. The accessibility evidence is wired to the same status, so it can never drift from the UI."
        }
      ],
      "caption": "One status union is the source of truth for the render, the disabled state, AND the screen-reader announcement — so the matrix and the a11y packet describe the same machine."
    },
    {
      "type": "compare",
      "title": "Happy-path demo vs integrated workflow",
      "subtitle": "Same /api/subscribe form. One handles the 200; the other handles the workflow.",
      "left": {
        "label": "Happy-path only",
        "tone": "warning",
        "lines": [
          "isLoading boolean, then setData(res)",
          "No res.ok check — a 500 hits the success branch",
          "Submit stays live during the call → double-posts",
          "200-with-no-result renders a blank panel",
          "No aria-live: screen-reader users hear nothing",
          "Slow network = infinite spinner, no escape"
        ],
        "verdict": "Renders on stage, breaks in production"
      },
      "right": {
        "label": "Integrated workflow",
        "tone": "success",
        "lines": [
          "Typed status union: idle/loading/empty/error/success",
          "res.ok checked; non-2xx routed to error state",
          "Button disabled + aria-busy while in-flight",
          "200-no-result renders a real empty state",
          "aria-live announces success (polite) and error (assertive)",
          "Error re-enables the form and moves focus to the message"
        ],
        "verdict": "Every reachable state designed and announced"
      },
      "caption": "The difference is not effort on the happy path — it is whether the other four states exist at all. The matrix forces you to find them before a user does."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "Standards grounding, not vibes: WCAG 2.2 SC 4.1.3 (Status Messages) requires that a status change — 'subscribed', 'that email is already in use' — reach assistive tech WITHOUT moving focus, which is exactly what aria-live gives you. The senior tell is that the error and empty states ship with the same care as success; SWEBOK frames this as handling the full input domain, not the happy case. A reviewer trusts the workflow that proves its failure states, not the one that demos its success."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence that this isn't just a happy-path answer.' Which response actually proves an integrated workflow?",
      "options": [
        "A screenshot of the green success toast after a valid submit.",
        "The state matrix plus a Playwright flow that drives the error path (mocked 500) and asserts the form re-enables and the aria-live region announces the failure.",
        "A note that says 'error handling is on the backlog'.",
        "A passing unit test that the onSubmit function calls fetch."
      ],
      "answer": 1,
      "explanation": "Proof that it is not happy-path-only must exercise a non-happy path. Option 2 drives the error state, asserts the UI recovers (re-enable + focus), and asserts the announcement reaches assistive tech — the matrix made executable. The success screenshot (1) and the fetch-was-called test (4) only cover the path that was never in doubt; the backlog note (3) concedes the holes exist."
    },
    {
      "type": "verification",
      "intro": "Prove the workflow — no vibes:",
      "items": [
        "Open the state matrix: every row (loading, empty, error, disabled, success) has an API condition that triggers it and a component-state transition that renders it. No row says 'TODO'.",
        "Drive the error path: mock a 500 (or kill the network) and confirm the UI shows a readable message, the submit re-enables, and focus lands on the message — not a frozen spinner.",
        "Drive the empty path: return a 200 with no result and confirm a real empty state renders, distinct from both loading and error.",
        "Run the keyboard checklist: reach submit by Tab alone, trigger each state, and confirm the aria-live region announces success (polite) and error (assertive) — verified with VoiceOver or NVDA, not assumed.",
        "Record the viewport failure note: shrink to 320px and confirm no state overflows or hides the error message off-screen.",
        "Confirm the Playwright flow is committed and green, and the packet is attached to the evidence ledger by name."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Walk the five states of your workflow out loud, naming the exact API condition that triggers each — using your own matrix, not a generic example.",
        "Explain why disabling the submit button during the loading state is an integration concern and not just a styling choice. What real failure does it prevent?",
        "Point to the one piece of evidence in your packet that proves the error state is reachable by keyboard AND announced to a screen reader, and explain why a success screenshot would not satisfy the reviewer."
      ]
    },
    {
      "type": "transfer",
      "text": "The same matrix governs any user-facing workflow where an external system can be slow, fail, or return nothing — a search box (loading, no-results, error), a checkout (processing, declined, success), an infinite-scroll feed (fetching-more, end-of-list, network-drop). What stays invariant: enumerate every reachable state, bind each to its trigger, and prove the failure and empty states reach the user by keyboard and announcement. What changes is the API surface and the stakes — a declined payment demands a more assertive announcement and a clearer recovery than a missed search hit. This is the synthesis skill the course has been building toward: your component_state_matrix_and_accessibility_packet.md is the exact artifact and proof the capstone reuses — carry it forward unchanged in structure into the fullstack product dashboard, where the same matrix proves the dashboard is integrated, not just rendered."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "deployment-env-observability": [
    {
      "type": "sprint-contract",
      "outcome": "Take one real (or realistic) frontend/fullstack surface you can deploy and ship it to a production-shaped environment with deliberate env config, a health signal, and an error boundary — then write the deploy_env_observability_packet.md that PROVES the live deploy serves the real workflow, not just that the build went green.",
      "intensity": "deep",
      "time": "30 min",
      "proof": "deploy_env_observability_packet.md attached to the evidence ledger: the per-environment variable matrix (dev/preview/prod), the exact health-check + error-boundary you wired, a Playwright smoke flow against the DEPLOYED url, a viewport/accessibility check, plus the reviewer objection and the failure autopsy. A reviewer hits the production url and watches the workflow complete through loading → success states.",
      "unlock": "Your packet shows a green build AND a passing smoke flow against the deployed url, with env values that differ by environment and an error boundary that catches the realistic prod failure.",
      "doNotClaim": "Do not claim a surface is 'deployed and observable' because the build pipeline went green. A green build proves it compiled, not that the live environment has the right env vars, that the API it calls is reachable from prod, or that a user can complete the workflow. Green-build-only is the exact failure this lesson exists to kill."
    },
    {
      "type": "mission",
      "text": "The PR is merged, CI is green, the Slack channel gets the deploy checkmark — and three minutes later support is on fire because production is throwing a white screen, since the prod build read an env var that only existed on your laptop. 'It works on my machine' just became 'it works everywhere except where users are.'"
    },
    {
      "type": "context",
      "text": "This synthesis lesson takes the component, state, form, and accessibility work from earlier in the course and makes it survive the trip to a real environment. Deployment Env Observability is the move that turns 'it renders locally' into 'a stranger on the production url can finish the workflow, and if it breaks you can see why.'"
    },
    {
      "type": "pretest",
      "prompt": "CI is green and the deploy succeeded. Your teammate says 'ship confirmed, it's live.' What has actually been proven — and what hasn't? Answer before reading on.",
      "reveal": "A green deploy proves the bundle compiled and the host accepted it. It proves nothing about behavior in that environment: whether prod has the right env vars, whether the API origin is reachable from the prod network, whether the browser rendering shows real data or an error boundary. 'Live' is a claim about the build artifact; 'works' is a claim about a request completing in production. They are different axes — and confusing them is how a green checkmark sits on top of a white screen of death."
    },
    {
      "type": "concept",
      "title": "Deploy = environment config + a health signal + a proof the workflow runs live",
      "text": "Promote the artifact to a prod-shaped environment, inject config per environment (never hardcode the API origin), expose one health signal, wrap the risky path in an error boundary, then prove a real user flow completes against the deployed url. Invariant: the proof runs in the environment, not on your machine."
    },
    {
      "type": "diagram",
      "title": "The deploy + observability loop — failure path and repair path",
      "subtitle": "Build promotes to prod with env-injected config; the smoke check either confirms the live workflow or trips the error boundary and routes to rollback. The dashed loop is the repair: env fix → redeploy → re-prove.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "build",
          "label": "Build / CI",
          "description": "compiles bundle — green ≠ working",
          "kind": "process",
          "tone": "muted"
        },
        {
          "id": "config",
          "label": "Env config",
          "description": "per-env vars injected at deploy",
          "kind": "store",
          "tone": "accent"
        },
        {
          "id": "prod",
          "label": "Prod deploy",
          "description": "the live url users hit",
          "kind": "service",
          "tone": "accent"
        },
        {
          "id": "api",
          "label": "API origin",
          "description": "must be reachable FROM prod",
          "kind": "external"
        },
        {
          "id": "health",
          "label": "Health + smoke",
          "description": "request completes? right data?",
          "kind": "decision",
          "tone": "success"
        },
        {
          "id": "boundary",
          "label": "Error boundary",
          "description": "catches the white-screen path",
          "kind": "process",
          "tone": "warning"
        },
        {
          "id": "rollback",
          "label": "Rollback / repair",
          "description": "fix env → redeploy → re-prove",
          "kind": "decision",
          "tone": "warning"
        }
      ],
      "edges": [
        {
          "from": "build",
          "to": "prod",
          "label": "promote",
          "kind": "control"
        },
        {
          "from": "config",
          "to": "prod",
          "label": "inject per-env",
          "kind": "data",
          "tone": "accent"
        },
        {
          "from": "prod",
          "to": "api",
          "label": "fetch (loading state)",
          "kind": "sync"
        },
        {
          "from": "prod",
          "to": "health",
          "label": "smoke the live url",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "api",
          "to": "boundary",
          "label": "unreachable / 500",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "boundary",
          "to": "rollback",
          "label": "trip on prod failure",
          "kind": "control",
          "tone": "warning"
        },
        {
          "from": "rollback",
          "to": "config",
          "label": "fix env, re-prove",
          "kind": "control",
          "dashed": true,
          "tone": "muted"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "env config — the value that differs per environment"
        },
        {
          "tone": "success",
          "label": "the live proof: a request completes in prod"
        },
        {
          "tone": "warning",
          "label": "failure + repair path — boundary catches, rollback fixes"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The packet's load-bearing artifact: env-aware config + a live smoke",
      "subtitle": "Read the API origin from the environment (never hardcode), expose a health endpoint, and prove the deployed url with a Playwright flow.",
      "filename": "deploy_env_observability_packet.md",
      "language": "ts",
      "code": "// env.ts — config resolved per environment, validated at boot\nconst API_ORIGIN = process.env.NEXT_PUBLIC_API_ORIGIN\nif (!API_ORIGIN) throw new Error('NEXT_PUBLIC_API_ORIGIN missing — env not wired for this environment')\nexport const env = { apiOrigin: API_ORIGIN } as const\n\n// app/api/health/route.ts — one cheap signal the deploy is alive\nexport async function GET() {\n  const upstream = await fetch(`${env.apiOrigin}/ping`).then(r => r.ok).catch(() => false)\n  return Response.json({ ok: upstream, origin: env.apiOrigin }, { status: upstream ? 200 : 503 })\n}\n\n// smoke.spec.ts — proof against the DEPLOYED url, not localhost\nimport { test, expect } from '@playwright/test'\nconst URL = process.env.DEPLOY_URL!            // the live prod/preview url\ntest('live workflow completes', async ({ page }) => {\n  await page.goto(URL)\n  await expect(page.getByRole('status')).toHaveText(/loading/i) // loading state shows\n  await expect(page.getByRole('heading', { name: /your orders/i })).toBeVisible() // real data\n  await expect(page.getByRole('alert')).toHaveCount(0)          // error boundary did NOT trip\n})",
      "steps": [
        {
          "lines": [
            2,
            3
          ],
          "label": "Read config from the environment",
          "note": "The API origin comes from an env var, validated at boot. Hardcoding 'localhost:4000' is the classic prod white-screen: it compiles, deploys green, and dies on the first prod fetch."
        },
        {
          "lines": [
            4
          ],
          "label": "Fail fast if env is unwired",
          "note": "Throwing at boot when the var is missing turns a silent prod failure into a loud deploy failure — you find out before users do."
        },
        {
          "lines": [
            8,
            9,
            10
          ],
          "label": "One health signal",
          "note": "A /health route that pings the upstream gives you a single yes/no the deploy can actually serve traffic — 503 means 'built, but not working'."
        },
        {
          "lines": [
            15
          ],
          "label": "Smoke the DEPLOYED url",
          "note": "DEPLOY_URL is the live prod/preview origin, not localhost. Testing localhost re-proves your laptop; testing the deploy proves the environment."
        },
        {
          "lines": [
            17
          ],
          "label": "Loading state is observable",
          "note": "Asserting the loading state appears confirms the component's state machine survived the trip — not just the success snapshot."
        },
        {
          "lines": [
            18,
            19
          ],
          "label": "Real data + boundary did not trip",
          "note": "Visible real data plus zero alerts proves the workflow completed AND the error boundary stayed quiet. If the alert count is >0, the boundary caught a prod failure — that's your autopsy."
        }
      ],
      "caption": "Fill every blank: real api origin per environment, the live url, the workflow's own heading and loading copy. A reviewer runs this against prod and watches loading → real data with no error boundary."
    },
    {
      "type": "compare",
      "title": "Green build vs proven deploy",
      "subtitle": "Same merged PR, same green CI. Only one packet proves a user can finish the workflow in production.",
      "left": {
        "label": "Green-build packet",
        "tone": "warning",
        "lines": [
          "Evidence: 'CI passed, deploy succeeded'",
          "API origin hardcoded to localhost in the bundle",
          "Same .env used for every environment",
          "No health endpoint — liveness is a guess",
          "Tests ran against localhost, never the live url",
          "No error boundary — prod failure = white screen"
        ],
        "verdict": "Proves it compiled — not that prod works"
      },
      "right": {
        "label": "Proven-deploy packet",
        "tone": "success",
        "lines": [
          "Env matrix: dev/preview/prod values DIFFER and are validated at boot",
          "API origin injected per environment, missing var throws loud",
          "/health returns 200 (or 503) against the real upstream",
          "Playwright smoke runs on DEPLOY_URL: loading → real data",
          "Error boundary asserted quiet (alert count = 0)",
          "Failure autopsy + rollback condition written down"
        ],
        "verdict": "Proves a stranger can finish the workflow live"
      },
      "caption": "The differDiffPerEnv values are the tell: if prod and dev read the same origin, you never actually configured an environment — you shipped your laptop."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "The senior tell, grounded in WCAG 2.2: observability isn't only logs — it's the user-facing states. A deploy that fails should degrade to a real error boundary with role=\"alert\" and a recovery action, not a blank viewport. If your only 'observability' is a dashboard nobody on call is watching, the loading/error/empty states ARE your first line of production telemetry — make them accessible and unmistakable."
    },
    {
      "type": "quiz",
      "question": "Reviewer challenge: 'Show me the exact evidence this deploy works in production, not just on your machine.' Which packet item actually answers it?",
      "options": [
        "A screenshot of green CI and the 'deploy succeeded' message.",
        "A passing Playwright smoke flow run against the live DEPLOY_URL showing loading → real data with the error boundary quiet, plus an env matrix whose prod values differ from dev.",
        "A long README describing the architecture in detail.",
        "A note that says 'tested locally and everything worked.'"
      ],
      "answer": 1,
      "explanation": "Only option 2 exercises the production environment: it hits the deployed url (not localhost), proves the workflow's state machine runs there (loading → real data), confirms the error boundary did not trip, and shows env config that genuinely differs per environment. Green CI (1) proves compilation, the README (3) proves nothing executes, and 'tested locally' (4) re-proves the one machine that was never the question."
    },
    {
      "type": "verification",
      "intro": "Prove the deploy is observable — no green-checkmark vibes:",
      "items": [
        "Open the env matrix: the API origin (and any secret-shaped var) reads DIFFERENTLY for dev vs prod. If they're identical, you didn't configure an environment.",
        "Confirm a missing required env var throws at boot, so an unwired environment fails the deploy loudly instead of white-screening users.",
        "Hit /health on the live url: it returns 200 when the upstream is reachable and 503 when it isn't — liveness is a fact, not a hope.",
        "Run the Playwright smoke against DEPLOY_URL (the deployed origin): the loading state appears, real data renders, and the error boundary (role=\"alert\") count is zero.",
        "Trip it on purpose: point the prod env var at a dead origin, redeploy, and confirm the error boundary catches it into an accessible recovery state — then record the rollback condition."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In your own words: why does a green build tell you nothing about whether production works, and which one packet item closes that gap?",
        "Explain the difference between testing against localhost and testing against DEPLOY_URL, using your own smoke flow as the example.",
        "Walk the failure path on your diagram: a prod env var points at a dead API — where does the request go, what does the user see, and what is your rollback condition?"
      ]
    },
    {
      "type": "transfer",
      "text": "Same pattern, any environment-bound surface. A mobile app reads its API base from a build flavor, not a hardcoded string, and you smoke the TestFlight build, not the simulator. A serverless function validates its secrets at cold start and exposes a health route the platform can poll. What stays invariant: config differs per environment, one signal says 'alive', the risky path degrades to a visible error boundary, and the proof runs IN the environment. What changes: a one-way-door prod deploy demands a rollback plan and a canary before full traffic, where a throwaway preview can ship on the smoke alone. This packet is your capstone evidence — the fullstack product dashboard reuses this exact deploy_env_observability_packet.md to prove its live url serves real data with browser proof, not a green checkmark."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ],
  "fullstack-capstone": [
    {
      "type": "sprint-contract",
      "outcome": "Take ONE real user-facing workflow (a checkout, an onboarding form, a settings panel) and ship it end-to-end: semantic UI, every component state designed (loading, empty, error, disabled, success), keyboard-accessible, wired to a real API, and proven in a browser. Then package the proof as component_state_matrix_and_accessibility_packet.md.",
      "intensity": "capstone",
      "time": "45–60 min",
      "proof": "component_state_matrix_and_accessibility_packet.md attached to the evidence ledger: a state matrix covering loading/empty/error/disabled/success for the workflow, a Playwright flow that drives it, a keyboard-navigation checklist, and a viewport failure screenshot note. A reviewer opens it and sees a working slice — not a description of one.",
      "unlock": "Your workflow runs in a browser across every state, survives a keyboard-only pass, recovers from an API error without losing context, and the packet links each claim to evidence.",
      "doNotClaim": "Do not claim the feature 'works' from a happy-path screenshot. A workflow that has only a success state is a demo, not a product. If you cannot show the error state recovering and the empty state rendering, you proved the easy 20% and skipped the 80% that ships."
    },
    {
      "type": "mission",
      "text": "The happy path passed in the demo, so it shipped — then a user's API call timed out mid-checkout, the spinner never resolved, focus vanished into a dead button, and the cart emptied itself. Every skill in this course exists to stop exactly that moment."
    },
    {
      "type": "context",
      "text": "This capstone is the synthesis phase of the course: it fuses semantic HTML, component state, form validation, responsive layout, accessibility, and API integration into one reviewable slice. The win is not new knowledge — it is the integration the previous lessons each rehearsed in isolation."
    },
    {
      "type": "pretest",
      "prompt": "You built a form that submits to an API and shows a success message. A reviewer asks: 'Is it done?' You say yes. What did you almost certainly forget? Answer before reading on.",
      "reveal": "The other four states. 'Done' for a real surface means the loading state (request in flight, button disabled, no double-submit), the empty state (nothing to show yet), the error state (the API said no — and the user keeps their input), and the disabled state (preconditions unmet). A surface with only a success path is a single frame of a five-frame state machine. The reviewer is not asking if it submits; they are asking what it does when reality refuses to cooperate."
    },
    {
      "type": "concept",
      "title": "A real surface is a state machine, proven across every state",
      "text": "Treat the workflow as a finite set of UI states — loading, empty, error, disabled, success — each with defined markup, focus, and recovery. The invariant: every state is designed, accessible, and proven. A surface that only renders success is unfinished, not minimal."
    },
    {
      "type": "diagram",
      "title": "The assembled fullstack slice — and where each state lives",
      "subtitle": "The whole course in one circuit: semantic UI (client) → validated form → API call, branching into the five states. The dashed path is the failure-and-repair loop the happy path forgets.",
      "rankdir": "LR",
      "nodes": [
        {
          "id": "client",
          "label": "Semantic UI",
          "description": "L1 · landmarks, labels, headings",
          "kind": "client",
          "tone": "accent"
        },
        {
          "id": "form",
          "label": "Validated Form",
          "description": "L2 · client-side validation + disabled gate",
          "kind": "process",
          "tone": "accent"
        },
        {
          "id": "api",
          "label": "API Integration",
          "description": "L3 · fetch the user's workflow",
          "kind": "service"
        },
        {
          "id": "loading",
          "label": "Loading state",
          "description": "in-flight · button disabled · no double-submit",
          "kind": "process"
        },
        {
          "id": "success",
          "label": "Success state",
          "description": "result rendered · focus moved",
          "kind": "store",
          "tone": "success"
        },
        {
          "id": "error",
          "label": "Error / Empty",
          "description": "API said no · input preserved · error boundary",
          "kind": "store",
          "tone": "warning"
        },
        {
          "id": "a11y",
          "label": "A11y + Responsive",
          "description": "L4 · keyboard, focus, viewport 320→1440",
          "kind": "decision",
          "tone": "accent"
        },
        {
          "id": "proof",
          "label": "Browser proof",
          "description": "Playwright flow + screenshots",
          "kind": "external",
          "tone": "success"
        }
      ],
      "edges": [
        {
          "from": "client",
          "to": "form",
          "label": "renders",
          "kind": "sync"
        },
        {
          "from": "form",
          "to": "api",
          "label": "submits (if valid)",
          "kind": "sync",
          "tone": "accent"
        },
        {
          "from": "api",
          "to": "loading",
          "label": "in flight",
          "kind": "async"
        },
        {
          "from": "loading",
          "to": "success",
          "label": "on 200",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "loading",
          "to": "error",
          "label": "on 4xx/5xx/timeout",
          "kind": "async",
          "tone": "warning"
        },
        {
          "from": "error",
          "to": "form",
          "label": "recover, input kept",
          "kind": "control",
          "dashed": true,
          "tone": "warning"
        },
        {
          "from": "a11y",
          "to": "success",
          "label": "audits every state",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "a11y",
          "to": "error",
          "label": "audits every state",
          "kind": "control",
          "tone": "accent"
        },
        {
          "from": "success",
          "to": "proof",
          "label": "asserted",
          "kind": "data",
          "tone": "success"
        },
        {
          "from": "error",
          "to": "proof",
          "label": "asserted",
          "kind": "data",
          "tone": "success"
        }
      ],
      "legend": [
        {
          "tone": "accent",
          "label": "the course's skills, assembled"
        },
        {
          "tone": "success",
          "label": "proven good path"
        },
        {
          "tone": "warning",
          "label": "the failure + repair path most demos skip"
        }
      ]
    },
    {
      "type": "code-walkthrough",
      "title": "The end-to-end slice: one component, all five states",
      "subtitle": "A real submit handler — semantic, validated, state-machine-driven, accessible. This is the artifact the packet points to.",
      "filename": "CheckoutForm.tsx",
      "language": "ts",
      "code": "type Status = 'idle' | 'loading' | 'success' | 'error'\n\nfunction CheckoutForm({ onPlace }: { onPlace: (cart: Cart) => Promise<Order> }) {\n  const [status, setStatus] = useState<Status>('idle')\n  const [error, setError] = useState<string | null>(null)\n  const [cart] = useCart()\n  const isEmpty = cart.items.length === 0\n  const liveRef = useRef<HTMLDivElement>(null)\n\n  async function handleSubmit(e: React.FormEvent) {\n    e.preventDefault()\n    if (isEmpty || status === 'loading') return // disabled gate\n    setStatus('loading'); setError(null)\n    try {\n      const order = await onPlace(cart)\n      setStatus('success')\n      liveRef.current?.focus() // move focus to the result\n    } catch (err) {\n      setStatus('error')\n      setError(getErrorMessage(err)) // input is NOT cleared\n    }\n  }\n\n  if (isEmpty) return <EmptyCart />          // empty state\n  return (\n    <form onSubmit={handleSubmit} aria-busy={status === 'loading'}>\n      <fieldset disabled={status === 'loading'}>\n        <CartSummary cart={cart} />\n        <button type=\"submit\">\n          {status === 'loading' ? 'Placing order…' : 'Place order'}\n        </button>\n      </fieldset>\n      <div ref={liveRef} tabIndex={-1} role=\"status\" aria-live=\"polite\">\n        {status === 'success' && <p>Order confirmed.</p>}\n        {status === 'error' && <p role=\"alert\">{error} — your cart is saved, try again.</p>}\n      </div>\n    </form>\n  )\n}",
      "steps": [
        {
          "lines": [
            1,
            3,
            4
          ],
          "label": "Model the states explicitly",
          "note": "A Status union plus an error string. The five states (idle/loading/success/error + the empty branch) are data, not scattered booleans. You cannot forget a state you had to name."
        },
        {
          "lines": [
            12,
            13
          ],
          "label": "The disabled gate",
          "note": "Empty cart or in-flight request → return early. This is what stops the double-submit that charges a card twice. L2's validation made it impossible to submit invalid input; this makes it impossible to submit twice."
        },
        {
          "lines": [
            16,
            17,
            18
          ],
          "label": "Success: move focus, announce",
          "note": "On 200, set success AND move focus to the live region. A sighted user sees the message; a screen-reader user is told. Accessibility is not a coat of paint — it is in the control flow."
        },
        {
          "lines": [
            19,
            20,
            21
          ],
          "label": "Error: preserve input, recover",
          "note": "The catch sets error WITHOUT clearing the cart. The user's work survives the API saying no. This single line is the difference between the mission's disaster and a recoverable hiccup."
        },
        {
          "lines": [
            24
          ],
          "label": "The empty state, rendered not skipped",
          "note": "isEmpty returns a real EmptyCart component — a designed state, not a blank screen. 'Nothing here yet' is a UI you build, not an accident you allow."
        },
        {
          "lines": [
            26,
            27,
            30,
            32
          ],
          "label": "Semantic + a11y wiring",
          "note": "A real <form> (Enter submits, free), <fieldset disabled> greys the whole group in one attribute, aria-busy + aria-live + role='alert' carry state changes to assistive tech. Every course skill shows up in nine lines of return."
        }
      ],
      "caption": "One component touches all five lessons: semantic form (L1), the disabled/validation gate (L2), the API call (L3), and keyboard/focus/announcement (L4) — proven, not described."
    },
    {
      "type": "compare",
      "title": "Hollow demo vs. real shipped slice",
      "subtitle": "Same checkout feature. One passes the demo; one survives a user. The reviewer can tell in thirty seconds.",
      "left": {
        "label": "Hollow (happy-path only)",
        "tone": "warning",
        "lines": [
          "<div onClick> instead of a real <form> — Enter does nothing",
          "One state: success. No loading, empty, error, or disabled",
          "API error throws to console; spinner spins forever",
          "On error the form clears — user retypes everything",
          "Focus never moves; screen reader announces nothing",
          "Proof: a screenshot of the success toast"
        ],
        "verdict": "Works in the demo, breaks on the first timeout"
      },
      "right": {
        "label": "Real (state machine + proof)",
        "tone": "success",
        "lines": [
          "Semantic <form>; Enter submits, button disabled while loading",
          "Five states modeled and rendered: loading/empty/error/disabled/success",
          "Catch maps the error to a message; input is preserved",
          "aria-live announces the result; focus moves to it",
          "Responsive 320→1440, no overflow, touch targets ≥ 44px",
          "Proof: Playwright flow asserting each state + a11y + viewport shots"
        ],
        "verdict": "Survives the API saying no — and proves it"
      },
      "caption": "The gap is never the success path; it is the four states and the proof the hollow version never built. The packet exists to make that gap impossible to fake."
    },
    {
      "type": "callout",
      "tone": "tip",
      "text": "WCAG 2.2 is not a final-checklist item — it is a state-machine constraint. The moment your status changes, ask: does focus go somewhere sensible, and is the change announced (aria-live / role='alert')? A spinner with no aria-busy and a success with no moved focus are invisible to a third of your error states' worst-affected users. Bake the announcement into the same handler that flips the state, never bolt it on after."
    },
    {
      "type": "quiz",
      "question": "A reviewer says: 'Show me the exact evidence this isn't just a happy-path answer.' Which response proves the capstone?",
      "options": [
        "A screenshot of the success message and a paragraph describing the other states.",
        "The state matrix plus a Playwright flow that drives loading, error, and empty, a keyboard checklist, and a viewport-failure screenshot — each linked from the packet.",
        "A long README explaining the architecture and the libraries chosen.",
        "A passing unit test that asserts the component renders without crashing."
      ],
      "answer": 1,
      "explanation": "Only option 2 points to executed proof of the non-happy states. The matrix names every state, the Playwright flow drives the failure paths (not just success), the keyboard checklist proves accessibility, and the viewport shot proves responsiveness. Describing states (1), explaining architecture (3), or a smoke test that only confirms it renders (4) all dodge the exact question — does it work when reality refuses to cooperate."
    },
    {
      "type": "verification",
      "intro": "Prove the slice — no vibes, every state inspected:",
      "items": [
        "Drive the workflow to its loading state and confirm the button is disabled and a second submit is impossible (no double-charge).",
        "Force an API error (offline, or a mocked 500) and confirm the error renders, the user's input survives, and the path back is obvious — not a dead spinner.",
        "Render the empty state deliberately and confirm it is a designed component, not a blank screen.",
        "Run the workflow keyboard-only (Tab/Enter/Esc): every control reachable, focus visible, focus moves to the result on success, and the change is announced (aria-live / role='alert').",
        "Open it at 320 and 1440px: no horizontal overflow, touch targets ≥ 44px, layout holds. Capture the viewport-failure screenshot note for the packet.",
        "The Playwright flow asserts AT LEAST one non-success state. A test that only checks the happy path caps this lesson — re-run it covering error or empty."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Roll-call the course from memory: name the skills this slice fuses (semantic UI, component state, form validation, API integration, accessibility, responsive layout) and where each one shows up in your CheckoutForm.",
        "Explain why a happy-path-only feature is unfinished rather than minimal — use the error state of your own workflow as the example.",
        "Walk through what your slice does the moment the API returns a 500: what the user sees, what survives, where focus goes, and what assistive tech announces."
      ]
    },
    {
      "type": "transfer",
      "text": "You arrived able to build a screen that worked in the demo. You leave able to ship a surface that survives a user: framed as a state machine, every state designed and accessible, wired to a real API, recoverable when that API says no, and proven in a browser across viewports — not described, demonstrated. That move is invariant. Swap checkout for a search-results page (empty = 'no matches', error = 'search is down, your query is kept'), an upload flow (loading = progress, error = 'retry this file'), or a settings panel on a phone at 320px — the five states, the focus management, and the proof packet stay exactly the same; only the workflow and its failure modes change. The component_state_matrix_and_accessibility_packet.md you built here is your first portfolio evidence that reads 'I shipped a real, accessible, API-backed surface and proved every state' — not 'I can describe one.' That is the whole course, assembled. You don't just talk about fullstack product engineering now. You can ship it."
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "30 days"
      ]
    }
  ]
}