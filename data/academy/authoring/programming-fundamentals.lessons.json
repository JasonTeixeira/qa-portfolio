{
  "your-first-program": [
    {
      "time": "15–25 min",
      "type": "sprint-contract",
      "proof": "A program that prints the exact line \"Hello, world!\" — run it yourself and read the output it produced.",
      "unlock": "Your program runs and prints the exact required line, with no typos.",
      "outcome": "Write and run your very first program: make the computer print an exact line of text on screen, and understand the edit → run → see-output loop that every programmer lives in.",
      "intensity": "micro",
      "doNotClaim": "Don't claim you \"ran code\" until you've actually pressed Run and SEEN your line appear in the output — reading about it is not the same as running it."
    },
    {
      "text": "Right now the computer does nothing you tell it — because you have never told it anything. In the next twenty minutes you type one line, press Run, and watch a machine do exactly what you said.",
      "type": "mission"
    },
    {
      "text": "Everything you will ever build is just instructions you write and the computer runs, one after another. The whole job rests on one loop: edit, run, see, adjust.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: a friend says \"the computer is smart — it figures out what I mean.\" You type an instruction with a small typo. What do you think happens?",
      "reveal": "The computer does NOT guess what you meant — it does exactly what you typed, or it stops and complains. That is the most important thing to learn first: the computer is precise, not smart. A typo is not \"close enough\"; it is wrong, and the program will tell you. This is good news — it means the rules are consistent, and you are always in control."
    },
    {
      "text": "A program runs instructions top to bottom. print(...) displays whatever is inside the parentheses; text goes in quotes so the computer shows it literally — that quoted text is a string.",
      "type": "concept",
      "title": "A program is instructions; print() shows text on screen"
    },
    {
      "code": "print(\"Hello, world!\")\nprint(\"I just ran my first program.\")",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "It means \"show this on screen\". The output it produces is the line you see below the code.",
          "label": "print is the instruction",
          "lines": [
            1
          ]
        },
        {
          "note": "Everything between ( and ) is what print displays.",
          "label": "The parentheses hold what to show",
          "lines": [
            1
          ]
        },
        {
          "note": "The \" \" wrap a string — exactly what is inside them is printed: Hello, world!",
          "label": "Quotes mark the text",
          "lines": [
            1
          ]
        },
        {
          "note": "Programs run top to bottom, so this line prints AFTER the first. Order is something you control.",
          "label": "A second instruction runs next",
          "lines": [
            2
          ]
        },
        {
          "note": "Change the text inside the quotes, press Run, and the output changes to match. That loop is the whole job.",
          "label": "Edit · run · see",
          "lines": [
            1,
            2
          ]
        }
      ],
      "title": "Your first instruction, part by part",
      "caption": "Output, in order: Hello, world! then I just ran my first program.",
      "filename": "hello.py",
      "language": "python",
      "subtitle": "One line of Python — read each piece, then watch the output appear."
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "run",
          "from": "edit",
          "kind": "data",
          "label": "instructions"
        },
        {
          "to": "see",
          "from": "run",
          "kind": "data",
          "tone": "success",
          "label": "produces"
        },
        {
          "to": "adjust",
          "from": "see",
          "kind": "control",
          "label": "compare to goal"
        },
        {
          "to": "edit",
          "from": "adjust",
          "kind": "control",
          "label": "try again",
          "dashed": true
        }
      ],
      "nodes": [
        {
          "id": "edit",
          "kind": "process",
          "tone": "accent",
          "label": "Edit",
          "description": "type print(\"...\")"
        },
        {
          "id": "run",
          "kind": "process",
          "label": "Run",
          "description": "press Run"
        },
        {
          "id": "see",
          "kind": "store",
          "tone": "success",
          "label": "See output",
          "description": "read what appeared"
        },
        {
          "id": "adjust",
          "kind": "decision",
          "label": "Adjust",
          "description": "fix or extend"
        }
      ],
      "title": "The loop every programmer lives in",
      "legend": [
        {
          "tone": "accent",
          "label": "where you type"
        },
        {
          "tone": "success",
          "label": "what the computer shows back"
        }
      ],
      "rankdir": "LR",
      "subtitle": "You edit the instruction, run it, read the output, and adjust — round and round.",
      "storyboard": [
        {
          "say": "Everything you build starts here. You write an instruction the computer will follow, like print with your words in quotes.",
          "nodes": [
            "edit"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "You hand those instructions to the machine and press Run. It executes your lines top to bottom, exactly as written.",
          "nodes": [
            "edit",
            "run"
          ],
          "edges": [
            [
              "edit",
              "run"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Running produces output. For your first program, that is your quoted text printed back on the screen.",
          "nodes": [
            "run",
            "see"
          ],
          "edges": [
            [
              "run",
              "see"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Now you compare what you see to what you wanted. This is the moment you decide whether it did the job.",
          "nodes": [
            "see",
            "adjust"
          ],
          "edges": [
            [
              "see",
              "adjust"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "If it is off, you go back and try again, changing the instruction and running it once more.",
          "nodes": [
            "adjust",
            "edit"
          ],
          "edges": [
            [
              "adjust",
              "edit"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "That is the whole job: edit, run, see, adjust, round and round. Master this loop and you can build anything.",
          "nodes": [
            "edit",
            "run",
            "see",
            "adjust"
          ],
          "edges": [
            [
              "edit",
              "run"
            ],
            [
              "run",
              "see"
            ],
            [
              "see",
              "adjust"
            ],
            [
              "adjust",
              "edit"
            ]
          ],
          "ms": 5200
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "No quotes — error",
        "lines": [
          "print(Hello)",
          "Hello is read as a command to look up",
          "there is no such command",
          "NameError: name 'Hello' is not defined"
        ],
        "verdict": "The computer cannot find \"Hello\""
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Quoted — a string",
        "lines": [
          "print(\"Hello\")",
          "\"Hello\" is text to display, not a command",
          "the quotes mark it as a string",
          "output: Hello"
        ],
        "verdict": "Literal text always goes in quotes"
      },
      "title": "Quoted vs unquoted text",
      "caption": "Text you want shown LITERALLY always goes inside quotes — the #1 beginner fix.",
      "subtitle": "The same word, with and without quotes — only one runs."
    },
    {
      "text": "Here is the habit that separates people who get unstuck fast from people who panic: an error is not the computer scolding you — it is the computer telling you exactly where it got confused. Read it, do not fear it. And on day one, nine times out of ten the answer is a quote: it must wrap the WHOLE piece of text — opening quote, your text, closing quote — \"Hello, world!\". When something breaks, check your quotes first, then read the message.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "Hello, world!",
      "title": "Print your first line",
      "starter": "# Make the program print this EXACT line:\n#     Hello, world!\n# Type your print(...) below, then press Run.\n\n  # your code here\n",
      "summary": "Make the program print this EXACT line — including the comma, the space, and the exclamation mark:  Hello, world!  Use print(\"...\") with the text inside the quotes, then press Run and read your output.",
      "language": "python"
    },
    {
      "fix": "The text is missing its quotes. Without quotes the computer reads Hello and world! as commands to look up, not text to show — and there is no such command, so it errors. Wrap the literal text in quotes: print(\"Hello, world!\"). Quotes turn it into a string the computer will simply display.",
      "task": "Find why the computer refuses to run this one line.",
      "type": "debug",
      "symptom": "This program is meant to print a greeting, but pressing Run gives an error instead of showing the text.",
      "language": "python",
      "brokenCode": "print(Hello, world!)"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Dogs, then Cats — Python runs the last line first",
        "Cats on one line, then Dogs on the next — instructions run top to bottom",
        "CatsDogs on one line — print joins them together",
        "Nothing — you need to run them at the same time"
      ],
      "question": "You run print(\"Cats\") and then print(\"Dogs\"). What appears on screen, and in what order?",
      "explanation": "A program runs its instructions top to bottom, in order. print() shows its text and moves to a new line, so you get Cats first, then Dogs underneath. Order is something you control — and rely on — in every program you write."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "You pressed Run and your line actually appeared in the output (you saw it, not just read about it)",
        "The output is EXACTLY:  Hello, world!  — same comma, same space, same exclamation mark",
        "Your text is wrapped in matching quotes inside print(...)",
        "You changed the text once, re-ran, and watched the output change to match (the edit, run, see loop)"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In one sentence, what does print(...) do?",
        "Why does the text you want shown go inside quotes?",
        "What are the three steps of the loop every programmer repeats? (edit, then what, then what?)",
        "A friend says \"the computer figured out what I meant.\" Gently correct them."
      ]
    },
    {
      "text": "Open the lab again and print three lines of your own: your name, your goal for learning to code, and one thing you are curious about. Run it and read all three. You just made the machine say exactly what you told it to — that is the entire job, scaled up.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A program that prints the exact line \"Hello, world!\" — run it yourself and read the output it produced.",
        "Diagnose and repair the broken case: This program is meant to print a greeting, but pressing Run gives an error instead of showing the text.",
        "Confirm every observable check: You pressed Run and your line actually appeared in the output (you saw it, not just read about it) · The output is EXACTLY:  Hello, world!  — same comma, same space, same exclamation mark · Your text is wrapped in matching quotes inside print(...) · You changed the text once, re-ran, and watched the output change to match (the edit, run, see loop)",
        "Complete the transfer without copying the worked example: Open the lab again and print three lines of your own: your name, your goal for learning to code, and one thing you are curious about. Run it and read all three. You just made the machine say exactly what you told it to — that is the entire job, scaled up."
      ]
    }
  ],
  "variables-and-values": [
    {
      "time": "20–30 min",
      "type": "sprint-contract",
      "proof": "A program that stores a computed total in a variable and prints it — the right number appears in the output.",
      "unlock": "Your variable holds the correct value and your program prints it.",
      "outcome": "Store a value in a named variable, change it by reassigning, and use a variable in a calculation — so your programs can remember and reuse values instead of repeating them.",
      "intensity": "micro",
      "doNotClaim": "Don't claim you \"understand variables\" until your program has STORED a value in a name and used that name again later — not just printed a number directly."
    },
    {
      "text": "In Lesson 1 you printed a line — then the computer forgot it instantly. Real programs remember: a score that climbs, a total built from parts. Today you give a value a name you can hold onto and reuse.",
      "type": "mission"
    },
    {
      "text": "A variable is the most-used thing in all of programming — every score, total, and result lives in one. Master \"store under a name, then use the name\" and every later topic is just variables being read and changed.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: you write  score = 0  and later  score = 10. After both lines run, what is in  score — 0, 10, or both?",
      "reveal": "It is 10. A variable holds ONE value at a time — the most recent one assigned to it. The second line replaces the first; the 0 is gone. A variable is a labelled box, not a list of everything ever put in it. This \"the latest value wins\" rule is exactly what lets a score go up or a total change as your program runs."
    },
    {
      "text": "Create one with assignment: name = value. The single = stores the right side into the name on the left (not math \"equals\"). Use the name to read its current value; assign again and the new value replaces the old.",
      "type": "concept",
      "title": "A variable is a name that holds a value"
    },
    {
      "code": "price = 20          # store 20 under the name \"price\"\nprint(price)        # use the name -> shows 20\nprice = price - 5   # compute the right side, store it back\nprint(price)        # the name now holds 15 -> shows 15",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "price = 20 puts the value 20 into the name price. The single = means \"store\", not \"is equal to\".",
          "label": "Assignment stores a value",
          "lines": [
            1
          ]
        },
        {
          "note": "print(price) substitutes the current value of price → shows 20.",
          "label": "Read by using the name",
          "lines": [
            2
          ]
        },
        {
          "note": "Compute 20 - 5 = 15, THEN store 15 back into price. The old 20 is replaced — a variable holds only its latest value.",
          "label": "Right side first, then store",
          "lines": [
            3
          ]
        },
        {
          "note": "print(price) now shows 15. The name followed the latest assignment.",
          "label": "Same name, new value",
          "lines": [
            4
          ]
        }
      ],
      "title": "Store, read, then reassign",
      "caption": "price = price - 5 is a command, not a claim that price equals price minus five.",
      "filename": "variables.py",
      "language": "python",
      "subtitle": "Watch the name follow its latest value as each line runs."
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "box",
          "from": "value",
          "kind": "data",
          "tone": "accent",
          "label": "store with ="
        },
        {
          "to": "read",
          "from": "box",
          "kind": "data",
          "tone": "success",
          "label": "substitutes 18"
        }
      ],
      "nodes": [
        {
          "id": "value",
          "kind": "process",
          "tone": "accent",
          "label": "per_box * boxes",
          "description": "compute the right side first"
        },
        {
          "id": "box",
          "kind": "store",
          "tone": "success",
          "label": "total_eggs",
          "description": "the named box (the variable)"
        },
        {
          "id": "read",
          "kind": "process",
          "label": "print(total_eggs)",
          "description": "read the box by its name"
        }
      ],
      "title": "Assignment: a labelled box",
      "legend": [
        {
          "tone": "accent",
          "label": "the value being stored"
        },
        {
          "tone": "success",
          "label": "the variable that holds it"
        }
      ],
      "rankdir": "LR",
      "subtitle": "The right side is computed, then dropped into the box on the left. Reading the name reads the box.",
      "storyboard": [
        {
          "say": "Start on the right side: per_box times boxes. The computer works out this value first, before anything gets a name.",
          "nodes": [
            "value"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "The single equals sign is the store arrow. It drops the finished right-side value into the box on the left.",
          "nodes": [
            "value",
            "box"
          ],
          "edges": [
            [
              "value",
              "box"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "That box has a label: total_eggs. The name is written on the outside, the computed value sleeps inside.",
          "nodes": [
            "box"
          ],
          "edges": [],
          "ms": 4000
        },
        {
          "say": "Later, saying the name reads the box. total_eggs substitutes its current contents, eighteen, wherever you use it.",
          "nodes": [
            "box",
            "read"
          ],
          "edges": [
            [
              "box",
              "read"
            ]
          ],
          "ms": 4800
        },
        {
          "say": "So print of total_eggs shows 18, because reading the name just hands back whatever value is stored inside.",
          "nodes": [
            "read"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "The whole loop: compute a value, store it under a name, then read the name to reuse it. That is a variable.",
          "nodes": [
            "value",
            "box",
            "read"
          ],
          "edges": [
            [
              "value",
              "box"
            ],
            [
              "box",
              "read"
            ]
          ],
          "ms": 5000
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "Cryptic",
        "lines": [
          "p = 6",
          "b = 3",
          "t = p * b",
          "# what is t? scroll back to find out"
        ],
        "verdict": "Forces the next reader to reconstruct meaning"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Meaningful",
        "lines": [
          "per_box = 6",
          "boxes = 3",
          "total_eggs = per_box * boxes",
          "# the line explains itself"
        ],
        "verdict": "Name the value for its meaning"
      },
      "title": "Cryptic names vs meaningful names",
      "caption": "lowercase with underscores between words (per_box, max_score) — a name is the cheapest documentation you write.",
      "subtitle": "Same calculation, two ways to name it. One reads itself."
    },
    {
      "text": "Pros treat a variable name as the cheapest documentation they will ever write. total_eggs explains itself; t and x force the next reader (usually future-you, at 11pm, debugging) to scroll back and reconstruct what it meant. The rule working programmers follow: name the value for its MEANING, lowercase with underscores between words. A good name is not neatness — it is how code stays readable long after you have forgotten writing it.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "48",
      "title": "Store a total and print it",
      "starter": "# A box holds 12 apples per crate. You have 4 crates.\n# 1. Store the apples-per-crate in a variable called  per_crate\n# 2. Store the number of crates in a variable called  crates\n# 3. Store the TOTAL apples (per_crate times crates) in a variable called  total\n# 4. Print  total  on its own line.\n\n  # your code here\n",
      "summary": "A crate holds 12 apples. You have 4 crates. Store 12 in a variable per_crate, store 4 in crates, then store per_crate times crates in a variable called total, and print total. The output should be a single line: 48. Use the variable names in your calculation — do not just print 48 directly.",
      "language": "python"
    },
    {
      "fix": "The line  score + 3  computes 8 but throws it away — nothing stores the result back. To change a variable you must assign the new value to it:  score = score + 3. Now the right side (5 + 3 = 8) is stored back into score, and print(score) shows 8.",
      "task": "Find why the 3 never sticks.",
      "type": "debug",
      "symptom": "This program should print the final score (start 5, then add 3), but it prints the wrong number.",
      "language": "python",
      "brokenCode": "score = 5\nscore + 3        # meant to add 3 to the score\nprint(score)     # prints 5, not 8"
    },
    {
      "type": "quiz",
      "answer": 2,
      "options": [
        "1 — assignment only happens once",
        "2 — only the last line counts",
        "3 — each line takes the current value and adds one, storing it back",
        "Error — you can't use count on both sides of ="
      ],
      "question": "After these three lines run — count = 1, then count = count + 1, then count = count + 1 — what does print(count) show?",
      "explanation": "Each  count = count + 1  reads the current value, adds one, and stores the result back. So 1 → 2 → 3. Using a variable on both sides is normal and useful: it is how you grow a counter, a score, or a running total step by step."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "You created variables with  name = value  (assignment), not just printed raw numbers",
        "You used a variable in a calculation (e.g. per_crate * crates)",
        "The stored total is correct and your program prints it (output: 48)",
        "You can explain that = means \"store the right side into the name on the left\", not \"is equal to\"",
        "You reassigned a variable at least once and saw the latest value win"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In one sentence, what is a variable?",
        "What does the single = actually do, and how is it different from math \"equals\"?",
        "When you write  score = score + 1, what gets stored, and where does it come from?",
        "Why does a good variable name save you time later?"
      ]
    },
    {
      "text": "In the lab, model something from your own life: store a price in a variable, store a quantity, compute and print the total. Then reassign the quantity to a bigger number, re-run, and watch the total update. That \"change an input, the result follows\" is the heartbeat of every program you will build.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A program that stores a computed total in a variable and prints it — the right number appears in the output.",
        "Diagnose and repair the broken case: This program should print the final score (start 5, then add 3), but it prints the wrong number.",
        "Confirm every observable check: You created variables with  name = value  (assignment), not just printed raw numbers · You used a variable in a calculation (e.g. per_crate * crates) · The stored total is correct and your program prints it (output: 48) · You can explain that = means \"store the right side into the name on the left\", not \"is equal to\" · You reassigned a variable at least once and saw the latest value win",
        "Complete the transfer without copying the worked example: In the lab, model something from your own life: store a price in a variable, store a quantity, compute and print the total. Then reassign the quantity to a bigger number, re-run, and watch the total update. That \"change an input, the result follows\" is the heartbeat of every program you will build."
      ]
    }
  ],
  "numbers-and-strings": [
    {
      "time": "30–40 min",
      "type": "sprint-contract",
      "proof": "A program that combines numbers and text into one exact formatted line and prints it.",
      "unlock": "Your line matches the required output exactly, with the numbers computed (not hardcoded into the text).",
      "outcome": "Tell numbers and text apart, do arithmetic on numbers, convert between text and numbers when you need to, and build a clean formatted line by dropping values into text with an f-string.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you \"get types\" until you've seen with your own eyes that \"2\" + \"3\" is \"23\" but 2 + 3 is 5 — the difference is the whole lesson."
    },
    {
      "text": "In Lesson 2 you added numbers and it just worked. So this will rattle you: add two quantities and get \"23\" instead of 5 — because your values have a hidden property called a type, and text and numbers obey different rules under the same + sign.",
      "type": "mission"
    },
    {
      "text": "Every value has a type, and the type decides what an operation means: numbers add, text joins. Input from forms, files, and the keyboard arrives as TEXT even when it looks like a number — so telling them apart and converting is foundational.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: what do you think  \"2\" + \"3\"  gives — and what does  2 + 3  give? Are they the same?",
      "reveal": "2 + 3 is 5 (numbers add). But \"2\" + \"3\" is \"23\" — with quotes they are TEXT, and + on text means \"join end to end\" (concatenation), not \"add\". Same symbol, different meaning, decided entirely by the TYPE of the values. Text that looks like a number is still text until you convert it."
    },
    {
      "type": "worked-example",
      "intro": "Compute on numbers, format with an f-string. Do the math first, then drop the values into text with { }.",
      "code": "price = 4\nqty = 3\ntotal = price * qty          # numbers -> arithmetic -> 12 (an int)\n\nline = f\"Total for {qty} items: ${total}\"\nprint(line)                  # -> Total for 3 items: $12",
      "language": "python",
      "steps": [
        "These are numbers (int): No quotes, so price and qty are ints — ready for arithmetic.",
        "Multiply the numbers: price * qty does real multiplication → 12, an int.",
        "An f-string starts with f: The f before the opening quote turns on substitution; anything in { } is evaluated and dropped in.",
        "Only the { } parts get replaced: {qty} → 3, {total} → 12. The dollar sign and words are plain text. Numbers become text automatically here.",
        "Print the finished line: Output: Total for 3 items: $12 — numbers computed, then formatted."
      ],
      "commonMistake": "This is supposed to add two quantities and show 5, but it prints 23."
    },
    {
      "text": "int is a whole number (4); float has a decimal (3.14); str is text in quotes (\"4\"). The type decides what + means — numbers add, strings join. Convert deliberately: int(\"4\") gives 4; str(4) gives \"4\".",
      "type": "concept",
      "title": "Values have types: int, float, and str"
    },
    {
      "left": {
        "tone": "warning",
        "label": "Strings — joins",
        "lines": [
          "\"2\" + \"3\"",
          "both are text (in quotes)",
          "+ on text glues end to end",
          "result: \"23\""
        ],
        "verdict": "Concatenation, not addition"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Numbers — adds",
        "lines": [
          "2 + 3",
          "both are int (no quotes)",
          "+ on numbers does arithmetic",
          "result: 5"
        ],
        "verdict": "Real addition"
      },
      "title": "The same + sign, two meanings",
      "caption": "\"4\" with quotes is text; 4 without is a number. They look alike and behave completely differently.",
      "subtitle": "The TYPE of the values decides what + does. This is the whole lesson."
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "toint",
          "from": "str",
          "kind": "data",
          "label": "convert"
        },
        {
          "to": "num",
          "from": "toint",
          "kind": "data",
          "tone": "success",
          "label": "now 25 + 1 = 26"
        },
        {
          "to": "tostr",
          "from": "num",
          "kind": "data",
          "label": "convert"
        },
        {
          "to": "str",
          "from": "tostr",
          "kind": "data",
          "label": "now joins with +"
        }
      ],
      "nodes": [
        {
          "id": "str",
          "kind": "store",
          "tone": "warning",
          "label": "\"25\"",
          "description": "str — from a form/keyboard"
        },
        {
          "id": "toint",
          "kind": "process",
          "tone": "accent",
          "label": "int(\"25\")",
          "description": "parse text to a number"
        },
        {
          "id": "num",
          "kind": "store",
          "tone": "success",
          "label": "25",
          "description": "int — arithmetic works"
        },
        {
          "id": "tostr",
          "kind": "process",
          "tone": "accent",
          "label": "str(25)",
          "description": "number back to text"
        }
      ],
      "title": "Converting across the type boundary",
      "legend": [
        {
          "tone": "warning",
          "label": "text (str) — joins under +"
        },
        {
          "tone": "success",
          "label": "number (int) — adds under +"
        },
        {
          "tone": "accent",
          "label": "a deliberate conversion"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Text that looks numeric is str until int() crosses it over; str() crosses a number back to text.",
      "storyboard": [
        {
          "say": "Start here: \"25\" in quotes is a string — text that only looks like a number, so plus would join it, not add it.",
          "nodes": [
            "str"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "To do math, you cross the type boundary: int(\"25\") reads the digits and hands back a real whole number.",
          "nodes": [
            "str",
            "toint"
          ],
          "edges": [
            [
              "str",
              "toint"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "Now it's the number 25 — a true int, so 25 + 1 gives 26, because plus on numbers means add.",
          "nodes": [
            "toint",
            "num"
          ],
          "edges": [
            [
              "toint",
              "num"
            ]
          ],
          "ms": 4800
        },
        {
          "say": "Going the other way, str(25) crosses the same boundary back, turning the number into the text \"25\".",
          "nodes": [
            "num",
            "tostr"
          ],
          "edges": [
            [
              "num",
              "tostr"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "Back as a string, plus now joins instead of adds — so it can concatenate cleanly with other text.",
          "nodes": [
            "tostr",
            "str"
          ],
          "edges": [
            [
              "tostr",
              "str"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "The core insight: the type, not the character, decides what plus means — so convert deliberately every time.",
          "nodes": [
            "str",
            "num"
          ],
          "edges": [
            [
              "str",
              "toint"
            ],
            [
              "num",
              "tostr"
            ]
          ],
          "ms": 5000
        }
      ]
    },
    {
      "text": "Reach for an f-string whenever you mix text and values: f\"{name} scored {points}\". It is cleaner and less error-prone than gluing pieces with + and str(), and it makes the final line obvious at a glance. Just remember the leading f — without it, the { } show up literally as characters.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "Coffee x3 = $12",
      "title": "Build a receipt line",
      "starter": "# You are building one line of a receipt.\n#   item   = \"Coffee\"     (text)\n#   price  = 4            (a whole number, an int)\n#   qty    = 3            (a whole number, an int)\n# Compute the total (price times qty) and build EXACTLY this line, then print it:\n#   Coffee x3 = $12\n# Use an f-string:  f\"...{qty}...{total}...\"  to drop the numbers into the text.\n\nitem = \"Coffee\"\nprice = 4\nqty = 3\n  # your code here\n",
      "summary": "You have item = \"Coffee\", price = 4, qty = 3. Compute total = price * qty (do NOT hardcode 12), then build and print EXACTLY this line using an f-string: Coffee x3 = $12. The numbers must come from your variables, dropped in with { }.",
      "language": "python"
    },
    {
      "code": "price = 4\nqty = 3\ntotal = price * qty          # numbers -> arithmetic -> 12 (an int)\n\nline = f\"Total for {qty} items: ${total}\"\nprint(line)                  # -> Total for 3 items: $12",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "No quotes, so price and qty are ints — ready for arithmetic.",
          "label": "These are numbers (int)",
          "lines": [
            1,
            2
          ]
        },
        {
          "note": "price * qty does real multiplication → 12, an int.",
          "label": "Multiply the numbers",
          "lines": [
            3
          ]
        },
        {
          "note": "The f before the opening quote turns on substitution; anything in { } is evaluated and dropped in.",
          "label": "An f-string starts with f",
          "lines": [
            5
          ]
        },
        {
          "note": "{qty} → 3, {total} → 12. The dollar sign and words are plain text. Numbers become text automatically here.",
          "label": "Only the { } parts get replaced",
          "lines": [
            5
          ]
        },
        {
          "note": "Output: Total for 3 items: $12 — numbers computed, then formatted.",
          "label": "Print the finished line",
          "lines": [
            6
          ]
        }
      ],
      "title": "Compute on numbers, format with an f-string",
      "caption": "Joining a number to text with + (like \"Total: \" + total) errors — use an f-string, which converts for you.",
      "filename": "receipt.py",
      "language": "python",
      "subtitle": "Do the math first, then drop the values into text with { }."
    },
    {
      "fix": "a and b are strings (\"2\" and \"3\"), and + on strings joins them → \"23\". They look like numbers but they are text. Convert each to an int first, then add:  print(int(a) + int(b))  → 5. Form and keyboard input is text; convert before you do math.",
      "task": "Find why it joins instead of adds.",
      "type": "debug",
      "symptom": "This is supposed to add two quantities and show 5, but it prints 23.",
      "language": "python",
      "brokenCode": "a = \"2\"          # came from a form, so it's text\nb = \"3\"\nprint(a + b)     # meant to add -> prints \"23\", not 5"
    },
    {
      "type": "tradeoff",
      "question": "The same + sign, two meanings",
      "optionA": {
        "label": "Strings — joins",
        "text": "\"2\" + \"3\" · both are text (in quotes) · + on text glues end to end · result: \"23\" Outcome: Concatenation, not addition"
      },
      "optionB": {
        "label": "Numbers — adds",
        "text": "2 + 3 · both are int (no quotes) · + on numbers does arithmetic · result: 5 Outcome: Real addition"
      },
      "guidance": "\"4\" with quotes is text; 4 without is a number. They look alike and behave completely differently."
    },
    {
      "type": "quiz",
      "answer": 2,
      "options": [
        "Gives 26 — Python sees a number",
        "Gives \"251\" — it joins 1 onto the text",
        "Raises an error — you can't add text and a number",
        "Gives 25 — the 1 is ignored"
      ],
      "question": "A value typed into a form arrives as  age = \"25\". What does  age + 1  do?",
      "explanation": "age is the text \"25\", and you cannot + a string and an int — Python stops with an error. Convert first: int(age) + 1 gives 26. This is exactly why later lessons convert input at the boundary before doing anything numeric with it."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "You can state the type of 4 (int), 4.0 (float), and \"4\" (str)",
        "You computed total with arithmetic on numbers (price * qty), not by typing 12 into the text",
        "Your output line is EXACTLY:  Coffee x3 = $12",
        "You built the line with an f-string, dropping the numbers in with { }",
        "You saw the difference firsthand: \"2\" + \"3\" joins to \"23\", but 2 + 3 adds to 5"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Name the three everyday types and give one example of each.",
        "Why does + do different things to \"2\" + \"3\" versus 2 + 3?",
        "When would you use int(...) and when would you use str(...)?",
        "What does the f in f\"...\" turn on, and what do the { } do?"
      ]
    },
    {
      "text": "In the lab, build a one-line summary about yourself by mixing types: a name (str), an age or a count (int), maybe a height (float), combined into one sentence with an f-string. Then take a number that arrived as text (like \"5\"), convert it with int(), and use it in a calculation. You have just done the core move behind handling any real-world input.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A program that combines numbers and text into one exact formatted line and prints it.",
        "Diagnose and repair the broken case: This is supposed to add two quantities and show 5, but it prints 23.",
        "Confirm every observable check: You can state the type of 4 (int), 4.0 (float), and \"4\" (str) · You computed total with arithmetic on numbers (price * qty), not by typing 12 into the text · Your output line is EXACTLY:  Coffee x3 = $12 · You built the line with an f-string, dropping the numbers in with { } · You saw the difference firsthand: \"2\" + \"3\" joins to \"23\", but 2 + 3 adds to 5",
        "Complete the transfer without copying the worked example: In the lab, build a one-line summary about yourself by mixing types: a name (str), an age or a count (int), maybe a height (float), combined into one sentence with an f-string. Then take a number that arrived as text (like \"5\"), convert it with int(), and use it in a calculation. You have just done the core move behind handling any real-world input."
      ]
    }
  ],
  "reading-input": [
    {
      "time": "25–35 min",
      "type": "sprint-contract",
      "proof": "A program that reads a name and a count from input, then prints one exact formatted line built from both — the numbers used as numbers, not text.",
      "unlock": "Your program reads the provided input and prints the exact required line, with the count converted to a real number.",
      "outcome": "Make your program RECEIVE data instead of only using values you typed in: read a line with input(), convert it to a number with int() when you need to do math, and print an exact line built from what you read.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you \"read input\" until your program has taken a line from input(), converted the numeric one with int(), and printed the exact line built from BOTH — printing a value you hardcoded is not reading input."
    },
    {
      "text": "Every program you have written already knew its answer — you typed it in. A real program does not know who is using it; it has to ask. Today your program stops talking to itself and starts listening.",
      "type": "mission"
    },
    {
      "text": "Reading input is the front door of almost every program — a login form, a search box, a number from a file. The shape is always the same: pull in a value, convert it (input arrives as TEXT), then use it.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: a user types  3  and you read it with  count = input(). You then write  count + 1  to get 4. What actually happens?",
      "reveal": "It does NOT give 4 — it errors. input() ALWAYS hands back text, so count is the string \"3\", not the number 3. And you cannot add a number to text, so  \"3\" + 1  stops with a TypeError. This is the catch that gets every beginner: a thing that LOOKS like a number on screen is still text until you convert it. Wrap it in int() first — count = int(input()) — and then count + 1 is 4. Read as text, convert before you do math."
    },
    {
      "type": "worked-example",
      "intro": "Read · convert · use. Two lines come in as text; only the numeric one is converted. Watch where int() goes.",
      "code": "name = input()              # reads a line -> text, e.g. \"Sam\"\nage_text = input()          # reads the next line -> text, e.g. \"30\"\nage = int(age_text)         # convert that text to a real number -> 30\n\nprint(f\"Hi {name}, next year you turn {age + 1}.\")",
      "language": "python",
      "steps": [
        "First input() → text: Reads one line and returns it as a string; name becomes \"Sam\". The prompt argument, if any, is just a hint — not part of the value.",
        "Second input() → next line: Each call reads the NEXT line in order; age_text becomes the text \"30\".",
        "Convert the numeric one: int(age_text) crosses the text \"30\" into the number 30, stored in age.",
        "Now arithmetic works: Because age is a real number, age + 1 → 31 (on text it would error). The f-string drops name and 31 into the sentence."
      ],
      "commonMistake": "This should read a count and print \"You have 4 items.\" (3 typed, plus 1), but it crashes with a TypeError instead."
    },
    {
      "text": "input() reads one line the user provides and hands it back as a string — always text. Convert when you need a number: int(input()) turns \"3\" into 3. It is the \"2\" vs 2 type difference from Lesson 3, at the boundary.",
      "type": "concept",
      "title": "input() reads a line as text; convert with int() to get a number"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "input",
          "from": "user",
          "kind": "async",
          "label": "one line"
        },
        {
          "to": "str",
          "from": "input",
          "kind": "data",
          "tone": "warning",
          "label": "always text"
        },
        {
          "to": "int",
          "from": "str",
          "kind": "data",
          "tone": "accent",
          "label": "convert now"
        },
        {
          "to": "num",
          "from": "int",
          "kind": "data",
          "tone": "success",
          "label": "count + 1 = 4"
        }
      ],
      "nodes": [
        {
          "id": "user",
          "kind": "external",
          "tone": "muted",
          "label": "User types 3",
          "description": "outside the program"
        },
        {
          "id": "input",
          "kind": "process",
          "label": "input()",
          "description": "reads the line"
        },
        {
          "id": "str",
          "kind": "store",
          "tone": "warning",
          "label": "\"3\"",
          "description": "str — even though it looks numeric"
        },
        {
          "id": "int",
          "kind": "process",
          "tone": "accent",
          "label": "int(...)",
          "description": "convert at the boundary"
        },
        {
          "id": "num",
          "kind": "store",
          "tone": "success",
          "label": "3",
          "description": "int — ready for math"
        }
      ],
      "title": "Input crosses the boundary as text",
      "legend": [
        {
          "tone": "warning",
          "label": "text as it arrives"
        },
        {
          "tone": "accent",
          "label": "convert at the door"
        },
        {
          "tone": "success",
          "label": "a real number, trusted downstream"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Outside the program everything is text. Convert at the door, then the rest of the program can trust real numbers.",
      "storyboard": [
        {
          "say": "It starts outside the program: a person at the keyboard types 3 and presses enter. The program does not know this value yet.",
          "nodes": [
            "user",
            "input"
          ],
          "edges": [
            [
              "user",
              "input"
            ]
          ],
          "ms": 4000
        },
        {
          "say": "input() opens the front door. It reads exactly one line the user provides and hands that line back to your program.",
          "nodes": [
            "input"
          ],
          "edges": [
            [
              "user",
              "input"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Here is the catch: whatever comes through that door is text. Typing 3 gives you the string \"3\", never the number.",
          "nodes": [
            "input",
            "str"
          ],
          "edges": [
            [
              "input",
              "str"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "So you convert at the boundary. int(...) takes the string \"3\" and turns it into a real number you can do math with.",
          "nodes": [
            "str",
            "int"
          ],
          "edges": [
            [
              "str",
              "int"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "Now it is the number 3. Add one and you get 4 — arithmetic that would have failed on the raw text.",
          "nodes": [
            "int",
            "num"
          ],
          "edges": [
            [
              "int",
              "num"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "The whole shape: pull a line in, convert it once at the door, then trust real numbers everywhere after.",
          "nodes": [
            "user",
            "input",
            "str",
            "int",
            "num"
          ],
          "edges": [
            [
              "user",
              "input"
            ],
            [
              "input",
              "str"
            ],
            [
              "str",
              "int"
            ],
            [
              "int",
              "num"
            ]
          ],
          "ms": 5000
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "Raw input — crashes",
        "lines": [
          "count = input()        # \"3\", a string",
          "count + 1",
          "# cannot + text and a number",
          "TypeError"
        ],
        "verdict": "Input is still text"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Convert at the boundary",
        "lines": [
          "count = int(input())   # 3, a number",
          "count + 1",
          "# real arithmetic",
          "result: 4"
        ],
        "verdict": "Convert the FIRST thing you do"
      },
      "title": "Math on raw input vs convert first",
      "caption": "int(input()) is right; int(input() + 1) errors — it + text and a number before converting. Convert first, then compute.",
      "subtitle": "The user types 3. Adding 1 should give 4 — only one version does."
    },
    {
      "text": "Pros convert input the instant it arrives, right at the boundary — count = int(input()) on one line — not three lines later when they have forgotten it is still text. The bug that bites beginners is letting a \"number\" travel through the program as a string and only exploding when they finally try to add it. Make the conversion the FIRST thing you do with numeric input. (If the user types something that is not a number, int() will error — handling that gracefully is a Foundations lesson; for now, trust the input.)",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "Sam, you have 3 items.",
      "stdin": "Sam\n3",
      "title": "Read a name and a count",
      "starter": "# This program will read TWO lines the user \"types\" (provided for you below):\n#   line 1 = a name        (e.g. Sam)\n#   line 2 = a number of items (e.g. 3)\n# 1. Read the name with  input()  into a variable  name  (it comes in as text).\n# 2. Read the next line with  input(), convert it with  int(), into  count.\n# 3. Print EXACTLY this line, using an f-string:\n#       Sam, you have 3 items.\n# (input() hands you each line in order; the count must be a real number.)\n\n  # your code here\n",
      "summary": "The program is given two input lines: a name (Sam) on the first line and a number (3) on the second. Read the name with input() into name, read the next line and convert it with int() into count, then print EXACTLY this line with an f-string: Sam, you have 3 items. The count must be read from input and converted — do not hardcode the name or the number.",
      "language": "python"
    },
    {
      "code": "name = input()              # reads a line -> text, e.g. \"Sam\"\nage_text = input()          # reads the next line -> text, e.g. \"30\"\nage = int(age_text)         # convert that text to a real number -> 30\n\nprint(f\"Hi {name}, next year you turn {age + 1}.\")",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "Reads one line and returns it as a string; name becomes \"Sam\". The prompt argument, if any, is just a hint — not part of the value.",
          "label": "First input() → text",
          "lines": [
            1
          ]
        },
        {
          "note": "Each call reads the NEXT line in order; age_text becomes the text \"30\".",
          "label": "Second input() → next line",
          "lines": [
            2
          ]
        },
        {
          "note": "int(age_text) crosses the text \"30\" into the number 30, stored in age.",
          "label": "Convert the numeric one",
          "lines": [
            3
          ]
        },
        {
          "note": "Because age is a real number, age + 1 → 31 (on text it would error). The f-string drops name and 31 into the sentence.",
          "label": "Now arithmetic works",
          "lines": [
            5
          ]
        }
      ],
      "title": "Read · convert · use",
      "caption": "int(input()) is right; int(input() + 1) errors — it tries to + text and a number BEFORE converting. Convert first, then compute.",
      "filename": "reading_input.py",
      "language": "python",
      "subtitle": "Two lines come in as text; only the numeric one is converted. Watch where int() goes."
    },
    {
      "fix": "input() returns TEXT, so count is the string \"3\", not the number 3 — and  \"3\" + 1  cannot add a number to text, so Python raises a TypeError. Convert the input to a number first:  count = int(input()). Now count is the real number 3, count + 1 is 4, and the line prints \"You have 4 items.\" Numeric input must be passed through int() before you do any math with it.",
      "task": "Find why adding 1 to the count crashes.",
      "type": "debug",
      "symptom": "This should read a count and print \"You have 4 items.\" (3 typed, plus 1), but it crashes with a TypeError instead.",
      "language": "python",
      "brokenCode": "count = input()              # the user typed 3\nprint(f\"You have {count + 1} items.\")   # meant to add 1 -> errors"
    },
    {
      "type": "tradeoff",
      "question": "Math on raw input vs convert first",
      "optionA": {
        "label": "Raw input — crashes",
        "text": "count = input()        # \"3\", a string · count + 1 · # cannot + text and a number · TypeError Outcome: Input is still text"
      },
      "optionB": {
        "label": "Convert at the boundary",
        "text": "count = int(input())   # 3, a number · count + 1 · # real arithmetic · result: 4 Outcome: Convert the FIRST thing you do"
      },
      "guidance": "int(input()) is right; int(input() + 1) errors — it + text and a number before converting. Convert first, then compute."
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "age is the number 25; age + age is 50",
        "age is the text \"25\"; age + age is \"2525\" (text joins, it does not add)",
        "age is the number 25; age + age errors",
        "age is the text \"25\"; age + age is 50"
      ],
      "question": "A program does  age = input()  and the user types  25. What is the type and value of  age, and what does  age + age  produce?",
      "explanation": "input() always returns a string, so age is the text \"25\", not the number 25. And + on text joins (concatenates), so age + age is \"2525\", exactly like \"2\" + \"3\" was \"23\" in Lesson 3. To add the numbers you must convert first: int(age) + int(age) gives 50. Input is text until you convert it."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "Your program reads its values with input() — it does not hardcode the name or the number",
        "You converted the numeric input with int() (not left it as text)",
        "You built the output with an f-string, dropping name and count in with { }",
        "Your output is EXACTLY:  Sam, you have 3 items.",
        "You can explain why input() gives back text, and why count needs int() before any math"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In one sentence, what does input() do, and what TYPE does it always return?",
        "Why must you usually wrap numeric input in int() (or float()) before using it?",
        "What is the prompt argument in input(\"Name? \"), and is it part of the value you get back?",
        "How does this connect to the \"2\" + \"3\" vs 2 + 3 lesson from Numbers & Strings?"
      ]
    },
    {
      "text": "In the lab, make the program ask for something real: read a person’s name and their age, convert the age with int(), and print a line that uses the age in a calculation (years until they turn 100, say). Then change the provided input and re-run — watch the SAME program produce a new, correct answer for new data. That is the leap from a program that knows its answer to one that works for anyone. Next, your program will look at what it read and DECIDE — greet differently, allow or deny, grade the number — which is exactly what Booleans & Decisions is for.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A program that reads a name and a count from input, then prints one exact formatted line built from both — the numbers used as numbers, not text.",
        "Diagnose and repair the broken case: This should read a count and print \"You have 4 items.\" (3 typed, plus 1), but it crashes with a TypeError instead.",
        "Confirm every observable check: Your program reads its values with input() — it does not hardcode the name or the number · You converted the numeric input with int() (not left it as text) · You built the output with an f-string, dropping name and count in with { } · Your output is EXACTLY:  Sam, you have 3 items. · You can explain why input() gives back text, and why count needs int() before any math",
        "Complete the transfer without copying the worked example: In the lab, make the program ask for something real: read a person’s name and their age, convert the age with int(), and print a line that uses the age in a calculation (years until they turn 100, say). Then change the provided input and re-run — watch the SAME program produce a new, correct answer for new data. That is the leap from a program that knows its answer to one that works for anyone. Next, your program will look at what it read and DECIDE — greet differently, allow or deny, grade the number — which is exactly what Booleans & Decisions is for."
      ]
    }
  ],
  "booleans-and-logic": [
    {
      "time": "30–40 min",
      "type": "sprint-contract",
      "proof": "A program that prints ONE of several exact lines depending on a value — change the value, and a different (correct) branch runs.",
      "unlock": "Your program prints exactly the right line for the value it is given, via if / elif / else.",
      "outcome": "Make your program CHOOSE: compare values to get a True/False answer, combine conditions with and/or/not, and run different code with if / elif / else so the program reacts instead of always doing the same thing.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you \"understand if\" until you've run the SAME program with two different values and watched it take two different branches — one correct line each time."
    },
    {
      "text": "Until now every line ran, every time, in lockstep. Real programs decide: pass or fail, in stock or sold out, logged in or kicked out. Today you hand your program a fork in the road.",
      "type": "mission"
    },
    {
      "text": "Every app you use is built on decisions: show this if the password matches, warn if the field is empty. The skill is turning a question into a True/False value, then branching on it.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: you write  if age >= 18:  and age is 18. Does the line inside the if run — and is  age >= 18  asking a question or storing a value?",
      "reveal": "It runs. age >= 18 is a question — \"is age at least 18?\" — and it answers with a boolean: True or False. Here 18 >= 18 is True, so the indented block runs. Note >= uses two characters and includes \"equal to\", so 18 counts. A comparison does NOT store anything; it produces a True/False answer that if then acts on. That answer — that yes/no value — is the whole idea today."
    },
    {
      "type": "worked-example",
      "intro": "One value picks exactly one branch. Read it top to bottom, the way Python does — the first True condition wins.",
      "code": "temp = 50\n\nif temp > 85:\n    print(\"Hot\")          # skipped: 50 > 85 is False\nelif temp >= 60:\n    print(\"Comfortable\")  # skipped: 50 >= 60 is False\nelse:\n    print(\"Cold\")         # runs: nothing above matched -> Cold",
      "language": "python",
      "steps": [
        "The value under test: temp is 50. Python will check each condition against it in order.",
        "Check the if first: temp > 85 → 50 > 85 → False, so this block is skipped entirely.",
        "Then the elif: Only checked because the if was False. temp >= 60 → 50 >= 60 → False, so skipped too.",
        "else catches the rest: Nothing above matched, so else runs and prints Cold. Change temp to 90 and the very first branch wins instead."
      ],
      "commonMistake": "This should print Adult for age 20, but it prints nothing — or errors — instead."
    },
    {
      "text": "A comparison (==, !=, <, >, <=, >=) produces a boolean: True or False. if runs its block when its condition is True; elif checks the next only if earlier ones were False; else catches the rest. Python takes the FIRST True branch.",
      "type": "concept",
      "title": "Comparisons make booleans; if / elif / else branch on them"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "hot",
          "from": "q1",
          "kind": "control",
          "tone": "success",
          "label": "True"
        },
        {
          "to": "q2",
          "from": "q1",
          "kind": "control",
          "tone": "warning",
          "label": "False"
        },
        {
          "to": "comf",
          "from": "q2",
          "kind": "control",
          "tone": "success",
          "label": "True"
        },
        {
          "to": "cold",
          "from": "q2",
          "kind": "control",
          "tone": "warning",
          "label": "False"
        }
      ],
      "nodes": [
        {
          "id": "q1",
          "kind": "decision",
          "tone": "accent",
          "label": "temp > 85 ?",
          "description": "the if"
        },
        {
          "id": "hot",
          "kind": "process",
          "label": "print Hot",
          "description": "if-branch"
        },
        {
          "id": "q2",
          "kind": "decision",
          "tone": "accent",
          "label": "temp >= 60 ?",
          "description": "the elif"
        },
        {
          "id": "comf",
          "kind": "process",
          "label": "print Comfortable",
          "description": "elif-branch"
        },
        {
          "id": "cold",
          "kind": "process",
          "tone": "success",
          "label": "print Cold",
          "description": "else — the catch-all"
        }
      ],
      "title": "How if / elif / else flows",
      "legend": [
        {
          "tone": "accent",
          "label": "a comparison → True/False"
        },
        {
          "tone": "success",
          "label": "the branch that runs"
        },
        {
          "tone": "warning",
          "label": "condition was False — move on"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Conditions are checked top to bottom; the first True branch runs and the rest are skipped.",
      "storyboard": [
        {
          "say": "Execution arrives at the first question: is the temperature above 85? Python evaluates this comparison down to a single True or False.",
          "nodes": [
            "q1"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "If that comparison is True, the Hot branch runs immediately and every remaining check below is skipped.",
          "nodes": [
            "q1",
            "hot"
          ],
          "edges": [
            [
              "q1",
              "hot"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "If the first question was False, only then does Python fall through to the elif and ask the next question: is temp at least 60?",
          "nodes": [
            "q1",
            "q2"
          ],
          "edges": [
            [
              "q1",
              "q2"
            ]
          ],
          "ms": 4800
        },
        {
          "say": "When that second condition is True, Comfortable prints, and again the rest of the chain is bypassed.",
          "nodes": [
            "q2",
            "comf"
          ],
          "edges": [
            [
              "q2",
              "comf"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "If both questions came back False, the else catches everything left over and prints Cold, no condition required.",
          "nodes": [
            "q2",
            "cold"
          ],
          "edges": [
            [
              "q2",
              "cold"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "The core insight: checks run top to bottom, the first True branch wins, and exactly one path ever executes.",
          "nodes": [
            "q1",
            "q2",
            "hot",
            "comf",
            "cold"
          ],
          "edges": [],
          "ms": 5000
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "Single = — error",
        "lines": [
          "if temp = 50:",
          "= means \"store 50 into temp\"",
          "you cannot store inside an if",
          "SyntaxError"
        ],
        "verdict": "A store, not a question"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Double == — asks",
        "lines": [
          "if temp == 50:",
          "== asks \"are these equal?\"",
          "produces True or False",
          "the if can act on it"
        ],
        "verdict": "A real comparison"
      },
      "title": "= stores, == asks",
      "caption": "One equals stores, two equals asks. Combine conditions with and (both True), or (either True), not (flip).",
      "subtitle": "The single most common condition bug. One equals is not the other."
    },
    {
      "text": "In most languages, curly braces { } mark what is \"inside\" a branch. Python deletes the braces and uses the indentation itself — which means in Python, whitespace is not style, it is syntax. The colon at the end of the line opens the branch; the four-space indent underneath says \"these lines belong to it.\" A stray colon or a wrong indent does not look wrong, but it silently puts code in the wrong branch. When an if misbehaves, check the colon and the indentation before anything else.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "Grade: B",
      "title": "Grade the score",
      "starter": "# A score is stored for you below.\n# Print EXACTLY ONE line, decided by the score:\n#   score >= 90            -> print  Grade: A\n#   score >= 70 (but < 90) -> print  Grade: B\n#   anything below 70      -> print  Grade: C\n# Use if / elif / else. Do NOT print more than one line.\n\nscore = 84\n  # your code here\n",
      "summary": "A variable score is set to 84. Print EXACTLY ONE line: Grade: A if score is 90 or above, Grade: B if it is 70 to 89, and Grade: C if it is below 70. Use if / elif / else so only one line prints. For score = 84 the correct output is the single line: Grade: B.",
      "language": "python"
    },
    {
      "code": "temp = 50\n\nif temp > 85:\n    print(\"Hot\")          # skipped: 50 > 85 is False\nelif temp >= 60:\n    print(\"Comfortable\")  # skipped: 50 >= 60 is False\nelse:\n    print(\"Cold\")         # runs: nothing above matched -> Cold",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "temp is 50. Python will check each condition against it in order.",
          "label": "The value under test",
          "lines": [
            1
          ]
        },
        {
          "note": "temp > 85 → 50 > 85 → False, so this block is skipped entirely.",
          "label": "Check the if first",
          "lines": [
            3,
            4
          ]
        },
        {
          "note": "Only checked because the if was False. temp >= 60 → 50 >= 60 → False, so skipped too.",
          "label": "Then the elif",
          "lines": [
            5,
            6
          ]
        },
        {
          "note": "Nothing above matched, so else runs and prints Cold. Change temp to 90 and the very first branch wins instead.",
          "label": "else catches the rest",
          "lines": [
            7,
            8
          ]
        }
      ],
      "title": "One value picks exactly one branch",
      "caption": "Exactly ONE branch runs — the first True one, or else if none are True.",
      "filename": "decide.py",
      "language": "python",
      "subtitle": "Read it top to bottom, the way Python does — the first True condition wins."
    },
    {
      "fix": "The condition uses a single =, which means \"store 18 into age\" — but you cannot store inside an if, so Python errors. A condition must ASK a question, which needs a comparison. Here you want \"is age at least 18?\", so write  if age >= 18:. (A single == would test \"exactly 18\"; >= is the right test for \"adult\".) Now the block runs and prints Adult.",
      "task": "Find why this if does not work.",
      "type": "debug",
      "symptom": "This should print Adult for age 20, but it prints nothing — or errors — instead.",
      "language": "python",
      "brokenCode": "age = 20\nif age = 18:\n    print(\"Adult\")"
    },
    {
      "type": "tradeoff",
      "question": "= stores, == asks",
      "optionA": {
        "label": "Single = — error",
        "text": "if temp = 50: · = means \"store 50 into temp\" · you cannot store inside an if · SyntaxError Outcome: A store, not a question"
      },
      "optionB": {
        "label": "Double == — asks",
        "text": "if temp == 50: · == asks \"are these equal?\" · produces True or False · the if can act on it Outcome: A real comparison"
      },
      "guidance": "One equals stores, two equals asks. Combine conditions with and (both True), or (either True), not (flip)."
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "big — the first branch always runs",
        "medium — x > 10 is False, but x > 3 is True, and it is the first True branch",
        "small — none of the conditions are True",
        "medium and small — every matching branch runs"
      ],
      "question": "With  x = 5, what does this print?  if x > 10: print(\"big\")  elif x > 3: print(\"medium\")  else: print(\"small\")",
      "explanation": "Python checks top to bottom and runs the FIRST branch whose condition is True. x > 10 is False (5 is not over 10), x > 3 is True (5 is over 3), so it prints medium and stops — the else is never reached. Only one branch ever runs."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "You used a comparison (==, !=, <, >, <=, or >=) to produce a True/False answer",
        "You wrote if / elif / else with a colon on each line and the body indented underneath",
        "Your program printed EXACTLY one line for the given value (Grade: B for score 84)",
        "You changed the value at least once, re-ran, and watched a DIFFERENT correct branch run",
        "You can explain why == (compare) is not the same as = (store)"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In one sentence, what is a boolean?",
        "What is the difference between = and == , and what happens if you use = in a condition?",
        "When Python has an if, an elif, and an else, how does it decide which block runs?",
        "What do and / or / not each do to True/False values?"
      ]
    },
    {
      "text": "In the lab, model a real rule from your life as a decision: a budget check (print \"Over budget\" or \"OK\"), a sign-in check (right name AND right code with and), or a weather call. Set the input value, run it, then change the value so a different branch wins. Making a program react to its input is the core of every interactive thing you will ever build.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Produce and run the required artifact: A program that prints ONE of several exact lines depending on a value — change the value, and a different (correct) branch runs.",
        "Diagnose and repair the broken case: This should print Adult for age 20, but it prints nothing — or errors — instead.",
        "Confirm every observable check: You used a comparison (==, !=, <, >, <=, or >=) to produce a True/False answer · You wrote if / elif / else with a colon on each line and the body indented underneath · Your program printed EXACTLY one line for the given value (Grade: B for score 84) · You changed the value at least once, re-ran, and watched a DIFFERENT correct branch run · You can explain why == (compare) is not the same as = (store)",
        "Complete the transfer without copying the worked example: In the lab, model a real rule from your life as a decision: a budget check (print \"Over budget\" or \"OK\"), a sign-in check (right name AND right code with and), or a weather call. Set the input value, run it, then change the value so a different branch wins. Making a program react to its input is the core of every interactive thing you will ever build."
      ]
    }
  ],
  "lists": [
    {
      "time": "30–40 min",
      "type": "sprint-contract",
      "proof": "A program that builds or modifies a list and prints a required value from it (a specific item and the list’s length).",
      "unlock": "Your list contains the right items and your program prints the correct value(s) from it.",
      "outcome": "Hold many values under one name with a list: create one, read an item by its position, measure it with len(), add to it with append(), and ask whether something is in it.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you \"know lists\" until you've read an item by its index AND changed the list’s length with append, then printed both — not just typed a list literal."
    },
    {
      "text": "Every variable so far holds one value — useless for fifty. Nobody writes score1, score2 … score50. A list lets one name hold a whole sequence in order: grow it, read any slot, count it.",
      "type": "mission"
    },
    {
      "text": "A todo app holds a list of tasks; a cart a list of items; a chart a list of numbers. Lists are how programs handle \"many of something\" — and next lesson, loops act on every item at once.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: you have  fruits = [\"apple\", \"banana\", \"cherry\"]. Which one does  fruits[0]  give you — and why might that surprise you?",
      "reveal": "fruits[0] is \"apple\" — the FIRST item. Positions (indexes) start at 0, not 1: index 0 is the first, 1 is the second, 2 is the third. So the last item of a 3-item list is at index 2, not 3. Counting from zero trips up every beginner once; after that it is automatic. The number in the [ ] is \"how many steps from the start\", and the start is zero steps away."
    },
    {
      "type": "worked-example",
      "intro": "Build · read · measure · grow. Watch the length change as the list grows in place.",
      "code": "names = [\"Ann\", \"Ben\"]   # a list of two strings, in order\nprint(names[0])          # index 0 = the first item -> Ann\nprint(len(names))        # how many items -> 2\n\nnames.append(\"Cy\")       # add \"Cy\" to the END (changes names in place)\nprint(len(names))        # the list grew -> 3\nprint(names[2])          # the new last item is at index 2 -> Cy",
      "language": "python",
      "steps": [
        "Create with [ ]: Two items in order: \"Ann\" at index 0, \"Ben\" at index 1.",
        "Read by index: names[0] reads position 0 → \"Ann\". Indexes start at zero.",
        "Count with len(): len(names) → 2, the number of items.",
        "append grows it in place: The dot means append is something the list does to itself; it adds \"Cy\" to the END and changes the list — it does not return a new one.",
        "The list is now longer: len(names) → 3, and names[2] (the new last slot) is \"Cy\". The last valid index is always len - 1."
      ],
      "commonMistake": "This should print the last color, \"green\", but it errors with \"list index out of range\"."
    },
    {
      "text": "Write values in square brackets: scores = [90, 75, 88]. Reach one by index (starting at 0): scores[0] is 90. Count with len(scores). Grow with scores.append(100) — it changes the list in place. Test membership with 75 in scores.",
      "type": "concept",
      "title": "A list holds many values in order, reachable by index"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "i1",
          "from": "i0",
          "kind": "data",
          "label": "next"
        },
        {
          "to": "i2",
          "from": "i1",
          "kind": "data",
          "label": "next"
        }
      ],
      "nodes": [
        {
          "id": "i0",
          "kind": "store",
          "tone": "accent",
          "label": "Ann",
          "description": "index 0 — first"
        },
        {
          "id": "i1",
          "kind": "store",
          "label": "Ben",
          "description": "index 1"
        },
        {
          "id": "i2",
          "kind": "store",
          "tone": "success",
          "label": "Cy",
          "description": "index 2 — last (len - 1)"
        }
      ],
      "title": "A list is slots numbered from zero",
      "legend": [
        {
          "tone": "accent",
          "label": "names[0] — the first item"
        },
        {
          "tone": "success",
          "label": "names[-1] — always the last item"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Each value sits in a numbered slot. The index is \"steps from the start\" — and the start is zero steps away.",
      "storyboard": [
        {
          "say": "Every value so far needed its own name — but nobody writes score1 through score50. One list holds a whole sequence in order.",
          "nodes": [
            "i0",
            "i1",
            "i2"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "The list starts at slot zero. Here Ann sits at index 0 — the very first value, zero steps from the start.",
          "nodes": [
            "i0"
          ],
          "edges": [],
          "ms": 4000
        },
        {
          "say": "The next slot follows in order: Ben lives at index 1, one step past the start.",
          "nodes": [
            "i0",
            "i1"
          ],
          "edges": [
            [
              "i0",
              "i1"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Follow the chain again and Cy sits at index 2 — position is just steps from the beginning.",
          "nodes": [
            "i1",
            "i2"
          ],
          "edges": [
            [
              "i1",
              "i2"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Reach any one by index: scores[0] is the first. Count them all with len, which gives three here.",
          "nodes": [
            "i0",
            "i1",
            "i2"
          ],
          "edges": [
            [
              "i0",
              "i1"
            ],
            [
              "i1",
              "i2"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "Grow it in place with append, and test membership with 'in' — one name now handles many of something.",
          "nodes": [
            "i0",
            "i1",
            "i2"
          ],
          "edges": [],
          "ms": 4400
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "Hardcoded position — fragile",
        "lines": [
          "colors = [\"red\", \"blue\", \"green\"]",
          "colors[3]  # guessing the last index",
          "list has indexes 0, 1, 2 only",
          "IndexError: list index out of range"
        ],
        "verdict": "Breaks the moment the length changes"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Negative index — robust",
        "lines": [
          "colors = [\"red\", \"blue\", \"green\"]",
          "colors[-1]  # count from the end",
          "-1 is always the final item",
          "result: green"
        ],
        "verdict": "Always the last, whatever the length"
      },
      "title": "Index from the front vs from the end",
      "caption": "For \"the last one\", list[-1] beats guessing the number. Use \"in\" to test membership: \"milk\" in cart.",
      "subtitle": "Two ways to grab the last item of a list. One breaks when the length changes."
    },
    {
      "text": "Indexes start at 0, so the last item is at len(list) - 1, never len(list). If a list has 5 items, the valid indexes are 0 through 4 — asking for [5] errors. When you want \"the last one\", a clean trick is list[-1]: negative indexes count from the end, so [-1] is always the final item no matter how long the list is.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "milk\n4",
      "title": "Grow a cart and read it",
      "starter": "# Below is a list of three cart items.\n# 1. Add  \"eggs\"  to the END of the list (use .append).\n# 2. Print the FIRST item in the list (index 0).\n# 3. Print how many items are in the list now (use len()).\n# Expected output, two lines:\n#   milk\n#   4\n\ncart = [\"milk\", \"bread\", \"apples\"]\n  # your code here\n",
      "summary": "Start with cart = [\"milk\", \"bread\", \"apples\"]. Append \"eggs\" to the end with cart.append(\"eggs\"), then print the first item with cart[0], then print the number of items with len(cart). The output must be exactly two lines: milk on the first line, then 4 on the second (the list has 4 items after the append).",
      "language": "python"
    },
    {
      "code": "names = [\"Ann\", \"Ben\"]   # a list of two strings, in order\nprint(names[0])          # index 0 = the first item -> Ann\nprint(len(names))        # how many items -> 2\n\nnames.append(\"Cy\")       # add \"Cy\" to the END (changes names in place)\nprint(len(names))        # the list grew -> 3\nprint(names[2])          # the new last item is at index 2 -> Cy",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "Two items in order: \"Ann\" at index 0, \"Ben\" at index 1.",
          "label": "Create with [ ]",
          "lines": [
            1
          ]
        },
        {
          "note": "names[0] reads position 0 → \"Ann\". Indexes start at zero.",
          "label": "Read by index",
          "lines": [
            2
          ]
        },
        {
          "note": "len(names) → 2, the number of items.",
          "label": "Count with len()",
          "lines": [
            3
          ]
        },
        {
          "note": "The dot means append is something the list does to itself; it adds \"Cy\" to the END and changes the list — it does not return a new one.",
          "label": "append grows it in place",
          "lines": [
            5
          ]
        },
        {
          "note": "len(names) → 3, and names[2] (the new last slot) is \"Cy\". The last valid index is always len - 1.",
          "label": "The list is now longer",
          "lines": [
            6,
            7
          ]
        }
      ],
      "title": "Build · read · measure · grow",
      "caption": "names[3] on a 3-item list errors (\"list index out of range\") — valid indexes are 0, 1, 2.",
      "filename": "lists.py",
      "language": "python",
      "subtitle": "Watch the length change as the list grows in place."
    },
    {
      "fix": "The list has 3 items, so the valid indexes are 0, 1, and 2 — there is no index 3. Because counting starts at 0, the LAST item lives at len(list) - 1, which is 2 here. Use  print(colors[2])  to get \"green\" — or, more robust, print(colors[-1]), which always grabs the final item.",
      "task": "Find why index 3 fails on this list.",
      "type": "debug",
      "symptom": "This should print the last color, \"green\", but it errors with \"list index out of range\".",
      "language": "python",
      "brokenCode": "colors = [\"red\", \"blue\", \"green\"]\nprint(colors[3])     # meant to print the last one"
    },
    {
      "type": "tradeoff",
      "question": "Index from the front vs from the end",
      "optionA": {
        "label": "Hardcoded position — fragile",
        "text": "colors = [\"red\", \"blue\", \"green\"] · colors[3]  # guessing the last index · list has indexes 0, 1, 2 only · IndexError: list index out of range Outcome: Breaks the moment the length changes"
      },
      "optionB": {
        "label": "Negative index — robust",
        "text": "colors = [\"red\", \"blue\", \"green\"] · colors[-1]  # count from the end · -1 is always the final item · result: green Outcome: Always the last, whatever the length"
      },
      "guidance": "For \"the last one\", list[-1] beats guessing the number. Use \"in\" to test membership: \"milk\" in cart."
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "len is 2 and nums[1] is 30 — append replaced an item",
        "len is 3 and nums[1] is 20 — append added to the end, the middle item is unchanged",
        "len is 3 and nums[1] is 30 — append inserts at the front",
        "Error — you can't append to a list that already has items"
      ],
      "question": "After  nums = [10, 20]  then  nums.append(30), what do  len(nums)  and  nums[1]  give?",
      "explanation": "append always adds to the END, so nums becomes [10, 20, 30]: length 3, with 30 at index 2. nums[1] is still 20 — appending does not disturb the existing items or their positions. The new value lands in the new last slot."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "You created a list with square brackets and comma-separated values",
        "You read an item by its index, remembering that the first item is [0]",
        "You used len() to count the items",
        "You used .append() to add an item and saw the length increase",
        "Your output is exactly two lines: milk then 4"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In one sentence, what is a list and why is it better than ten separate variables?",
        "Why is the first item at index 0, and what index holds the last item of a 4-item list?",
        "What does .append() do, and where does the new item go?",
        "How would you check whether \"milk\" is somewhere in a list?"
      ]
    },
    {
      "text": "In the lab, build a list that means something to you — songs, tasks, weekly expenses. Print the first one with [0] and the last one with [-1], count them with len(), then append a new one and print the count again. You have just done the create → read → measure → grow cycle that sits under every list-backed feature you will build.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A program that builds or modifies a list and prints a required value from it (a specific item and the list’s length).",
        "Diagnose and repair the broken case: This should print the last color, \"green\", but it errors with \"list index out of range\".",
        "Confirm every observable check: You created a list with square brackets and comma-separated values · You read an item by its index, remembering that the first item is [0] · You used len() to count the items · You used .append() to add an item and saw the length increase · Your output is exactly two lines: milk then 4",
        "Complete the transfer without copying the worked example: In the lab, build a list that means something to you — songs, tasks, weekly expenses. Print the first one with [0] and the last one with [-1], count them with len(), then append a new one and print the count again. You have just done the create → read → measure → grow cycle that sits under every list-backed feature you will build."
      ]
    }
  ],
  "loops": [
    {
      "time": "35–45 min",
      "type": "sprint-contract",
      "proof": "A program that loops over a list and prints a single computed result — a total or a count — that is only correct if the loop actually ran over every item.",
      "unlock": "Your loop visits every item and your program prints the correct accumulated result.",
      "outcome": "Do something to every item in a sequence without copy-paste: loop over a list with for, repeat an action a fixed number of times with range(), and build up a result (a total or a count) in a variable as you go.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you \"understand loops\" until your program has built a running total or count across a list and printed the right answer — adding a list by hand is not looping."
    },
    {
      "text": "Last lesson you built a list that can hold a hundred values. Picture writing a hundred lines to add them up. That is not programming; that is typing. A loop says \"do this for every item\" exactly once.",
      "type": "mission"
    },
    {
      "text": "Looping is how every program handles \"for each\": total each line of a receipt, email each user, draw each enemy. Bring along the if from Lesson 5 and you can count, filter, and decide across a whole dataset.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: you want to add up [10, 20, 30]. You write  total = 0  before a loop that does  total = total + price  for each price. Why does total have to start at 0 BEFORE the loop, not inside it?",
      "reveal": "Because the loop body runs once per item, and you want to keep adding onto the same running total. If total = 0 were inside the loop, it would reset to 0 on every pass and wipe out your progress — you would end with just the last value. Setting it to 0 once, before the loop, gives you an empty bucket; each pass pours one more value in. Start outside, accumulate inside."
    },
    {
      "type": "worked-example",
      "intro": "A running total, pass by pass. The bucket is created once; each pass pours one more value in.",
      "code": "nums = [4, 6, 10]\ntotal = 0                # the bucket, created ONCE before the loop\n\nfor n in nums:           # n becomes 4, then 6, then 10\n    total = total + n    # add the current item onto the running total\n\nprint(total)             # after all passes -> 20",
      "language": "python",
      "steps": [
        "Create the accumulator first: total = 0 runs ONCE, before the loop — the empty bucket.",
        "The loop variable changes each pass: n becomes 4, then 6, then 10 — the next item of the list on every pass.",
        "Pass 1 and 2: n=4: total = 0 + 4 → 4. n=6: total = 4 + 6 → 10. The body re-runs with the new n.",
        "Pass 3: n=10: total = 10 + 10 → 20. Each pass builds on the last because total lives OUTSIDE the loop.",
        "After the loop: print(total) shows 20 — the sum built one item at a time."
      ],
      "commonMistake": "This should add [5, 5, 5] to get 15, but it prints 5."
    },
    {
      "text": "for n in nums: runs the indented body once per item, with n set to each value in turn. The accumulator pattern: create a result (total = 0) BEFORE the loop, update it (total = total + n) INSIDE — when the loop ends, it holds the answer.",
      "type": "concept",
      "title": "A for-loop repeats a block once per item"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "loop",
          "from": "init",
          "kind": "data",
          "tone": "accent",
          "label": "empty bucket"
        },
        {
          "to": "update",
          "from": "loop",
          "kind": "control",
          "label": "each n"
        },
        {
          "to": "loop",
          "from": "update",
          "kind": "control",
          "label": "next item",
          "dashed": true
        },
        {
          "to": "result",
          "from": "loop",
          "kind": "data",
          "tone": "success",
          "label": "when done"
        }
      ],
      "nodes": [
        {
          "id": "init",
          "kind": "process",
          "tone": "accent",
          "label": "total = 0",
          "description": "before the loop — once"
        },
        {
          "id": "loop",
          "kind": "decision",
          "label": "for n in nums",
          "description": "visit each item"
        },
        {
          "id": "update",
          "kind": "process",
          "label": "total = total + n",
          "description": "inside — every pass"
        },
        {
          "id": "result",
          "kind": "store",
          "tone": "success",
          "label": "total = 20",
          "description": "after the loop"
        }
      ],
      "title": "Accumulate inside, initialise outside",
      "legend": [
        {
          "tone": "accent",
          "label": "set up once, outside"
        },
        {
          "tone": "success",
          "label": "the answer, after the loop"
        }
      ],
      "rankdir": "LR",
      "subtitle": "The accumulator is set up once, then the loop body updates the SAME bucket on every pass.",
      "storyboard": [
        {
          "say": "Before the loop even starts, we set up one bucket: total = 0. This happens once, outside the repetition.",
          "nodes": [
            "init"
          ],
          "edges": [],
          "ms": 3600
        },
        {
          "say": "We hand the empty bucket to the loop and start marching through nums, one value at a time.",
          "nodes": [
            "init",
            "loop"
          ],
          "edges": [
            [
              "init",
              "loop"
            ]
          ],
          "ms": 3800
        },
        {
          "say": "For each item, n takes the next value and we drop into the loop body to do the work.",
          "nodes": [
            "loop",
            "update"
          ],
          "edges": [
            [
              "loop",
              "update"
            ]
          ],
          "ms": 4000
        },
        {
          "say": "Inside, we update the SAME bucket: total = total + n. We never make a new total, we add onto it.",
          "nodes": [
            "update"
          ],
          "edges": [],
          "ms": 4600
        },
        {
          "say": "Then we loop back for the next item, and the bucket carries its running value forward each pass.",
          "nodes": [
            "update",
            "loop"
          ],
          "edges": [
            [
              "update",
              "loop"
            ]
          ],
          "ms": 4000
        },
        {
          "say": "When nums runs out, the loop exits and the bucket holds the finished answer: total = 20.",
          "nodes": [
            "loop",
            "result"
          ],
          "edges": [
            [
              "loop",
              "result"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "That is the accumulator pattern: initialise outside once, accumulate inside every pass, read the result when done.",
          "nodes": [
            "init",
            "update",
            "result"
          ],
          "edges": [],
          "ms": 4800
        }
      ]
    },
    {
      "left": {
        "tone": "success",
        "label": "for item in list — the value",
        "lines": [
          "for score in scores:",
          "    if score >= 60:",
          "        passes = passes + 1",
          "# reads like English; you get each value"
        ],
        "verdict": "Default when you want each item"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "accent",
        "label": "for i in range(n) — a count",
        "lines": [
          "for i in range(5):",
          "    print(i)",
          "# runs 5 times: i = 0,1,2,3,4",
          "# use when you need the position / a count"
        ],
        "verdict": "When you need to repeat N times"
      },
      "title": "for item vs for i in range",
      "caption": "A loop and an if together count matches: loop the scores, test each with if, bump a counter — passes ends at 2.",
      "subtitle": "Two loop shapes. Reach for the one that fits what you need."
    },
    {
      "text": "Reach for  for item in list  when you want each VALUE, and  for i in range(n)  when you want to do something a set number of times or need the position number. Beginners often default to range() with indexing (for i in range(len(list)): ... list[i]) — it works, but \"for item in list\" is cleaner and reads like English. Use indexes only when you actually need the index.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "60",
      "title": "Total the prices",
      "starter": "# Below is a list of prices.\n# Add them all up and print the TOTAL on its own line.\n# Use a running-total variable that starts at 0, then a for-loop\n# that adds each price to it. Do NOT type the answer in by hand.\n# Expected output (one line):\n#   60\n\nprices = [10, 25, 5, 20]\ntotal = 0\n  # your code here\n",
      "summary": "You are given prices = [10, 25, 5, 20] and total = 0. Write a for-loop that adds each price to total, then print total on its own line. The output must be the single line 60 (10 + 25 + 5 + 20). The total must be computed by the loop — do not just print 60.",
      "language": "python"
    },
    {
      "code": "nums = [4, 6, 10]\ntotal = 0                # the bucket, created ONCE before the loop\n\nfor n in nums:           # n becomes 4, then 6, then 10\n    total = total + n    # add the current item onto the running total\n\nprint(total)             # after all passes -> 20",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "total = 0 runs ONCE, before the loop — the empty bucket.",
          "label": "Create the accumulator first",
          "lines": [
            2
          ]
        },
        {
          "note": "n becomes 4, then 6, then 10 — the next item of the list on every pass.",
          "label": "The loop variable changes each pass",
          "lines": [
            4
          ]
        },
        {
          "note": "n=4: total = 0 + 4 → 4. n=6: total = 4 + 6 → 10. The body re-runs with the new n.",
          "label": "Pass 1 and 2",
          "lines": [
            5
          ]
        },
        {
          "note": "n=10: total = 10 + 10 → 20. Each pass builds on the last because total lives OUTSIDE the loop.",
          "label": "Pass 3",
          "lines": [
            5
          ]
        },
        {
          "note": "print(total) shows 20 — the sum built one item at a time.",
          "label": "After the loop",
          "lines": [
            7
          ]
        }
      ],
      "title": "A running total, pass by pass",
      "caption": "Put total = 0 inside the loop and it resets every pass, leaving only the last item (10). Start outside, accumulate inside.",
      "filename": "loops.py",
      "language": "python",
      "subtitle": "The bucket is created once; each pass pours one more value in."
    },
    {
      "fix": "total = 0 is INSIDE the loop, so it resets to 0 on every pass — wiping out everything added before. Each pass becomes 0 + n, so you keep only the last item. Move  total = 0  ABOVE the loop (run it once), and leave only  total = total + n  inside. Now total accumulates: 5 → 10 → 15.",
      "task": "Find why only the last number survives.",
      "type": "debug",
      "symptom": "This should add [5, 5, 5] to get 15, but it prints 5.",
      "language": "python",
      "brokenCode": "nums = [5, 5, 5]\nfor n in nums:\n    total = 0            # meant to total the list\n    total = total + n\nprint(total)             # prints 5, not 15"
    },
    {
      "type": "tradeoff",
      "question": "for item vs for i in range",
      "optionA": {
        "label": "for item in list — the value",
        "text": "for score in scores: ·     if score >= 60: ·         passes = passes + 1 · # reads like English; you get each value Outcome: Default when you want each item"
      },
      "optionB": {
        "label": "for i in range(n) — a count",
        "text": "for i in range(5): ·     print(i) · # runs 5 times: i = 0,1,2,3,4 · # use when you need the position / a count Outcome: When you need to repeat N times"
      },
      "guidance": "A loop and an if together count matches: loop the scores, test each with if, bump a counter — passes ends at 2."
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "3 times, with i ending at 3",
        "3 times, printing 0, 1, 2 — range(3) gives 0 up to but not including 3",
        "4 times, printing 0, 1, 2, 3",
        "1 time, because range(3) is a single value"
      ],
      "question": "How many times does the body run, and what is the last value of i, in:  for i in range(3): print(i)",
      "explanation": "range(3) produces 0, 1, 2 — it starts at 0 and stops BEFORE 3, so the body runs 3 times and the last i is 2. \"Up to but not including the number\" is how range works, which also means range(len(list)) lines up perfectly with the list’s valid indexes."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "You wrote a for-loop with a colon and an indented body",
        "You created your accumulator (total = 0) BEFORE the loop, not inside it",
        "You updated the accumulator inside the loop (total = total + price)",
        "Your output is the single line 60, computed by the loop (not typed in)",
        "You can explain why the accumulator resets if you put it inside the loop"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In one sentence, what does a for-loop do?",
        "What is the difference between  for item in list  and  for i in range(n)?",
        "Why must a running total be set to 0 before the loop instead of inside it?",
        "How would you count how many items in a list pass a test (combine a loop with an if)?"
      ]
    },
    {
      "text": "In the lab, take a list that matters to you — daily steps, expenses, quiz scores — and use a loop to compute something real: the total, the average (total divided by len), or a count of items over a threshold using an if inside the loop. The \"set up an accumulator, loop, update it, print the result\" pattern is one of the most reused moves in all of programming.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A program that loops over a list and prints a single computed result — a total or a count — that is only correct if the loop actually ran over every item.",
        "Diagnose and repair the broken case: This should add [5, 5, 5] to get 15, but it prints 5.",
        "Confirm every observable check: You wrote a for-loop with a colon and an indented body · You created your accumulator (total = 0) BEFORE the loop, not inside it · You updated the accumulator inside the loop (total = total + price) · Your output is the single line 60, computed by the loop (not typed in) · You can explain why the accumulator resets if you put it inside the loop",
        "Complete the transfer without copying the worked example: In the lab, take a list that matters to you — daily steps, expenses, quiz scores — and use a loop to compute something real: the total, the average (total divided by len), or a count of items over a threshold using an if inside the loop. The \"set up an accumulator, loop, update it, print the result\" pattern is one of the most reused moves in all of programming."
      ]
    }
  ],
  "functions-basics": [
    {
      "time": "35–45 min",
      "type": "sprint-contract",
      "proof": "A program that defines a function, calls it with arguments, and prints a computed result that the function returned.",
      "unlock": "Your function takes inputs, returns the right value, and your program prints it via that returned value.",
      "outcome": "Define your own function with def, give it inputs (parameters), send a value back with return, and call it to reuse a block of code — so you stop copy-pasting the same steps and start naming them.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you \"understand functions\" until your program has DEFINED a function, CALLED it with arguments, and used the value it RETURNED — printing inside the function without ever returning is not the whole skill."
    },
    {
      "text": "Look back: a loop that totals a list, an if that grades a score. Useful — but each lives in one spot. A function lets you name a block of code once and run it on demand, with different inputs every time.",
      "type": "mission"
    },
    {
      "text": "Functions are how every program stays manageable: small named pieces — calculate_total, is_valid — snapped together. Naming a step makes code readable; reusing it kills copy-paste bugs. Every library you import is just someone else's functions.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: a function does  print(unit_price * qty)  but has no return. You write  bill = total_price(7, 6)  and then  print(bill). What ends up in  bill — the number 42, or something else?",
      "reveal": "bill is None — NOT 42. print() shows a value on screen but does not hand it BACK to the caller. To get a value out of a function so you can store and reuse it, the function must return it. A function that only prints can show you something; a function that returns gives you something. This difference — print vs return — is the single most important idea in this lesson."
    },
    {
      "type": "worked-example",
      "intro": "Define once, call many. Watch the arguments fill the parameters and the value come back to the caller.",
      "code": "def area(width, height):     # width and height are parameters (inputs)\n    return width * height    # hand the result back to the caller\n\nroom = area(3, 4)            # arguments 3, 4 fill width, height -> returns 12\nprint(room)                  # -> 12\n\nprint(area(10, 2))           # call again with new inputs -> returns 20",
      "language": "python",
      "steps": [
        "def names the function: Two parameters, width and height — placeholders for the inputs the caller supplies. Nothing runs yet; defining is not calling.",
        "return hands a value back: Computes width * height and sends it to whoever called the function, ending it.",
        "Calling fills the parameters: area(3, 4): the arguments 3 and 4 fill width and height in order; the call becomes the returned 12.",
        "Store and use the result: room = area(3, 4) stores 12, so print(room) shows 12. The call evaluates to its return value.",
        "Reuse with new inputs: area(10, 2) runs the SAME function again → 20. One definition, many calls — the whole point."
      ],
      "commonMistake": "This should print 42, but it prints None instead."
    },
    {
      "text": "def total_price(unit_price, qty): names a function; the names in parentheses are parameters. return sends a value back to the caller and ends the function. Call it with arguments — total_price(7, 6) — and the call becomes whatever was returned.",
      "type": "concept",
      "title": "def names a block; parameters feed it; return hands a value back"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "params",
          "from": "call",
          "kind": "data",
          "tone": "accent",
          "label": "arguments in"
        },
        {
          "to": "body",
          "from": "params",
          "kind": "control",
          "label": "run"
        },
        {
          "to": "ret",
          "from": "body",
          "kind": "data",
          "label": "compute"
        },
        {
          "to": "call",
          "from": "ret",
          "kind": "data",
          "tone": "success",
          "label": "call becomes 12"
        }
      ],
      "nodes": [
        {
          "id": "call",
          "kind": "client",
          "tone": "accent",
          "label": "area(3, 4)",
          "description": "the caller, with arguments"
        },
        {
          "id": "params",
          "kind": "process",
          "label": "width=3, height=4",
          "description": "arguments fill parameters"
        },
        {
          "id": "body",
          "kind": "process",
          "label": "width * height",
          "description": "the body runs"
        },
        {
          "id": "ret",
          "kind": "store",
          "tone": "success",
          "label": "return 12",
          "description": "value flows back"
        }
      ],
      "title": "How a function call works",
      "legend": [
        {
          "tone": "accent",
          "label": "arguments flow in"
        },
        {
          "tone": "success",
          "label": "return flows the result back"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Arguments flow in to fill the parameters; the function runs; return flows the result back out.",
      "storyboard": [
        {
          "say": "It starts with a call: area(3, 4). You name the function and hand it two arguments, 3 and 4.",
          "nodes": [
            "call"
          ],
          "edges": [],
          "ms": 3600
        },
        {
          "say": "Those arguments flow in to fill the parameters — width becomes 3, height becomes 4.",
          "nodes": [
            "call",
            "params"
          ],
          "edges": [
            [
              "call",
              "params"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "With its parameters filled, the function body runs the code you named once and can reuse anywhere.",
          "nodes": [
            "params",
            "body"
          ],
          "edges": [
            [
              "params",
              "body"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "The body computes width times height, using whatever inputs it was given this time.",
          "nodes": [
            "body"
          ],
          "edges": [
            [
              "params",
              "body"
            ]
          ],
          "ms": 3800
        },
        {
          "say": "Then return sends the result back — return 12 hands a value to the caller and ends the function.",
          "nodes": [
            "body",
            "ret"
          ],
          "edges": [
            [
              "body",
              "ret"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "The result flows back out, and the call itself becomes 12 — that's the whole loop of a function.",
          "nodes": [
            "ret",
            "call"
          ],
          "edges": [
            [
              "ret",
              "call"
            ]
          ],
          "ms": 4600
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "Only prints — bill is None",
        "lines": [
          "def total_price(u, q):",
          "    print(u * q)   # shows 42 on screen",
          "bill = total_price(7, 6)",
          "print(bill)        # None — nothing was handed back"
        ],
        "verdict": "Shows a value, gives back nothing"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Returns — bill is 42",
        "lines": [
          "def total_price(u, q):",
          "    return u * q   # hands 42 back",
          "bill = total_price(7, 6)",
          "print(bill)        # 42 — stored and reusable"
        ],
        "verdict": "The caller can store and reuse it"
      },
      "title": "print shows; return gives back",
      "caption": "Does the caller need to USE the result? return it. Does it only need to appear on screen? print it.",
      "subtitle": "Same function, one difference — only one lets the caller USE the result."
    },
    {
      "text": "print shows a value to a human; return hands a value to the rest of your program. Ask yourself: does the caller need to USE this result (store it, do math on it, pass it on)? Then return it. Does it only need to appear on screen right now? Then print it. Most reusable functions return — and the caller decides whether to print. Mixing these up is the most common beginner function bug.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "6 items = $42",
      "title": "Write and call a function",
      "starter": "import json\nimport sys\n\n# Define total_price(unit_price, qty) so it RETURNS the computed total.\ndef total_price(unit_price, qty):\n    ...  # your code here\n\n# The browser uses the visible example. The controlled evaluator supplies other\n# JSON cases over stdin, so hardcoding 42 cannot pass mastery.\nraw = sys.stdin.read().strip()\ncase = json.loads(raw) if raw else {\"unitPrice\": 7, \"qty\": 6}\nbill = total_price(case[\"unitPrice\"], case[\"qty\"])\nprint(f\"{case['qty']} items = ${bill}\")\n",
      "summary": "Define total_price(unit_price, qty) so it returns unit_price × qty. The visible example is 7 × 6, while the controlled evaluator supplies different JSON inputs over stdin. Print the quantity and computed return value; a hardcoded 42 will fail the hidden cases.",
      "language": "python"
    },
    {
      "code": "def area(width, height):     # width and height are parameters (inputs)\n    return width * height    # hand the result back to the caller\n\nroom = area(3, 4)            # arguments 3, 4 fill width, height -> returns 12\nprint(room)                  # -> 12\n\nprint(area(10, 2))           # call again with new inputs -> returns 20",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "Two parameters, width and height — placeholders for the inputs the caller supplies. Nothing runs yet; defining is not calling.",
          "label": "def names the function",
          "lines": [
            1
          ]
        },
        {
          "note": "Computes width * height and sends it to whoever called the function, ending it.",
          "label": "return hands a value back",
          "lines": [
            2
          ]
        },
        {
          "note": "area(3, 4): the arguments 3 and 4 fill width and height in order; the call becomes the returned 12.",
          "label": "Calling fills the parameters",
          "lines": [
            4
          ]
        },
        {
          "note": "room = area(3, 4) stores 12, so print(room) shows 12. The call evaluates to its return value.",
          "label": "Store and use the result",
          "lines": [
            4,
            5
          ]
        },
        {
          "note": "area(10, 2) runs the SAME function again → 20. One definition, many calls — the whole point.",
          "label": "Reuse with new inputs",
          "lines": [
            7
          ]
        }
      ],
      "title": "Define once, call many",
      "caption": "Defining a function does NOT run it; only calling it does.",
      "filename": "functions.py",
      "language": "python",
      "subtitle": "Watch the arguments fill the parameters and the value come back to the caller."
    },
    {
      "fix": "The line  unit_price * qty  computes 42 but throws it away — the function never returns anything, so the call hands back None. Add return:  return unit_price * qty. Now total_price(7, 6) evaluates to 42, bill stores 42, and print(bill) shows 42. A function with no return gives back None.",
      "task": "Find why the caller gets None instead of the total.",
      "type": "debug",
      "symptom": "This should print 42, but it prints None instead.",
      "language": "python",
      "brokenCode": "def total_price(unit_price, qty):\n    unit_price * qty          # meant to give back the total\n\nbill = total_price(7, 6)\nprint(bill)                   # prints None, not 42"
    },
    {
      "type": "tradeoff",
      "question": "print shows; return gives back",
      "optionA": {
        "label": "Only prints — bill is None",
        "text": "def total_price(u, q): ·     print(u * q)   # shows 42 on screen · bill = total_price(7, 6) · print(bill)        # None — nothing was handed back Outcome: Shows a value, gives back nothing"
      },
      "optionB": {
        "label": "Returns — bill is 42",
        "text": "def total_price(u, q): ·     return u * q   # hands 42 back · bill = total_price(7, 6) · print(bill)        # 42 — stored and reusable Outcome: The caller can store and reuse it"
      },
      "guidance": "Does the caller need to USE the result? return it. Does it only need to appear on screen? print it."
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "10 — the function only runs once",
        "20 — double(5) returns 10 into x, then double(10) returns 20",
        "None — you can't call a function with a variable",
        "5 — the return value is ignored"
      ],
      "question": "What does this print?  def double(n): return n * 2  — then  x = double(5)  and  print(double(x))",
      "explanation": "double(5) returns 10, which is stored in x. Then double(x) is double(10), which returns 20, and that is printed. Each call runs the same function with whatever argument you give it and hands back a fresh result — that reuse is the entire point of writing a function."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "You defined a function with def, a name, and parameters in parentheses",
        "Your function uses return to hand a value back (not just print inside it)",
        "You called the function with arguments and stored the returned value in a variable",
        "Your output is EXACTLY:  6 items = $42  — with the 42 coming from the function’s return",
        "You can explain the difference between print (show) and return (hand back)"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "In one sentence, what is a function and why write one?",
        "What is the difference between a parameter and an argument?",
        "What does return do, and how is it different from print?",
        "If a function has no return statement, what does calling it give back?"
      ]
    },
    {
      "text": "In the lab, turn a repeated calculation from earlier into a function: a tip calculator tip(bill, percent), a grader grade(score) using your if/elif/else, or a total summer that loops over a list. Then call it two or three times with different inputs and print each result. The moment you reuse one definition with new inputs, you have crossed from \"scripting\" into \"building\".",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A program that defines a function, calls it with arguments, and prints a computed result that the function returned.",
        "Diagnose and repair the broken case: This should print 42, but it prints None instead.",
        "Confirm every observable check: You defined a function with def, a name, and parameters in parentheses · Your function uses return to hand a value back (not just print inside it) · You called the function with arguments and stored the returned value in a variable · Your output is EXACTLY:  6 items = $42  — with the 42 coming from the function’s return · You can explain the difference between print (show) and return (hand back)",
        "Complete the transfer without copying the worked example: In the lab, turn a repeated calculation from earlier into a function: a tip calculator tip(bill, percent), a grader grade(score) using your if/elif/else, or a total summer that loops over a list. Then call it two or three times with different inputs and print each result. The moment you reuse one definition with new inputs, you have crossed from \"scripting\" into \"building\"."
      ]
    }
  ],
  "build-a-tiny-program": [
    {
      "time": "45–60 min",
      "type": "sprint-contract",
      "proof": "A single working program that defines a function, loops over a list with a decision inside, and prints two exact lines computed from the data (a passing count and an average).",
      "unlock": "Your program runs end to end and prints both required lines, with every number computed from the data — nothing hardcoded.",
      "outcome": "Combine everything from First Steps — a variable, a list, a loop, an if, and a function — into ONE small program that processes real in-code data and prints a useful result. This is your first genuinely shipped program.",
      "intensity": "capstone",
      "doNotClaim": "Don't claim you \"shipped a program\" until it RUNS top to bottom and prints both correct lines from the data — a program that prints the right numbers only because you typed them in is not the skill."
    },
    {
      "text": "Seven lessons ago you could not print a line. Today none of it is practice: you snap print, variables, types, decisions, lists, loops, and functions into one real program — a tiny gradebook that reports how many passed and the class average.",
      "type": "mission"
    },
    {
      "text": "Every real program is the basics combined: take data, loop over it, decide, compute, report. A bank statement, a leaderboard, a sales report — same shape. Holding several pieces at once and wiring them together is what \"can build\" means.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: you need a passing COUNT and a running TOTAL from one list. Do you need two separate loops over the scores, or can one loop do both jobs at the same time?",
      "reveal": "One loop is enough — and better. On each pass you can add the score to your total AND, with an if, bump the passing count when it qualifies. Two accumulators (total = 0 and passed = 0) set up before the loop, both updated inside the same loop, both ready when it ends. Looping the data twice would work but wastes effort; real programs do as much as they can in one well-organised pass."
    },
    {
      "type": "worked-example",
      "intro": "The gradebook, every line earning its place. One program — variable, list, loop, if, function — working as a team.",
      "code": "def summarize(scores):\n    total = 0\n    passed = 0\n    for s in scores:             # one pass over the data\n        total = total + s        # running total\n        if s >= 60:              # decision\n            passed = passed + 1  # count the passes\n    print(f\"Passed: {passed} of {len(scores)}\")\n    return total / len(scores)   # hand back the average\n\nscores = [40, 80, 95, 50, 85]\navg = summarize(scores)\nprint(f\"Average: {int(avg)}\")    # -> Passed: 3 of 5 / Average: 70",
      "language": "python",
      "steps": [
        "Wrap the job in a function (L8): summarize(scores) names the whole task and lets you reuse it on any list of scores.",
        "Two accumulators, set up first (L2 & L7): total and passed created BEFORE the loop — the empty buckets. Inside the loop they would reset every pass.",
        "One loop, running total (L7): Walk the list once; total grows on every pass.",
        "A decision inside the loop (L5): The if counts only scores >= 60 — loop and if working together.",
        "Compute and report after (L1, L3, L8): After the loop: print the count, then return the average (total / len). The caller prints it with an f-string and int(avg)."
      ],
      "commonMistake": "This gradebook should print \"Average: 70\", but it prints \"Average: 17\" — far too low."
    },
    {
      "text": "No new syntax — just arranging what you have: start with data, set up result holders before the loop, loop once (update the total, use an if to count), compute final figures after, then report. Wrap it in a function to name and reuse the whole job.",
      "type": "concept",
      "title": "A real program = data → loop → decide → accumulate → report"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "loop",
          "from": "data",
          "kind": "data",
          "label": "feeds"
        },
        {
          "to": "loop",
          "from": "init",
          "kind": "data",
          "tone": "accent",
          "label": "empty buckets"
        },
        {
          "to": "decide",
          "from": "loop",
          "kind": "control",
          "label": "each score"
        },
        {
          "to": "loop",
          "from": "decide",
          "kind": "control",
          "label": "next item",
          "dashed": true
        },
        {
          "to": "after",
          "from": "loop",
          "kind": "data",
          "label": "when done"
        },
        {
          "to": "report",
          "from": "after",
          "kind": "data",
          "tone": "success",
          "label": "figures"
        }
      ],
      "nodes": [
        {
          "id": "data",
          "kind": "store",
          "tone": "muted",
          "label": "scores (list)",
          "description": "the data — L6"
        },
        {
          "id": "init",
          "kind": "process",
          "tone": "accent",
          "label": "total=0, passed=0",
          "description": "holders before the loop — L2"
        },
        {
          "id": "loop",
          "kind": "decision",
          "label": "for s in scores",
          "description": "one pass — L7"
        },
        {
          "id": "decide",
          "kind": "decision",
          "tone": "warning",
          "label": "if s >= 60",
          "description": "count passes — L5"
        },
        {
          "id": "after",
          "kind": "process",
          "label": "total / len",
          "description": "compute after — L3"
        },
        {
          "id": "report",
          "kind": "store",
          "tone": "success",
          "label": "print + return",
          "description": "report — L1 & L8"
        }
      ],
      "title": "The shape of every data program",
      "legend": [
        {
          "tone": "accent",
          "label": "set up before the loop"
        },
        {
          "tone": "warning",
          "label": "a decision inside the loop"
        },
        {
          "tone": "success",
          "label": "report the result"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Data flows into a loop that updates accumulators with a decision inside; final figures are computed after, then reported.",
      "storyboard": [
        {
          "say": "Every real program has one shape: take data, loop over it, decide, compute, report. Here it's a tiny gradebook of scores.",
          "nodes": [
            "data"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "Before the loop, set up empty result holders: total starts at zero, passed starts at zero. Nothing counted yet.",
          "nodes": [
            "init",
            "loop"
          ],
          "edges": [
            [
              "init",
              "loop"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Now the data feeds the loop. We walk the list one score at a time until every item is seen.",
          "nodes": [
            "data",
            "loop"
          ],
          "edges": [
            [
              "data",
              "loop"
            ],
            [
              "loop",
              "decide"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "For each score we do two things: add it to the total, and use an if to check whether it passed.",
          "nodes": [
            "decide",
            "loop"
          ],
          "edges": [
            [
              "loop",
              "decide"
            ],
            [
              "decide",
              "loop"
            ]
          ],
          "ms": 4800
        },
        {
          "say": "The decision — if the score is sixty or more — bumps the passed count; then we loop back for the next item.",
          "nodes": [
            "decide",
            "loop"
          ],
          "edges": [
            [
              "decide",
              "loop"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "When the list runs out, the loop is done and we exit it to compute the final figures.",
          "nodes": [
            "loop",
            "after"
          ],
          "edges": [
            [
              "loop",
              "after"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "After the loop we divide total by length to get the average, then print and return the result — one reusable function.",
          "nodes": [
            "after",
            "report"
          ],
          "edges": [
            [
              "after",
              "report"
            ]
          ],
          "ms": 4800
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "total = 0 inside — wrong",
        "lines": [
          "for s in scores:",
          "    total = 0          # resets every pass",
          "    total = total + s",
          "return total / len(scores)",
          "# total ends as just 85 -> 85/5 = 17"
        ],
        "verdict": "Keeps only the last score"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "total = 0 outside — right",
        "lines": [
          "total = 0              # once, before the loop",
          "for s in scores:",
          "    total = total + s",
          "return total / len(scores)",
          "# total = 350 -> 350/5 = 70"
        ],
        "verdict": "Accumulates across every score"
      },
      "title": "Accumulator outside vs inside the loop",
      "caption": "Set up accumulators once before the loop; compute final figures once after it. The loop body should only UPDATE.",
      "subtitle": "The capstone's make-or-break bug. One line's placement changes the answer."
    },
    {
      "text": "When a task feels big, name the steps in plain words first: \"loop the scores, total them, count the passes, average it, print.\" That sentence IS your program’s outline — turn each clause into a line or two of code. Real engineers do exactly this: decompose the job into small steps you already know how to write, then assemble them. You are not memorising new magic; you are arranging familiar pieces.",
      "tone": "tip",
      "type": "callout"
    },
    {
      "type": "lab",
      "check": "Passed: 3 of 5\nAverage: 70",
      "title": "Ship the gradebook",
      "starter": "# CAPSTONE — ship a tiny gradebook in one small program.\n# scores below are quiz results (0-100). Build ONE program that:\n#   1. defines a function  summarize(scores)  which LOOPS over the scores and,\n#      inside the loop, builds a running total AND counts how many are >= 60 (passing);\n#   2. computes the average  (total divided by how many scores there are);\n#   3. prints the passing-count line, then RETURNS the average.\n#   4. OUTSIDE the function: call summarize(scores), store the returned average in  avg,\n#      and print the average line. Print the average as a whole number with  int(avg).\n# Required output — EXACTLY two lines (use f-strings):\n#   Passed: 3 of 5\n#   Average: 70\n\nscores = [40, 80, 95, 50, 85]\n  # your code here\n",
      "summary": "Build one program over scores = [40, 80, 95, 50, 85]. Define summarize(scores) that loops once, builds a running total, counts scores >= 60, prints \"Passed: 3 of 5\", and RETURNS the average (total / len). Outside the function, call it, store the average in avg, and print \"Average: 70\" (use int(avg)). Output must be exactly two lines: Passed: 3 of 5 then Average: 70 — every number computed from the data, nothing hardcoded.",
      "language": "python"
    },
    {
      "code": "def summarize(scores):\n    total = 0\n    passed = 0\n    for s in scores:             # one pass over the data\n        total = total + s        # running total\n        if s >= 60:              # decision\n            passed = passed + 1  # count the passes\n    print(f\"Passed: {passed} of {len(scores)}\")\n    return total / len(scores)   # hand back the average\n\nscores = [40, 80, 95, 50, 85]\navg = summarize(scores)\nprint(f\"Average: {int(avg)}\")    # -> Passed: 3 of 5 / Average: 70",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "summarize(scores) names the whole task and lets you reuse it on any list of scores.",
          "label": "Wrap the job in a function (L8)",
          "lines": [
            1
          ]
        },
        {
          "note": "total and passed created BEFORE the loop — the empty buckets. Inside the loop they would reset every pass.",
          "label": "Two accumulators, set up first (L2 & L7)",
          "lines": [
            2,
            3
          ]
        },
        {
          "note": "Walk the list once; total grows on every pass.",
          "label": "One loop, running total (L7)",
          "lines": [
            4,
            5
          ]
        },
        {
          "note": "The if counts only scores >= 60 — loop and if working together.",
          "label": "A decision inside the loop (L5)",
          "lines": [
            6,
            7
          ]
        },
        {
          "note": "After the loop: print the count, then return the average (total / len). The caller prints it with an f-string and int(avg).",
          "label": "Compute and report after (L1, L3, L8)",
          "lines": [
            8,
            9
          ]
        }
      ],
      "title": "The gradebook, every line earning its place",
      "caption": "Output, two lines: Passed: 3 of 5 then Average: 70 — every number computed from the data.",
      "filename": "gradebook.py",
      "language": "python",
      "subtitle": "One program — variable, list, loop, if, function — working as a team."
    },
    {
      "fix": "total = 0 sits INSIDE the loop, so it resets every pass — after the final pass total is just the last score (85), not the sum. The function then returns 85 / 5 = 17. Move  total = 0  ABOVE the loop so it runs once, and leave only  total = total + s  inside. Now total accumulates 40+80+95+50+85 = 350, and 350 / 5 = 70. Accumulators are created before the loop, updated inside it.",
      "task": "Find why the average comes out as 17 instead of 70.",
      "type": "debug",
      "symptom": "This gradebook should print \"Average: 70\", but it prints \"Average: 17\" — far too low.",
      "language": "python",
      "brokenCode": "def summarize(scores):\n    for s in scores:\n        total = 0                # meant to total the scores\n        total = total + s\n    return total / len(scores)\n\nscores = [40, 80, 95, 50, 85]\nprint(f\"Average: {int(summarize(scores))}\")"
    },
    {
      "type": "tradeoff",
      "question": "Accumulator outside vs inside the loop",
      "optionA": {
        "label": "total = 0 inside — wrong",
        "text": "for s in scores: ·     total = 0          # resets every pass ·     total = total + s · return total / len(scores) · # total ends as just 85 -> 85/5 = 17 Outcome: Keeps only the last score"
      },
      "optionB": {
        "label": "total = 0 outside — right",
        "text": "total = 0              # once, before the loop · for s in scores: ·     total = total + s · return total / len(scores) · # total = 350 -> 350/5 = 70 Outcome: Accumulates across every score"
      },
      "guidance": "Set up accumulators once before the loop; compute final figures once after it. The loop body should only UPDATE."
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "It must be inside the loop, or it never runs",
        "Because total is only complete once the loop has visited every score; computing it mid-loop would use a partial total",
        "It makes no difference where you put it",
        "Because len(scores) only works after a loop"
      ],
      "question": "In the gradebook, why is  average = total / len(scores)  written AFTER the loop instead of inside it?",
      "explanation": "The running total is only finished after the loop has added every score. Computing the average inside the loop would divide an incomplete total and run needlessly on every pass. The pattern is: accumulate inside the loop, compute final results once the loop is done."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes (this is your shipped program):",
      "items": [
        "Your program defines a function and CALLS it (Lesson 7)",
        "It loops over the list once, with an accumulator set up before the loop (Lessons 2 & 6)",
        "It uses an if INSIDE the loop to count only the passing scores (Lessons 4 & 6)",
        "It computes the average AFTER the loop and formats output with f-strings (Lessons 3 & 6)",
        "It prints EXACTLY two lines — Passed: 3 of 5 then Average: 70 — with every number computed from the data, nothing typed in"
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Walk through your program in plain English, one step per line of code.",
        "Why are total and passed set up before the loop, and updated inside it?",
        "Why does the average get computed after the loop, not during it?",
        "Which First Steps lesson did each part of your program come from? (variable, list, loop, if, function)"
      ]
    },
    {
      "type": "calibration",
      "artifact": "A single working program that defines a function, loops over a list with a decision inside, and prints two exact lines computed from the data (a passing count and an average).",
      "weak": "The program only handles the demonstration input, hardcodes a required result, or cannot explain a failing case.",
      "passing": "The artifact satisfies every verification item: Your program defines a function and CALLS it (Lesson 7) · It loops over the list once, with an accumulator set up before the loop (Lessons 2 & 6) · It uses an if INSIDE the loop to count only the passing scores (Lessons 4 & 6) · It computes the average AFTER the loop and formats output with f-strings (Lessons 3 & 6) · It prints EXACTLY two lines — Passed: 3 of 5 then Average: 70 — with every number computed from the data, nothing typed in",
      "excellent": "Passing evidence plus an independently chosen edge case, a regression test, and the transfer challenge: You just shipped a real program — stop and own that, because most people who say \"I want to learn to code\" never get here. Now make it yours: change the pass mark, track the \"top score\" by keeping the largest value as you loop, or point summarize() at a completely different list and watch it still work — that reuse is the whole payoff of a function. Then build a sibling from scratch: a tiny expense report, a step-count week, a quiz scorer. Same shape, your data. Everything ahead — files, the web, data, AI — is this exact move at larger scale: take data, process it in a loop with decisions, report a useful result. You do not just know some syntax now. You can build. Welcome.",
      "note": "Judge the artifact from executable evidence and explanation. A polished output cannot compensate for a hardcoded answer or an untested failure path."
    },
    {
      "text": "You just shipped a real program — stop and own that, because most people who say \"I want to learn to code\" never get here. Now make it yours: change the pass mark, track the \"top score\" by keeping the largest value as you loop, or point summarize() at a completely different list and watch it still work — that reuse is the whole payoff of a function. Then build a sibling from scratch: a tiny expense report, a step-count week, a quiz scorer. Same shape, your data. Everything ahead — files, the web, data, AI — is this exact move at larger scale: take data, process it in a loop with decisions, report a useful result. You do not just know some syntax now. You can build. Welcome.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Produce and run the required artifact: A single working program that defines a function, loops over a list with a decision inside, and prints two exact lines computed from the data (a passing count and an average).",
        "Diagnose and repair the broken case: This gradebook should print \"Average: 70\", but it prints \"Average: 17\" — far too low.",
        "Confirm every observable check: Your program defines a function and CALLS it (Lesson 7) · It loops over the list once, with an accumulator set up before the loop (Lessons 2 & 6) · It uses an if INSIDE the loop to count only the passing scores (Lessons 4 & 6) · It computes the average AFTER the loop and formats output with f-strings (Lessons 3 & 6) · It prints EXACTLY two lines — Passed: 3 of 5 then Average: 70 — with every number computed from the data, nothing typed in",
        "Complete the transfer without copying the worked example: You just shipped a real program — stop and own that, because most people who say \"I want to learn to code\" never get here. Now make it yours: change the pass mark, track the \"top score\" by keeping the largest value as you loop, or point summarize() at a completely different list and watch it still work — that reuse is the whole payoff of a function. Then build a sibling from scratch: a tiny expense report, a step-count week, a quiz scorer. Same shape, your data. Everything ahead — files, the web, data, AI — is this exact move at larger scale: take data, process it in a loop with decisions, report a useful result. You do not just know some syntax now. You can build. Welcome."
      ]
    }
  ],
  "input-validation": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A passing validate_age() plus a negative test that proves a hostile input is rejected, and a teach-back.",
      "unlock": "All verification checks confirmed and the broken case fixed.",
      "outcome": "Write a validator that rejects malformed, wrong-type, out-of-range, and hostile input at the boundary — and explain what belongs at the client, server, and database.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until you've written a NEGATIVE test that proves invalid input is rejected — not just that valid input passes."
    },
    {
      "text": "Your signup endpoint just accepted an empty email, a 50,000-character name, and age = -3 — and that garbage is now in your database. The gradebook you shipped in First Steps worked only because its data was clean; real data never is. Today your code stops trusting input and checks it at the boundary first.",
      "type": "mission"
    },
    {
      "text": "Unchecked input is the #1 cause of crashes, corrupted data, and a whole class of security bugs. It is the through-line of this module: distrust the outside world, check it at the edge.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: a teammate says \"the frontend already validates the form, so the API doesn't need to.\" Why is that dangerous?",
      "reveal": "The client is untrusted. Anyone can call the API directly — curl, a script, a malicious client — and skip the UI entirely. Validation must happen server-side, at the boundary. Client validation is only a UX nicety; it is never a security control."
    },
    {
      "type": "worked-example",
      "intro": "The boundary pattern, one check at a time. Parse into a known shape, then check type → presence → length → range, rejecting with an actionable error.",
      "code": "def validate_signup(data):\n    if not isinstance(data, dict):\n        raise ValueError(\"signup must be an object\")\n    email = data.get(\"email\")\n    if not isinstance(email, str) or \"@\" not in email:\n        raise ValueError(\"email must be a valid address\")\n    name = data.get(\"name\", \"\")\n    if not isinstance(name, str) or not (1 <= len(name) <= 100):\n        raise ValueError(\"name must be 1-100 characters\")\n    age = data.get(\"age\")\n    if not isinstance(age, int) or not (0 <= age <= 120):\n        raise ValueError(\"age must be an integer 0-120\")\n    return {\"email\": email, \"name\": name.strip(), \"age\": age}",
      "language": "python",
      "steps": [
        "Shape first: Parse into a known shape before trusting any field. If it is not even a dict, reject immediately.",
        "Presence + type: `data.get(\"email\")` is missing-safe; the isinstance check rejects non-strings before you ever call a string method.",
        "Length (stops DoS / overflow): A 50,000-char name is a denial-of-service vector. Bound it at the edge.",
        "Range: age = -3 is the bug from the mission. isinstance(age, int) also blocks the hostile string \"25\".",
        "Normalize only when safe: Return a clean shape; `.strip()` is an explicit, safe transform — never silently coerce a wrong type."
      ],
      "commonMistake": "This \"validator\" accepts the string \"25\" and even -3 silently — bad data flows downstream."
    },
    {
      "text": "A trust boundary is any line where untrusted data crosses into your system. At that edge, confirm shape → type → presence → range → length → rules before the system believes a word of it. Reject by default; normalize only when the transform is safe and explicit.",
      "type": "concept",
      "title": "Input is hostile until proven valid"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "boundary",
          "from": "client",
          "kind": "sync",
          "label": "crosses the edge"
        },
        {
          "to": "reject",
          "from": "boundary",
          "kind": "control",
          "tone": "warning",
          "label": "invalid"
        },
        {
          "to": "logic",
          "from": "boundary",
          "kind": "data",
          "tone": "success",
          "label": "valid only"
        },
        {
          "to": "db",
          "from": "logic",
          "kind": "sync",
          "tone": "success",
          "label": "writes"
        }
      ],
      "nodes": [
        {
          "id": "client",
          "kind": "client",
          "tone": "warning",
          "label": "Untrusted input",
          "description": "form · API · curl · script"
        },
        {
          "id": "boundary",
          "kind": "decision",
          "tone": "accent",
          "label": "Validator",
          "description": "shape · type · range · length"
        },
        {
          "id": "reject",
          "kind": "process",
          "tone": "warning",
          "label": "Reject",
          "description": "actionable ValueError"
        },
        {
          "id": "logic",
          "kind": "service",
          "tone": "success",
          "label": "App logic",
          "description": "now trusts the data"
        },
        {
          "id": "db",
          "kind": "store",
          "tone": "success",
          "label": "Database",
          "description": "CHECK backstops invariants"
        }
      ],
      "title": "Where validation lives — at the boundary, before any side effect",
      "legend": [
        {
          "tone": "warning",
          "label": "hostile until proven valid"
        },
        {
          "tone": "accent",
          "label": "the boundary check"
        },
        {
          "tone": "success",
          "label": "trusted past the edge"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Untrusted input must pass the validator before it can touch the database. Validate first, act second.",
      "storyboard": [
        {
          "say": "Everything starts here: untrusted input arrives from the outside world, and you cannot assume any of it is well-formed.",
          "nodes": [
            "client"
          ],
          "edges": [],
          "ms": 3600
        },
        {
          "say": "That input crosses the trust boundary and hits the validator first, before it can touch anything real inside your system.",
          "nodes": [
            "client",
            "boundary"
          ],
          "edges": [
            [
              "client",
              "boundary"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "At the edge the validator checks shape, type, presence, range, length, and rules, deciding one thing: is this data trustworthy?",
          "nodes": [
            "boundary"
          ],
          "edges": [],
          "ms": 4600
        },
        {
          "say": "If anything fails, the request is rejected immediately, so an empty email or an age of minus three never gets any further.",
          "nodes": [
            "boundary",
            "reject"
          ],
          "edges": [
            [
              "boundary",
              "reject"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Only valid, checked data is allowed through to your application logic, which can now trust every field it receives.",
          "nodes": [
            "boundary",
            "logic"
          ],
          "edges": [
            [
              "boundary",
              "logic"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Because the data was cleaned at the boundary, the write to the database stores only trustworthy values, not garbage.",
          "nodes": [
            "logic",
            "db"
          ],
          "edges": [
            [
              "logic",
              "db"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "The core insight: validate first, act second. Guard the boundary once and every layer behind it stays clean.",
          "nodes": [
            "boundary",
            "logic",
            "db"
          ],
          "edges": [
            [
              "boundary",
              "logic"
            ],
            [
              "logic",
              "db"
            ]
          ],
          "ms": 4800
        }
      ]
    },
    {
      "type": "lab",
      "check": "rejected: age must be an integer",
      "title": "Write the validator",
      "starter": "import json\nimport sys\n\ndef validate_age(value):\n    # Reject bool and every non-int with: age must be an integer\n    # Reject integers outside 0..120 with: age must be between 0 and 120\n    ...  # your code here\n\nraw = sys.stdin.read().strip()\nvalues = [json.loads(raw)] if raw else [25, \"25\", -3, 200]\nfor value in values:\n    try:\n        print(f\"accepted: {validate_age(value)!r}\")\n    except ValueError as error:\n        print(f\"rejected: {error}\")\n",
      "summary": "Implement validate_age(value): accept only a real integer in 0–120, rejecting booleans, strings, and out-of-range values with the specified errors. The browser shows four examples; the controlled evaluator supplies additional JSON values over stdin.",
      "language": "python"
    },
    {
      "code": "def validate_signup(data):\n    if not isinstance(data, dict):\n        raise ValueError(\"signup must be an object\")\n    email = data.get(\"email\")\n    if not isinstance(email, str) or \"@\" not in email:\n        raise ValueError(\"email must be a valid address\")\n    name = data.get(\"name\", \"\")\n    if not isinstance(name, str) or not (1 <= len(name) <= 100):\n        raise ValueError(\"name must be 1-100 characters\")\n    age = data.get(\"age\")\n    if not isinstance(age, int) or not (0 <= age <= 120):\n        raise ValueError(\"age must be an integer 0-120\")\n    return {\"email\": email, \"name\": name.strip(), \"age\": age}",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "Parse into a known shape before trusting any field. If it is not even a dict, reject immediately.",
          "label": "Shape first",
          "lines": [
            2,
            3
          ]
        },
        {
          "note": "`data.get(\"email\")` is missing-safe; the isinstance check rejects non-strings before you ever call a string method.",
          "label": "Presence + type",
          "lines": [
            4,
            5,
            6
          ]
        },
        {
          "note": "A 50,000-char name is a denial-of-service vector. Bound it at the edge.",
          "label": "Length (stops DoS / overflow)",
          "lines": [
            7,
            8,
            9
          ]
        },
        {
          "note": "age = -3 is the bug from the mission. isinstance(age, int) also blocks the hostile string \"25\".",
          "label": "Range",
          "lines": [
            10,
            11,
            12
          ]
        },
        {
          "note": "Return a clean shape; `.strip()` is an explicit, safe transform — never silently coerce a wrong type.",
          "label": "Normalize only when safe",
          "lines": [
            13
          ]
        }
      ],
      "title": "The boundary pattern, one check at a time",
      "caption": "Common mistake: validating AFTER a side effect (e.g. after the DB insert). By then the bad data is already in.",
      "filename": "signup.py",
      "language": "python",
      "subtitle": "Parse into a known shape, then check type → presence → length → range, rejecting with an actionable error."
    },
    {
      "fix": "Truthiness is not validation: \"25\" and -3 are both truthy, and 0 is falsy (a valid age!). Check the TYPE explicitly with isinstance(value, int), then the range. Reject, don't coerce.",
      "task": "Find why hostile input slips through.",
      "type": "debug",
      "symptom": "This \"validator\" accepts the string \"25\" and even -3 silently — bad data flows downstream.",
      "language": "python",
      "brokenCode": "def validate_age(value):\n    if value:                     # truthiness, not a real check\n        return value\n    raise ValueError(\"bad age\")"
    },
    {
      "type": "tradeoff",
      "question": "Invalid input at the boundary: reject vs normalize",
      "optionA": {
        "label": "Reject (default)",
        "text": "isinstance + range check fails · raise ValueError(\"age 0-120\") · Caller sees exactly what was wrong · Hostile input never enters the system · Friction: the user must resend Outcome: Safe, explicit, preserves intent"
      },
      "optionB": {
        "label": "Over-normalize",
        "text": "age = int(value or 0) · \"-3\" silently becomes 0 or -3 · No error — looks like it worked · Hides attacks; corrupts the real value · Smoother UX, wrong data Outcome: Quietly changes what the user meant"
      },
      "guidance": "Reject by default at trust boundaries. Normalize only when the transform is safe AND explicit — like trimming whitespace."
    },
    {
      "left": {
        "tone": "success",
        "label": "Reject (default)",
        "lines": [
          "isinstance + range check fails",
          "raise ValueError(\"age 0-120\")",
          "Caller sees exactly what was wrong",
          "Hostile input never enters the system",
          "Friction: the user must resend"
        ],
        "verdict": "Safe, explicit, preserves intent"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "warning",
        "label": "Over-normalize",
        "lines": [
          "age = int(value or 0)",
          "\"-3\" silently becomes 0 or -3",
          "No error — looks like it worked",
          "Hides attacks; corrupts the real value",
          "Smoother UX, wrong data"
        ],
        "verdict": "Quietly changes what the user meant"
      },
      "title": "Invalid input at the boundary: reject vs normalize",
      "caption": "Reject by default at trust boundaries. Normalize only when the transform is safe AND explicit — like trimming whitespace.",
      "subtitle": "Both run; only one preserves what the user actually meant."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "validate_age(25) is accepted",
        "validate_age('25') is REJECTED (it is a string, not an int)",
        "validate_age(-3) is REJECTED (out of range)",
        "validate_age(200) is REJECTED (out of range)",
        "The error message names the constraint",
        "A negative test asserts a rejection — not just that valid input passes"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Formatting an error message",
        "A critical uniqueness / invariant constraint (e.g. unique email)",
        "Trimming whitespace",
        "Showing a red border on the form"
      ],
      "question": "Which validation belongs at the database layer, not just the application?",
      "explanation": "Critical invariants (uniqueness, foreign keys, not-null) should be backstopped by DB constraints. Application checks can race or be bypassed; the database is the last line of defense."
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain \"trust boundary\" in one sentence.",
        "Why is client-side validation insufficient on its own?",
        "Name two validation failures that become security bugs.",
        "When would you normalize instead of reject?"
      ]
    },
    {
      "text": "Take one function in your own code that accepts external input (a request handler, a CLI argument, a file parser). Add a boundary validator and one negative test. Note which checks belong at the client, the server, and the database. You now reject bad input cleanly — but next lesson asks the harder question: when something fails anyway, how do you fail loudly and safely instead of silently?",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Lab checkpoint passed — the validator rejects the hostile string input",
        "Broken case understood and fixed (truthiness ≠ validation)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own code"
      ]
    }
  ],
  "error-handling": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A function that catches deliberately, logs operator context, raises a clear error, plus a test proving the error is NOT swallowed.",
      "unlock": "All verification checks confirmed and the swallowed-exception case fixed.",
      "outcome": "Handle errors so failures surface loudly, users get a safe clear message, operators get enough context to diagnose — and you never swallow an exception.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until you've shown a swallowed-exception bug and fixed it with a test that proves the error now surfaces."
    },
    {
      "text": "A background job has been \"succeeding\" for a week — because it wrapped everything in `except Exception: pass`. Thousands of records silently failed and no one knew. Last lesson you rejected bad input at the door; today you handle the failures that slip past every guard, making them loud, safe, and diagnosable.",
      "type": "mission"
    },
    {
      "text": "Error handling is where junior and senior code diverge. The same try/except can hide a production outage for a week or turn it into a 30-second fix.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: why is `except Exception: pass` one of the most dangerous lines you can write?",
      "reveal": "It swallows EVERY error — including ones you never anticipated (a real bug, disk-full, a typo). The program keeps running as if nothing happened, so the failure becomes invisible and undebuggable. Catch narrowly, handle deliberately, and never silently pass."
    },
    {
      "type": "worked-example",
      "intro": "Catch deliberately — one branch at a time. Specific catches, operator context logged, a safe user message, retry only where it is safe.",
      "code": "import logging\n\ndef charge_card(user_id, amount):\n    try:\n        return payment_api.charge(user_id, amount)\n    except TransientNetworkError as e:\n        logging.warning(\"charge retryable: user=%s amount=%s (%s)\", user_id, amount, e)\n        raise RetryableError(\"payment temporarily unavailable\") from e\n    except CardDeclinedError as e:\n        logging.error(\"charge declined: user=%s amount=%s (%s)\", user_id, amount, e)\n        raise UserError(\"Your card was declined.\") from e",
      "language": "python",
      "steps": [
        "The risky call: Only the line that can fail goes in the try. Wrapping more would catch bugs you meant to let crash.",
        "Transient → retryable: A network blip is recoverable. Log operator context, then re-raise a typed RetryableError — retry only if the call is idempotent.",
        "Declined → non-retryable: Retrying a declined card just fails again. Surface a safe user message; log the real error for the operator.",
        "Chain with `from e`: Python keeps the original as `__cause__`, so the operator sees the true root cause — not just your wrapper."
      ],
      "commonMistake": "This \"handles\" errors — and hid a week of silent data loss."
    },
    {
      "text": "Good error handling answers four questions: recoverable or fatal? retryable or not? what does the USER safely see? what does the OPERATOR need logged? Safe failure = the system fails in a known, observable state — loudly, with context — never silently.",
      "type": "concept",
      "title": "Recoverable vs fatal · user message vs operator context"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "classify",
          "from": "err",
          "kind": "sync",
          "label": "caught narrowly"
        },
        {
          "to": "crash",
          "from": "classify",
          "kind": "control",
          "tone": "warning",
          "label": "no — fatal"
        },
        {
          "to": "retry",
          "from": "classify",
          "kind": "control",
          "tone": "accent",
          "label": "yes"
        },
        {
          "to": "log",
          "from": "retry",
          "kind": "data",
          "tone": "success",
          "label": "always log"
        },
        {
          "to": "user",
          "from": "log",
          "kind": "data",
          "tone": "success",
          "label": "then surface"
        }
      ],
      "nodes": [
        {
          "id": "err",
          "kind": "client",
          "tone": "warning",
          "label": "Exception raised",
          "description": "network · declined · bug"
        },
        {
          "id": "classify",
          "kind": "decision",
          "tone": "accent",
          "label": "Recoverable?",
          "description": "can I handle this?"
        },
        {
          "id": "crash",
          "kind": "process",
          "tone": "warning",
          "label": "Let it surface",
          "description": "unknown bug → crash loudly"
        },
        {
          "id": "retry",
          "kind": "decision",
          "tone": "accent",
          "label": "Retryable?",
          "description": "transient AND idempotent"
        },
        {
          "id": "log",
          "kind": "store",
          "tone": "success",
          "label": "Log operator context",
          "description": "inputs + original error · chain `from e`"
        },
        {
          "id": "user",
          "kind": "service",
          "tone": "success",
          "label": "Safe user message",
          "description": "no stack trace leaked"
        }
      ],
      "title": "An error arrives — classify, then act",
      "legend": [
        {
          "tone": "accent",
          "label": "classify before acting"
        },
        {
          "tone": "success",
          "label": "log context + safe message"
        },
        {
          "tone": "warning",
          "label": "let the unhandleable crash"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Every failure routes through the same four questions. A bare `except: pass` short-circuits all of them.",
      "storyboard": [
        {
          "say": "An exception is raised — but here it's caught narrowly, at the door, not swallowed by a blanket except.",
          "nodes": [
            "err",
            "classify"
          ],
          "edges": [
            [
              "err",
              "classify"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "First question: is this recoverable or fatal? Every failure routes through this one decision.",
          "nodes": [
            "classify"
          ],
          "edges": [],
          "ms": 4000
        },
        {
          "say": "If it's fatal, don't hide it — let it surface loudly so the system fails in a known, observable state.",
          "nodes": [
            "classify",
            "crash"
          ],
          "edges": [
            [
              "classify",
              "crash"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "If it's recoverable, ask the next question: is this the kind of error worth retrying?",
          "nodes": [
            "classify",
            "retry"
          ],
          "edges": [
            [
              "classify",
              "retry"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Either way, always log operator context — the trace and detail an engineer needs to fix it fast.",
          "nodes": [
            "retry",
            "log"
          ],
          "edges": [
            [
              "retry",
              "log"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Then give the user a safe message — no leaked internals — and surface the failure honestly.",
          "nodes": [
            "log",
            "user"
          ],
          "edges": [
            [
              "log",
              "user"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "A bare except-pass short-circuits all four questions — that's how a background job silently fails for a week.",
          "nodes": [
            "err",
            "classify",
            "crash",
            "retry",
            "log",
            "user"
          ],
          "edges": [],
          "ms": 5000
        }
      ]
    },
    {
      "type": "lab",
      "check": "error surfaced: cannot divide by zero",
      "title": "Make the failure loud",
      "starter": "def safe_divide(a, b):\n    # Don't swallow it. Catch ZeroDivisionError, log operator context (print a line),\n    # then chain a clear error:  raise ValueError(\"cannot divide by zero\") from e\n    ...  # your code here\n\n\n# --- test harness (do not edit) ---\ntry:\n    print(\"ok:\", safe_divide(10, 2))\n    safe_divide(10, 0)\n    print(\"BUG: no error raised — it was swallowed!\")\nexcept ValueError as e:\n    print(f\"error surfaced: {e}\")\n",
      "summary": "Implement safe_divide(a, b) like the worked example: catch ZeroDivisionError, log operator context, and `raise ValueError(\"cannot divide by zero\") from e` — never swallow it. The checkpoint proves the error SURFACES; the verification list below then confirms you caught, logged, and chained it.",
      "language": "python"
    },
    {
      "code": "import logging\n\ndef charge_card(user_id, amount):\n    try:\n        return payment_api.charge(user_id, amount)\n    except TransientNetworkError as e:\n        logging.warning(\"charge retryable: user=%s amount=%s (%s)\", user_id, amount, e)\n        raise RetryableError(\"payment temporarily unavailable\") from e\n    except CardDeclinedError as e:\n        logging.error(\"charge declined: user=%s amount=%s (%s)\", user_id, amount, e)\n        raise UserError(\"Your card was declined.\") from e",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "Only the line that can fail goes in the try. Wrapping more would catch bugs you meant to let crash.",
          "label": "The risky call",
          "lines": [
            4,
            5
          ]
        },
        {
          "note": "A network blip is recoverable. Log operator context, then re-raise a typed RetryableError — retry only if the call is idempotent.",
          "label": "Transient → retryable",
          "lines": [
            6,
            7,
            8
          ]
        },
        {
          "note": "Retrying a declined card just fails again. Surface a safe user message; log the real error for the operator.",
          "label": "Declined → non-retryable",
          "lines": [
            9,
            10,
            11
          ]
        },
        {
          "note": "Python keeps the original as `__cause__`, so the operator sees the true root cause — not just your wrapper.",
          "label": "Chain with `from e`",
          "lines": [
            8,
            11
          ]
        }
      ],
      "title": "Catch deliberately — one branch at a time",
      "caption": "Common mistake: an overbroad `except Exception` that also catches the bugs you should let crash. Catch what you can actually handle.",
      "filename": "payments.py",
      "language": "python",
      "subtitle": "Specific catches, operator context logged, a safe user message, retry only where it is safe."
    },
    {
      "fix": "A bare/overbroad `except` + `pass` swallows everything. Catch the specific recoverable error, log operator context (which record, the real error), and either re-raise or record the failure to a dead-letter list — never `pass`.",
      "task": "Find why failures became invisible.",
      "type": "debug",
      "symptom": "This \"handles\" errors — and hid a week of silent data loss.",
      "language": "python",
      "brokenCode": "for record in batch:\n    try:\n        process(record)\n    except Exception:\n        pass   # \"it won't crash now\""
    },
    {
      "type": "tradeoff",
      "question": "An external API call fails: retry vs fail fast",
      "optionA": {
        "label": "Retry",
        "text": "Right for transient failures (timeout, 503) · ONLY if the call is idempotent · Idempotent = safe to repeat, same effect · Trap: retrying a timeout that may have charged · Wrong for a declined card (400) — fails again Outcome: Recovers blips a single retry would fix"
      },
      "optionB": {
        "label": "Fail fast",
        "text": "Surface the error immediately · Right for non-retryable errors · A declined card should NOT be retried · Gives up on a transient blip too early · Safe default when outcome is unknown Outcome: Safe, but abandons recoverable failures"
      },
      "guidance": "The double-charge trap is not the declined card — it is retrying a timeout whose real outcome you do not know."
    },
    {
      "left": {
        "tone": "success",
        "label": "Retry",
        "lines": [
          "Right for transient failures (timeout, 503)",
          "ONLY if the call is idempotent",
          "Idempotent = safe to repeat, same effect",
          "Trap: retrying a timeout that may have charged",
          "Wrong for a declined card (400) — fails again"
        ],
        "verdict": "Recovers blips a single retry would fix"
      },
      "type": "compare",
      "right": {
        "tone": "accent",
        "label": "Fail fast",
        "lines": [
          "Surface the error immediately",
          "Right for non-retryable errors",
          "A declined card should NOT be retried",
          "Gives up on a transient blip too early",
          "Safe default when outcome is unknown"
        ],
        "verdict": "Safe, but abandons recoverable failures"
      },
      "title": "An external API call fails: retry vs fail fast",
      "caption": "The double-charge trap is not the declined card — it is retrying a timeout whose real outcome you do not know.",
      "subtitle": "Classify first — the right move depends on whether the failure is transient AND idempotent."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "safe_divide(10, 2) returns 5.0",
        "safe_divide(10, 0) RAISES — it does not return or swallow",
        "The raised error carries a clear, safe message — no stack trace or internals leaked to the user",
        "Operator context (the inputs / original error) is logged, and the cause is chained with `from e`",
        "A test ASSERTS the error is raised — proving it is not swallowed"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Catch it and `pass`",
        "Catch it, log operator context, and re-raise (or fail loudly)",
        "Return None and continue",
        "Print 'error' and move on"
      ],
      "question": "What is the safest way to handle an error you can't recover from?",
      "explanation": "Unrecoverable errors must surface. Log enough context to diagnose, then re-raise or fail. Swallowing it or returning None hides the failure and corrupts everything downstream. Log once, at the boundary that has the context — re-wrapping at every layer just creates noise."
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain \"swallowed exception\" and why it is dangerous.",
        "When should you re-raise an error vs handle it?",
        "What is the difference between the user message and the operator context?",
        "When is retrying an error the WRONG move?"
      ]
    },
    {
      "text": "Find one try/except in your own code. Is it swallowing? Make it catch narrowly, log operator context, surface a safe message, and classify retryable vs not. Add a test that proves the error surfaces. Validation and error handling both got easier to reason about in one spot — and next lesson shows why: small, pure functions are where this clarity actually lives.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Lab checkpoint passed — the error surfaces, it is not swallowed",
        "Broken case understood (a bare `except` hides failures)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own code"
      ]
    }
  ],
  "functions-and-modules": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A pure function (inputs in, value out, no hidden state) + a test asserting it across several inputs.",
      "unlock": "All verification checks confirmed and the impure function refactored.",
      "outcome": "Write a small, single-purpose, PURE function with a clear signature — and a test that proves it — then refactor a side-effecting function to be testable.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until you've turned a function that reads a global / prints into a pure one that returns a value, with a test."
    },
    {
      "text": "Your `calculate_total()` works perfectly when you run it by hand, then its tests fail at random for no reason you can see — because it reads a global `cart` and prints instead of returning. Today you make it small, pure, and predictable enough that a test is just one line.",
      "type": "mission"
    },
    {
      "text": "Functions are the unit of reuse AND the unit of testing. Small, single-purpose, predictable functions are why some codebases are a joy and others a minefield.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: why is a function that reads a global variable harder to test than one that takes everything as a parameter?",
      "reveal": "A function that depends on hidden global state behaves differently depending on what ran before it — tests become order-dependent and flaky. A function that takes its inputs as parameters and returns a value (a pure function) gives the same output for the same input every time, so a test is just `assert f(x) == expected`."
    },
    {
      "type": "worked-example",
      "intro": "A module: related functions behind one interface. Group the pure functions in a file; other code imports the contract, not the internals.",
      "code": "# pricing.py - a MODULE: related functions, one clear interface\ndef line_total(price, qty):\n    return price * qty\n\ndef cart_total(items):\n    return sum(line_total(i[\"price\"], i[\"qty\"]) for i in items)\n\n\n# main.py - import only what you need from the module\nfrom pricing import cart_total\n\nprint(cart_total([{\"price\": 10, \"qty\": 3}]))   # -> 30",
      "language": "python",
      "steps": [
        "The smallest pure unit: `line_total` does one job: price * qty. Nothing hidden, nothing printed.",
        "Compose, do not repeat: `cart_total` reuses `line_total` — small pure pieces wired together, each testable alone.",
        "Import the contract: main.py pulls in only `cart_total`. It depends on the interface, not the internals.",
        "The caller does the I/O: Printing lives here, OUTSIDE the pure functions — so the core stays deterministic and reusable."
      ],
      "commonMistake": "This function \"works\" in the REPL but its test is flaky — it passes or fails depending on what ran first."
    },
    {
      "text": "Its name says what it does, its parameters declare what it needs, its return value is what it promises. PURE functions (output depends only on inputs, no side effects) are trivial to test and reuse. A MODULE groups related functions behind a clear interface.",
      "type": "concept",
      "title": "A function is a contract: name · inputs · output · one job"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "pure",
          "from": "args",
          "kind": "data",
          "tone": "success",
          "label": "all it needs"
        },
        {
          "to": "out",
          "from": "pure",
          "kind": "data",
          "tone": "success",
          "label": "deterministic"
        },
        {
          "to": "impure",
          "from": "args",
          "kind": "data",
          "label": "declared input"
        },
        {
          "to": "impure",
          "from": "global",
          "kind": "control",
          "tone": "warning",
          "label": "hidden input",
          "dashed": true
        },
        {
          "to": "flaky",
          "from": "impure",
          "kind": "async",
          "tone": "warning",
          "label": "order-dependent"
        }
      ],
      "nodes": [
        {
          "id": "args",
          "kind": "client",
          "tone": "accent",
          "label": "Arguments",
          "description": "price, qty"
        },
        {
          "id": "pure",
          "kind": "process",
          "tone": "success",
          "label": "Pure function",
          "description": "inputs in → value out"
        },
        {
          "id": "out",
          "kind": "store",
          "tone": "success",
          "label": "Return value",
          "description": "same inputs → same output"
        },
        {
          "id": "global",
          "kind": "store",
          "tone": "warning",
          "label": "Global state",
          "description": "discount, cart, tax_rate"
        },
        {
          "id": "impure",
          "kind": "process",
          "tone": "warning",
          "label": "Impure function",
          "description": "also reads hidden state"
        },
        {
          "id": "flaky",
          "kind": "external",
          "tone": "warning",
          "label": "Flaky result",
          "description": "depends on what ran first"
        }
      ],
      "title": "Pure vs impure — what a test can actually pin down",
      "legend": [
        {
          "tone": "success",
          "label": "pure — testable in one line"
        },
        {
          "tone": "warning",
          "label": "hidden global → flaky test"
        }
      ],
      "rankdir": "LR",
      "subtitle": "A pure function depends only on its arguments. An impure one also reads hidden state, so the same call can return different answers.",
      "storyboard": [
        {
          "say": "A pure function is a promise: its parameters declare everything it needs, and nothing else can reach in.",
          "nodes": [
            "args",
            "pure"
          ],
          "edges": [
            [
              "args",
              "pure"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Because the arguments are all it sees, the same inputs always produce the same return value. Deterministic.",
          "nodes": [
            "pure",
            "out"
          ],
          "edges": [
            [
              "pure",
              "out"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "That is why a pure function is trivial to test: one line, one call, one expected answer, no setup.",
          "nodes": [
            "args",
            "pure",
            "out"
          ],
          "edges": [
            [
              "args",
              "pure"
            ],
            [
              "pure",
              "out"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "Now the impure twin. It still takes your declared arguments, but that is only half the story.",
          "nodes": [
            "args",
            "impure"
          ],
          "edges": [
            [
              "args",
              "impure"
            ]
          ],
          "ms": 4000
        },
        {
          "say": "It also secretly reads global state, a hidden input your function signature never mentions.",
          "nodes": [
            "global",
            "impure"
          ],
          "edges": [
            [
              "global",
              "impure"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "So the same call returns different answers depending on that hidden state, and your test fails at random.",
          "nodes": [
            "impure",
            "flaky"
          ],
          "edges": [
            [
              "impure",
              "flaky"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "The fix is not more tests, it is fewer hidden inputs: make the function pure and the flakiness disappears.",
          "nodes": [
            "args",
            "pure",
            "out",
            "global",
            "impure",
            "flaky"
          ],
          "edges": [
            [
              "args",
              "pure"
            ],
            [
              "pure",
              "out"
            ],
            [
              "global",
              "impure"
            ],
            [
              "impure",
              "flaky"
            ]
          ],
          "ms": 5000
        }
      ]
    },
    {
      "left": {
        "tone": "warning",
        "label": "Before — reads a global, prints",
        "lines": [
          "cart = [{\"price\": 10, \"qty\": 3}]",
          "def calculate_total():",
          "    total = 0",
          "    for item in cart:   # hidden global",
          "        total += item[\"price\"] * item[\"qty\"]",
          "    print(\"Total:\", total)   # side effect"
        ],
        "verdict": "Untestable: depends on a global, returns nothing"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "After — inputs in, value out",
        "lines": [
          "def calculate_total(items):",
          "    return sum(",
          "        i[\"price\"] * i[\"qty\"]",
          "        for i in items",
          "    )",
          "# assert calculate_total(cart) == 30"
        ],
        "verdict": "Pure: same inputs → same output, one assert"
      },
      "title": "Same job, side-effecting vs pure",
      "caption": "Common mistake: doing two things in one function (compute AND print/save). Split it — the function returns a value; a thin caller does the I/O.",
      "subtitle": "In one file the AFTER def replaces the BEFORE. Only one can be tested with a single assert."
    },
    {
      "type": "lab",
      "check": "PASS: True",
      "title": "Refactor it to be pure",
      "starter": "# This \"works\" in the REPL but FAILS in tests when another part of the app\n# changes `discount`. Refactor line_total to be PURE: depend ONLY on its\n# parameters (price, qty), RETURN the total, read no globals, no print.\ndiscount = 0\n\ndef line_total(price, qty):\n    return (price * qty) - discount   # <-- hidden dependency on a global; fix this\n\n\n# --- test harness (do not edit) ---\ncases = [(10, 3, 30), (5, 4, 20), (0, 9, 0)]\ncorrect = all(line_total(p, q) == exp for p, q, exp in cases)\nglobals()[\"discount\"] = 7             # another module changes the global mid-run\npure = all(line_total(p, q) == exp for p, q, exp in cases)  # only a PURE fn survives\nprint(f\"correct: {correct}\")\nprint(f\"pure (survives a changed global): {pure}\")\nprint(f\"PASS: {correct and pure}\")\n",
      "summary": "`line_total` reads a global `discount`, so it breaks when another part of the app changes it. Refactor it to depend ONLY on its parameters and RETURN the total. The harness changes the global mid-run — only a pure function survives both checks.",
      "language": "python"
    },
    {
      "code": "# pricing.py - a MODULE: related functions, one clear interface\ndef line_total(price, qty):\n    return price * qty\n\ndef cart_total(items):\n    return sum(line_total(i[\"price\"], i[\"qty\"]) for i in items)\n\n\n# main.py - import only what you need from the module\nfrom pricing import cart_total\n\nprint(cart_total([{\"price\": 10, \"qty\": 3}]))   # -> 30",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "`line_total` does one job: price * qty. Nothing hidden, nothing printed.",
          "label": "The smallest pure unit",
          "lines": [
            2,
            3
          ]
        },
        {
          "note": "`cart_total` reuses `line_total` — small pure pieces wired together, each testable alone.",
          "label": "Compose, do not repeat",
          "lines": [
            5,
            6
          ]
        },
        {
          "note": "main.py pulls in only `cart_total`. It depends on the interface, not the internals.",
          "label": "Import the contract",
          "lines": [
            9,
            10
          ]
        },
        {
          "note": "Printing lives here, OUTSIDE the pure functions — so the core stays deterministic and reusable.",
          "label": "The caller does the I/O",
          "lines": [
            12
          ]
        }
      ],
      "title": "A module: related functions behind one interface",
      "caption": "A module is just a file that groups related functions; the import line is the contract other code relies on.",
      "filename": "pricing.py + main.py",
      "language": "python",
      "subtitle": "Group the pure functions in a file; other code imports the contract, not the internals."
    },
    {
      "fix": "It reads the global `tax_rate` — if another test changed it, this one breaks. Pass `tax_rate` as a parameter (`def price_with_tax(amount, tax_rate):`) and return the value. No hidden state → deterministic test.",
      "task": "Why does the test depend on what ran before it?",
      "type": "debug",
      "symptom": "This function \"works\" in the REPL but its test is flaky — it passes or fails depending on what ran first.",
      "language": "python",
      "brokenCode": "tax_rate = 0.0   # a global other code mutates\n\ndef price_with_tax(amount):\n    return amount + amount * tax_rate   # depends on hidden global state"
    },
    {
      "type": "tradeoff",
      "question": "Same job, side-effecting vs pure",
      "optionA": {
        "label": "Before — reads a global, prints",
        "text": "cart = [{\"price\": 10, \"qty\": 3}] · def calculate_total(): ·     total = 0 ·     for item in cart:   # hidden global ·         total += item[\"price\"] * item[\"qty\"] ·     print(\"Total:\", total)   # side effect Outcome: Untestable: depends on a global, returns nothing"
      },
      "optionB": {
        "label": "After — inputs in, value out",
        "text": "def calculate_total(items): ·     return sum( ·         i[\"price\"] * i[\"qty\"] ·         for i in items ·     ) · # assert calculate_total(cart) == 30 Outcome: Pure: same inputs → same output, one assert"
      },
      "guidance": "Common mistake: doing two things in one function (compute AND print/save). Split it — the function returns a value; a thin caller does the I/O."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "line_total(10, 3) RETURNS 30 (a value, not a print)",
        "Same inputs → same output every time (pure / deterministic)",
        "It reads no globals and keeps no hidden state",
        "It does ONE job — compute, not compute-and-print",
        "A test asserts it across several inputs (incl. an edge like qty 0)",
        "Related functions can live in a module (a .py file) and be imported where needed"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "It prints its result",
        "It takes its inputs as parameters and returns a value, reading no hidden state",
        "It reads from a global config",
        "It does several things at once"
      ],
      "question": "What makes a function easiest to test?",
      "explanation": "A pure function — inputs in, value out, no hidden state — gives the same result every time, so a test is just `assert f(x) == expected`. Globals and side effects make tests order-dependent and flaky."
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain \"single responsibility\" for a function in one sentence.",
        "What is the difference between a pure function and a side-effecting one?",
        "Why is a function that reads a global hard to test?",
        "When is a side effect unavoidable — and where should it live?",
        "Why group related functions into a module behind a clear interface?"
      ]
    },
    {
      "text": "Find a function in your own code that prints or saves while it computes. Split it: a pure function that returns the value + a thin caller that does the I/O. Add a test for the pure part. Your functions are clean now — but a pure function still goes wrong if you feed it the wrong KIND of value, and next lesson is about exactly that: knowing what a value really is.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Lab checkpoint passed — line_total is pure and correct across inputs",
        "Broken case understood (a global dependency makes tests flaky)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own code"
      ]
    }
  ],
  "types-and-data": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A function that converts string input to int, rejects non-numbers, returns the right type — plus a test covering a good and a bad case.",
      "unlock": "All verification checks confirmed and the type-confusion bug fixed.",
      "outcome": "Identify a value's type, convert input safely at the boundary, choose the right data structure, and write a function that can't be fooled by a string that looks like a number.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until your function turns ['2','3'] into 5 (not '23') and rejects a non-number with a clear error."
    },
    {
      "text": "Your form adds two quantities and shows '23' instead of 5 — because the inputs arrived as strings ('2' and '3') and `+` quietly concatenated them. The \"2\" + \"3\" ghost from First Steps was not a quirk; it is the bug that haunts every form, file, and API. Today you defend against it: know what your values are, and convert them at the edge.",
      "type": "mission"
    },
    {
      "text": "Type confusion is a top source of subtle bugs: '5' + '5' = '55', `if count:` silently skipping zero, a list used where a dict belonged. Converting at the boundary underpins forms, APIs, and data pipelines.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: in Python, what is `'2' + '3'`? And `2 + 3`? Why does the first one surprise people?",
      "reveal": "`'2' + '3'` is `'23'` (string concatenation); `2 + 3` is `5` (integer addition). `+` does different things depending on the operand TYPE. Input from forms, files, and APIs usually arrives as STRINGS — so you must convert deliberately (`int('2')`) before doing math."
    },
    {
      "type": "worked-example",
      "intro": "Convert at the boundary, then compute on real types. Parse strings to int/float at the edge, handle the failure, then math is safe.",
      "code": "def order_total(qty_str, price_str):\n    try:\n        qty = int(qty_str)\n        price = float(price_str)\n    except ValueError:\n        raise ValueError(\"quantity and price must be numbers\")\n    if qty < 0 or price < 0:\n        raise ValueError(\"quantity and price must be non-negative\")\n    return qty * price",
      "language": "python",
      "steps": [
        "Know the incoming type: qty_str and price_str are STRINGS — form/file/API data almost always is.",
        "Convert explicitly at the edge: `int(...)` and `float(...)` parse deliberately. Past this point the code works with real numbers.",
        "Handle conversion failure: `int(\"oops\")` raises ValueError — catch it and reject with a clear message, exactly like Lesson 1.",
        "Validate the real value: Now that they are numbers, a range check makes sense (negatives rejected).",
        "Compute on the real type: qty * price — never `str + int`, which would raise TypeError, or `str + str`, which would concatenate."
      ],
      "commonMistake": "This sums quantities but returns '235' instead of 10."
    },
    {
      "text": "Core types: int, float, str, bool, list, dict, set, None. TYPE decides `+`, `==`, truthiness — `'2'+'3'` concatenates, `2+3` adds. Truthiness traps: 0, '', [], {} are all falsy. Pick structures by access: list (order), dict (lookup), set (uniqueness).",
      "type": "concept",
      "title": "A value's type decides what its operations mean"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "strpath",
          "from": "input",
          "kind": "control",
          "tone": "warning",
          "label": "left as str"
        },
        {
          "to": "concat",
          "from": "strpath",
          "kind": "data",
          "tone": "warning",
          "label": "+ concatenates"
        },
        {
          "to": "convert",
          "from": "input",
          "kind": "control",
          "tone": "accent",
          "label": "convert first"
        },
        {
          "to": "intpath",
          "from": "convert",
          "kind": "data",
          "tone": "success",
          "label": "now numbers"
        },
        {
          "to": "sum",
          "from": "intpath",
          "kind": "data",
          "tone": "success",
          "label": "+ adds"
        }
      ],
      "nodes": [
        {
          "id": "input",
          "kind": "client",
          "tone": "warning",
          "label": "Raw input",
          "description": "\"2\", \"3\" from a form"
        },
        {
          "id": "strpath",
          "kind": "process",
          "tone": "warning",
          "label": "str + str",
          "description": "no conversion"
        },
        {
          "id": "concat",
          "kind": "store",
          "tone": "warning",
          "label": "\"23\"",
          "description": "concatenated — the bug"
        },
        {
          "id": "convert",
          "kind": "decision",
          "tone": "accent",
          "label": "int() at the edge",
          "description": "parse deliberately"
        },
        {
          "id": "intpath",
          "kind": "process",
          "tone": "success",
          "label": "int + int",
          "description": "real numeric types"
        },
        {
          "id": "sum",
          "kind": "store",
          "tone": "success",
          "label": "5",
          "description": "correct sum"
        }
      ],
      "title": "Same `+`, different meaning — the type dispatches the operation",
      "legend": [
        {
          "tone": "warning",
          "label": "stringly-typed — silent concat"
        },
        {
          "tone": "accent",
          "label": "convert at the boundary"
        },
        {
          "tone": "success",
          "label": "real types → correct math"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Form/API input arrives as `str`. Until you convert, `+` concatenates instead of adding.",
      "storyboard": [
        {
          "say": "Your form adds two numbers but shows '23' instead of 5. The trail starts here, where raw input arrives from the outside world.",
          "nodes": [
            "input"
          ],
          "edges": [],
          "ms": 4000
        },
        {
          "say": "Form, API, and file input all arrive as text. Python sees '2' and '3' as strings, not numbers, unless you say otherwise.",
          "nodes": [
            "input",
            "strpath"
          ],
          "edges": [
            [
              "input",
              "strpath"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Left as strings, the plus sign concatenates: it glues '2' and '3' end to end, producing the string '23'.",
          "nodes": [
            "strpath",
            "concat"
          ],
          "edges": [
            [
              "strpath",
              "concat"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "The fix is a single decision at the boundary: convert with int() the moment the value crosses into your code.",
          "nodes": [
            "input",
            "convert"
          ],
          "edges": [
            [
              "input",
              "convert"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "Now the same values are real integers, so the same plus sign does arithmetic instead of gluing text.",
          "nodes": [
            "convert",
            "intpath"
          ],
          "edges": [
            [
              "convert",
              "intpath"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "int plus int adds, and the form finally shows 5. The operator never changed; the type did.",
          "nodes": [
            "intpath",
            "sum"
          ],
          "edges": [
            [
              "intpath",
              "sum"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "The lesson: type decides what plus, equals, and truthiness mean. Convert at the edge and the ghost of '2'+'3' disappears.",
          "nodes": [
            "convert",
            "concat",
            "sum"
          ],
          "edges": [],
          "ms": 4800
        }
      ]
    },
    {
      "type": "lab",
      "check": "PASS: True",
      "title": "Don't be fooled by a string",
      "starter": "def total_quantity(values):\n    # values is a list of STRINGS, e.g. [\"2\", \"3\", \"5\"]. Convert each to an int\n    # and RETURN the integer sum. If a value isn't a whole number, raise ValueError.\n    ...  # your code here\n\n\n# --- test harness (do not edit) ---\nprint(\"sum:\", total_quantity([\"2\", \"3\", \"5\"]))     # must be 10 (int), not \"235\"\nok = total_quantity([\"2\", \"3\", \"5\"]) == 10\ntry:\n    total_quantity([\"2\", \"oops\"])\n    rejected = False\nexcept ValueError:\n    rejected = True\nprint(f\"PASS: {ok and rejected}\")\n",
      "summary": "Implement total_quantity(values) where values is a list of strings like ['2','3','5']. Convert each to an int and RETURN the sum — '2'+'3' must give 5, not '23'. Reject any value that isn't a whole number.",
      "language": "python"
    },
    {
      "code": "def order_total(qty_str, price_str):\n    try:\n        qty = int(qty_str)\n        price = float(price_str)\n    except ValueError:\n        raise ValueError(\"quantity and price must be numbers\")\n    if qty < 0 or price < 0:\n        raise ValueError(\"quantity and price must be non-negative\")\n    return qty * price",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "qty_str and price_str are STRINGS — form/file/API data almost always is.",
          "label": "Know the incoming type",
          "lines": [
            1
          ]
        },
        {
          "note": "`int(...)` and `float(...)` parse deliberately. Past this point the code works with real numbers.",
          "label": "Convert explicitly at the edge",
          "lines": [
            3,
            4
          ]
        },
        {
          "note": "`int(\"oops\")` raises ValueError — catch it and reject with a clear message, exactly like Lesson 1.",
          "label": "Handle conversion failure",
          "lines": [
            5,
            6
          ]
        },
        {
          "note": "Now that they are numbers, a range check makes sense (negatives rejected).",
          "label": "Validate the real value",
          "lines": [
            7,
            8
          ]
        },
        {
          "note": "qty * price — never `str + int`, which would raise TypeError, or `str + str`, which would concatenate.",
          "label": "Compute on the real type",
          "lines": [
            9
          ]
        }
      ],
      "title": "Convert at the boundary, then compute on real types",
      "caption": "Common mistake: assuming input is already the right type. Convert before you compute, or `+` will concatenate.",
      "filename": "orders.py",
      "language": "python",
      "subtitle": "Parse strings to int/float at the edge, handle the failure, then math is safe."
    },
    {
      "fix": "`values` are strings, and `+` on strings concatenates. Convert each to int first: `return sum(int(v) for v in values)` — and let `int()` reject non-numbers.",
      "task": "Why does it concatenate instead of add?",
      "type": "debug",
      "symptom": "This sums quantities but returns '235' instead of 10.",
      "language": "python",
      "brokenCode": "def total_quantity(values):\n    result = values[0]\n    for v in values[1:]:\n        result = result + v     # values are strings -> this concatenates\n    return result"
    },
    {
      "type": "tradeoff",
      "question": "String input: convert at the boundary vs pass it around",
      "optionA": {
        "label": "Convert at the boundary",
        "text": "Parse to int/float/date the moment it enters · One place handles conversion failure · Everything downstream works with real types · A type error surfaces at the edge, near the cause · Cost: an explicit parse + error path up front Outcome: Bugs caught at the source"
      },
      "optionB": {
        "label": "Keep it a string",
        "text": "Pass the raw string deeper into the code · Every caller must remember to convert · One forgotten int() → silent concatenation · Bugs hide far from where input arrived · Simpler at the edge, fragile everywhere else Outcome: Stringly-typed — bugs hide downstream"
      },
      "guidance": "Convert at the boundary; once past the edge, code should work with real types, not stringly-typed data."
    },
    {
      "left": {
        "tone": "success",
        "label": "Convert at the boundary",
        "lines": [
          "Parse to int/float/date the moment it enters",
          "One place handles conversion failure",
          "Everything downstream works with real types",
          "A type error surfaces at the edge, near the cause",
          "Cost: an explicit parse + error path up front"
        ],
        "verdict": "Bugs caught at the source"
      },
      "type": "compare",
      "right": {
        "tone": "warning",
        "label": "Keep it a string",
        "lines": [
          "Pass the raw string deeper into the code",
          "Every caller must remember to convert",
          "One forgotten int() → silent concatenation",
          "Bugs hide far from where input arrived",
          "Simpler at the edge, fragile everywhere else"
        ],
        "verdict": "Stringly-typed — bugs hide downstream"
      },
      "title": "String input: convert at the boundary vs pass it around",
      "caption": "Convert at the boundary; once past the edge, code should work with real types, not stringly-typed data.",
      "subtitle": "\"Parse, don't smear.\" Where you convert decides where the bugs live."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "total_quantity(['2','3','5']) returns 10 (an int, not '235')",
        "'2' + '3' produces 5, not '23' — values are converted, not concatenated",
        "A non-numeric value is REJECTED with a ValueError",
        "The function returns the right TYPE (int, not str)",
        "A test covers a normal case AND a bad-input case"
      ]
    },
    {
      "type": "quiz",
      "answer": 2,
      "options": [
        "Returns 26",
        "Returns '251'",
        "Raises TypeError (can’t add str + int)",
        "Returns 25"
      ],
      "question": "Input from a web form arrives as `age = '25'`. What does `age + 1` do?",
      "explanation": "`age` is the string '25'. `'25' + 1` raises TypeError — you can't add a str and an int. Convert first: `int(age) + 1` → 26. Form/API input is strings; convert deliberately."
    },
    {
      "type": "teachback",
      "prompts": [
        "Why does '2' + '3' give '23' but 2 + 3 give 5?",
        "Where should you convert input types — and why there?",
        "Name a truthiness pitfall (e.g. with 0 or an empty list).",
        "When would you reach for a dict instead of a list?"
      ]
    },
    {
      "text": "Find a place in your own code where input crosses a boundary (a form field, CLI arg, JSON value). Convert it to its real type at the edge with explicit error handling, and pick the data structure that fits the access pattern. With clean types in hand, the next lesson tackles the SHAPE of your logic — the nested ifs and loops that turn correct data into incorrect results.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Lab checkpoint passed — ['2','3'] sums to 5 and a non-number is rejected",
        "Broken case understood (string `+` concatenates)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own code"
      ]
    }
  ],
  "control-flow": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A filter that drops items WITHOUT mutating-while-iterating, plus a test that proves adjacent matches are not skipped.",
      "unlock": "All verification checks confirmed and the skip bug fixed.",
      "outcome": "Flatten nested conditionals with guard clauses, write a correct loop, and avoid the classic loop bugs (off-by-one, infinite loop, mutate-while-iterating).",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until your filter keeps the right items with two cancelled orders next to each other — the case that trips the skip bug."
    },
    {
      "text": "A teammate's `process_order()` is five levels of nested if/else — nobody, including the author, can tell which path runs. And the loop that 'removes cancelled orders' silently leaves half of them behind. Your data is clean by now; today you keep the LOGIC clean too — guard clauses for the path, a correct loop for the filter.",
      "type": "mission"
    },
    {
      "text": "Control flow is the shape of your logic. Deeply nested conditionals hide bugs; guard clauses make the valid path obvious. And loops have a famous set of traps that bite everyone.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: what goes wrong if you loop over a list and remove items from it as you go?",
      "reveal": "Removing an item shifts the later indices, so the loop skips the element right after each removal — you end up processing every other item. Iterate over a COPY, or (better) build a NEW list with the items you want to keep (a comprehension)."
    },
    {
      "type": "worked-example",
      "intro": "The guard-clause version, line by line. Each guard is one reason to stop; the happy path lands flat at the end.",
      "code": "def charge(order):\n    if order is None:\n        return \"missing\"\n    if order[\"total\"] <= 0:\n        return \"empty\"\n    if order[\"status\"] != \"open\":\n        return \"not open\"\n    return pay(order)",
      "language": "python",
      "steps": [
        "Reject the bad case early: Handle missing first with its own return — no nesting, one reason to stop.",
        "Next invalid case: Empty order: another flat guard. Read top to bottom like a checklist.",
        "Last guard: Wrong status bails out here. Every failure mode is named and unindented.",
        "Happy path, flat: Once the guards pass, the real work runs at the base indent — obvious and impossible to miss."
      ],
      "commonMistake": "This loop should remove cancelled orders but leaves half of them behind."
    },
    {
      "text": "A guard clause handles an invalid case and returns immediately, so the happy path stays flat (nesting past ~3 levels is a refactor signal). The three loop traps: off-by-one, infinite loop, and mutate-while-iterating. To prune a list, build a new one — never remove in place.",
      "type": "concept",
      "title": "Guard clauses flatten logic; loops have classic traps"
    },
    {
      "left": {
        "tone": "warning",
        "label": "Before — deep nesting",
        "lines": [
          "def charge(order):",
          "    if order is not None:",
          "        if order[\"total\"] > 0:",
          "            if order[\"status\"] == \"open\":",
          "                return pay(order)",
          "            else: return \"not open\"",
          "        else: return \"empty\"",
          "    else: return \"missing\""
        ],
        "verdict": "Which path runs? Buried in indentation"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "After — guard clauses",
        "lines": [
          "def charge(order):",
          "    if order is None:",
          "        return \"missing\"",
          "    if order[\"total\"] <= 0:",
          "        return \"empty\"",
          "    if order[\"status\"] != \"open\":",
          "        return \"not open\"",
          "    return pay(order)   # happy path, flat"
        ],
        "verdict": "Reject bad cases first; the win is unindented"
      },
      "title": "Same logic: deep nesting vs guard clauses",
      "caption": "Common mistake: each `if` adding another level. Invert it — `if not valid: return` early, keep the happy path flat.",
      "subtitle": "Both return the same answers. Only one lets you see which path runs."
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "i1",
          "from": "i0",
          "kind": "control",
          "label": "advance"
        },
        {
          "to": "shift",
          "from": "i1",
          "kind": "async",
          "tone": "warning",
          "label": "remove()"
        },
        {
          "to": "skip",
          "from": "shift",
          "kind": "control",
          "tone": "warning",
          "label": "cursor moves on"
        },
        {
          "to": "fix",
          "from": "i1",
          "kind": "data",
          "tone": "success",
          "label": "instead:",
          "dashed": true
        }
      ],
      "nodes": [
        {
          "id": "i0",
          "kind": "process",
          "tone": "success",
          "label": "i=0 active",
          "description": "kept"
        },
        {
          "id": "i1",
          "kind": "decision",
          "tone": "warning",
          "label": "i=1 cancelled",
          "description": "removed → list shifts left"
        },
        {
          "id": "shift",
          "kind": "queue",
          "tone": "warning",
          "label": "index 2 → 1",
          "description": "next cancelled slides under cursor"
        },
        {
          "id": "skip",
          "kind": "external",
          "tone": "warning",
          "label": "cursor → i=2",
          "description": "the shifted item is SKIPPED"
        },
        {
          "id": "fix",
          "kind": "store",
          "tone": "success",
          "label": "Build a new list",
          "description": "[o for o in orders if keep(o)]"
        }
      ],
      "title": "Why remove-while-iterating skips items",
      "legend": [
        {
          "tone": "warning",
          "label": "mutation shifts indices → skip"
        },
        {
          "tone": "success",
          "label": "filter into a new list — no mutation"
        }
      ],
      "rankdir": "LR",
      "subtitle": "The loop cursor advances by index. A removal shifts every later item left, so the next one slides under the cursor unseen.",
      "storyboard": [
        {
          "say": "The loop starts at index zero, processing the first active order. The cursor sits on i=0 and everything looks fine.",
          "nodes": [
            "i0"
          ],
          "edges": [],
          "ms": 3600
        },
        {
          "say": "The cursor advances to index one, where it finds a cancelled order that we've decided to remove.",
          "nodes": [
            "i0",
            "i1"
          ],
          "edges": [
            [
              "i0",
              "i1"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "We call remove() on the cancelled item at index one, deleting it from the list in place.",
          "nodes": [
            "i1",
            "shift"
          ],
          "edges": [
            [
              "i1",
              "shift"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Here's the trap: removing index one shifts every later item left, so what was index two now becomes index one.",
          "nodes": [
            "shift"
          ],
          "edges": [],
          "ms": 4800
        },
        {
          "say": "But the cursor still advances to index two, skipping right over the item that just slid into slot one, unseen.",
          "nodes": [
            "shift",
            "skip"
          ],
          "edges": [
            [
              "shift",
              "skip"
            ]
          ],
          "ms": 4800
        },
        {
          "say": "That's how a loop meant to remove all cancelled orders silently leaves half of them behind.",
          "nodes": [
            "skip",
            "i1"
          ],
          "edges": [],
          "ms": 4000
        },
        {
          "say": "The fix is simple: never mutate while iterating. Build a new list of the items you want to keep instead.",
          "nodes": [
            "i1",
            "fix"
          ],
          "edges": [
            [
              "i1",
              "fix"
            ]
          ],
          "ms": 4600
        }
      ]
    },
    {
      "type": "lab",
      "check": "PASS: True",
      "title": "Filter without skipping",
      "starter": "def active_orders(orders):\n    # Return only the orders whose status != 'cancelled'.\n    # Do NOT remove from `orders` while looping over it — build a NEW list.\n    ...  # your code here\n\n\n# --- test harness (do not edit) ---\ndata = [\n    {\"id\": 1, \"status\": \"active\"},\n    {\"id\": 2, \"status\": \"cancelled\"},\n    {\"id\": 3, \"status\": \"cancelled\"},   # adjacent cancelled -> trips the skip bug\n    {\"id\": 4, \"status\": \"active\"},\n]\nids = [o[\"id\"] for o in active_orders(data)]\nids2 = [o[\"id\"] for o in active_orders([\n    {\"id\": 9, \"status\": \"cancelled\"},\n    {\"id\": 8, \"status\": \"active\"},\n])]\nprint(\"kept ids:\", ids)\nprint(f\"PASS: {ids == [1, 4] and ids2 == [8] and len(data) == 4}\")\n",
      "summary": "Implement active_orders(orders): return only the orders whose status != 'cancelled'. Do NOT mutate the input while iterating. The harness puts two cancelled orders next to each other (the case that trips the skip bug) and checks none slip through — across two datasets.",
      "language": "python"
    },
    {
      "code": "def charge(order):\n    if order is None:\n        return \"missing\"\n    if order[\"total\"] <= 0:\n        return \"empty\"\n    if order[\"status\"] != \"open\":\n        return \"not open\"\n    return pay(order)",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "Handle missing first with its own return — no nesting, one reason to stop.",
          "label": "Reject the bad case early",
          "lines": [
            2,
            3
          ]
        },
        {
          "note": "Empty order: another flat guard. Read top to bottom like a checklist.",
          "label": "Next invalid case",
          "lines": [
            4,
            5
          ]
        },
        {
          "note": "Wrong status bails out here. Every failure mode is named and unindented.",
          "label": "Last guard",
          "lines": [
            6,
            7
          ]
        },
        {
          "note": "Once the guards pass, the real work runs at the base indent — obvious and impossible to miss.",
          "label": "Happy path, flat",
          "lines": [
            8
          ]
        }
      ],
      "title": "The guard-clause version, line by line",
      "caption": "Same behavior as the five-level nest — but you can see the path, and you can see which case you forgot.",
      "filename": "orders.py",
      "language": "python",
      "subtitle": "Each guard is one reason to stop; the happy path lands flat at the end."
    },
    {
      "fix": "`remove` shifts every later index down by one, so the loop skips the item right after each removal (and it mutates the caller's list). Build a new list instead: `[o for o in orders if o['status'] != 'cancelled']`.",
      "task": "Why does it skip some cancelled orders?",
      "type": "debug",
      "symptom": "This loop should remove cancelled orders but leaves half of them behind.",
      "language": "python",
      "brokenCode": "def active_orders(orders):\n    for o in orders:\n        if o[\"status\"] == \"cancelled\":\n            orders.remove(o)     # mutating the list we're iterating\n    return orders"
    },
    {
      "type": "tradeoff",
      "question": "Same logic: deep nesting vs guard clauses",
      "optionA": {
        "label": "Before — deep nesting",
        "text": "def charge(order): ·     if order is not None: ·         if order[\"total\"] > 0: ·             if order[\"status\"] == \"open\": ·                 return pay(order) ·             else: return \"not open\" ·         else: return \"empty\" ·     else: return \"missing\" Outcome: Which path runs? Buried in indentation"
      },
      "optionB": {
        "label": "After — guard clauses",
        "text": "def charge(order): ·     if order is None: ·         return \"missing\" ·     if order[\"total\"] <= 0: ·         return \"empty\" ·     if order[\"status\"] != \"open\": ·         return \"not open\" ·     return pay(order)   # happy path, flat Outcome: Reject bad cases first; the win is unindented"
      },
      "guidance": "Common mistake: each `if` adding another level. Invert it — `if not valid: return` early, keep the happy path flat."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "active_orders keeps only the non-cancelled orders",
        "Two ADJACENT cancelled orders are both dropped — none skipped",
        "The input list is NOT mutated (no remove-while-iterating)",
        "The happy path is flat (guard clauses, not deep nesting)",
        "A test covers adjacent-cancelled and an all-cancelled case"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Call list.remove() inside a for loop over the list",
        "Build a new list with a comprehension (filter)",
        "Delete by index in a while loop",
        "Set the items to None"
      ],
      "question": "What's the safest way to drop items from a list while looping?",
      "explanation": "Mutating a list while iterating it skips elements because the indices shift. Build a new list: `[x for x in items if keep(x)]` — correct, readable, and it leaves the original untouched."
    },
    {
      "type": "teachback",
      "prompts": [
        "What does a guard clause do, and why does it flatten logic?",
        "Why does removing items while looping skip elements?",
        "Name the three classic loop bugs.",
        "When is a while loop the right choice over a for loop?"
      ]
    },
    {
      "text": "Find a loop in your own code that removes or edits a collection while iterating it, or a function nested more than three levels deep. Refactor: filter into a new list, or add guard clauses. Add a test for the edge that used to break. So far every value has lived in memory, where nothing can go wrong reaching it — next lesson steps outside the program, into files, where the resource itself can leak or vanish.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Lab checkpoint passed — adjacent cancelled orders are both dropped, input not mutated",
        "Broken case understood (mutating while iterating skips items)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own code"
      ]
    }
  ],
  "files-and-io": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A reader that uses `with`, parses key=value lines, and lets a missing file raise — plus a test for a valid and a missing file.",
      "unlock": "All verification checks confirmed and the leak / swallowed-missing-file bugs fixed.",
      "outcome": "Read a file with a context manager so it always closes, parse it into a dict, stream large files instead of loading them, and handle the missing-file case explicitly.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until your reader closes the file on every path and a missing file raises (not a silent empty dict)."
    },
    {
      "text": "A nightly job crashed because a config file was missing — and even when it worked, it leaked open file handles until the process ran out of them. The moment you reach for a file, the outside world can fail in ways pure code never does. Read files the safe way: always close, and handle missing explicitly.",
      "type": "mission"
    },
    {
      "text": "Files (and stdin/stdout, network streams) are I/O — slow, fallible, and full of resources you must release. The `with` statement and explicit error handling are the difference between a robust job and a 3am page.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: why is `f = open('x'); data = f.read()` riskier than `with open('x') as f:`?",
      "reveal": "If anything between `open` and `f.close()` raises, the file is never closed — you leak a file handle (and on some systems keep the file locked). `with open(...) as f:` guarantees the file closes even when an error occurs. Always use `with` for files."
    },
    {
      "type": "worked-example",
      "intro": "Read a config safely, line by line. A context manager that always closes, streams the file, and lets a missing file surface.",
      "code": "def read_config(path):\n    config = {}\n    with open(path) as f:\n        for line in f:\n            line = line.strip()\n            if not line:\n                continue\n            key, value = line.split(\"=\", 1)\n            config[key] = value\n    return config",
      "language": "python",
      "steps": [
        "Open with `with`: Closes the file on every path — success or error. A missing file raises FileNotFoundError here, never a silent {}.",
        "Stream, do not slurp: Iterating the file object reads ONE line at a time. A `.read()` would load a multi-gigabyte file into memory.",
        "Skip blanks defensively: Strip whitespace; ignore empty lines so a stray blank does not crash the parse.",
        "Parse with a bound: `split(\"=\", 1)` splits on the FIRST `=` only — a value containing `=` survives intact.",
        "Return a real value: The handle is already closed by the time we return — the `with` block exited."
      ],
      "commonMistake": "This config reader leaks file handles and silently returns {} when the file is missing."
    },
    {
      "text": "A context manager (`with`) closes its resource when the block exits — even on an exception. Iterate a file object to STREAM line by line; never `.read()` a multi-gigabyte file into memory. Surface I/O failures (missing, permission, encoding); never swallow them.",
      "type": "concept",
      "title": "`with` guarantees cleanup; I/O is slow and fallible"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "missing",
          "from": "open",
          "kind": "sync",
          "label": "try to open"
        },
        {
          "to": "close",
          "from": "missing",
          "kind": "control",
          "tone": "warning",
          "label": "raises → still closes"
        },
        {
          "to": "stream",
          "from": "missing",
          "kind": "data",
          "tone": "success",
          "label": "opened"
        },
        {
          "to": "raise",
          "from": "stream",
          "kind": "sync",
          "label": "per line"
        },
        {
          "to": "close",
          "from": "raise",
          "kind": "control",
          "tone": "warning",
          "label": "error → still closes"
        },
        {
          "to": "close",
          "from": "raise",
          "kind": "data",
          "tone": "success",
          "label": "done → closes"
        }
      ],
      "nodes": [
        {
          "id": "open",
          "kind": "process",
          "tone": "accent",
          "label": "with open(path)",
          "description": "acquire the handle"
        },
        {
          "id": "missing",
          "kind": "decision",
          "tone": "warning",
          "label": "Missing file?",
          "description": "FileNotFoundError surfaces"
        },
        {
          "id": "stream",
          "kind": "process",
          "tone": "success",
          "label": "for line in f",
          "description": "stream, not .read()"
        },
        {
          "id": "raise",
          "kind": "decision",
          "tone": "warning",
          "label": "Parse error?",
          "description": "exception mid-read"
        },
        {
          "id": "close",
          "kind": "store",
          "tone": "success",
          "label": "Handle closed",
          "description": "guaranteed on every path"
        }
      ],
      "title": "The `with` block guarantees the close — on every exit",
      "legend": [
        {
          "tone": "accent",
          "label": "acquire"
        },
        {
          "tone": "success",
          "label": "stream + guaranteed close"
        },
        {
          "tone": "warning",
          "label": "errors surface, handle still released"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Whether the body succeeds, raises, or hits a missing file, the context manager releases the handle. A bare `open()` only closes on the happy path.",
      "storyboard": [
        {
          "say": "A nightly job leaks handles and crashes on a missing config. The fix starts here: with open(path) claims the file under a context manager.",
          "nodes": [
            "open"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "The first thing the outside world can break is existence. We try to open, and ask: is the file even there?",
          "nodes": [
            "open",
            "missing"
          ],
          "edges": [
            [
              "open",
              "missing"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "If the file is missing, open raises — but the context manager still runs its exit, so the handle is released, not leaked.",
          "nodes": [
            "missing",
            "close"
          ],
          "edges": [
            [
              "missing",
              "close"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "On the happy path the file opened, so we stream it: for line in f, one line at a time, never loading gigabytes into memory.",
          "nodes": [
            "missing",
            "stream"
          ],
          "edges": [
            [
              "missing",
              "stream"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Per line, a new risk appears: a parse or encoding error mid-stream. Surface it, don't swallow it.",
          "nodes": [
            "stream",
            "raise"
          ],
          "edges": [
            [
              "stream",
              "raise"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Whether that line raises an error or the loop finishes cleanly, both paths land at the same place: the handle gets closed.",
          "nodes": [
            "raise",
            "close"
          ],
          "edges": [
            [
              "raise",
              "close"
            ],
            [
              "raise",
              "close"
            ]
          ],
          "ms": 5000
        },
        {
          "say": "That's the lesson: with guarantees the close on every exit — success, error, or missing file. A bare open only cleans up on the happy path.",
          "nodes": [
            "open",
            "close"
          ],
          "edges": [],
          "ms": 4800
        }
      ]
    },
    {
      "type": "lab",
      "check": "PASS: True",
      "title": "Read it safely",
      "starter": "def read_config(path):\n    # Open the file with a `with` block (it always closes), read \"key=value\"\n    # lines into a dict, skipping blank lines. If the file is MISSING, let it\n    # raise FileNotFoundError — do NOT silently return {}.\n    ...  # your code here\n\n\n# --- test harness (do not edit) ---\nwith open(\"app.conf\", \"w\") as f:\n    f.write(\"host=localhost\\n\\nport=8080\\n\")\ncfg = read_config(\"app.conf\")\nok = cfg == {\"host\": \"localhost\", \"port\": \"8080\"}\ntry:\n    read_config(\"missing.conf\")\n    missing_handled = False\nexcept FileNotFoundError:\n    missing_handled = True\nprint(\"config:\", cfg)\nprint(f\"PASS: {ok and missing_handled}\")\n",
      "summary": "Implement read_config(path): open with a `with` block, parse \"key=value\" lines into a dict (skip blanks), and let a MISSING file raise FileNotFoundError — never a silent {}. The harness writes a file, reads it back, and checks the missing-file case.",
      "language": "python"
    },
    {
      "code": "def read_config(path):\n    config = {}\n    with open(path) as f:\n        for line in f:\n            line = line.strip()\n            if not line:\n                continue\n            key, value = line.split(\"=\", 1)\n            config[key] = value\n    return config",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "Closes the file on every path — success or error. A missing file raises FileNotFoundError here, never a silent {}.",
          "label": "Open with `with`",
          "lines": [
            3
          ]
        },
        {
          "note": "Iterating the file object reads ONE line at a time. A `.read()` would load a multi-gigabyte file into memory.",
          "label": "Stream, do not slurp",
          "lines": [
            4
          ]
        },
        {
          "note": "Strip whitespace; ignore empty lines so a stray blank does not crash the parse.",
          "label": "Skip blanks defensively",
          "lines": [
            5,
            6,
            7
          ]
        },
        {
          "note": "`split(\"=\", 1)` splits on the FIRST `=` only — a value containing `=` survives intact.",
          "label": "Parse with a bound",
          "lines": [
            8,
            9
          ]
        },
        {
          "note": "The handle is already closed by the time we return — the `with` block exited.",
          "label": "Return a real value",
          "lines": [
            10
          ]
        }
      ],
      "title": "Read a config safely, line by line",
      "caption": "Common mistake: a bare `open()` without `with` — one exception before `close()` and the handle leaks. The same `with` pattern manages DB cursors and locks.",
      "filename": "config.py",
      "language": "python",
      "subtitle": "A context manager that always closes, streams the file, and lets a missing file surface."
    },
    {
      "fix": "(1) No `with`/close → the handle leaks if an error occurs mid-read. (2) `except Exception: return {}` swallows the missing-file (and parse) errors, so the job runs with empty config and fails mysteriously later. Use `with open(path)` and let FileNotFoundError surface.",
      "task": "Find the two bugs.",
      "type": "debug",
      "symptom": "This config reader leaks file handles and silently returns {} when the file is missing.",
      "language": "python",
      "brokenCode": "def read_config(path):\n    try:\n        f = open(path)                 # no `with` -> never closed on error\n        cfg = {}\n        for line in f.readlines():     # loads the whole file into memory\n            key, value = line.split(\"=\", 1)\n            cfg[key] = value\n        return cfg\n    except Exception:\n        return {}                      # swallows missing-file / parse errors"
    },
    {
      "type": "tradeoff",
      "question": "Reading a large file: load it all vs stream it",
      "optionA": {
        "label": "Read all",
        "text": "data = f.read() · # or f.readlines() · Whole file into memory at once · Random access — but a big file OOMs · Fine only when the file is small Outcome: Simple, but blows up on large files"
      },
      "optionB": {
        "label": "Stream",
        "text": "for line in f: ·     process(line) · One line in memory at a time · Constant memory, handles huge files · Sequential, single pass Outcome: Constant memory — the safe default"
      },
      "guidance": "Stream by default (`for line in f:`). Only load the whole file when it is small and you genuinely need random access."
    },
    {
      "left": {
        "tone": "warning",
        "label": "Read all",
        "lines": [
          "data = f.read()",
          "# or f.readlines()",
          "Whole file into memory at once",
          "Random access — but a big file OOMs",
          "Fine only when the file is small"
        ],
        "verdict": "Simple, but blows up on large files"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "success",
        "label": "Stream",
        "lines": [
          "for line in f:",
          "    process(line)",
          "One line in memory at a time",
          "Constant memory, handles huge files",
          "Sequential, single pass"
        ],
        "verdict": "Constant memory — the safe default"
      },
      "title": "Reading a large file: load it all vs stream it",
      "caption": "Stream by default (`for line in f:`). Only load the whole file when it is small and you genuinely need random access.",
      "subtitle": "Both produce the same lines; only one survives a gigabyte file."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "read_config uses `with`, so the file always closes",
        "It parses key=value lines into a dict, skipping blanks",
        "A MISSING file raises FileNotFoundError — it does NOT silently return {}",
        "A large file would stream line-by-line, not load into memory",
        "A test covers a valid file AND a missing file"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "It's faster",
        "It guarantees the file closes even if an error occurs",
        "It reads the whole file at once",
        "It validates the file path"
      ],
      "question": "Why use `with open(path) as f:` instead of `f = open(path)`?",
      "explanation": "`with` is a context manager: it closes the file automatically when the block exits — even on an exception. A manual `open()` leaks the handle if anything raises before `close()`."
    },
    {
      "type": "teachback",
      "prompts": [
        "What does the `with` statement guarantee for a file?",
        "Why stream a large file instead of reading it all at once?",
        "Why is `except: return {}` on a missing config dangerous?",
        "Name another resource (besides a file) that `with` can manage."
      ]
    },
    {
      "text": "Find file handling in your own code that uses a bare `open()` or swallows a missing-file error. Switch to `with`, stream if the file can be large, and let (or log-and-raise) the missing/unreadable case surface. Add a test for the missing file. You have now written five lessons of careful code — validation, error paths, pure functions, clean loops, safe I/O. Next lesson asks the only question that proves any of it: how do you KNOW it works before production finds out for you?",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Lab checkpoint passed — valid file parses, missing file raises",
        "Broken case understood (no `with` leaks; swallowed missing-file hides failures)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own code"
      ]
    }
  ],
  "testing-and-debugging": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A function that survives its edge case + a test suite that includes the regression case, all green.",
      "unlock": "All verification checks confirmed and the empty-input crash fixed with a regression test.",
      "outcome": "Write the cheapest test that catches a real bug — including the edge/negative case the happy path misses — and turn a bug into a regression test.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until your test suite includes the edge case that crashed in prod — not just the happy path."
    },
    {
      "text": "Your `average()` passed every test and shipped — then crashed in production on an empty list, because the tests only ever checked the happy path. A bad test is worse than none: it sells false confidence. Add the test that would have caught it, fix the bug, and stop confusing \"tests are green\" with \"the code is right\".",
      "type": "mission"
    },
    {
      "text": "Tests are how you change code without fear. The skill is choosing the cheapest test that catches the real risk — usually an edge or negative case — and turning every production bug into a regression test.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: `average()` works on [2, 4, 6] and passes its test. What input might still crash it in production?",
      "reveal": "The empty list: `sum([]) / len([])` is `0 / 0` → ZeroDivisionError. The happy-path test never tried it. Edge cases (empty, zero, negative, huge, missing) are where bugs hide — test those, not just the obvious case."
    },
    {
      "type": "worked-example",
      "intro": "Test the risk — Arrange / Act / Assert with the edge that bites. Guard the empty case, then a tiny suite that includes the regression case.",
      "code": "def safe_average(numbers):\n    if not numbers:\n        return 0\n    return sum(numbers) / len(numbers)\n\ndef test_safe_average():\n    assert safe_average([2, 4, 6]) == 4\n    assert safe_average([10]) == 10\n    assert safe_average([]) == 0\n    assert safe_average([-2, 2]) == 0",
      "language": "python",
      "steps": [
        "The edge the happy path forgot: An empty list short-circuits to 0 — `sum([]) / len([])` would be 0/0, a ZeroDivisionError.",
        "The normal computation: Only reached once the edge is handled. Order matters: guard first, compute second.",
        "Assert behavior, not internals: Normal and single-element cases. We check input → output, so a refactor of the body cannot break the test.",
        "The regression case: The empty list — the exact input that crashed prod. This line is why the bug can never silently return.",
        "Negatives too: Edge inputs (empty, zero, negative, huge, missing) are where bugs hide — test those, not just the obvious case."
      ],
      "commonMistake": "average() works in tests but crashed in production."
    },
    {
      "text": "Spend test effort where a failure would hurt. The pyramid — many fast unit, fewer integration, a few E2E — keeps feedback fast. Assert BEHAVIOR (inputs → outputs), not internals, so a refactor does not break them. Every prod bug becomes a regression test.",
      "type": "concept",
      "title": "Cheapest test that catches the failure; behavior over implementation"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "repro",
          "from": "bug",
          "kind": "sync",
          "tone": "accent",
          "label": "first: make it fail"
        },
        {
          "to": "isolate",
          "from": "repro",
          "kind": "control",
          "tone": "accent",
          "label": "narrow the cause"
        },
        {
          "to": "fix",
          "from": "isolate",
          "kind": "control",
          "tone": "success",
          "label": "minimal change"
        },
        {
          "to": "regress",
          "from": "fix",
          "kind": "data",
          "tone": "success",
          "label": "test now passes"
        },
        {
          "to": "bug",
          "from": "regress",
          "kind": "control",
          "tone": "muted",
          "label": "stops the return",
          "dashed": true
        }
      ],
      "nodes": [
        {
          "id": "bug",
          "kind": "client",
          "tone": "warning",
          "label": "Prod failure",
          "description": "average([]) crashed"
        },
        {
          "id": "repro",
          "kind": "process",
          "tone": "accent",
          "label": "Reproduce",
          "description": "a failing test for []"
        },
        {
          "id": "isolate",
          "kind": "process",
          "tone": "accent",
          "label": "Isolate",
          "description": "0 / 0 → ZeroDivisionError"
        },
        {
          "id": "fix",
          "kind": "process",
          "tone": "success",
          "label": "Fix",
          "description": "guard the empty case"
        },
        {
          "id": "regress",
          "kind": "store",
          "tone": "success",
          "label": "Regression test",
          "description": "green forever after"
        }
      ],
      "title": "The debug loop — every bug ends as a regression test",
      "legend": [
        {
          "tone": "accent",
          "label": "reproduce as a FAILING test first"
        },
        {
          "tone": "success",
          "label": "fix → regression test locks it"
        }
      ],
      "rankdir": "LR",
      "subtitle": "A production failure is not \"fixed\" until a test exists that fails before the fix and passes after. That test is the deliverable.",
      "storyboard": [
        {
          "say": "It starts with a production failure — your average() crashed on an empty list because the tests only ever checked the happy path.",
          "nodes": [
            "bug"
          ],
          "edges": [],
          "ms": 4200
        },
        {
          "say": "First move: reproduce it. Write a test that makes it fail — that failing test is the real proof the bug exists.",
          "nodes": [
            "bug",
            "repro"
          ],
          "edges": [
            [
              "bug",
              "repro"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "With a failing test in hand, narrow the cause — isolate the exact input and path that triggers the crash.",
          "nodes": [
            "repro",
            "isolate"
          ],
          "edges": [
            [
              "repro",
              "isolate"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Now the fix is small and targeted: the minimal change that makes that failing test go green.",
          "nodes": [
            "isolate",
            "fix"
          ],
          "edges": [
            [
              "isolate",
              "fix"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "The test that flipped from red to green is the deliverable — a regression test, kept forever, asserting behavior not internals.",
          "nodes": [
            "fix",
            "regress"
          ],
          "edges": [
            [
              "fix",
              "regress"
            ]
          ],
          "ms": 4800
        },
        {
          "say": "That stored test guards the door: if this bug ever returns, the suite catches it before it ships again.",
          "nodes": [
            "regress",
            "bug"
          ],
          "edges": [
            [
              "regress",
              "bug"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "The insight: a bug isn't fixed until a test would have caught it. Every prod failure becomes a permanent regression test.",
          "nodes": [
            "bug",
            "repro",
            "isolate",
            "fix",
            "regress"
          ],
          "edges": [
            [
              "fix",
              "regress"
            ],
            [
              "regress",
              "bug"
            ]
          ],
          "ms": 5000
        }
      ]
    },
    {
      "type": "lab",
      "check": "PASS: True",
      "title": "Catch the edge case",
      "starter": "def safe_average(numbers):\n    # Return the average of the numbers. EDGE CASE: an EMPTY list must return 0,\n    # not crash with ZeroDivisionError (sum([]) / len([]) is 0 / 0).\n    ...  # your code here\n\n\n# --- test harness: a tiny suite (happy path + the edge that bites) ---\nassert safe_average([2, 4, 6]) == 4, \"normal case\"\nassert safe_average([10]) == 10, \"single element\"\nassert safe_average([]) == 0, \"EMPTY list must return 0, not crash\"   # regression\nprint(\"all assertions passed\")\nprint(\"PASS: True\")\n",
      "summary": "Implement safe_average(numbers): return the mean, but an EMPTY list must return 0 — not crash with ZeroDivisionError. The harness is a tiny test suite that includes the empty-list regression case; make every assertion pass.",
      "language": "python"
    },
    {
      "code": "def safe_average(numbers):\n    if not numbers:\n        return 0\n    return sum(numbers) / len(numbers)\n\ndef test_safe_average():\n    assert safe_average([2, 4, 6]) == 4\n    assert safe_average([10]) == 10\n    assert safe_average([]) == 0\n    assert safe_average([-2, 2]) == 0",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "An empty list short-circuits to 0 — `sum([]) / len([])` would be 0/0, a ZeroDivisionError.",
          "label": "The edge the happy path forgot",
          "lines": [
            2,
            3
          ]
        },
        {
          "note": "Only reached once the edge is handled. Order matters: guard first, compute second.",
          "label": "The normal computation",
          "lines": [
            4
          ]
        },
        {
          "note": "Normal and single-element cases. We check input → output, so a refactor of the body cannot break the test.",
          "label": "Assert behavior, not internals",
          "lines": [
            7,
            8
          ]
        },
        {
          "note": "The empty list — the exact input that crashed prod. This line is why the bug can never silently return.",
          "label": "The regression case",
          "lines": [
            9
          ]
        },
        {
          "note": "Edge inputs (empty, zero, negative, huge, missing) are where bugs hide — test those, not just the obvious case.",
          "label": "Negatives too",
          "lines": [
            10
          ]
        }
      ],
      "title": "Test the risk — Arrange / Act / Assert with the edge that bites",
      "caption": "Common mistake: testing only the happy path. The bug lives in the case you did not try.",
      "filename": "stats.py",
      "language": "python",
      "subtitle": "Guard the empty case, then a tiny suite that includes the regression case."
    },
    {
      "fix": "An empty list → `sum([]) / len([])` → `0 / 0` → ZeroDivisionError. The happy-path test never tried `[]`. Guard it (`if not nums: return 0`) and add `assert average([]) == 0` as a regression test.",
      "task": "What input crashes it, and what test was missing?",
      "type": "debug",
      "symptom": "average() works in tests but crashed in production.",
      "language": "python",
      "brokenCode": "def average(nums):\n    return sum(nums) / len(nums)   # passes on [2,4,6]...\n\n# the only test:\nassert average([2, 4, 6]) == 4"
    },
    {
      "type": "tradeoff",
      "question": "You found a bug: just fix it vs fix it AND add a test",
      "optionA": {
        "label": "Fix + regression test",
        "text": "Write the failing test FIRST (reproduces the bug) · Then fix until it is green · Test fails before the fix, passes after · The next refactor can never silently break it · Cost: one extra test Outcome: The bug can never silently return"
      },
      "optionB": {
        "label": "Just fix it",
        "text": "Patch the bug and move on · No test documents what was wrong · Faster right now · Same bug reappears in the next refactor · Nobody knows it was ever fixed Outcome: Fast today, the bug returns tomorrow"
      },
      "guidance": "Always turn a bug into a regression test — the failing-test-first loop produces the test most likely to catch a real failure."
    },
    {
      "left": {
        "tone": "success",
        "label": "Fix + regression test",
        "lines": [
          "Write the failing test FIRST (reproduces the bug)",
          "Then fix until it is green",
          "Test fails before the fix, passes after",
          "The next refactor can never silently break it",
          "Cost: one extra test"
        ],
        "verdict": "The bug can never silently return"
      },
      "type": "compare",
      "right": {
        "tone": "warning",
        "label": "Just fix it",
        "lines": [
          "Patch the bug and move on",
          "No test documents what was wrong",
          "Faster right now",
          "Same bug reappears in the next refactor",
          "Nobody knows it was ever fixed"
        ],
        "verdict": "Fast today, the bug returns tomorrow"
      },
      "title": "You found a bug: just fix it vs fix it AND add a test",
      "caption": "Always turn a bug into a regression test — the failing-test-first loop produces the test most likely to catch a real failure.",
      "subtitle": "Same patch ships. Only one stops the bug from coming back."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "safe_average([2,4,6]) returns 4",
        "safe_average([]) returns 0 — it does NOT crash",
        "The test suite includes the empty-list edge (the regression case)",
        "The tests assert behavior (input → output), not internals",
        "Each assertion has a clear message"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Delete the failing-in-prod path",
        "Add a regression test for the empty input, then fix the bug",
        "Wrap everything in try/except",
        "Only test in production"
      ],
      "question": "A function passes its tests but crashed in prod on an empty input. Best next step?",
      "explanation": "Turn the bug into a regression test: write a test for the empty input that fails now, then fix until it’s green. It documents the bug and stops it returning. Happy-path-only tests give false confidence."
    },
    {
      "type": "teachback",
      "prompts": [
        "What is 'the cheapest test that catches the failure'?",
        "Why test behavior instead of implementation details?",
        "What's the loop for turning a bug into a regression test?",
        "Name two edge cases worth testing for a function that takes a list."
      ]
    },
    {
      "text": "Take a function in your own code and write the cheapest test that catches its riskiest failure — an edge or negative case, not the happy path. If you hit a bug recently, add a regression test that fails before your fix and passes after. Tests protect your code from your future self; next lesson protects production from your present self — the command-line tools that can delete real data with a single forgotten flag.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Lab checkpoint passed — empty list returns 0, all assertions green",
        "Broken case understood (happy-path-only tests miss the edge)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own code"
      ]
    }
  ],
  "cli-workflow": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A safety gate that previews by default and blocks an unconfirmed production action, plus tests for each path.",
      "unlock": "All verification checks confirmed and the dangerous defaults removed.",
      "outcome": "Build a command that defaults to dry-run, never defaults to production, and requires explicit confirmation for high-blast-radius actions.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until a forgotten flag results in a dry-run, not a production deletion."
    },
    {
      "text": "An internal cleanup script ran with its defaults and deleted production data — because 'no environment specified' quietly meant prod, and nothing asked the human to confirm. There is no try/except for a deleted table. Make the tool safe by default, so a forgotten flag previews instead of destroys.",
      "type": "mission"
    },
    {
      "text": "Internal CLI tools are sharp: a wrong flag or a defaulted environment can delete real data with no undo. Safe defaults, dry-run previews, and confirmation for high-impact actions separate a useful tool from an outage.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: why is it dangerous for a destructive command to DEFAULT to executing — or to default the environment to production?",
      "reveal": "Defaults are what run when someone forgets a flag. If \"execute\" or \"production\" is the default, a tired engineer who omits a flag destroys real data. Safe tools default to dry-run and require explicit opt-in for destructive, production actions."
    },
    {
      "type": "worked-example",
      "intro": "Safe by default — the decision gate. Dry-run unless explicitly told to execute; production needs confirmation.",
      "code": "def decide(args):\n    if not args.get(\"execute\"):\n        return \"dry-run\"\n    if args.get(\"env\") == \"production\" and not args.get(\"confirmed\"):\n        return \"blocked: confirm production\"\n    return \"execute\"",
      "language": "python",
      "steps": [
        "Default to dry-run: The FIRST check: no explicit execute → preview only. A forgotten flag can never destroy data.",
        "Guard production: Even with execute=True, production without confirmed=True is blocked. No accidental prod, ever.",
        "Execute only when earned: Reached only when the caller explicitly opted in AND (non-prod or confirmed). The safe path is the default; the dangerous path is opt-in."
      ],
      "commonMistake": "This deploy tool deleted production records when it was run with no arguments."
    },
    {
      "text": "Safe tools default to the harmless option: a DRY-RUN that previews the exact target set first. Limit BLAST RADIUS (explicit scope, no bare wildcards). Require CONFIRMATION for high-impact actions; never default env to production. Keep an AUDIT TRAIL. The default is what runs when a flag is forgotten.",
      "type": "concept",
      "title": "Safe defaults · dry-run · blast radius · confirmation"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "exec",
          "from": "cmd",
          "kind": "sync",
          "label": "evaluate"
        },
        {
          "to": "dry",
          "from": "exec",
          "kind": "control",
          "tone": "success",
          "label": "no / missing"
        },
        {
          "to": "prod",
          "from": "exec",
          "kind": "control",
          "tone": "accent",
          "label": "yes"
        },
        {
          "to": "blocked",
          "from": "prod",
          "kind": "control",
          "tone": "warning",
          "label": "prod, unconfirmed"
        },
        {
          "to": "run",
          "from": "prod",
          "kind": "data",
          "tone": "success",
          "label": "confirmed / non-prod"
        }
      ],
      "nodes": [
        {
          "id": "cmd",
          "kind": "client",
          "label": "Command run",
          "description": "maybe a flag forgotten"
        },
        {
          "id": "exec",
          "kind": "decision",
          "tone": "accent",
          "label": "execute = True?",
          "description": "explicit opt-in"
        },
        {
          "id": "dry",
          "kind": "process",
          "tone": "success",
          "label": "dry-run",
          "description": "preview, touch nothing"
        },
        {
          "id": "prod",
          "kind": "decision",
          "tone": "accent",
          "label": "env = production?",
          "description": "and confirmed?"
        },
        {
          "id": "blocked",
          "kind": "process",
          "tone": "warning",
          "label": "blocked",
          "description": "confirm production"
        },
        {
          "id": "run",
          "kind": "store",
          "tone": "success",
          "label": "execute",
          "description": "audited + scoped"
        }
      ],
      "title": "The safety gate — a forgotten flag lands on dry-run",
      "legend": [
        {
          "tone": "accent",
          "label": "explicit opt-in required"
        },
        {
          "tone": "success",
          "label": "safe outcome (dry-run / audited execute)"
        },
        {
          "tone": "warning",
          "label": "prod blocked until confirmed"
        }
      ],
      "subtitle": "Action requires explicit opt-in; production requires explicit confirmation. Every \"no\" routes to the safe outcome.",
      "storyboard": [
        {
          "say": "A command runs. Before anything touches data, the tool evaluates what the operator actually asked it to do.",
          "nodes": [
            "cmd",
            "exec"
          ],
          "edges": [
            [
              "cmd",
              "exec"
            ]
          ],
          "ms": 3600
        },
        {
          "say": "First gate: was execute explicitly set to true? Action requires opt-in, so the tool asks instead of assuming.",
          "nodes": [
            "exec"
          ],
          "edges": [],
          "ms": 3800
        },
        {
          "say": "If the flag is missing, the answer is no, and we fall to a dry-run that previews the exact target set instead of destroying it.",
          "nodes": [
            "exec",
            "dry"
          ],
          "edges": [
            [
              "exec",
              "dry"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "Only an explicit yes proceeds to the second gate: is this pointed at the production environment?",
          "nodes": [
            "exec",
            "prod"
          ],
          "edges": [
            [
              "exec",
              "prod"
            ]
          ],
          "ms": 4200
        },
        {
          "say": "Production without confirmation is blocked. There is no try-except for a deleted table, so unconfirmed prod stops here.",
          "nodes": [
            "prod",
            "blocked"
          ],
          "edges": [
            [
              "prod",
              "blocked"
            ]
          ],
          "ms": 4800
        },
        {
          "say": "Execution runs only when it's confirmed, or when it's a non-prod environment where the blast radius is contained.",
          "nodes": [
            "prod",
            "run"
          ],
          "edges": [
            [
              "prod",
              "run"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Every no routes to the safe outcome. The default is what runs when a flag is forgotten, so make the default preview, not destroy.",
          "nodes": [
            "exec",
            "dry",
            "prod",
            "blocked"
          ],
          "edges": [
            [
              "exec",
              "dry"
            ],
            [
              "prod",
              "blocked"
            ]
          ],
          "ms": 5200
        }
      ]
    },
    {
      "type": "lab",
      "check": "PASS: True",
      "title": "Make it safe by default",
      "starter": "def decide(args):\n    # args is a dict, e.g. {\"execute\": False, \"env\": \"staging\", \"confirmed\": False}\n    # SAFETY RULES:\n    #   - Default to \"dry-run\" unless execute is explicitly True.\n    #   - Production requires confirmed=True (no accidental prod) -> else block it.\n    #   - Otherwise -> \"execute\".\n    # Return one of: \"dry-run\", \"execute\", \"blocked: confirm production\"\n    ...  # your code here\n\n\n# --- test harness (do not edit) ---\ncases = [\n    ({\"execute\": False, \"env\": \"staging\"}, \"dry-run\"),\n    ({\"execute\": True, \"env\": \"staging\"}, \"execute\"),\n    ({\"execute\": True, \"env\": \"production\"}, \"blocked: confirm production\"),\n    ({\"execute\": True, \"env\": \"production\", \"confirmed\": True}, \"execute\"),\n]\nok = all(decide(a) == exp for a, exp in cases)\nfor a, exp in cases:\n    print(f\"{a} -> {decide(a)!r}\")\nprint(f\"PASS: {ok}\")\n",
      "summary": "Implement decide(args): default to \"dry-run\" unless execute is True; block a production action that isn't confirmed (\"blocked: confirm production\"); otherwise \"execute\". The harness checks all four paths — a forgotten execute must NOT run.",
      "language": "python"
    },
    {
      "code": "def decide(args):\n    if not args.get(\"execute\"):\n        return \"dry-run\"\n    if args.get(\"env\") == \"production\" and not args.get(\"confirmed\"):\n        return \"blocked: confirm production\"\n    return \"execute\"",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "The FIRST check: no explicit execute → preview only. A forgotten flag can never destroy data.",
          "label": "Default to dry-run",
          "lines": [
            2,
            3
          ]
        },
        {
          "note": "Even with execute=True, production without confirmed=True is blocked. No accidental prod, ever.",
          "label": "Guard production",
          "lines": [
            4,
            5
          ]
        },
        {
          "note": "Reached only when the caller explicitly opted in AND (non-prod or confirmed). The safe path is the default; the dangerous path is opt-in.",
          "label": "Execute only when earned",
          "lines": [
            6
          ]
        }
      ],
      "title": "Safe by default — the decision gate",
      "caption": "Common mistake: defaulting `execute=True` or `env=\"production\"`. The default is what runs when a flag is forgotten — make it the SAFE option.",
      "filename": "deploy.py",
      "language": "python",
      "subtitle": "Dry-run unless explicitly told to execute; production needs confirmation."
    },
    {
      "fix": "Both defaults are unsafe: `env='production'` means a forgotten flag targets prod, and `execute=True` means it acts with no opt-in. Default `env=None` (require an explicit choice) and `execute=False` (dry-run), and require confirmation before touching production.",
      "task": "Find the two dangerous defaults.",
      "type": "debug",
      "symptom": "This deploy tool deleted production records when it was run with no arguments.",
      "language": "python",
      "brokenCode": "def run(env=\"production\", execute=True):   # dangerous defaults\n    if execute:\n        delete_records(env)"
    },
    {
      "type": "tradeoff",
      "question": "A destructive command: dry-run first vs execute immediately",
      "optionA": {
        "label": "Dry-run first",
        "text": "Show exactly what WOULD change · Require an explicit second step to execute · Confirmation gate for production · Accidental data loss becomes very hard · Cost: one extra deliberate step Outcome: Cheap step; a deleted table is not"
      },
      "optionB": {
        "label": "Execute immediately",
        "text": "One command does it all · Fast for the happy path · No preview of the target set · A typo or forgotten flag → no undo · The cleanup-script outage from the mission Outcome: One mistake away from an outage"
      },
      "guidance": "Default to dry-run for anything destructive: preview the exact target set, then require an explicit execute (and confirmation for prod)."
    },
    {
      "left": {
        "tone": "success",
        "label": "Dry-run first",
        "lines": [
          "Show exactly what WOULD change",
          "Require an explicit second step to execute",
          "Confirmation gate for production",
          "Accidental data loss becomes very hard",
          "Cost: one extra deliberate step"
        ],
        "verdict": "Cheap step; a deleted table is not"
      },
      "type": "compare",
      "right": {
        "tone": "warning",
        "label": "Execute immediately",
        "lines": [
          "One command does it all",
          "Fast for the happy path",
          "No preview of the target set",
          "A typo or forgotten flag → no undo",
          "The cleanup-script outage from the mission"
        ],
        "verdict": "One mistake away from an outage"
      },
      "title": "A destructive command: dry-run first vs execute immediately",
      "caption": "Default to dry-run for anything destructive: preview the exact target set, then require an explicit execute (and confirmation for prod).",
      "subtitle": "One typo decides whether you preview or delete."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "The command defaults to dry-run — no destructive action without opt-in",
        "It never defaults the environment to production",
        "A production action requires explicit confirmation",
        "A dry-run shows the exact target set before executing",
        "The action is auditable (operator / action / target / time)"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Execute against production",
        "Dry-run / preview, taking no destructive action",
        "Delete everything matching a wildcard",
        "Pick the last-used environment"
      ],
      "question": "What should a destructive CLI command do by DEFAULT (when a flag is forgotten)?",
      "explanation": "The default is what runs when someone forgets a flag — make it the SAFE option: a dry-run that takes no destructive action and never defaults to production. Require explicit opt-in (and confirmation) to actually execute."
    },
    {
      "type": "teachback",
      "prompts": [
        "What does 'safe defaults' mean for a destructive tool?",
        "Why dry-run before executing?",
        "What is 'blast radius' and how do you limit it?",
        "Why should the environment never default to production?"
      ]
    },
    {
      "text": "Find a script or command in your own tooling that can destroy or overwrite data. Add a dry-run default, explicit environment selection, a preview of the target set, and confirmation for production — and log who ran what, where, and when. Safe tools protect your data from a bad command; the last lesson protects your work itself — a history you can read, revert, and recover, so almost nothing is ever truly lost.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Lab checkpoint passed — a forgotten execute dry-runs; unconfirmed prod is blocked",
        "Broken case understood (defaulting to prod / execute is dangerous)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your own tooling"
      ]
    }
  ],
  "git-fundamentals": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A commit checker that rejects empty/lazy commits, plus a branch + revert you can demonstrate.",
      "unlock": "All verification checks confirmed and the giant-\"wip\"-commit habit replaced with atomic commits.",
      "outcome": "Make small atomic commits with clear messages, use branches to isolate work, and recover safely when something goes wrong — never panic-force-push a shared branch.",
      "intensity": "standard",
      "doNotClaim": "Don't claim mastery until you can revert one change without undoing the others — which only atomic commits allow."
    },
    {
      "text": "Your teammate's branch is one giant commit titled 'stuff', and when a bug appeared, nobody could tell which change caused it or revert just that part. This is the safety net under every other skill: make your history small commits you can read, revert, and recover — so a mistake becomes a thirty-second undo instead of a panic.",
      "type": "mission"
    },
    {
      "text": "Git history is a tool, not a chore. Small atomic commits with clear messages let you bisect a bug, revert one change, and review work sanely. Branches isolate risk; almost nothing in git is truly lost.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before you read on: why is one giant commit titled 'wip' worse than five small commits with clear messages?",
      "reveal": "A giant 'wip' commit can't be reviewed, can't be reverted in part, and tells you nothing about what changed or why. Small atomic commits (one logical change each, with a clear message) let you review, revert just the bad one, and bisect to find which change introduced a bug."
    },
    {
      "type": "worked-example",
      "intro": "A commit gate, one rule at a time. The exact logic your lab implements: reject the empty and the lazy commit, accept the clear one.",
      "code": "LAZY = {\"\", \"wip\", \"fix\", \"stuff\", \"update\", \".\", \"asdf\"}\n\ndef check_commit(staged_files, message):\n    if not staged_files:\n        return \"nothing to commit\"\n    if message in LAZY or len(message) < 10:\n        return \"weak message\"\n    return \"ok\"",
      "language": "python",
      "steps": [
        "Name the lazy messages: A set of the throwaway messages that say nothing — \"wip\", \"fix\", \"stuff\". Set membership is the fast, readable check.",
        "Atomic: there must be a change: No staged files → nothing to commit. A commit with no content is not a commit.",
        "Reject the weak message: Lazy text OR under 10 chars fails — that is what makes history unreviewable later.",
        "Otherwise it is good: Staged change + a real message = \"ok\". This gate is exactly what a pre-commit hook would enforce on a team."
      ],
      "commonMistake": "A bug appeared and the team couldn't revert just the change that caused it."
    },
    {
      "text": "An ATOMIC commit is one logical change you could revert on its own. A good MESSAGE says what changed and why. Use a BRANCH per task. Recover with `git revert` (safe undo) and `git reflog` (find \"lost\" commits). The one rule: NEVER force-push a shared branch.",
      "type": "concept",
      "title": "Atomic commits · clear messages · branches · you can recover"
    },
    {
      "left": {
        "tone": "success",
        "label": "Atomic — one logical change",
        "lines": [
          "git add signup.py",
          "git commit -m \"validate signup:",
          "  reject empty email + bad age\"",
          "# stage deliberately, describe what + why",
          "# revert THIS change alone, bisect to it"
        ],
        "verdict": "Reviewable, revertable, bisectable"
      },
      "mono": true,
      "type": "compare",
      "right": {
        "tone": "warning",
        "label": "Blob — everything at once",
        "lines": [
          "git add .",
          "git commit -m \"wip\"",
          "# 12 unrelated changes in one commit",
          "# revert undoes ALL twelve",
          "# bisect can not narrow it down"
        ],
        "verdict": "Unreviewable, all-or-nothing"
      },
      "title": "Atomic commits vs the \"wip\" blob",
      "caption": "One logical change per commit, staged deliberately (not a blind `git add .`), with an imperative message: \"add\", \"fix\", \"validate\".",
      "subtitle": "Same code lands. Only one history you can review, revert, and bisect."
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "revert",
          "from": "atomic",
          "kind": "data",
          "tone": "success",
          "label": "enables"
        },
        {
          "to": "bisect",
          "from": "atomic",
          "kind": "data",
          "tone": "success",
          "label": "enables"
        },
        {
          "to": "reflog",
          "from": "atomic",
          "kind": "data",
          "tone": "success",
          "label": "recoverable"
        },
        {
          "to": "atomic",
          "from": "force",
          "kind": "control",
          "tone": "warning",
          "label": "NEVER on shared",
          "dashed": true
        }
      ],
      "nodes": [
        {
          "id": "atomic",
          "kind": "process",
          "tone": "accent",
          "label": "Atomic commits",
          "description": "one logical change each"
        },
        {
          "id": "revert",
          "kind": "process",
          "tone": "success",
          "label": "git revert",
          "description": "undo ONE change safely"
        },
        {
          "id": "bisect",
          "kind": "process",
          "tone": "success",
          "label": "git bisect",
          "description": "find the bad commit fast"
        },
        {
          "id": "reflog",
          "kind": "store",
          "tone": "success",
          "label": "git reflog",
          "description": "recover a \"lost\" commit"
        },
        {
          "id": "force",
          "kind": "external",
          "tone": "warning",
          "label": "force-push shared",
          "description": "rewrites others history"
        }
      ],
      "title": "Why atomic commits make recovery cheap",
      "legend": [
        {
          "tone": "accent",
          "label": "one revertable unit per commit"
        },
        {
          "tone": "success",
          "label": "recovery you actually get"
        },
        {
          "tone": "warning",
          "label": "the one move that destroys it"
        }
      ],
      "rankdir": "LR",
      "subtitle": "Each commit is one revertable unit. That is what lets revert, bisect, and reflog do their job — and a blob defeats all three.",
      "storyboard": [
        {
          "say": "It starts here: an atomic commit is one logical change, small enough that you could revert it on its own.",
          "nodes": [
            "atomic"
          ],
          "edges": [],
          "ms": 4000
        },
        {
          "say": "Because the change is isolated, git revert can cleanly undo just that one commit without touching everything else.",
          "nodes": [
            "atomic",
            "revert"
          ],
          "edges": [
            [
              "atomic",
              "revert"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "The same discipline lets git bisect binary-search your history to pinpoint exactly which commit introduced a bug.",
          "nodes": [
            "atomic",
            "bisect"
          ],
          "edges": [
            [
              "atomic",
              "bisect"
            ]
          ],
          "ms": 4600
        },
        {
          "say": "And git reflog remembers where every commit pointed, so even 'lost' work stays recoverable.",
          "nodes": [
            "atomic",
            "reflog"
          ],
          "edges": [
            [
              "atomic",
              "reflog"
            ]
          ],
          "ms": 4400
        },
        {
          "say": "Here's the risk: force-pushing a shared branch rewrites history others rely on, defeating revert, bisect, and reflog at once.",
          "nodes": [
            "force",
            "atomic",
            "revert",
            "bisect",
            "reflog"
          ],
          "edges": [
            [
              "force",
              "atomic"
            ]
          ],
          "ms": 5200
        },
        {
          "say": "The insight: keep commits small and never force-push shared branches, and any mistake becomes a thirty-second recovery.",
          "nodes": [
            "atomic",
            "revert",
            "bisect",
            "reflog",
            "force"
          ],
          "edges": [
            [
              "atomic",
              "revert"
            ],
            [
              "atomic",
              "bisect"
            ],
            [
              "atomic",
              "reflog"
            ]
          ],
          "ms": 5000
        }
      ]
    },
    {
      "type": "lab",
      "check": "PASS: True",
      "title": "Reject the lazy commit",
      "starter": "LAZY = {\"\", \"wip\", \"fix\", \"stuff\", \"update\", \".\", \"asdf\"}\n\ndef check_commit(staged_files, message):\n    # A good commit is ATOMIC (has staged changes) with a clear message.\n    #   - no staged files            -> \"nothing to commit\"\n    #   - empty/lazy or < 10 chars   -> \"weak message\"\n    #   - otherwise                  -> \"ok\"\n    ...  # your code here\n\n\n# --- test harness (do not edit) ---\ncases = [\n    ([], \"add user validation\", \"nothing to commit\"),\n    ([\"a.py\"], \"wip\", \"weak message\"),\n    ([\"a.py\"], \"fix\", \"weak message\"),\n    ([\"a.py\"], \"add input validation to signup\", \"ok\"),\n]\nok = all(check_commit(f, m) == exp for f, m, exp in cases)\nfor f, m, exp in cases:\n    print(f\"{m!r} -> {check_commit(f, m)!r}\")\nprint(f\"PASS: {ok}\")\n",
      "summary": "Implement check_commit(staged_files, message): return \"nothing to commit\" with no staged files, \"weak message\" for an empty/lazy message (in LAZY) or under 10 chars, else \"ok\". The harness checks all four cases — a \"wip\" commit must be rejected.",
      "language": "python"
    },
    {
      "code": "LAZY = {\"\", \"wip\", \"fix\", \"stuff\", \"update\", \".\", \"asdf\"}\n\ndef check_commit(staged_files, message):\n    if not staged_files:\n        return \"nothing to commit\"\n    if message in LAZY or len(message) < 10:\n        return \"weak message\"\n    return \"ok\"",
      "type": "code-walkthrough",
      "steps": [
        {
          "note": "A set of the throwaway messages that say nothing — \"wip\", \"fix\", \"stuff\". Set membership is the fast, readable check.",
          "label": "Name the lazy messages",
          "lines": [
            1
          ]
        },
        {
          "note": "No staged files → nothing to commit. A commit with no content is not a commit.",
          "label": "Atomic: there must be a change",
          "lines": [
            4,
            5
          ]
        },
        {
          "note": "Lazy text OR under 10 chars fails — that is what makes history unreviewable later.",
          "label": "Reject the weak message",
          "lines": [
            6,
            7
          ]
        },
        {
          "note": "Staged change + a real message = \"ok\". This gate is exactly what a pre-commit hook would enforce on a team.",
          "label": "Otherwise it is good",
          "lines": [
            8
          ]
        }
      ],
      "title": "A commit gate, one rule at a time",
      "caption": "The same shape as the boundary validators from Lesson 1 — name the bad cases, reject them, let the good one through.",
      "filename": "commit_check.py",
      "language": "python",
      "subtitle": "The exact logic your lab implements: reject the empty and the lazy commit, accept the clear one."
    },
    {
      "fix": "Everything is in one non-atomic commit, so `git revert` would undo all 12 changes and `git bisect` can't narrow it down. Commit one logical change at a time with a clear message — then you can revert or bisect precisely.",
      "task": "Why can't they isolate and revert the bad change?",
      "type": "debug",
      "symptom": "A bug appeared and the team couldn't revert just the change that caused it.",
      "language": "bash",
      "brokenCode": "git add .\ngit commit -m \"wip\"   # 12 unrelated changes blended into one commit"
    },
    {
      "type": "tradeoff",
      "question": "Atomic commits vs the \"wip\" blob",
      "optionA": {
        "label": "Atomic — one logical change",
        "text": "git add signup.py · git commit -m \"validate signup: ·   reject empty email + bad age\" · # stage deliberately, describe what + why · # revert THIS change alone, bisect to it Outcome: Reviewable, revertable, bisectable"
      },
      "optionB": {
        "label": "Blob — everything at once",
        "text": "git add . · git commit -m \"wip\" · # 12 unrelated changes in one commit · # revert undoes ALL twelve · # bisect can not narrow it down Outcome: Unreviewable, all-or-nothing"
      },
      "guidance": "One logical change per commit, staged deliberately (not a blind `git add .`), with an imperative message: \"add\", \"fix\", \"validate\"."
    },
    {
      "type": "verification",
      "intro": "Prove it — no vibes:",
      "items": [
        "Each commit is one logical change (atomic)",
        "Messages say what changed and why (not 'wip' / 'fix')",
        "Work happens on a branch, not directly on a shared main",
        "You can revert one commit and recover a lost one with reflog",
        "You never force-push a shared branch"
      ]
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "One big commit per feature",
        "Small atomic commits with clear messages",
        "Committing only at the end of the day",
        "Force-pushing to keep it clean"
      ],
      "question": "What makes git history actually useful?",
      "explanation": "Small atomic commits (one logical change, clear message) let you review, revert a single change, and bisect to find a bug. Giant 'wip' commits and force-pushing shared branches destroy that — and other people's work."
    },
    {
      "type": "teachback",
      "prompts": [
        "What is an 'atomic' commit and why does it matter?",
        "What makes a commit message good?",
        "How do you safely undo a commit that's already pushed?",
        "Why is force-pushing a shared branch dangerous?"
      ]
    },
    {
      "text": "On your next task: work on a branch and make each commit one logical change with a message that says what and why. Practice `git revert` on a throwaway commit and look at `git reflog`, so recovery is never scary. Then stop and look at the engineer you have become across this module. You distrust input and validate it at the boundary (L1). You make failures loud, safe, and diagnosable (L2). You write small, pure, testable functions (L3) and you know exactly what your values are (L4). You keep your logic flat and your loops correct (L5), read files without leaking or crashing (L6), and catch the bug before production does (L7). You build tools that protect data instead of destroying it (L8), and now you keep a history you can trust (L9). That is not nine tricks — it is one instinct, repeated: distrust the careless path, build the safe one, prove it. First Steps taught you to make a computer obey. Foundations taught you to make code that survives the real world. That is the difference between writing programs and engineering them — and you can do it now.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "7 days",
        "16 days"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Observable evidence — Lab checkpoint passed — a \"wip\"/empty commit is rejected, a clear one is \"ok\"",
        "Broken case understood (a giant commit can't be reverted or bisected)",
        "All verification checks confirmed",
        "Teach-back delivered with a concrete example",
        "Transfer task scheduled on your next real task"
      ]
    }
  ]
}
