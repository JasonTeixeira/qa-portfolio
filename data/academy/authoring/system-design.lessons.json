{
  "what-system-design-is": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A requirements extractor that separates functional from non-functional requirements and flags the one constraint that dictates the hardest tradeoff.",
      "unlock": "You can restate any \"design X\" prompt as {functional reqs, non-functional reqs, constraints} before drawing a single box.",
      "outcome": "Turn a vague product ask into an explicit design problem: functional requirements, non-functional requirements (scale, latency, availability), and the constraints that force tradeoffs.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you can design a system because you can name components — you can design it when you can state its requirements and the constraint that makes them conflict."
    },
    {
      "text": "A stakeholder says \"build me a URL shortener.\" You draw boxes for an hour and it falls apart in review because you never asked: how many redirects per second, how fresh must the click counts be, can a link ever 404 by mistake? System design is not box-drawing — it is converting an ambiguous ask into requirements and constraints that MAKE the tradeoffs for you. This lesson makes that conversion the first move, every time.",
      "type": "mission"
    },
    {
      "text": "Every real design interview and every real design doc starts the same way: gather requirements, then let the numbers and constraints drive the architecture. Functional requirements say WHAT the system does (shorten a URL, redirect, count clicks). Non-functional requirements say HOW WELL (10k redirects/sec, p99 < 50ms, 99.99% available, counts may lag 1 min). Constraints (budget, a single region, a legal data-residency rule) are what turn \"just add a cache\" into a real decision. Skip this step and you optimize the wrong thing.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: for \"design a URL shortener,\" write two functional requirements and two non-functional requirements. Which type usually decides the architecture?",
      "reveal": "Functional: create a short code for a long URL; redirect a short code to its long URL. Non-functional: handle N redirects/sec, redirect p99 latency < 50ms, 99.99% availability, click counts may be eventually consistent. The NON-FUNCTIONAL ones (scale, latency, availability, consistency freshness) decide the architecture — a shortener at 10 req/sec is a single box; at 100k req/sec it needs caching, sharding, and a CDN. Same functional spec, completely different system."
    },
    {
      "code": "// Raw ask: \"a link shortener that never loses a click count and is always fast\"\nconst spec = {\n  functional: [\"create short code\", \"redirect to long URL\", \"report click count\"],\n  nonFunctional: {\n    redirectsPerSec: 50000,     // scale\n    redirectP99Ms: 50,          // latency\n    availability: \"99.99%\",     // uptime\n    countFreshness: \"eventual\", // consistency requirement on the counter\n  },\n  constraints: [\"single cloud region\", \"counts must survive a crash\"],\n};\n// The hard tradeoff: \"always fast\" (cache redirects) vs \"never lose a count\"\n// (durable writes) — these pull in opposite directions, so they drive the design.\nfunction hardestTradeoff(s) {\n  if (s.nonFunctional.redirectP99Ms <= 100 && s.constraints.includes(\"counts must survive a crash\"))\n    return \"low-latency reads vs durable write accounting\";\n  return \"none obvious\";\n}",
      "type": "worked-example",
      "intro": "Split a raw ask into functional vs non-functional requirements, then name the constraint that forces the hardest tradeoff.",
      "steps": [
        "Functional requirements are verbs the user performs — list them first, plainly.",
        "Non-functional requirements are numbers: throughput, latency percentile, availability, freshness.",
        "Constraints are the boundaries you cannot cross (region, budget, durability, legal).",
        "The design is driven by where two requirements conflict under a constraint — name that tension explicitly."
      ],
      "language": "ts",
      "commonMistake": "Jumping to components (\"we'll use Redis and Postgres\") before writing the requirements. You end up defending tools instead of solving the stated problem, and you cannot justify a single choice because there is no number or constraint behind it."
    },
    {
      "text": "System design is the discipline of turning an ambiguous product ask into a defensible architecture, and it always begins with three lists. FUNCTIONAL requirements are what the system must DO — the user-visible verbs (shorten, redirect, count). NON-FUNCTIONAL requirements are how well it must do them, and they are always numeric: throughput (requests/sec), latency at a percentile (p99 < 50ms, not \"fast\"), availability (99.9% vs 99.99%), durability, and consistency freshness (strong vs eventual). CONSTRAINTS are the immovable boundaries: budget, one region, a data-residency law, a fixed team size. The reason you gather these first is that the architecture is DERIVED from them, not chosen from taste: a requirement of 50 req/sec and one of 500k req/sec produce completely different systems from the identical functional spec. And the single most useful move is to find where two requirements CONFLICT under a constraint — \"always fast redirects\" (favor a cache) versus \"never lose a click count\" (favor durable writes) — because that tension is exactly what the rest of the course teaches you to resolve. If a domain expert reads your design and cannot find the driving numbers and the central tension, you have drawn boxes, not designed a system.",
      "type": "concept",
      "title": "A design problem is requirements plus the constraint that makes them fight"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "func",
          "from": "ask",
          "kind": "data",
          "tone": "accent",
          "label": "extract verbs"
        },
        {
          "to": "nonfunc",
          "from": "ask",
          "kind": "data",
          "tone": "accent",
          "label": "extract numbers"
        },
        {
          "to": "constraints",
          "from": "nonfunc",
          "kind": "control",
          "tone": "warning",
          "label": "bound by"
        },
        {
          "to": "tension",
          "from": "func",
          "kind": "control",
          "tone": "warning",
          "label": "where reqs conflict"
        },
        {
          "to": "tension",
          "from": "constraints",
          "kind": "control",
          "tone": "warning",
          "label": "forces"
        },
        {
          "to": "arch",
          "from": "tension",
          "kind": "sync",
          "tone": "success",
          "label": "derives"
        }
      ],
      "nodes": [
        {
          "id": "ask",
          "kind": "client",
          "tone": "default",
          "label": "raw product ask",
          "description": "\"build a URL shortener\""
        },
        {
          "id": "func",
          "kind": "process",
          "tone": "accent",
          "label": "functional reqs",
          "description": "shorten / redirect / count"
        },
        {
          "id": "nonfunc",
          "kind": "process",
          "tone": "accent",
          "label": "non-functional reqs",
          "description": "RPS, p99, availability"
        },
        {
          "id": "constraints",
          "kind": "decision",
          "tone": "warning",
          "label": "constraints",
          "description": "region, durability, budget"
        },
        {
          "id": "tension",
          "kind": "decision",
          "tone": "warning",
          "label": "the driving tradeoff",
          "description": "fast reads vs durable counts"
        },
        {
          "id": "arch",
          "kind": "service",
          "tone": "success",
          "label": "derived architecture"
        }
      ],
      "title": "From ask to design problem",
      "rankdir": "LR",
      "subtitle": "requirements + constraints derive the architecture",
      "storyboard": [
        {
          "ms": 7450,
          "say": "Everything starts with a raw ask like 'build me a URL shortener' — ambiguous, and useless as a blueprint until you interrogate it.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/what-system-design-is__b0.mp3",
          "edges": [],
          "nodes": [
            "ask"
          ]
        },
        {
          "ms": 6725,
          "say": "First, extract the verbs: what must the system do? Shorten a URL, redirect, count clicks — those are the functional requirements.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/what-system-design-is__b1.mp3",
          "edges": [
            [
              "ask",
              "func"
            ]
          ],
          "nodes": [
            "ask",
            "func"
          ]
        },
        {
          "ms": 8525,
          "say": "Then extract the numbers: ten thousand redirects per second, p99 under fifty milliseconds, four nines — how well it must do it, always numeric.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/what-system-design-is__b2.mp3",
          "edges": [
            [
              "ask",
              "nonfunc"
            ]
          ],
          "nodes": [
            "ask",
            "nonfunc"
          ]
        },
        {
          "ms": 6975,
          "say": "Those numbers are bounded by immovable constraints — budget, a single region, data-residency law, the size of your team.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/what-system-design-is__b3.mp3",
          "edges": [
            [
              "nonfunc",
              "constraints"
            ]
          ],
          "nodes": [
            "nonfunc",
            "constraints"
          ]
        },
        {
          "ms": 6925,
          "say": "Where the functional needs collide with the constraints, you find the driving tradeoff — the one tension this design must resolve.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/what-system-design-is__b4.mp3",
          "edges": [
            [
              "func",
              "tension"
            ],
            [
              "constraints",
              "tension"
            ]
          ],
          "nodes": [
            "func",
            "constraints",
            "tension"
          ]
        },
        {
          "ms": 7675,
          "say": "The architecture is then derived from that tradeoff, not chosen from a catalog — which is why it survives review instead of falling apart.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/what-system-design-is__b5.mp3",
          "edges": [
            [
              "tension",
              "arch"
            ]
          ],
          "nodes": [
            "tension",
            "arch"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "functional: shorten,redirect\nnonFunctional: p99,uptime,counts\ntradeoff: low-latency reads vs durable writes",
      "title": "Extract requirements and flag the driving tradeoff",
      "starter": "// Classify each requirement and detect the driving tradeoff.\n// A requirement is non-functional if it names a number/quality:\n//   rps, latency, availability, durability, consistency.\n// Tension rule: if there is a latency requirement <= 100ms AND a durability\n// requirement, flag \"low-latency reads vs durable writes\".\nconst spec = {\n  requirements: [\n    { id: \"shorten\", kind: \"does: create short code\" },\n    { id: \"redirect\", kind: \"does: resolve short code\" },\n    { id: \"p99\", quality: \"latency\", value: 50 },\n    { id: \"uptime\", quality: \"availability\", value: \"99.99%\" },\n    { id: \"counts\", quality: \"durability\", value: \"survive crash\" },\n  ],\n};\nfunction analyze(s) { /* return { functional:[ids], nonFunctional:[ids], tradeoff } */ }\nconst r = analyze(spec);\nconsole.log(\"functional:\", r.functional.join(\",\"));\nconsole.log(\"nonFunctional:\", r.nonFunctional.join(\",\"));\nconsole.log(\"tradeoff:\", r.tradeoff);",
      "summary": "Given a raw spec, sort each requirement into functional vs non-functional, then detect the classic tension: a low-latency read requirement together with a durability constraint. Print the split and the flagged tradeoff.",
      "language": "js"
    },
    {
      "fix": "// Start from requirements, then derive components:\nconst requirements = {\n  functional: [\"redirect\", \"count clicks\"],\n  nonFunctional: { rps: 50000, p99Ms: 50, availability: \"99.99%\" },\n  constraints: [\"counts must survive a crash\"],\n};\n// NOW a cache is justified by (rps + p99), durable store by the crash constraint.",
      "task": "The doc names components with no requirements behind them, so no choice can be justified. Fix the starting point.",
      "type": "debug",
      "symptom": "A design review stalls: the team argues about Redis vs Memcached for 40 minutes and never resolves anything.",
      "language": "ts",
      "brokenCode": "// design doc, first line:\nconst design = { cache: \"Redis\", db: \"Postgres\", queue: \"Kafka\" };"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "List functional + non-functional reqs and constraints before any box. Slower to start, but every later choice is justified by a number or a constraint you wrote down.",
        "label": "Extract requirements first"
      },
      "optionB": {
        "text": "Sketch components immediately from experience. Feels productive, but you optimize the wrong axis and cannot defend choices when scale or freshness numbers finally surface.",
        "label": "Draw the architecture first"
      },
      "guidance": "Extract requirements first (A). The architecture is derived from the numbers; without them you are guessing. The 10 minutes of requirements gathering is the highest-leverage part of the whole design and is exactly what a senior reviewer checks for.",
      "question": "A stakeholder gives a one-line ask. Start drawing the architecture, or spend the first 10 minutes extracting requirements?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Nothing of substance — a fixed functional spec should always yield essentially the same architecture, whatever the team.",
        "The non-functional requirements — the throughput, latency, availability, and freshness targets each team designs for.",
        "The datastore each team defaulted to; the database you pick is what fixes the shape of everything else in the design.",
        "The functional requirements — one team must have quietly added a feature, and extra features are what reshape the layout."
      ],
      "question": "Two teams get the identical functional spec for a URL shortener but produce very different architectures. What most likely differs?",
      "explanation": "Functional requirements say what the system does; they were identical here. The architecture is driven by the NON-functional numbers: a shortener at 10 req/sec is one box, one at 100k req/sec needs caching, sharding, and a CDN. Same \"what,\" different \"how well,\" different system."
    },
    {
      "type": "verification",
      "intro": "You have framed a design problem when:",
      "items": [
        "You have separate lists of functional and non-functional requirements.",
        "Every non-functional requirement is a number or a named quality (not \"fast\" / \"reliable\").",
        "You have named the single tradeoff the constraints force.",
        "Every component you later add traces back to a requirement or constraint."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why the same functional spec can produce two different architectures.",
        "Explain the difference between a functional requirement, a non-functional requirement, and a constraint."
      ]
    },
    {
      "text": "Every remaining lesson in this course resolves a tension between two requirements — the very tension you learn to name here. The capstone is graded on whether its computed checks satisfy the requirements you first extract.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Requirements split into functional vs non-functional",
        "Every non-functional req is a number/quality",
        "The driving tradeoff is named explicitly"
      ]
    }
  ],
  "back-of-envelope": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A capacity estimator that computes average and peak RPS, yearly storage, and peak bandwidth from a handful of product numbers.",
      "unlock": "You can size a system in under two minutes and defend each number, including the peak factor.",
      "outcome": "Estimate a system's load and footprint from first principles: DAU × actions → average RPS → peak RPS, and bytes/record → storage/year and bandwidth.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a system \"scales\" without a number — scale is the estimate that says single-box or fleet, and it is arithmetic, not opinion."
    },
    {
      "text": "In a design review someone asks \"how many servers?\" and the room goes quiet. Estimation is the skill that answers it: from daily active users and a couple of per-user actions you derive requests per second, then storage per year, then bandwidth — the numbers that decide whether one box suffices or you need a sharded fleet. This lesson turns \"it depends\" into a defended calculation.",
      "type": "mission"
    },
    {
      "text": "Capacity estimation is the sizing step of every design. The chain is mechanical: DAU (daily active users) × actions/user/day = requests/day; divide by 86,400 seconds to get AVERAGE RPS; multiply by a PEAK FACTOR (traffic is not uniform — a common rule of thumb is that the busy period carries several times the average, so designers often use 2–10×) to size for the spike. For storage: records/day × bytes/record × retention → bytes. For bandwidth: RPS × bytes/response. These estimates are order-of-magnitude, not exact, and that is the point — they tell you which architecture class you are in.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: 100 million DAU each doing 10 writes/day. What is the average writes-per-second, and why is that not the number you provision for?",
      "reveal": "100e6 × 10 = 1e9 writes/day. 1e9 / 86,400 ≈ 11,574 average writes/sec. You do NOT provision for the average because traffic is bursty — the daily peak can be several times the mean. Apply a peak factor (say 3–5×) → provision for ~35k–58k writes/sec. Sizing to the average guarantees you fall over at peak."
    },
    {
      "code": "const SECONDS_PER_DAY = 86400;\nfunction estimate({ dau, actionsPerUserPerDay, bytesPerRecord, peakFactor, retentionYears, bytesPerResponse }) {\n  const perDay = dau * actionsPerUserPerDay;\n  const avgRps = perDay / SECONDS_PER_DAY;\n  const peakRps = avgRps * peakFactor;            // provision for the spike, not the mean\n  const storagePerYear = perDay * bytesPerRecord * 365;\n  const totalStorage = storagePerYear * retentionYears;\n  const peakBandwidth = peakRps * bytesPerResponse; // bytes/sec at peak\n  return { avgRps, peakRps, storagePerYear, totalStorage, peakBandwidth };\n}\n// 1M DAU, 20 actions/day, 200 B/record, 3x peak, 2y retention, 1KB/response",
      "type": "worked-example",
      "intro": "The estimation chain from product numbers to RPS, storage, and bandwidth.",
      "steps": [
        "DAU × actions/user/day = total actions/day.",
        "actions/day ÷ 86,400 = average RPS; this is the baseline, not the target.",
        "average RPS × peak factor = peak RPS — always size for the peak.",
        "records/day × bytes/record × 365 × retention = storage; peak RPS × bytes/response = bandwidth."
      ],
      "language": "ts",
      "commonMistake": "Provisioning for average RPS and forgetting the peak factor. Real traffic has daily and event-driven spikes several times the mean, so an average-sized system is saturated exactly when it matters most."
    },
    {
      "text": "Back-of-the-envelope estimation is deliberately crude arithmetic whose job is to place you in the right architecture class — single box, one beefy DB, or a sharded fleet with caches and a CDN. The load chain: DAU × actions/user/day gives actions/day; divide by 86,400 (seconds in a day) for AVERAGE requests/sec. Because real traffic is bursty (a daily peak, a launch spike), you multiply by a PEAK FACTOR — a rule-of-thumb multiplier, commonly in the 2–10× range depending on the product — and provision for that peak RPS, never the mean. The storage chain: records/day × bytes/record × 365 × retention-years gives total bytes; keep a factor for indexes and replication. The bandwidth chain: peak RPS × bytes/response gives peak egress. Two disciplines make these useful: round aggressively (1 day ≈ 86,400 s ≈ 1e5; 1 year ≈ 3e7 s) so you can do it in your head, and treat every output as order-of-magnitude — the goal is to know whether you are at 10 RPS or 100,000 RPS, because that difference, not the third significant figure, is what changes the design.",
      "type": "concept",
      "title": "Sizing is a mechanical chain from product numbers to capacity"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "perday",
          "from": "dau",
          "kind": "data",
          "tone": "accent",
          "label": "multiply"
        },
        {
          "to": "avg",
          "from": "perday",
          "kind": "data",
          "tone": "accent",
          "label": "÷ seconds/day"
        },
        {
          "to": "peak",
          "from": "avg",
          "kind": "control",
          "tone": "warning",
          "label": "× peak factor"
        },
        {
          "to": "storage",
          "from": "perday",
          "kind": "data",
          "tone": "success",
          "label": "× bytes × retention"
        },
        {
          "to": "bw",
          "from": "peak",
          "kind": "data",
          "tone": "success",
          "label": "× bytes/response"
        }
      ],
      "nodes": [
        {
          "id": "dau",
          "kind": "client",
          "tone": "accent",
          "label": "DAU × actions/day"
        },
        {
          "id": "perday",
          "kind": "process",
          "tone": "default",
          "label": "actions / day"
        },
        {
          "id": "avg",
          "kind": "process",
          "tone": "default",
          "label": "average RPS",
          "description": "÷ 86,400"
        },
        {
          "id": "peak",
          "kind": "decision",
          "tone": "warning",
          "label": "peak RPS",
          "description": "× peak factor"
        },
        {
          "id": "storage",
          "kind": "store",
          "tone": "success",
          "label": "storage / year",
          "description": "bytes/record × 365"
        },
        {
          "id": "bw",
          "kind": "external",
          "tone": "success",
          "label": "peak bandwidth",
          "description": "RPS × bytes/response"
        }
      ],
      "title": "The estimation chain",
      "rankdir": "LR",
      "subtitle": "product numbers derive RPS, storage, bandwidth",
      "storyboard": [
        {
          "ms": 7725,
          "say": "Someone asks how many servers you need — the answer starts with two product numbers: daily active users and actions per user per day.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/back-of-envelope__b0.mp3",
          "edges": [],
          "nodes": [
            "dau"
          ]
        },
        {
          "ms": 7425,
          "say": "Multiply DAU by actions per user and you have total actions per day — the root number every other estimate derives from.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/back-of-envelope__b1.mp3",
          "edges": [
            [
              "dau",
              "perday"
            ]
          ],
          "nodes": [
            "dau",
            "perday"
          ]
        },
        {
          "ms": 7700,
          "say": "Divide actions per day by 86,400 seconds and you get average requests per second — the mean load on the system.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/back-of-envelope__b2.mp3",
          "edges": [
            [
              "perday",
              "avg"
            ]
          ],
          "nodes": [
            "perday",
            "avg"
          ]
        },
        {
          "ms": 7050,
          "say": "But traffic is bursty, so multiply the average by a peak factor of two to ten — you provision for the spike, never the mean.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/back-of-envelope__b3.mp3",
          "edges": [
            [
              "avg",
              "peak"
            ]
          ],
          "nodes": [
            "avg",
            "peak"
          ]
        },
        {
          "ms": 7300,
          "say": "The same actions-per-day number branches to storage: multiply by bytes per record and retention to get storage per year.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/back-of-envelope__b4.mp3",
          "edges": [
            [
              "perday",
              "storage"
            ]
          ],
          "nodes": [
            "perday",
            "storage"
          ]
        },
        {
          "ms": 6725,
          "say": "Finally, peak RPS times bytes per response gives peak bandwidth — the pipe you must be able to fill at the worst moment.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/back-of-envelope__b5.mp3",
          "edges": [
            [
              "peak",
              "bw"
            ]
          ],
          "nodes": [
            "peak",
            "bw"
          ]
        },
        {
          "ms": 7475,
          "say": "The chain is crude on purpose: two product numbers place you in an architecture class — one box, one beefy database, or a sharded fleet.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/back-of-envelope__b6.mp3",
          "edges": [],
          "nodes": [
            "dau",
            "peak",
            "storage",
            "bw"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "peakRps: 2083.3333333333335\nservers: 2\nmonthlyEgressGB: 972\nmonthlyEgressCostUSD: 77.76",
      "title": "Build the capacity estimator",
      "starter": "const SECONDS_PER_DAY = 86400;\nconst SECONDS_PER_MONTH = 2592000;\n// Turn a raw estimate into a provisioning + cost decision.\n// Return { peakRps, servers, monthlyEgressGB, monthlyEgressCostUSD }.\nfunction planCapacity(i) { /* your code */ }\nconst input = { dau: 3_000_000, actionsPerUserPerDay: 12, bytesPerResponse: 900, peakFactor: 5, rpsPerServer: 1500, costPerGB: 0.08 };\nconst r = planCapacity(input);\nconsole.log(\"peakRps:\", r.peakRps);\nconsole.log(\"servers:\", r.servers);\nconsole.log(\"monthlyEgressGB:\", r.monthlyEgressGB);\nconsole.log(\"monthlyEgressCostUSD:\", r.monthlyEgressCostUSD);",
      "summary": "You can already derive peak RPS and bandwidth. Now turn the raw estimate into a provisioning decision: peak RPS, servers needed at a per-server RPS ceiling, monthly egress in GB, and the monthly egress bill. Print all four.",
      "language": "js"
    },
    {
      "fix": "const avgRps = (dau * actions) / 86400;\nconst peakRps = avgRps * PEAK_FACTOR;   // e.g. 4x for a lunchtime spike\nconst serversNeeded = Math.ceil(peakRps / perServerRps);  // size to the peak",
      "task": "Capacity is sized to average RPS, so it saturates at the daily peak. Fix the sizing to account for the peak.",
      "type": "debug",
      "symptom": "A service is sized to handle 12k RPS and runs fine in tests but collapses every day at lunchtime.",
      "language": "ts",
      "brokenCode": "const avgRps = (dau * actions) / 86400;\nconst serversNeeded = Math.ceil(avgRps / perServerRps);  // sized to the mean"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Use 1e5 s/day, round DAU to a power of ten, and aim for the right order of magnitude. Fast, doable in your head, and enough to pick the architecture class.",
        "label": "Rounded order-of-magnitude"
      },
      "optionB": {
        "text": "Pull precise DAU and byte sizes from dashboards first. More accurate, but slow, and the third significant figure never changes whether you need sharding.",
        "label": "Exact analytics"
      },
      "guidance": "Use rounded figures (A) for the design decision — the estimate exists to tell you 10 RPS vs 100k RPS, and rounding does not change that. Reach for exact analytics (B) later, for capacity provisioning and cost, once the architecture class is settled.",
      "question": "For a first estimate, do you gather exact analytics numbers or use rounded order-of-magnitude figures?"
    },
    {
      "type": "quiz",
      "answer": 2,
      "options": [
        "Whatever the exact server model supports; provisioning is a hardware-spec question, not something the RPS estimate decides.",
        "Below 10,000 RPS to save cost, since not every user is active at once so real concurrent demand sits under the mean.",
        "Several times 10,000 RPS — apply a peak factor, because bursty traffic makes the daily peak a multiple of the average.",
        "Exactly 10,000 RPS — the average is the honest number, and sizing to it keeps cost matched to typical load."
      ],
      "question": "You compute an average of 10,000 RPS. Your product has a sharp lunchtime spike. What do you provision for?",
      "explanation": "Average RPS is a baseline, not a provisioning target. Bursty traffic means the daily peak is a multiple of the mean, so you multiply by a peak factor (commonly 2–10×) and size for that peak. Provisioning to the average guarantees saturation during the spike."
    },
    {
      "type": "verification",
      "intro": "Your estimate is defensible when:",
      "items": [
        "You derived average RPS from DAU and actions, not guessed it.",
        "You applied an explicit peak factor and provisioned for peak RPS.",
        "Storage accounts for bytes/record and retention.",
        "Every number is order-of-magnitude and you can reproduce it in your head."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Walk through the chain from DAU to peak RPS out loud.",
        "Explain why you provision for peak RPS rather than average RPS."
      ]
    },
    {
      "text": "These RPS and storage numbers are the inputs to the caching, sharding, and load-balancing lessons — how many shards, how big a cache, how many LB backends all start from this estimate. The capstone validates that your chosen stores fit the estimated footprint.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Average RPS derived from DAU × actions ÷ seconds/day",
        "Peak RPS computed with an explicit peak factor",
        "Storage and bandwidth estimated from bytes and retention"
      ]
    }
  ],
  "building-blocks": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A request-path classifier that assigns each component its correct role and rejects a path that puts a component in the wrong place.",
      "unlock": "You can trace a read and a write request through the standard blocks and say why each hop exists.",
      "outcome": "Name the core components of a web system (client, load balancer, app server, cache, database, queue, object store) and trace a request through the path, knowing what each block is for.",
      "intensity": "standard",
      "doNotClaim": "Don't claim to know an architecture because you can list its boxes — you know it when you can trace a request through them and justify each hop."
    },
    {
      "text": "A new engineer sees \"client → LB → app → cache → DB → queue\" and can name each box but cannot say why a read hits the cache before the DB, or why the queue is off the critical path. The blocks only mean something as a request PATH. This lesson wires the boxes into the journey a request actually takes and the job each block does on the way.",
      "type": "mission"
    },
    {
      "text": "Nearly every web backend is assembled from the same handful of building blocks, each with one job. The CLIENT initiates. The LOAD BALANCER spreads requests across identical app servers and hides failures. The APP SERVER holds business logic and is kept stateless so any instance can serve any request. The CACHE answers hot reads fast to spare the database. The DATABASE is the durable source of truth. The QUEUE decouples slow or bursty work so it happens asynchronously off the request path. The OBJECT STORE holds large blobs (images, files). Knowing the blocks is table stakes; knowing the PATH — and why each hop exists — is the actual skill.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: in a read request, why does the app server check the cache before the database? And why is sending a welcome email put on a queue instead of done inline?",
      "reveal": "The cache is checked first because it serves hot reads in microseconds and shields the database from the majority of traffic — a cache hit means the DB never sees the request. The welcome email goes on a queue because it is slow and non-critical: doing it inline would block the user's response on an external mail service. The queue lets the request return immediately while a worker sends the email asynchronously — decoupling latency-critical from latency-tolerant work."
    },
    {
      "code": "// A read request path and each block's role.\nconst readPath = [\n  { block: \"client\",        role: \"initiates the request\" },\n  { block: \"load-balancer\", role: \"routes to a healthy app server\" },\n  { block: \"app-server\",    role: \"stateless business logic\" },\n  { block: \"cache\",         role: \"serves hot reads, shields the DB\" },   // checked first\n  { block: \"database\",      role: \"durable source of truth\" },            // on cache miss\n];\n// Off the critical path: slow, non-urgent work is queued.\nconst async = [\n  { block: \"queue\",  role: \"decouples slow work (email, thumbnails)\" },\n  { block: \"worker\", role: \"consumes the queue asynchronously\" },\n];",
      "type": "worked-example",
      "intro": "The standard read path and where each block sits, plus what belongs off the path.",
      "steps": [
        "The load balancer sits in front so any app server can serve any request (they are stateless).",
        "The cache is read before the database; a hit spares the DB entirely.",
        "The database is the durable source of truth, hit on a cache miss.",
        "Slow, non-critical work (email, image processing) goes on a queue, off the request path."
      ],
      "language": "ts",
      "commonMistake": "Putting state in the app server (a user session in local memory). It breaks the moment the load balancer sends the next request to a different instance — app servers must be stateless so any of them can handle any request."
    },
    {
      "text": "A web system is a small set of components arranged so a request flows through them, each doing exactly one job. The CLIENT sends the request. The LOAD BALANCER receives it and forwards to one of several identical, STATELESS app servers — statelessness is what lets it route freely and survive an instance dying. The APP SERVER runs business logic and, for a read, consults the CACHE first: a cache hit returns in microseconds and the request never touches the database, which is the whole point — the cache absorbs the hot majority of traffic. On a cache miss it reads the DATABASE, the durable source of truth, and typically populates the cache on the way back. Writes go to the database (and invalidate or update the cache). Work that is slow or non-urgent — sending email, generating thumbnails, updating analytics — is pushed onto a QUEUE and handled by a WORKER asynchronously, so it never adds latency to the user's request. Large binary blobs live in an OBJECT STORE, not the relational database. The skill is not naming these boxes; it is tracing a specific request through them and justifying every hop — why the cache is before the DB, why the app server holds no state, why the queue is off the critical path.",
      "type": "concept",
      "title": "The blocks are a request path, and each hop has one job"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "lb",
          "from": "client",
          "kind": "sync",
          "tone": "accent",
          "label": "request"
        },
        {
          "to": "app",
          "from": "lb",
          "kind": "sync",
          "tone": "accent",
          "label": "route"
        },
        {
          "to": "cache",
          "from": "app",
          "kind": "data",
          "tone": "success",
          "label": "read (first)"
        },
        {
          "to": "db",
          "from": "cache",
          "kind": "data",
          "label": "miss → read"
        },
        {
          "to": "queue",
          "from": "app",
          "kind": "async",
          "tone": "warning",
          "label": "enqueue slow work"
        },
        {
          "to": "worker",
          "from": "queue",
          "kind": "async",
          "tone": "warning",
          "label": "consume"
        }
      ],
      "nodes": [
        {
          "id": "client",
          "kind": "client",
          "tone": "default",
          "label": "client"
        },
        {
          "id": "lb",
          "kind": "service",
          "tone": "accent",
          "label": "load balancer",
          "description": "routes to healthy app server"
        },
        {
          "id": "app",
          "kind": "service",
          "tone": "accent",
          "label": "app server (stateless)"
        },
        {
          "id": "cache",
          "kind": "store",
          "tone": "success",
          "label": "cache",
          "description": "hot reads, shields DB"
        },
        {
          "id": "db",
          "kind": "store",
          "tone": "success",
          "label": "database",
          "description": "source of truth"
        },
        {
          "id": "queue",
          "kind": "queue",
          "tone": "warning",
          "label": "queue",
          "description": "slow work off-path"
        },
        {
          "id": "worker",
          "kind": "service",
          "tone": "default",
          "label": "worker"
        }
      ],
      "title": "The standard request path",
      "rankdir": "LR",
      "subtitle": "each block one job; slow work is queued off-path",
      "storyboard": [
        {
          "ms": 6125,
          "say": "Every request starts at the client, which sends it to the load balancer — the single front door for the whole system.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/building-blocks__b0.mp3",
          "edges": [
            [
              "client",
              "lb"
            ]
          ],
          "nodes": [
            "client",
            "lb"
          ]
        },
        {
          "ms": 8175,
          "say": "The load balancer routes to any app server it likes — the servers are stateless, so every instance is interchangeable and one dying costs nothing.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/building-blocks__b1.mp3",
          "edges": [
            [
              "lb",
              "app"
            ]
          ],
          "nodes": [
            "lb",
            "app"
          ]
        },
        {
          "ms": 7725,
          "say": "For a read, the app server asks the cache first: a hit returns in microseconds and the request never touches the database at all.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/building-blocks__b2.mp3",
          "edges": [
            [
              "app",
              "cache"
            ]
          ],
          "nodes": [
            "app",
            "cache"
          ]
        },
        {
          "ms": 7200,
          "say": "Only on a cache miss do we fall through to the database — the durable source of truth — and the answer refills the cache on the way back.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/building-blocks__b3.mp3",
          "edges": [
            [
              "cache",
              "db"
            ]
          ],
          "nodes": [
            "cache",
            "db"
          ]
        },
        {
          "ms": 7675,
          "say": "Anything slow — emails, thumbnails, reports — is not done here; the app just drops a job on the queue and responds to the user immediately.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/building-blocks__b4.mp3",
          "edges": [
            [
              "app",
              "queue"
            ]
          ],
          "nodes": [
            "app",
            "queue"
          ]
        },
        {
          "ms": 7375,
          "say": "A worker consumes that job off the critical path, at its own pace — if it is slow or fails, the user's request never feels it.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/building-blocks__b5.mp3",
          "edges": [
            [
              "queue",
              "worker"
            ]
          ],
          "nodes": [
            "queue",
            "worker"
          ]
        },
        {
          "ms": 8075,
          "say": "That is the whole design: one job per box, cache before database, and slow work queued aside so the request path stays fast and hard to kill.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/building-blocks__b6.mp3",
          "edges": [
            [
              "client",
              "lb"
            ],
            [
              "lb",
              "app"
            ],
            [
              "app",
              "cache"
            ],
            [
              "cache",
              "db"
            ],
            [
              "app",
              "queue"
            ],
            [
              "queue",
              "worker"
            ]
          ],
          "nodes": [
            "client",
            "lb",
            "app",
            "cache",
            "db",
            "queue",
            "worker"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "client: initiates request\nload-balancer: routes to healthy app server\napp-server: stateless business logic\ncache: serves hot reads, shields DB\ndatabase: durable source of truth\nvalid: true problems: none",
      "title": "Classify components on the request path",
      "starter": "// Map each block to its role; validate the read path.\n// Rules for a VALID read path:\n//   - \"cache\" index < \"database\" index (cache is consulted first)\n//   - \"app-server\" must be marked stateless\nconst path = [\"client\", \"load-balancer\", \"app-server\", \"cache\", \"database\"];\nconst stateless = { \"app-server\": true };\nconst ROLES = {\n  \"client\": \"initiates request\",\n  \"load-balancer\": \"routes to healthy app server\",\n  \"app-server\": \"stateless business logic\",\n  \"cache\": \"serves hot reads, shields DB\",\n  \"database\": \"durable source of truth\",\n};\nfunction analyze(path, stateless) { /* return { roles:[...], valid, problems:[...] } */ }\nconst r = analyze(path, stateless);\nfor (const line of r.roles) console.log(line);\nconsole.log(\"valid:\", r.valid, \"problems:\", r.problems.join(\"; \") || \"none\");",
      "summary": "Assign each component its canonical role and validate the read path: cache must be positioned before database, and the app server must be stateless. Print each role and whether the path is valid.",
      "language": "js"
    },
    {
      "fix": "// Move session state OUT of the app server into a shared store.\n// App servers stay stateless; any instance can serve any request.\nawait sessionStore.set(req.id, req.user);  // shared cache/DB, not local memory",
      "task": "Session state lives in one app server's memory, so a load-balanced request to another instance loses it. Fix the statefulness.",
      "type": "debug",
      "symptom": "Users are randomly logged out and shopping carts vanish between page loads, but only after the service scaled from one app server to several.",
      "language": "ts",
      "brokenCode": "// app server stores session in local process memory\napp.use((req) => { sessions[req.id] = req.user; });"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Enqueue the job and return immediately; a worker processes it async. The user gets a fast response and a spike in uploads becomes a growing queue, not timeouts.",
        "label": "Queue it for a worker"
      },
      "optionB": {
        "text": "Do the image work during the request. Simpler and the result is ready immediately, but the user waits seconds and a burst of uploads saturates your app servers.",
        "label": "Process inline"
      },
      "guidance": "Queue it (A) when the work is slow and the user does not need the result synchronously — decoupling protects request latency and absorbs bursts. Keep it inline (B) only when the result is required to answer the request and is genuinely fast. Match placement to whether the work is on the critical path.",
      "question": "A user action triggers slow image processing. Do it inline in the request, or push it to a queue for a worker?"
    },
    {
      "type": "quiz",
      "answer": 3,
      "options": [
        "Caches guarantee stronger consistency than the database, so reading them first always returns the most up-to-date value.",
        "The cache is the durable source of truth, so reading it first protects the data the database might lose on a crash.",
        "The database cannot serve reads directly; it only accepts writes, so reads must be routed through the cache in front of it.",
        "A cache hit answers in microseconds and means the database never serves that request, absorbing the hot majority of reads."
      ],
      "question": "Why is the cache consulted before the database on a read?",
      "explanation": "The cache holds hot data and answers in microseconds; a hit means the database never sees that request, so the cache absorbs the majority of read traffic. The database is the durable source of truth, hit only on a miss — the cache is faster and less durable, the opposite of option A."
    },
    {
      "type": "verification",
      "intro": "You understand the request path when:",
      "items": [
        "You can trace a read from client to LB to app to cache to DB and back.",
        "Your app servers hold no request-specific state.",
        "Slow, non-urgent work is on a queue, off the critical path.",
        "You can justify why each hop exists, not just name it."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Trace a write request through the blocks and say what each does.",
        "Explain why app servers must be stateless behind a load balancer."
      ]
    },
    {
      "text": "This path is the skeleton the whole course fleshes out: the load-balancing lesson deepens the LB, the caching module deepens the cache, the queues module deepens the queue. The capstone composes these exact blocks into one validated system.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Each block mapped to its single role",
        "Read path traced cache-before-DB",
        "App servers kept stateless"
      ]
    }
  ],
  "latency-throughput-availability": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "An availability calculator that composes component availabilities in series (product) and in parallel (redundancy) to a system number.",
      "unlock": "You can explain why tail latency matters, and compute a system's availability from its components' 9s.",
      "outcome": "Reason with the three core system metrics: latency (and why you report p50/p95/p99, not the mean), throughput, and availability — including how availability composes across serial and redundant components.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a system is \"fast\" from an average latency — the tail (p99) is what users feel, and availability is a computed product, not a wish."
    },
    {
      "text": "A dashboard shows 20ms average latency and the team calls the service fast — yet support tickets pile up about slow pages. The average hid a brutal p99 of 2 seconds affecting one request in a hundred, and at scale that is a lot of angry users. Meanwhile leadership wants \"four nines\" but the design chains five services in series, each at three nines, which cannot possibly reach it. This lesson makes latency percentiles and availability composition into numbers you compute, not adjectives you assert.",
      "type": "mission"
    },
    {
      "text": "Three metrics describe how well a system performs. LATENCY is how long one request takes; because a few slow requests dominate user pain, you report PERCENTILES — p50 (median), p95, p99 — never just the mean, which a handful of huge outliers can distort. THROUGHPUT is how many requests/sec the system sustains. AVAILABILITY is the fraction of time it is up, quoted in \"nines\": 99.9% (three nines) is ~8.8 hours down/year, 99.99% (four nines) ~52 minutes/year — each extra nine cuts downtime 10×. Crucially, availability COMPOSES: services in SERIES multiply (all must be up, so the system is worse than its weakest link), while REDUNDANT copies in PARALLEL multiply their unavailabilities (only one must be up, so redundancy improves it).",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: a request passes through 4 independent services in series, each 99.9% available. Is the end-to-end availability higher or lower than 99.9%, and roughly what is it?",
      "reveal": "Lower. In series, ALL four must be up, so you MULTIPLY: 0.999^4 ≈ 0.996, i.e. about 99.6% — worse than any single component. Chaining dependencies erodes availability; every hop you add in series drags the total down. To get it back up you either remove hops or add redundancy (parallel copies) at the weak points."
    },
    {
      "code": "// Series: all must be up → multiply the availabilities.\nfunction series(components) {\n  return components.reduce((a, c) => a * c, 1);\n}\n// Parallel (redundant): at least one must be up → multiply the UNAVAILABILITIES.\nfunction parallel(replicas) {\n  const bothDown = replicas.reduce((u, c) => u * (1 - c), 1);\n  return 1 - bothDown;\n}\n// A path of two services in series, the second one duplicated for redundancy:\nconst svcA = 0.999;\nconst svcB = parallel([0.99, 0.99]);   // two 99% replicas → ~99.99%\nconst system = series([svcA, svcB]);",
      "type": "worked-example",
      "intro": "Compose availability across serial dependencies (product) and redundant replicas (parallel).",
      "steps": [
        "Latency is reported as percentiles (p50/p95/p99) because the tail is what users feel.",
        "Series availability = product of components; the system is worse than the weakest link.",
        "Parallel (redundant) availability = 1 − (product of unavailabilities); redundancy raises it.",
        "To hit a target, remove serial hops or add parallel redundancy at the weak component."
      ],
      "language": "ts",
      "commonMistake": "Reporting or optimizing the MEAN latency. A mean of 20ms can hide a p99 of 2s; at 1000 req/s that tail is 10 slow requests every second. Users experience the tail, so you must track and target p95/p99."
    },
    {
      "text": "Three numbers govern performance, and each has a subtlety that separates a real answer from a vibe. LATENCY: the mean lies, because a small number of very slow requests barely move the average but define the user experience — so you report PERCENTILES. p50 is the median, p99 is the value 99% of requests beat; a p99 of 2s means 1 in 100 users waits 2 seconds, and at scale \"1 in 100\" is thousands of people. THROUGHPUT is requests/sec sustained, and it trades against latency once queues build. AVAILABILITY is uptime fraction in NINES, where each nine is a 10× cut in downtime: 99% ≈ 3.65 days/year down, 99.9% ≈ 8.8 hours, 99.99% ≈ 52 minutes, 99.999% ≈ 5 minutes. The load-bearing insight is COMPOSITION. When a request depends on several components IN SERIES (all must succeed), their availabilities MULTIPLY — 0.999 × 0.999 × 0.999 ≈ 0.997 — so the whole is always LESS available than its weakest part, and adding dependencies makes it worse. When you add REDUNDANT replicas IN PARALLEL (only one need succeed), you multiply their UNAVAILABILITIES: two 99% replicas give 1 − 0.01×0.01 = 99.99%. That is why hitting a high availability target is not aspiration but arithmetic: count your serial hops, find the weakest, and add parallelism until the product clears the goal.",
      "type": "concept",
      "title": "Tail latency is felt; availability is multiplied"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "svcA",
          "from": "client",
          "kind": "sync",
          "tone": "accent",
          "label": "must be up"
        },
        {
          "to": "redundant",
          "from": "svcA",
          "kind": "control",
          "tone": "warning",
          "label": "series (× )"
        },
        {
          "to": "redundant",
          "from": "r1",
          "kind": "data",
          "tone": "success",
          "label": "either up"
        },
        {
          "to": "redundant",
          "from": "r2",
          "kind": "data",
          "tone": "success",
          "label": "either up"
        },
        {
          "to": "system",
          "from": "redundant",
          "kind": "sync",
          "tone": "warning",
          "label": "compose"
        }
      ],
      "nodes": [
        {
          "id": "client",
          "kind": "client",
          "tone": "default",
          "label": "request"
        },
        {
          "id": "svcA",
          "kind": "service",
          "tone": "accent",
          "label": "service A (99.9%)",
          "description": "serial hop"
        },
        {
          "id": "r1",
          "kind": "service",
          "tone": "success",
          "label": "replica 1 (99%)",
          "description": "parallel"
        },
        {
          "id": "r2",
          "kind": "service",
          "tone": "success",
          "label": "replica 2 (99%)",
          "description": "parallel"
        },
        {
          "id": "redundant",
          "kind": "decision",
          "tone": "success",
          "label": "service B (redundant → 99.99%)"
        },
        {
          "id": "system",
          "kind": "store",
          "tone": "warning",
          "label": "system availability",
          "description": "product of the chain"
        }
      ],
      "title": "Availability composition",
      "rankdir": "LR",
      "subtitle": "series multiplies down; redundancy multiplies unavailability",
      "storyboard": [
        {
          "ms": 8925,
          "say": "Every request starts at the client and must pass through service A, which is up 99.9 percent of the time — about 8.8 hours of downtime a year.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/latency-throughput-availability__b0.mp3",
          "edges": [
            [
              "client",
              "svcA"
            ]
          ],
          "nodes": [
            "client",
            "svcA"
          ]
        },
        {
          "ms": 8925,
          "say": "Service A sits in series with what comes next, and series components multiply availabilities — every dependency you chain drags the total down.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/latency-throughput-availability__b1.mp3",
          "edges": [
            [
              "svcA",
              "redundant"
            ]
          ],
          "nodes": [
            "svcA",
            "redundant"
          ]
        },
        {
          "ms": 10000,
          "say": "Service B runs as two replicas at 99 percent each; the path fails only if both die at once, so you multiply unavailabilities: 1 percent times 1 percent.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/latency-throughput-availability__b2.mp3",
          "edges": [
            [
              "r1",
              "redundant"
            ],
            [
              "r2",
              "redundant"
            ]
          ],
          "nodes": [
            "r1",
            "r2",
            "redundant"
          ]
        },
        {
          "ms": 10250,
          "say": "That 0.01 percent joint failure turns two mediocre boxes into 99.99 percent — redundancy buys nearly two extra nines from ordinary hardware.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/latency-throughput-availability__b3.mp3",
          "edges": [],
          "nodes": [
            "redundant"
          ]
        },
        {
          "ms": 10475,
          "say": "Now compose the whole path: 99.9 percent times 99.99 percent is about 99.89 — the series chain, not the redundant pair, sets your ceiling.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/latency-throughput-availability__b4.mp3",
          "edges": [
            [
              "redundant",
              "system"
            ]
          ],
          "nodes": [
            "redundant",
            "system"
          ]
        },
        {
          "ms": 8725,
          "say": "So four nines is a design property, not a wish: series multiplies you down, parallel multiplies failure away — count the nines along the entire path.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/latency-throughput-availability__b5.mp3",
          "edges": [
            [
              "client",
              "svcA"
            ],
            [
              "redundant",
              "system"
            ]
          ],
          "nodes": [
            "client",
            "svcA",
            "system"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "target 0.999 needs 2 replicas -> availability 0.999880\ntarget 0.9999 needs 3 replicas -> availability 0.999979",
      "title": "Compose system availability",
      "starter": "// Inverse problem: how many parallel replicas of the weak component\n// are needed for the whole serial chain to meet a target availability?\nfunction chainAvailability(components) { /* all must be up */ }\nfunction redundant(replicas) { /* at least one up */ }\nfunction replicasForTarget(target, weak, others) { /* smallest replica count that hits target */ }\nconst others = [0.99999, 0.99999];\nconst weak = 0.99;\nfor (const target of [0.999, 0.9999]) {\n  const r = replicasForTarget(target, weak, others);\n  console.log(\"target\", target, \"needs\", r.replicas, \"replicas -> availability\", r.availability.toFixed(6));\n}",
      "summary": "Inverse problem: given a serial chain with one weak component, find the smallest number of parallel replicas of that weak component needed to hit a target availability. Solve it for two targets and print the replica count and resulting availability.",
      "language": "js"
    },
    {
      "fix": "const chain = [0.999, 0.999, 0.999, 0.999, 0.999];\nconst endToEnd = chain.reduce((a, c) => a * c, 1); // ≈ 0.995 — below 0.9999\nconst ok = endToEnd >= 0.9999; // false: series multiplies availability DOWN",
      "task": "The check evaluates components individually instead of composing them in series. Fix it to compute the real end-to-end availability.",
      "type": "debug",
      "symptom": "A design promises 99.99% availability, but it is a linear chain of five microservices each rated 99.9%, and it keeps missing the SLO.",
      "language": "ts",
      "brokenCode": "const target = 0.9999;\nconst chain = [0.999, 0.999, 0.999, 0.999, 0.999];\nconst ok = chain.every(a => a >= target);  // wrong: checks each part, not the product"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Run two instances in parallel; only one needs to be up. Two 99.9% replicas give ~99.9999% — redundancy multiplies unavailability down cheaply and survives a single instance failure.",
        "label": "Add a redundant replica"
      },
      "optionB": {
        "text": "Invest in better hardware, retries, and code to lift one box. Real gains are marginal and it still has a single point of failure — one crash is 100% down.",
        "label": "Harden the single instance"
      },
      "guidance": "Add redundancy (A) for availability — parallelism attacks the math directly and removes the single point of failure, which hardening never can. Use B for latency or correctness fixes, but availability targets are met by removing serial single points and adding parallel copies.",
      "question": "You must raise a component from 99.9% to 99.99%. Add a redundant replica, or make the single instance more reliable?"
    },
    {
      "type": "quiz",
      "answer": 3,
      "options": [
        "98% — multiply the two availabilities (0.99 × 0.99), since both copies have to be working for the pair to work.",
        "100% — with a redundant backup there is always one node up, so the pair simply never has any downtime at all.",
        "99% — a second copy is only a spare, so the redundant pair is no more available than a single one of the nodes.",
        "99.99% — multiply the unavailabilities (0.01 × 0.01); the redundant pair fails only when both nodes fail at once."
      ],
      "question": "Two components each 99% available. What is their availability if they are REDUNDANT (parallel, only one need be up)?",
      "explanation": "For redundant components you multiply the UNAVAILABILITIES: both must fail for the pair to fail, so P(both down) = 0.01 × 0.01 = 0.0001, giving availability 1 − 0.0001 = 99.99%. Multiplying the availabilities (0.99 × 0.99 = 98%) is the SERIES formula, which applies when both must be up, not when either suffices."
    },
    {
      "type": "verification",
      "intro": "You reason with the metrics when:",
      "items": [
        "You report latency as p50/p95/p99, never just the mean.",
        "You compute serial availability as a product of components.",
        "You compute redundant availability from the product of unavailabilities.",
        "You can turn an availability target into a required number of serial hops or replicas."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why a good average latency can still mean a bad user experience.",
        "Explain why adding a service in series lowers availability and adding a replica in parallel raises it."
      ]
    },
    {
      "text": "Availability composition is the backbone of the reliability module: failure-modes, resilience patterns, and SLOs all reason about which components are serial single points and where redundancy buys back 9s. The capstone must hit an availability target you compute this way.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Latency reasoned about as percentiles",
        "Series availability computed as a product",
        "Redundant availability computed from unavailabilities"
      ]
    }
  ],
  "sql-vs-nosql": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A store-selector that recommends a store class from the workload's access pattern and consistency needs, and refuses a mismatch.",
      "unlock": "You can justify a store choice by the queries and consistency the workload actually needs.",
      "outcome": "Choose a data store from the ACCESS PATTERN, not fashion: relational for rich queries/transactions/joins, key-value for point lookups, document for nested aggregates, wide-column for huge write throughput.",
      "intensity": "standard",
      "doNotClaim": "Don't claim \"NoSQL is faster\" or \"SQL doesn't scale\" — the right store is the one whose model matches your access pattern."
    },
    {
      "text": "A team picks a document database because it is trendy, then discovers their core feature needs multi-row transactions and ad-hoc joins the store cannot do — so they bolt joins into application code and lose consistency. The store should have been chosen from the ACCESS PATTERN. This lesson turns \"SQL or NoSQL?\" into a decision driven by the queries and guarantees the workload demands.",
      "type": "mission"
    },
    {
      "text": "Data stores differ by the DATA MODEL and the guarantees they optimize for. RELATIONAL (SQL: Postgres, MySQL) gives normalized tables, JOINs, secondary indexes, and ACID transactions — ideal when data is highly related and you need strong consistency and flexible queries. KEY-VALUE (Redis, DynamoDB) is a giant hash map: blazing point reads/writes by key, but no rich query. DOCUMENT (MongoDB) stores nested JSON aggregates — good when you read/write a whole object at once and rarely join. WIDE-COLUMN (Cassandra) is built for massive write throughput and horizontal scale with tunable consistency, at the cost of ad-hoc query flexibility. The choice follows the access pattern: what do you query BY, do you need joins/transactions, and how strong must consistency be.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: your workload is \"look up a user session by session-id, millions of times per second, no joins, no reporting.\" Which store class fits, and why not relational?",
      "reveal": "A KEY-VALUE store fits: the entire access pattern is point lookup by a single key at very high rate, which a hash-map-shaped store serves in O(1) with trivial horizontal scaling. Relational is over-powered here — its joins, secondary indexes, and ACID overhead buy you nothing for a pure key lookup and add latency and operational weight. Match the store to the pattern; do not pay for query power you never use."
    },
    {
      "code": "// Recommend a store class from the workload shape.\nfunction pickStore(w) {\n  if (w.needsTransactions || w.needsJoins) return \"relational\";      // rich queries + ACID\n  if (w.accessBy === \"key\" && !w.needsQuery) return \"key-value\";     // point lookups\n  if (w.shape === \"nested-aggregate\" && !w.needsJoins) return \"document\";\n  if (w.writeThroughput === \"massive\" && w.consistency === \"tunable\") return \"wide-column\";\n  return \"relational\"; // safe default: rich model, strong consistency\n}",
      "type": "worked-example",
      "intro": "Pick a store class from the access pattern and consistency requirement.",
      "steps": [
        "If the workload needs multi-row transactions or joins → relational.",
        "If it is pure point lookup by key at high rate → key-value.",
        "If you read/write whole nested objects and rarely join → document.",
        "If write throughput is massive and consistency can be tuned → wide-column."
      ],
      "language": "ts",
      "commonMistake": "Choosing a store by popularity (\"everyone uses Mongo\") instead of the access pattern, then reimplementing joins or transactions in application code — which is slower, buggier, and loses the consistency the database would have given you for free."
    },
    {
      "text": "There is no universally \"faster\" or \"more scalable\" database — there are data models optimized for different ACCESS PATTERNS, and the engineering skill is matching them. RELATIONAL stores (Postgres, MySQL) normalize data into tables and give you JOINs, secondary indexes, and ACID transactions; choose them when your data is richly interconnected and you need strong consistency or ad-hoc queries — which is most transactional applications. KEY-VALUE stores are hash maps at scale: O(1) get/put by key, effortless horizontal partitioning, but no query beyond the key — choose them for sessions, caches, and lookups where the key is always known. DOCUMENT stores keep denormalized nested aggregates (a whole order with its line-items as one JSON doc); choose them when you fetch and mutate a complete object at once and joins are rare. WIDE-COLUMN stores (Cassandra) are engineered for enormous write throughput and linear horizontal scale with TUNABLE consistency, trading away ad-hoc query flexibility. The decision procedure is always the same three questions: what do I query BY, do I need joins or multi-row transactions, and how strong must consistency be. Answer those from the workload and the store falls out — and the classic failure is inverting it: picking the store first and then contorting the workload (hand-rolled joins, application-level transactions) to fit a model that never suited it.",
      "type": "concept",
      "title": "The access pattern picks the store, not the trend"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "txn",
          "from": "workload",
          "kind": "sync",
          "tone": "accent",
          "label": "ask"
        },
        {
          "to": "relational",
          "from": "txn",
          "kind": "control",
          "tone": "success",
          "label": "yes"
        },
        {
          "to": "bykey",
          "from": "txn",
          "kind": "control",
          "tone": "warning",
          "label": "no"
        },
        {
          "to": "kv",
          "from": "bykey",
          "kind": "control",
          "tone": "success",
          "label": "yes"
        },
        {
          "to": "nested",
          "from": "bykey",
          "kind": "control",
          "tone": "warning",
          "label": "no"
        },
        {
          "to": "doc",
          "from": "nested",
          "kind": "control",
          "label": "yes"
        },
        {
          "to": "wide",
          "from": "nested",
          "kind": "control",
          "label": "no, huge writes"
        }
      ],
      "nodes": [
        {
          "id": "workload",
          "kind": "client",
          "tone": "accent",
          "label": "workload access pattern"
        },
        {
          "id": "txn",
          "kind": "decision",
          "tone": "warning",
          "label": "joins / transactions?"
        },
        {
          "id": "relational",
          "kind": "store",
          "tone": "success",
          "label": "relational (SQL)",
          "description": "ACID, joins"
        },
        {
          "id": "bykey",
          "kind": "decision",
          "tone": "warning",
          "label": "point lookup by key?"
        },
        {
          "id": "kv",
          "kind": "store",
          "tone": "success",
          "label": "key-value",
          "description": "O(1) by key"
        },
        {
          "id": "nested",
          "kind": "decision",
          "tone": "warning",
          "label": "nested aggregate?"
        },
        {
          "id": "doc",
          "kind": "store",
          "tone": "default",
          "label": "document"
        },
        {
          "id": "wide",
          "kind": "store",
          "tone": "default",
          "label": "wide-column",
          "description": "massive writes"
        }
      ],
      "title": "Store selection by access pattern",
      "rankdir": "TB",
      "subtitle": "the workload questions route to a store class",
      "storyboard": [
        {
          "ms": 7600,
          "say": "Never pick a database by trend — start from the workload's access pattern, because every store class optimizes a different data model.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sql-vs-nosql__b0.mp3",
          "edges": [],
          "nodes": [
            "workload"
          ]
        },
        {
          "ms": 7200,
          "say": "First question: does the workload need joins or multi-row transactions? That single answer decides most transactional apps.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sql-vs-nosql__b1.mp3",
          "edges": [
            [
              "workload",
              "txn"
            ]
          ],
          "nodes": [
            "workload",
            "txn"
          ]
        },
        {
          "ms": 9275,
          "say": "If yes, choose relational SQL — Postgres or MySQL give you joins, secondary indexes, and ACID; bolting joins into app code loses consistency.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sql-vs-nosql__b2.mp3",
          "edges": [
            [
              "txn",
              "relational"
            ]
          ],
          "nodes": [
            "txn",
            "relational"
          ]
        },
        {
          "ms": 6425,
          "say": "If not, ask whether every read is a point lookup by a known key — sessions, caches, simple lookups fit this shape.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sql-vs-nosql__b3.mp3",
          "edges": [
            [
              "txn",
              "bykey"
            ]
          ],
          "nodes": [
            "txn",
            "bykey"
          ]
        },
        {
          "ms": 8100,
          "say": "When the key is always known, key-value stores like Redis or DynamoDB give O(1) gets and easy partitioning — but no query beyond the key.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sql-vs-nosql__b4.mp3",
          "edges": [
            [
              "bykey",
              "kv"
            ]
          ],
          "nodes": [
            "bykey",
            "kv"
          ]
        },
        {
          "ms": 8725,
          "say": "Otherwise ask if the data is a nested aggregate read as one unit — if yes, a document store fits; if writes are huge and columnar, go wide-column.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sql-vs-nosql__b5.mp3",
          "edges": [
            [
              "bykey",
              "nested"
            ],
            [
              "nested",
              "doc"
            ],
            [
              "nested",
              "wide"
            ]
          ],
          "nodes": [
            "bykey",
            "nested",
            "doc",
            "wide"
          ]
        },
        {
          "ms": 8225,
          "say": "The insight: there is no universally faster database — the skill is matching the store's data model to your access pattern, in this exact order.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sql-vs-nosql__b6.mp3",
          "edges": [],
          "nodes": [
            "workload",
            "relational",
            "kv",
            "doc",
            "wide"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "ledger: mismatch: use relational\nsessions: ok\ncatalog: mismatch: use document\nmetrics: ok",
      "title": "Build the store selector",
      "starter": "// Audit a proposed store against the workload.\n// Return \"ok\" if the proposal is right, else \"mismatch: use <class>\".\nfunction recommend(w) { /* the correct class for this workload */ }\nfunction auditStoreChoice(w) { /* compare w.proposed to recommend(w) */ }\nconst proposals = [\n  { name: \"ledger\", needsTransactions: true, proposed: \"document\" },\n  { name: \"sessions\", accessBy: \"key\", needsQuery: false, proposed: \"key-value\" },\n  { name: \"catalog\", shape: \"nested-aggregate\", needsJoins: false, proposed: \"relational\" },\n  { name: \"metrics\", writeThroughput: \"massive\", consistency: \"tunable\", proposed: \"wide-column\" },\n];\nfor (const w of proposals) console.log(w.name + \":\", auditStoreChoice(w));",
      "summary": "Instead of picking a store, AUDIT a proposed one: for each workload, compute the correct store class internally and report \"ok\" if the proposal matches, else \"mismatch: use X\". Run it over four proposals and print each verdict.",
      "language": "js"
    },
    {
      "fix": "// The access pattern is rich ad-hoc query + aggregation → relational.\n// SELECT region, count(*) FROM orders\n//   WHERE total > 100 AND created_at > now() - interval '7 days'\n//   GROUP BY region;\n// The DB does this with an index; don't scan a KV store in app code.",
      "task": "The workload needs ad-hoc filtered/aggregated queries, which a key-value store cannot serve. Fix the store choice.",
      "type": "debug",
      "symptom": "A team chose a key-value store for their orders service, and now every \"orders in the last week over $100 grouped by region\" report requires scanning the entire keyspace in application code.",
      "language": "ts",
      "brokenCode": "// key-value store; the report:\nconst all = await kv.scanAll();\nconst filtered = all.filter(o => o.total > 100 && recent(o)).groupBy(o => o.region);"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "One Postgres handles both the key lookups (via primary key) and the reports (via indexes/joins). Simpler operationally; scales vertically and with read replicas until you truly outgrow it.",
        "label": "Relational store"
      },
      "optionB": {
        "text": "Optimize the hot path with a KV store and ship data to an analytics system for reports. Faster hot lookups, but two systems, sync complexity, and eventual consistency between them.",
        "label": "Key-value + separate analytics"
      },
      "guidance": "Start relational (A) unless the point-lookup scale genuinely exceeds what a well-indexed relational store plus replicas can serve. A relational DB does key lookups fine and gives you reports for free; only split to a KV hot path (B) when measured scale forces it — premature splitting adds two systems to keep in sync.",
      "question": "Your app is mostly point lookups by key, but has a few reporting queries. One relational store, or a key-value store plus something for reports?"
    },
    {
      "type": "quiz",
      "answer": 0,
      "options": [
        "Key-value — a hash-map-shaped store matches point lookups by key exactly and partitions horizontally with ease.",
        "Any store works; for a plain key lookup the data model is irrelevant, so just pick whichever the team already runs.",
        "Document — nesting related fields into one aggregate is what makes single-key reads fast, so a document store fits.",
        "Relational — its ACID transactions and join engine are what keep high-rate key lookups fast and correct at scale."
      ],
      "question": "A workload is pure O(1) point lookup by a known key, at very high rate, with no joins or ad-hoc queries. Which store class fits best?",
      "explanation": "The access pattern is entirely get-by-key at high rate — exactly what a key-value (hash-map-shaped) store optimizes, with trivial partitioning. Relational query power and ACID overhead buy nothing here; the model does matter, because the store is optimized around the access pattern."
    },
    {
      "type": "verification",
      "intro": "You choose a store correctly when:",
      "items": [
        "You wrote down what the workload queries BY before choosing.",
        "You checked whether joins or multi-row transactions are required.",
        "You matched the store class to the access pattern, not to popularity.",
        "You did not plan to reimplement joins or transactions in application code."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why \"NoSQL is faster\" is a category error.",
        "Give a workload for each of relational, key-value, document, and wide-column."
      ]
    },
    {
      "text": "Once the store class is chosen, the next lessons make it fast and scalable: indexing (query cost), replication (read scale + durability), and sharding (write scale). The capstone must justify its store from the workload you specify.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Store chosen from the access pattern",
        "Transaction/join needs checked explicitly",
        "No plan to reimplement DB features in app code"
      ]
    }
  ],
  "indexing": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A query-cost estimator that compares scan cost vs index cost using selectivity and recommends whether the index helps.",
      "unlock": "You can predict when an index speeds a query up, when it is ignored, and what it costs on writes.",
      "outcome": "Understand how a B-tree index turns an O(n) scan into an O(log n) lookup, why selectivity decides whether the index is even used, and the write/storage cost every index adds.",
      "intensity": "standard",
      "doNotClaim": "Don't claim \"add an index\" fixes every slow query — a low-selectivity index is ignored, and every index taxes writes."
    },
    {
      "text": "A query over 10 million rows takes 4 seconds; someone adds an index and it is instant — then someone adds an index to every column and writes slow to a crawl while some queries still scan. Indexes are a precise tool: they help HIGH-SELECTIVITY lookups by turning a full scan into a tree descent, they are ignored for low-selectivity predicates, and they cost you on every write. This lesson makes the index decision a computed one.",
      "type": "mission"
    },
    {
      "text": "A database index is an auxiliary structure — usually a B-tree — that lets the engine find matching rows without scanning the table. A full table scan is O(n): read every row. A B-tree index makes an equality or range lookup O(log n): descend the tree to the leaf, then walk the range. But the optimizer only USES an index when the predicate is SELECTIVE enough — if a query matches most rows, jumping around the index plus fetching each row costs more than a straight sequential scan, so the planner picks the scan. And every index must be updated on every insert/update/delete and consumes storage, so indexes speed reads at the cost of writes. The craft is indexing the columns your selective queries filter/sort by — and no more.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: you index a \"country\" column, but 90% of rows are country = \"US\". Will a query for country = \"US\" use the index? What about country = \"IS\" (0.1% of rows)?",
      "reveal": "The \"US\" query (90% selectivity — matches most rows) will likely NOT use the index: fetching 90% of the table via random index lookups is slower than one sequential scan, so the planner scans. The \"IS\" query (0.1% — highly selective) WILL use the index: it isolates a tiny fraction of rows, turning an O(n) scan into an O(log n) descent plus a few fetches. Selectivity — the fraction of rows a predicate matches — decides whether an index is worth using."
    },
    {
      "code": "// Rough cost model: scan reads every row; index descends the tree then fetches matches.\nfunction scanCost(rows) { return rows; }                       // O(n)\nfunction indexCost(rows, selectivity) {\n  const matched = rows * selectivity;\n  return Math.log2(rows) + matched;   // O(log n) descent + one fetch per matched row\n}\nfunction shouldUseIndex(rows, selectivity) {\n  return indexCost(rows, selectivity) < scanCost(rows);\n}",
      "type": "worked-example",
      "intro": "Estimate scan cost vs index cost from row count and selectivity, and decide.",
      "steps": [
        "Scan cost grows with total rows (O(n)); index lookup cost is log(rows) plus one fetch per matched row.",
        "Selectivity = fraction of rows matched; low selectivity (few matches) favors the index.",
        "High selectivity (many matches) makes per-row index fetches cost more than a scan.",
        "Every index also adds write cost and storage — count that against the read speedup."
      ],
      "language": "ts",
      "commonMistake": "Indexing every column \"to be safe.\" Each index slows every write and consumes storage, and low-selectivity indexes are never even used — so you pay the write tax for zero read benefit. Index the columns your selective queries actually filter or sort by."
    },
    {
      "text": "An index is a sorted auxiliary structure, classically a B-TREE, that the database maintains alongside a table so it can locate rows without reading all of them. Without an index, finding matching rows is a full table SCAN: O(n), every row read. With a B-tree index, an equality or range predicate becomes an O(log n) descent to the leaf followed by a walk across the matching range — the difference between 10,000,000 reads and ~24 tree steps. But two facts keep this from being free. First, SELECTIVITY governs whether the index is used at all: selectivity is the fraction of rows a predicate matches, and the optimizer compares the index plan (log n descent + one random fetch per matched row) against a sequential scan (n cheap sequential reads). When a predicate matches a large fraction of the table, the many random fetches lose to a single scan, so the planner IGNORES the index — an index on a column where 90% of rows share one value does nothing for the common query. Highly selective predicates (matching a tiny fraction) are exactly where an index wins. Second, an index is not read-only: every insert, update, and delete must also update every affected index, and each index consumes storage, so indexes accelerate reads at a direct WRITE cost. The discipline: index the columns your selective queries filter and sort by, verify the planner actually uses them, and delete indexes that earn nothing.",
      "type": "concept",
      "title": "An index trades write cost for O(log n) reads — only when selective"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "selectivity",
          "from": "query",
          "kind": "sync",
          "tone": "accent",
          "label": "plan"
        },
        {
          "to": "scan",
          "from": "selectivity",
          "kind": "control",
          "tone": "warning",
          "label": "low (many rows)"
        },
        {
          "to": "btree",
          "from": "selectivity",
          "kind": "control",
          "tone": "success",
          "label": "high (few rows)"
        },
        {
          "to": "rows",
          "from": "btree",
          "kind": "data",
          "tone": "success",
          "label": "fetch matches"
        },
        {
          "to": "rows",
          "from": "scan",
          "kind": "data",
          "label": "read all"
        },
        {
          "to": "writecost",
          "from": "btree",
          "kind": "control",
          "tone": "warning",
          "label": "maintained on write"
        }
      ],
      "nodes": [
        {
          "id": "query",
          "kind": "client",
          "tone": "accent",
          "label": "query predicate"
        },
        {
          "id": "selectivity",
          "kind": "decision",
          "tone": "warning",
          "label": "selectivity?",
          "description": "fraction of rows matched"
        },
        {
          "id": "scan",
          "kind": "process",
          "tone": "default",
          "label": "sequential scan",
          "description": "O(n)"
        },
        {
          "id": "btree",
          "kind": "store",
          "tone": "success",
          "label": "B-tree index",
          "description": "O(log n) descent"
        },
        {
          "id": "rows",
          "kind": "store",
          "tone": "success",
          "label": "matched rows"
        },
        {
          "id": "writecost",
          "kind": "external",
          "tone": "warning",
          "label": "write/storage tax",
          "description": "every write updates the index"
        }
      ],
      "title": "Scan vs index by selectivity",
      "rankdir": "LR",
      "subtitle": "the planner picks the cheaper path",
      "storyboard": [
        {
          "ms": 7125,
          "say": "A query arrives with a predicate over ten million rows, and the planner's first job is deciding how to find the matches.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/indexing__b0.mp3",
          "edges": [
            [
              "query",
              "selectivity"
            ]
          ],
          "nodes": [
            "query"
          ]
        },
        {
          "ms": 5575,
          "say": "The key question is selectivity: what fraction of the table does this predicate actually match?",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/indexing__b1.mp3",
          "edges": [
            [
              "query",
              "selectivity"
            ]
          ],
          "nodes": [
            "selectivity"
          ]
        },
        {
          "ms": 6450,
          "say": "If the predicate matches many rows, the planner chooses a sequential scan and simply reads every row in order.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/indexing__b2.mp3",
          "edges": [
            [
              "selectivity",
              "scan"
            ],
            [
              "scan",
              "rows"
            ]
          ],
          "nodes": [
            "selectivity",
            "scan"
          ]
        },
        {
          "ms": 6400,
          "say": "If it matches only a few rows, the B-tree wins: an O of log n descent to the leaf, then fetch just the matches.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/indexing__b3.mp3",
          "edges": [
            [
              "selectivity",
              "btree"
            ],
            [
              "btree",
              "rows"
            ]
          ],
          "nodes": [
            "selectivity",
            "btree",
            "rows"
          ]
        },
        {
          "ms": 7100,
          "say": "But the index is never free: every insert and update must maintain the tree, so each index adds a write and storage tax.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/indexing__b4.mp3",
          "edges": [
            [
              "btree",
              "writecost"
            ]
          ],
          "nodes": [
            "btree",
            "writecost"
          ]
        },
        {
          "ms": 6950,
          "say": "So indexes are a precise tool: add them for selective lookups the planner will actually use, and skip them everywhere else.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/indexing__b5.mp3",
          "edges": [
            [
              "selectivity",
              "btree"
            ],
            [
              "selectivity",
              "scan"
            ]
          ],
          "nodes": [
            "query",
            "selectivity",
            "btree",
            "scan"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "rows 1000000 break-even selectivity 0.249995\nrows 100000 break-even selectivity 0.249958",
      "title": "Build the query-cost estimator",
      "starter": "const SEEK = 4; // a random index fetch costs ~4x a sequential row read\nfunction scanReads(rows) { /* full scan cost */ }\nfunction indexReads(rows, selectivity) { /* tree descent + seek penalty per matched row */ }\nfunction breakEvenSelectivity(rows) { /* largest selectivity where index still wins */ }\nfor (const rows of [1_000_000, 100_000]) {\n  console.log(\"rows\", rows, \"break-even selectivity\", breakEvenSelectivity(rows).toFixed(6));\n}",
      "summary": "Find the break-even selectivity: the maximum fraction of matched rows at which the index still beats a full scan, using a cost model that charges a random-seek penalty per matched row. Binary-search it for two table sizes and print each to 6 decimals.",
      "language": "js"
    },
    {
      "fix": "-- Drop the low-selectivity index; index a SELECTIVE column the query filters by.\nDROP INDEX events_status_idx;\nCREATE INDEX ON events (user_id, created_at);  -- selective, matches the real query\n-- verify with EXPLAIN that the planner actually uses it.",
      "task": "A low-selectivity index is never used yet still taxes every write. Fix the indexing strategy.",
      "type": "debug",
      "symptom": "A table has an index on every column, writes have become painfully slow, and yet the dashboard query is still doing a full scan.",
      "language": "ts",
      "brokenCode": "CREATE INDEX ON events (status);  -- status is 'ok' for 95% of rows\n-- query: SELECT * FROM events WHERE status = 'ok';  (still scans)"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "If the query's predicate is selective, the index turns an O(n) scan into O(log n). Reads get dramatically faster; you pay a small, bounded write/storage cost on that one index.",
        "label": "Add the index (if selective)"
      },
      "optionB": {
        "text": "Avoid the write tax and keep writes fast. Fine if the query is rare or low-selectivity, but a frequent selective query keeps scanning the whole table.",
        "label": "Leave it unindexed"
      },
      "guidance": "Add the index (A) when the query is frequent AND selective — that is precisely where a B-tree pays off and the write cost of one index is small. Leave it (B) when the predicate is low-selectivity (the planner would ignore the index anyway) or the query is rare. Let selectivity and frequency, not habit, decide.",
      "question": "A read-heavy table gets one new slow filtered query. Add an index for it, or leave the table un-indexed on that column?"
    },
    {
      "type": "quiz",
      "answer": 3,
      "options": [
        "The table has too few rows for an index to be maintained, so the planner drops it and scans the whole table instead.",
        "Indexes only serve ORDER BY, never WHERE, so a filtering query cannot use one no matter how selective it happens to be.",
        "Indexes only accelerate numeric columns, so the planner ignores an index built on text or other non-numeric types.",
        "The predicate has low selectivity — it matches most rows, so one sequential scan beats many random index fetches."
      ],
      "question": "Why might the query planner refuse to use an index on a column even though one exists?",
      "explanation": "When a predicate matches a large fraction of rows (low selectivity), fetching each match through the index costs more than one straight sequential scan, so the cost-based planner chooses the scan. Indexes win on HIGH-selectivity predicates that isolate a small fraction of rows."
    },
    {
      "type": "verification",
      "intro": "You index deliberately when:",
      "items": [
        "You index columns your selective queries filter or sort by.",
        "You checked selectivity before assuming an index helps.",
        "You verified (EXPLAIN) the planner actually uses the index.",
        "You accounted for the write and storage cost each index adds."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain how a B-tree index changes a lookup from O(n) to O(log n).",
        "Explain why an index on a low-selectivity column can be worse than no index."
      ]
    },
    {
      "text": "Indexing is the single-node read-speed lever; when one node is not enough, replication (next) scales reads across copies and sharding scales writes across partitions. The capstone's data layer must justify its indexes by the queries it serves.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Index vs scan reasoned about via selectivity",
        "Planner use verified, not assumed",
        "Write/storage cost of each index accounted for"
      ]
    }
  ],
  "replication": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A replication-lag checker that decides whether a given read can be served from a follower without violating read-after-write consistency.",
      "unlock": "You can explain replication lag and route reads to avoid the read-your-own-writes anomaly.",
      "outcome": "Understand leader/follower replication, the sync-vs-async durability/latency tradeoff, and the read-after-write anomaly that async replication lag creates.",
      "intensity": "standard",
      "doNotClaim": "Don't claim followers are \"just extra copies\" — async replication lags, and a naive read from a follower can miss the user's own just-made write."
    },
    {
      "text": "A user updates their profile, the app reads it back from a follower replica, and the OLD value comes back — because the write went to the leader and the follower has not caught up yet. Replication is how you scale reads and survive a node loss, but async replicas LAG, and that lag produces real anomalies. This lesson makes the lag visible and teaches you to route reads so a user always sees their own writes.",
      "type": "mission"
    },
    {
      "text": "Replication keeps copies of the data on multiple nodes for read scaling and fault tolerance. The common model is LEADER/FOLLOWER: all writes go to the LEADER, which streams a change log to one or more FOLLOWERS that serve reads. The key choice is SYNCHRONOUS vs ASYNCHRONOUS. Sync replication waits for a follower to acknowledge before the write is confirmed — durable and consistent, but slower and stalls if a follower is down. Async confirms the write as soon as the leader has it and ships to followers in the background — fast and available, but followers LAG, so a read from a follower may return stale data. That lag causes the READ-AFTER-WRITE (read-your-own-writes) anomaly: a user who just wrote can read their own stale value from a lagging follower. The fix is to route a user's reads to the leader (or a caught-up follower) for a window after their write.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: with async leader/follower replication, a user updates their name and the app immediately reads it from a follower. What can go wrong, and how do you prevent it?",
      "reveal": "The follower may not have received the update yet (replication lag), so the read returns the OLD name — the user does not see their own write, which looks like a bug. Prevention: for a short window after a user's write, route THAT user's reads to the leader or to a follower known to be caught up past the write's position. This \"read-your-writes\" routing trades a little leader load for correctness the user can feel."
    },
    {
      "code": "// Each write has a monotonic log position; each follower reports how far it has applied.\nfunction canServeFromFollower(followerPos, userLastWritePos) {\n  // Safe only if the follower has applied up to the user's most recent write.\n  return followerPos >= userLastWritePos;\n}\nfunction routeRead(followerPos, userLastWritePos) {\n  return canServeFromFollower(followerPos, userLastWritePos) ? \"follower\" : \"leader\";\n}",
      "type": "worked-example",
      "intro": "Decide if a follower read is safe given its replication position vs the write position.",
      "steps": [
        "Writes go to the leader and get a monotonically increasing log position.",
        "Followers apply the leader's log and report the position they have reached.",
        "A follower read is safe only if the follower's position ≥ the user's last-write position.",
        "If the follower is behind the user's write, route that read to the leader."
      ],
      "language": "ts",
      "commonMistake": "Load-balancing all reads across followers without tracking each user's last write. Right after a write, the user's read lands on a lagging follower and returns stale data — the read-your-own-writes anomaly, which looks like data loss to the user."
    },
    {
      "text": "Replication puts copies of the data on multiple nodes to scale reads and survive failures, and the workhorse model is LEADER/FOLLOWER (a.k.a. primary/replica): every write is applied at the LEADER, which appends it to a replication log at a monotonically increasing position and streams that log to FOLLOWERS, which apply it and serve reads. The pivotal choice is durability vs latency. SYNCHRONOUS replication holds the write \"unconfirmed\" until at least one follower acknowledges it — the data survives a leader crash and reads are consistent, but every write pays a round-trip and stalls entirely if the sync follower is unavailable. ASYNCHRONOUS replication confirms the write as soon as the leader has durably stored it and ships to followers in the background — writes are fast and stay available even if a follower is down, but followers LAG behind the leader by some replication delay. That lag is not academic: it produces the READ-AFTER-WRITE anomaly, where a user who just updated a value reads it back from a follower that has not yet applied the update and sees the OLD value — indistinguishable from a bug or data loss to that user. The standard fix is position-aware routing: record the log position of a user's latest write, and serve their subsequent reads only from a replica whose applied position has caught up to (≥) that write — otherwise send the read to the leader. This buys read-your-own-writes consistency for the cost of a little extra leader traffic in the window right after each write.",
      "type": "concept",
      "title": "Followers lag, so reads must respect the write position"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "leader",
          "from": "client",
          "kind": "sync",
          "tone": "accent",
          "label": "write @ pos N"
        },
        {
          "to": "f1",
          "from": "leader",
          "kind": "async",
          "tone": "success",
          "label": "stream log"
        },
        {
          "to": "f2",
          "from": "leader",
          "kind": "async",
          "tone": "warning",
          "label": "stream (behind)"
        },
        {
          "to": "router",
          "from": "read",
          "kind": "sync",
          "label": "read-your-writes"
        },
        {
          "to": "f1",
          "from": "router",
          "kind": "data",
          "tone": "success",
          "label": "pos ≥ N → ok"
        },
        {
          "to": "leader",
          "from": "router",
          "kind": "control",
          "tone": "warning",
          "label": "else → leader"
        }
      ],
      "nodes": [
        {
          "id": "client",
          "kind": "client",
          "tone": "accent",
          "label": "client write"
        },
        {
          "id": "leader",
          "kind": "store",
          "tone": "success",
          "label": "leader",
          "description": "accepts writes, log pos"
        },
        {
          "id": "f1",
          "kind": "store",
          "tone": "success",
          "label": "follower (caught up)",
          "description": "pos ≥ write"
        },
        {
          "id": "f2",
          "kind": "store",
          "tone": "warning",
          "label": "follower (lagging)",
          "description": "pos < write"
        },
        {
          "id": "router",
          "kind": "decision",
          "tone": "warning",
          "label": "read router",
          "description": "pos check"
        },
        {
          "id": "read",
          "kind": "client",
          "tone": "default",
          "label": "user read"
        }
      ],
      "title": "Leader/follower replication and lag",
      "rankdir": "LR",
      "subtitle": "route reads past the user's write position",
      "storyboard": [
        {
          "ms": 6875,
          "say": "A user updates their profile: the write goes to the leader, which commits it at log position N and confirms immediately.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/replication__b0.mp3",
          "edges": [
            [
              "client",
              "leader"
            ]
          ],
          "nodes": [
            "client",
            "leader"
          ]
        },
        {
          "ms": 6650,
          "say": "The leader streams its replication log to the followers, which apply each change in order and serve the read traffic.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/replication__b1.mp3",
          "edges": [
            [
              "leader",
              "f1"
            ],
            [
              "leader",
              "f2"
            ]
          ],
          "nodes": [
            "leader",
            "f1",
            "f2"
          ]
        },
        {
          "ms": 7475,
          "say": "Because replication is asynchronous, one follower is already caught up to N, but the other is still behind — it holds the old profile.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/replication__b2.mp3",
          "edges": [
            [
              "leader",
              "f2"
            ]
          ],
          "nodes": [
            "f1",
            "f2"
          ]
        },
        {
          "ms": 7300,
          "say": "Moments later the same user reads their profile back; hit the lagging follower and they see stale data — their own write vanishes.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/replication__b3.mp3",
          "edges": [
            [
              "read",
              "router"
            ]
          ],
          "nodes": [
            "read",
            "f2",
            "router"
          ]
        },
        {
          "ms": 8150,
          "say": "The fix is read-your-writes routing: the router remembers the user wrote at position N and only picks a follower that has replayed to N or beyond.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/replication__b4.mp3",
          "edges": [
            [
              "read",
              "router"
            ],
            [
              "router",
              "f1"
            ]
          ],
          "nodes": [
            "router",
            "f1"
          ]
        },
        {
          "ms": 7500,
          "say": "If no follower has reached N yet, the router falls back to the leader — lag never disappears, so you route reads around it, not hope it's zero.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/replication__b5.mp3",
          "edges": [
            [
              "router",
              "leader"
            ]
          ],
          "nodes": [
            "router",
            "leader"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "chosen: f3\nlag: 0\nf1 lag 4\nf2 lag 0\nf3 lag 0\nf4 lag 0",
      "title": "Build the read-after-write router",
      "starter": "// Pick the safe follower with the smallest position that is still >= the write.\n// Fall back to the leader if none qualify.\nfunction replicationLag(writePos, followerPos) { /* how far behind, never negative */ }\nfunction pickFollower(followers, userLastWritePos) { /* choose target + report lag */ }\nconst userLastWritePos = 100;\nconst followers = [\n  { name: \"f1\", pos: 96 },\n  { name: \"f2\", pos: 104 },\n  { name: \"f3\", pos: 100 },\n  { name: \"f4\", pos: 101 },\n];\nconst r = pickFollower(followers, userLastWritePos);\nconsole.log(\"chosen:\", r.target);\nconsole.log(\"lag:\", r.lag);\nfor (const f of followers) console.log(f.name, \"lag\", replicationLag(userLastWritePos, f.pos));",
      "summary": "Given many followers and the user's last-write position, choose the safe follower that is least over-provisioned (smallest position still at or past the write) so the most-caught-up replicas stay free for stricter reads; fall back to the leader if none qualify. Print the choice, its lag, and every follower's lag.",
      "language": "js"
    },
    {
      "fix": "const writePos = getUserLastWritePos(userId);\nconst replica = pickAnyFollower();\nreturn replica.appliedPos >= writePos\n  ? replica.read(userId)      // caught up: safe\n  : leader.read(userId);      // behind: read your own write from the leader",
      "task": "Reads are load-balanced across followers with no regard for whether they have applied the user's latest write. Fix the routing.",
      "type": "debug",
      "symptom": "Right after saving a setting, users sometimes see the old value on the very next page load; a refresh a second later shows the new value.",
      "language": "ts",
      "brokenCode": "const replica = pickAnyFollower();       // random follower, ignores lag\nreturn replica.read(userId);              // may be behind the just-made write"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Wait for a follower to acknowledge before confirming the write. The payment record survives a leader crash and reads are consistent — at the cost of higher write latency and a stall if the sync follower is down.",
        "label": "Synchronous replication"
      },
      "optionB": {
        "text": "Confirm as soon as the leader has it; followers catch up later. Fast and available, but a leader crash before the change ships can lose the confirmation, and followers can serve stale reads.",
        "label": "Asynchronous replication"
      },
      "guidance": "For money and other data you cannot lose, use synchronous (A) — durability and consistency outweigh the latency cost, and you often use semi-sync (wait for one of several followers) to avoid a full stall. Use async (B) for high-volume, loss-tolerant data (feeds, analytics) where speed and availability matter more than never losing the last write.",
      "question": "For a payment confirmation write, do you replicate synchronously or asynchronously?"
    },
    {
      "type": "quiz",
      "answer": 3,
      "options": [
        "Data corruption on the follower node; restore that replica from its last backup so it matches the leader again.",
        "Normal async behavior that cannot be avoided; followers always lag the leader, so the stale read is simply unfixable.",
        "A caching bug serving a stale copy; clear the cache on every write so the next read recomputes from the database.",
        "The read-after-write anomaly from replication lag; route post-write reads to the leader or a caught-up follower."
      ],
      "question": "A user updates a value, then immediately reads it from an asynchronous follower and sees the OLD value. What is this, and the standard fix?",
      "explanation": "Async followers lag the leader, so a read right after a write can hit a follower that has not yet applied it — the read-your-own-writes (read-after-write) anomaly. The fix is position-aware routing: send that user's reads to the leader or a follower whose applied position has reached the write, until the followers catch up."
    },
    {
      "type": "verification",
      "intro": "You handle replication correctly when:",
      "items": [
        "You can explain leader/follower and why writes go to the leader.",
        "You choose sync vs async by durability vs latency, deliberately.",
        "You track each user's last-write position for read routing.",
        "Post-write reads never return a value older than the user's own write."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain the read-after-write anomaly and where the stale value comes from.",
        "Explain the durability/latency tradeoff between sync and async replication."
      ]
    },
    {
      "text": "Replication scales reads and adds durability; the consistency questions it raises (stale followers, read-your-writes) are the same ones CAP, quorums, and eventual consistency formalize in Module 4. Sharding (next) is its complement — replication copies data, sharding splits it.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Leader/follower model explained",
        "Sync vs async chosen by durability/latency",
        "Read-your-writes preserved via position-aware routing"
      ]
    }
  ],
  "sharding-partitioning": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A consistent-hash ring that distributes keys across nodes and reports the per-node key counts so you can see balance and hotspots.",
      "unlock": "You can choose a shard key that avoids hotspots and explain why consistent hashing minimizes reshuffling when a node joins or leaves.",
      "outcome": "Split data across nodes by a shard key, understand why the key choice determines load balance, and use consistent hashing to spread keys evenly and detect hotspots.",
      "intensity": "standard",
      "doNotClaim": "Don't claim sharding \"just splits the data\" — a bad shard key creates a hotspot that overloads one node while the others idle."
    },
    {
      "text": "A service shards user data by \"signup month\" and one node melts under all the new users while older-month nodes sit idle — a classic HOTSPOT from a bad shard key. Sharding scales writes and storage past a single node, but only if the KEY spreads load evenly, and only if adding a node does not reshuffle everything. This lesson builds the consistent-hash ring that makes both true, and a hotspot detector to prove the spread.",
      "type": "mission"
    },
    {
      "text": "Sharding (horizontal partitioning) splits a dataset across multiple nodes so writes, storage, and throughput scale beyond one machine — each node owns a subset of the keys. The SHARD KEY decides which node a record lands on, and choosing it well is the whole game: a key with skewed distribution (few hot values) piles load on one node (a HOTSPOT) while others idle. Naive modulo sharding (node = hash(key) % N) spreads keys, but changing N remaps almost every key. CONSISTENT HASHING (Karger et al., 1997) fixes that: hash both keys and nodes onto a ring; each key is owned by the next node clockwise, so adding or removing a node only remaps the keys between it and its neighbor — about K/N keys on average, the theoretical minimum. Virtual nodes (many ring positions per physical node) smooth the distribution further.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: you shard 8 keys across 4 nodes with node = hash(key) % 4. You add a 5th node (% 5). Roughly how many of the 8 keys move? Why does consistent hashing do better?",
      "reveal": "With modulo, changing 4→5 changes hash % N for almost every key, so most of the 8 keys move — a near-total reshuffle that invalidates caches and thrashes the network. Consistent hashing places nodes on a hash ring and gives each key to the next node clockwise; adding a node only steals the keys in the arc between it and its clockwise neighbor — on average about K/N keys (here ~1–2 of 8), not most of them. That minimal-movement property is why consistent hashing underpins distributed caches and databases."
    },
    {
      "code": "// A tiny deterministic hash into [0, RING).\nconst RING = 1024;\nfunction hash(str) {\n  let h = 2166136261;                    // FNV-1a-ish, deterministic\n  for (let i = 0; i < str.length; i++) { h ^= str.charCodeAt(i); h = (h * 16777619) >>> 0; }\n  return h % RING;\n}\n// Place nodes on the ring; each key goes to the next node clockwise (wrapping).\nfunction buildRing(nodes) {\n  return nodes.map(n => ({ node: n, pos: hash(n) })).sort((a, b) => a.pos - b.pos);\n}\nfunction ownerOf(key, ring) {\n  const p = hash(key);\n  for (const slot of ring) if (slot.pos >= p) return slot.node;\n  return ring[0].node;                    // wrap around\n}",
      "type": "worked-example",
      "intro": "Build a consistent-hash ring: place nodes on the ring, then assign each key to the next node clockwise.",
      "steps": [
        "Hash each NODE to a position on a fixed ring.",
        "Hash each KEY to a ring position; its owner is the first node clockwise (≥ its position).",
        "Wrap around: a key past the last node belongs to the first node.",
        "Count keys per node to see the distribution and spot hotspots."
      ],
      "language": "ts",
      "commonMistake": "Choosing a low-cardinality or skewed shard key (country, signup-month, a boolean). Most records collapse onto a few values, so a few nodes carry nearly all the load — a hotspot — while the rest idle. A good shard key has high cardinality and even distribution."
    },
    {
      "text": "Sharding scales a store horizontally by splitting the keyspace across nodes, and two problems decide whether it works: load BALANCE and reshuffling COST. Balance depends entirely on the SHARD KEY. If the key is skewed or low-cardinality — country, signup month, a status flag — records pile onto a handful of values and the nodes owning them become HOTSPOTS, saturated while their peers sit idle; a good shard key is high-cardinality and uniformly distributed (a user id, a hashed key). Reshuffling cost is the other trap: with naive modulo sharding, node = hash(key) % N, changing the node count N alters hash % N for nearly every key, so scaling out remaps almost the entire dataset and invalidates every cache. CONSISTENT HASHING (Karger, Lehman, Leighton, Panigrahy, Levine, Lewin, 1997) solves this by mapping both keys and nodes onto the same circular hash space; each key is owned by the first node encountered going CLOCKWISE from the key's position (wrapping past the end back to the first node). Now adding or removing a node only affects the keys in the arc between that node and its neighbor — on average about K/N of K total keys, which is the minimum any scheme can achieve. Because a single hash position per node can still leave uneven gaps, real systems assign each physical node many VIRTUAL positions on the ring, which averages out the arc sizes and smooths the distribution. This is the mechanism under Dynamo, Cassandra, and consistent-hash load balancers alike: even spread, minimal movement on membership change.",
      "type": "concept",
      "title": "Consistent hashing spreads keys and minimizes movement"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "ring",
          "from": "key",
          "kind": "data",
          "tone": "accent",
          "label": "hash"
        },
        {
          "to": "nodeA",
          "from": "ring",
          "kind": "sync",
          "tone": "success",
          "label": "clockwise owner"
        },
        {
          "to": "nodeB",
          "from": "ring",
          "kind": "sync",
          "tone": "success",
          "label": "next arc"
        },
        {
          "to": "nodeC",
          "from": "ring",
          "kind": "sync",
          "tone": "success",
          "label": "next arc"
        },
        {
          "to": "hotspot",
          "from": "key",
          "kind": "control",
          "tone": "warning",
          "label": "skewed key"
        }
      ],
      "nodes": [
        {
          "id": "key",
          "kind": "client",
          "tone": "accent",
          "label": "key",
          "description": "hash(key) → ring pos"
        },
        {
          "id": "ring",
          "kind": "process",
          "tone": "accent",
          "label": "hash ring",
          "description": "0 … RING-1, wraps"
        },
        {
          "id": "nodeA",
          "kind": "store",
          "tone": "success",
          "label": "node A @ pos"
        },
        {
          "id": "nodeB",
          "kind": "store",
          "tone": "success",
          "label": "node B @ pos"
        },
        {
          "id": "nodeC",
          "kind": "store",
          "tone": "success",
          "label": "node C @ pos"
        },
        {
          "id": "hotspot",
          "kind": "store",
          "tone": "warning",
          "label": "hotspot (bad key)",
          "description": "skewed key → one node overloaded"
        }
      ],
      "title": "The consistent-hash ring",
      "rankdir": "LR",
      "subtitle": "keys go to the next node clockwise; adding a node moves ~K/N keys",
      "storyboard": [
        {
          "ms": 7875,
          "say": "Every record starts with its shard key — we hash that key onto a fixed ring of positions, so placement depends on the key, not on the node count.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sharding-partitioning__b0.mp3",
          "edges": [
            [
              "key",
              "ring"
            ]
          ],
          "nodes": [
            "key",
            "ring"
          ]
        },
        {
          "ms": 6375,
          "say": "From the key's hash position, we walk clockwise around the ring; the first node we hit — here node A — owns that key.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sharding-partitioning__b1.mp3",
          "edges": [
            [
              "ring",
              "nodeA"
            ]
          ],
          "nodes": [
            "ring",
            "nodeA"
          ]
        },
        {
          "ms": 8150,
          "say": "Nodes B and C own the next arcs, so the keyspace is carved into ranges — each node serves only the slice of hashes that lands on its arc.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sharding-partitioning__b2.mp3",
          "edges": [
            [
              "ring",
              "nodeB"
            ],
            [
              "ring",
              "nodeC"
            ]
          ],
          "nodes": [
            "nodeB",
            "nodeC"
          ]
        },
        {
          "ms": 8175,
          "say": "Now the payoff: adding a node just splits one arc, so only about K over N keys move — modulo sharding would remap nearly the whole dataset.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sharding-partitioning__b3.mp3",
          "edges": [
            [
              "ring",
              "nodeA"
            ],
            [
              "ring",
              "nodeB"
            ],
            [
              "ring",
              "nodeC"
            ]
          ],
          "nodes": [
            "ring",
            "nodeA",
            "nodeB",
            "nodeC"
          ]
        },
        {
          "ms": 8525,
          "say": "But the ring can't save a bad key: shard by signup month and every new user hashes to the same few values, melting one node while its peers idle.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sharding-partitioning__b4.mp3",
          "edges": [
            [
              "key",
              "hotspot"
            ]
          ],
          "nodes": [
            "key",
            "hotspot"
          ]
        },
        {
          "ms": 8025,
          "say": "So sharding works when two things hold — a high-cardinality, uniform shard key for balance, and consistent hashing to keep rescaling cheap.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/sharding-partitioning__b5.mp3",
          "edges": [
            [
              "key",
              "ring"
            ]
          ],
          "nodes": [
            "key",
            "ring",
            "nodeA",
            "nodeB",
            "nodeC"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "single: A=2 B=5 C=5 spread 3\nvnodes: A=4 B=4 C=4 spread 0",
      "title": "Build a consistent-hash ring and detect balance",
      "starter": "const RING = 1024;\n// Ring positions are given (already hashed). Assign each key to the first\n// placement clockwise (wrapping), then count keys per NODE and the spread.\nfunction owner(keyPos, ring) { /* first placement with pos >= keyPos, wrapping */ }\nfunction distribution(keyPositions, ring, nodes) { /* keys per node */ }\nfunction spread(counts, nodes) { /* max count minus min count */ }\nconst nodes = [\"A\", \"B\", \"C\"];\nconst keyPositions = [40, 120, 210, 300, 380, 460, 540, 620, 700, 800, 900, 1000];\nconst single = [{ node: \"A\", pos: 100 }, { node: \"B\", pos: 500 }, { node: \"C\", pos: 900 }].sort((a, b) => a.pos - b.pos);\nconst vnodes = [\n  { node: \"A\", pos: 100 }, { node: \"B\", pos: 200 }, { node: \"C\", pos: 350 },\n  { node: \"A\", pos: 500 }, { node: \"B\", pos: 650 }, { node: \"C\", pos: 800 },\n  { node: \"A\", pos: 900 }, { node: \"B\", pos: 1000 }, { node: \"C\", pos: 1024 },\n].sort((a, b) => a.pos - b.pos);\nconst ds = distribution(keyPositions, single, nodes);\nconst dv = distribution(keyPositions, vnodes, nodes);\nconsole.log(\"single:\", nodes.map(n => n + \"=\" + ds[n]).join(\" \"), \"spread\", spread(ds, nodes));\nconsole.log(\"vnodes:\", nodes.map(n => n + \"=\" + dv[n]).join(\" \"), \"spread\", spread(dv, nodes));",
      "summary": "Virtual nodes: given precomputed ring positions, implement clockwise ownership and key distribution, then compare a single-placement ring against a 3-virtual-nodes-per-node ring over the same 12 keys. Print each layout's per-node counts and spread to show vnodes flatten the imbalance.",
      "language": "js"
    },
    {
      "fix": "// Use a high-cardinality, evenly-distributed key (e.g. the account id) on a\n// consistent-hash ring, so records spread evenly and adding a node moves ~K/N keys.\nconst shard = ownerOf(record.account_id, ring);",
      "task": "The shard key has only three values, so almost all records land on one or two nodes — a hotspot. Fix the shard key.",
      "type": "debug",
      "symptom": "A sharded write layer has one node at 95% CPU while the other three sit near idle, and it started when the team sharded by \"account_tier\" (free/pro/enterprise).",
      "language": "ts",
      "brokenCode": "const shard = nodes[hash(record.account_tier) % nodes.length]; // 3 possible values"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Keys and nodes on a ring; adding/removing a node remaps only ~K/N keys. Slightly more machinery (ring, virtual nodes), but scaling out barely disturbs the cluster or the caches.",
        "label": "Consistent hashing"
      },
      "optionB": {
        "text": "Dead simple to implement and perfectly balanced for a FIXED N. But changing N remaps nearly every key, so every scale-out event reshuffles the whole dataset and invalidates caches.",
        "label": "Modulo (hash % N)"
      },
      "guidance": "Use consistent hashing (A) whenever the node set can change — which is almost always in a system built to scale. Modulo (B) is acceptable only for a truly fixed partition count; the moment you add a node its total-reshuffle cost dominates, which is exactly the problem consistent hashing was invented to solve.",
      "question": "You need to scale a store horizontally. Naive modulo sharding (hash % N) or consistent hashing?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "It never rehashes any key at all; keys are pinned to their first node for life, so adding a node moves nothing.",
        "Each key belongs to the next node clockwise on a ring, so a new node only steals the keys in one arc — about K/N.",
        "It uses a faster, better-distributed hash function, and the stronger hash is what keeps most keys from relocating.",
        "It replicates every key onto every node, so adding a node merely copies data and no key ever has to move at all."
      ],
      "question": "Why does consistent hashing move far fewer keys than modulo sharding when a node is added?",
      "explanation": "On the ring, a key belongs to the first node clockwise. Inserting a node only reassigns the keys in the arc between it and its clockwise neighbor — roughly K/N of K keys — instead of re-evaluating hash % N for every key, which modulo sharding does. That minimal movement (proven optimal) is the defining property of consistent hashing."
    },
    {
      "type": "verification",
      "intro": "You shard correctly when:",
      "items": [
        "Your shard key is high-cardinality and evenly distributed.",
        "You can show per-node key counts and confirm no hotspot.",
        "You use consistent hashing (with virtual nodes) so scaling moves ~K/N keys.",
        "You can explain why a skewed key overloads one node."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain how a key finds its owner on a consistent-hash ring.",
        "Explain why sharding by a low-cardinality column creates a hotspot."
      ]
    },
    {
      "text": "Sharding scales writes across partitions the way replication scales reads across copies; together they are the storage foundation the distributed-systems module (CAP, quorums) reasons about. The load-balancing lesson reuses this exact consistent-hash ring for request routing, and the capstone shards its store by a key you must defend.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Shard key chosen for high cardinality + even spread",
        "Consistent-hash ring assigns keys clockwise",
        "Per-node distribution measured for hotspots"
      ]
    }
  ],
  "caching-strategies": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A working LRU cache that evicts the least-recently-used entry at capacity and reports the hit rate over a request trace.",
      "unlock": "You can pick a caching strategy for a workload and predict its hit rate and staleness behavior.",
      "outcome": "Choose a caching strategy (cache-aside, write-through, write-back) and reason about hit rate, TTL, and eviction — implementing an LRU cache and measuring its hit rate.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a cache \"makes things fast\" without a hit rate — a cache with a low hit rate adds a hop and buys nothing."
    },
    {
      "text": "A team adds a cache and latency barely improves, because the hit rate is 20% — most requests miss, pay the cache lookup AND the database read. Caching is not a magic speed switch; it is a strategy with a measurable hit rate, an eviction policy that decides what survives, and a TTL that bounds staleness. This lesson builds a real LRU cache and measures the number that actually matters: the hit rate.",
      "type": "mission"
    },
    {
      "text": "A cache stores hot data on a fast tier so repeated reads skip the slow store. Three write strategies decide correctness and speed. CACHE-ASIDE (lazy): the app checks the cache; on a miss it reads the DB and populates the cache — simple, and only requested data is cached. WRITE-THROUGH: writes go to the cache AND the DB synchronously — the cache is always fresh, at higher write latency. WRITE-BACK (write-behind): writes hit the cache and flush to the DB later — fastest writes, but a crash can lose un-flushed data. Because a cache is finite, it needs an EVICTION policy; LRU (least-recently-used) evicts the entry unused for the longest, a good fit when recent data is likely to be reused. TTL caps how long an entry lives regardless of use, bounding staleness. The metric that decides if any of it helped is the HIT RATE: hits / (hits + misses).",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: an LRU cache holds 2 entries. Requests come in: A, B, A, C. Which key gets evicted when C arrives, and why?",
      "reveal": "B is evicted. After A, B the cache holds {A, B}. The request for A makes A the most-recently-used, so the order (LRU→MRU) is B, A. When C arrives the cache is full, so it evicts the LEAST-recently-used entry — B — leaving {A, C}. LRU keeps what was touched most recently on the bet that recent access predicts near-future access."
    },
    {
      "code": "// JS Map preserves insertion order; re-inserting a key moves it to \"most recent\".\nclass LRU {\n  constructor(capacity) { this.cap = capacity; this.map = new Map(); this.hits = 0; this.misses = 0; }\n  get(key) {\n    if (!this.map.has(key)) { this.misses++; return undefined; }\n    const val = this.map.get(key);\n    this.map.delete(key); this.map.set(key, val);   // touch → most recent\n    this.hits++; return val;\n  }\n  put(key, val) {\n    if (this.map.has(key)) this.map.delete(key);\n    else if (this.map.size >= this.cap) this.map.delete(this.map.keys().next().value); // evict LRU\n    this.map.set(key, val);\n  }\n  hitRate() { const t = this.hits + this.misses; return t ? this.hits / t : 0; }\n}",
      "type": "worked-example",
      "intro": "An LRU cache: a Map ordered by recency, evicting the oldest entry at capacity.",
      "steps": [
        "A Map keeps entries in insertion order; the first key is the least-recently-used.",
        "On get, delete+reinsert the key so it becomes most-recently-used; count hit or miss.",
        "On put at capacity, evict the first (oldest) key before inserting.",
        "Hit rate = hits / (hits + misses) — the number that says whether the cache earns its place."
      ],
      "language": "ts",
      "commonMistake": "Deploying a cache and never measuring the hit rate. A cache with a poor hit rate adds a network hop and memory cost while most requests still hit the database — you must measure hits/(hits+misses) to know the cache is actually working."
    },
    {
      "text": "A cache trades memory for latency by keeping hot data on a fast tier, but \"add a cache\" is three real decisions. The WRITE STRATEGY governs freshness and speed: CACHE-ASIDE (the app reads through — miss → load from DB → populate) is the default, caching only what is actually requested and tolerating a brief staleness window; WRITE-THROUGH writes to cache and DB together so the cache is never stale, paying extra write latency; WRITE-BACK writes to the cache and flushes to the DB asynchronously for the fastest writes, risking loss of un-flushed data on a crash. Because cache memory is finite, an EVICTION policy decides what to keep when full. LRU — least-recently-used — evicts the entry untouched for the longest, which works because access patterns are usually temporally local: what you read recently you tend to read again. A TTL adds a time bound so an entry expires regardless of use, capping how stale a served value can be. Above all, a cache is judged by its HIT RATE = hits / (hits + misses): a high hit rate means most reads skip the database and latency drops; a low hit rate means you added a hop and some memory for almost nothing. The engineering loop is: pick the write strategy for your freshness needs, pick an eviction policy for your access pattern, set a TTL for your staleness tolerance, then MEASURE the hit rate and tune until the cache earns its cost.",
      "type": "concept",
      "title": "A cache is a strategy plus an eviction policy, judged by hit rate"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "cache",
          "from": "app",
          "kind": "data",
          "tone": "accent",
          "label": "get(key)"
        },
        {
          "to": "hit",
          "from": "cache",
          "kind": "sync",
          "tone": "success",
          "label": "present"
        },
        {
          "to": "db",
          "from": "cache",
          "kind": "data",
          "label": "miss → load"
        },
        {
          "to": "evict",
          "from": "db",
          "kind": "control",
          "tone": "warning",
          "label": "populate (full?)"
        },
        {
          "to": "rate",
          "from": "cache",
          "kind": "data",
          "tone": "warning",
          "label": "hits/misses"
        }
      ],
      "nodes": [
        {
          "id": "app",
          "kind": "service",
          "tone": "accent",
          "label": "app"
        },
        {
          "id": "cache",
          "kind": "store",
          "tone": "success",
          "label": "LRU cache",
          "description": "bounded, recency-ordered"
        },
        {
          "id": "hit",
          "kind": "process",
          "tone": "success",
          "label": "hit → return"
        },
        {
          "id": "db",
          "kind": "store",
          "tone": "default",
          "label": "database",
          "description": "source of truth"
        },
        {
          "id": "evict",
          "kind": "decision",
          "tone": "warning",
          "label": "evict LRU entry",
          "description": "at capacity"
        },
        {
          "id": "rate",
          "kind": "external",
          "tone": "warning",
          "label": "hit rate metric"
        }
      ],
      "title": "Cache-aside read path with LRU eviction",
      "rankdir": "LR",
      "subtitle": "hit serves fast; miss loads and may evict",
      "storyboard": [
        {
          "ms": 7450,
          "say": "Every read starts at the app, which asks the LRU cache for the key first — the database is never the first stop in cache-aside.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/caching-strategies__b0.mp3",
          "edges": [
            [
              "app",
              "cache"
            ]
          ],
          "nodes": [
            "app",
            "cache"
          ]
        },
        {
          "ms": 7000,
          "say": "If the key is present, that's a hit: the cache returns immediately from memory and the slow database is skipped entirely.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/caching-strategies__b1.mp3",
          "edges": [
            [
              "cache",
              "hit"
            ]
          ],
          "nodes": [
            "cache",
            "hit"
          ]
        },
        {
          "ms": 6475,
          "say": "On a miss you pay twice: the cache lookup was wasted, and now the app must load the value from the database anyway.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/caching-strategies__b2.mp3",
          "edges": [
            [
              "cache",
              "db"
            ]
          ],
          "nodes": [
            "cache",
            "db"
          ]
        },
        {
          "ms": 8050,
          "say": "The loaded value populates the cache, but memory is finite — if the cache is full, LRU evicts the entry untouched the longest to make room.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/caching-strategies__b3.mp3",
          "edges": [
            [
              "db",
              "evict"
            ]
          ],
          "nodes": [
            "db",
            "evict"
          ]
        },
        {
          "ms": 7175,
          "say": "The cache reports hits and misses as a metric — hit rate is the number that tells you whether the cache is actually earning its keep.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/caching-strategies__b4.mp3",
          "edges": [
            [
              "cache",
              "rate"
            ]
          ],
          "nodes": [
            "cache",
            "rate"
          ]
        },
        {
          "ms": 8250,
          "say": "At a 20 percent hit rate, most reads pay cache plus database — a cache only speeds you up when hot data stays resident and the hit rate is high.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/caching-strategies__b5.mp3",
          "edges": [
            [
              "cache",
              "rate"
            ]
          ],
          "nodes": [
            "rate",
            "evict",
            "cache"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "cap 2 hitRate 0.00\ncap 3 hitRate 0.67",
      "title": "Implement an LRU cache and measure hit rate",
      "starter": "class LruCache {\n  constructor(cap) { /* map, hits, misses */ }\n  get(key) { /* touch on hit; count hit or miss */ }\n  put(key, val) { /* evict least-recently-used at capacity */ }\n  hitRate() { /* hits over total */ }\n}\nfunction runTrace(cap, trace) {\n  const c = new LruCache(cap);\n  for (const k of trace) { if (c.get(k) === undefined) c.put(k, k); }\n  return c.hitRate();\n}\nconst trace = [\"A\", \"B\", \"C\", \"A\", \"B\", \"C\", \"A\", \"B\", \"C\"];\nfor (const cap of [2, 3]) console.log(\"cap\", cap, \"hitRate\", runTrace(cap, trace).toFixed(2));",
      "summary": "Capacity sensitivity: run the SAME cyclic access trace through an LRU cache at capacity 2 and at capacity 3 and print each hit rate, revealing how a cache smaller than the working set thrashes to zero while one that just fits jumps.",
      "language": "js"
    },
    {
      "fix": "get(key) {\n  if (!this.map.has(key)) { this.misses++; return undefined; }\n  const val = this.map.get(key);\n  this.map.delete(key); this.map.set(key, val); // move to most-recently-used\n  this.hits++; return val;\n}",
      "task": "The get does not update recency, so frequently-read keys look \"old\" and get evicted. Fix the LRU touch.",
      "type": "debug",
      "symptom": "A cache evicts entries but the \"hot\" keys keep getting thrown out, so the hit rate stays low even though those keys are read constantly.",
      "language": "ts",
      "brokenCode": "get(key) {\n  if (!this.map.has(key)) return undefined;\n  return this.map.get(key);   // reads but never marks the key as recently used\n}"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Write to cache and DB synchronously. The cache is always consistent with the store and nothing is lost on a crash — at the cost of higher write latency, since every write waits on the DB.",
        "label": "Write-through"
      },
      "optionB": {
        "text": "Write to the cache and flush to the DB later. Fastest writes and absorbs write bursts, but a crash before the flush loses the un-persisted writes — unacceptable for durable data.",
        "label": "Write-back"
      },
      "guidance": "Use write-through (A) when writes must be durable and consistent — you accept the latency for safety. Use write-back (B) only for loss-tolerant, write-heavy data (counters, metrics) where throughput matters more than never losing the last few writes. Match the strategy to whether a crash can lose data you care about.",
      "question": "Writes must be durable but you also want a fast cache. Write-through or write-back?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "The database CPU on its own — if the DB still looks busy the cache must simply need a larger memory allocation.",
        "The cache hit rate, hits / (hits + misses) — how often reads are actually served by the cache instead of the DB.",
        "The number of keys stored; the more distinct keys the cache holds, the more requests it is necessarily serving.",
        "The total memory the cache consumes — if it is using a lot of RAM it must be holding enough hot data to help most reads."
      ],
      "question": "A newly added cache barely improves latency. What single metric tells you whether it is doing its job?",
      "explanation": "A cache only helps when reads actually hit it. The hit rate = hits/(hits+misses) directly measures how often the cache spares the database; a low hit rate means most requests still pay the DB read plus the extra cache hop. Memory and key count do not tell you whether the cache is earning its cost — the hit rate does."
    },
    {
      "type": "verification",
      "intro": "Your caching is sound when:",
      "items": [
        "You chose a write strategy (aside/through/back) for your freshness and durability needs.",
        "Your eviction policy fits the access pattern (LRU for temporal locality).",
        "You set a TTL that bounds acceptable staleness.",
        "You measure the hit rate and it justifies the cache."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain how LRU decides what to evict and why recency is a good predictor.",
        "Explain why hit rate, not memory size, tells you if a cache is working."
      ]
    },
    {
      "text": "This LRU and hit-rate machinery reappears in CDN edge caching (next module) and is exactly what the capstone measures to prove its read path is fast. The invalidation lesson (next) tackles what happens when the cached value goes stale.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Working LRU with correct eviction",
        "Hit rate computed over a trace",
        "Write strategy chosen by durability/freshness"
      ]
    }
  ],
  "cache-invalidation": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A single-flight guard that, when a hot key expires, lets exactly one request recompute while the rest wait for and share that one result.",
      "unlock": "You can prevent a thundering herd from hammering the origin when a popular cached key expires.",
      "outcome": "Handle the hard part of caching: staleness after a write, and the cache stampede (thundering herd) when a hot key expires — using a single-flight guard so only one request recomputes.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a TTL is enough — when a hot key expires, hundreds of concurrent misses can stampede the database unless one request fills the cache for all."
    },
    {
      "text": "A wildly popular item is cached with a 60-second TTL. At the tick it expires, and in that instant 500 concurrent requests all MISS, all hit the database at once, and the origin buckles — a cache STAMPEDE (thundering herd). The cache did its job 99.9% of the time and then caused an outage on expiry. This lesson builds the single-flight guard that lets exactly one request refill the cache while the rest wait for its result.",
      "type": "mission"
    },
    {
      "text": "Two problems make invalidation \"one of the two hard things in computer science.\" STALENESS: after a write, the cached copy is out of date until it is invalidated or its TTL lapses — you must decide how fresh reads must be and invalidate or update the cache on writes accordingly. The STAMPEDE (thundering herd / dogpile): when a HOT key expires, every concurrent request misses simultaneously and all recompute against the origin at once, converting one expiry into a flood. The classic fix is SINGLE-FLIGHT (request coalescing): the first request to miss acquires a lock and recomputes; concurrent requests for the same key do NOT also recompute — they wait on that in-flight computation and share its result. Complementary tactics include serving the stale value while one request refreshes in the background, and jittering TTLs so many keys do not expire at the same instant.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: a hot key with 500 concurrent readers expires. Without any guard, how many database recomputations happen? With single-flight, how many?",
      "reveal": "Without a guard: all 500 requests miss at once and all 500 recompute against the database — a stampede that can take the origin down. With single-flight: exactly ONE request recomputes; the other 499 detect an in-flight computation for that key, wait for it, and receive the same freshly-cached result. One recomputation instead of 500 — the origin sees a single load regardless of how many readers arrive at the expiry instant."
    },
    {
      "code": "// Coalesce concurrent recomputations of the same key into one.\nclass SingleFlight {\n  constructor() { this.inflight = new Map(); this.computeCount = 0; }\n  async do(key, compute) {\n    if (this.inflight.has(key)) return this.inflight.get(key);  // join the in-flight one\n    const p = (async () => { this.computeCount++; return await compute(); })();\n    this.inflight.set(key, p);\n    try { return await p; } finally { this.inflight.delete(key); }\n  }\n}",
      "type": "worked-example",
      "intro": "A single-flight guard: the first miss computes; concurrent misses await the same in-flight promise.",
      "steps": [
        "On a miss, check whether a computation for this key is already in flight.",
        "If yes, return that same promise — do NOT start another computation.",
        "If no, start the computation, record it as in-flight, and let others join it.",
        "When it resolves, remove the in-flight entry; all joiners share the one result."
      ],
      "language": "ts",
      "commonMistake": "Relying on TTL alone for a hot key. The instant it expires, every concurrent reader misses and stampedes the origin — a self-inflicted outage. You need request coalescing (single-flight), stale-while-revalidate, or TTL jitter so one expiry does not become hundreds of simultaneous recomputations."
    },
    {
      "text": "Cache invalidation is hard because a cache is a second copy of the truth, and copies drift. The first problem is STALENESS: after the underlying data changes, the cached value is wrong until you invalidate it (delete/refresh on write) or its TTL expires — so you must decide, per data, how stale a read may be and wire invalidation into the write path accordingly. The second, sharper problem is the CACHE STAMPEDE, also called the thundering herd or dogpile: for a HOT key with many concurrent readers, the moment the entry expires ALL of those readers miss at the same instant and ALL recompute against the origin simultaneously, so a routine expiry becomes a synchronized flood that can overwhelm the database — the cache protected you 99.9% of the time and then caused the outage. The primary defense is SINGLE-FLIGHT (a.k.a. request coalescing): when a key is missing, the first request to arrive starts the recomputation and registers it as \"in flight\"; every other concurrent request for the same key detects that in-flight work and WAITS on it instead of launching its own, so the origin sees exactly one load no matter how many readers pile up. Two complements harden this further: STALE-WHILE-REVALIDATE serves the last known (slightly stale) value immediately while a single background request refreshes it, so no reader blocks; and TTL JITTER spreads expiries randomly so a batch of keys populated together does not all expire on the same tick. Together they ensure that a popular key's expiry costs one recomputation, not a herd.",
      "type": "concept",
      "title": "Single-flight turns a stampede into one recomputation"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "guard",
          "from": "herd",
          "kind": "sync",
          "tone": "warning",
          "label": "all miss"
        },
        {
          "to": "leader",
          "from": "guard",
          "kind": "control",
          "tone": "accent",
          "label": "first only"
        },
        {
          "to": "waiters",
          "from": "guard",
          "kind": "control",
          "tone": "success",
          "label": "others wait"
        },
        {
          "to": "origin",
          "from": "leader",
          "kind": "data",
          "tone": "success",
          "label": "recompute (once)"
        },
        {
          "to": "cache",
          "from": "leader",
          "kind": "data",
          "tone": "success",
          "label": "refill"
        },
        {
          "to": "waiters",
          "from": "cache",
          "kind": "data",
          "tone": "success",
          "label": "shared result"
        }
      ],
      "nodes": [
        {
          "id": "herd",
          "kind": "client",
          "tone": "warning",
          "label": "concurrent readers",
          "description": "500 miss at expiry"
        },
        {
          "id": "guard",
          "kind": "decision",
          "tone": "accent",
          "label": "single-flight guard",
          "description": "in-flight for key?"
        },
        {
          "id": "leader",
          "kind": "process",
          "tone": "accent",
          "label": "one recompute"
        },
        {
          "id": "origin",
          "kind": "store",
          "tone": "success",
          "label": "database / origin",
          "description": "sees ONE load"
        },
        {
          "id": "waiters",
          "kind": "process",
          "tone": "success",
          "label": "waiters share result"
        },
        {
          "id": "cache",
          "kind": "store",
          "tone": "success",
          "label": "cache refilled"
        }
      ],
      "title": "Single-flight against a stampede",
      "rankdir": "LR",
      "subtitle": "one request recomputes; the herd waits and shares",
      "storyboard": [
        {
          "ms": 7575,
          "say": "A hot key just expired, so 500 concurrent readers all miss the cache in the same instant — this is the makings of a stampede.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cache-invalidation__b0.mp3",
          "edges": [],
          "nodes": [
            "herd"
          ]
        },
        {
          "ms": 7325,
          "say": "Without protection, every one of those misses would hit the database at once; instead, they all funnel into a single-flight guard.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cache-invalidation__b1.mp3",
          "edges": [
            [
              "herd",
              "guard"
            ]
          ],
          "nodes": [
            "herd",
            "guard"
          ]
        },
        {
          "ms": 6750,
          "say": "The guard makes the key decision: only the first request through becomes the leader and is allowed to recompute the value.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cache-invalidation__b2.mp3",
          "edges": [
            [
              "guard",
              "leader"
            ]
          ],
          "nodes": [
            "guard",
            "leader"
          ]
        },
        {
          "ms": 7600,
          "say": "Everyone else is parked — the other 499 requests simply wait on the leader instead of stampeding the origin themselves.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cache-invalidation__b3.mp3",
          "edges": [
            [
              "guard",
              "waiters"
            ]
          ],
          "nodes": [
            "guard",
            "waiters"
          ]
        },
        {
          "ms": 8200,
          "say": "The leader queries the database exactly once, so the origin sees a single recompute instead of a synchronized flood of 500.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cache-invalidation__b4.mp3",
          "edges": [
            [
              "leader",
              "origin"
            ]
          ],
          "nodes": [
            "leader",
            "origin"
          ]
        },
        {
          "ms": 6350,
          "say": "With the fresh value in hand, the leader refills the cache, and every waiter is handed that same shared result.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cache-invalidation__b5.mp3",
          "edges": [
            [
              "leader",
              "cache"
            ],
            [
              "cache",
              "waiters"
            ]
          ],
          "nodes": [
            "leader",
            "cache",
            "waiters"
          ]
        },
        {
          "ms": 7350,
          "say": "That's the core insight: collapse N identical misses into one recompute — the herd waits briefly, the origin does the work once.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cache-invalidation__b6.mp3",
          "edges": [
            [
              "guard",
              "leader"
            ],
            [
              "leader",
              "origin"
            ]
          ],
          "nodes": [
            "guard",
            "leader",
            "origin",
            "waiters"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "naiveComputes: 6\ncoalescedComputes: 1\nbothCorrect: true",
      "title": "Build the single-flight stampede guard",
      "starter": "class Coalescer {\n  constructor() { /* inflight map, computes counter */ }\n  async run(key, compute) { /* first caller computes; concurrent callers join it */ }\n}\nasync function recompute() { await new Promise(r => setTimeout(r, 5)); return \"v1\"; }\nlet naiveComputes = 0;\nasync function naiveRun() { naiveComputes++; return recompute(); }\nconst naive = await Promise.all(Array.from({ length: 6 }, () => naiveRun()));\nconst co = new Coalescer();\nconst coalesced = await Promise.all(Array.from({ length: 6 }, () => co.run(\"hot\", recompute)));\nconsole.log(\"naiveComputes:\", naiveComputes);\nconsole.log(\"coalescedComputes:\", co.computes);\nconsole.log(\"bothCorrect:\", naive.every(v => v === \"v1\") && coalesced.every(v => v === \"v1\"));",
      "summary": "Quantify the stampede: fire a burst of 6 concurrent requests for the same expired key through a NAIVE cache (every caller recomputes) and through a coalescing guard (one recompute, joiners share it), and print both compute counts to prove the guard collapses the herd.",
      "language": "js"
    },
    {
      "fix": "let v = cache.get(key);\nif (v === undefined) {\n  v = await singleFlight.do(key, async () => {   // one recompute for all\n    const fresh = await db.query(expensive);\n    cache.set(key, fresh, 60);\n    return fresh;\n  });\n}\nreturn v;",
      "task": "When the 60s TTL lapses, all concurrent misses run the expensive query at once. Add single-flight so only one does.",
      "type": "debug",
      "symptom": "A dashboard's most popular query is cached, yet every minute the database briefly spikes to 100% CPU at the same second.",
      "language": "ts",
      "brokenCode": "let v = cache.get(key);\nif (v === undefined) { v = await db.query(expensive); cache.set(key, v, 60); }\nreturn v;   // every concurrent miss runs db.query"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Return the last known value immediately and refresh in the background with a single request. No reader blocks; the tradeoff is serving briefly stale data during the refresh window.",
        "label": "Stale-while-revalidate"
      },
      "optionB": {
        "text": "Make readers wait for the recomputation (coalesced via single-flight). Everyone gets fresh data, but they all pause for the recompute latency at the expiry moment.",
        "label": "Block until fresh"
      },
      "guidance": "Prefer stale-while-revalidate (A) when a few seconds of staleness is acceptable — it keeps latency flat through expiries and, combined with single-flight, still hits the origin only once. Block-until-fresh (B) is right only when the data must never be served stale (prices at checkout, permissions). Choose by how much staleness the read can tolerate.",
      "question": "A hot key just expired and a refresh is needed. Block all readers until it recomputes, or serve the stale value while one request refreshes?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "It lets all 300 recompute in parallel so the fresh value is ready as fast as possible for every waiting reader.",
        "It permits exactly one recomputation and has the other 299 wait for and share that single freshly-cached result.",
        "It returns an error to 299 of the requests, shedding the load so that only one caller is served during the miss.",
        "It extends the key’s TTL on the fly so the entry never actually expires and no recomputation is ever triggered."
      ],
      "question": "A popular cached key expires and 300 requests miss simultaneously. What does a single-flight (request-coalescing) guard do?",
      "explanation": "Single-flight coalesces concurrent misses for the same key: the first request recomputes while the rest detect the in-flight work and await it, so the origin sees ONE load instead of 300. This converts a stampede into a single recomputation, which is the whole point of the guard."
    },
    {
      "type": "verification",
      "intro": "You control invalidation when:",
      "items": [
        "Writes invalidate or refresh the cached copy, per your freshness needs.",
        "A hot key's expiry triggers exactly one recomputation (single-flight).",
        "You use stale-while-revalidate or TTL jitter where staleness is tolerable.",
        "You can show the origin sees one load, not a herd, at expiry."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain what a cache stampede is and why TTL alone does not prevent it.",
        "Explain how single-flight makes a hot key's expiry cost one recomputation."
      ]
    },
    {
      "text": "Single-flight is the same coalescing idea you will see as idempotent dedup in the async-queues lesson and as the half-open probe in the circuit breaker — one guarded action protecting a shared resource. The capstone's cache must survive a hot-key expiry.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Writes invalidate/refresh the cache",
        "Single-flight yields one recompute under concurrency",
        "Stampede reduced to a single origin load"
      ]
    }
  ],
  "load-balancing": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A load balancer that implements round-robin and least-connections and reports the resulting request distribution across healthy backends.",
      "unlock": "You can pick a balancing algorithm for a workload and explain how health checks keep traffic off dead backends.",
      "outcome": "Distribute requests across backends: understand L4 vs L7, the round-robin / least-connections / consistent-hash algorithms, and health checks that route around failed backends.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a load balancer just \"spreads traffic\" — the algorithm and health checks decide whether load is even and whether a dead backend still gets requests."
    },
    {
      "text": "A backend crashes and the load balancer keeps sending it one request in three, so a third of users get errors — because there were no health checks. Meanwhile a naive round-robin sends equal requests to backends with wildly different load, and the slow ones pile up. A load balancer is only as good as its ALGORITHM and its HEALTH CHECKS. This lesson implements the algorithms and proves the distribution they produce.",
      "type": "mission"
    },
    {
      "text": "A load balancer (LB) sits in front of a pool of identical backends and distributes requests so no single one is overwhelmed, while hiding failures. It operates at LAYER 4 (transport: routes by IP/port, fast, protocol-agnostic) or LAYER 7 (application: reads HTTP, can route by path/header/cookie, richer but heavier). The ALGORITHM decides who gets each request: ROUND-ROBIN cycles evenly through backends (simple, ignores actual load); LEAST-CONNECTIONS sends the next request to the backend with the fewest in-flight requests (adapts to uneven request durations); CONSISTENT-HASH routes by a key so the same client/session sticks to the same backend (useful for cache locality). HEALTH CHECKS are non-negotiable: the LB periodically probes each backend and removes unhealthy ones from rotation, so a crashed backend stops receiving traffic instead of erroring one request in N.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: with round-robin over 3 backends, one backend handles requests that take 10× longer than the others. What goes wrong, and which algorithm fixes it?",
      "reveal": "Round-robin gives each backend an equal SHARE of requests regardless of how long each takes, so the backend stuck with slow requests accumulates a growing backlog while the fast ones idle between turns — uneven actual load despite even request counts. LEAST-CONNECTIONS fixes it: it routes each new request to the backend with the fewest in-flight requests, so the slow backend (holding many open connections) stops receiving new work until it drains. It balances load, not just request count."
    },
    {
      "code": "class LoadBalancer {\n  constructor(backends) { this.backends = backends.map(id => ({ id, inflight: 0, healthy: true })); this.rr = 0; }\n  healthy() { return this.backends.filter(b => b.healthy); }\n  roundRobin() {\n    const pool = this.healthy();\n    const b = pool[this.rr % pool.length]; this.rr++;\n    return b;\n  }\n  leastConnections() {\n    return this.healthy().reduce((best, b) => b.inflight < best.inflight ? b : best);\n  }\n}",
      "type": "worked-example",
      "intro": "Round-robin (cycle a pointer) and least-connections (pick the min in-flight) over healthy backends.",
      "steps": [
        "Round-robin advances a pointer over the healthy pool, giving each an equal turn.",
        "Least-connections scans the healthy pool and picks the backend with the fewest in-flight requests.",
        "Both operate only over HEALTHY backends — unhealthy ones are excluded from selection.",
        "Health checks flip a backend's healthy flag so failures leave rotation automatically."
      ],
      "language": "ts",
      "commonMistake": "Running a load balancer with no health checks. When a backend crashes, round-robin keeps handing it its share of requests, so 1/N of all traffic errors until a human intervenes. The LB must probe backends and pull unhealthy ones from rotation automatically."
    },
    {
      "text": "A load balancer fronts a pool of interchangeable backends and has two jobs: spread work evenly and hide failures. It works at one of two layers. LAYER 4 balances at the transport level (IP/port), forwarding packets without understanding the payload — fast and protocol-agnostic. LAYER 7 balances at the application level, parsing HTTP so it can route by path, header, or cookie and terminate TLS — more capable, more expensive per request. The ALGORITHM chooses the target. ROUND-ROBIN cycles through backends in order, giving each an equal COUNT of requests; it is simple but blind to how much work each request is, so it imbalances when request durations vary. LEAST-CONNECTIONS routes each request to the backend currently handling the fewest in-flight requests, which tracks actual LOAD and self-corrects when some requests run long. CONSISTENT-HASH routes by a request key (client IP, session id) so a given client always lands on the same backend — valuable for session or cache locality — and, being consistent hashing, it barely reshuffles when the backend set changes. Underpinning all of them are HEALTH CHECKS: the LB continuously probes each backend (a TCP connect or an HTTP endpoint) and, on failure, marks it unhealthy and removes it from the selectable pool, so a crashed backend stops receiving traffic instead of erroring one request in N. Without health checks, no algorithm can save you — the LB will faithfully route to a dead node. With them, the pool self-heals: failures leave rotation, recoveries rejoin.",
      "type": "concept",
      "title": "The algorithm balances load; health checks route around death"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "lb",
          "from": "client",
          "kind": "sync",
          "tone": "accent",
          "label": "incoming"
        },
        {
          "to": "b1",
          "from": "lb",
          "kind": "sync",
          "tone": "success",
          "label": "route"
        },
        {
          "to": "b2",
          "from": "lb",
          "kind": "sync",
          "tone": "success",
          "label": "route"
        },
        {
          "to": "b3",
          "from": "health",
          "kind": "control",
          "tone": "warning",
          "label": "probe fails"
        },
        {
          "to": "lb",
          "from": "health",
          "kind": "control",
          "tone": "warning",
          "label": "remove from pool"
        }
      ],
      "nodes": [
        {
          "id": "client",
          "kind": "client",
          "tone": "accent",
          "label": "requests"
        },
        {
          "id": "lb",
          "kind": "service",
          "tone": "accent",
          "label": "load balancer",
          "description": "L4/L7 + algorithm"
        },
        {
          "id": "b1",
          "kind": "service",
          "tone": "success",
          "label": "backend 1"
        },
        {
          "id": "b2",
          "kind": "service",
          "tone": "success",
          "label": "backend 2"
        },
        {
          "id": "b3",
          "kind": "service",
          "tone": "warning",
          "label": "backend 3 (dead)",
          "description": "failed health check"
        },
        {
          "id": "health",
          "kind": "decision",
          "tone": "warning",
          "label": "health checks",
          "description": "probe → mark unhealthy"
        }
      ],
      "title": "Load balancer over a healthy pool",
      "rankdir": "LR",
      "subtitle": "algorithm selects; health checks gate the pool",
      "storyboard": [
        {
          "ms": 6775,
          "say": "Every request starts at the client and hits one address: the load balancer, which fronts a pool of identical backends.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/load-balancing__b0.mp3",
          "edges": [
            [
              "client",
              "lb"
            ]
          ],
          "nodes": [
            "client",
            "lb"
          ]
        },
        {
          "ms": 7800,
          "say": "The load balancer's first job is spreading work: an algorithm like round-robin or least-connections picks which backend gets each request.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/load-balancing__b1.mp3",
          "edges": [
            [
              "lb",
              "b1"
            ],
            [
              "lb",
              "b2"
            ]
          ],
          "nodes": [
            "lb",
            "b1",
            "b2"
          ]
        },
        {
          "ms": 7000,
          "say": "Round-robin gives each backend an equal count of requests, but it is blind to load, so a slow backend quietly piles up work.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/load-balancing__b2.mp3",
          "edges": [
            [
              "lb",
              "b1"
            ],
            [
              "lb",
              "b2"
            ]
          ],
          "nodes": [
            "b1",
            "b2"
          ]
        },
        {
          "ms": 7600,
          "say": "Now backend three crashes; without a check, the balancer would keep sending it one request in three and a third of users would see errors.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/load-balancing__b3.mp3",
          "edges": [],
          "nodes": [
            "b3"
          ]
        },
        {
          "ms": 6375,
          "say": "That is the second job, hiding failures: health checks probe every backend, and backend three stops answering.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/load-balancing__b4.mp3",
          "edges": [
            [
              "health",
              "b3"
            ]
          ],
          "nodes": [
            "health",
            "b3"
          ]
        },
        {
          "ms": 8150,
          "say": "After repeated failed probes, the health checker tells the balancer to pull backend three from the pool, so traffic only flows to healthy nodes.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/load-balancing__b5.mp3",
          "edges": [
            [
              "health",
              "lb"
            ],
            [
              "lb",
              "b1"
            ],
            [
              "lb",
              "b2"
            ]
          ],
          "nodes": [
            "health",
            "lb",
            "b1",
            "b2"
          ]
        },
        {
          "ms": 7850,
          "say": "The insight: a load balancer is only as good as its health checks; the algorithm spreads load, but the gated pool is what hides failure.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/load-balancing__b6.mp3",
          "edges": [
            [
              "health",
              "lb"
            ]
          ],
          "nodes": [
            "lb",
            "health"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "weighted: {\"big\":6,\"small\":2,\"down\":0}",
      "title": "Implement round-robin and least-connections",
      "starter": "// Smooth weighted round-robin over healthy backends.\nclass WeightedLB {\n  constructor(backends) { /* {id, weight, healthy, current} */ }\n  healthyPool() { /* only healthy backends */ }\n  next() { /* pick by weighted current-value, decrement by total */ }\n}\nconst lb = new WeightedLB([\n  { id: \"big\", weight: 3 },\n  { id: \"small\", weight: 1 },\n  { id: \"down\", weight: 5, healthy: false },\n]);\nconst counts = { big: 0, small: 0, down: 0 };\nfor (let i = 0; i < 8; i++) counts[lb.next().id]++;\nconsole.log(\"weighted:\", JSON.stringify(counts));",
      "summary": "Weighted round-robin: implement smooth weighted selection over healthy backends (a \"big\" backend weight 3, a \"small\" weight 1, one down) and route 8 requests, printing the per-backend counts to show traffic splits in proportion to weight while the unhealthy one gets none.",
      "language": "js"
    },
    {
      "fix": "pickBackend() {\n  const pool = this.backends.filter(b => b.healthy); // health checks maintain this flag\n  const b = pool[this.rr % pool.length];\n  this.rr++; return b;\n}",
      "task": "Round-robin cycles over ALL backends including the crashed one, so 1/3 of traffic hits a dead node. Fix it to route only to healthy backends.",
      "type": "debug",
      "symptom": "Exactly one third of requests return 502 errors, steadily, and it started when one of three backends crashed.",
      "language": "ts",
      "brokenCode": "pickBackend() {\n  const b = this.backends[this.rr % this.backends.length]; // includes the dead one\n  this.rr++; return b;\n}"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Route to whichever backend has the fewest in-flight requests. It tracks real load, so a backend stuck on slow requests stops getting new ones until it drains — even load under variable durations.",
        "label": "Least-connections"
      },
      "optionB": {
        "text": "Give each backend an equal number of requests. Dead simple and stateless, but blind to duration: a backend saddled with long requests keeps getting its turn and its backlog grows.",
        "label": "Round-robin"
      },
      "guidance": "Use least-connections (A) when request durations are uneven — it balances actual concurrent load, not just counts. Round-robin (B) is fine when requests are uniform and cheap, where its statelessness is a virtue. Match the algorithm to how variable your request cost is.",
      "question": "Request durations vary a lot across your backends. Round-robin or least-connections?"
    },
    {
      "type": "quiz",
      "answer": 2,
      "options": [
        "Nothing prevents it; a load balancer cannot tell a backend has died, so 1/N of traffic errors until a human acts.",
        "Consistent hashing pins each client to a backend, and that pinning is what stops any single backend from failing.",
        "Health checks: the LB periodically probes each backend and removes any that fail from the selectable pool.",
        "Round-robin notices the slow responses from the dead node and automatically skips it in the rotation from then on."
      ],
      "question": "A backend crashes. What keeps the load balancer from continuing to send it 1/N of all requests?",
      "explanation": "The balancing algorithm has no idea a backend is dead; round-robin will keep including it. Health checks are what detect the failure — the LB periodically probes each backend and drops unhealthy ones from rotation, so a crashed node stops receiving traffic. Without health checks, 1/N of requests keep erroring."
    },
    {
      "type": "verification",
      "intro": "Your load balancing is sound when:",
      "items": [
        "You chose the algorithm (round-robin / least-conn / consistent-hash) for your workload.",
        "Only healthy backends are eligible for selection.",
        "Health checks add and remove backends automatically.",
        "You can show the resulting request distribution across the pool."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why least-connections beats round-robin when request durations vary.",
        "Explain what health checks do and why no algorithm works without them."
      ]
    },
    {
      "text": "Consistent-hash load balancing reuses the ring from the sharding lesson; the health-check idea is the same liveness signal the circuit breaker and failure-mode lessons build on. The capstone puts a load balancer in front of its stateless app tier.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Round-robin and least-connections implemented",
        "Only healthy backends selected",
        "Distribution across the pool measured"
      ]
    }
  ],
  "cdn-edge": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A cache-control decision function that classifies each response as edge-cacheable or not, plus an edge hit-ratio calculation over a request mix.",
      "unlock": "You can decide what belongs at the edge, set the right Cache-Control, and estimate the origin offload a CDN provides.",
      "outcome": "Push static and cacheable content to edge locations near users: understand Cache-Control (public/private, max-age, no-store), what is cacheable vs dynamic, and how edge hit ratio cuts origin load and latency.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a CDN speeds up \"everything\" — only cacheable content is served from the edge; per-user dynamic responses must not be edge-cached."
    },
    {
      "text": "A team puts their whole site behind a CDN and users start seeing OTHER users' account pages, because a personalized, private response got cached at the edge and served to everyone. A CDN is powerful but blunt: it caches whatever Cache-Control tells it to, so the skill is deciding what is safely cacheable and marking dynamic/private responses no-store. This lesson makes cacheability a computed decision and quantifies the origin offload.",
      "type": "mission"
    },
    {
      "text": "A CDN (content delivery network) is a fleet of edge caches spread geographically close to users. A request for cacheable content is served from the nearest edge — low latency, and the origin never sees it — while a miss is fetched from the origin and cached at the edge for the next user. What is cacheable is governed by the CACHE-CONTROL header: public (any cache may store it) vs private (only the user's browser); max-age (seconds it stays fresh); no-store (never cache — for sensitive or per-request data); no-cache (revalidate before use). STATIC assets (images, CSS, JS, videos) are ideal edge content: identical for everyone, long max-age. DYNAMIC, PERSONALIZED responses (a logged-in dashboard, a cart) must be private or no-store so one user's data is never served to another. The payoff is the EDGE HIT RATIO: the fraction of requests served from the edge, which directly offloads the origin and cuts latency.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: which of these should be edge-cached with a long max-age, and which must be no-store — a site logo PNG, and a logged-in user's account balance page?",
      "reveal": "The logo PNG: edge-cache it with a long max-age and public — it is identical for every user and changes rarely, so serving it from the edge is pure win. The account balance page: no-store (or at least private) — it is personalized and sensitive, so if the edge cached it, a different user could be served someone else's balance. The rule: cache what is shared and static; never edge-cache per-user or sensitive dynamic responses."
    },
    {
      "code": "// Classify a response for edge caching from its attributes.\nfunction cacheControl(res) {\n  if (res.personalized || res.sensitive) return { store: false, header: \"no-store\" };\n  if (res.static) return { store: true, header: \"public, max-age=86400\" };  // 1 day\n  return { store: true, header: \"public, max-age=60\" };                     // short for dynamic-but-shared\n}\n// Edge hit ratio over a request mix: cacheable + repeated → hit.\nfunction edgeHitRatio(requests) {\n  let hits = 0;\n  for (const r of requests) if (cacheControl(r).store && r.repeated) hits++;\n  return hits / requests.length;\n}",
      "type": "worked-example",
      "intro": "Decide cacheability from response attributes, then compute the edge hit ratio.",
      "steps": [
        "Personalized or sensitive responses → no-store; never let them reach an edge cache.",
        "Static, shared assets → public with a long max-age.",
        "Shared-but-changing content → public with a short max-age.",
        "Edge hit ratio = edge-served requests / total; it measures origin offload."
      ],
      "language": "ts",
      "commonMistake": "Edge-caching a personalized or authenticated response (missing private/no-store). The CDN serves one user's cached page to the next user requesting that URL — a data leak. Per-user and sensitive responses must be marked no-store (or private) so only static/shared content lives at the edge."
    },
    {
      "text": "A CDN moves cacheable content to edge nodes physically near users, so a hit is served in a few milliseconds from a nearby city instead of a cross-continent round trip to the origin — and, just as importantly, the origin never processes that request, so its load drops by the edge hit ratio. What may be cached is dictated entirely by the CACHE-CONTROL response header, which is a contract between origin and caches: PUBLIC means any shared cache (including the CDN edge) may store the response; PRIVATE restricts it to the end user's own browser; MAX-AGE=N sets how many seconds it stays fresh before revalidation; NO-STORE forbids caching anywhere (for sensitive or strictly per-request data); NO-CACHE allows storage but requires revalidation before reuse. The classification that matters: STATIC, identical-for-everyone assets — images, CSS, JS bundles, video segments — are ideal edge content and get long max-age and public, because caching them once serves millions. DYNAMIC, PERSONALIZED responses — a logged-in dashboard, a shopping cart, anything tied to a user or a session — must be private or no-store, because an edge cache keyed by URL would otherwise serve one user's data to the next, a real and common security bug. The metric that justifies the CDN is the EDGE HIT RATIO: the fraction of requests answered at the edge; a high ratio means most traffic never touches the origin, which is both the latency win and the scaling win. So the craft is deciding, per response, whether it is shared-and-static (push it to the edge with a long life) or per-user-dynamic (keep it off the edge entirely), and then measuring the hit ratio to confirm the offload.",
      "type": "concept",
      "title": "The edge caches only what is shared; Cache-Control is the contract"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "edge",
          "from": "user",
          "kind": "sync",
          "tone": "accent",
          "label": "request"
        },
        {
          "to": "decide",
          "from": "edge",
          "kind": "control",
          "tone": "warning",
          "label": "cacheable?"
        },
        {
          "to": "static",
          "from": "decide",
          "kind": "data",
          "tone": "success",
          "label": "public → cache"
        },
        {
          "to": "private",
          "from": "decide",
          "kind": "control",
          "tone": "warning",
          "label": "no-store"
        },
        {
          "to": "origin",
          "from": "edge",
          "kind": "data",
          "label": "miss → fetch"
        },
        {
          "to": "origin",
          "from": "private",
          "kind": "sync",
          "tone": "warning",
          "label": "always origin"
        }
      ],
      "nodes": [
        {
          "id": "user",
          "kind": "client",
          "tone": "accent",
          "label": "user (nearby)"
        },
        {
          "id": "edge",
          "kind": "store",
          "tone": "success",
          "label": "edge cache",
          "description": "serves cacheable content"
        },
        {
          "id": "decide",
          "kind": "decision",
          "tone": "warning",
          "label": "Cache-Control?",
          "description": "public/private/no-store"
        },
        {
          "id": "static",
          "kind": "store",
          "tone": "success",
          "label": "static asset",
          "description": "public, long max-age"
        },
        {
          "id": "origin",
          "kind": "service",
          "tone": "default",
          "label": "origin server",
          "description": "dynamic + misses"
        },
        {
          "id": "private",
          "kind": "external",
          "tone": "warning",
          "label": "personalized response",
          "description": "no-store → origin only"
        }
      ],
      "title": "Edge caching by Cache-Control",
      "rankdir": "LR",
      "subtitle": "static goes to the edge; dynamic/private goes to origin",
      "storyboard": [
        {
          "ms": 7700,
          "say": "A user's request never crosses the continent first — it lands at the nearest edge cache, a CDN node sitting in a city close to them.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cdn-edge__b0.mp3",
          "edges": [
            [
              "user",
              "edge"
            ]
          ],
          "nodes": [
            "user",
            "edge"
          ]
        },
        {
          "ms": 6850,
          "say": "The edge doesn't guess what's safe to keep — it reads the Cache-Control header, the contract the origin stamps on every response.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cdn-edge__b1.mp3",
          "edges": [
            [
              "edge",
              "decide"
            ]
          ],
          "nodes": [
            "edge",
            "decide"
          ]
        },
        {
          "ms": 8250,
          "say": "Static assets marked public get stored at the edge, so the next user in that city is served in milliseconds and the origin never hears about it.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cdn-edge__b2.mp3",
          "edges": [
            [
              "decide",
              "static"
            ]
          ],
          "nodes": [
            "decide",
            "static"
          ]
        },
        {
          "ms": 6626,
          "say": "On a miss, the edge fetches from the origin server once, caches the answer, and every later request for it stays local.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cdn-edge__b3.mp3",
          "edges": [
            [
              "edge",
              "origin"
            ]
          ],
          "nodes": [
            "edge",
            "origin"
          ]
        },
        {
          "ms": 6626,
          "say": "Personalized responses must say no-store — cache someone's account page at the edge and every user in that city sees it.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cdn-edge__b4.mp3",
          "edges": [
            [
              "decide",
              "private"
            ]
          ],
          "nodes": [
            "decide",
            "private"
          ]
        },
        {
          "ms": 8875,
          "say": "So private traffic always goes to origin, and the whole system rests on one header: Cache-Control decides what the edge may keep and what it must never touch.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cdn-edge__b5.mp3",
          "edges": [
            [
              "private",
              "origin"
            ]
          ],
          "nodes": [
            "private",
            "origin",
            "decide"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "logo.png: public, max-age=86400\napp.js: public, max-age=86400\ndashboard: no-store\npricing: public, max-age=60\nbyteOffload: 0.9718",
      "title": "Cache-control decision + edge hit ratio",
      "starter": "// Classify each response, then measure origin OFFLOAD BY BYTES:\n// every repeat fetch of a cacheable object is bytes the origin never serves.\nfunction classify(res) { /* no-store, long max-age, or short max-age */ }\nfunction byteOffload(requests) { /* origin bytes saved over total bytes requested */ }\nconst requests = [\n  { name: \"logo.png\", static: true, bytes: 20000, count: 100 },\n  { name: \"app.js\", static: true, bytes: 500000, count: 50 },\n  { name: \"dashboard\", personalized: true, bytes: 8000, count: 30 },\n  { name: \"pricing\", static: false, bytes: 12000, count: 10 },\n];\nfor (const r of requests) console.log(r.name + \":\", classify(r));\nconsole.log(\"byteOffload:\", byteOffload(requests).toFixed(4));",
      "summary": "Measure origin offload BY BYTES, not by request count: every repeat fetch of a cacheable object is bytes the origin never serves, so a large cached bundle offloads far more than a small logo. Classify each response, then compute the byte-weighted offload ratio and print it to 4 decimals.",
      "language": "js"
    },
    {
      "fix": "if (isPersonalized(res)) {\n  res.setHeader(\"Cache-Control\", \"private, no-store\"); // never edge-cache per-user data\n} else {\n  res.setHeader(\"Cache-Control\", \"public, max-age=300\"); // shared/static only\n}",
      "task": "A personalized response is marked public and cached at the edge, so it is served to other users. Fix the header for personalized content.",
      "type": "debug",
      "symptom": "After enabling the CDN, users occasionally load a page showing a DIFFERENT user's name and account details.",
      "language": "ts",
      "brokenCode": "res.setHeader(\"Cache-Control\", \"public, max-age=300\"); // set on ALL responses\nreturn renderUserDashboard(currentUser);                  // personalized!"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Cache at the edge for a minute or two so updates propagate quickly. Slightly more origin revalidation traffic, but users never see badly stale content and you still offload most requests.",
        "label": "Short max-age (+ revalidation)"
      },
      "optionB": {
        "text": "Cache for hours to maximize the hit ratio and minimize origin load. Best offload, but an update can take hours to appear unless you actively purge the edge.",
        "label": "Long max-age"
      },
      "guidance": "Use a short max-age (A) for shared content that changes intra-day — it keeps freshness acceptable while still offloading the origin, and pairs well with explicit purge-on-update. Reserve long max-age (B) for truly immutable assets (versioned/fingerprinted files), where staleness is impossible because the URL changes when the content does.",
      "question": "A page is shared across users but its content changes a few times a day. Long max-age at the edge, or short max-age?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Edge caching a dashboard would make the origin respond faster than the edge, which defeats the whole purpose of a CDN.",
        "The edge keys on URL, not user, so it serves one user’s cached page to the next visitor on that URL — a data leak.",
        "Personalized pages are too large for edge storage, so the CDN evicts them and reads fall back to the origin anyway.",
        "CDNs can only cache static images, never HTML, so a dynamic dashboard cannot be stored at the edge in the first place."
      ],
      "question": "Why must a logged-in user's personalized dashboard NOT be cached at a CDN edge with a public header?",
      "explanation": "Edge caches key on the URL, not the user. A public, cacheable personalized response gets stored once and served to everyone hitting that URL, exposing one user's data to others. Personalized or sensitive responses must be private/no-store so only shared, static content lives at the edge."
    },
    {
      "type": "verification",
      "intro": "Your edge strategy is sound when:",
      "items": [
        "Static shared assets are public with an appropriate max-age.",
        "Personalized and sensitive responses are private/no-store.",
        "You can compute the edge hit ratio and the origin offload it gives.",
        "No per-user data can ever be served from a shared cache."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain the difference between public, private, and no-store Cache-Control.",
        "Explain why the edge hit ratio measures the CDN's value."
      ]
    },
    {
      "text": "The edge hit ratio is the same offload logic as the cache hit rate from the caching lesson, applied geographically; the no-store rule for personalized data is the security discipline the safety and entitlement lessons reinforce. The capstone serves its static assets from the edge and its dynamic API from the origin.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Cacheable vs dynamic classified by Cache-Control",
        "Personalized responses marked no-store",
        "Edge hit ratio and origin offload computed"
      ]
    }
  ],
  "cap-and-consistency": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A CP-vs-AP decision function that, per data requirement, chooses whether to sacrifice availability (stay consistent) or serve stale (stay available) during a partition.",
      "unlock": "You can state, for a given piece of data, whether it must be CP or AP and defend it with the CAP theorem.",
      "outcome": "Apply the CAP theorem: under a network partition you must choose Consistency OR Availability, and pick strong vs eventual consistency from what the data actually requires (PACELC extends this to the no-partition case).",
      "intensity": "standard",
      "doNotClaim": "Don't claim a system is \"consistent AND available\" during a partition — CAP proves you get at most one of those two when the network splits."
    },
    {
      "text": "During a network blip between data centers, a bank keeps serving account balances from both sides and a customer double-spends — because the design chose availability over consistency for data that needed the opposite. CAP is not a slide; it is the law that when the network partitions, you must PICK: refuse the request to stay consistent, or answer with possibly-stale data to stay available. This lesson turns that choice into a per-data decision you can defend.",
      "type": "mission"
    },
    {
      "text": "The CAP theorem (Eric Brewer's 2000 conjecture, proven by Seth Gilbert and Nancy Lynch in 2002) states that a distributed data store cannot simultaneously guarantee all three of: CONSISTENCY (every read sees the most recent write), AVAILABILITY (every request gets a non-error response), and PARTITION TOLERANCE (it keeps working despite dropped messages between nodes). Since network partitions are a fact of life, partition tolerance is not optional — so the real choice under a partition is C vs A. A CP system refuses/ blocks requests it cannot serve consistently (banking, inventory); an AP system answers with possibly-stale data to stay up (feeds, likes, DNS). PACELC (Daniel Abadi, 2010) extends this: if Partitioned, choose A or C; Else (normal operation) choose Latency or C — because even without a partition, waiting for global consistency costs latency. CONSISTENCY MODELS name the spectrum: STRONG (linearizable — reads always see the latest write) vs EVENTUAL (replicas converge over time; reads may be stale).",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: during a partition, an inventory system for a limited-stock flash sale can either reject some purchases or risk overselling. Which does a correct design choose, and what is it giving up?",
      "reveal": "It chooses CONSISTENCY (CP): reject or block purchases it cannot confirm against a single authoritative count, giving up AVAILABILITY for the duration of the partition. Overselling limited stock is a correctness violation you cannot undo cheaply, so you would rather serve fewer purchases than sell inventory that does not exist. By CAP, during the partition you cannot have both — and for scarce inventory, consistency wins over uptime."
    },
    {
      "code": "// During a partition, pick the survivable behavior per data type.\nfunction capChoice(data) {\n  // Correctness-critical data must not diverge → sacrifice availability.\n  if (data.correctnessCritical) return { mode: \"CP\", partitionBehavior: \"reject/block to stay consistent\" };\n  // Availability-critical data tolerates staleness → keep serving.\n  return { mode: \"AP\", partitionBehavior: \"serve possibly-stale to stay available\" };\n}",
      "type": "worked-example",
      "intro": "Choose CP or AP per data requirement: correctness-critical → CP; availability-critical → AP.",
      "steps": [
        "Partition tolerance is mandatory in a distributed store — the real choice is C vs A.",
        "Correctness-critical data (money, inventory, uniqueness) → CP: refuse rather than diverge.",
        "Availability-critical data (feeds, counts, presence) → AP: serve stale rather than fail.",
        "PACELC reminds you that even without a partition, strong consistency costs latency."
      ],
      "language": "ts",
      "commonMistake": "Declaring a whole system \"consistent and highly available\" as if CAP did not apply. Under a partition you get at most one of C or A; the honest design states, per data type, which one it sacrifices — and different data in the same system can make different choices."
    },
    {
      "text": "The CAP theorem — conjectured by Brewer (2000) and formally proven by Gilbert and Lynch (2002) — says a distributed data store cannot at once guarantee CONSISTENCY (every read returns the most recent write), AVAILABILITY (every request receives a non-error response), and PARTITION TOLERANCE (it keeps operating despite lost or delayed messages between nodes). Because real networks DO partition, partition tolerance is not something you can opt out of; therefore, at the moment of a partition, the design must choose between C and A. A CP choice keeps data correct by refusing or blocking any request it cannot serve consistently — the right call for money, inventory, and uniqueness constraints, where a wrong answer is worse than no answer. An AP choice keeps serving, returning possibly-stale data and reconciling later — the right call for feeds, like-counts, presence, and DNS, where being up matters more than being perfectly fresh. Crucially this is a PER-DATA decision, not a per-system one: the same application can make account balances CP and the activity feed AP. PACELC (Abadi, 2010) completes the picture: if there is a Partition, trade A vs C; Else, in normal operation, trade Latency vs Consistency — because a strongly consistent read may need to coordinate across replicas, which costs time even when nothing is broken. The consistency-model spectrum names your options: STRONG (linearizable) consistency guarantees every read sees the latest acknowledged write, at higher latency and lower availability; EVENTUAL consistency lets replicas diverge briefly and converge, buying latency and availability at the cost of possible staleness. Naming, per data, where you sit on that spectrum is what \"designing for consistency\" actually means.",
      "type": "concept",
      "title": "Under a partition you pick C or A — per piece of data"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "choice",
          "from": "partition",
          "kind": "control",
          "tone": "warning",
          "label": "forces"
        },
        {
          "to": "cp",
          "from": "choice",
          "kind": "sync",
          "tone": "success",
          "label": "consistency"
        },
        {
          "to": "ap",
          "from": "choice",
          "kind": "sync",
          "tone": "accent",
          "label": "availability"
        },
        {
          "to": "cp",
          "from": "money",
          "kind": "data",
          "tone": "success",
          "label": "requires"
        },
        {
          "to": "ap",
          "from": "feed",
          "kind": "data",
          "tone": "accent",
          "label": "requires"
        }
      ],
      "nodes": [
        {
          "id": "partition",
          "kind": "external",
          "tone": "warning",
          "label": "network partition",
          "description": "nodes cannot talk"
        },
        {
          "id": "choice",
          "kind": "decision",
          "tone": "warning",
          "label": "C or A?",
          "description": "CAP: pick one"
        },
        {
          "id": "cp",
          "kind": "process",
          "tone": "success",
          "label": "CP: stay consistent",
          "description": "reject/block"
        },
        {
          "id": "ap",
          "kind": "process",
          "tone": "accent",
          "label": "AP: stay available",
          "description": "serve stale"
        },
        {
          "id": "money",
          "kind": "store",
          "tone": "success",
          "label": "money / inventory",
          "description": "correctness-critical"
        },
        {
          "id": "feed",
          "kind": "store",
          "tone": "accent",
          "label": "feed / likes",
          "description": "availability-critical"
        }
      ],
      "title": "CAP under a partition",
      "rankdir": "LR",
      "subtitle": "partition tolerance is fixed; choose C or A per data",
      "storyboard": [
        {
          "ms": 7550,
          "say": "It starts with a network partition: the link between data centers drops, and both sides keep running without seeing each other's writes.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cap-and-consistency__b0.mp3",
          "edges": [],
          "nodes": [
            "partition"
          ]
        },
        {
          "ms": 7226,
          "say": "You cannot opt out of partitions — real networks fail. So the partition forces a decision you must have designed in advance.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cap-and-consistency__b1.mp3",
          "edges": [
            [
              "partition",
              "choice"
            ]
          ],
          "nodes": [
            "partition",
            "choice"
          ]
        },
        {
          "ms": 7500,
          "say": "That decision is CAP's real content: while partitioned, each piece of data gets consistency or availability — never both.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cap-and-consistency__b2.mp3",
          "edges": [],
          "nodes": [
            "choice"
          ]
        },
        {
          "ms": 7850,
          "say": "Choose CP and the system stays consistent: it blocks or rejects any request it can't answer correctly, trading uptime for truth.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cap-and-consistency__b3.mp3",
          "edges": [
            [
              "choice",
              "cp"
            ]
          ],
          "nodes": [
            "choice",
            "cp"
          ]
        },
        {
          "ms": 7425,
          "say": "Choose AP and the system stays available: every request gets an answer, even a stale one, and conflicts are reconciled later.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cap-and-consistency__b4.mp3",
          "edges": [
            [
              "choice",
              "ap"
            ]
          ],
          "nodes": [
            "choice",
            "ap"
          ]
        },
        {
          "ms": 7725,
          "say": "Money and inventory require CP — a bank serving balances from both sides of a partition is how a customer double-spends.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cap-and-consistency__b5.mp3",
          "edges": [
            [
              "money",
              "cp"
            ]
          ],
          "nodes": [
            "money",
            "cp"
          ]
        },
        {
          "ms": 8350,
          "say": "Feeds and likes require AP — a slightly stale like count is fine, but downtime isn't. The insight: choose C or A per data, not per system.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/cap-and-consistency__b6.mp3",
          "edges": [
            [
              "feed",
              "ap"
            ]
          ],
          "nodes": [
            "feed",
            "ap"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "account-balance: PC/EC\ninventory-count: PC/EC\nactivity-feed: PA/EL\naudit-log: PA/EC",
      "title": "Build the CP-vs-AP decision function",
      "starter": "// PACELC has two axes per data type.\n// If Partitioned, choose PC or PA. Else (normal operation), choose EL or EC.\n// Return a string like \"PC/EC\".\nfunction pacelcChoice(data) { /* decide both axes */ }\nconst dataTypes = [\n  { name: \"account-balance\", correctnessCritical: true, latencySensitive: false },\n  { name: \"inventory-count\", correctnessCritical: true, latencySensitive: true },\n  { name: \"activity-feed\", correctnessCritical: false, latencySensitive: true },\n  { name: \"audit-log\", correctnessCritical: false, latencySensitive: false },\n];\nfor (const d of dataTypes) console.log(d.name + \":\", pacelcChoice(d));",
      "summary": "Extend the CP/AP choice to full PACELC: for each data type decide the partition axis (PC or PA) AND the else-latency axis (EL or EC), returning a string like \"PC/EC\". Run it over four data types and print each classification.",
      "language": "js"
    },
    {
      "fix": "async function withdraw(acct, amt) {\n  // CP: require a quorum/leader so both sides can't independently debit.\n  if (!await canReachQuorum(acct)) throw new Unavailable(\"withdrawals paused during partition\");\n  await quorumConditionalWrite(acct, amt); // reject rather than risk a double-spend\n}",
      "task": "Balances are treated as AP (serve/write from any replica during a partition), causing divergence. Make this correctness-critical path CP.",
      "type": "debug",
      "symptom": "A wallet service stayed fully available during a data-center partition, and afterward some balances were negative from double-spends.",
      "language": "ts",
      "brokenCode": "async function withdraw(acct, amt) {\n  const bal = await anyReplica.read(acct); // may be stale during a partition\n  if (bal >= amt) await anyReplica.write(acct, bal - amt); // both sides do this\n}"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Keep serving a possibly-stale like-count and reconcile later. The feature stays up during a partition; the only cost is a briefly inaccurate number that nobody is harmed by.",
        "label": "AP (eventual)"
      },
      "optionB": {
        "text": "Guarantee an exactly-correct like-count by coordinating across replicas, refusing to serve if it cannot. Correct to the like, but it makes a trivial feature unavailable during a partition.",
        "label": "CP (strong)"
      },
      "guidance": "Choose AP (A) for like-counts — the data is not correctness-critical, so availability and low latency beat exactness, and a stale count harms no one. Reserve CP (B) for data where a wrong value causes real damage (money, inventory, uniqueness). CAP is decided by the data's cost-of-being-wrong, not by a blanket preference.",
      "question": "A social app must decide the consistency of its like-count during a partition. CP (accurate, may reject) or AP (always shows a number, may be stale)?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Partition tolerance — you simply engineer the network so that partitions never occur and thus keep both C and A.",
        "Either consistency or availability — during a partition a store cannot keep both and must sacrifice one of them.",
        "Durability — a partition always erases un-replicated data, so the guarantee you give up is that writes survive.",
        "None of them — a well-architected system is proven to preserve all three of C, A, and P even during a partition."
      ],
      "question": "The CAP theorem says that when a network partition occurs, a distributed store must give up which guarantee?",
      "explanation": "Partitions are unavoidable, so partition tolerance is a given. Gilbert and Lynch proved that during a partition a store cannot provide both consistency (reads see the latest write) and availability (every request answered) — it must sacrifice one. Claiming all three during a partition contradicts the theorem."
    },
    {
      "type": "verification",
      "intro": "You apply CAP correctly when:",
      "items": [
        "You treat partition tolerance as mandatory, so the choice is C vs A.",
        "Each data type is labeled CP or AP with a reason.",
        "Correctness-critical data refuses rather than diverges under a partition.",
        "You can explain how PACELC adds a latency-vs-consistency choice even without a partition."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why \"consistent and available during a partition\" is impossible.",
        "Give one CP data example and one AP data example and justify each."
      ]
    },
    {
      "text": "CAP frames the choices the rest of this module implements: quorums (next-next) are how you tune the C/A dial, async queues embrace eventual (AP) consistency, and the fallacies lesson explains why partitions happen at all. The capstone must label its data CP or AP.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Partition tolerance treated as mandatory",
        "Each data type labeled CP or AP with justification",
        "Correctness-critical data kept CP"
      ]
    }
  ],
  "async-queues": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "An idempotent consumer with a dedup ledger that processes each message-id exactly once even when the same message is delivered multiple times.",
      "unlock": "You can build a consumer that is safe under at-least-once delivery and explain why exactly-once is usually idempotency plus dedup.",
      "outcome": "Decouple work with a message queue and handle its realities: at-least-once delivery means duplicates, so consumers must be IDEMPOTENT, and backpressure protects a slow consumer.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a queue delivers \"exactly once\" — most give at-least-once, so duplicates are normal and your consumer must dedup them."
    },
    {
      "text": "A payment worker consumes a \"charge card\" message, the network hiccups before it acknowledges, the queue redelivers the message, and the customer gets charged TWICE. Queues decouple producers from consumers, but the delivery guarantee is almost always AT-LEAST-ONCE, which means duplicates are not an edge case — they are guaranteed. This lesson builds the idempotent consumer with a dedup ledger that makes redelivery harmless.",
      "type": "mission"
    },
    {
      "text": "A message queue decouples a producer from a consumer: the producer enqueues work and returns immediately; a consumer processes it asynchronously, absorbing bursts and surviving consumer downtime. The delivery guarantee is the crux. AT-LEAST-ONCE (the common default) guarantees a message is delivered but may deliver it MORE than once — because acknowledgments can be lost, a consumer may process a message and crash before acking, so the queue redelivers it. AT-MOST-ONCE risks losing messages. True EXACTLY-ONCE processing is expensive and usually approximated as at-least-once delivery PLUS an idempotent consumer: the consumer records each processed message-id in a DEDUP LEDGER and skips any id it has already handled, so redelivery is a no-op. BACKPRESSURE handles a consumer that cannot keep up — the queue buffers, and the producer is slowed or shed rather than overwhelming the consumer. IDEMPOTENCY (processing the same message twice has the same effect as once) is the property that makes at-least-once safe.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: your queue guarantees at-least-once delivery and a \"send email\" message is delivered twice. What must the consumer do so the user does not get two emails?",
      "reveal": "The consumer must be IDEMPOTENT via a dedup ledger: before acting, it checks whether this message-id has already been processed; if so, it skips (the email was already sent) and just acknowledges. Only a never-seen id triggers the actual send, and the id is recorded so the inevitable redelivery is a no-op. At-least-once delivery makes duplicates certain, so exactly-once EFFECT comes from the consumer deduping, not from the queue."
    },
    {
      "code": "// Process each message-id at most once, even under redelivery.\nclass IdempotentConsumer {\n  constructor() { this.processed = new Set(); this.effects = 0; }   // ledger of handled ids\n  handle(msg) {\n    if (this.processed.has(msg.id)) return \"skipped-duplicate\";      // already done → no-op\n    // ... do the real side effect exactly once ...\n    this.effects++;\n    this.processed.add(msg.id);                                      // record before ack\n    return \"processed\";\n  }\n}",
      "type": "worked-example",
      "intro": "An idempotent consumer: check the dedup ledger by message-id before doing the work.",
      "steps": [
        "At-least-once delivery means the same message-id can arrive more than once.",
        "Before performing the side effect, check the dedup ledger for the message-id.",
        "If already present, skip — the effect already happened; just acknowledge.",
        "If new, do the effect exactly once and record the id so redelivery is a no-op."
      ],
      "language": "ts",
      "commonMistake": "Assuming the queue delivers exactly once and performing a side effect without deduping. Under at-least-once delivery a redelivered message runs the effect twice — a double charge, a duplicate email — so the consumer, not the queue, must guarantee exactly-once EFFECT via a dedup ledger."
    },
    {
      "text": "A message queue turns a synchronous, coupled call into an asynchronous, decoupled one: the producer enqueues and moves on, and a consumer processes the message later, which lets the system absorb bursts (the queue grows instead of failing) and survive consumer restarts (messages wait). The decisive property is the DELIVERY GUARANTEE. The overwhelmingly common one is AT-LEAST-ONCE: the queue will deliver a message, but may deliver it more than once, because the acknowledgment path is unreliable — a consumer can finish the work and then crash before its ack is recorded, so the queue, seeing no ack, redelivers. That makes DUPLICATES a certainty, not an anomaly. The disciplined response is not to wish for exactly-once delivery (which requires expensive coordination and still cannot cover external side effects) but to make the CONSUMER idempotent: give every message a stable id, keep a DEDUP LEDGER of processed ids, and, before performing the side effect, check the ledger — if the id is present, the work already happened, so skip and re-acknowledge; if it is new, perform the effect exactly once and record the id. This composition — at-least-once delivery plus an idempotent consumer — delivers exactly-once EFFECT, which is what you actually care about (one charge, one email), and it is robust precisely because it assumes redelivery will happen. The other reality queues manage is BACKPRESSURE: when the consumer cannot keep up, the queue buffers the backlog and the system slows or sheds producers rather than crushing the consumer — the queue acting as a shock absorber between mismatched rates.",
      "type": "concept",
      "title": "At-least-once delivery + idempotent consumer = exactly-once effect"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "queue",
          "from": "producer",
          "kind": "async",
          "tone": "accent",
          "label": "enqueue"
        },
        {
          "to": "consumer",
          "from": "queue",
          "kind": "async",
          "tone": "accent",
          "label": "deliver"
        },
        {
          "to": "consumer",
          "from": "dupe",
          "kind": "async",
          "tone": "warning",
          "label": "deliver again"
        },
        {
          "to": "ledger",
          "from": "consumer",
          "kind": "data",
          "tone": "success",
          "label": "seen id?"
        },
        {
          "to": "effect",
          "from": "ledger",
          "kind": "control",
          "tone": "success",
          "label": "new → do once"
        },
        {
          "to": "consumer",
          "from": "ledger",
          "kind": "control",
          "tone": "warning",
          "label": "dup → skip"
        }
      ],
      "nodes": [
        {
          "id": "producer",
          "kind": "service",
          "tone": "accent",
          "label": "producer"
        },
        {
          "id": "queue",
          "kind": "queue",
          "tone": "accent",
          "label": "message queue",
          "description": "at-least-once"
        },
        {
          "id": "consumer",
          "kind": "service",
          "tone": "default",
          "label": "consumer"
        },
        {
          "id": "ledger",
          "kind": "store",
          "tone": "success",
          "label": "dedup ledger",
          "description": "processed message-ids"
        },
        {
          "id": "effect",
          "kind": "process",
          "tone": "success",
          "label": "side effect (once)"
        },
        {
          "id": "dupe",
          "kind": "external",
          "tone": "warning",
          "label": "redelivered dupe",
          "description": "ack was lost"
        }
      ],
      "title": "Idempotent consumer under at-least-once",
      "rankdir": "LR",
      "subtitle": "the dedup ledger makes redelivery a no-op",
      "storyboard": [
        {
          "ms": 7150,
          "say": "A payment producer enqueues a charge-card message and returns immediately — the queue decouples it from whoever does the work.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/async-queues__b0.mp3",
          "edges": [
            [
              "producer",
              "queue"
            ]
          ],
          "nodes": [
            "producer",
            "queue"
          ]
        },
        {
          "ms": 7050,
          "say": "The queue delivers the message to the consumer, but the guarantee is at-least-once: delivered for sure, possibly more than once.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/async-queues__b1.mp3",
          "edges": [
            [
              "queue",
              "consumer"
            ]
          ],
          "nodes": [
            "queue",
            "consumer"
          ]
        },
        {
          "ms": 7650,
          "say": "If the consumer finishes the charge but crashes before its ack lands, the queue sees no ack and redelivers — a duplicate arrives.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/async-queues__b2.mp3",
          "edges": [
            [
              "dupe",
              "consumer"
            ]
          ],
          "nodes": [
            "dupe",
            "consumer"
          ]
        },
        {
          "ms": 5925,
          "say": "So before doing anything, the consumer checks a dedup ledger: have I already seen this message id?",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/async-queues__b3.mp3",
          "edges": [
            [
              "consumer",
              "ledger"
            ]
          ],
          "nodes": [
            "consumer",
            "ledger"
          ]
        },
        {
          "ms": 6850,
          "say": "If the id is new, the ledger records it and the side effect runs exactly once — the card is charged a single time.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/async-queues__b4.mp3",
          "edges": [
            [
              "ledger",
              "effect"
            ]
          ],
          "nodes": [
            "ledger",
            "effect"
          ]
        },
        {
          "ms": 8100,
          "say": "If the id is already recorded, the ledger says skip — redelivery becomes a harmless no-op, which is what idempotent means under at-least-once.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/async-queues__b5.mp3",
          "edges": [
            [
              "ledger",
              "consumer"
            ]
          ],
          "nodes": [
            "ledger",
            "consumer",
            "dupe"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "m1: processed\nm2: processed\nm3: processed\nm1: processed\neffects: 4\nleakedDuplicates: 1",
      "title": "Build the idempotent consumer with a dedup ledger",
      "starter": "// A dedup ledger bounded to `capacity` ids. Once an id is evicted,\n// a later redelivery of it is no longer recognized as a duplicate.\nclass BoundedDedupConsumer {\n  constructor(capacity) { /* bounded ledger, effects counter */ }\n  handle(msg) { /* skip if still remembered, else process and record (evicting oldest) */ }\n}\nconst everSeen = new Set();\nconst c = new BoundedDedupConsumer(2);\nconst stream = [{ id: \"m1\" }, { id: \"m2\" }, { id: \"m3\" }, { id: \"m1\" }];\nlet leaks = 0;\nfor (const msg of stream) {\n  const wasDup = everSeen.has(msg.id); everSeen.add(msg.id);\n  const r = c.handle(msg);\n  if (wasDup && r === \"processed\") leaks++;\n  console.log(msg.id + \":\", r);\n}\nconsole.log(\"effects:\", c.effects);\nconsole.log(\"leakedDuplicates:\", leaks);",
      "summary": "A real dedup ledger is BOUNDED. Build a consumer whose ledger holds only the last 2 ids; feed m1,m2,m3 then a redelivered m1 and show the redelivery is reprocessed because m1 was already evicted — the exactly-once guarantee silently breaks. Print each outcome, the total effects, and the leaked-duplicate count.",
      "language": "js"
    },
    {
      "fix": "async function onMessage(msg) {\n  if (await ledger.has(msg.id)) { await ack(msg); return; } // already charged → skip\n  await chargeCard(msg.amount);\n  await ledger.add(msg.id);   // record BEFORE ack so redelivery is a no-op\n  await ack(msg);\n}",
      "task": "The charge is not idempotent, so a redelivered message charges again. Add a dedup ledger keyed by message-id.",
      "type": "debug",
      "symptom": "A payments worker occasionally charges a customer twice, always after a transient network error around the acknowledgment.",
      "language": "ts",
      "brokenCode": "async function onMessage(msg) {\n  await chargeCard(msg.amount);   // side effect with no dedup\n  await ack(msg);                 // if this fails, the msg is redelivered → double charge\n}"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Let the queue grow and process the backlog as capacity allows; slow or pause producers if it grows too large. No work is lost, and the queue smooths the burst — at the cost of increased latency during the spike.",
        "label": "Buffer with backpressure"
      },
      "optionB": {
        "text": "Shed load by discarding messages when behind. Keeps latency flat, but you lose work — unacceptable for anything that must happen (payments, orders).",
        "label": "Drop messages"
      },
      "guidance": "Buffer with backpressure (A) for must-happen work — the queue exists precisely to absorb bursts, and slowing producers is better than losing messages. Drop (B) only for truly disposable, high-volume signals (some metrics/telemetry) where a gap is acceptable and freshness beats completeness. Match the policy to whether the work can be lost.",
      "question": "A consumer falls behind during a traffic spike. Let the queue buffer the backlog (backpressure), or drop messages to keep up?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Acknowledging each message twice so the broker treats it as fully confirmed and never redelivers it to the consumer.",
        "An idempotent consumer with a dedup ledger that records processed message-ids and skips any already handled.",
        "Processing messages fast enough that a redelivery always arrives after the work is done, so duplicates never overlap.",
        "Switching the queue into an exactly-once delivery mode, which removes the duplicates at the broker for free."
      ],
      "question": "Your queue guarantees at-least-once delivery. What makes processing effectively \"exactly once\"?",
      "explanation": "At-least-once delivery guarantees duplicates will happen (lost acks trigger redelivery). Exactly-once EFFECT comes from the consumer being idempotent: it records processed message-ids and skips any it has already handled, so a redelivered message is a harmless no-op. The queue does not provide this — the consumer does."
    },
    {
      "type": "verification",
      "intro": "Your async processing is safe when:",
      "items": [
        "You assume at-least-once delivery and expect duplicates.",
        "The consumer dedups by a stable message-id before side effects.",
        "The number of real effects equals the number of unique messages.",
        "A slow consumer triggers backpressure, not lost messages."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain why at-least-once delivery guarantees duplicates.",
        "Explain how a dedup ledger turns at-least-once delivery into exactly-once effect."
      ]
    },
    {
      "text": "Idempotency here is the same guarantee the async work in the building-blocks lesson relies on and the same dedup discipline the reliability module's retries demand — a retried request must be idempotent too. The capstone's write path is idempotent under retry.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Consumer assumes at-least-once and expects duplicates",
        "Dedup ledger keyed by message-id",
        "Unique effects == unique message-ids"
      ]
    }
  ],
  "consensus-quorum": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A quorum validator that, given N/R/W, decides whether the configuration guarantees a read sees the latest acknowledged write (R + W > N).",
      "unlock": "You can tune (N, R, W) to trade consistency against latency/availability and prove when reads are guaranteed fresh.",
      "outcome": "Understand how replicas agree: leader election picks one coordinator, and a read/write quorum with R + W > N guarantees a read overlaps the latest write.",
      "intensity": "standard",
      "doNotClaim": "Don't claim more replicas alone give consistency — consistency comes from the quorum overlap R + W > N, not from the replica count N."
    },
    {
      "text": "A cluster with 5 replicas still returns stale reads, because it writes to 1 and reads from 1 — no overlap, so a read can miss the latest write entirely. Consistency in a replicated store is not about how many copies you have; it is about the QUORUM MATH: reads and writes must be guaranteed to overlap. This lesson makes R + W > N the checkable rule that decides whether a configuration is consistent.",
      "type": "mission"
    },
    {
      "text": "When data is replicated across N nodes, the system must AGREE on the current value. Two mechanisms. LEADER ELECTION selects a single coordinator (via a consensus protocol like Raft/Paxos) so writes have one ordering authority, avoiding split-brain. QUORUMS let you tune consistency without a single leader for every operation: a write must be acknowledged by W of the N replicas, and a read must gather responses from R of the N. The guarantee is the OVERLAP condition R + W > N (from Amazon's Dynamo): if the read set and write set together exceed N, they must share at least one replica, so any read is guaranteed to include a replica holding the latest acknowledged write. Tuning the dial: high W (e.g. W=N) makes writes consistent but slow/less-available; high R makes reads consistent but slower; a common balanced choice is N=3, W=2, R=2 (sum 4 > 3), which survives one node failure while guaranteeing fresh reads.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: N=5 replicas, W=1 (write to one), R=1 (read from one). Can a read miss the latest write? What is the smallest R+W that fixes it?",
      "reveal": "Yes — with W=1, R=1 the write set and read set are single replicas that may be different nodes, so a read can hit a replica that never received the write (R+W = 2, which is NOT > 5). To guarantee overlap you need R + W > N, i.e. R + W ≥ 6 for N=5. For example W=3, R=3 (sum 6) forces the read and write quorums to share at least one replica, so every read includes the latest write. The replica count N alone guarantees nothing; the overlap does."
    },
    {
      "code": "// Does this (N, R, W) guarantee a read observes the latest acknowledged write?\nfunction quorumConsistent(N, R, W) {\n  return R + W > N;   // read set and write set must overlap in >=1 replica\n}\n// Also require W and R to be achievable within N.\nfunction validConfig(N, R, W) {\n  return W >= 1 && R >= 1 && W <= N && R <= N;\n}",
      "type": "worked-example",
      "intro": "Check the quorum overlap: a read is guaranteed fresh iff R + W > N.",
      "steps": [
        "A write must be acked by W replicas; a read must gather R replica responses.",
        "R + W > N forces the read and write sets to overlap in at least one replica.",
        "That shared replica holds the latest acknowledged write, so the read sees it.",
        "Tune the dial: higher W/R buys consistency at the cost of latency/availability."
      ],
      "language": "ts",
      "commonMistake": "Believing that adding replicas (raising N) improves consistency. Raising N without raising R and W actually makes overlap HARDER (R + W must exceed a larger N). Consistency comes from the R + W > N relationship, not from the replica count."
    },
    {
      "text": "Replicating data across N nodes creates the agreement problem: which value is current? Two tools solve it. LEADER ELECTION uses a consensus protocol (Raft, Paxos) to choose exactly one coordinator that orders writes, preventing split-brain where two nodes both think they are in charge and diverge. QUORUM replication offers a tunable alternative: instead of routing every operation through one leader, you require a write to be acknowledged by W of the N replicas and a read to collect responses from R of the N. The load-bearing rule, from Amazon's Dynamo, is the overlap condition R + W > N. The intuition is pigeonhole: if the set of W replicas that stored the write and the set of R replicas answering the read together number more than N, they cannot be disjoint — they must share at least one replica, and that replica holds the most recent acknowledged write, so the read is guaranteed to observe it. This turns \"is my system consistent?\" into arithmetic. It also exposes the tuning dial: to make writes strongly consistent you push W up (at W = N a write must reach every replica — maximally consistent, but one slow or down replica blocks all writes); to make reads consistent you push R up (slower reads); and the sweet spot for many systems is N = 3 with W = 2, R = 2, whose sum 4 exceeds 3 so reads are guaranteed fresh, while tolerating a single replica failure on either path. The critical misconception to kill is that more replicas means more consistency — raising N alone widens the gap R + W must clear, so it is the RELATIONSHIP among N, R, and W, not the size of N, that determines whether a read can ever be stale.",
      "type": "concept",
      "title": "Consistency is the quorum overlap R + W > N, not the replica count"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "r1",
          "from": "write",
          "kind": "data",
          "tone": "accent",
          "label": "ack"
        },
        {
          "to": "r2",
          "from": "write",
          "kind": "data",
          "tone": "success",
          "label": "ack"
        },
        {
          "to": "r2",
          "from": "read",
          "kind": "data",
          "tone": "success",
          "label": "read"
        },
        {
          "to": "r3",
          "from": "read",
          "kind": "data",
          "label": "read"
        },
        {
          "to": "fresh",
          "from": "r2",
          "kind": "control",
          "tone": "success",
          "label": "holds latest"
        }
      ],
      "nodes": [
        {
          "id": "write",
          "kind": "process",
          "tone": "accent",
          "label": "write to W replicas",
          "description": "W acks required"
        },
        {
          "id": "r1",
          "kind": "store",
          "tone": "success",
          "label": "replica 1"
        },
        {
          "id": "r2",
          "kind": "store",
          "tone": "success",
          "label": "replica 2 (overlap)",
          "description": "in both W and R sets"
        },
        {
          "id": "r3",
          "kind": "store",
          "tone": "default",
          "label": "replica 3"
        },
        {
          "id": "read",
          "kind": "process",
          "tone": "accent",
          "label": "read from R replicas",
          "description": "R responses"
        },
        {
          "id": "fresh",
          "kind": "decision",
          "tone": "success",
          "label": "read sees latest write",
          "description": "guaranteed if R+W>N"
        }
      ],
      "title": "Quorum overlap R + W > N",
      "rankdir": "LR",
      "subtitle": "the shared replica carries the latest write",
      "storyboard": [
        {
          "ms": 7775,
          "say": "Five copies of your data does not mean fresh reads — what matters is whether the write set and the read set are guaranteed to share a replica.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/consensus-quorum__b0.mp3",
          "edges": [],
          "nodes": [
            "write",
            "read"
          ]
        },
        {
          "ms": 7950,
          "say": "A write goes out and waits for acknowledgments from W replicas — here it lands on replica 1 and replica 2 before it is confirmed.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/consensus-quorum__b1.mp3",
          "edges": [
            [
              "write",
              "r1"
            ],
            [
              "write",
              "r2"
            ]
          ],
          "nodes": [
            "write",
            "r1",
            "r2"
          ]
        },
        {
          "ms": 6800,
          "say": "Replica 3 never received the write — it still holds the old value, and that is completely normal in a quorum system.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/consensus-quorum__b2.mp3",
          "edges": [],
          "nodes": [
            "r3"
          ]
        },
        {
          "ms": 6350,
          "say": "Later, a read fans out to R replicas — it happens to query replica 2 and the stale replica 3.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/consensus-quorum__b3.mp3",
          "edges": [
            [
              "read",
              "r2"
            ],
            [
              "read",
              "r3"
            ]
          ],
          "nodes": [
            "read",
            "r2",
            "r3"
          ]
        },
        {
          "ms": 8550,
          "say": "Because R plus W exceeds N, pigeonhole math forces at least one shared replica — here replica 2 sits in both the write set and the read set.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/consensus-quorum__b4.mp3",
          "edges": [
            [
              "write",
              "r2"
            ],
            [
              "read",
              "r2"
            ]
          ],
          "nodes": [
            "r2"
          ]
        },
        {
          "ms": 8450,
          "say": "Replica 2 holds the latest version, so the read compares responses and returns the fresh value — replica 3's stale copy is outvoted.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/consensus-quorum__b5.mp3",
          "edges": [
            [
              "r2",
              "fresh"
            ]
          ],
          "nodes": [
            "r2",
            "fresh"
          ]
        },
        {
          "ms": 7975,
          "say": "That is the whole guarantee: choose W and R so the sets must overlap, and every read is certain to touch a replica carrying the newest write.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/consensus-quorum__b6.mp3",
          "edges": [
            [
              "write",
              "r2"
            ],
            [
              "read",
              "r2"
            ],
            [
              "r2",
              "fresh"
            ]
          ],
          "nodes": [
            "write",
            "read",
            "fresh"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "N=3 F=1 feasible: 2,2 count 1\nN=5 F=1 feasible: 2,4 3,3 3,4 4,2 4,3 4,4 count 6\nN=5 F=2 feasible: 3,3 count 1",
      "title": "Build the quorum consistency validator",
      "starter": "// Enumerate every (R,W) for N replicas that both guarantees fresh reads\n// and survives F node failures on reads and writes.\nfunction overlaps(N, R, W) { /* fresh-read condition */ }\nfunction tolerates(N, W, F) { /* write still reachable after F failures */ }\nfunction feasibleConfigs(N, F) { /* all valid R,W pairs */ }\nfor (const [N, F] of [[3, 1], [5, 1], [5, 2]]) {\n  const cfgs = feasibleConfigs(N, F);\n  console.log(\"N=\" + N, \"F=\" + F, \"feasible:\", cfgs.join(\" \") || \"none\", \"count\", cfgs.length);\n}",
      "summary": "Search the config space, do not just test one: given N replicas and a requirement to survive F node failures on BOTH reads and writes, enumerate every (R,W) that still guarantees fresh reads (R+W>N) with R,W within N-F. Print the feasible set and its size for three (N,F) pairs.",
      "language": "js"
    },
    {
      "fix": "const N = 7;\n// R + W must exceed N. e.g. W=4, R=4 (sum 8 > 7) restores overlap.\nconst W = 4;\nconst R = 4;\n// consistency is R + W > N, not the size of N.",
      "task": "Raising N without raising R and W broke the overlap condition. Fix the quorum so reads are guaranteed fresh again.",
      "type": "debug",
      "symptom": "A team raised replicas from 3 to 7 \"for stronger consistency,\" and stale reads got MORE frequent, not less.",
      "language": "ts",
      "brokenCode": "const N = 7;\nconst W = 2;   // unchanged from the 3-replica setup\nconst R = 2;   // 2 + 2 = 4, NOT > 7"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Ack a write after one replica stores it. Fast and stays writable even if two replicas are down — but reads are not guaranteed fresh unless R is high, so it suits eventually-consistent data.",
        "label": "Low W (W=1)"
      },
      "optionB": {
        "text": "Require all three replicas to ack each write. Reads can then be fresh with R=1, but any single slow or down replica blocks all writes — low availability for writes.",
        "label": "High W (W=3)"
      },
      "guidance": "For an availability-first activity log, use low W (A) — writes stay fast and available, and eventual consistency is fine for a log. Use high W (B) only when writes must be immediately durable across replicas and you can tolerate write unavailability when a replica is down. Tune (N,R,W) to the data's consistency need, keeping R + W > N only where fresh reads are required.",
      "question": "On N=3 replicas, you want fast, always-available writes for an activity log. High W (W=3) or low W (W=1)?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Any (R, W) is fine, since all replicas converge eventually and a read will observe the latest write given time.",
        "R=2, W=2 — the sum R + W = 4 exceeds N = 3, so the read and write quorums must overlap on a shared replica.",
        "R=1, W=1 works too, as long as N is large enough that a randomly chosen read replica is likely to be current.",
        "R=1, W=1 — one node each is enough because every replica holds the same data, giving the fastest reads and writes."
      ],
      "question": "With N=3 replicas, which (R, W) guarantees every read observes the latest acknowledged write?",
      "explanation": "The guarantee is R + W > N. With N=3, R=2, W=2 gives 4 > 3, forcing the read and write quorums to overlap in at least one replica that holds the latest write. R=1, W=1 sums to 2, not greater than 3, so the read and write sets can be disjoint and a read can miss the latest write — regardless of how large N is."
    },
    {
      "type": "verification",
      "intro": "You use quorums correctly when:",
      "items": [
        "You can state R + W > N and why it guarantees fresh reads.",
        "You tune (N, R, W) to the data's consistency requirement.",
        "You know that raising N alone does not improve consistency.",
        "A single leader (elected) orders writes where you need a single authority."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain the pigeonhole intuition behind R + W > N.",
        "Explain how raising N without raising R and W can worsen consistency."
      ]
    },
    {
      "text": "Quorums are the concrete dial behind the CAP choice from earlier in this module: high R/W leans CP, low R/W leans AP. Leader election is the liveness mechanism the reliability module's failover depends on. The capstone's consistency-critical store uses a consistent quorum.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "R + W > N stated and applied",
        "(N,R,W) tuned to the consistency need",
        "Understands N alone does not give consistency"
      ]
    }
  ],
  "distributed-fallacies": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A fallacy auditor that scans a design's assumptions and flags each of the eight fallacies it violates.",
      "unlock": "You can name the eight fallacies and spot them as unstated assumptions in a real design.",
      "outcome": "Recognize the eight fallacies of distributed computing and the partial-failure and clock assumptions they encode — and audit a design for where it wrongly assumes the network is reliable, fast, or homogeneous.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a remote call is \"just like a local call\" — the fallacies are exactly the assumptions that make distributed systems fail in ways local code never does."
    },
    {
      "text": "A design assumes a remote service call always returns quickly and never fails, so it has no timeout and no retry — and the first time the network hiccups, threads pile up waiting forever and the whole service hangs. The eight FALLACIES OF DISTRIBUTED COMPUTING are the exact false assumptions behind failures like this. This lesson makes them a checklist you run against a design to catch the assumption before it causes the outage.",
      "type": "mission"
    },
    {
      "text": "The fallacies of distributed computing are a canonical list of false assumptions that engineers coming from single-machine code make when building distributed systems. Most were articulated by L. Peter Deutsch and colleagues at Sun Microsystems, with the eighth added by James Gosling. They are: (1) the network is reliable, (2) latency is zero, (3) bandwidth is infinite, (4) the network is secure, (5) topology does not change, (6) there is one administrator, (7) transport cost is zero, (8) the network is homogeneous. Each names an assumption that is safe locally but false across a network, and each maps to a real defense: assume the network fails → add timeouts, retries, and idempotency; assume latency is nonzero → batch and cache; assume bandwidth is finite → paginate and compress. Underlying them is PARTIAL FAILURE — in a distributed system some parts fail while others keep running, so you cannot treat the whole as up-or-down — and unsynchronized CLOCKS, which is why you cannot trust wall-clock ordering across machines.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: a service calls a remote API with no timeout and no retry, and stores a timestamp from the remote machine's clock to order events. Which fallacies is it committing?",
      "reveal": "At least three. No timeout/retry assumes \"the network is reliable\" (fallacy 1) and implicitly \"latency is zero\" (fallacy 2) — a call can hang or fail, so unbounded waiting piles up. Trusting a remote machine's clock to order events assumes clocks are synchronized, which relates to the fallacies' clock/consistency assumptions — different machines' clocks drift, so wall-clock timestamps cannot reliably order distributed events. The fix: bounded timeouts + retries + idempotency, and logical/causal ordering instead of raw wall-clock time."
    },
    {
      "code": "// Each fallacy maps to a design signal that reveals the false assumption.\nconst FALLACIES = [\n  { id: 1, name: \"network is reliable\",     violatedIf: d => !d.hasTimeout || !d.hasRetry },\n  { id: 2, name: \"latency is zero\",         violatedIf: d => d.chattyRemoteCalls },\n  { id: 3, name: \"bandwidth is infinite\",   violatedIf: d => d.unboundedPayloads },\n  { id: 4, name: \"network is secure\",       violatedIf: d => !d.encryptsInTransit },\n  { id: 5, name: \"topology never changes\",  violatedIf: d => d.hardcodedHosts },\n  { id: 6, name: \"one administrator\",       violatedIf: d => d.assumesSingleOpsOwner },\n  { id: 7, name: \"transport cost is zero\",  violatedIf: d => d.ignoresEgressCost },\n  { id: 8, name: \"network is homogeneous\",  violatedIf: d => d.assumesUniformNetwork },\n];\nfunction auditFallacies(design) {\n  return FALLACIES.filter(f => f.violatedIf(design)).map(f => f.id + \": \" + f.name);\n}",
      "type": "worked-example",
      "intro": "Audit a design against the eight fallacies, flagging each assumption it violates.",
      "steps": [
        "Each fallacy is a specific false assumption, not a vague \"networks are hard\".",
        "No timeout/retry → assumes the network is reliable (fallacy 1).",
        "Chatty fine-grained remote calls → assumes latency is zero (fallacy 2).",
        "Hardcoded hosts → assumes topology never changes (fallacy 5)."
      ],
      "language": "ts",
      "commonMistake": "Treating a remote call like a local function call — synchronous, instant, and infallible. That single mental error is the root of most of the eight fallacies: it drops timeouts (reliability), ignores round-trip cost (latency), and assumes the call always succeeds (partial failure)."
    },
    {
      "text": "The fallacies of distributed computing are the industry's checklist of the false assumptions programmers carry over from single-machine code, most articulated by L. Peter Deutsch and colleagues at Sun Microsystems (the eighth added by James Gosling). They are, in order: (1) THE NETWORK IS RELIABLE — it is not; packets drop, connections reset, so every remote call needs a timeout, retries, and idempotency. (2) LATENCY IS ZERO — every round trip costs real milliseconds, so chatty fine-grained calls are lethal; batch and cache. (3) BANDWIDTH IS INFINITE — it is finite, so unbounded payloads and missing pagination will saturate a link; compress and page. (4) THE NETWORK IS SECURE — assume hostile networks and encrypt in transit. (5) TOPOLOGY DOESN'T CHANGE — nodes come and go and IPs move, so hardcoded hosts break; use service discovery. (6) THERE IS ONE ADMINISTRATOR — real systems span teams and clouds with different policies. (7) TRANSPORT COST IS ZERO — moving bytes costs money and CPU (serialization, egress). (8) THE NETWORK IS HOMOGENEOUS — links and nodes differ, so uniform assumptions fail. Two deeper truths sit beneath the list. PARTIAL FAILURE: unlike a single machine that is up or down, a distributed system has some parts failing while others run, so \"did it work?\" is often genuinely unknown (the ambiguous timeout) and must be handled with idempotent retries and reconciliation. And CLOCKS: separate machines' clocks drift, so you cannot order events across them by wall-clock time — you need logical/causal ordering. Running a design against these eight named assumptions turns \"distributed systems are hard\" into a concrete audit that catches the missing timeout or the hardcoded host before it becomes an incident.",
      "type": "concept",
      "title": "The eight fallacies are the assumptions that make remote calls fail"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "audit",
          "from": "design",
          "kind": "sync",
          "tone": "accent",
          "label": "inspect"
        },
        {
          "to": "reliable",
          "from": "audit",
          "kind": "control",
          "tone": "warning",
          "label": "flag"
        },
        {
          "to": "latency",
          "from": "audit",
          "kind": "control",
          "tone": "warning",
          "label": "flag"
        },
        {
          "to": "topology",
          "from": "audit",
          "kind": "control",
          "tone": "warning",
          "label": "flag"
        },
        {
          "to": "fixes",
          "from": "reliable",
          "kind": "data",
          "tone": "success",
          "label": "add timeout/retry"
        },
        {
          "to": "fixes",
          "from": "latency",
          "kind": "data",
          "tone": "success",
          "label": "batch/cache"
        },
        {
          "to": "fixes",
          "from": "topology",
          "kind": "data",
          "tone": "success",
          "label": "service discovery"
        }
      ],
      "nodes": [
        {
          "id": "design",
          "kind": "client",
          "tone": "accent",
          "label": "design under review"
        },
        {
          "id": "audit",
          "kind": "decision",
          "tone": "warning",
          "label": "fallacy audit",
          "description": "scan 8 assumptions"
        },
        {
          "id": "reliable",
          "kind": "external",
          "tone": "warning",
          "label": "no timeout/retry",
          "description": "fallacy 1"
        },
        {
          "id": "latency",
          "kind": "external",
          "tone": "warning",
          "label": "chatty calls",
          "description": "fallacy 2"
        },
        {
          "id": "topology",
          "kind": "external",
          "tone": "warning",
          "label": "hardcoded hosts",
          "description": "fallacy 5"
        },
        {
          "id": "fixes",
          "kind": "process",
          "tone": "success",
          "label": "defenses (timeout, batch, discovery)"
        }
      ],
      "title": "Auditing a design against the fallacies",
      "rankdir": "TB",
      "subtitle": "each unstated assumption maps to a real defense",
      "storyboard": [
        {
          "ms": 8200,
          "say": "It starts with a design under review — code that calls a remote service and quietly assumes the network will behave like a local function call.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/distributed-fallacies__b0.mp3",
          "edges": [],
          "nodes": [
            "design"
          ]
        },
        {
          "ms": 7700,
          "say": "We run the design through a fallacy audit, checking each of the classic false assumptions engineers carry over from single-machine code.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/distributed-fallacies__b1.mp3",
          "edges": [
            [
              "design",
              "audit"
            ]
          ],
          "nodes": [
            "design",
            "audit"
          ]
        },
        {
          "ms": 8175,
          "say": "First flag: the design trusts the network is reliable — there is no timeout and no retry, so one dropped packet leaves threads waiting forever.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/distributed-fallacies__b2.mp3",
          "edges": [
            [
              "audit",
              "reliable"
            ]
          ],
          "nodes": [
            "reliable"
          ]
        },
        {
          "ms": 7625,
          "say": "Second flag: it assumes latency is zero, making chatty fine-grained calls where every round trip silently adds real milliseconds.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/distributed-fallacies__b3.mp3",
          "edges": [
            [
              "audit",
              "latency"
            ]
          ],
          "nodes": [
            "latency"
          ]
        },
        {
          "ms": 7400,
          "say": "Third flag: hosts are hardcoded, assuming the topology never changes — the moment a server moves or scales, the design breaks.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/distributed-fallacies__b4.mp3",
          "edges": [
            [
              "audit",
              "topology"
            ]
          ],
          "nodes": [
            "topology"
          ]
        },
        {
          "ms": 9175,
          "say": "Each flagged assumption maps to a concrete defense: timeouts and retries for reliability, batching and caching for latency, service discovery for topology.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/distributed-fallacies__b5.mp3",
          "edges": [
            [
              "reliable",
              "fixes"
            ],
            [
              "latency",
              "fixes"
            ],
            [
              "topology",
              "fixes"
            ]
          ],
          "nodes": [
            "fixes"
          ]
        },
        {
          "ms": 8150,
          "say": "That is the insight: the fallacies are not trivia — they are an audit checklist where every unstated assumption you catch becomes a specific fix you ship.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/distributed-fallacies__b6.mp3",
          "edges": [],
          "nodes": [
            "audit",
            "fixes"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "violations: 4\ntotalRisk: 16\ntopFix: 1: network is reliable",
      "title": "Build the fallacy auditor",
      "starter": "// Each fallacy has a severity. Score a design's total risk and the top fix.\nconst FALLACIES = [\n  { id: 1, name: \"network is reliable\",    severity: 5, violatedIf: d => !d.hasTimeout || !d.hasRetry },\n  { id: 2, name: \"latency is zero\",        severity: 3, violatedIf: d => d.chattyRemoteCalls },\n  { id: 3, name: \"bandwidth is infinite\",  severity: 2, violatedIf: d => d.unboundedPayloads },\n  { id: 4, name: \"network is secure\",      severity: 5, violatedIf: d => !d.encryptsInTransit },\n  { id: 5, name: \"topology never changes\", severity: 3, violatedIf: d => d.hardcodedHosts },\n  { id: 6, name: \"one administrator\",      severity: 1, violatedIf: d => d.assumesSingleOpsOwner },\n  { id: 7, name: \"transport cost is zero\", severity: 2, violatedIf: d => d.ignoresEgressCost },\n  { id: 8, name: \"network is homogeneous\", severity: 1, violatedIf: d => d.assumesUniformNetwork },\n];\nfunction prioritizeFallacies(design) { /* total severity of violations + highest-severity one */ }\nconst design = {\n  hasTimeout: false, hasRetry: false,\n  chattyRemoteCalls: true,\n  encryptsInTransit: false,\n  hardcodedHosts: true,\n};\nconst r = prioritizeFallacies(design);\nconsole.log(\"violations:\", r.count);\nconsole.log(\"totalRisk:\", r.totalRisk);\nconsole.log(\"topFix:\", r.topFix);",
      "summary": "Go past listing violations: each fallacy carries a severity weight, so compute the design's total risk score and name the single highest-severity fix to do first. Run it on a naive design and print the violation count, total risk, and top fix.",
      "language": "js"
    },
    {
      "fix": "// Bound every remote call; assume it can fail or stall.\nconst opts = { timeout: 500 };  // fail fast, don't wait forever\nconst a = await http.get(urlA, opts);\nconst b = await http.get(urlB, opts);\nconst c = await http.get(urlC, opts);  // + retry with backoff on transient errors",
      "task": "The code assumes the network is reliable and instant (fallacies 1 and 2) with no timeouts. Fix the remote calls.",
      "type": "debug",
      "symptom": "A service that calls three downstream APIs occasionally hangs completely, with all its worker threads stuck, until it is restarted.",
      "language": "ts",
      "brokenCode": "const a = await http.get(urlA);  // no timeout\nconst b = await http.get(urlB);  // no timeout\nconst c = await http.get(urlC);  // one slow dependency hangs all threads"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Order by causality (logical clocks / version vectors), not wall time. Correct across machines despite clock drift — at the cost of extra metadata and conceptual overhead.",
        "label": "Logical/causal ordering"
      },
      "optionB": {
        "text": "Tag each event with the local machine's clock and sort. Simple, but clocks drift between machines, so events can be ordered wrong — exactly the clock assumption the fallacies warn against.",
        "label": "Wall-clock timestamps"
      },
      "guidance": "Use logical/causal ordering (A) whenever correctness depends on the order of events across machines — clock drift makes wall-clock ordering unreliable, which is the clock corollary of the fallacies. Wall-clock timestamps (B) are fine only for coarse, human-facing display or within a single machine, never for cross-machine correctness.",
      "question": "Ordering events across two services. Use each machine's wall-clock timestamp, or logical/causal ordering?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Databases are always relational — a false generalization, but not one of the eight networking fallacies.",
        "The network is reliable — the first fallacy; remote calls can drop, so they need timeouts and retries.",
        "CPUs are infinitely fast — a plausible assumption, but the fallacies concern the network, not the CPU.",
        "Caches never go stale — a genuine hazard in system design, yet not part of the canonical eight fallacies."
      ],
      "question": "Which is one of the eight fallacies of distributed computing?",
      "explanation": "\"The network is reliable\" is the first fallacy — the false assumption that remote calls always succeed, which is why distributed code needs timeouts, retries, and idempotency. The others listed are not part of the canonical eight (network reliable, latency zero, bandwidth infinite, network secure, topology unchanging, one admin, transport free, network homogeneous)."
    },
    {
      "type": "verification",
      "intro": "You respect the fallacies when:",
      "items": [
        "Every remote call has a timeout and a retry policy.",
        "You do not order cross-machine events by wall-clock time.",
        "Hosts come from discovery, not hardcoded addresses.",
        "You can name the eight fallacies and audit a design against them."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Name four of the eight fallacies and the defense each implies.",
        "Explain partial failure and why it makes a timeout ambiguous."
      ]
    },
    {
      "text": "The fallacies are the \"why\" behind the entire reliability module: timeouts, retries, circuit breakers, and blast-radius thinking all exist because the network is not reliable, latency is not zero, and failures are partial. The capstone must handle a partial failure without hanging.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Design audited against all eight fallacies",
        "Remote calls bounded with timeout + retry",
        "Cross-machine ordering does not rely on wall clocks"
      ]
    }
  ],
  "failure-modes-blast-radius": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A blast-radius propagator that, given a dependency graph, computes every component that fails when one component goes down — and shows how a bulkhead shrinks it.",
      "unlock": "You can compute the blast radius of any component and place bulkheads to contain cascading failures.",
      "outcome": "Trace how one component's failure propagates through its dependents (the blast radius), and use bulkheads to contain it so a single failure does not take down the whole system.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a component failure is \"isolated\" without tracing its dependents — one shared dependency can cascade a local failure into a total outage."
    },
    {
      "text": "A single recommendation service goes down, and because the product page calls it synchronously with no isolation, the whole product page fails — then the homepage that embeds it fails — and a minor dependency has taken down the site. This is a BLAST RADIUS problem: failures propagate through the dependency graph. This lesson computes that radius from the graph and shows how a bulkhead contains it.",
      "type": "mission"
    },
    {
      "text": "In a system of interdependent components, a failure rarely stays local: everything that DEPENDS on the failed component can fail too, and their dependents in turn — the set of everything that goes down is the BLAST RADIUS. You compute it by walking the dependency graph from the failed node to all its (transitive) dependents. The defense is a BULKHEAD (from ship design: watertight compartments so one breach does not sink the vessel): isolate components so a failure in one cannot spread — separate thread pools/connection pools per dependency, so a slow dependency exhausts only its own pool; graceful degradation, so a caller treats a failed non-critical dependency as optional and returns a reduced result instead of failing. A bulkhead effectively CUTS an edge in the propagation graph, shrinking the blast radius from \"the whole system\" to \"one feature.\"",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: the product page calls recommendations synchronously and fails if recommendations fails. The homepage embeds the product page. If recommendations goes down, what is the blast radius? How does a bulkhead change it?",
      "reveal": "Blast radius = recommendations → product page → homepage: all three fail, because each hard-depends on the one below. A bulkhead breaks the chain by making recommendations OPTIONAL to the product page — the product page catches the recommendation failure and renders without that section (graceful degradation). Now the blast radius is just recommendations; the product page and homepage stay up with a reduced experience. The bulkhead cut the propagation edge."
    },
    {
      "code": "// dependents[X] = components that fail if X fails.\nfunction blastRadius(dependents, failed) {\n  const down = new Set([failed]);\n  const stack = [failed];\n  while (stack.length) {\n    const node = stack.pop();\n    for (const dep of (dependents[node] || [])) {\n      if (!down.has(dep)) { down.add(dep); stack.push(dep); }   // propagate to dependents\n    }\n  }\n  return down;\n}\n// A bulkhead makes a dependency optional → remove that edge before propagating.\nfunction withBulkhead(dependents, from, to) {\n  const copy = {}; for (const k in dependents) copy[k] = dependents[k].filter(d => !(k === from && d === to));\n  return copy;\n}",
      "type": "worked-example",
      "intro": "Walk the dependency graph to compute the blast radius; a bulkhead removes an edge.",
      "steps": [
        "Model who fails-if: dependents[X] lists components that go down when X does.",
        "Traverse from the failed node to all transitive dependents — that set is the blast radius.",
        "A bulkhead makes a caller tolerate a failed dependency (optional/degraded), cutting the edge.",
        "Recompute the radius on the bulkheaded graph to see the containment."
      ],
      "language": "ts",
      "commonMistake": "Calling every dependency synchronously and hard-failing if any is unavailable, with no isolation. A single non-critical service then becomes a single point of failure for everything upstream — the blast radius is the whole system when it should have been one feature."
    },
    {
      "text": "A production system is a graph of components that depend on one another, and the crucial reliability insight is that failure is TRANSITIVE: when a component fails, everything that hard-depends on it can fail, and everything that depends on THOSE can fail, and so on. The BLAST RADIUS of a failure is the full set of components that go down as a result — you compute it by traversing the dependency graph from the failed node outward to all its transitive dependents. A design where a minor service (recommendations, avatars, related-items) is called synchronously and treated as required means its blast radius includes every page that embeds it, up to the whole site — a tiny failure with a system-sized radius. The containment tool is the BULKHEAD, named for a ship's watertight compartments: you isolate components so a breach in one cannot flood the rest. Concretely that means (a) separate resource pools per dependency — its own thread pool and connection pool — so a slow or failing dependency exhausts only its own resources and cannot starve the caller's threads and hang everything; and (b) graceful degradation — the caller treats a non-critical dependency as OPTIONAL, catches its failure, and returns a reduced result (the product page without the recommendation strip) instead of failing outright. In graph terms, a bulkhead REMOVES the propagation edge from the caller to the failed dependency, so the blast radius shrinks from \"everything upstream\" to just the failed component. Designing for reliability is therefore two moves: compute the blast radius of each critical component to find the dangerous single points, and place bulkheads on the edges that would otherwise cascade a local failure into a global one.",
      "type": "concept",
      "title": "Failures propagate along dependencies; bulkheads cut the edges"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "product",
          "from": "recs",
          "kind": "control",
          "tone": "warning",
          "label": "propagates (no bulkhead)"
        },
        {
          "to": "home",
          "from": "product",
          "kind": "control",
          "tone": "warning",
          "label": "propagates"
        },
        {
          "to": "recs",
          "from": "bulkhead",
          "kind": "control",
          "tone": "success",
          "label": "isolate"
        },
        {
          "to": "degraded",
          "from": "bulkhead",
          "kind": "sync",
          "tone": "success",
          "label": "serve reduced"
        }
      ],
      "nodes": [
        {
          "id": "recs",
          "kind": "service",
          "tone": "warning",
          "label": "recommendations (fails)",
          "description": "the failure origin"
        },
        {
          "id": "product",
          "kind": "service",
          "tone": "accent",
          "label": "product page",
          "description": "depends on recs"
        },
        {
          "id": "home",
          "kind": "client",
          "tone": "accent",
          "label": "homepage",
          "description": "embeds product page"
        },
        {
          "id": "bulkhead",
          "kind": "decision",
          "tone": "success",
          "label": "bulkhead (recs optional)",
          "description": "cuts the edge"
        },
        {
          "id": "degraded",
          "kind": "service",
          "tone": "success",
          "label": "degraded product page",
          "description": "renders without recs"
        }
      ],
      "title": "Blast radius and bulkhead containment",
      "rankdir": "BT",
      "subtitle": "a failure propagates to dependents unless an edge is cut",
      "storyboard": [
        {
          "ms": 5000,
          "say": "Start with the recommendations service — a minor dependency — and it has just gone down.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/failure-modes-blast-radius__b0.mp3",
          "edges": [],
          "nodes": [
            "recs"
          ]
        },
        {
          "ms": 6550,
          "say": "The product page calls it synchronously and treats it as required, so when recs fails, the product page fails with it.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/failure-modes-blast-radius__b1.mp3",
          "edges": [
            [
              "recs",
              "product"
            ]
          ],
          "nodes": [
            "recs",
            "product"
          ]
        },
        {
          "ms": 7150,
          "say": "Failure is transitive: the homepage embeds the product page, so it goes down too — the whole site dies from one small service.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/failure-modes-blast-radius__b2.mp3",
          "edges": [
            [
              "product",
              "home"
            ]
          ],
          "nodes": [
            "product",
            "home"
          ]
        },
        {
          "ms": 7775,
          "say": "That set of everything knocked out — recs, product page, homepage — is the blast radius: walk the graph from the failure to all its dependents.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/failure-modes-blast-radius__b3.mp3",
          "edges": [
            [
              "recs",
              "product"
            ],
            [
              "product",
              "home"
            ]
          ],
          "nodes": [
            "recs",
            "product",
            "home"
          ]
        },
        {
          "ms": 6875,
          "say": "The fix is a bulkhead: declare recommendations optional and isolate the failure so it cannot propagate past that edge.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/failure-modes-blast-radius__b4.mp3",
          "edges": [
            [
              "bulkhead",
              "recs"
            ]
          ],
          "nodes": [
            "bulkhead",
            "recs"
          ]
        },
        {
          "ms": 7050,
          "say": "With recs walled off, the bulkhead serves a degraded product page — no recommendations, but the page and homepage stay up.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/failure-modes-blast-radius__b5.mp3",
          "edges": [
            [
              "bulkhead",
              "degraded"
            ]
          ],
          "nodes": [
            "bulkhead",
            "degraded"
          ]
        },
        {
          "ms": 7750,
          "say": "The insight: blast radius is set by your edges, not your failures — cut the synchronous hard dependency and a tiny outage stays tiny.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/failure-modes-blast-radius__b6.mp3",
          "edges": [
            [
              "bulkhead",
              "recs"
            ],
            [
              "bulkhead",
              "degraded"
            ]
          ],
          "nodes": [
            "bulkhead",
            "recs",
            "degraded"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "auth blast 3\ncache blast 3\napi blast 2\nweb blast 1",
      "title": "Compute blast radius with and without a bulkhead",
      "starter": "// Rank every node by how many others fail with it (its blast radius).\nfunction radius(dependents, failed) { /* count transitive dependents, excluding self? include self here */ }\nfunction worstSpof(dependents) { /* every node ranked by blast radius, worst first */ }\nconst dependents = {\n  auth: [\"api\", \"web\"],\n  api: [\"web\"],\n  web: [],\n  cache: [\"api\"],\n};\nfor (const r of worstSpof(dependents)) console.log(r.node, \"blast\", r.blast);",
      "summary": "Find the single point of failure: compute the blast radius (transitive dependents) for EVERY node in the graph and rank them, so the node whose failure takes down the most is surfaced first. Print each node and its blast size, worst first.",
      "language": "js"
    },
    {
      "fix": "let items = [];\ntry {\n  items = await relatedItems.fetch(cart, { timeout: 100, pool: \"relatedItemsPool\" }); // isolated pool + timeout\n} catch { items = []; } // degrade gracefully: checkout renders without related items\nreturn renderCheckout(cart, items);",
      "task": "A non-critical dependency shares resources and is treated as required, so its slowness blocks checkout. Add a bulkhead.",
      "type": "debug",
      "symptom": "When the non-critical \"related items\" service is slow, the entire checkout flow times out, even though checkout does not logically need related items.",
      "language": "ts",
      "brokenCode": "const items = await relatedItems.fetch(cart); // shares the request thread pool, no timeout\nreturn renderCheckout(cart, items);           // blocks checkout on a non-critical call"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Isolate the call (own pool + timeout) and render without recommendations if it fails. The page stays up during a recs outage with a slightly reduced experience; the blast radius is just recs.",
        "label": "Optional via bulkhead"
      },
      "optionB": {
        "text": "Call recs inline and fail the page if it fails. Simpler code and the page is always \"complete,\" but a recs outage takes down the whole page and everything embedding it.",
        "label": "Required, synchronous"
      },
      "guidance": "Bulkhead it (A) — a non-critical dependency should never be a single point of failure for the page. Isolation plus graceful degradation shrinks the blast radius to the one feature. Reserve required/synchronous (B) for genuinely essential dependencies (the product's own data), and even then add timeouts.",
      "question": "A page depends on a non-critical recommendations service. Call it synchronously as required, or make it optional with a bulkhead?"
    },
    {
      "type": "quiz",
      "answer": 2,
      "options": [
        "Adding more replicas of the minor service, so that even if some die the callers can always reach a healthy copy.",
        "Retrying the failed call in a tight loop until it eventually succeeds, so the caller never renders without it.",
        "A bulkhead: isolate the dependency behind its own pool and timeout so its failure does not propagate to callers.",
        "Nothing contains it — a synchronous dependency’s failure always cascades to everything upstream that calls it."
      ],
      "question": "A minor service fails and takes down every page that calls it synchronously. What contains this?",
      "explanation": "The failure cascades because callers treat the minor service as required and share resources with it. A bulkhead isolates it (separate pool + timeout) and lets callers degrade gracefully (render without it), cutting the propagation edge so the blast radius shrinks to the failed service. Replicas or infinite retries do not stop the cascade; isolation does."
    },
    {
      "type": "verification",
      "intro": "You contain failures when:",
      "items": [
        "You can compute the blast radius of each critical component from the dependency graph.",
        "Non-critical dependencies are optional (graceful degradation), not required.",
        "Each dependency has an isolated resource pool and a timeout.",
        "A single non-critical failure cannot take down the whole system."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain how a failure propagates through a dependency graph.",
        "Explain how a bulkhead shrinks the blast radius."
      ]
    },
    {
      "text": "Blast-radius thinking is the reliability lens for the whole module: the resilience patterns (next) are the per-edge tools (timeout, retry, circuit breaker) that implement bulkheads, and SLOs measure whether containment held. The capstone must survive one component failure without a full outage.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Blast radius computed from the dependency graph",
        "Non-critical dependencies made optional",
        "Bulkhead demonstrably shrinks the radius"
      ]
    }
  ],
  "resilience-patterns": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A circuit breaker that transitions closed → open → half-open → closed (or back to open) based on failures and a cooldown, tested against a scripted call sequence.",
      "unlock": "You can implement a circuit breaker and explain how timeout, retry, and breaker compose to protect a caller.",
      "outcome": "Apply the core resilience patterns to a remote call: timeout (bound the wait), retry with exponential backoff (recover from transient failures), and circuit breaker (stop hammering a dead dependency) — implementing the breaker state machine.",
      "intensity": "standard",
      "doNotClaim": "Don't claim retries make a system resilient — retrying a hard-down dependency amplifies the outage; you need a breaker to stop."
    },
    {
      "text": "A dependency goes down and the caller retries every failed request three times, so the dependency now gets 3× the traffic while it is trying to recover — the retries are a self-inflicted denial-of-service. Resilience is not \"retry harder\"; it is a small set of composed patterns: bound the wait (timeout), retry transient failures gently (backoff), and STOP calling a dead dependency (circuit breaker). This lesson builds the breaker state machine that makes the caller give up fast and probe for recovery.",
      "type": "mission"
    },
    {
      "text": "Three patterns protect a caller from a flaky dependency. TIMEOUT bounds how long you wait — without it, one stuck call holds a thread forever (the hang from the fallacies lesson). RETRY WITH EXPONENTIAL BACKOFF handles TRANSIENT failures (a blip, a brief 503) by retrying after growing delays (e.g. 100ms, 200ms, 400ms), ideally with jitter to avoid synchronized retry storms — but only for idempotent operations and only a few times. The CIRCUIT BREAKER (Michael Nygard, Release It!) handles a dependency that is DOWN, not just blipping: it is a state machine. CLOSED = normal, calls pass, failures are counted; when failures exceed a threshold it trips to OPEN = calls fail fast immediately without touching the dependency, sparing it and the caller; after a cooldown it moves to HALF-OPEN = a single trial call is allowed — if it succeeds the breaker CLOSES (recovered), if it fails it re-OPENS. Together: timeout bounds each attempt, backoff spaces retries, and the breaker stops retrying entirely once the dependency is clearly down.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: a circuit breaker is OPEN after a dependency failed repeatedly. The cooldown elapses. What happens on the next call, and what are the two possible outcomes?",
      "reveal": "After the cooldown the breaker moves to HALF-OPEN and allows a single TRIAL call through to test the dependency. Two outcomes: if the trial SUCCEEDS, the dependency has recovered, so the breaker transitions to CLOSED and normal traffic resumes; if the trial FAILS, the dependency is still down, so the breaker returns to OPEN and starts another cooldown. Half-open is the controlled probe that lets the breaker detect recovery without flooding a still-broken dependency with full traffic."
    },
    {
      "code": "class CircuitBreaker {\n  constructor(threshold, cooldownMs) {\n    this.state = \"closed\"; this.failures = 0; this.threshold = threshold;\n    this.cooldownMs = cooldownMs; this.openedAt = 0;\n  }\n  onResult(ok, now) {\n    if (this.state === \"open\" && now - this.openedAt >= this.cooldownMs) this.state = \"half-open\"; // cooldown → probe\n    if (this.state === \"half-open\") {\n      this.state = ok ? \"closed\" : \"open\";          // trial closes or re-opens\n      if (this.state === \"closed\") this.failures = 0; else this.openedAt = now;\n      return this.state;\n    }\n    if (ok) { this.failures = 0; }\n    else if (++this.failures >= this.threshold) { this.state = \"open\"; this.openedAt = now; } // trip\n    return this.state;\n  }\n}",
      "type": "worked-example",
      "intro": "The circuit breaker state machine: count failures to open, cooldown to half-open, trial to close.",
      "steps": [
        "CLOSED: calls pass; count consecutive failures; at the threshold, trip to OPEN.",
        "OPEN: fail fast; after the cooldown elapses, allow the next call as a HALF-OPEN probe.",
        "HALF-OPEN: one trial call — success → CLOSED (reset), failure → OPEN (new cooldown).",
        "Compose with timeout (bound each call) and backoff (space retries while closed)."
      ],
      "language": "ts",
      "commonMistake": "Retrying a hard-down dependency aggressively with no circuit breaker. The retries multiply load on a service that is already failing, delaying its recovery and amplifying the outage — you need a breaker to STOP calling once the dependency is clearly down."
    },
    {
      "text": "Resilience against a flaky dependency is three composed patterns, each solving a different failure shape. A TIMEOUT bounds a single call: because \"the network is reliable\" is a fallacy, any call can stall, and without a deadline one stuck request holds a thread indefinitely and cascades into a hang — so every remote call gets a timeout and fails fast when exceeded. RETRY WITH EXPONENTIAL BACKOFF handles TRANSIENT faults — a momentary blip, a brief overload — by retrying after exponentially growing delays (100ms, 200ms, 400ms…), preferably with random JITTER so many clients do not retry in lockstep and create a synchronized thundering herd; retries must be limited in number and only applied to idempotent operations. But retries are exactly wrong when the dependency is not blipping but DOWN — then every retry piles more load on a service trying to recover, turning a partial outage into a self-inflicted denial of service. The CIRCUIT BREAKER (from Michael Nygard's Release It!, popularized by Martin Fowler) fixes that with a three-state machine wrapping the call. CLOSED is normal operation: calls pass through and consecutive failures are counted; when the count crosses a threshold the breaker TRIPS to OPEN. OPEN means the dependency is presumed dead: calls FAIL FAST immediately without even attempting the dependency, which protects both the caller (no waiting) and the dependency (no load) — and after a cooldown timeout the breaker moves to HALF-OPEN. HALF-OPEN allows a single TRIAL call: if it succeeds the dependency has recovered and the breaker returns to CLOSED (resetting the failure count); if it fails the breaker snaps back to OPEN for another cooldown. Composed, these three give a caller that waits bounded time per attempt, retries gently through transient faults, and stops entirely once a dependency is genuinely down — probing periodically to detect its recovery.",
      "type": "concept",
      "title": "Timeout bounds, backoff spaces, the breaker stops"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "threshold",
          "from": "closed",
          "kind": "control",
          "tone": "warning",
          "label": "on failure"
        },
        {
          "to": "open",
          "from": "threshold",
          "kind": "control",
          "tone": "warning",
          "label": "trip"
        },
        {
          "to": "cooldown",
          "from": "open",
          "kind": "async",
          "tone": "accent",
          "label": "wait"
        },
        {
          "to": "halfopen",
          "from": "cooldown",
          "kind": "control",
          "tone": "accent",
          "label": "probe"
        },
        {
          "to": "trial",
          "from": "halfopen",
          "kind": "sync",
          "tone": "accent",
          "label": "call"
        },
        {
          "to": "closed",
          "from": "trial",
          "kind": "control",
          "tone": "success",
          "label": "success → close"
        },
        {
          "to": "open",
          "from": "trial",
          "kind": "control",
          "tone": "warning",
          "label": "fail → re-open"
        }
      ],
      "nodes": [
        {
          "id": "closed",
          "kind": "process",
          "tone": "success",
          "label": "CLOSED",
          "description": "calls pass, count failures"
        },
        {
          "id": "threshold",
          "kind": "decision",
          "tone": "warning",
          "label": "failures ≥ threshold"
        },
        {
          "id": "open",
          "kind": "process",
          "tone": "warning",
          "label": "OPEN",
          "description": "fail fast, no calls"
        },
        {
          "id": "cooldown",
          "kind": "decision",
          "tone": "accent",
          "label": "cooldown elapsed"
        },
        {
          "id": "halfopen",
          "kind": "process",
          "tone": "accent",
          "label": "HALF-OPEN",
          "description": "one trial call"
        },
        {
          "id": "trial",
          "kind": "decision",
          "tone": "accent",
          "label": "trial result?"
        }
      ],
      "title": "Circuit breaker state machine",
      "rankdir": "LR",
      "subtitle": "closed → open → half-open → closed/open",
      "storyboard": [
        {
          "ms": 7200,
          "say": "The breaker starts CLOSED: every request flows to the dependency, and each failure is quietly counted against a threshold.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/resilience-patterns__b0.mp3",
          "edges": [
            [
              "closed",
              "threshold"
            ]
          ],
          "nodes": [
            "closed",
            "threshold"
          ]
        },
        {
          "ms": 7500,
          "say": "When failures cross the threshold, the breaker trips to OPEN — instead of retrying harder, it stops calling entirely and fails fast.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/resilience-patterns__b1.mp3",
          "edges": [
            [
              "threshold",
              "open"
            ]
          ],
          "nodes": [
            "threshold",
            "open"
          ]
        },
        {
          "ms": 8050,
          "say": "OPEN is the protection: with zero traffic hitting it, the struggling dependency finally gets room to recover instead of a retry storm.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/resilience-patterns__b2.mp3",
          "edges": [],
          "nodes": [
            "open"
          ]
        },
        {
          "ms": 6525,
          "say": "After a cooldown elapses, the breaker moves to HALF-OPEN and lets a single probe request through as a careful test.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/resilience-patterns__b3.mp3",
          "edges": [
            [
              "open",
              "cooldown"
            ],
            [
              "cooldown",
              "halfopen"
            ]
          ],
          "nodes": [
            "open",
            "cooldown",
            "halfopen"
          ]
        },
        {
          "ms": 6375,
          "say": "That one trial call decides everything: if it succeeds, the breaker closes and normal traffic resumes.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/resilience-patterns__b4.mp3",
          "edges": [
            [
              "halfopen",
              "trial"
            ],
            [
              "trial",
              "closed"
            ]
          ],
          "nodes": [
            "halfopen",
            "trial",
            "closed"
          ]
        },
        {
          "ms": 6875,
          "say": "If the trial fails, the breaker re-opens and waits out another cooldown — never flooding a dependency that is still down.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/resilience-patterns__b5.mp3",
          "edges": [
            [
              "trial",
              "open"
            ]
          ],
          "nodes": [
            "trial",
            "open"
          ]
        },
        {
          "ms": 8350,
          "say": "The insight: resilience is not retrying harder — the breaker converts repeated failure into fast failure, so your client stops being the attack.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/resilience-patterns__b6.mp3",
          "edges": [],
          "nodes": [
            "closed",
            "open",
            "halfopen"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "t=0 -> closed\nt=10 -> closed\nt=20 -> open\nt=1100 -> half-open\nt=1110 -> closed",
      "title": "Implement the circuit breaker state machine",
      "starter": "// Circuit breaker that needs K consecutive half-open successes to close.\nclass CircuitBreakerK {\n  constructor(failThreshold, cooldownMs, successThreshold) { /* state, counters */ }\n  onResult(ok, now) { /* closed->open on failThreshold; open->half-open after cooldown; half-open->closed after K successes */ }\n}\nconst cb = new CircuitBreakerK(3, 1000, 2);\nconst events = [\n  { ok: false, t: 0 }, { ok: false, t: 10 }, { ok: false, t: 20 },\n  { ok: true, t: 1100 }, { ok: true, t: 1110 },\n];\nfor (const e of events) console.log(\"t=\" + e.t, \"->\", cb.onResult(e.ok, e.t));",
      "summary": "A single lucky call should not close a breaker. Extend the state machine so half-open requires K consecutive successful trials before closing (fail-fast back to open on any trial failure). Drive it through three failures, a cooldown, and two successes, printing the state after each event.",
      "language": "js"
    },
    {
      "fix": "async function call() {\n  if (breaker.state === \"open\") throw new Error(\"circuit open — fail fast\");\n  try { const r = await downstream(); breaker.onResult(true, Date.now()); return r; }\n  catch (e) { breaker.onResult(false, Date.now()); throw e; } // trips after threshold → stops calling\n}",
      "task": "Unconditional retries hammer a dead dependency and amplify the outage. Wrap the call in a circuit breaker.",
      "type": "debug",
      "symptom": "When a downstream service goes fully down, the caller's error rate and latency get WORSE over time instead of stabilizing, and the downstream service never recovers.",
      "language": "ts",
      "brokenCode": "async function call() {\n  for (let i = 0; i < 3; i++) {           // always retries 3x, even when down\n    try { return await downstream(); } catch {}\n  }\n  throw new Error(\"failed\");\n}"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Retry transient blips with backoff while CLOSED, but once failures cross a threshold, OPEN the breaker and fail fast — sparing the dependency and returning quickly. Probes for recovery via half-open.",
        "label": "Circuit breaker (fail fast when down)"
      },
      "optionB": {
        "text": "Keep retrying every failed call with growing delays. Great for transient faults, but against a hard-down dependency the retries add load and delay its recovery — and the caller keeps paying retry latency.",
        "label": "Retry with backoff only"
      },
      "guidance": "Use both, layered (A): backoff handles transient faults, and the breaker catches the case where the dependency is actually down and retries would only make it worse. Retry-only (B) is fine when failures are always brief, but without a breaker a sustained outage turns your retries into an attack on the recovering service.",
      "question": "A dependency is returning errors. Keep retrying with backoff, or trip a circuit breaker and fail fast?"
    },
    {
      "type": "quiz",
      "answer": 2,
      "options": [
        "It retries every call twice as fast as normal, on the theory that hammering the dependency speeds its recovery.",
        "It queues incoming calls and holds them until the dependency returns, then releases the whole backlog at once.",
        "It fails calls fast without touching the dependency; after a cooldown, one half-open trial call tests recovery.",
        "It blocks all calls permanently, requiring a human to manually reset the breaker before any traffic can resume."
      ],
      "question": "A circuit breaker is OPEN. What does it do to calls, and how does it detect recovery?",
      "explanation": "In OPEN the breaker short-circuits: calls fail immediately without touching the dependency, protecting both sides. After the cooldown it goes HALF-OPEN and lets a single trial call through — success closes the breaker (recovered), failure re-opens it. It neither queues nor blocks forever; it probes."
    },
    {
      "type": "verification",
      "intro": "Your call is resilient when:",
      "items": [
        "Every remote call has a timeout.",
        "Retries use exponential backoff with jitter and only apply to idempotent operations.",
        "A circuit breaker trips on sustained failure and fails fast when open.",
        "The breaker probes via half-open and recovers to closed on success."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Walk through the three breaker states and the transitions between them.",
        "Explain why retries alone can make an outage worse and a breaker fixes it."
      ]
    },
    {
      "text": "These patterns implement the bulkheads from the previous lesson (timeout + breaker isolate a dependency) and are exactly what the capstone's resilient client composes. The observability lesson (next) measures whether they are working via SLOs and error budgets.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Circuit breaker transitions closed/open/half-open correctly",
        "Retries use bounded exponential backoff",
        "Timeout bounds every remote call"
      ]
    }
  ],
  "observability-slos": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "An error-budget calculator that turns an SLO and a measurement window into allowed downtime, budget consumed, and a burn rate that flags when the budget will be exhausted early.",
      "unlock": "You can turn a reliability goal into an SLO, an error budget, and a burn-rate alert.",
      "outcome": "Make reliability measurable: define an SLI (a metric), set an SLO (a target), derive the error budget (1 − SLO), and compute the burn rate that tells you whether you are spending the budget too fast.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a service is \"reliable\" without an SLI and an SLO — reliability you cannot measure is an opinion, and the error budget is what makes it a decision."
    },
    {
      "text": "Leadership asks \"is the service reliable?\" and gets a shrug and a gut feeling. Then an incident burns through a month of acceptable downtime in a single afternoon and nobody notices until customers complain. Reliability has to be a NUMBER: an SLI you measure, an SLO you target, an error budget you spend, and a burn rate that alarms when you are spending it too fast. This lesson turns reliability into that arithmetic.",
      "type": "mission"
    },
    {
      "text": "Google's SRE practice makes reliability quantitative with three linked terms. An SLI (service level INDICATOR) is a measured metric of service health — request success rate, latency under a threshold, availability. An SLO (service level OBJECTIVE) is the TARGET for an SLI over a window — \"99.9% of requests succeed over 30 days.\" The ERROR BUDGET is the flip side: 1 − SLO, the amount of failure you are ALLOWED. A 99.9% SLO permits 0.1% failure, which over 30 days is about 43 minutes of downtime — that budget is a resource you spend on risk (deploys, experiments). The BURN RATE measures how fast you are consuming it: burning at 1× exactly exhausts the budget over the window; burning at 10× exhausts a month's budget in ~3 days, which should page someone. The discipline: if the budget is healthy, ship faster; if it is nearly spent, freeze risky changes until reliability recovers.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: your SLO is 99.9% success over 30 days. Roughly how much total downtime does the error budget allow, and what does a 99.9% target imply about acceptable failures?",
      "reveal": "99.9% success means 0.1% allowed failure. Over 30 days (≈ 43,200 minutes) that is about 43.2 minutes of allowed downtime/failure — the error budget. The 99.9% target explicitly ACCEPTS that ~0.1% of requests may fail; chasing 100% is infinitely expensive and pointless, so the budget is a deliberate allowance you spend on shipping and experiments. When it is nearly gone you slow down; when it is healthy you can take more risk."
    },
    {
      "code": "// SLO over a window → error budget (allowed bad time), then how fast you're burning it.\nfunction errorBudget(sloFraction, windowMinutes) {\n  return (1 - sloFraction) * windowMinutes;   // e.g. 0.001 * 43200 = 43.2 min allowed\n}\nfunction burnRate(actualBadMinutes, elapsedMinutes, sloFraction) {\n  // 1x burn spends the budget exactly over the window; >1x is too fast.\n  const allowedFractionOfTime = 1 - sloFraction;\n  const actualFractionOfTime = actualBadMinutes / elapsedMinutes;\n  return actualFractionOfTime / allowedFractionOfTime;\n}",
      "type": "worked-example",
      "intro": "From SLO and window to allowed downtime, budget consumed, and burn rate.",
      "steps": [
        "Error budget (minutes) = (1 − SLO) × window minutes — the failure you are allowed.",
        "Burn rate = (actual bad fraction of time) ÷ (allowed bad fraction of time).",
        "Burn rate 1× spends the budget exactly over the window; >1× spends it too fast.",
        "A high burn rate (e.g. ≥ 10×) is an early warning to freeze risky changes and alert."
      ],
      "language": "ts",
      "commonMistake": "Setting a 100% availability target (or none at all). 100% is unachievable and infinitely expensive, and having no SLO means no error budget — so there is no principled way to decide when to ship versus when to stop and fix reliability. Pick an achievable SLO and manage its budget."
    },
    {
      "text": "Observability makes a system's behavior inspectable through metrics, logs, and traces; Site Reliability Engineering turns that into decisions with three linked definitions. An SLI, the service level INDICATOR, is a concrete measured signal of health — the fraction of successful requests, the fraction served under a latency threshold, availability. An SLO, the service level OBJECTIVE, is a target for that indicator over a defined window: \"99.9% of requests succeed over 30 days.\" The pivotal reframe is the ERROR BUDGET, which is simply 1 − SLO: the amount of unreliability you have explicitly decided to ALLOW. A 99.9% SLO permits 0.1% failure, which over a 30-day window is roughly 43 minutes of downtime — and that is not a threat, it is a BUDGET, a resource you deliberately spend on the risk of shipping features, running experiments, and doing migrations. From it comes the BURN RATE: the ratio of how fast you are actually consuming the budget to the rate that would spend it exactly over the window. A burn rate of 1× exhausts the budget precisely at the end of the window; a burn rate of 10× means you are consuming a month's allowance in about three days, which should page an engineer because at that pace an incident is eating the whole budget in an afternoon. This is what makes reliability a decision rather than a feeling: when the error budget is healthy, the team can ship aggressively; when it is nearly spent or burning fast, risky changes freeze until reliability recovers. And the deeper point the error budget encodes is that 100% reliability is the wrong target — it is infinitely expensive and blocks all progress — so you choose an achievable SLO and then manage its budget as the shared currency between shipping speed and reliability.",
      "type": "concept",
      "title": "SLI measures, SLO targets, the error budget decides"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "slo",
          "from": "sli",
          "kind": "data",
          "tone": "accent",
          "label": "compare to target"
        },
        {
          "to": "budget",
          "from": "slo",
          "kind": "data",
          "tone": "success",
          "label": "1 − SLO"
        },
        {
          "to": "burn",
          "from": "budget",
          "kind": "data",
          "tone": "warning",
          "label": "consumption"
        },
        {
          "to": "ship",
          "from": "burn",
          "kind": "control",
          "tone": "success",
          "label": "low → ok"
        },
        {
          "to": "freeze",
          "from": "burn",
          "kind": "control",
          "tone": "warning",
          "label": "high → stop"
        }
      ],
      "nodes": [
        {
          "id": "sli",
          "kind": "process",
          "tone": "accent",
          "label": "SLI (measured)",
          "description": "success rate, latency"
        },
        {
          "id": "slo",
          "kind": "decision",
          "tone": "accent",
          "label": "SLO (target)",
          "description": "99.9% over 30d"
        },
        {
          "id": "budget",
          "kind": "store",
          "tone": "success",
          "label": "error budget = 1 − SLO",
          "description": "~43 min/30d"
        },
        {
          "id": "burn",
          "kind": "decision",
          "tone": "warning",
          "label": "burn rate",
          "description": "how fast spent"
        },
        {
          "id": "ship",
          "kind": "process",
          "tone": "success",
          "label": "ship faster",
          "description": "budget healthy"
        },
        {
          "id": "freeze",
          "kind": "process",
          "tone": "warning",
          "label": "freeze risky changes",
          "description": "budget burning fast"
        }
      ],
      "title": "SLI → SLO → error budget → burn rate",
      "rankdir": "LR",
      "subtitle": "reliability as a spendable budget",
      "storyboard": [
        {
          "ms": 6775,
          "say": "Start with what you can actually measure: the SLI, a concrete signal of health like the fraction of requests that succeed.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/observability-slos__b0.mp3",
          "edges": [],
          "nodes": [
            "sli"
          ]
        },
        {
          "ms": 8600,
          "say": "The SLI alone means nothing until you compare it to a target: the SLO, say 99.9 percent of requests succeed over 30 days.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/observability-slos__b1.mp3",
          "edges": [
            [
              "sli",
              "slo"
            ]
          ],
          "nodes": [
            "sli",
            "slo"
          ]
        },
        {
          "ms": 9675,
          "say": "Here is the reframe: one minus the SLO is your error budget, the failure you have explicitly decided to allow, about 43 minutes a month at 99.9.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/observability-slos__b2.mp3",
          "edges": [
            [
              "slo",
              "budget"
            ]
          ],
          "nodes": [
            "slo",
            "budget"
          ]
        },
        {
          "ms": 7550,
          "say": "Every failed request spends that budget, and the burn rate tells you how fast: an afternoon incident can torch a whole month's allowance.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/observability-slos__b3.mp3",
          "edges": [
            [
              "budget",
              "burn"
            ]
          ],
          "nodes": [
            "budget",
            "burn"
          ]
        },
        {
          "ms": 6600,
          "say": "The burn rate is a decision, not a dashboard: burning slow means budget to spare, so ship faster and take risks.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/observability-slos__b4.mp3",
          "edges": [
            [
              "burn",
              "ship"
            ]
          ],
          "nodes": [
            "burn",
            "ship"
          ]
        },
        {
          "ms": 8575,
          "say": "Burning hot means the budget is nearly gone, so freeze risky changes until it recovers; reliability is now a number you spend, not a gut feeling.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/observability-slos__b5.mp3",
          "edges": [
            [
              "burn",
              "freeze"
            ]
          ],
          "nodes": [
            "burn",
            "freeze"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "spike: short 50.0 long 20.0 -> page\nslow-burn: short 8.0 long 7.0 -> ticket\nhealthy: short 0.5 long 0.4 -> none",
      "title": "Build the error-budget + burn-rate calculator",
      "starter": "// Multi-window burn-rate alerting.\n// Page when both windows burn >= 14.4x; ticket when both >= 6x; else none.\nfunction windowBurn(badFraction, sloFraction) { /* bad fraction over allowed fraction */ }\nfunction alertDecision(shortBurn, longBurn) { /* page | ticket | none */ }\nconst SLO = 0.999;\nconst scenarios = [\n  { name: \"spike\", shortBad: 0.05, longBad: 0.02 },\n  { name: \"slow-burn\", shortBad: 0.008, longBad: 0.007 },\n  { name: \"healthy\", shortBad: 0.0005, longBad: 0.0004 },\n];\nfor (const s of scenarios) {\n  const sb = windowBurn(s.shortBad, SLO);\n  const lb = windowBurn(s.longBad, SLO);\n  console.log(s.name + \":\", \"short\", sb.toFixed(1), \"long\", lb.toFixed(1), \"->\", alertDecision(sb, lb));\n}",
      "summary": "Implement multi-window burn-rate alerting (Google SRE): convert a short-window and long-window bad-fraction into burn rates and decide page / ticket / none using the two-window thresholds (page at 14.4x, ticket at 6x, both windows must agree). Print each scenario's rates and decision.",
      "language": "js"
    },
    {
      "fix": "// Alert when the error budget is burning too fast, not on every failure.\nconst rate = burnRate(recentBadMinutes, recentWindowMinutes, SLO);\nif (rate >= 10) page(onCall); // fast burn: budget will be gone soon\n// slow, within-budget failures are expected and do not page.",
      "task": "Alerting per failure ignores the error budget and causes alert fatigue. Alert on burn rate instead.",
      "type": "debug",
      "symptom": "An alert fires on every single failed request, so the on-call is paged constantly and ignores the pager — then misses the incident that actually burns the whole error budget.",
      "language": "ts",
      "brokenCode": "if (request.failed) page(onCall);   // alerts on every failure, no budget context"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Pick an achievable target (e.g. 99.9%) and treat the 0.1% as a budget to spend on shipping. When the budget is healthy you ship fast; when it burns, you slow down — a principled speed/reliability dial.",
        "label": "SLO + error budget"
      },
      "optionB": {
        "text": "Chase zero downtime. It sounds safe, but it is infinitely expensive, blocks all risky (i.e. all) changes, and gives no framework for deciding when it is OK to take a risk.",
        "label": "Aim for 100%"
      },
      "guidance": "Set an SLO with an error budget (A). 100% is unattainable and freezes progress; the error budget makes the speed-vs-reliability tradeoff explicit and lets the team spend allowed unreliability deliberately. The right number is the lowest reliability your users are actually fine with, not the highest you can imagine.",
      "question": "Your team wants to ship features fast, but reliability matters. Target 100% availability, or set an SLO with an error budget?"
    },
    {
      "type": "quiz",
      "answer": 0,
      "options": [
        "About 43 minutes of allowed failure a month — a budget you spend on the risk of deploys and experiments.",
        "Thirty days of downtime, since the SLO window is 30 days and that is the period the budget is measured across.",
        "Zero — a 99.9% target means essentially no failures are permitted, so there is no budget left to spend on anything.",
        "Only a latency figure, not availability; the budget caps how slow requests may be, not how many may fail outright."
      ],
      "question": "Your SLO is 99.9% over 30 days. What is the error budget and what is it for?",
      "explanation": "The error budget is 1 − SLO = 0.1%, which over ~43,200 minutes (30 days) is about 43 minutes of allowed failure. It is a resource, not a failure: you spend it on the risk of deploys and experiments, and you slow down when it is nearly gone. 99.9% explicitly allows 0.1% failure — it is not zero."
    },
    {
      "type": "verification",
      "intro": "Your reliability is measurable when:",
      "items": [
        "You have an SLI (a real measured metric) for each critical journey.",
        "Each SLI has an achievable SLO with a defined window.",
        "You compute the error budget (1 − SLO) as allowed downtime.",
        "You alert on burn rate, not on every individual failure."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain the relationship between SLI, SLO, and error budget.",
        "Explain why a burn-rate alert is better than paging on every failure."
      ]
    },
    {
      "text": "The error budget is the scoreboard for everything in this module: blast-radius containment and resilience patterns exist to protect it, and it is what tells you whether they worked. The capstone must state an SLO and show its design fits the budget.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "SLI and SLO defined for a journey",
        "Error budget computed as (1 − SLO) × window",
        "Burn rate computed and used for alerting"
      ]
    }
  ],
  "rate-limiting-load-shedding": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A token-bucket rate limiter that refills at a steady rate, allows bursts up to capacity, and admits or rejects each request based on available tokens.",
      "unlock": "You can size a token bucket for a rate and burst, and explain how load shedding preserves the core under overload.",
      "outcome": "Protect a service from overload: implement a token-bucket rate limiter (rate + burst) and understand graceful load shedding — dropping or degrading low-priority work to keep the core serving.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a rate limiter just \"caps requests\" — a token bucket separates sustained rate from burst allowance, and load shedding chooses WHAT to drop."
    },
    {
      "text": "A traffic spike or a misbehaving client hammers your API, and with no rate limit the service tips over — taking down every user, not just the abusive one. Rate limiting is the valve that admits a sustained rate while tolerating short bursts, and load shedding is the triage that drops low-priority work first so the core survives. This lesson builds the token-bucket limiter that makes both precise.",
      "type": "mission"
    },
    {
      "text": "Two mechanisms keep a service alive under more load than it can handle. RATE LIMITING caps how much a client (or the whole system) may do. The TOKEN BUCKET is the standard algorithm: a bucket holds up to CAPACITY tokens and refills at a steady REFILL RATE (tokens/sec); each request costs one token — if a token is available the request is admitted and a token is removed, otherwise it is rejected (or throttled). This elegantly separates two concerns: the refill rate bounds the SUSTAINED average, while the capacity bounds the BURST (a full bucket lets that many requests through instantly). Contrast the leaky bucket, which shapes output to a constant rate with no burst. LOAD SHEDDING is what you do when even the allowed load exceeds capacity: rather than degrade uniformly (everyone slow) or crash (everyone down), you DROP or degrade the lowest-priority work first — shed background/optional requests, serve cached/reduced responses — so the critical path keeps working. Both are graceful-degradation tools: fail some requests deliberately to keep the system serving the rest.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: a token bucket has capacity 10 and refills 2 tokens/sec. It starts full. 10 requests arrive instantly, then 4 more one second later. How many are admitted?",
      "reveal": "First burst: the bucket starts full with 10 tokens, so all 10 instant requests are admitted (bucket now empty). One second later the bucket has refilled 2 tokens (2 tokens/sec × 1s). Of the 4 new requests, 2 are admitted (using both tokens) and 2 are rejected. Total: 12 admitted, 2 rejected. The capacity (10) set the burst size; the refill rate (2/sec) set the sustained rate — exactly the two knobs a token bucket separates."
    },
    {
      "code": "class TokenBucket {\n  constructor(capacity, refillPerSec) {\n    this.capacity = capacity; this.tokens = capacity;   // start full\n    this.refillPerSec = refillPerSec; this.last = 0;\n  }\n  tryAdmit(now) {                                        // now in seconds\n    const elapsed = now - this.last; this.last = now;\n    this.tokens = Math.min(this.capacity, this.tokens + elapsed * this.refillPerSec); // lazy refill\n    if (this.tokens >= 1) { this.tokens -= 1; return true; }  // spend a token → admit\n    return false;                                             // empty → reject\n  }\n}",
      "type": "worked-example",
      "intro": "A token bucket: refill by elapsed time (capped at capacity), spend one token per admitted request.",
      "steps": [
        "The bucket starts full and refills lazily by elapsed time × refill rate, capped at capacity.",
        "Capacity bounds the BURST; refill rate bounds the SUSTAINED average.",
        "Each admitted request spends one token; an empty bucket rejects (or throttles).",
        "Load shedding decides WHAT to drop when even allowed load exceeds capacity — lowest priority first."
      ],
      "language": "ts",
      "commonMistake": "Conflating rate and burst — using one fixed limit per interval (a fixed window). A fixed window allows a full quota at the boundary AND another full quota just after, so two windows' worth can arrive back-to-back. The token bucket cleanly separates sustained rate (refill) from burst (capacity), avoiding that boundary spike."
    },
    {
      "text": "A service has finite capacity, and two mechanisms keep it from being destroyed by more demand than it can serve. RATE LIMITING throttles how fast requests are admitted, and the TOKEN BUCKET is the workhorse algorithm because it captures two distinct limits with one structure. Picture a bucket that holds up to CAPACITY tokens and is refilled at a constant REFILL RATE of r tokens per second; every request must take one token to proceed, and if the bucket is empty the request is rejected or delayed. The refill rate r governs the SUSTAINED throughput — over the long run you cannot exceed r requests/sec, because that is how fast tokens arrive. The capacity b governs the BURST — a bucket that has sat idle is full, so up to b requests can be admitted in an instant before the rate limit bites. That separation is exactly what real APIs want (\"100 requests/sec sustained, but tolerate a burst of 100\"), and it is why the token bucket is preferred over a fixed-window counter (which allows a full quota on each side of a window boundary, permitting a 2× spike) and over the leaky bucket (which enforces a smooth constant output and permits no burst at all). When demand exceeds even the rate-limited capacity — a genuine overload — the answer is LOAD SHEDDING: instead of letting everything slow down together or crash, the system deliberately drops or degrades the LOWEST-priority work first (background jobs, optional enrichment, non-logged-in traffic) and protects the critical path, and it may serve cached or reduced responses to survive. Both are forms of graceful degradation: you choose to fail some requests on purpose so the system keeps serving the rest, rather than failing all of them by accident.",
      "type": "concept",
      "title": "The token bucket separates rate from burst; shedding chooses what to drop"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "bucket",
          "from": "req",
          "kind": "sync",
          "tone": "accent",
          "label": "arrive"
        },
        {
          "to": "bucket",
          "from": "refill",
          "kind": "data",
          "tone": "success",
          "label": "add tokens"
        },
        {
          "to": "admit",
          "from": "bucket",
          "kind": "control",
          "tone": "warning",
          "label": "has token?"
        },
        {
          "to": "serve",
          "from": "admit",
          "kind": "sync",
          "tone": "success",
          "label": "yes"
        },
        {
          "to": "shed",
          "from": "admit",
          "kind": "control",
          "tone": "warning",
          "label": "no → reject"
        }
      ],
      "nodes": [
        {
          "id": "req",
          "kind": "client",
          "tone": "accent",
          "label": "incoming requests"
        },
        {
          "id": "bucket",
          "kind": "store",
          "tone": "accent",
          "label": "token bucket",
          "description": "capacity + refill rate"
        },
        {
          "id": "refill",
          "kind": "process",
          "tone": "success",
          "label": "refill r/sec",
          "description": "sustained rate"
        },
        {
          "id": "admit",
          "kind": "decision",
          "tone": "warning",
          "label": "token available?"
        },
        {
          "id": "serve",
          "kind": "service",
          "tone": "success",
          "label": "admit → serve"
        },
        {
          "id": "shed",
          "kind": "external",
          "tone": "warning",
          "label": "reject / shed low priority",
          "description": "protect the core"
        }
      ],
      "title": "Token bucket + load shedding",
      "rankdir": "LR",
      "subtitle": "refill sets sustained rate, capacity sets burst; overload sheds low priority",
      "storyboard": [
        {
          "ms": 8975,
          "say": "Requests arrive at whatever rate clients feel like sending, but your service can only survive a finite rate, so every request first hits the token bucket.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/rate-limiting-load-shedding__b0.mp3",
          "edges": [
            [
              "req",
              "bucket"
            ]
          ],
          "nodes": [
            "req",
            "bucket"
          ]
        },
        {
          "ms": 8375,
          "say": "Meanwhile a refill process drips tokens into the bucket at r per second; that steady drip is the sustained rate you are willing to serve long-term.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/rate-limiting-load-shedding__b1.mp3",
          "edges": [
            [
              "refill",
              "bucket"
            ]
          ],
          "nodes": [
            "refill",
            "bucket"
          ]
        },
        {
          "ms": 9026,
          "say": "The bucket holds at most capacity tokens, so a quiet period banks up to b tokens; that stored slack is exactly how big a burst you can absorb at once.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/rate-limiting-load-shedding__b2.mp3",
          "edges": [],
          "nodes": [
            "bucket"
          ]
        },
        {
          "ms": 7375,
          "say": "Every request must withdraw one token, so admission reduces to a single question: is there a token in the bucket right now?",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/rate-limiting-load-shedding__b3.mp3",
          "edges": [
            [
              "bucket",
              "admit"
            ]
          ],
          "nodes": [
            "bucket",
            "admit"
          ]
        },
        {
          "ms": 6600,
          "say": "If a token is there, the request takes it and gets served; within budget, users never notice the limiter exists.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/rate-limiting-load-shedding__b4.mp3",
          "edges": [
            [
              "admit",
              "serve"
            ]
          ],
          "nodes": [
            "admit",
            "serve"
          ]
        },
        {
          "ms": 8675,
          "say": "If the bucket is empty you are past both budgets, so the request is rejected — and under overload you shed low-priority work first to protect what matters.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/rate-limiting-load-shedding__b5.mp3",
          "edges": [
            [
              "admit",
              "shed"
            ]
          ],
          "nodes": [
            "admit",
            "shed"
          ]
        },
        {
          "ms": 9100,
          "say": "One bucket encodes two limits: refill rate caps sustained throughput, capacity caps the burst, and shedding turns overload into triage instead of collapse.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/rate-limiting-load-shedding__b6.mp3",
          "edges": [
            [
              "refill",
              "bucket"
            ],
            [
              "admit",
              "shed"
            ]
          ],
          "nodes": [
            "refill",
            "bucket",
            "shed"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "low admitted: 7 rejected: 3\nhigh admitted: 3 rejected: 0",
      "title": "Implement a token-bucket rate limiter",
      "starter": "// Token bucket that reserves `reservedForHigh` tokens for high-priority requests.\n// Low-priority requests may only spend down to the reserved floor.\nclass PriorityLimiter {\n  constructor(capacity, refillPerSec, reservedForHigh) { /* tokens, last time */ }\n  tryAdmit(priority, now) { /* lazy refill; low stops at the reserved floor, high may use all */ }\n}\nconst lim = new PriorityLimiter(10, 0, 3);\nlet lowAdmitted = 0, lowRejected = 0;\nfor (let i = 0; i < 10; i++) (lim.tryAdmit(\"low\", 0) ? lowAdmitted++ : lowRejected++);\nlet highAdmitted = 0, highRejected = 0;\nfor (let i = 0; i < 3; i++) (lim.tryAdmit(\"high\", 0) ? highAdmitted++ : highRejected++);\nconsole.log(\"low admitted:\", lowAdmitted, \"rejected:\", lowRejected);\nconsole.log(\"high admitted:\", highAdmitted, \"rejected:\", highRejected);",
      "summary": "Add priority load shedding: a token bucket that reserves capacity for high-priority traffic, so low-priority requests may only draw the bucket down to a reserved floor while high-priority may use it all. Send 10 low then 3 high at a drained moment and print admits/rejects per class.",
      "language": "js"
    },
    {
      "fix": "// Token bucket: refill ~1.67 tokens/sec (100/min), capacity caps the burst.\nconst tb = new TokenBucket(100, 100 / 60);\nif (tb.tryAdmit(nowSeconds())) admit(); else reject();\n// sustained ~100/min; burst bounded by capacity, no boundary doubling.",
      "task": "The fixed window allows a full quota on each side of the boundary. Replace it with a token bucket that separates rate from burst.",
      "type": "debug",
      "symptom": "A \"100 requests per minute\" fixed-window limiter still lets clients briefly send 200 requests in a couple of seconds, overwhelming the backend.",
      "language": "ts",
      "brokenCode": "// fixed window: reset a counter every 60s\nif (count < 100) { count++; admit(); } else reject();\n// at 59.9s a client sends 100, then 100 more at 60.1s → 200 in ~0.2s"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Refill 100 tokens/sec with a capacity that permits a burst. Clients get a sustained 100/sec AND can spike briefly when the bucket is full — ideal for user-facing APIs with bursty traffic.",
        "label": "Token bucket"
      },
      "optionB": {
        "text": "Shape output to a constant 100/sec, queuing or dropping anything above it. Perfectly smooth downstream load and no bursts — good for protecting a fragile downstream, but it forbids the burst you wanted to allow.",
        "label": "Leaky bucket"
      },
      "guidance": "Use the token bucket (A) when you want a sustained rate WITH burst tolerance — the common case for public APIs. Use the leaky bucket (B) when the downstream must see a perfectly smooth rate and bursts would harm it. The deciding question is whether bursts are desirable (token) or dangerous (leaky).",
      "question": "Your API needs 100 req/sec sustained but should tolerate short bursts. Token bucket or leaky bucket?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Both control the same thing — together they just set one overall cap on the total number of requests allowed.",
        "Refill rate sets the sustained long-run rate; capacity sets the maximum burst that a full bucket admits at once.",
        "Neither one affects bursts; a token bucket smooths all traffic to a constant rate and forbids bursts entirely.",
        "Refill rate sets the burst size, while capacity governs the per-request latency each admitted request experiences."
      ],
      "question": "In a token bucket, what do the refill rate and the capacity control, respectively?",
      "explanation": "The refill rate (tokens/sec) bounds the long-term SUSTAINED rate, because that is how fast tokens replenish. The capacity bounds the BURST, because a full bucket lets that many requests through instantly. Separating these two knobs is exactly why the token bucket is preferred over a single fixed-window limit."
    },
    {
      "type": "verification",
      "intro": "Your overload protection is sound when:",
      "items": [
        "A token bucket enforces a sustained rate and a bounded burst.",
        "You sized capacity for the burst and refill for the sustained rate.",
        "Under true overload, low-priority work is shed to protect the core.",
        "Rejected requests fail cleanly (429/backoff), not by crashing the service."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain how capacity and refill rate give you burst and sustained-rate control separately.",
        "Explain how load shedding preserves the critical path under overload."
      ]
    },
    {
      "text": "Rate limiting and shedding are the last line of the reliability module — they cap the input so the resilience patterns and error budget are not overwhelmed. The token bucket reappears in the capstone as the front-door limiter, and its shedding logic protects the core under a computed load spike.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Token bucket enforces sustained rate + burst",
        "Capacity and refill sized to the requirement",
        "Overload sheds low-priority work first"
      ]
    }
  ],
  "tradeoff-analysis-adrs": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A tradeoff scorer that ranks design options against weighted criteria and produces the decision an ADR would record.",
      "unlock": "You can turn a fuzzy \"which architecture?\" argument into a scored decision with recorded rejected alternatives.",
      "outcome": "Make and record architectural decisions deliberately: score options against weighted criteria, know when NOT to distribute (a monolith often wins), and capture the decision + rejected alternatives in an ADR.",
      "intensity": "standard",
      "doNotClaim": "Don't claim the \"best\" architecture in the abstract — the best option is the highest weighted score against THIS system's criteria, and it is often the simplest."
    },
    {
      "text": "A team spends weeks debating microservices vs monolith by trading opinions, then splits into 12 services and drowns in distributed-systems complexity for a product with 500 users — a decision no one wrote down or scored. Architecture is a series of tradeoffs, and the senior skill is making them EXPLICIT: weight the criteria, score the options, pick the winner, and record it (with the rejected alternatives) in an ADR. This lesson turns architecture arguments into a defensible calculation.",
      "type": "mission"
    },
    {
      "text": "An architect's core output is DECISIONS under conflicting constraints, and two disciplines keep them honest. TRADEOFF ANALYSIS: enumerate the real options, define the criteria that matter for THIS system (e.g. simplicity, scalability, team velocity, operational cost), WEIGHT them by importance, score each option per criterion, and pick the highest weighted total — which forces the conversation onto explicit priorities instead of preferences. A recurring result is \"do NOT distribute\": a MONOLITH is simpler to build, deploy, debug, and reason about, and for most systems (small team, moderate scale) it beats microservices, whose independent scaling and deployment come at the cost of network calls, distributed data, and operational overhead you should only pay when scale or org size demands it. The ARCHITECTURE DECISION RECORD (ADR) captures each significant decision as a short document: the context, the decision, the alternatives considered and WHY they were rejected, and the consequences — so future engineers understand the reasoning rather than re-litigating it.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: a 3-person team building a new product with a few hundred users is choosing between a monolith and 10 microservices. Which usually wins, and what criterion decides it?",
      "reveal": "The MONOLITH usually wins for a small team at low scale. The deciding criteria are SIMPLICITY and TEAM VELOCITY: a monolith is one deploy, one codebase, in-process calls, and easy local debugging, so a small team ships far faster and operates far less. Microservices buy independent scaling and deployment — valuable at large scale or with many teams — but at that size they impose distributed-systems overhead (network failure, data consistency, ops) that a 3-person team cannot afford. You distribute when scale or org structure FORCES it, not by default."
    },
    {
      "code": "// Weighted decision matrix: score each option per criterion (0-5), weight the criteria.\nfunction scoreOption(scores, weights) {\n  return Object.keys(weights).reduce((sum, c) => sum + (scores[c] || 0) * weights[c], 0);\n}\nfunction decide(options, weights) {\n  const ranked = options\n    .map(o => ({ name: o.name, total: scoreOption(o.scores, weights) }))\n    .sort((a, b) => b.total - a.total);\n  return { chosen: ranked[0], rejected: ranked.slice(1) };   // ADR records both\n}",
      "type": "worked-example",
      "intro": "Score options against weighted criteria; the highest weighted total is the recorded decision.",
      "steps": [
        "Enumerate real options (monolith, modular monolith, microservices).",
        "Define and WEIGHT the criteria that matter for this system.",
        "Score each option per criterion; compute the weighted total.",
        "Record the chosen option AND the rejected alternatives (with scores) in an ADR."
      ],
      "language": "ts",
      "commonMistake": "Choosing microservices by default (\"that's how scalable systems are built\") without weighing simplicity and team size. For most systems the distributed-systems tax — network failure, data consistency, deployment orchestration — outweighs the scaling benefit you do not yet need; the monolith is frequently the correct, boring answer."
    },
    {
      "text": "Architecture is the practice of making decisions under conflicting constraints, and its two disciplines are scoring the tradeoff and recording it. TRADEOFF ANALYSIS replaces \"I prefer X\" with a weighted decision matrix: list the genuine options, name the criteria that actually matter for THIS system and its stage — simplicity, scalability, team velocity, operational cost, time-to-market — assign each criterion a WEIGHT reflecting its importance here (a pre-product-market-fit startup weights velocity and simplicity heavily; a hyperscale platform weights scalability), score every option against every criterion, and choose the highest weighted total. The value is not the arithmetic precision but that it forces the argument onto explicit priorities: disagreements become disagreements about weights, which are discussable, instead of clashing intuitions. A result this method surfaces again and again is WHEN NOT TO DISTRIBUTE. A monolith — one codebase, one deployment, in-process calls, transactional local data, trivial local debugging — is dramatically simpler than a set of microservices, and simplicity is a first-class virtue. Microservices buy independent scaling, independent deployment, and team autonomy, but they impose the entire distributed-systems tax this course has been about: network calls that fail, data split across services with no cross-service transaction, and heavy operational machinery. You should pay that tax only when SCALE or ORGANIZATION size genuinely forces it, not because microservices sound modern. Finally, every significant decision is captured in an ARCHITECTURE DECISION RECORD: a short, dated document stating the context, the decision, the alternatives considered and precisely why each was rejected, and the consequences accepted. The ADR is what lets a future engineer understand why the system is the way it is — and prevents the team from re-fighting a settled decision because no one remembered the reasoning.",
      "type": "concept",
      "title": "The best architecture is the highest weighted score — often the simplest"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "score",
          "from": "options",
          "kind": "sync",
          "tone": "accent",
          "label": "evaluate"
        },
        {
          "to": "score",
          "from": "criteria",
          "kind": "control",
          "tone": "accent",
          "label": "weight"
        },
        {
          "to": "chosen",
          "from": "score",
          "kind": "sync",
          "tone": "success",
          "label": "max total"
        },
        {
          "to": "rejected",
          "from": "score",
          "kind": "control",
          "tone": "muted",
          "label": "lower totals"
        },
        {
          "to": "adr",
          "from": "chosen",
          "kind": "data",
          "tone": "success",
          "label": "record"
        },
        {
          "to": "adr",
          "from": "rejected",
          "kind": "data",
          "tone": "muted",
          "label": "record why"
        }
      ],
      "nodes": [
        {
          "id": "options",
          "kind": "client",
          "tone": "accent",
          "label": "options",
          "description": "monolith / microservices"
        },
        {
          "id": "criteria",
          "kind": "process",
          "tone": "accent",
          "label": "weighted criteria",
          "description": "simplicity, scale, velocity"
        },
        {
          "id": "score",
          "kind": "decision",
          "tone": "warning",
          "label": "weighted scores"
        },
        {
          "id": "chosen",
          "kind": "store",
          "tone": "success",
          "label": "chosen option",
          "description": "highest total"
        },
        {
          "id": "rejected",
          "kind": "external",
          "tone": "muted",
          "label": "rejected alternatives",
          "description": "with reasons"
        },
        {
          "id": "adr",
          "kind": "store",
          "tone": "success",
          "label": "ADR",
          "description": "context + decision + consequences"
        }
      ],
      "title": "Weighted tradeoff decision → ADR",
      "rankdir": "LR",
      "subtitle": "score options against weighted criteria, record the winner and the rejected",
      "storyboard": [
        {
          "ms": 6800,
          "say": "Big architecture calls fail when teams trade opinions; this flow replaces debate with a scored, written decision.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/tradeoff-analysis-adrs__b0.mp3",
          "edges": [],
          "nodes": [
            "options",
            "adr"
          ]
        },
        {
          "ms": 7550,
          "say": "Start by enumerating the genuine options on the table, like monolith versus microservices, not just the loudest proposal.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/tradeoff-analysis-adrs__b1.mp3",
          "edges": [],
          "nodes": [
            "options"
          ]
        },
        {
          "ms": 8225,
          "say": "Then define the criteria that matter for this system and weight them: a pre-market-fit startup weights simplicity and velocity, not hyperscale.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/tradeoff-analysis-adrs__b2.mp3",
          "edges": [],
          "nodes": [
            "criteria"
          ]
        },
        {
          "ms": 8750,
          "say": "Every option is evaluated against every weighted criterion, producing a total score that turns preference into arithmetic you can argue about.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/tradeoff-analysis-adrs__b3.mp3",
          "edges": [
            [
              "options",
              "score"
            ],
            [
              "criteria",
              "score"
            ]
          ],
          "nodes": [
            "options",
            "criteria",
            "score"
          ]
        },
        {
          "ms": 7550,
          "say": "The highest weighted total wins; the lower totals become explicit rejected alternatives instead of silently forgotten paths.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/tradeoff-analysis-adrs__b4.mp3",
          "edges": [
            [
              "score",
              "chosen"
            ],
            [
              "score",
              "rejected"
            ]
          ],
          "nodes": [
            "score",
            "chosen",
            "rejected"
          ]
        },
        {
          "ms": 6700,
          "say": "Both outcomes flow into an ADR: record the winner, and just as importantly, record why each alternative lost.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/tradeoff-analysis-adrs__b5.mp3",
          "edges": [
            [
              "chosen",
              "adr"
            ],
            [
              "rejected",
              "adr"
            ]
          ],
          "nodes": [
            "chosen",
            "rejected",
            "adr"
          ]
        },
        {
          "ms": 8750,
          "say": "The value is not the arithmetic, it is the forced conversation about weights, plus a written record the next engineer can revisit when constraints change.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/tradeoff-analysis-adrs__b6.mp3",
          "edges": [],
          "nodes": [
            "criteria",
            "adr"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "monolith total: 42\nmicroservices total: 19\nscalability weight to flip: 9",
      "title": "Build the tradeoff scorer",
      "starter": "// Sensitivity analysis: find the smallest integer weight for `criterion`\n// at which the runner-up overtakes the winner.\nfunction weightedTotal(scores, weights) { /* sum score*weight over criteria */ }\nfunction flipPoint(winner, runnerUp, weights, criterion) { /* first weight where runnerUp wins */ }\nconst weights = { simplicity: 3, velocity: 3, scalability: 1, opsCost: 2 };\nconst monolith = { name: \"monolith\", scores: { simplicity: 5, velocity: 5, scalability: 2, opsCost: 5 } };\nconst micro = { name: \"microservices\", scores: { simplicity: 2, velocity: 2, scalability: 5, opsCost: 1 } };\nconsole.log(\"monolith total:\", weightedTotal(monolith.scores, weights));\nconsole.log(\"microservices total:\", weightedTotal(micro.scores, weights));\nconsole.log(\"scalability weight to flip:\", flipPoint(monolith, micro, weights, \"scalability\"));",
      "summary": "The ADR field \"what would change our mind\": after scoring, run a sensitivity analysis to find how high the scalability weight would have to rise for microservices to overtake the monolith. Print both totals and the flip point.",
      "language": "js"
    },
    {
      "fix": "// ADR-014: Adopt microservices for the billing domain\n// Context: billing scaled to 50k TPS; team split into 4 squads.\n// Decision: extract billing into its own service.\n// Alternatives rejected: modular monolith (couldn't scale billing independently).\n// Consequences: cross-service transactions replaced with sagas; +ops cost accepted.",
      "task": "The decision was never recorded with its rationale and alternatives, so it gets re-litigated. Capture it as an ADR.",
      "type": "debug",
      "symptom": "Six months after a big architecture decision, no one remembers why microservices were chosen over a monolith, and the team keeps re-arguing it in every planning meeting.",
      "language": "ts",
      "brokenCode": "// the only record of the decision:\ngit log --oneline | grep \"split into services\"  // a commit message, no rationale"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "One codebase and deploy; ship fast, debug locally, use transactions. You can extract services later WHEN scale or team growth forces it. Lowest complexity for the current stage.",
        "label": "Start with a monolith"
      },
      "optionB": {
        "text": "Split into services now for \"future scale.\" You get independent scaling you do not need yet, and pay the full distributed-systems tax (network failure, split data, ops) while still tiny.",
        "label": "Microservices upfront"
      },
      "guidance": "Start with a monolith (A) — for a small team at low scale, simplicity and velocity dominate, and the monolith wins the weighted score. Distribute (B) only when a measured bottleneck or org structure forces it; premature microservices spend your scarce team on distributed-systems overhead instead of the product. Record the decision in an ADR either way.",
      "question": "A new product with a small team must pick an architecture. Default to microservices for future scale, or start with a monolith?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Only when the language lacks networking support, forcing all of the code to run inside a single process by necessity.",
        "For a small team at moderate scale, where simplicity and shipping velocity outweigh independent per-service scaling.",
        "Only for programs with no database, since a monolith cannot coordinate data across more than one separate datastore.",
        "Never — microservices scale better in essentially every case, so they are the correct default for any serious system."
      ],
      "question": "When is a monolith often the RIGHT architectural choice over microservices?",
      "explanation": "Microservices buy independent scaling and deployment at the cost of the distributed-systems tax (network failures, split data, heavy ops). For a small team at moderate scale, a monolith's simplicity, transactional data, and fast iteration win the weighted tradeoff. You distribute when scale or org size forces it — not by default."
    },
    {
      "type": "verification",
      "intro": "You decide like an architect when:",
      "items": [
        "Options are scored against weighted, system-specific criteria.",
        "You considered and can defend NOT distributing.",
        "The decision and rejected alternatives are recorded in an ADR.",
        "Future engineers can understand the reasoning without re-arguing it."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain how a weighted decision matrix turns an architecture argument into a decision.",
        "Explain why a monolith is often the correct choice and when to distribute."
      ]
    },
    {
      "text": "This scoring discipline is how the interview method (next) chooses among designs and how the capstone justifies its stores, caches, and consistency choices — each is a recorded tradeoff, not a default. Every design decision you make from here should be defensible as a weighted choice.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Options scored against weighted criteria",
        "The not-distribute option seriously considered",
        "Decision + rejected alternatives recorded as an ADR"
      ]
    }
  ],
  "the-interview-method": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A design-step completeness checker that verifies a proposed design walked all six method steps in order and flags any it skipped.",
      "unlock": "You can run a design discussion end-to-end with a method, and catch when you skipped a step.",
      "outcome": "Drive a system-design discussion with a repeatable method: frame requirements → estimate → high-level design → deep-dive a component → find the bottleneck → discuss tradeoffs — and check your process is complete.",
      "intensity": "standard",
      "doNotClaim": "Don't claim you \"designed the system\" if you jumped to boxes — a complete design walks frame → estimate → high-level → deep-dive → bottleneck → tradeoff."
    },
    {
      "text": "In a design interview a candidate immediately starts drawing databases and caches, never asks a requirement, never estimates scale, and never identifies the bottleneck — and produces an impressive-looking diagram that answers no actual question. The difference between senior and junior here is METHOD: a repeatable sequence that guarantees you frame before you build and find the bottleneck before you optimize. This lesson makes that method a checklist you can verify you followed.",
      "type": "mission"
    },
    {
      "text": "Whether in an interview or a real design doc, strong system design follows a repeatable METHOD rather than improvisation. The six steps: (1) FRAME — clarify functional and non-functional requirements and constraints (the first lesson's skill); (2) ESTIMATE — back-of-envelope the scale (RPS, storage, bandwidth) so the design is sized to reality; (3) HIGH-LEVEL DESIGN — sketch the major components and the request path (the building blocks); (4) DEEP-DIVE — pick one component and detail it (the data model, the cache strategy, the sharding); (5) BOTTLENECK — identify where this design will break FIRST under the estimated load and address it; (6) TRADEOFFS — state what you optimized for and what you gave up (consistency vs availability, cost vs latency). Running these in order is what separates a designed system from a pile of boxes: you frame before building, size before choosing, and find the bottleneck before optimizing the wrong thing.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: a candidate opens a design interview by drawing \"client → API → database → cache.\" What crucial steps did they skip, and why does it matter?",
      "reveal": "They skipped FRAME (no requirements — what does it do, at what scale, how consistent?) and ESTIMATE (no RPS/storage numbers). It matters because the architecture is DERIVED from requirements and scale: without them, the boxes are ungrounded guesses that might be wildly over- or under-built. They also have not identified the BOTTLENECK, so they cannot know whether the cache even addresses the real constraint. A diagram without frame, estimate, and bottleneck answers no actual question."
    },
    {
      "code": "const METHOD = [\"frame\", \"estimate\", \"high-level\", \"deep-dive\", \"bottleneck\", \"tradeoff\"];\n// Check a design transcript walked all steps in the canonical order.\nfunction checkMethod(stepsTaken) {\n  const missing = METHOD.filter(s => !stepsTaken.includes(s));\n  // order check: the taken steps, filtered to method steps, must match canonical order\n  const seq = stepsTaken.filter(s => METHOD.includes(s));\n  const canonical = METHOD.filter(s => seq.includes(s));\n  const inOrder = seq.join(\",\") === canonical.join(\",\");\n  return { complete: missing.length === 0, missing, inOrder };\n}",
      "type": "worked-example",
      "intro": "The six-step method and a checker that verifies all steps were walked in order.",
      "steps": [
        "Frame requirements and constraints FIRST — never start with boxes.",
        "Estimate scale so the design is sized to real numbers.",
        "Sketch the high-level components, then deep-dive one.",
        "Find the bottleneck before optimizing, then state the tradeoffs you accepted."
      ],
      "language": "ts",
      "commonMistake": "Jumping straight to the high-level design (drawing components) and skipping frame + estimate. Without requirements and scale numbers, you cannot justify a single component or know where the bottleneck is — you have produced a diagram, not a design, and it usually answers the wrong question."
    },
    {
      "text": "System design done well is a repeatable PROCESS, and the process is what distinguishes a senior answer from a junior one — juniors draw a familiar diagram; seniors run a method that guarantees the diagram answers the actual question. The six steps, in order: FRAME the problem by clarifying functional requirements, non-functional requirements (scale, latency, availability, consistency), and constraints — because everything downstream is derived from these. ESTIMATE the scale with back-of-the-envelope math (peak RPS, storage/year, bandwidth) so every subsequent choice is sized to reality rather than taste. Produce a HIGH-LEVEL DESIGN: the major components and the request path, at the altitude of boxes and arrows. Then DEEP-DIVE into one component that matters — the data model and its sharding key, the cache strategy and its invalidation, the consistency mechanism — showing you can go from the map to the mechanism. Identify the BOTTLENECK: given the estimated load, where does THIS design saturate first (the single database? the hot key? the synchronous fan-out?), and address it specifically — this is the step that separates real engineering from box-drawing, because optimizing anything else is wasted. Finally, state the TRADEOFFS: what you optimized for and what you consciously gave up (you chose availability over strong consistency for the feed; you chose a cache and accepted staleness), because a design without acknowledged tradeoffs is hiding its weaknesses. Running these in sequence is the point: framing before building prevents solving the wrong problem, estimating before choosing prevents mis-sizing, and finding the bottleneck before optimizing prevents polishing a component that was never the constraint. The method is also self-checking — you can literally verify you walked all six steps and catch the one you skipped.",
      "type": "concept",
      "title": "A design is a method walked in order, not a diagram drawn from memory"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "estimate",
          "from": "frame",
          "kind": "sync",
          "tone": "accent",
          "label": "then"
        },
        {
          "to": "highlevel",
          "from": "estimate",
          "kind": "sync",
          "tone": "accent",
          "label": "then"
        },
        {
          "to": "deepdive",
          "from": "highlevel",
          "kind": "sync",
          "label": "then"
        },
        {
          "to": "bottleneck",
          "from": "deepdive",
          "kind": "control",
          "tone": "warning",
          "label": "then"
        },
        {
          "to": "tradeoff",
          "from": "bottleneck",
          "kind": "sync",
          "tone": "success",
          "label": "then"
        }
      ],
      "nodes": [
        {
          "id": "frame",
          "kind": "process",
          "tone": "accent",
          "label": "1 frame",
          "description": "requirements + constraints"
        },
        {
          "id": "estimate",
          "kind": "process",
          "tone": "accent",
          "label": "2 estimate",
          "description": "RPS, storage, bandwidth"
        },
        {
          "id": "highlevel",
          "kind": "service",
          "tone": "default",
          "label": "3 high-level",
          "description": "components + path"
        },
        {
          "id": "deepdive",
          "kind": "store",
          "tone": "default",
          "label": "4 deep-dive",
          "description": "one component in detail"
        },
        {
          "id": "bottleneck",
          "kind": "decision",
          "tone": "warning",
          "label": "5 bottleneck",
          "description": "where it breaks first"
        },
        {
          "id": "tradeoff",
          "kind": "decision",
          "tone": "success",
          "label": "6 tradeoffs",
          "description": "what you gave up"
        }
      ],
      "title": "The six-step design method",
      "rankdir": "LR",
      "subtitle": "frame → estimate → high-level → deep-dive → bottleneck → tradeoff",
      "storyboard": [
        {
          "ms": 8426,
          "say": "A junior starts drawing databases immediately; a senior runs a six-step method that guarantees the diagram answers the actual question.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/the-interview-method__b0.mp3",
          "edges": [],
          "nodes": [
            "frame",
            "estimate",
            "highlevel",
            "deepdive",
            "bottleneck",
            "tradeoff"
          ]
        },
        {
          "ms": 8525,
          "say": "Step one, frame: clarify functional requirements, non-functional targets, and constraints, because everything downstream is derived from these answers.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/the-interview-method__b1.mp3",
          "edges": [],
          "nodes": [
            "frame"
          ]
        },
        {
          "ms": 8175,
          "say": "Then estimate: back-of-envelope math on peak requests per second, storage, and bandwidth, so the design is sized to reality instead of taste.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/the-interview-method__b2.mp3",
          "edges": [
            [
              "frame",
              "estimate"
            ]
          ],
          "nodes": [
            "estimate"
          ]
        },
        {
          "ms": 7850,
          "say": "Only now do you sketch the high-level design: the major components and the request path, at whiteboard altitude, not implementation detail.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/the-interview-method__b3.mp3",
          "edges": [
            [
              "estimate",
              "highlevel"
            ]
          ],
          "nodes": [
            "highlevel"
          ]
        },
        {
          "ms": 7925,
          "say": "Then deep-dive into the pieces that matter most for this problem, like the data model and storage choices, using the requirements you framed.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/the-interview-method__b4.mp3",
          "edges": [
            [
              "highlevel",
              "deepdive"
            ]
          ],
          "nodes": [
            "deepdive"
          ]
        },
        {
          "ms": 8275,
          "say": "Next, hunt the bottleneck: with your estimates in hand, ask which component breaks first at the stated scale, that risk is where the design earns its keep.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/the-interview-method__b5.mp3",
          "edges": [
            [
              "deepdive",
              "bottleneck"
            ]
          ],
          "nodes": [
            "bottleneck"
          ]
        },
        {
          "ms": 7425,
          "say": "Finish by naming the tradeoffs you made and what you would revisit; the method, not the diagram, is what separates senior from junior.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/the-interview-method__b6.mp3",
          "edges": [
            [
              "bottleneck",
              "tradeoff"
            ]
          ],
          "nodes": [
            "tradeoff"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "skipped: frame,estimate\nrushed: bottleneck\ntotalMinutes: 28",
      "title": "Build the design-method completeness checker",
      "starter": "const METHOD = [\"frame\", \"estimate\", \"high-level\", \"deep-dive\", \"bottleneck\", \"tradeoff\"];\nconst MIN_MINUTES = { frame: 3, estimate: 3, \"high-level\": 5, \"deep-dive\": 8, bottleneck: 3, tradeoff: 3 };\n// Flag skipped (absent or 0) and rushed (below the minimum) steps; total the minutes.\nfunction reviewTranscript(spent) { /* return { skipped, rushed, total } */ }\nconst spent = { \"high-level\": 12, \"deep-dive\": 10, bottleneck: 2, tradeoff: 4 };\nconst r = reviewTranscript(spent);\nconsole.log(\"skipped:\", r.skipped.join(\",\") || \"none\");\nconsole.log(\"rushed:\", r.rushed.join(\",\") || \"none\");\nconsole.log(\"totalMinutes:\", r.total);",
      "summary": "Grade the method on TIME, not just presence: given minutes spent per step, flag steps that were skipped (0 or absent) and steps that were rushed (below their minimum), and report total minutes. Run it on a transcript that skips frame + estimate and rushes the bottleneck.",
      "language": "js"
    },
    {
      "fix": "drawComponents();\nconst bottleneck = findBottleneck(estimatedLoad); // e.g. the synchronous 3rd-party call\naddress(bottleneck);   // make it async / add a timeout; THEN optimize what remains\n// find the bottleneck BEFORE optimizing anything.",
      "task": "The process skipped the bottleneck-identification step and optimized the wrong component. Fix the method.",
      "type": "debug",
      "symptom": "A design review keeps optimizing the caching layer, but latency does not improve — because the real bottleneck is a synchronous third-party call nobody identified.",
      "language": "ts",
      "brokenCode": "// review process:\ndrawComponents();\noptimizeCache();   // jumped to optimizing without finding the bottleneck"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Cover frame → estimate → high-level → deep-dive → bottleneck → tradeoff, even briefly. You demonstrate you can size, find the real constraint, and reason about tradeoffs — the senior signal.",
        "label": "Walk the full method"
      },
      "optionB": {
        "text": "Produce a beautiful, deep component diagram. Impressive locally, but if you skipped framing and the bottleneck, you may have optimized the wrong thing and answered a question no one asked.",
        "label": "Polish one detailed diagram"
      },
      "guidance": "Walk the full method (A). A complete-but-shallow pass that frames, sizes, finds the bottleneck, and names tradeoffs beats a gorgeous diagram that skipped the steps that make a design correct. Depth on one component matters, but only AFTER you have framed and found the real constraint.",
      "question": "You have limited time in a design discussion. Spend it polishing a detailed diagram, or walking the full method at lower detail?"
    },
    {
      "type": "quiz",
      "answer": 0,
      "options": [
        "The architecture is derived from scale, and optimizing a non-bottleneck wastes effort on what is not the constraint.",
        "The bottleneck is always the database, so no analysis is needed — you can optimize it and skip the estimate step.",
        "Estimation is optional busywork; any component you optimize will help, so the order of the steps does not matter.",
        "Bottlenecks only reveal themselves after launch under real production traffic, so there is nothing to find in design."
      ],
      "question": "In the design method, why must you estimate scale and identify the bottleneck before optimizing a component?",
      "explanation": "Scale estimates size the design (10 RPS vs 100k RPS are different systems), and the bottleneck is where the design saturates first under that load. Optimizing anything other than the bottleneck polishes a component that was not the constraint, so latency does not improve — which is exactly the failure mode the method prevents by ordering estimate and bottleneck before optimization."
    },
    {
      "type": "verification",
      "intro": "You run the method when:",
      "items": [
        "You frame requirements and constraints before drawing anything.",
        "You estimate scale before choosing components.",
        "You identify the bottleneck before optimizing.",
        "You state the tradeoffs you consciously accepted."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "List the six steps in order and what each produces.",
        "Explain why finding the bottleneck must precede optimization."
      ]
    },
    {
      "text": "This method is the scaffold for the capstone: you will frame its requirements, estimate its load, design the components, deep-dive the data layer, find the bottleneck, and defend the tradeoffs — and its computed checks verify each step landed. It is also exactly how you should approach any real design doc.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "All six method steps walked in order",
        "Scale estimated before component choice",
        "Bottleneck identified before optimization"
      ]
    }
  ],
  "designing-a-real-system": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A composed shortener that generates unique short codes, caches redirects, shards by code, and rate-limits creation — with computed checks proving each invariant holds.",
      "unlock": "You can compose the course's primitives into one coherent system and verify it behaves correctly.",
      "outcome": "Apply the whole ladder to one end-to-end design — a URL shortener — composing id generation, caching, sharding, and rate limiting into a working request path and verifying its invariants.",
      "intensity": "standard",
      "doNotClaim": "Don't claim the pieces work because each passed alone — a real system is proven by the invariants that hold when they COMPOSE."
    },
    {
      "text": "Every prior lesson built one primitive: estimation, caching, sharding, rate limiting, consistency. A real system is not any one of them — it is all of them COMPOSED on one request path, and the proof it works is the INVARIANTS that hold across the composition: every short code is unique, every redirect resolves, load spreads across shards, and creation is rate-limited. This lesson designs a URL shortener end-to-end and verifies those invariants with computed checks.",
      "type": "mission"
    },
    {
      "text": "A URL shortener is the canonical end-to-end design because it exercises the whole ladder in a small space. CREATE: take a long URL, generate a UNIQUE short code (a base-62 encoding of a monotonic id, or a hashed id), and store the mapping. REDIRECT (the hot path, read-heavy): look up the code and 301/302 to the long URL — served from a CACHE for speed, falling back to the sharded store on a miss. SCALE: the mapping table is SHARDED by short code (consistent hashing) so writes and storage spread across nodes; the redirect cache absorbs the read majority. PROTECT: creation is RATE-LIMITED (token bucket) so no client can exhaust the id space or hammer the store. The design's correctness is defined by INVARIANTS: (1) uniqueness — no two long URLs get the same code; (2) resolvability — every created code redirects to its URL; (3) balanced sharding — codes spread across shards; (4) rate-limited creation — bursts beyond capacity are rejected. Composing the primitives and checking these invariants is what \"designing a real system\" means.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: for a URL shortener, which primitives from this course does the CREATE path need, and which does the REDIRECT path need?",
      "reveal": "CREATE needs: unique id/code generation (uniqueness invariant), a sharded store to spread writes (sharding), and a rate limiter to bound creation (token bucket). REDIRECT needs: a cache for the hot read path (caching/hit rate), with a fallback to the sharded store on a miss (sharding), and it is the read-heavy path so it dominates the capacity estimate. The two paths compose the estimation, caching, sharding, and rate-limiting lessons into one system — which is exactly what makes the shortener the canonical end-to-end exercise."
    },
    {
      "code": "// base-62 short code from a monotonic id → uniqueness by construction.\nconst B62 = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\";\nfunction encode(n) { let s = \"\"; do { s = B62[n % 62] + s; n = Math.floor(n / 62); } while (n > 0); return s; }\nfunction shardFor(code, shards) {                       // deterministic shard by code\n  let h = 0; for (const c of code) h = (h * 31 + c.charCodeAt(0)) >>> 0;\n  return shards[h % shards.length];\n}",
      "type": "worked-example",
      "intro": "Compose id-gen, sharding, cache, and rate limiting into one shortener and verify its invariants.",
      "steps": [
        "Generate a unique code by base-62 encoding a monotonic id — uniqueness by construction.",
        "Shard the mapping by code so writes and storage spread across nodes.",
        "Serve redirects from a cache; fall back to the sharded store on a miss.",
        "Rate-limit creation with a token bucket; verify every invariant holds after composing."
      ],
      "language": "ts",
      "commonMistake": "Testing each primitive in isolation and assuming the system works. The real bugs live in the composition — a redirect that the cache serves but the shard never stored, or a code collision under concurrent creation — so correctness must be proven by the system-level INVARIANTS, not by each part passing alone."
    },
    {
      "text": "Designing a real system is the act of COMPOSING the primitives the course built into one coherent request path, and its correctness is defined not by the parts but by the INVARIANTS that must hold across the whole. Take the URL shortener. Its CREATE path generates a short code and stores a mapping; the natural technique is to encode a monotonically increasing id in base-62 (0-9, a-z, A-Z), which yields short, URL-safe codes that are UNIQUE by construction because each id is used once — this satisfies the uniqueness invariant without a collision check. The mapping is SHARDED by short code (via consistent hashing) so that as the system grows, writes and storage distribute across nodes rather than piling on one — the balanced-sharding invariant. Its REDIRECT path is the hot, read-heavy path: a lookup of code → long URL, served from a CACHE so the overwhelming read majority never touches the store, with a fallback to the correct shard on a miss — and the resolvability invariant demands that every code ever created still redirects to its URL, whether from cache or store. Creation is RATE-LIMITED with a token bucket so no client can flood the creation path or race the id generator — the rate-limited-creation invariant. The crucial mindset shift at this level is that verifying each primitive alone is NOT verifying the system: the interesting failures are compositional — a cache that returns a value the shard never persisted, a code that two concurrent creates both claim, a redirect to a shard the router computed differently than the writer did. So the proof of a real design is a set of computed, system-level checks: create some URLs and assert all codes are unique; redirect each and assert every one resolves to the right URL; count codes per shard and assert they spread; and fire a creation burst past the limiter and assert the excess is rejected. When those invariants hold across the composed path, you have designed a system — not assembled boxes.",
      "type": "concept",
      "title": "A real system is composed primitives, proven by its invariants"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "limiter",
          "from": "client",
          "kind": "sync",
          "tone": "warning",
          "label": "create"
        },
        {
          "to": "idgen",
          "from": "limiter",
          "kind": "sync",
          "tone": "accent",
          "label": "admitted"
        },
        {
          "to": "shards",
          "from": "idgen",
          "kind": "data",
          "tone": "success",
          "label": "store mapping"
        },
        {
          "to": "cache",
          "from": "client",
          "kind": "data",
          "tone": "accent",
          "label": "redirect"
        },
        {
          "to": "shards",
          "from": "cache",
          "kind": "data",
          "label": "miss → shard"
        },
        {
          "to": "redirect",
          "from": "cache",
          "kind": "sync",
          "tone": "success",
          "label": "resolve"
        }
      ],
      "nodes": [
        {
          "id": "client",
          "kind": "client",
          "tone": "accent",
          "label": "client"
        },
        {
          "id": "limiter",
          "kind": "decision",
          "tone": "warning",
          "label": "rate limiter",
          "description": "token bucket on create"
        },
        {
          "id": "idgen",
          "kind": "process",
          "tone": "accent",
          "label": "id → base62 code",
          "description": "unique by construction"
        },
        {
          "id": "cache",
          "kind": "store",
          "tone": "success",
          "label": "redirect cache",
          "description": "hot read path"
        },
        {
          "id": "shards",
          "kind": "store",
          "tone": "success",
          "label": "sharded mapping store",
          "description": "by code (consistent hash)"
        },
        {
          "id": "redirect",
          "kind": "external",
          "tone": "default",
          "label": "301 to long URL"
        }
      ],
      "title": "The composed URL shortener",
      "rankdir": "LR",
      "subtitle": "create + redirect paths over shards, cache, and a limiter",
      "storyboard": [
        {
          "ms": 6626,
          "say": "One client, two request paths: writing a new short link, and the read-heavy redirect that serves every click after it.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/designing-a-real-system__b0.mp3",
          "edges": [
            [
              "client",
              "limiter"
            ],
            [
              "client",
              "cache"
            ]
          ],
          "nodes": [
            "client"
          ]
        },
        {
          "ms": 7550,
          "say": "Creates hit the rate limiter first, so a burst of write traffic can never starve the system or mint codes faster than it can store them.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/designing-a-real-system__b1.mp3",
          "edges": [
            [
              "client",
              "limiter"
            ],
            [
              "limiter",
              "idgen"
            ]
          ],
          "nodes": [
            "client",
            "limiter"
          ]
        },
        {
          "ms": 8600,
          "say": "Admitted creates take a monotonic id, encode it in base sixty-two, and store the mapping — each id is used once, so uniqueness holds by construction.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/designing-a-real-system__b2.mp3",
          "edges": [
            [
              "idgen",
              "shards"
            ]
          ],
          "nodes": [
            "idgen",
            "shards"
          ]
        },
        {
          "ms": 8025,
          "say": "The mapping lands in a store sharded by short code with consistent hashing, so writes and storage spread across nodes as the system grows.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/designing-a-real-system__b3.mp3",
          "edges": [
            [
              "idgen",
              "shards"
            ]
          ],
          "nodes": [
            "shards"
          ]
        },
        {
          "ms": 7325,
          "say": "Redirects are the hot path: the client hits the cache, and on a hit we answer straight away with a 301 to the long URL.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/designing-a-real-system__b4.mp3",
          "edges": [
            [
              "client",
              "cache"
            ],
            [
              "cache",
              "redirect"
            ]
          ],
          "nodes": [
            "client",
            "cache",
            "redirect"
          ]
        },
        {
          "ms": 8000,
          "say": "On a miss, the cache falls back to the owning shard, fills itself, and still resolves — slower, but never wrong, because the store is the source of truth.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/designing-a-real-system__b5.mp3",
          "edges": [
            [
              "cache",
              "shards"
            ],
            [
              "cache",
              "redirect"
            ]
          ],
          "nodes": [
            "cache",
            "shards",
            "redirect"
          ]
        },
        {
          "ms": 9675,
          "say": "The design is the composition: limiter, id generator, shards, and cache each solve one problem, and the invariants — unique codes, correct redirects — hold across all of them.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/designing-a-real-system__b6.mp3",
          "edges": [],
          "nodes": [
            "limiter",
            "idgen",
            "shards",
            "cache"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "created: 5 rejected: 2\nuniqueCodes: true\nnoDanglingCacheEntry: true\nperShard: 1,2,2",
      "title": "Compose the shortener and verify its invariants",
      "starter": "const B62 = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\";\nfunction toCode(n) { let s = \"\"; do { s = B62[n % 62] + s; n = Math.floor(n / 62); } while (n > 0); return s; }\nfunction pickShard(code, shardCount) { let h = 0; for (const c of code) h = (h * 31 + c.charCodeAt(0)) >>> 0; return h % shardCount; }\nclass Shortener {\n  constructor(shardCount, limiterCapacity) { /* shards[], cache, tokens, nextId */ }\n  create(url) { /* limiter check, then store to shard BEFORE cache; return code or null */ }\n  resolve(code) { /* cache first, then shard */ }\n}\nconst s = new Shortener(3, 5);\nconst urls = [\"a\", \"b\", \"c\", \"d\", \"e\", \"f\", \"g\"];\nconst codes = urls.map(u => s.create(u));\nconst created = codes.filter(c => c !== null);\nconst rejected = codes.filter(c => c === null).length;\nconst unique = new Set(created).size === created.length;\nconst noDangling = [...s.cache.keys()].every(c => s.resolve(c) !== undefined && s.shards[pickShard(c, 3)].has(c));\nconst perShard = Array.from({ length: 3 }, (_, i) => s.shards[i].size).join(\",\");\nconsole.log(\"created:\", created.length, \"rejected:\", rejected);\nconsole.log(\"uniqueCodes:\", unique);\nconsole.log(\"noDanglingCacheEntry:\", noDangling);\nconsole.log(\"perShard:\", perShard);",
      "summary": "Compose the shortener and prove a coherence invariant the parts alone cannot: with id-gen, sharding, a cache, and a token-bucket limiter given, implement create (store to shard BEFORE cache, behind the limiter) and resolve (cache then shard). Create 7 URLs through a capacity-5 limiter and verify no cached code is missing from its shard.",
      "language": "js"
    },
    {
      "fix": "// Both paths MUST shard by the SAME key — the short code.\nconst writeShard = shardFor(code, shards);\nshards[writeShard][code] = longUrl;\nconst readShard = shardFor(code, shards);  // same function, same key → same shard\nreturn shards[readShard][code];",
      "task": "The write path and read path shard by different keys, so the reader looks in the wrong shard. Fix the composition so both use the same shard key.",
      "type": "debug",
      "symptom": "Some short links 404 even though creation reported success — the redirect finds nothing for a code that was definitely created.",
      "language": "ts",
      "brokenCode": "// writer shards by user_id, reader shards by code:\nconst writeShard = shardFor(userId, shards);   // stored here\nconst readShard = shardFor(code, shards);       // looked up here — different shard!"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Encode an ever-increasing id. Codes are unique by construction (no collision check needed) and compact, but they are sequential — guessable and they leak how many links exist.",
        "label": "Monotonic counter → base-62"
      },
      "optionB": {
        "text": "Generate random codes and check the store for collisions before committing. Unguessable and non-sequential, but you must handle (rare) collisions with a retry and pay a lookup per create.",
        "label": "Random string + collision check"
      },
      "guidance": "Use the counter (A) when uniqueness-by-construction and compactness matter and enumeration is acceptable (or you obscure the counter with an encoding/offset). Use random codes (B) when guessability is a real concern (private links) and accept the collision-check cost. Both satisfy the uniqueness invariant; choose by whether enumeration is a threat.",
      "question": "For generating short codes, use a monotonic counter encoded to base-62, or a random string with a collision check?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "Invariants are just a formality; a thorough suite of unit tests on each individual primitive already proves the system.",
        "Interesting bugs are compositional — a reader and writer sharding by different keys — caught only by whole-path invariants.",
        "Primitives never contain bugs once they have been written, so the only place a real defect can hide is in the glue code between them.",
        "A system genuinely cannot be tested at all until it is deployed, so invariants are the only check available beforehand."
      ],
      "question": "Why is a composed system verified by system-level invariants rather than by each primitive passing in isolation?",
      "explanation": "Each primitive can be individually correct while their COMPOSITION is broken — a code stored on one shard but looked up on another, a cache serving a value the store never had. Only invariants over the whole path (uniqueness, resolvability, balanced sharding, rate-limited creation) catch these compositional failures, which is why they define the design's correctness."
    },
    {
      "type": "verification",
      "intro": "Your composed system is correct when:",
      "items": [
        "Every generated short code is unique.",
        "Every created code redirects to its exact long URL (cache or shard).",
        "Codes spread across shards without a hotspot.",
        "Creation beyond the rate limit is rejected, not accepted."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Explain which course primitives the create and redirect paths compose.",
        "Explain why the read and write paths must shard by the same key."
      ]
    },
    {
      "text": "This is the dress rehearsal for the capstone: the same compose-then-verify-invariants discipline, applied to a system YOU specify from a spec, is exactly what the final lesson asks you to design and defend with computed checks.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "Primitives composed into one request path",
        "All four invariants verified by computed checks",
        "Read and write paths use a consistent shard key"
      ]
    }
  ],
  "capstone-design-and-defend": [
    {
      "time": "60–90 min",
      "type": "sprint-contract",
      "proof": "A design validator that takes a spec + a proposed design and computes whether it passes every gate: capacity, consistency, cache, and failure containment.",
      "unlock": "You can produce a design whose correctness is proven by computed checks against the requirements, not asserted.",
      "outcome": "Design a scalable system to a spec and DEFEND it with computed checks: capacity fits the budget, the store/consistency choice matches the data, the cache meets its hit-rate target, and a component failure is contained.",
      "intensity": "standard",
      "doNotClaim": "Don't claim a design is \"scalable\" — prove it: the capacity fits, the consistency matches the data, the cache hits its target, and a failure is contained, each by a computed check."
    },
    {
      "text": "Given a spec — a read-heavy news feed at high scale with a mix of correctness-critical and availability-tolerant data — you must produce a design AND prove it holds up: that its capacity estimate fits the infrastructure budget, that its store choices match each data type's consistency needs, that its cache clears the hit-rate target the read-heavy load requires, and that a single component failure is contained. This capstone is the whole course as one graded, computed defense — no vibes, only checks.",
      "type": "mission"
    },
    {
      "text": "The capstone is the entire ladder applied and DEFENDED. You take a written spec (traffic, data types, budget, availability target) and produce a design, then subject it to GATES that are each a computed check derived from an earlier lesson: (1) CAPACITY — the estimated peak RPS and storage (back-of-envelope) must fit within the provisioned budget (servers × per-server capacity), or the design is under-provisioned. (2) CONSISTENCY — each data type must be assigned a store/model matching its needs: correctness-critical data gets a CP/quorum store, availability-tolerant data gets an AP/eventual store (CAP). (3) CACHE — for a read-heavy workload the cache hit rate must clear a target (e.g. ≥ 0.8) so the origin is not overwhelmed (caching). (4) FAILURE CONTAINMENT — a designated non-critical component's failure must have a bounded blast radius (bulkheads), not a system-wide one. A design PASSES only when every gate's computed check passes; that is what \"design and defend\" means — the defense is arithmetic, not assertion.",
      "type": "context"
    },
    {
      "type": "pretest",
      "prompt": "Before reading: you claim a design is \"scalable and reliable.\" What four computed checks would actually PROVE that, drawing on this course?",
      "reveal": "Four gates: (1) CAPACITY — computed peak RPS and storage fit the provisioned budget (estimation). (2) CONSISTENCY — each data type is on a store whose model matches it: CP for correctness-critical, AP for availability-tolerant (CAP). (3) CACHE — the hit rate meets the read-heavy target so the origin is protected (caching). (4) CONTAINMENT — a non-critical component's failure has a bounded blast radius (bulkheads). \"Scalable and reliable\" is proven when all four computed checks pass — not when the diagram looks impressive."
    },
    {
      "code": "// Each gate is a boolean computed from the spec + design.\nfunction capacityGate(spec, design) {\n  const provisioned = design.servers * design.rpsPerServer;\n  return spec.peakRps <= provisioned && design.storageBudgetGB >= spec.storageNeededGB;\n}\nfunction consistencyGate(spec, design) {\n  return spec.dataTypes.every(dt =>\n    dt.correctnessCritical ? design.stores[dt.name] === \"CP\" : design.stores[dt.name] === \"AP\");\n}\nfunction cacheGate(spec, design) { return design.cacheHitRate >= spec.cacheHitTarget; }\nfunction containmentGate(spec, design) { return design.blastRadius[spec.nonCriticalComponent] <= spec.maxBlastRadius; }",
      "type": "worked-example",
      "intro": "Run every gate as a computed check; the design passes only if all pass.",
      "steps": [
        "Capacity gate: provisioned RPS ≥ peak RPS AND storage budget ≥ storage needed.",
        "Consistency gate: every correctness-critical type on CP, every tolerant type on AP.",
        "Cache gate: the cache hit rate meets the read-heavy target.",
        "Containment gate: the non-critical component's blast radius is within the allowed bound."
      ],
      "language": "ts",
      "commonMistake": "Defending a design with narrative (\"we use Kafka, so it scales\") instead of computed checks. Scalability and reliability are properties you PROVE against the spec's numbers — capacity fits, consistency matches, cache clears its target, failure is contained — and a design that cannot pass those computed gates is not done, however good the story sounds."
    },
    {
      "text": "The capstone crystallizes the course's founding principle — proof, not vibes — into a design you must DEFEND with arithmetic. You start from a spec: the traffic (DAU, actions, peak factor), the data types and their consistency needs, the infrastructure budget, and the availability/hit-rate targets. You produce a design: the stores, the cache, the sharding, the failure isolation. Then you subject that design to a series of GATES, each a computed check inherited directly from an earlier lesson, and the design is valid only if ALL of them pass. The CAPACITY gate takes the back-of-envelope peak RPS and storage estimate and checks they fit the provisioned budget (servers × per-server RPS, storage budget) — an under-provisioned design fails here regardless of how elegant it is. The CONSISTENCY gate walks each data type and verifies it sits on a store whose model matches its nature: correctness-critical data (money, inventory) on a CP/quorum store, availability-tolerant data (feeds, counts) on an AP/eventual store — a mismatch (a bank balance on an AP store, or a like-count needlessly CP) fails the gate. The CACHE gate confirms the cache hit rate clears the target the read-heavy workload demands, so the origin is not overwhelmed. The CONTAINMENT gate confirms that a designated non-critical component's failure has a blast radius within the allowed bound — i.e. that bulkheads actually isolate it. This is the essence of staff-level design: you do not argue that a system is scalable and reliable, you DERIVE it — every claim is a computed check against a requirement, and a design that cannot pass all four gates is, by definition, not finished. Composing the whole course this way is the final proof that you can not only design a system but defend that it meets its spec.",
      "type": "concept",
      "title": "A defended design passes every computed gate, or it is not done"
    },
    {
      "type": "diagram",
      "edges": [
        {
          "to": "capacity",
          "from": "spec",
          "kind": "sync",
          "tone": "accent",
          "label": "check"
        },
        {
          "to": "consistency",
          "from": "capacity",
          "kind": "control",
          "tone": "warning",
          "label": "and"
        },
        {
          "to": "cache",
          "from": "consistency",
          "kind": "control",
          "tone": "warning",
          "label": "and"
        },
        {
          "to": "containment",
          "from": "cache",
          "kind": "control",
          "tone": "warning",
          "label": "and"
        },
        {
          "to": "verdict",
          "from": "containment",
          "kind": "sync",
          "tone": "success",
          "label": "all pass"
        }
      ],
      "nodes": [
        {
          "id": "spec",
          "kind": "client",
          "tone": "accent",
          "label": "spec + design",
          "description": "traffic, data, budget, targets"
        },
        {
          "id": "capacity",
          "kind": "decision",
          "tone": "warning",
          "label": "capacity gate",
          "description": "peak RPS ≤ provisioned"
        },
        {
          "id": "consistency",
          "kind": "decision",
          "tone": "warning",
          "label": "consistency gate",
          "description": "CP/AP per data type"
        },
        {
          "id": "cache",
          "kind": "decision",
          "tone": "warning",
          "label": "cache gate",
          "description": "hit rate ≥ target"
        },
        {
          "id": "containment",
          "kind": "decision",
          "tone": "warning",
          "label": "containment gate",
          "description": "blast radius bounded"
        },
        {
          "id": "verdict",
          "kind": "store",
          "tone": "success",
          "label": "design PASSES",
          "description": "all gates true"
        }
      ],
      "title": "The capstone defense gates",
      "rankdir": "LR",
      "subtitle": "spec + design → four computed gates → pass/fail",
      "storyboard": [
        {
          "ms": 7425,
          "say": "Everything starts with the spec: traffic numbers, data types, budget, and availability targets — plus the design you built to meet them.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/capstone-design-and-defend__b0.mp3",
          "edges": [],
          "nodes": [
            "spec"
          ]
        },
        {
          "ms": 7975,
          "say": "First, the capacity gate: your back-of-envelope peak RPS and storage must fit the provisioned budget — servers times per-server capacity.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/capstone-design-and-defend__b1.mp3",
          "edges": [
            [
              "spec",
              "capacity"
            ]
          ],
          "nodes": [
            "spec",
            "capacity"
          ]
        },
        {
          "ms": 8000,
          "say": "Pass capacity and the consistency gate takes over: every data type must land in a store whose guarantees match its correctness needs.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/capstone-design-and-defend__b2.mp3",
          "edges": [
            [
              "capacity",
              "consistency"
            ]
          ],
          "nodes": [
            "capacity",
            "consistency"
          ]
        },
        {
          "ms": 6425,
          "say": "Next, the cache gate: your hit-rate math must show the read-heavy load actually stays off the backing stores at peak.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/capstone-design-and-defend__b3.mp3",
          "edges": [
            [
              "consistency",
              "cache"
            ]
          ],
          "nodes": [
            "consistency",
            "cache"
          ]
        },
        {
          "ms": 8925,
          "say": "Then containment: when a shard or dependency fails, the blast radius must stay bounded — the design survives partial failure by arithmetic, not hope.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/capstone-design-and-defend__b4.mp3",
          "edges": [
            [
              "cache",
              "containment"
            ]
          ],
          "nodes": [
            "cache",
            "containment"
          ]
        },
        {
          "ms": 6775,
          "say": "Notice the gates are chained with AND — one failed check anywhere invalidates the whole design; there is no partial credit.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/capstone-design-and-defend__b5.mp3",
          "edges": [
            [
              "capacity",
              "consistency"
            ],
            [
              "consistency",
              "cache"
            ],
            [
              "cache",
              "containment"
            ]
          ],
          "nodes": [
            "capacity",
            "consistency",
            "cache",
            "containment"
          ]
        },
        {
          "ms": 7450,
          "say": "Only when all four computed checks pass does the design earn its verdict — that's the course's founding principle: proof, not vibes.",
          "audio": "https://hocrntqhgvmeaxwlhzwl.supabase.co/storage/v1/object/public/academy-voice/system-design/capstone-design-and-defend__b6.mp3",
          "edges": [
            [
              "containment",
              "verdict"
            ]
          ],
          "nodes": [
            "containment",
            "verdict"
          ]
        }
      ]
    },
    {
      "type": "lab",
      "check": "capacity: false\nconsistency: true\ncache: true\ncontainment: false\nVERDICT: FAIL\nfailedGates: capacity,containment",
      "title": "Validate a design against its spec",
      "starter": "// Every gate is a computed boolean. Containment must COMPUTE the blast radius\n// from the dependency graph, not read a stored number.\nfunction capacityOk(spec, design) { /* provisioned RPS >= peak AND storage fits */ }\nfunction consistencyOk(spec, design) { /* correctness-critical -> CP, tolerant -> AP */ }\nfunction cacheOk(spec, design) { /* hit rate meets target */ }\nfunction computeBlastRadius(dependents, failed) { /* count of dependents that fail with it */ }\nfunction containmentOk(spec, design) { /* computed blast radius within the allowed bound */ }\nconst spec = {\n  peakRps: 60000, storageNeededGB: 900,\n  dataTypes: [{ name: \"payments\", correctnessCritical: true }, { name: \"timeline\", correctnessCritical: false }],\n  cacheHitTarget: 0.85,\n  nonCriticalComponent: \"recommendations\", maxBlastRadius: 1,\n};\nconst design = {\n  servers: 50, rpsPerServer: 1000, storageBudgetGB: 1200,\n  stores: { payments: \"CP\", timeline: \"AP\" },\n  cacheHitRate: 0.9,\n  dependents: { recommendations: [\"productPage\"], productPage: [\"checkout\"], checkout: [] },\n};\nconst gates = {\n  capacity: capacityOk(spec, design),\n  consistency: consistencyOk(spec, design),\n  cache: cacheOk(spec, design),\n  containment: containmentOk(spec, design),\n};\nfor (const [k, v] of Object.entries(gates)) console.log(k + \":\", v);\nconst failed = Object.entries(gates).filter(([, v]) => !v).map(([k]) => k);\nconsole.log(\"VERDICT:\", failed.length === 0 ? \"PASS\" : \"FAIL\");\nconsole.log(\"failedGates:\", failed.join(\",\") || \"none\");",
      "summary": "Defend a design by computed gates against a spec where two gates FAIL — proving the gates actually discriminate. Implement capacity, consistency, cache, and containment, where containment COMPUTES the blast radius from the dependency graph (not a literal). Print each gate, the verdict, and which gates failed.",
      "language": "js"
    },
    {
      "fix": "const design = {\n  servers: 50, rpsPerServer: 1000,     // provisioned 50k >= 40k peak\n  stores: { account: \"CP\" },           // correctness-critical balance on a CP store\n};\n// run the gates and require PASS before calling it done:\nif (!capacityGate(spec, design) || !consistencyGate(spec, design)) throw new Error(\"design fails its spec\");",
      "task": "The design was asserted done without running the capacity and consistency gates, which it fails. Fix it so both gates pass.",
      "type": "debug",
      "symptom": "A design is declared \"done and scalable,\" but in production it is constantly under-provisioned at peak and the bank-balance data occasionally shows stale values.",
      "language": "ts",
      "brokenCode": "const design = { servers: 20, rpsPerServer: 1000,   // provisioned 20k\n  stores: { account: \"AP\" } };                       // balance on an AP store!\n// spec.peakRps = 40000; account is correctnessCritical\nconst done = true; // asserted, never checked against the spec"
    },
    {
      "type": "tradeoff",
      "optionA": {
        "text": "Run capacity, consistency, cache, and containment checks against the spec and fix any failure first. Slower to declare done, but the design provably meets its requirements — no production surprise.",
        "label": "After the gates pass"
      },
      "optionB": {
        "text": "Present a compelling design narrative and ship on confidence. Fast, but \"sounds scalable\" is not \"fits 40k peak RPS\" — an unchecked gate becomes a production incident.",
        "label": "After a good walkthrough"
      },
      "guidance": "Ship after the gates pass (A). The entire course exists to make \"scalable and reliable\" a computed property, not a story; a design that has not passed its capacity, consistency, cache, and containment checks is unfinished no matter how good the walkthrough. Proof over vibes, especially under deadline pressure.",
      "question": "Under deadline, ship the design after a convincing walkthrough, or after every gate's computed check passes?"
    },
    {
      "type": "quiz",
      "answer": 1,
      "options": [
        "It ships with the most detailed diagram in the room, since more boxes and arrows demonstrate a more rigorous design.",
        "Its key claims — capacity, consistency, cache hit rate, containment — each pass a computed check against the spec.",
        "It is built on trendy, well-marketed technologies whose reputation for scaling stands in for any computed check.",
        "A senior engineer signed off on it verbally, and their experience is treated as sufficient proof that it will hold up."
      ],
      "question": "What makes a system design \"defended\" rather than merely asserted?",
      "explanation": "A defended design derives its properties: capacity fits the budget, each data type's store matches its consistency need, the cache clears its hit-rate target, and a component failure is contained — each verified by a computed check against the spec. Trendy tech, pretty diagrams, and verbal approval are assertions; the gates are proof, which is the course's entire premise."
    },
    {
      "type": "verification",
      "intro": "Your design is defended when:",
      "items": [
        "The capacity gate passes: provisioned RPS and storage cover the peak estimate.",
        "The consistency gate passes: every data type is on a matching CP/AP store.",
        "The cache gate passes: the hit rate clears the read-heavy target.",
        "The containment gate passes: a non-critical failure has a bounded blast radius."
      ]
    },
    {
      "type": "teachback",
      "prompts": [
        "Name the four gates and the earlier lesson each one comes from.",
        "Explain why a design that cannot pass its computed gates is not finished."
      ]
    },
    {
      "text": "This gate-driven defense is how you should approve any real design doc: turn each \"-ility\" claim into a computed check against the requirements. Everything the course taught — estimation, storage, caching, distribution, reliability, and the architect's judgment — converges here into one provable artifact.",
      "type": "transfer"
    },
    {
      "type": "spaced-review",
      "schedule": [
        "1 day",
        "3 days",
        "1 week",
        "1 month"
      ]
    },
    {
      "type": "unlock-gate",
      "criteria": [
        "All four gates pass as computed checks",
        "Each data type matched to a consistency-appropriate store",
        "A non-critical failure is provably contained"
      ]
    }
  ]
}