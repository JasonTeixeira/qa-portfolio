# Design OS Scorecard — the autonomous loop's ledger

> Driven by [DESIGN_OS_PROGRAM.md](./DESIGN_OS_PROGRAM.md). The loop reads this each iteration, builds the lowest
> unblocked surface in the current stage, gates it, design-reviews to ≥95, scoped-commits, and updates the row.
> `design` = panel score (≥95 to pass). A surface is GREEN only at design ≥95 with all standing gates green.

## Locked direction (operator-signed 2026-06-27)
**Editorial dark-luxury** — near-black layered surfaces with real depth + hairline rules; serif display +
mono/grotesk body pairing; ONE disciplined accent used semantically; elevation/overlap/soft-shadow + grain;
slow compositor-only reveals. A premium dev magazine that happens to teach. (Coheres with the existing
disciplined-dark academy — evolve it, don't fight it.)

## Surfaces
| Stage | Surface | design | gates | status |
|---|---|---:|:---:|---|
| 0 | Token foundation (oklch palette + roles, type scale + pairing, spacing/radius/elevation, motion, grain) | built | tsc·build·a11y ✓ | **awaiting operator sign-off** |
| 0 | Reference surface (lesson header + callout, editorial dark-luxury) | rendered | a11y 7/7 | **awaiting operator sign-off** |
| 1 | Lesson player (the 20 block-types styled with character) | — | — | Stage 1 |
| 1 | Course overview (modules + lessons, editorial) | — | — | Stage 1 |
| 1 | Lab surface (the interactive Pyodide lab, beautiful) | — | — | Stage 1 |
| 2 | Reveal/scroll choreography + micro-interactions | — | — | Stage 2 |
| 3 | Academy-wide rollout + fold into COURSE_TEMPLATE.md | — | — | Stage 3 |

## Loop status
- **Next:** Stage 0 — token foundation + a reference surface. EXIT Stage 0 = tokens defined + reference ≥95 +
  **operator signs the direction on a rendered surface** (the first taste checkpoint).
- Autonomy bounds (from DESIGN_OS_PROGRAM §Autonomy bounds): build/gate/commit autonomously; PAUSE for taste,
  irreversible/external actions, a gate stuck after 3 rounds, and every stage boundary. Never push / never fake a gate.
