# Evidence Ledger — AI Engineering, RAG, Evals, Safety, LLMOps

**Course:** `career-ai_engineering_rag_eval` (20 lessons)
**Built:** 2026-07-03 · "Proof, not paper" — every source below was fetched via WebFetch and quoted from the real page. No URL, excerpt, or quote is invented.

## Summary

| Metric | Value |
|---|---|
| Load-bearing factual claims checked | 20 |
| VERIFIED (real authority confirms the specific claim) | 11 |
| QUALIFIED (emerging practice / true-with-caveat / not pinnable to one authority) | 9 |
| CORRECTED (course factually wrong) | 0 |
| Distinct real sources fetched | 13 |
| Tier-1 (standards / provider docs) | 11 |
| Tier-2 (papers / preprints) | 2 |

**On the QUALIFIED count.** AI engineering is largely emerging practice, not settled standard. Nine claims are QUALIFIED not because they are wrong, but because the domain lacks a single settled authority for them: prompt contracts, LLM-as-judge calibration, chunking strategy, per-request observability/SLOs, and treating model output as an untrusted boundary are consensus *engineering practice*, not codified standards. Where the course leans on a real authority (OWASP, NIST) it does so with unusually honest hedging — it repeatedly flags its own paraphrases in-line (e.g. "this is a paraphrase, not a NIST quote"; "not NIST's wording"). That in-course honesty is why there are **zero CORRECTED defects**: the course does not overclaim.

**Notable strength.** This course is a positive outlier for source discipline. It never attributes a fabricated quote to NIST/OWASP; it names the risk category and then explicitly marks the applied inference. Several lessons pre-empt the exact defect this audit hunts for.

---

## Module 0 — Foundations (Framing, Contracts, Structured Outputs, Ingestion)

| Claim (lesson) | Source | Verdict | Excerpt / Note |
|---|---|---|---|
| NIST's GenAI Profile treats confident fabrication (confabulation) as a named top risk to manage (`m00-l01-prompt-contracts`) | nist-ai-600-1-genai-profile (T1) | **VERIFIED** | "Confabulation: The production of confidently stated but erroneous or false content (known colloquially as 'hallucinations' or 'fabrications')." Course also correctly notes NIST does *not* prescribe abstention as the control — accurate hedge. |
| A grounded system should refuse the unsupported claim (prompt contract refusal clause) (`m00-l01-prompt-contracts`) | nist-ai-600-1-genai-profile (T1) | **QUALIFIED** | Refusal/abstention is sound engineering practice but is *not* NIST's prescribed control — the course itself flags this. No single authority mandates the refusal clause; it is consensus practice. |
| Structured output = a JSON Schema *enforced at decode time* (constrained decoding), not a prompt request (`m00-l02-structured-outputs`) | openai-structured-outputs (T1) | **VERIFIED** | "Structured Outputs is a feature that ensures the model will always generate responses that adhere to your supplied JSON Schema, so you don't need to worry about the model omitting a required key, or hallucinating an invalid enum value." |
| "Treat model output like an untrusted request body" — validate LLM output before downstream use (`m00-l02-structured-outputs`) | owasp-llm05-improper-output-handling (T1) | **VERIFIED** | "Improper Output Handling refers specifically to insufficient validation, sanitization, and handling of the outputs generated by large language models before they are passed downstream." Course honestly notes the *exact phrasing* "like a request body" is its own applied inference — correct. |
| Data provenance / attribution supports transparency & accountability; provenance is required to ground a citation (`m00-l03-document-ingestion`) | nist-ai-rmf-accountable-transparent (T1) | **VERIFIED** | "accountable and transparent" is one of NIST's seven trustworthy-AI characteristics. Course correctly notes NIST does *not* name "traceability" as a property — accurate hedge. |

## Module 1 — Retrieval (Chunking, Embeddings/Indexing, Retrieval Quality, Grounding)

| Claim (lesson) | Source | Verdict | Excerpt / Note |
|---|---|---|---|
| Un-versioned / un-attributable retrieval sources undermine provenance; NIST GenAI Profile flags this under Information Integrity (`m01-l00-chunking-metadata`) | nist-ai-600-1-information-integrity (T1) | **VERIFIED** | Profile names "Information Integrity" as a risk and includes action MS-2.5-006: "Verify ... that fine-tuning or retrieval-augmented generation data is grounded." Directly supports the chunk-versioning/provenance claim. |
| A stable deterministic chunk_id as upsert key makes re-ingestion idempotent (`m01-l00-chunking-metadata`) | — | **QUALIFIED** | Sound data-engineering practice; no AI standard codifies it. True with caveat, not pinnable to one authority. |
| An embedding is a vector; distance/cosine similarity measures relatedness (small distance = high relatedness) (`m01-l01-embeddings-indexing`) | openai-embeddings (T1) | **VERIFIED** | "An embedding is a vector (list) of floating point numbers. The distance between two vectors measures their relatedness. Small distances suggest high relatedness and large distances suggest low relatedness." |
| Distance metric must match vector normalization (normalized → cosine); metric mismatch silently reorders neighbors (`m01-l01-embeddings-indexing`) | openai-embeddings (T1) | **QUALIFIED** | The relatedness/cosine claim is VERIFIED by the provider doc; the specific "normalized→cosine vs dot-product mismatch" failure mode is correct vector-math practice but not stated in the fetched provider page. Emerging/implementation practice. |
| Retrieval quality = recall@k / MRR measured on a labeled (question→gold chunk) eval set (`m01-l02-retrieval-quality`) | nist-ai-rmf-measure-manage (T1) | **QUALIFIED** | recall@k and MRR are standard IR metrics (correct definitions in-lesson) but are not defined by a single AI authority; NIST's MEASURE function supports "measure before manage" generally. Course honestly labels its NIST phrasing a paraphrase. Metric definitions themselves = established IR practice, not a pinnable AI standard. |
| The MEASURE function's outcomes inform MANAGE — measure a risk before managing it (`m01-l02-retrieval-quality`, `m03-l03-security-privacy-ai`) | nist-ai-rmf-measure-manage (T1) | **VERIFIED** | "It uses knowledge relevant to AI risks identified in the MAP function and informs the MANAGE function." Course flags "you cannot manage a risk you haven't measured" as a paraphrase — accurate. |
| RAG = combining parametric + non-parametric (retrieved) memory for generation (`m01-l03-rag-answer-grounding`) | lewis-2020-rag (T2) | **VERIFIED** | "models which combine pre-trained parametric and non-parametric memory for language generation" — the foundational RAG paper (Lewis et al. 2020). Tier 2 (peer/preprint), cited with real arXiv URL. |

## Module 2 — Evals & Safety (Eval Datasets, LLM Judges, Guardrails, Tool Calling)

| Claim (lesson) | Source | Verdict | Excerpt / Note |
|---|---|---|---|
| Automated RAG evaluation can be reference-free, scoring faithfulness/relevance without human ground truth (RAGAS) (`m02-l00-eval-dataset-design`, `m02-l01-llm-judges-graders`) | es-2023-ragas (T2) | **VERIFIED** | "We introduce Ragas ... a framework for reference-free evaluation of Retrieval Augmented Generation (RAG) pipelines." Faithfulness is a named RAGAS dimension. Tier 2 preprint (arXiv:2309.15217). |
| An LLM judge is itself a model and hallucinates verdicts; report judge-vs-human agreement before it gates release (`m02-l01-llm-judges-graders`) | nist-ai-rmf-measure-manage (T1) | **QUALIFIED** | NIST MEASURE treats measurement validity as a risk (supports the spirit), but "calibrate the judge against human labels" is emerging eval practice, not a codified standard. Correct and important — just not pinnable to one authority. |
| A guardrail must be an out-of-band check that can independently return "blocked," not a system-prompt line (`m02-l02-safety-policy-guardrails`) | owasp-llm01-prompt-injection (T1) | **VERIFIED** | Prompt injection (LLM01:2025) exists because instructions and data share one channel; OWASP mitigation is defense-in-depth incl. output filtering + segregating untrusted content — enforcement outside the prompt. |
| Prompt injection is a *design assumption*, not an edge case (`m02-l02-safety-policy-guardrails`, `m02-l03-tool-calling`, `m03-l03-security-privacy-ai`) | owasp-llm01-prompt-injection (T1); owasp-llm-top10-2025 (T1) | **VERIFIED** | "A Prompt Injection Vulnerability occurs when user prompts alter the LLM's behavior or output in unintended ways." Ranked LLM01:2025 (top risk) — the course's "#1 for exactly this reason" is accurate. |
| A model-emitted tool call is untrusted input; validate arguments (shape *and* values) before execution (`m02-l03-tool-calling-workflows`) | anthropic-tool-use (T1); owasp-llm01-prompt-injection (T1) | **QUALIFIED** | Anthropic docs confirm the mechanism (tool_use block → JSON arguments → your code executes). "Treat tool args as untrusted / bound the values (top_k ≤ 20)" is correct security practice; the course itself flags that boundary-validation is an OWASP rule, *not* a NIST MEASURE prescription — accurate. Mechanism VERIFIED, the value-bounding discipline is emerging practice. |

## Module 3 — Production Pressure (Agents, Observability, Regression/Drift, Security/Privacy)

| Claim (lesson) | Source | Verdict | Excerpt / Note |
|---|---|---|---|
| Tool use lets a model emit a structured request that your code executes and returns; structure belongs in the schema, not regex over prose (`m03-l00-agent-workflow-control`) | anthropic-tool-use (T1) | **VERIFIED** | "if you're writing a regex to extract a decision from model output, that decision should have been a tool call." Confirms the agent-workflow contract the course teaches. |
| Observability = per-request trace with tokens, cost, per-stage latency, gated on p95 SLO + cost budget (`m03-l01-observability-cost-latency`) | — | **QUALIFIED** | Consensus LLMOps practice (mirrors general SLO/observability discipline) but no AI-specific standard codifies the trace schema or gate. True and valuable; not pinnable to one authority. |
| Regression/drift: freeze an eval set and fail CI when a model/prompt change regresses it (`m03-l02-regression-testing-drift`) | nist-ai-rmf-measure-manage (T1) | **QUALIFIED** | NIST MEASURE says "AI systems should be tested before deployment and regularly while in operation" (supports regression testing in spirit). The concrete CI-gate-on-eval mechanism is emerging LLMOps practice, not a codified standard. |
| Untrusted retrieved text becoming model instructions is the injection boundary; enforce with least-privilege tools + PII/secret redaction outside the model (`m03-l03-security-privacy-ai`) | owasp-llm01-prompt-injection (T1); owasp-llm05-improper-output-handling (T1) | **VERIFIED** | Maps cleanly to LLM01 (injection via shared channel, incl. indirect injection through retrieved content) and LLM05 (validate/sanitize output before downstream). Course honestly notes "least-privilege" and "gate release" are not NIST RMF terms — accurate. |

## Module 4 — Ship (Human Feedback, LLMOps Release, Interview, Capstone)

| Claim (lesson) | Source | Verdict | Excerpt / Note |
|---|---|---|---|
| A release gate blocks deploy on eval/cost/latency regression; measurement informs the ship decision (`m04-l01-llmops-release-process`) | nist-ai-rmf-measure-manage (T1) | **QUALIFIED** | NIST MEASURE→MANAGE supports "measure then decide" in spirit (VERIFIED linkage above). The specific release-gate mechanism is emerging LLMOps practice, not a codified standard — QUALIFIED as applied practice built on a real NIST function. |

---

## Defects found

**None.** No CORRECTED verdicts. No claim in the checked set misdefines a metric, misattributes a framework, or misstates a provider mechanism.

The course is a positive outlier for citation honesty: every NIST/OWASP reference audited was either accurate or already carried an in-line disclaimer distinguishing the real standard from the course's applied inference (e.g. "this is a paraphrase, not a NIST quote"; "neither the OWASP API Top 10 nor the NIST GenAI Profile explicitly prescribes 'treat model output like a request body'"; "'least-privilege' and 'gate release' are not NIST RMF terms"). Those hedges were verified against the primary sources and found correct.

## Verification statement

All 13 sources were fetched via WebFetch (NIST PDFs were retrieved and their text extracted locally for verbatim quotation). Every excerpt above is copied from the fetched page/PDF, not reconstructed from memory. Tier labels are honest: OWASP and NIST publications and official OpenAI/Anthropic docs are Tier 1; Lewis et al. 2020 (RAG) and Es et al. 2023 (RAGAS) are Tier 2 preprints/papers cited by their real arXiv IDs. No source, URL, quote, or excerpt was fabricated.
