# Proof Inventory — AI / QA Automation Portfolio

Rule: every claim is backed by a real artifact, labeled T1 (live repo/CI/public), T2 (local artifact on disk, screenshot-able), or T3 (written/planned only).
Verified on disk 2026-07-11. Screenshot candidates map to the site's evidence slots.

---

## 1. Nexural QA OS — signed-evidence QA runner fleet (verifier of the 39-repo federation)

Turbo/pnpm monorepo: 23 packages, 86 runner dirs (85 registered), 329 test files, autonomous-SDET CLI (`qa auto`, `qa gen-tests`, `qa clean`, `qa verify`).

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Built an 85-runner QA fleet with Ed25519-signed, redacted evidence bundles and a `qa verify` trust command | `evidence/qa-run-*/` (10 run dirs: `qa-run.json`, `evidence-manifest.json`, `spans.jsonl`, semgrep/gitleaks reports); `packages/cli/src/runner-registry.ts` | T1 (repo `JasonTeixeira/nexural-qa-os`, pushed) | terminal: `qa verify` passing on a signed run dir |
| Drove real measured coverage 52% → 91% lines / 3,698 tests, with enforced ratcheting thresholds | vitest config thresholds + coverage output; memory-recorded progression | T2 | coverage summary table from `pnpm test:coverage` |
| Found + fixed 11 false-pass "honesty bugs" (runners returning green doing zero work) and converted fake fallbacks to honest-skips | runner conformance suite `runner-conformance.test.ts`; git log on `feat/sdet-autonomy` | T1 | git log excerpt of honesty-fix commits |
| Full-fleet E2E CI gate: every wired runner exercised against fixtures, hard-fail on hang/crash | `scripts/e2e-fleet.mjs`; `.github/workflows/qa.yml`, `proof-gate.yml`, `slsa-provenance.yml`, `sign-artifacts.yml` | T1 | GitHub Actions green run of qa.yml |
| Built autonomous SDET loop: dead-code detection, test *generation* (unit/property/BDD) that compiles and passes, Definition-of-Done oracle, `--watch` daemon | `qa auto` / `qa gen-tests` in `packages/cli`; `packages/test-gen/` | T2 | `qa auto` converging to "complete and verified" exit 0 |

DIAGRAM: (1) evidence chain — runner → DAG orchestrator → signed evidence → `qa verify` → CI gate; (2) the honesty ladder — fake-green → honest-skip → real-run → signed proof.

## 2. Voza — 256-screen native app, 4-tier machine-computed verification

Expo/RN speaking-first English app for LATAM. Repo: `~/code/active/Voza-e2e-stabilization/mobile` (the canonical tree; `~/code/active/Voza` holds no proof artifacts).

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Stabilized a 256-screen native app to 256/256 Tier-1-3 verification via a machine-computed proof report (not hand-edited) | `mobile/PROOF_REPORT.md` (regenerated by `npm run verify`); `src/verification/screenRegistry.ts` | T2 | PROOF_REPORT scorecard section |
| Auto-generated 257 Maestro device flows from the screen registry; 256/256 device-cert passed on iOS simulator (2026-06-14; 254 first-pass + 2 transient retries clean) | `mobile/e2e/generated/` (257 flows on disk); cert run happens on operator device — local report honestly shows 0/256 until re-run | T2 (flows) / T3 (cert result recorded, re-runnable) | e2e/generated directory listing + a flow yaml |
| Wired 36/36 native service contracts to the production backend (voza.talk, 176 API routes) with a data-path E2E smoke that calls every contract | `src/verification/engineSmoke.test.ts`; live mappers validated against captured prod responses (`src/api/__fixtures__/live/`) | T1 (backend live) / T2 (smoke) | engineSmoke green run |
| Built live signed-in device journeys (Maestro) walking real authenticated data | `mobile/e2e/live/signin-journey.yaml`, `authed-journey.yaml` | T2 | authed-journey pass output |
| Diagnosed + shipped prod backend fixes (2× 500s) verified live on Vercel within one session | memory-recorded commit `51495a7`, routes now 200 | T1 (prod endpoints) | curl of the two endpoints returning success:true |

DIAGRAM: (1) 4-tier verification pyramid — Static → Logic → Engine contracts → Device cert; (2) registry-driven pipeline: screenRegistry → build gate → auto-generated Maestro flow → device cert.

## 3. sage-kernel — proof-first MCP-native SDLC operating system

Terminal-only stdio MCP server; doctrine "nothing stated, everything proven."

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Built a 140-tool MCP SDLC server with an append-only, HMAC-anchored proof ledger and proof graph | `apps/mcp-server/tools.json` (140 tools verified), `.sage-kernel/proof/ledger.jsonl` + `ledger.anchor.json` + `graph-latest.json` | T2 | `npm run cockpit` terminal panel / tools.json count |
| 68/68 release gates green, enforced coverage + complexity gates wired into `release:check` so quality can't silently rot | `scripts/release-check.mjs`; 123 test files in `tests/` | T2 | release:check 68/68 output |
| Wrote AST engines (SAST + taint tracking, mutation testing, dead-code, module graph) that found a real HIGH (`new Function()`) in a sibling repo | `packages/security/sast.mjs`, `taint.mjs`; `packages/testing/module-graph.mjs` | T2 | SAST findings table on a real repo |
| 8.76x incremental-SAST speedup via content-hash caching, gated so the gain can't regress | `packages/perf/` (cache, incremental-sast, budget) | T2 | perf:incremental gate output |
| Chaos/fault-injection matrix: 8 scenarios (lease contention, corrupt-lock recovery, tamper-detected ledger, fail-closed DAG) | `packages/orchestration/chaos.mjs`, `dag.mjs`; `tests/` chaos + property suites | T2 | chaos:matrix pass summary |
| Live autonomy proven: a real Claude agent, routed by the kernel, fixed a bug and turned a test green | `providers/claude-*.mjs` brain adapters; `docs/BRAIN_ACTIVATION.md` | T2 | agent-run transcript excerpt |

DIAGRAM: (1) proof ledger flow — claim → command evidence → signed ledger entry → proof graph → claim firewall; (2) engineering loop: plan → run → prove → score → learn.

## 4. Course Auditor Harness — content claim/source integrity auditor

Python CLI that audits course content into claim/source/assessment/lab ledgers with a scored release gate (≥95 approved / ≥85 pilot / <70 blocked).

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Built a tested Python audit harness (14 pytest suites, 17-phase self-improvement loop completed) | `~/course-auditor-harness/` (`course_auditor/`, `tests/`, `audit-output/`, `AUTONOMOUS_LOOP.md`) | T2 | pytest green run |
| Ran a 23-course / 460+ lesson baseline audit, extracting 17k+ claims and producing a sourcing-priority ranking | `sageideas.dev/docs/academy/AUDIT_BASELINE.md`; Supabase→markdown exporter `scripts/academy/audit-export.mjs` | T2 | AUDIT_BASELINE ranking table |
| Bridged a live Supabase CMS into the auditor's ingest format (block-JSON → per-lesson markdown with interaction markers) | `scripts/academy/audit-export.mjs` | T2 | exporter run + generated lesson md |

DIAGRAM: audit pipeline — Supabase course → exporter → claim extraction → source ledger → scored verdict gate.

## 5. Academy Evidence & Publishing QA (nexural-trading-academy + sageideas.dev)

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Built per-course third-party evidence ledgers across 24 fact-critical courses (claim → Tier-1 source → VERIFIED/CORRECTED/QUALIFIED) | 24× `EVIDENCE_LEDGER.md` + 24× `sources.json` verified in `~/nexural-trading-academy/courses/` | T2 | one ledger's verdict table |
| Caught real factual defects via verification (reversed dealer-gamma wings, T+2→T+1 settlement, mislabeled NQ tick value) and fixed them in-lesson | ledger CORRECTED rows | T2 | CORRECTED rows close-up |
| Built a mechanical publish gate that executes every lab (160/160 ran + byte-diffed) — then the *content* audit blocked publication of an 8-course AI-generated batch as a procedurally-generated shell | `sageideas.dev/scripts/academy/authoring/audit-courses.ts`; `~/AI_CAREER_OPERATING_SYSTEM/HANDOFF_8_COURSES/PREMIUM_BUILD/` (loop_reports/) | T2 | audit-courses 8/8 mechanical PASS + the DO-NOT-PUBLISH verdict |
| Built a voice narration pipeline (Supabase → TTS batch → quality gates → CDN MP3) proven e2e, and a voice-sync-ready narrated diagram engine over 449 diagrams | `nexural-website/scripts/academy/narrate-batch.ts` + `components/academy/narration/LessonNarration.tsx`; `sageideas.dev/components/academy/visuals/` (`NarratedDiagram.tsx`, `SageDiagram.tsx`, `arch-icons.tsx`) | T2 | narrated diagram mid-beat with caption track |

DIAGRAM: two-gate publishing — mechanical gate (schema/labs/structure) vs content-truth gate (claims/sources) — and why passing one ≠ shippable.

## 6. GIGGL — mobile release engineering + headless iOS delivery

AI comedy app (Expo monorepo, live backend at api.joingiggl.app).

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Ran fully headless EAS iOS production builds + TestFlight submission via ASC API key (no Apple 2FA), documented as a reusable playbook | `apps/mobile/eas.json` (ASC submit wiring); playbook in memory/`reference_eas_ios_headless` | T2 | EAS build page for build #8 |
| Exposed a false-green CI: missing `packageManager` silently skipped turbo gates; fix made ~1,374 tests actually run | `docs/LAUNCH_PROGRAM.md`; `.github/workflows/phase1-verify.yml`, `sage-gate.yml` | T1 (repo `JasonTeixeira/giggl`) | before/after gate output |
| Wired 8 social surfaces to a live backend with prod migrations (0048-0051) and a professional audit that found + fixed 2 CRITICAL / ~18 HIGH issues | `docs/PHASE3_SOCIAL_WIRING.md`; `supabase/migrations/`; 80 test files | T1 (backend live) / T2 | PHASE3 doc + migration list |

DIAGRAM: headless release lane — repo → EAS cloud build (ASC API key) → Brotli log forensics → TestFlight, with the 2FA-free boundary marked.

## 7. Nexural Website — headless dashboard audit loop, market data infra, research lake

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Drove a ~55-route production dashboard from avg 92 → 99.6 (0 console errors, 0 network failures, axe-clean) via a 3-iteration headless Playwright audit→fix→re-audit loop with real minted JWT sessions | loop results recorded; harness scripts were deliberately throwaway (rebuildable from spec) — live site is the standing proof | T1 (live prod) / T3 (harness) | authed dashboard route + axe 0-violations output |
| Built a survivorship-bias-free DuckDB research lake (19.8M bars, 23,971 tickers incl. delisted) and honestly falsified my own strategy overlays; data-completeness check caught a ~34% inflated result | `scripts/lake/BACKTEST_RESULTS.md`, `backtest-*.sql`, `check-completeness.sh`, `seed-postgres-prices.sh` | T2 | BACKTEST_RESULTS findings section |
| Ops-hardened a 24/7 market WebSocket worker: graceful-shutdown socket reclaim, heartbeat, backoff under a hard 2-connection provider ceiling; holiday/session-aware serving | `services/market-stream/`; `lib/market/session.ts` | T2 (Railway deploy is live but private) | worker logs across a redeploy with no connection leak |
| Enforced a no-fake-data ratchet on cockpit screens (CI script bans `Math.random`/seeded mocks on the allowlist) | `scripts/check-cockpit-no-fake-data.sh`; `.github/workflows/ci.yml`, `academy-polish-gate.yml` | T1 | ratchet script failing on an injected violation |

DIAGRAM: (1) headless audit SDLC loop — mint session → drive routes → score (console/network/axe/content) → triage → fix → re-audit; (2) ingest-once-serve-many: flat files → DuckDB lake → Postgres serving layer → cockpit.

## 8. Priority OS — lead + ops platform for a real client (Priority Property Maintenance)

| Claim | Artifact | Tier | Screenshot |
|---|---|---|---|
| Shipped a 20-page dual-audience site with a working lead-capture loop: estimate wizard / concierge → lead store → admin cockpit with LIVE badge + SLA | `web/` (20 pages), `web/admin/leads.html`, deployed at web-sage-ideas.vercel.app | T1 (public deploy) | lead appearing in admin cockpit after completing the wizard |
| Built a B2B proposal→contract wizard computing annual contract value + MSA preview, feeding both leads inbox and B2B pipeline (verified e2e) | `web/commercial.html`, `web/commercial-flow.js`, `web/admin/b2b.html` | T1 | contract wizard final step + pipeline entry |
| Authored a 23-doc operating blueprint (architecture, pricing, GTM, SEO system, risk register) for the AI layer (photo-estimate, voice receptionist, membership) | `ARCHITECTURE.md` + `docs/` (23 files) | T2 (docs) / T3 (AI features planned, not built) | ARCHITECTURE.md section map |

DIAGRAM: buy-the-commodity/build-the-moat split — HouseCall Pro (FSM) vs the differentiated Next.js/Supabase layer.

---

## Skills matrix — which claims prove what

| Skill | Strongest proof |
|---|---|
| AI evals / LLM QA | QA OS AI safety/eval runners + honest-skip fixes (§1); course content-truth gate blocking an AI-generated shell (§5); kernel rubric/verifier brain adapters (§3) |
| Browser QA | Headless Playwright audit loop to 99.6 with axe + minted JWT sessions (§7); cross-browser divergence runner with live-proven bug fixes (§1) |
| Mobile E2E | 257 auto-generated Maestro flows + 4-tier proof report (§2); live authed device journeys (§2); GIGGL device wiring audits (§6) |
| CI / release gates | proof-gate + SLSA provenance + signed artifacts workflows (§1); `release:check` 68/68 with anti-rot wiring (§3); false-green turbo gate exposure (§6); no-fake-data ratchet (§7) |
| Ops automation | market-stream shutdown/heartbeat/backoff hardening + session calendar (§7); `qa auto --watch` daemon (§1); headless EAS/TestFlight lane (§6) |
| MCP / agent engineering | 140-tool MCP server + proof ledger + live agent-fixes-bug proof (§3); QA OS MCP tools reading real evidence (§1) |
| Data pipelines | DuckDB lake + completeness-gated backtests + lake→Postgres seeder (§7); Supabase→auditor export bridge (§4); narration batch pipeline (§5) |

## Verification gaps (memory claims artifacts I could NOT find on disk)

- **Voza Tier-4 cert result:** the 256/256 device-cert pass is memory-recorded; the on-disk `PROOF_REPORT.md` honestly shows 0/256 cert-passed (cert runs on the operator's machine and must be re-run to screenshot). Flows themselves (257) verified on disk.
- **nexural-website dashboard-audit harness:** scripts were deliberately not committed (throwaway by design); only the results and rebuild spec survive. Re-run before claiming on the site.
- **qa-os `CLEANUP_PLAN.md`:** deleted as planned after execution — the audit's findings survive only in memory + git history.
- **sage-kernel blueprint docs:** `ULTIMATE_MCP_ENGINEERING_OS_BLUEPRINT.txt` / `WORLD_CLASS_90_99_PROGRAM.txt` not at recorded paths (docs/ has ARCHITECTURE.md, 100_SCORE_IMPLEMENTATION_PROGRAM.md instead); non-blocking.
- **`/Users/Sage/code/active/Voza`** (non-stabilization tree) contains none of the proof artifacts — always cite `Voza-e2e-stabilization`.
