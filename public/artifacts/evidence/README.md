# Evidence Gallery

This folder contains **portfolio evidence artifacts** (screenshots and exports) that demonstrate real QA automation outputs.

Guidelines:
- Keep each file named with a stable ID, e.g. `playwright-report.png`.
- Prefer PNG for screenshots.
- Each evidence item should have a corresponding entry in `lib/evidenceData.ts`.

