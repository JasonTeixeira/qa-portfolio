import { spawnSync } from 'node:child_process'
import { readFileSync, writeFileSync } from 'node:fs'

const slug = 'career-ux_ui_product_design_for_engineers'
const lessonPath = `data/academy/authoring/${slug}.lessons.json`
const solutionPath = `data/academy/authoring/${slug}.lab_solutions.json`
const readJson = (path) => JSON.parse(readFileSync(path, 'utf8'))
const writeJson = (path, value) => writeFileSync(path, `${JSON.stringify(value, null, 2)}\n`)
const lessons = readJson(lessonPath); const solutions = readJson(solutionPath)
const deep = new Set(['workflow-structure', 'forms-and-validation', 'empty-loading-error-states', 'design-tokens', 'component-states', 'keyboard-and-screen-reader-flows', 'usability-test-plan', 'prototype-to-code-handoff', 'instrumentation-plan'])
if (Object.keys(lessons).length !== 20 || JSON.stringify(Object.keys(lessons)) !== JSON.stringify(Object.keys(solutions))) throw new Error('UX/UI lesson/reference drift')
const required = (blocks, type, key) => { const block = blocks.find((candidate) => candidate.type === type); if (!block) throw new Error(`${key}: missing ${type}`); return block }
const remove = (blocks, type) => { const index = blocks.findIndex((candidate) => candidate.type === type); return index < 0 ? null : blocks.splice(index, 1)[0] }
const before = (blocks, type, block) => blocks.splice(blocks.findIndex((candidate) => candidate.type === type), 0, block)
const after = (blocks, type, block) => blocks.splice(blocks.findIndex((candidate) => candidate.type === type) + 1, 0, block)
const exactOutput = (solution, key) => { if (solution.language !== 'python') throw new Error(`${key}: expected Python reference`); const result = spawnSync('python3', ['-I', '-c', solution.code], { encoding: 'utf8', input: solution.stdin ?? '', timeout: 10_000 }); if (result.status !== 0 || result.stderr) throw new Error(`${key}: ${result.stderr}`); return result.stdout.trimEnd() }

for (const [key, blocks] of Object.entries(lessons)) {
  const contract = required(blocks, 'sprint-contract', key); const walkthrough = required(blocks, 'code-walkthrough', key); const comparison = required(blocks, 'compare', key); const verification = required(blocks, 'verification', key); const transfer = required(blocks, 'transfer', key); const solution = solutions[key]
  for (const type of ['worked-example', 'lab', 'debug', 'tradeoff', 'calibration', 'unlock-gate']) remove(blocks, type)
  const starter = [`# ${walkthrough.title}`, `# Mission: ${contract.outcome}`, '# Novice workflow:', '# 1. Name the user, trigger, goal, context, and evidence before proposing a surface.', '# 2. Enumerate loading, empty, error, success, denied, destructive, responsive, keyboard, and screen-reader states.', '# 3. Implement the TODO as a deterministic critique or design-decision evaluator.', '# 4. Run every case, add one counterexample, and retain it as a regression.', '# 5. State which prototype, browser, assistive-technology, usability, analytics, and expert evidence is still required.', '# Evidence checklist:', ...verification.items.map((item, index) => `# ${index + 1}. ${item}`), '# Local execution is practice feedback only; it cannot create mastery evidence.', '', 'def evaluate_design_evidence(case):', '    # TODO: return the defensible design decision and missing-evidence flags.', '    raise NotImplementedError("complete the design evidence decision")', '', '# Keep the evaluator deterministic and explain how real human and visual evidence will challenge it.', '# A passing local output never substitutes for user research, accessibility testing, or design review.'].join('\n')
  before(blocks, 'concept', { type: 'worked-example', title: `${walkthrough.title}: problem-to-evidence walk-through`, intro: `Trace one usable and one misleading ${key.replaceAll('-', ' ')} decision before editing the TODO.`, setup: contract.outcome, code: walkthrough.code, language: walkthrough.language, steps: ['Predict the critique result before executing.', ...(walkthrough.steps ?? []).map((step) => `${step.label}: ${step.note ?? `inspect lines ${(step.lines ?? []).join(', ')}`}`), 'Change one user, state, hierarchy, accessibility, or evidence constraint and identify the required repair.'], result: contract.proof, commonMistake: `${comparison.left.label}: ${comparison.left.verdict}` })
  after(blocks, 'concept', { type: 'lab', title: `${walkthrough.title}: design evidence lab`, language: 'python', starter, check: exactOutput(solution, key), summary: `Implement the deterministic design critique or decision for this lesson and prove every supplied state. ${contract.outcome} Match the exact observable contract, retain a failure regression, and name the real prototype, accessibility, usability, visual, analytics, or expert evidence still required. Local output remains practice-only evidence.` })
  after(blocks, 'lab', { type: 'debug', language: 'python', brokenCode: starter, symptom: comparison.left.verdict, task: `Reproduce ${comparison.left.label}, repair the design decision, and retain the minimized user-flow, state, hierarchy, accessibility, measurement, or handoff case as a regression check.`, fix: `${comparison.right.lines.join(' · ')}. ${comparison.right.verdict}` })
  after(blocks, 'debug', { type: 'tradeoff', question: comparison.title, optionA: { label: comparison.left.label, text: `${comparison.left.lines.join(' · ')} Outcome: ${comparison.left.verdict}` }, optionB: { label: comparison.right.label, text: `${comparison.right.lines.join(' · ')} Outcome: ${comparison.right.verdict}` }, guidance: `${comparison.caption} Defend comprehension, task success, accessibility, consistency, responsiveness, measurement, engineering cost, and reversibility with evidence.` })
  before(blocks, 'transfer', { type: 'calibration', artifact: contract.proof, weak: 'A polished happy path exists, but user evidence, complete states, accessibility, measurement, failure handling, and handoff proof are absent.', passing: `The exact Python reference passes and every verification item is evidenced: ${verification.items.join(' · ')}`, excellent: `Passing evidence plus a counterexample, retained regression, prototype/usability/accessibility proof plan, reviewer objection, and transfer: ${transfer.text}`, note: 'Local-practice calibration only; prototype testing, assistive technology, user research, and expert review remain required.' })
  after(blocks, 'spaced-review', { type: 'unlock-gate', criteria: ['Build evidence — match the exact observable design-decision contract.', 'Debug evidence — retain a minimized unusable, inaccessible, or misleading state as a regression.', `Decision evidence — defend ${comparison.title}.`, `Verification evidence — ${verification.items.join(' · ')}`, `Transfer evidence — ${transfer.text}`], practiceOnlyNotice: 'This deterministic local lab is practice feedback only, not controlled mastery evidence or certification.' })
  contract.intensity = key === 'design-review-packet' ? 'capstone' : deep.has(key) ? 'deep' : 'standard'; contract.time = contract.intensity === 'capstone' ? 'Multi-day' : contract.intensity === 'deep' ? '2–4 hrs' : '60–90 min'
}
writeJson(lessonPath, lessons); writeJson(solutionPath, solutions)
console.log('Upgraded 20 UX/UI Product Design lessons with exact Python critique contracts and human-evidence boundaries.')
